/**
 * Base interface for navigator search result data
 *
 * Based on AS3 com.sulake.habbo.communication.messages.incoming.navigator.class_1662
 */
export interface INavigatorSearchResultData
{
	readonly disposed: boolean;

	dispose(): void;
}
