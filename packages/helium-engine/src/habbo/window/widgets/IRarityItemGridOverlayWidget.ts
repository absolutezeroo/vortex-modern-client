import type {IRarityItemOverlayWidget} from './IRarityItemOverlayWidget';

/**
 * Interface for the rarity item grid overlay widget.
 *
 * Used in grid item displays to show rarity level information.
 * Extends the base rarity overlay without additional members.
 *
 * @see sources/win63_version/habbo/window/widgets/IRarityItemGridOverlayWidget.as
 */
export interface IRarityItemGridOverlayWidget extends IRarityItemOverlayWidget
{
}

