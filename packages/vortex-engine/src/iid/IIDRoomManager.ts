import {createIID} from '@core/runtime/IID';
import type {IRoomManager} from '@room/IRoomManager';

/**
 * IID for Room Manager
 *
 * Based on AS3: com.sulake.iid.IIDRoomManager
 */
export const IID_RoomManager = createIID<IRoomManager>('IRoomManager');
