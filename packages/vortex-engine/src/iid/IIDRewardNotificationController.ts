import {createIID} from '@core/runtime/IID';

/**
 * IID for the wired reward-notification controller.
 *
 * Based on AS3: com.sulake.iid.IIDRewardNotificationController
 */
export const IID_RewardNotificationController = createIID<unknown>('IRewardNotification');
