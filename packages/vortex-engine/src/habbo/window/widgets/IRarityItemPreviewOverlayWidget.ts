import type {IRarityItemOverlayWidget} from './IRarityItemOverlayWidget';

/**
 * Interface for the rarity item preview overlay widget.
 *
 * Used in preview/catalog views to show rarity level information.
 * Extends the base rarity overlay without additional members.
 *
 * @see sources/win63_version/habbo/window/widgets/IRarityItemPreviewOverlayWidget.as
 */
export interface IRarityItemPreviewOverlayWidget extends IRarityItemOverlayWidget
{
}
