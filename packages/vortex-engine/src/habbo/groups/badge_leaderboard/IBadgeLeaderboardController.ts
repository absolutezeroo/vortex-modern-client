/**
 * The marker interface `BadgeLeaderboardController` announces itself under.
 *
 * @see sources/WIN63-202607011411-782849652/src/com/sulake/habbo/groups/badge_leaderboard/_SafeCls_2123.as
 *
 * **Name DERIVED, and the body is genuinely empty** — AS3 declares `public interface _SafeCls_2123`
 * with no members at all, and nothing in any tree resolves the obfuscated name. It exists only so
 * the controller can be referred to abstractly; kept because the declaration does.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface IBadgeLeaderboardController
{
}
