/**
 * FurnitureCoinsChestLogic
 *
 * The coins chest adds nothing to the chest base — AS3's class body is an empty constructor.
 * It exists so the factory can map `furniture_coinschest` to a type of its own.
 *
 * Name DERIVED: `_SafeCls_2227` in the primary tree, absent from the 2016 one. Named for the
 * logic type the factory maps to it.
 *
 * @see sources/WIN63-202607011411-782849652/src/com/sulake/habbo/room/object/logic/furniture/_SafeCls_2227.as
 */
import {FurnitureChestLogic} from './FurnitureChestLogic';

export class FurnitureCoinsChestLogic extends FurnitureChestLogic
{
}
