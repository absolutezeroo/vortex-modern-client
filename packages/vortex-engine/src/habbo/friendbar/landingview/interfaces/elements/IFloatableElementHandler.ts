/**
 * Optional capability for an `IElementHandler` that can be laid out floating
 * outside the widget's scrollable item list (e.g. a background image).
 *
 * AS3 identifier recovered from sources/win63_version/habbo/friendbar/landingview/interfaces/elements/class_4118.as
 * (obfuscated as `_SafeCls_4505` in the primary source).
 *
 * @see sources/win63_2026_crypted_version/src/com/sulake/habbo/friendbar/landingview/interfaces/elements/_SafeCls_4505.as
 */
export interface IFloatableElementHandler
{
    // AS3: sources/win63_2026_crypted_version/src/com/sulake/habbo/friendbar/landingview/interfaces/elements/_SafeCls_4505.as::isFloating()
    isFloating(value: boolean): boolean;
}
