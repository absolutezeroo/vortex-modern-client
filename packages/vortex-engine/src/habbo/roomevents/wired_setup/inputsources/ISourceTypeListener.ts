/**
 * ISourceTypeListener — receives the selected source type from a source-type selector.
 *
 * Name derived: the AS3 interface is obfuscated as `_SafeCls_2977` with no readable counterpart in
 * any tree; the name follows its single member `set sourceType`.
 *
 * AS3: sources/WIN63-202607011411-782849652/src/com/sulake/habbo/roomevents/wired_setup/inputsources/_SafeCls_2977.as
 */
export interface ISourceTypeListener
{
    // AS3: _SafeCls_2977.as::set sourceType()
    set sourceType(value: number);
}
