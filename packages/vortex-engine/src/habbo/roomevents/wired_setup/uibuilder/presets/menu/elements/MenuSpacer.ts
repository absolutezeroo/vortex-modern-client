import type {IMenuElement} from './IMenuElement';

/**
 * MenuSpacer — a quick-menu separator element (a horizontal divider between groups of items).
 *
 * Name derived: the AS3 class is obfuscated as `_SafeCls_4499` with no readable counterpart; the name
 * follows its role (MenuPreset.SPACER).
 *
 * AS3: sources/WIN63-202607011411-782849652/src/com/sulake/habbo/roomevents/wired_setup/uibuilder/presets/menu/elements/_SafeCls_4499.as
 */
export class MenuSpacer implements IMenuElement
{
}
