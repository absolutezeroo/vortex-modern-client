/**
 * IMenuElement — marker interface for a quick-menu element (a MenuItem or a MenuSpacer).
 *
 * Name derived: the AS3 interface is obfuscated as `_SafeCls_4019` (an empty marker) with no readable
 * counterpart; the name follows its two implementers.
 *
 * AS3: sources/WIN63-202607011411-782849652/src/com/sulake/habbo/roomevents/wired_setup/uibuilder/presets/menu/elements/_SafeCls_4019.as
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type -- faithful empty AS3 marker interface
export interface IMenuElement
{
}
