/**
 * VariableManagementConfig — shared constants for the variable-management sub-controller and the
 * overview tab's "manage" flow. PAGE_SIZE is the page length requested for the variable-holder table.
 *
 * AS3: sources/WIN63-202607011411-782849652/src/com/sulake/habbo/roomevents/wired_menu/variables_management/overview/VariableManagementConfig.as
 */
export class VariableManagementConfig
{
    // AS3: VariableManagementConfig.as::PAGE_SIZE
    static readonly PAGE_SIZE: number = 50;
}
