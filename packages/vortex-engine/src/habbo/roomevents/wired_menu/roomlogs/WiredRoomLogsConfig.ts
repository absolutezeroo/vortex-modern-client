/**
 * WiredRoomLogsConfig — static configuration for the wired room-logs list (currently just the
 * server-side page size).
 *
 * AS3: sources/WIN63-202607011411-782849652/src/com/sulake/habbo/roomevents/wired_menu/roomlogs/WiredRoomLogsConfig.as
 */
export class WiredRoomLogsConfig
{
    // AS3: WiredRoomLogsConfig.as::PAGE_SIZE
    static readonly PAGE_SIZE: number = 50;
}
