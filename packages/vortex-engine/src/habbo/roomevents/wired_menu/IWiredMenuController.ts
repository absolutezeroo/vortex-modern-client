/**
 * IWiredMenuController — empty marker interface implemented by WiredMenuController alongside
 * ILinkEventTracker and IDisposable. AS3 declares no members on it; it exists only as a nominal tag
 * the view/tab layer can reference without importing the concrete controller.
 *
 * Name derived: fully obfuscated in AS3 (interface `_SafeCls_1885`, empty); named for its role.
 *
 * AS3: sources/WIN63-202607011411-782849652/src/com/sulake/habbo/roomevents/wired_menu/_SafeCls_1885.as
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface IWiredMenuController
{
}
