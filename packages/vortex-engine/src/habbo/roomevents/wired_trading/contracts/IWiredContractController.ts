/**
 * IWiredContractController — empty nominal marker implemented by {@link WiredContractController}.
 * AS3 declares no members beyond the disposable base it extends.
 *
 * AS3: sources/WIN63-202607011411-782849652/src/com/sulake/habbo/roomevents/wired_trading/contracts/_SafeCls_2293.as
 * (name derived: obfuscated in every tree, named for the class that implements it)
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface IWiredContractController
{
}
