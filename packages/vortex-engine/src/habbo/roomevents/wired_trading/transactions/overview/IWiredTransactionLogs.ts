/**
 * Marker interface for the paged transaction-logs controller.
 *
 * **AS3 declares it with no members at all**, and nothing takes it as a parameter type — the only
 * reference is `WiredTransactionLogsController implements _SafeCls_1968`. Its sibling
 * `IWiredTransactionDetails` is the same shape, so the pair reads as a convention that was never
 * filled in rather than an abstraction. Kept so the class list stays a faithful port.
 *
 * AS3: sources/WIN63-202607011411-782849652/src/com/sulake/habbo/roomevents/wired_trading/transactions/overview/_SafeCls_1968.as
 * (name derived: obfuscated in every tree and empty, so named for its one implementor)
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type -- faithful: the AS3 interface declares no members
export interface IWiredTransactionLogs
{
}
