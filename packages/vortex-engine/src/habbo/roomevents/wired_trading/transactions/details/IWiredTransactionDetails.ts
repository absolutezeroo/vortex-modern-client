/**
 * Marker interface for the transaction-details controller.
 *
 * **AS3 declares it with no members at all** — see the note on `IWiredTransactionLogs`, which is the
 * same shape for the same reason.
 *
 * AS3: sources/WIN63-202607011411-782849652/src/com/sulake/habbo/roomevents/wired_trading/transactions/details/_SafeCls_2287.as
 * (name derived: obfuscated in every tree and empty, so named for its one implementor)
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type -- faithful: the AS3 interface declares no members
export interface IWiredTransactionDetails
{
}
