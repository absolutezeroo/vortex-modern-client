/**
 * The special-items display's DI component interface.
 *
 * Empty in AS3 too: `SpecialItemsController` implements it purely to be attachable under
 * `IIDSpecialItemsController`, and nothing ever calls through it.
 *
 * AS3: sources/WIN63-202607011411-782849652/src/com/sulake/habbo/catalog/special_items_display/ISpecialItemsDisplay.as
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface ISpecialItemsDisplay
{
}
