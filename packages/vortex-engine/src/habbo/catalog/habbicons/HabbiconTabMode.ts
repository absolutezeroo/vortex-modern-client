/**
 * The three tabs across the top of the habbicon hub.
 *
 * The two obfuscated constants keep their literal strings in the dump, so both names are recovered
 * from the values rather than derived.
 *
 * AS3: sources/WIN63-202607011411-782849652/src/com/sulake/habbo/catalog/habbicons/HabbiconTabMode.as
 */
export class HabbiconTabMode
{
    // AS3: HabbiconTabMode.as::ALL_SETS
    static readonly ALL_SETS: string = 'all_sets';

    // AS3: HabbiconTabMode.as::_SafeStr_10350 (name recovered from its value, "owned")
    static readonly OWNED: string = 'owned';

    // AS3: HabbiconTabMode.as::_SafeStr_10714 (name recovered from its value, "favourited")
    static readonly FAVOURITED: string = 'favourited';
}
