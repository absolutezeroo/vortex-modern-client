/**
 * Marker for a window that handles touch input itself.
 *
 * AS3 declares no members: the interface exists purely so the tablet event
 * processor can test a window for it. `ButtonController` and
 * `DropBaseController` are the two implementors.
 *
 * @see sources/WIN63-202607011411-782849652/src/com/sulake/core/window/utils/tablet/ITouchAwareWindow.as
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface ITouchAwareWindow
{
}
