{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "[ -f \"${CLAUDE_PROJECT_DIR:-.}/scripts/eslint-fix-hook.mjs\" ] && node \"${CLAUDE_PROJECT_DIR:-.}/scripts/eslint-fix-hook.mjs\" || exit 0",
            "statusMessage": "eslint --fix",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
