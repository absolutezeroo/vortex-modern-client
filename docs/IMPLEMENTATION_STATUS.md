# Vortex - Implementation Status

> **Last updated**: 2026-08-13 (full re-measure of every table; previous full pass 2026-07-28)
> **Method**: filesystem snapshot plus targeted AS3/TS directory counts. Module rows count the
> **primary** tree (`WIN63-…-782849652/src/com/sulake/`) recursively against the matching TS
> directory. Message rows are the one exception: the primary tree keeps almost every
> incoming/outgoing/parser class under `src/unknowns/`, so those are counted against
> `win63_version/habbo/communication/messages/`, which has them laid out per category. The two
> methodologies were mixed silently in earlier revisions of this file — that is why some AS3 counts
> moved in the 2026-07-28 pass without any AS3 tree changing (`habbo/ui` 375 → 369,
> `habbo/roomevents` 347 → 448, `habbo/quest` 41 → 64).
> **Important**: the file counts in this document are **not** a member-level AS3 parity
> certification. A TS count greater than an AS3 count only means files exist; completion still
> requires reading the AS3 source and auditing public API, lifecycle, parser/composer behavior, and
> dispose paths. Since 2026-08-20 there *is* a mechanical member-level measure — see "Member
> coverage" below — but it answers "is anything missing", never "is anything right".
>
> The 2026-07-17 cross-module parity audit put a number on that caveat: 188 divergences across every
> system except `communication/`, in code that already existed and typechecked clean. File counts
> say nothing about whether the code runs. See "Cross-module parity audit" below.

---

## Snapshot

| Metric                                                        | 2026-07-19    | 2026-07-28    | 2026-08-13    |
|---------------------------------------------------------------|---------------|---------------|---------------|
| Primary AS3 — `WIN63-…-782849652/src/com/sulake/`              | 3,345 `.as`   | 3,345 `.as`   | 3,345 `.as`   |
| Primary AS3 — `WIN63-…-782849652/src/unknowns/` (also client)  | 1,839 `.as`   | 1,839 `.as`   | 1,839 `.as`   |
| Secondary AS3 — `win63_version/`                               | 4,783 `.as`   | 4,783 `.as`   | 4,783 `.as`   |
| Tertiary AS3 — `PRODUCTION-…/src/com/sulake/`                  | 4,029 `.as`   | 4,029 `.as`   | 4,029 `.as`   |
| Engine TypeScript files                                        | 2,857 `.ts`   | 3,482 `.ts`   | **4,540** `.ts` |
| Client TypeScript files                                        | 22 `.ts`      | 21 `.ts`      | **64** `.ts`  |
| Shipped window layouts (`src/assets/window-layouts/`)          | 857 `.json`   | **783** `.xml`  | 783 `.xml`    |
| Shipped window skins (`src/assets/window-skins/`)              | 133 `.json`   | 133 `.xml`      | 133 `.xml`    |
| Vortex-authored layouts / skins (`src/vortex-layouts`, `src/vortex-skins`) | — | 4 / 0 | 3 / 0 |
| Engine `AS3:` trace comments                                   | 4,666         | 34,795        | **38,115**    |
| Engine `TODO(AS3)` markers                                     | 265           | 327           | **323**       |

Re-measure `TODO(AS3)` with `node scripts/todo-inventory.mjs`, not `grep -c`: the 2026-08-13 column
above is the script's figure (**323** `TODO(AS3)`, plus 57 other TODOs and 29 markers the script
flags as possibly-stale candidates — see the 2026-08-09 note under "TODO inventory"). A bare
`grep -rn --include=*.ts packages` reads `packages/*/dist/**/*.d.ts` too and inflates the count
by roughly 80.

**The count is not the goal, and it moved the wrong way on purpose.** The chat-command pass replaced
one marker reading *"~50 slash commands … don't exist in this port yet"* with six that each name a
single missing member — so 45 commands landed and the number went 304 → 309. A marker covering fifty
things is worth less than five covering five, and any reading of this file that treats the total as
a score will get that backwards.

### Boot ordering (2026-08-16) — the asset library was empty for every component in `prepareCore()`

Fifteen `Asset not found` / `no bitmap asset` / `not in the asset library` warnings at boot, and all
of them one cause: `App.ts` registered the images from `initClientUi()` — *after* `connect()` — and
the layouts right after `Vortex.bootstrap()` returned. Every component built inside `prepareCore()`
therefore read an empty library. AS3 cannot have this bug: `HabboWindowManagerComponent(context,
flags, assets)` is handed an `AssetLibrary` already filled from its `[Embed]`ed SWF resource.

The channel already existed and already documented this exact reasoning —
`IVortexConfig.windowAssets`, which is how skins/atlases/layouts were handed in. It now also carries
`imageUrls`, `libraryImages` and `libraryLayouts`, and `VortexMain` seeds the library in
`registerWindowAssetLibraryContent()`, called immediately after `new AssetLibrary(ctx)` and before
any `attachComponent`.

Three things the fix turned up that are worth keeping:

- **`registerAssetUrl()` was a silent no-op before `initComponent()`.** `HabboWindowManager`
  creates its `ResourceManager` in `initContexts()`, so the image URLs — which must go in *before*
  the manager is attached — landed on a null field and vanished. They now buffer into
  `_pendingAssetUrls`/`_pendingAssets` and replay when the ResourceManager is built.
- **`HabboUserDefinedRoomEvents` was constructed without an asset library.** AS3's third
  constructor argument is forwarded verbatim to `WiredMenuController`, `WiredChestController`,
  `WiredTransactionLogsController`, `WiredTransactionDetailsController` and
  `RewardNotificationController`. Dropped, `Component.assets` stayed null and
  `WiredChestWrapperView` could not find `chest_generic_xml` — the chest window was never built at
  all. Same omission previously fixed for HabboGroupsManager, HabboCatalog,
  HabboSoundManagerFlash10 and HabboFriendBar; this was the last one.
- **Fire-and-forget decoding is a race, not an optimisation.** The library bitmaps were registered
  from an unawaited `.then()`; `getAssetByName(...).content` is read synchronously, so
  `add_friends_icon` missed whenever the friend-list packet beat `createImageBitmap`. They are
  awaited now.

Two warnings in the same log were **not** bugs and must not be "fixed":

- `illumina_light_border_top_center` is declared by no `*Com.as` in any tree — the layouts reference
  it, the client never shipped it, and AS3's `IlluminaBorderWidget.getPiece()` skips a null piece.
- `No container found for widget: RWE_ME_MENU` — the port matches
  `DesktopLayoutManager.getWidgetContainer()` line for line, and the `memenu` layout carries no
  `room_widget*` tag, so AS3 returns false here too (silently; only the port logs it).

#### `RoomEngine` dropped a room asked for before the engine was ready — fixed

`Cannot create room — manager not initialized (state: 1)` was the one boot warning left, and it was
a whole unported mechanism. AS3's `initializeRoom()` opens with `if(!_SafeStr_5461)` and **parks**
the room in `_SafeStr_5320` — `RoomData`, name recovered from PRODUCTION, keyed by room identifier
and carrying floor/wall/landscape type and camera position — then `roomManagerInitialized()` replays
the lot once the manager reports ready. The port called `createRoomInstance()` immediately, got null
and dropped the room forever. It is what killed the `RoomPreviewer` inside the chest window, built
at DI time long before `RoomManager` finishes loading its placeholder object content.

Three pieces, all from the primary tree:

- `RoomData` (`habbo/room/utils/`), the parked entry. Obfuscated to `_SafeCls_1852` in the primary
  tree, which is where the *member* names are readable; PRODUCTION obfuscates those and predates
  `cameraInitPosition`, but gives the class name. The four door values are TS-only: AS3 re-reads
  the door out of the parked room XML, this port takes it as separate `initializeRoom()` arguments,
  so the queue has to hold them to replay the same call.
- `updateObjectRoom()`'s buffering branch, which parks a floor/wall/landscape push arriving before
  the room object exists. It carried a `TODO(AS3)` saying it could not be ported because
  `win63_version` decompiles it into `null.floorType = param2` — the primary tree has it intact.
  This is the third time that rule has paid (see "win63_version is a worse decompile").
- `RoomPreviewerWidget`'s `REE_INITIALIZED` subscription (AS3 l.68-71, `onRoomInitialized` l.201),
  which the port had not ported at all. Without it the deferred room is built and no canvas is ever
  attached to it.

Also folded in: `initializeRoom()` now feeds the parked floor/wall/landscape types to the three
`RoomObjectRoomUpdateMessage`s instead of the hardcoded `"111"/"201"/"1"`, which is what AS3's
`createRoom(param3, param4, param5)` does with them; and the `data == null` early return, which
becomes load-bearing once a `updateObjectRoom()`-only entry can be replayed with no plane parser.

Verified headlessly: before the room manager reports ready the preview room sits in `_roomDatas`
(`parked: ["room_2147418115"]`, `built: []`, no warning); after, `parked: []`, `built:
[2147418115]`, and `getRoomInstance(2147418115)` returns a real instance.

### The room-users packet was parsed against a 2016 build (2026-08-16)

`Parse error for 996: End of buffer` in `UsersMessageParser`, at a `readString()` inside the
per-user loop. The parser was transcribed from `PRODUCTION-201601012205-226667486`, and the 2026
client reads **one more field** at the end of every type-1 (player) avatar:

```as3
_loc16_.achievementScore = param1.readInteger();
_loc16_.isModerator      = param1.readBoolean();
_loc16_.badgesRank       = param1.readInteger();   // absent from the 2016 build
```

Both sides agree that field exists: the primary tree reads it
(`src/unknowns/_SafePkg_2184/_SafeCls_2309.as::parse()` — obfuscated, found by its `_users` field
and `IMessageParser`), and the emulator writes it
(`RoomAvatarSerializer.SerializePlayerAvatar`, `.WriteInteger(snapshot.BadgesRank)`). Not reading it
left four bytes per player in the buffer; the next iteration consumed them as `webId` and every
field after that was one slot out, until a length-prefixed string read a garbage length and ran off
the end. With one avatar it merely left junk; it corrupts from the second onward, which is why it
surfaced during 100-avatar testing.

`RoomUserData._badgesRank` already existed, carrying a comment asserting that "the initial
room-users list simply doesn't carry it" — read off the same 2016 build. Taking that note at face
value is what kept the field unwired.

Verified by bundling the real parser with esbuild and feeding it packets encoded exactly as
`RoomAvatarSerializer` writes them:

- without the read — `RangeError: End of buffer`, 0 users, **139 bytes** left unconsumed
- with it — 3 players parsed, **0 bytes** left, every field on the right value
- a mixed player + pet + bot packet also lands exactly, so the type-2 and type-4 branches match the
  emulator too

Fixed in the same pass, same root cause: `convertSwimFigure()` was also the 2016 version. It
hardcodes the swimsuit colour to `10001` and always emits a swim type, where the 2026 client derives
both from the wire value (`…=<name>/<r,g,b>`) against a 32-entry colour table — and leaves both at
`1` when the string carries no `=`. The file's five AS3 traces were repointed from PRODUCTION to the
primary tree.

### `|| 1` ate every infinite animation loop (2026-08-16)

Reported as "the dragons' flame isn't animated". `rare_dragonlamp` is
`furniture_multistate` / `furniture_animated`; its flame is **layer 5 of animation 1** — four
frames, `frameRepeat: 2`, `loopCount: 0`. In this format `loopCount: 0` means *loop forever*:
`AnimationLayerData` stops a layer only on `loopCount > 0 && loopIndex >= loopCount`.

`AnimationData.initialize()` read it as `(layerDef['loopCount'] as number) || 1`. JavaScript treats
`0` as falsy, so every infinite loop became a single pass. AS3 does not have the bug because it
tests for the attribute's *presence*, not its truthiness:

```as3
_loc7_ = 1;
_loc10_ = _loc5_.@loopCount;
if(_loc10_.length > 0) { _loc7_ = int(_loc10_); }   // "0" is kept as 0
```

Measured on the real `rare_dragonlamp.nitro`, walking the frame counter the way
`updateFramesForAnimation()` does:

- before — `[1,1,2,2,3,3,4,4,4,4,4,4,…]`: one pass, then held on the last frame forever
- after — `[1,1,2,2,3,3,4,4,1,1,2,2,…]`: loops

This was never dragon-specific: it silenced the loop on **every** furni whose animation declares
`loopCount="0"`, which is the normal way to write a continuously animated layer.

The same absent-vs-zero mistake was swept for in the visualization parsers and found once more:
`PlaneMaskManager` read `parseFloat(bmpData.normalMinX) || -1`, where AS3 guards with
`if(String(@normalMinX) != "")`. A normal coordinate of exactly `0` is a legitimate bound, and
collapsing it to `±1` widened the mask's range so it matched planes it should not have.

### Two AS3 type tests the port turned into unchecked casts (2026-08-16)

Same transcription habit behind both: AS3 leans on `as` and on implicit nullability, TypeScript's
equivalents *assert* rather than test, and the null arrives anyway.

- **`ProductGridItem.setDraggable()`** — `interactive.setMouseCursorForState is not a function`,
  which took down every catalog page carrying a `ClubGiftWidget`. AS3 writes
  `if(_view as IInteractiveWindow && param1)`, and its `as` yields **null** when the object does not
  implement the interface: that line is a runtime type test. `as unknown as IInteractiveWindow` is a
  compile-time assertion that never yields null, so every view reached there was taken for
  interactive — and the club-gift item hands over its plain `image_container`. `IInteractiveWindow`
  is erased at runtime and implemented by four unrelated controllers, so the check is for the member
  the method actually uses.
- **`ITableObject.getTableCell()`** — `Cannot read properties of null (reading 'type')` at
  `TableCellView.initializeView()`, thrown inside the window manager's update receiver. AS3 declares
  `:TableCell`, but object types are implicitly nullable there and every implementation returns null
  from its default branch. Transcribed non-nullable, each of the eleven had to launder that through
  `null as unknown as TableCell`, and `TableCellView` was left dereferencing a null it had been told
  could not exist. The signature is now `TableCell | null`. This is exactly what the `TODO(AS3)` on
  `TransactionPreviewTableObject` called the real fix, and why it deferred it.

AS3 survives the unguarded deref because every `getTableCell()` covers every column its table
declares — the default branch is unreachable there, and **statically unreachable in this port too**:
all eleven were checked against their tables' `TableColumn` ids and every one matches. So the trigger
is not a column mismatch, and it was not reproducible. A null cell therefore renders empty and logs
which column and which row object produced it; if that warning ever appears, the fix belongs in that
row object's `getTableCell()`, not in `TableCellView`.

### The transverse-debt pass (2026-08-14) — a stale asset, a four-module sweep, three wrong headers

Three items off the "Not yet done" list, and one find that was not on any list.

#### The avatar configurations were the 2016 build, and two of them were not shipped at all

`packages/vortex-client/src/assets/configurations/` is gitignored — derived dump content,
regenerated per checkout — but **nothing regenerated the avatar XMLs**. They had been copied in by
hand from `PRODUCTION-201601012205-226667486` and never refreshed. Measured against the primary
dump:

| Asset                  | Shipped        | Dump   | What was missing                              |
|------------------------|----------------|--------|-----------------------------------------------|
| `HabboAvatarAnimation` | 21,405 (2016)  | 56,253 | the `Default`, **`Lay`** and **`Sleep`** actions |
| `HabboAvatarGeometry`  | 15,996 (2016)  | 19,020 | `petl`/`petr` + all 8 `order-before` attributes |
| `HabboAvatarPartSets`  | 5,746 (2016)   | 7,380  | the `misc` and `pet` activePartSets           |
| `HabboAvatarFigure`    | 5,345          | 5,345  | — already current                             |
| `action_offset_lay`    | **absent**     | 278    | `App.ts` asks for it; the lookup returned null |
| `action_offset_swim`   | **absent**     | 486    | likewise                                       |

All three stale files are **strict subsets** of the dump's — nothing was removed between the
builds, only added — so the swap could not regress anything. `AvatarActionManager.ts:187` already
reads `'action_offset_' + state`: the code was ported and waiting on an asset nobody shipped, the
same shape as every other entry under "Ported but never wired".

The fix is `packages/vortex-client/tools/import-avatar-configurations.mjs`
(`pnpm --filter vortex-client import:avatar-configurations`), so it stays fixed across checkouts.
**Why there was no importer: `loadCryptedManifest()` only scans `*Com.as`, and these six embeds are
declared by `HabboAvatarRenderLib.as`** — a `SimpleApplication`, not a component manifest, so
`findComFiles()` never sees it. Any other embed declared by a `*Lib.as` is invisible to
`import-crypted-images.mjs` and `import-crypted-sounds.mjs` for the same reason; that is worth a
sweep of its own.

The tool also brought in a seventh file the port had no idea existed:
`dance_sixseven_animation`. AS3 registers it from
`AvatarRenderManager.registerBuiltInAnimations()`, called out of `onAvatarActionsLoaded()` right
after `updateActions()` — **that method was not ported at all**. It is now, along with its
`BUILT_IN_ANIMATION_ASSET_NAMES` constant, and the asset name is in both
`EMBEDDED_AVATAR_XML_ASSET_NAMES` lists.

#### The ported-but-never-wired sweep, four modules

Recipe as documented under priority 0, with one correction: **matching a handler name anywhere in
the module produces false negatives**, because a same-named method on another class of the module
satisfies the grep. `onActivityPoints` read as present in `habbo/quest` purely because
`QuestController.onActivityPoints()` exists — the registration was missing. The reliable form
resolves each AS3 `_SafeCls_N` through the registry to its **id**, then asks whether that id's
event class is constructed in the port's handler.

| Module               | AS3 registrations | Missing before | Missing after | What they were |
|----------------------|-------------------|----------------|---------------|----------------|
| `habbo/friendlist`   | 12                | 0              | 0             | clean — the candidate named in priority 0 has no gap |
| `habbo/quest`        | 24                | **8**          | **0**         | all eight events existed *and* were registered; nothing subscribed |
| `habbo/notifications`| 36                | 11             | 9             | 2 wired; the other 9 need the message ported first |
| `habbo/groups`       | 22                | 3             | 2             | 1 was a correct deviation, 1 a documented Phase-1 cut, 1 an unported message |

`habbo/quest` is the clean case and the largest: `ObjectAdd`/`ObjectRemove`/`ObjectRemoveMultiple`
and `RoomSettingsSaved` all drive `roomCompetitionController.onContextChanged()`;
`AchievementsScore` publishes the score as the `achievements.categories.score` localization
parameter; `ScrSendUserInfo` starts the room competition on VIP + responseType 2;
`IsFirstLoginOfDay` and the whole-wallet `ActivityPoints` (which zeroes every
`ActivityPointTypeEnum` before applying the wallet) close it out. Every target member —
`onContextChanged()`, `sendRoomCompetitionInit()`, `setIsFirstLoginOfDay()`, `registerParameter()`
— was already ported. It was eight `addMessageEvent` lines.

Two of the three `habbo/groups` findings **did not survive verification**, which is the rule
working as intended:

- `onUserObject` caches the own-user id in AS3; the port's `get avatarId()` reads
  `_sessionDataManager.userId`, fed by the same message. A correct deviation — now commented at the
  getter so the next sweep stops re-flagging it.
- `onRelationshipStatusInfo` needs `ExtendedProfileWindowCtrl.refreshRelationships()`, which the
  file's own header records as a deliberate Phase-1 scope cut, not an accident. The message layer
  (event, parser, `RelationshipStatusInfo`, `RelationshipStatusEnum`) is fully ported and ready.
- `onBadgeLeaderboardResult` is genuine: id **2503** is in no client registry.

#### The last three modules, swept 2026-08-14 — and the recipe mechanised

`habbo/toolbar`, `habbo/navigator` and `habbo/session` were the three the section above named as
not yet swept. They are done, and the sweep is now a script rather than a procedure, because
running it by hand is what let two findings hide.

| Module           | AS3 subscriptions | Missing before | Missing after |
|------------------|-------------------|----------------|---------------|
| `habbo/toolbar`  | 3                 | **1**          | 0             |
| `habbo/navigator`| 55                | **1**          | 0             |
| `habbo/session`  | 75                | **6**          | 1 (documented)|

**Two corrections to the recipe, both of which changed the result:**

1. **"Constructed somewhere" is not the question — "constructed by *this* module" is.** A first pass
   asking only whether the TS event class is built anywhere in the engine reported toolbar and
   navigator clean and found 2 in session. Narrowing it to the module found 8. This is the
   `ActivityPoints` lesson again, one level up: `UserRights` is constructed by four other modules,
   `HabboGroupDetails` by three, and each was still missing where AS3 also subscribes it.
2. **Strip comments before matching.** A commented-out registration reads as a construction. Two
   findings sat behind the identical line `// TODO: Register additional message events when
   implemented` — `RespectNotificationMessageEvent` in `RoomChatHandler` and `DanceMessageEvent` in
   `RoomUsersHandler` — with the event, the parser and the registry entry all already in place.

What the seven were:

- **`RoomChatHandler` was 7 of AS3's 10.** `RespectNotification` (chat type 3) and
  `HandItemReceived` (type 5) were ported and unsubscribed; **`SpecialSystemChat` (id 3102, type
  12) had no message at all** and is newly ported — event, parser, registry entry. Its name is
  **derived, not recovered**: `win63_version` is older and does not carry it, and the emulator has
  no constant for 3102 either, so it is named for the AS3 handler (`onSpecialSystemChat`); this is
  stated at both declarations. `RoomSessionChatEvent` was also missing `CHAT_TYPE_PING` (11) and
  `CHAT_TYPE_SPECIAL_SYSTEM` (12).
- **`RoomUsersHandler`** now dispatches `RoomSessionFriendRequestEvent` — `FriendRequestWidgetHandler`
  had been listening for it since it was written — and `RoomSessionDanceEvent`.
- **The dance round trip is closed at both ends.** `AvatarInfoWidgetHandler` now handles `RSDE_DANCE`
  as AS3 does (own avatar only, matched on room object id), and the optimistic `isDancing` the port
  set at the *send* site is gone. Checked before removing it: the emulator's `DanceMessageHandler`
  is real and `SetAvatarDanceAsync` broadcasts `DanceMessageComposer` to the whole room, sender
  included — so the echo genuinely arrives.
- **`HabboNewNavigator._groupDetails` was never filled.** `onGroupDetails()` and
  `getCachedGroupDetails()` were both written; nothing called them. `onGroupDetails` is now on
  `IHabboNewNavigator`, reached through a new `HabboNavigator.newNavigator` getter — the same shape
  as the existing `transitionalNavigator` one, and for the same reason.
- **`HabboToolbar.addHabboConnectionMessageEvent()` was dead code** — helper, tracking list and
  disposal all ported, never called once. AS3's single toolbar subscription is `onUserRights`, whose
  whole body is a second attempt at `initVideoOfferExtension()` (the first runs from
  `onPerksUpdated()`, before `catalog.videoOffers.enabled` may have flipped).

**The one left open is deliberate and now carries a `TODO(AS3)` at `RoomSessionManager`:**
`onRoomVisualizationSettings` (2986) returns immediately unless `_roomViewerMode`, the standalone
room-viewer embed this port does not implement. The message is ported and `RoomMessageHandler`
subscribes it for the normal path.

Verified: `tsc --noEmit` clean on both packages, ESLint clean on every touched file, and the sweep
re-run reports 0 missing in toolbar and navigator, 1 documented in session.

#### The ten "server is waiting" send gaps, triaged — 2026-08-14

`wire-coverage` reported 10 send gaps with a real (non-stub) emulator handler behind them, which
reads as a ready-made worklist. It is not one, and the difference matters before anyone plans around
that number: **a send gap is only cheap when the code that would send it is already ported.**
Resolving all ten through WIN63's registry to their AS3 composer, then to the class that sends it:

| id   | composer sender (AS3)                          | what it actually costs |
|------|-------------------------------------------------|------------------------|
| 394  | `HabboInventory.initComponent()`                | **done** — wiring      |
| 2069 | `HabboInventory.initComponent()`                | **done** — wiring      |
| 3862 | `FurniModel.requestInitialization()`            | needs `_isInRoom`, i.e. `roomEntered()`/`roomLeft()` and the three handlers that call them |
| 2931 | `ClubDiscountPromoExtension.onTextRegionClicked()` | that click handler is not ported |
| 1760 | `AchievementsResolutionController`               | the port's is a 177-line log-only shell |
| 916  | `AchievementsResolutionController`               | same |
| 3426 | `BCFloorPlanEditor`                              | whole window unported |
| 880  | `BCFloorPlanEditor`                              | whole window unported |
| 625  | `VariableManagementDetailView`                   | the `habbo/roomevents` remainder |
| 785  | —                                                | **not a client gap** |

**Two of ten were wiring.** `GetSilverMessageComposer` (394) and `GetNftCreditsMessageComposer`
(2069) — both of which kept their real names through obfuscation — did not exist in this port at
all, so the two balances were never requested once. They are now sent from `initComponent()` where
AS3 sends them. Send gaps 47 → 45, real-handler gaps 10 → 8.

**785 is not a gap at all.** It is absent from *both* tables of `_SafeCls_2046.as`: the emulator
carries a header this client build does not have. Two others nearly went the same way for the
opposite reason — a first pass reported 394 and 2069 as absent from the registry because the grep
assumed obfuscated `_SafeCls_N` names, and those two are in clear. **Match both forms.**

`HabboInventory.initComponent()` also revealed a question this pass deliberately did not answer: AS3
sends five composers there and this port sends none, reaching 540 through `requestFurni()` and
`habbo_club` through `onPurseTimer()` instead. Adding the boot sends without checking those paths
would double every request rather than close a gap; the marker at that site says so, and
`_SafeCls_2019` has no port equivalent under any name.

#### `variables_management/detail` — the permanent-variable editor, 2026-08-14

The first of `habbo/roomevents`' enumerated remainders, and the slice that closes send gap **625**
from the triage above: editing the wired variables stored on one user, pet or bot.

What was there: `VariableManagementDetailController` as a 53-line spine against a 267-line original,
its interface, and nothing else. No view, no previewer, **and no message layer at all** — events
1557 and 1643 unregistered, composer 625 absent, `WiredUserPermanentVariablesList` non-existent.
Both ends of the chain were already live, which is why this was worth doing before anything else in
the module: `WiredMenuController` constructs the controller, and the overview's row click already
sent 3777 to ask for the data that had nowhere to go.

Ported: the DTO, two parsers, two events, the 625 composer, `PermanentVariableHolderPreviewer`
(130 l.), `VariableManagementDetailView` (490 l.) and the controller's remaining 214 lines.

**All four names are recovered, not derived.** `win63_version` carries readable filenames for every
one — `WiredUserPermanentVariablesEvent`, `WiredSetUserPermanentVariableResultEvent`,
`WiredSetUserPermanentVariableComposer`, `WiredUserPermanentVariablesList` — which is the
"read `win63_version` filenames first" rule paying off again. It also caught a false claim already
in the tree: `RequestVariableManagementDetailComposer` (3777) documents itself as "fully obfuscated
in AS3", where `win63_version` names it `WiredGetUserPermanentVariablesComposer`. Corrected at the
declaration; the class is left unrenamed to avoid churning its call sites.

Three details worth keeping:

- **The owner block on the wire is conditional.** `WiredUserPermanentVariablesList` reads owner
  id/name/figure only when `entityType != 1`, because a user is its own owner. Reading them
  unconditionally would consume the variable count as the owner id and desynchronise everything
  after it.
- **One message does all three writes.** Edit, create and delete are all 625, separated by a `mode`
  of 0/1/2. Delete still carries a value, and AS3 sends 0.
- **Two sources have to agree before anything draws.** The wire sends variable ids and values, never
  what those variables *are*, so `onGetData()` asks the synchroniser for the catalogue first and
  applies the list in the callback. A stored value whose definition is unknown is dropped rather
  than rendered.

`Util.windowIsChild()` was added while porting the view's click-outside handling: AS3 has it as a
member of every window (`WindowController.windowIsChild()`) and this port's `IWindow` does not, so
`WiredMenuInspectionTab` had grown a private copy. Both now share one, and the tab's copy is gone.
(`TableRowView` keeps its own — AS3 declares one on that class too.)

Send gaps 45 → 44, real-handler gaps 8 → 7.

#### The chests tab — 2026-08-14

Second of `habbo/roomevents`' enumerated remainders: lock/unlock the room's wired chests, and a
ten-row preview of their recent transactions.

It was a 31-line stub against 264 lines, `TransactionPreviewTableObject` was missing, and its
message layer did not exist — event 2910, composers 2016 and 1630, and the two DTOs. All of it is
ported, plus `TransactionConfig` (14 lines, one integer) out of the deferred
`wired_trading/transactions/overview` block, which is the only thing the tab needed from there.
**That dependency looked like a blocker and was not** — worth checking before deferring a slice on
the strength of an import list.

Two things the port had to get right:

- **One header answers two windows.** 2910 feeds both this tab's preview and the paged transactions
  window, and the payload says which: a page is only the preview's if `amount` is 10, `currentPage`
  is 1 **and** `logListType` is 1. The two `logListType` names are derived — 0 is read by
  `WiredTransactionLogsView`, 1 by this tab, and that is all the evidence there is.
- **Nothing pushes a chest transaction**, so the tab polls: `update()` re-requests every 20 seconds
  while it is being viewed. The lock buttons also go quiet for 500 ms after a click, so a double
  click cannot send the same command twice.

**The server knows nothing about chests.** Unlike camera and crafting — which at least have handlers
that accept and drop — vortex-emulator has **no constant at all** for 2910, 2016 or 1630, so they do
not even register as send gaps in `wire-coverage`. The tab is complete client-side and mute in
practice. That is a third category worth naming: *not implemented* is visible in the gap count,
*stubbed* is visible once the script reads bodies, but *absent from the header table* is invisible
to both.

Names: `WiredTransactionInfo` and every accessor on it are recovered — that class survived
obfuscation. Everything else here is **derived**, and says so: `win63_version` predates wired chests
entirely (not one chest message anywhere in it) and the emulator has no constants to corroborate
against.

Left as a `TODO(AS3)` at `TransactionPreviewTableObject.getTableCell()`: AS3's `ITableObject`
declares `:TableCell` and its implementations return null from the default branch, so the port's
non-nullable signature is faithful to a signature AS3 does not honour. `VariableValueTableObject`
already casts around it; widening the interface is the real fix but `TableRowView` feeds the result
straight into `TableCellView` at three unguarded call sites, so it needs AS3's null path read first.

Sweep of `habbo/roomevents` after this: 21 of 24 AS3 subscriptions wired, the remaining 3 all in the
deferred `wired_trading` chest sub-controllers.

#### The sandbox donation tool, and the eight files under it — 2026-08-14

Third of `habbo/roomevents`' enumerated remainders. `misc/SelfDonationTool` gives the player
furniture in a sandbox environment, reached only by its own `selfdonation/open` link.

**The estimate moved three times before it settled, and that is the lesson worth keeping.** It reads
as 3 files / 353 lines. Its view extends `AbstractUbuntuWiredUI` and uses `UbuntuPresetManager`, both
in the deferred `wired_trading` block → 650. Its section needs `ItemTypeSelectionPreset` and
`ChestItemIconPreviewerPreset` from `presets/contracts` → 1,196. The previewer needs
`ChestItemTypeRenderableWrapper` from `wired_trading/chests/subcontrollers/views` → **1,250, eleven
files.** Every layer looked like the last one. Reading a class's import list bounds it; only walking
the imports transitively sizes it.

Ported, bottom-up: `ItemTypeTableObject`, `ChestItemTypeRenderableWrapper`,
`ChestItemIconPreviewerPreset`, `ItemTypeSelectionPreset`, `ItemTypeSelectionSection`,
`UbuntuPresetManager`, `AbstractUbuntuWiredUI`, `ISelfDonationTool`, `SelfDonationToolView`,
`SelfDonationTool`, three `PresetManager` factory methods, a new `IID_SelfDonationTool`, and the
message layer (event 3407, its parser, composer 1119).

This closes `presets/sections` at 12/12, opens `presets/contracts` and `contracts/itemtable`, and
starts `wired_trading` at its two root files — which is what most of the other 41 deferred files in
that block extend.

**`ItemTypeSelectionPreset.updateFilters()` is a reconstruction, not a transcription**, and carries a
`TODO(AS3)` saying so. The primary tree's body is mangled: an empty inner `for each`, an
unconditional `push`, and a `do {…break;} while(item.matchesSubstring(query))` whose condition can
never execute. As decompiled it returns the entire catalogue for every query — not a filter at all.
The surviving pieces show the intent (split the query on spaces, match with `matchesSubstring`) but
not whether the terms combine with AND or OR, because the loop that used them is gone. The port
applies the one readable predicate — `matchesSubstring` on the whole lowercased query — and leaves
the term split unused rather than inventing a combinator. This is the *primary* tree being
unreliable, not `win63_version`: the usual "the primary settles it" rule has nothing to appeal to
here.

Two smaller reconstructions, both noted at the site: the wall-item loop guards on a `fullCode` that
is still the previous iteration's value (hoisted-local artefact — applied to the item's own code
here), and `POSTER_IDS` is transcribed verbatim including its gaps, since 60-82 and 524-999 are
simply not posters that exist.

The tool is constructed and attached in `HabboUserDefinedRoomEvents.initComponent()` alongside
`wiredMenu`. Constructing it *is* the wiring — there is no toolbar entry, only the link tracker it
registers itself, so it is created unconditionally as AS3 does and refuses outside a sandbox at
`open()` rather than by not existing. It is gated three times over on purpose: `open()` refuses
outside a sandbox, `validate()` refuses again before sending, and the server refuses a third time.

#### The reward notification, and the trade-rule presets under it — 2026-08-14

Fourth slice of the `habbo/roomevents` remainder. A completed wired transaction can pay out, and
`RewardNotificationController` is what raises the "you won this" window.

**The sizing question was asked the right way this time and still moved once.** The first question
was not "how big" but "who constructs it" — the four remaining `presets/contracts` files are all
built by `PresetManager` factories whose only callers live in the deferred `wired_trading` block, so
porting them alone would have been dead code. That pointed at `reward_notification` as the one
consumer with a live construction site (`HabboUserDefinedRoomEvents`, same as `SelfDonationTool`).
The estimate then went 503 → 1,240 lines when `NodeOverviewPreset` turned out to **extend**
`TradeRuleEditorPreset` — checking a class's imports does not reveal its base class.

Ported: `TradeRuleNodeView`, `TradeRuleEditorPreset`, `NodeOverviewPreset`,
`WiredTransactionSuccessContents`, its parser, the 2677 event, `IRewardNotification`,
`RewardNotificationView`, `RewardNotificationController`, `createNodeOverviewPreset`, and a new
`IID_RewardNotificationController`. `presets/contracts` is now 5/6 — only `TradeRuleListEditorPreset`
is left, and its callers are still in the deferred contracts sub-controllers.

Three things worth keeping:

- **The notification's id is not on the wire.** `WiredTransactionSuccessMessageParser` holds a
  class-level counter starting at 1 and stamps each payload with it. That id is what the controller
  keys its map on and what `wiredrewards/open/<id>` refers to, so it must never repeat within a
  session — hence a static that is deliberately never reset.
- **Arriving and opening are separate.** Every rewarding payload is stored; only `openByDefault`
  pops a window. The rest wait for the link, which is why the map outlives the views.
- **Chip views are pooled across every editor**, keyed by style name and capped at 50 per style.
  The chip template is not a layout: `TradeRuleEditorPreset` *removes* the first grid item from the
  rule window and clones that, which is why `addNode()` inserts at `numGridItems - 1`.

`IHTMLTextWindow.initializeLinkStyle()` was declared but missing — the interface's own header
documented it and no implementation existed. It is now on the interface and implemented on
`HTMLTextController`, writing AS3's four link rules into `htmlStyleSheetString`. It carries a
`TODO(AS3)` because **nothing reads that field yet**: the value is recorded, the pixels are
unchanged. That gap is upstream and predates this slice.

#### Two bugs the user hit, and one claim of mine that did not survive — 2026-08-16

**The me-menu's "rooms" opened nothing.** `HabboNavigator.openNavigator()` is faithful to an AS3 body
that is *empty* (`HabboNavigator.as:730` is `{}`) — nothing is meant to call it. AS3 reaches the
navigator through `HabboNewNavigator.legacyNavigator`, whose field **is** the `LegacyNavigator`
wrapper forwarding to `HabboNewNavigator.open()`. This port returned the wrong object twice: the
accessor handed back the DI-resolved *old* navigator, and `HabboToolbar.get navigator()` returned
`_navigator` directly where AS3 returns `_newNavigator.legacyNavigator`. Both fixed; the DI-held one
is AS3's `_SafeStr_4588` and is now named `_oldNavigator`.

Second, in the same call path: `performSearch()` returned early on a cache hit, skipping its trailing
`open()`. AS3 has no return — it serves the cache and falls through — so a repeat search answered
instantly and silently.

**The wired "select area" button did nothing**, and the reason it did nothing is worth keeping:
`RoomAreaSelectionManager` was a 52-line stub whose `activate()` returned false, and `InArea` reads
that as "unavailable" and greys both buttons. The state machine is ported in full now, plus the three
things the engine lacked — `isMoveBlocked()`/`isAreaSelectionMode()` were both `return false; // TODO`,
tile mouse events never reached the selector, and a click that ends a drag has to be claimed before
placement/move/walk (`_SafeCls_90.as:2382`).

**And the correction worth recording.** I first shipped that without the highlight, claiming it needed
"the plane-level highlight and a ColorMatrixFilter equivalent, neither of which this port has". Both
halves were false, and I had not measured before saying it:

- `ColorMatrixFilter` comes from pixi.js and `VariableHoldersHighlighter` already uses it.
- `RoomPlaneParser.extractPlanes()` was already ported **with** its `isHighlight` flag, already
  tracking what it built in `_highlightPlanes`. `RoomPlane.isHighlighter`,
  `RoomObjectSprite.filters`/`skipMouseHandling` and the canvas forwarding both to the Pixi sprite all
  existed.

What was actually missing was three seams: the parser's public
`initializeHighlightArea()`/`clearHighlightArea()`/`isPlaneTemporaryHighlighter()`; the same pair on
`RoomVisualization` plus a start index on `createPlanesAndSprites()` so only the appended planes get
sprites; and the three filter presets. **AS3's filter offsets are 0-255 and PixiJS wants 0-1** — every
fifth-column term is divided by 255, the conversion `VariableHoldersHighlighter` documents. Getting it
wrong washes the floor white instead of tinting it.

The lesson is the one the mandate already states and I did not follow: *verify before you assert a
blocker*. A "not ported yet" note is a claim with a shelf life, and this one had expired.

#### Where the port stands — measured 2026-08-16

Three tools, three questions. Re-run them rather than quoting these numbers later.

**`sweep-unwired.mjs` — does the port subscribe what AS3 subscribes?** 471 subscriptions across the
thirteen main modules, **419 wired, 46 missing**:

| Module | Wired | Missing |
|---|---|---|
| `habbo/catalog` | 53/58 | **4** (+1 unresolved) — was 13, closed by the vault and habbicons (2026-08-16) |
| `habbo/friendbar` | 34/44 | **10** — all in `talent/`, `onBoardingHc`, `popup`, none in the landing view |
| `habbo/notifications` | 28/37 | **8** |
| `habbo/room` | 51/57 | **6** |
| `habbo/ui` | 1/5 | **4** — the worst ratio in the port; three are messages with no id in the registry at all (YouTube furni playlists/video) |
| `habbo/groups` | 23/26 | 3 |
| `habbo/session` | 74/75 | 1 |
| `habbo/inventory` | 53/54 | 1 (deliberate — the badge rarity pair, see above) |
| `navigator`, `messenger`, `quest`, `toolbar`, `roomevents` | full | 0 |

**`wire-coverage.mjs` — can the client trigger what the server implements?** 40 send gaps, of which
**3 have a real handler**: 3426 and 880 (the floor-plan editor, `BCFloorPlanEditor` at 1,879 lines,
wholly unported) and 785, which has no entry in the WIN63 registry in either table and is an
emulator-side leftover rather than a client gap. 29 of the other 37 sit behind emulator stubs.

**`todo-inventory.mjs` — what is knowingly incomplete?** 363 TODOs, 310 of them `TODO(AS3)`. By kind:
203 micro-gaps with no blocker cited, 86 blocked on a whole unported module, 28 Flash-only
(BitmapData/filter/shader/Timer), 27 wire-level, 19 decided-against. Hottest modules: `catalog` 63,
`ui` 62, `room` 35, `inventory` 31, `roomevents` 29. **27 are flagged stale** — their blocker may have
landed since; `--stale` lists them and this session's correction is exactly why that list is worth
reading.

**The "SolidJS" backlog is down to five.** The recipe above reports `FurniModel` (one call),
`RoomCompetitionController` (550 l. AS3 / 169 TS), `CitizenshipVipQuestsPromoExtension`,
`VideoOfferExtension` (251/117) and `OfferExtension`.

#### The resolution furni — 2026-08-16, and two more send gaps close

Fourth and fifth of the six real "server is waiting" gaps: **1760** and **916**. A resolution furni
is one the player points at a single achievement, which it then tracks for a season.

Three windows, one controller, and the port had none of them — `AchievementsResolutionController`
was 177 lines of `log.debug` against 387 of AS3, and both views were absent. The routing was already
complete: `QuestMessageHandler` subscribes 3143/1844/1166 and calls straight into the controller, so
the module's sweep read **24/24 with nothing working**. That is the failure mode the sweep cannot
see — a subscription that lands in a stub.

Ported: `AchievementResolutionProgressView`, `AchievementResolutionCompletedView`, the real
controller, `HabboQuestEngine.getXmlWindow()`, and the two composers.

**One header reads *and* writes.** `GetResolutionAchievements(stuffId, 0)` asks for the candidate
list; the same composer with a non-zero achievement id **commits** the player's choice. The save
button and the three refresh paths are identical on the wire and are not the same operation — which
is why the second parameter's default matters.

**Nothing polls.** The progress panel is refreshed by re-asking with the 0 form whenever an event
could have moved the number: a level-up notification, or a completed achievement whose id matches the
one on screen.

Details worth keeping:

- **The asset key has no `_xml` suffix here.** `HabboUserDefinedRoomEvents.getXmlWindow()` appends one
  because the wired layouts ship as `wired_menu_view_xml.xml`; the quest layouts ship as
  `AchievementsResolutions.xml`. `App.ts` keys every layout by its filename minus the extension, so
  appending would look up a name that does not exist — and a missing layout returns null in silence.
  The quest engine's own `getXmlWindow()` therefore uses the name verbatim.
- **The progress bar is three windows.** Left cap, stretching middle, right cap; the right one appears
  only at exactly 100%. The middle's authored width is captured once at build time as the 100% mark,
  so nothing else may resize it.
- **`AchievementResolutionCompletedView.get disposed()` returns `!= null`** where every sibling
  returns `== null` — it answers "disposed" while alive. A bug in the dump, transcribed with a note
  rather than corrected: nothing reads it, so a silent fix would only hide the discrepancy.
- `onResolutionCompleted(badgeCode, stuffCode)` hands the two to the view **swapped**; the view's
  first parameter is the stuff code. Kept.
- `onLevelUp` compares the notification's *type* against the view's *achievement id*. AS3's
  comparison, transcribed.

Real-handler send gaps: **5 -> 3**, and the three that remain are two floor-plan-editor composers
(3426, 880 — `BCFloorPlanEditor` is 1,879 lines and wholly unported) plus nothing else.

#### The toolbar promo bars, and five classes that only look like gaps — 2026-08-16

Third of the six real "server is waiting" send gaps: **2931**, the discounted club-extension offer.
Both its call sites — `ClubDiscountPromoExtension` and `CitizenshipVipDiscountPromoExtension` — were
shells that built no window, so there was no button to click and the composer did not exist.

Both are now real: they build from `club_discount_promotion_xml` /
`vip_discount_promotion_v2_xml`, wire their regions, attach to the extension slot, and send the
event-log line plus 2931 on click. Details worth keeping:

- **`isExtensionEnabled()` had lost half its test.** AS3 requires club level 2 *and* the config flag;
  the port checked only the flag, which would have shown the bar to a player with no club at all.
- **The two extensions disagree about which id they detach, and that is AS3's code, not a slip.**
  `CitizenshipVipDiscountPromoExtension.onClubChanged()` detaches `vip_quests` in its `else` branch
  and declines to attach while `vip_quests` is up — the two promos share one slot and this is how the
  quests bar wins it. Its `club_promo` id is only ever detached by `dispose()`. The club version
  detaches its own id. Transcribed as written, mismatch included.
- **The sliding highlight is 26 ticks against 20 steps.** The strip reaches the right edge at tick 20;
  the remaining six *crop* it so it slides off rather than overhanging. AS3 does that with
  `BitmapData.copyPixels()`; here it is an `OffscreenCanvas` blit, the established synchronous
  stand-in (`core/utils/BitmapSlot`) — `createImageBitmap()` is async and cannot run a 25ms tick.
- `extend_hilite`'s `content` is a Pixi `Texture`, not an `ImageBitmap`. `AssetBitmap.resolveSync()`
  exists for exactly that, and casting straight through throws *inside the render pass*, taking the
  whole frame with it.

**The "SolidJS" comments are a measurable backlog, and a third of it is false.** 30 files still say
rendering is "handled by SolidJS" — a framework this port does not use. Cross-checking which of them
build no window *and* whose AS3 counterpart does gives 13 candidates. Five are not gaps at all:

`ToolbarView`, `MeMenuController`, `MeMenuSettingsMenuView`, `MeMenuSoundSettingsView`,
`MeMenuSoundSettingsItem` and `MeMenuChatSettingsView` are the **2023 me-menu design**. In the 2026
tree `HabboToolbar` constructs `BottomBarLeft`, which constructs `MeMenuNewController`; `ToolbarView`
is never constructed at all, and `MeMenuController` only by `ToolbarView`. Porting their window code
would be porting dead code. Each now says so at the top of the file instead of blaming SolidJS.

The recipe, worth re-running before quoting the backlog:

```bash
for f in $(grep -rl "SolidJS" packages/vortex-engine/src); do
  grep -q "buildFromXML\|buildWidgetLayout\|getXmlWindow\|findChildByName" "$f" && continue
  base=$(basename "$f" .ts)
  as3=$(find sources/WIN63-202607011411-782849652/src/com/sulake/habbo -name "$base.as" | head -1)
  [ -n "$as3" ] && grep -q "buildFromXML\|findChildByName" "$as3" && echo "$base"
done
```

It reports 13; six of those are the dead me-menu chain above. The genuine remainder is
`AchievementsResolutionController`, `RoomCompetitionController`, `CitizenshipVipQuestsPromoExtension`,
`VideoOfferExtension`, `OfferExtension` and one call in `FurniModel`.

Send gaps with a real handler behind them: **6 -> 5** (and the count was 7 until `785
ClickCharacterEvent` turned out to have no entry in the WIN63 registry at all, in either table — the
client never sends it, so it is an emulator-side leftover rather than a client gap).

#### `habbo/inventory` was 42/54 subscribed — 2026-08-15

Found while sizing the "server is implemented and waiting" send gaps. One of them (3862,
`RequestFurniInventoryWhenNotInRoom`) turned out to be blocked on `FurniModel._isInRoom`, which
nothing set, because the room-lifecycle messages that set it were never subscribed. Sweeping the
module properly turned up **twelve** missing subscriptions, all against models that were fully
ported and simply never fed.

Wired, in three groups:

- **Room lifecycle** (611, 3404, 1086, 2914). AS3 hangs `onRoomLeft` on *both* 611 and 3404 —
  opening a connection to another room and closing the current one both mean "you left". 1086 is
  filtered on the player's own name, because the refusal also reaches everyone already inside.
- **Marketplace** (1038, 1397, 2821, 3599). `MarketplaceModel` had every setter, `setItemStats()`
  and `onNotEnoughCredits()` ported, and nothing called any of them — the tab opened with a
  disabled model, zero commission and no price history. 3599 re-requests on a rights change, gated
  on the tab having been opened at least once.
- **Badges / clothing** (639, 3070, 1231).

**Two consumers elsewhere were waiting on 1231.** `HabboAvatarEditorManager.get inventory()`
returned `null` with a TODO saying so, which reports every sellable clothing item as *unowned* and
therefore hides it — purchased clothes were invisible in the editor. And
`PurchasableClothingConfirmationView.open()` could never take its already-bound branch, so wearing an
outfit the player already owns always asked for confirmation. Both closed.

**One message stays deliberately unwired, and the reason is the point:** 1292
(`HabboUserBadgesMessageEvent`). Its handler re-asserts every equipped badge with
`updateBadge(code, true, 0, ownerCount, badgeRarityId)`. The 2026 wire carries four fields per badge;
**this port's parser reads two and vortex-emulator's serializer writes two**, so the rarity pair does
not exist to pass. Wiring it as it stands would call `Badge.updateMetadata(0, 0)` and wipe the rarity
that 3926 has already set correctly — strictly worse than leaving it out. It needs the parser widened
*and* the emulator's serializer with it.

The same 2026 rarity pair was found missing from **639**, and there the split is the other way round:
vortex-emulator already writes `OwnerCount`/`BadgeRarityId`, and the client's
`AchievementNotificationData` stopped at `showDialogToUser` and dropped them. That one is a
client-only fix and is done.

`win63_version` is what led the parser astray in the first place: its `HabboUserBadgesMessageEventParser`
is one of the bodies that decompiled badly — `while(0 < _loc4_)` with the counter never tested, and
`push(null)` for every badge. Two reads per badge is what that body shows; the primary tree has four.

Sweep after: **53/54**.

#### Bloc C closes — the configuration cache and four smaller gaps, 2026-08-15

`UserDefinedRoomEventsCtrl`'s remaining `TODO(AS3)`s, worked through. Two were real features, two
were behaviour the port had suppressed, and one was a note that had gone stale.

**The configuration cache** (`WiredConfigurationCache`, 114 lines of AS3 plus ~50 in the controller)
is the substantial one: a built wired dialog is kept whole and handed back when the same element is
reopened, instead of being rebuilt. The key is `holder + style + element code` — **the style is part
of it**, so the same element under two wired skins is two frames. The code goes through
`getElementByCode(def.code).code` rather than `def.code`, because an element answers to both its
`code` and its `negativeCode`, and keying on the raw one would build a second frame for an inverted
condition.

The cache changes what `close()` means: with `useCache` on (AS3 hard-codes `true`) the frame is no
longer disposed on close — it is still referenced by its cache entry, and only `clearCache()` ever
disposes it. `clearCache()` closes first, because the open frame is one of the entries it is about
to destroy. `HabboUserDefinedRoomEvents.resetCache()` is its only caller, on a wired-style change.

The other four:

- **Dual furni picking was never switched on.** `_mergedSourceMode` is read off
  `def.inputSourcesConf.isDualFurniPickingMode()` when the dialog opens; the port's note said it came
  from the input-source picker's `setMergedSourceType()`, which is a different thing — that sets a
  *row's* type. Nothing assigned the flag, so the second furni set was unreachable. The
  `hideFurniHighlights()` AS3 runs between `highlightActiveWired()` and rebuilding the sets was
  missing too: it clears the *previous* def's picks, and opening a def while none was open reaches
  there without `close()` having done it.
- **`fixQuantifierNames()`** re-derives the two quantifier radio labels from the def as it stands,
  because `getQuantifierKey()` folds `isInvert` into the key — an inverted condition needs the
  negated wording. It was deferred for wanting "a RadioGroupPreset option accessor"; the port has had
  `radioAt()` all along. Its AS3 name is `get`, **recovered** from the decompiler's identifier footer
  (`@identifier _SafeStr_4547 = "get"`), which the trace now says instead of claiming it derived.
- **The header's variable button** (`buttonVisible` for elements naming a variable) was deferred,
  with the exact `IVariableType` runtime test it needed already written forty lines above in
  `createHeader()`.
- **Paste-into mode.** With the open dialog in "copy into", clicking another wired writes this
  dialog's settings onto it rather than opening it — but only onto the same element type; anything
  else is refused with `${notification.wired.pasted_into_fail}`. `update(2, id)` is the save mode,
  targeting the clicked def. `stuffSelected()` already had its half of the feature.

Bloc C's `TODO(AS3)` count goes 7 -> 2, and both survivors are the visual editors this class only
hosts (`RoomAreaSelectionManager`, `FloorDrawingPreset`) plus the `time_display` notification key,
which has no reader in the port yet. Module-wide: 38 -> 29.

#### Three boot-time faults the wired slices surfaced — 2026-08-15

All three came out of one console log, and none of them was in the code just written — the new
controllers only made existing faults reachable.

**1. `initComponent()` built its sub-controllers in the wrong order, and it was fatal.**
`UbuntuPresetManager`'s constructor reads `roomEvents.wiredCtrl.getStyleByName('ubuntu')`, and three
sub-controllers build one from their own constructor — the contract controller, the reward
notification and the self-donation tool. The port created all three *before* `_wiredCtrl`, so the
first of them threw `Cannot read properties of undefined (reading 'getStyleByName')`, inside
`initComponent()`, leaving the whole component half-built and every wired field undefined.

AS3's constructor order is `UserDefinedRoomEventsCtrl` **first**; the port now follows it exactly,
constructors then attaches, as AS3 does. The port's own comment claimed "wiredMenu must exist before
WiredEnvironment" — `WiredEnvironment`'s constructor touches only `communication`, and AS3 builds it
*before* wiredMenu. Ordering claims in a comment are worth checking against the constructor bodies.

**2. `roomObjectAddedHandler` dereferenced `_wiredCtrl` unguarded.** AS3 can — it assigns the field
in its constructor, so the REOE_ADDED listener cannot outrun it. This port assigns in
`initComponent()`, which `Component` runs *after* attaching each dependency's listeners, so a furni
added in between arrives with nothing to tell. The inventory's room previewer is the path that does
it. Guarded, with the deviation named.

**3. Incoming header 3407 was registered twice** — `SelfDonationResultMessageEvent` and
`AcceptFriendResultMessageEvent` — so the later `set()` silently unregistered the earlier and
self-donation results never fired. WIN63's registry settles it: `_SafeStr_4546[3407] =
SelfDonationResultMessageEvent` (a name that survived obfuscation, so unambiguous) and
`_SafeStr_4546[3707] = _SafeCls_2256`, which is exactly the class `HabboFriendList` subscribes as
`onAcceptFriendResult`. A digit transposition.

**vortex-emulator disagrees and is wrong**: `Headers.cs:855` has `AcceptFriendResultComposer = 3407`
and `FriendListMap.cs` wires its serializer there. Until that line reads 3707, the emulator's
accept-friend replies reach the client's self-donation parser. The client is not the side to bend
here — header source-of-truth order is the WIN63 registry first, the emulator as corroboration only.

The duplicate was found by a one-liner worth keeping, since `HabboMessages` only logs a collision at
runtime and only for the pair that collides:

```bash
for t in _events _composers; do
  grep -o "this\.$t\.set([0-9]*," packages/vortex-engine/src/habbo/communication/HabboMessages.ts |
    sed "s/.*set(//;s/,//" | sort -n | uniq -d
done
```

It reports one remaining hit, `_composers` 1111, which is a false positive: the second registration
(`GiveStarGemToUserMessageComposer`) is already commented out because the 2026 build removed that
message — the emulator agrees, `GiveStarGemToUserMessageEvent = -1 // REMOVED in 2026`.

#### The transaction windows — 2026-08-15, and `wired_trading` closes

Last slice of the `habbo/roomevents` remainder: 1,717 lines of AS3 across 11 files, of which one
(`TransactionConfig`) had already been pulled forward for the chests tab. Two windows — the paged
log of every chest movement, and one transaction opened out into its item breakdown.

**`chests/` was the dependency, not the reverse.** The July estimate deferred transactions because
they "drag in the whole `chests/` subsystem"; with chests landed the day before, this slice needed
nothing new from it beyond three helpers it reuses directly — `FurniChestView.getChestBasedItemName`,
`FurniChestItemView.initChestBasedIconUI`, and `WiredChestWrapperView.relocateBubbleFocus`.

Its wire layer was half-present. 2910 (a page of logs) and 2016 (ask for the room's log) had shipped
with the chests tab; **1306 and 475 were missing entirely** — one transaction's breakdown and the
request for it — along with the `WiredTransactionDetails` DTO. 847, the profile open behind a
username cell, turned out to be already ported as `GetExtendedProfileMessageComposer`.

Details worth keeping:

- **475 sends the transaction id as a `Long`.** AS3 takes a `Number` and wraps it before pushing, and
  `WiredTransactionInfo.transactionId` is read with `readLong()` to match. Every chest id around it
  is a plain int, so this is the one place the width matters.
- **`WiredTransactionDetails`' two furniture maps are keyed by object.** AS3 stores
  `ChestItemType -> count` in a `Map`, and a `ChestItemType` carries a type id, a wall/floor flag and
  a poster id — so the same furniture in two states stays two rows. That rules out a plain object;
  `OrderedMap` keeps the object keys *and* the server's order, which is the render order.
- **`isIncompleteData` is why the breakdown can say "+N".** The server truncates long lists; the view
  sums what actually arrived and shows the difference against the reported count as one extra cell.
  Without the flag the window would under-report silently.
- **One header, two windows, filtered by page size.** `WiredTransactionLogsController` drops a page of
  ten (the chests tab's preview) *and* anything that is not `PAGE_SIZE` — a whitelist, not two guards.
- **Paging is rate-limited twice**: nothing inside 280 ms, and no repeat of the still-pending page
  inside 2 s. The pending page is cleared when its answer lands, which is what lets the refresh
  button work at all.
- `TransactionChestItemWrapper` dresses a bare `ChestItemType` as an `IChestStorageItem` with a fixed
  `specialType` of 1 — which is also why LTD and rarity badges never appear on a transaction cell.
- Both `_SafeCls_1968` and `_SafeCls_2287` are **empty interfaces** with no members and no parameter
  ever typed as them. Ported as markers, with a note: they read as a convention that was never filled
  in rather than an abstraction.

With this, `HabboUserDefinedRoomEvents.initComponent()` builds every sub-controller AS3's constructor
does. What is left in the module is the `WiredMenuEvent` toolbar dispatch, three UI helpers on
`HabboUserDefinedRoomEvents` nothing calls yet, and Bloc C's `TODO(AS3)`s.

#### The wired chests, end to end — 2026-08-15

Sixth slice of the `habbo/roomevents` remainder, and the largest: **3,938 lines of AS3 across 15
files**, landed in two commits — the message layer first (`975ca9c8`, explicitly labelled a
foundation with its consumer named), then the whole UI at once.

**It had to be one commit, and that was proved rather than asserted.** Six foundation files were
written, typechecked, and found not to compile without `WiredChestWrapperView`, which needs the
controller, which needs the two sub-controllers, which need the wrapper back. There is no compilable
intermediate state in `chests/`; the wrapper is the pivot.

Ported: `IWiredChest`, `IWiredChestControllerHost`, `ChestType`, `IChestSubController`,
`AbstractChestSubController`, `FurniChestSubController`, `CoinChestSubController`, `IChestItemView`,
`FurniChestItemView`, `FurniChestView`, `ChestSettingsUI`, `ChestNotificationSettingsUI`,
`WiredChestWiredUpdateConfirmationView`, `WiredChestUpgradeConfirmationView`,
`WiredChestWrapperView`, `WiredChestController`, a new `IID_WiredChestController`, and the wiring in
`HabboUserDefinedRoomEvents.initComponent()` / `dispose()`.

**Two wire bugs surfaced only when the consumer was written**, both in code that had already been
committed as correct:

- `RequestWiredChestLogsComposer` was `(amount, page, chestId)`. The real call sites —
  `WiredChestWrapperView::onViewLogsClick()` and `WiredTransactionLogsView::requestPage()` — pass
  `(chestId, PAGE_SIZE, page)`. Same three integers, opposite meaning; the wire never complains.
- `DepositWiredChestCoinsComposer` (3514) was named for a call site it does not have. Its only caller
  is the wrapper's `start_deposit_btn`, which serves a furniture chest as readily as a coin one —
  renamed `StartWiredChestDepositComposer`. `CoinChestSubController` sends 2843, not 3514.

A third message was simply missing: **3611, withdraw-everything**, now
`WithdrawAllWiredChestContentsComposer`. The emulator has 3611 as `FriendListUpdateComposer` in the
*other* direction — noted at the declaration so nobody reads that as corroboration.

Details worth keeping:

- **Every setting the chest window shows lives in the furniture's own stuff data.** There is no
  "settings changed" message: a save comes back as `REOE_UPDATED` on the room object, which is why
  the controller subscribes to it and why `viewingChestUpdated()` exists.
- **Opening is server-initiated in both directions.** The server pushes "open chest N", the client
  asks for contents, and whichever sub-controller receives them calls `setOpenStatus(id, this)`. The
  window has no other way to appear. `requestedChestId` and `activeChestId` are separate so a reply
  for a chest the player already left is dropped rather than rendered.
- **Closing sends 2935.** `setClosedStatus()` runs on every hide, not just the close button, because
  a chest left open holds server-side state.
- **The lock checkbox cancels its own operation and asks first** — but only for a non-owner when
  locking, always when unlocking. Two flags keep the confirmed flip from re-asking
  (`_ignoreCheckboxSelectEvents`) and from being echoed to the server
  (`_ignoreCheckboxSelectedEvents`).
- `SaveWiredChestSettingsComposer`'s ninth field is the upgrade button's `disabled` state, which is
  true exactly when the chest already has wired — so the wire receives `is_wired_enabled`. Reading it
  as "the control was greyed out" would transcribe the expression instead of the meaning.

**A port deviation the AS3 type hides:** `getStuffDataMap()` is `_SafeCls_481` (the port's
`OrderedMap`) in AS3, but this port's `IRoomObjectModel.getStringToStringMap()` returns a native
`Map`. Both settings screens took `OrderedMap` and had to move to `Map`/`.get()`. Two getters in
`FurniChestView`/`CoinChestSubController` had also lost `ITextFieldWindow` for `ITextWindow`, which
only showed up as a missing `restrict`.

This closes `wiredChest` and the `wiredChest.onPermissionsChanged()` TODO in `WiredMenuController`.
`wired_trading/transactions/` — the paged logs window and the detail view — is what remains of the
block, and it is now unblocked: `chests/` was its dependency, not the other way round.

#### The contract editors — 2026-08-14, and `presets/contracts` closes at 6/6

Fifth slice of the `habbo/roomevents` remainder, and the one where the sizing method finally held:
**1,569 lines / 17 files, verified before starting and unchanged at the end.** The check that made
the difference was adding base classes to the transitive walk — the previous slice moved 503 → 1,240
purely because `NodeOverviewPreset` *extends* a class its import list does not mention.

Why contracts rather than the transactions window, which looked like the obvious next step since the
chests tab's "view in detail" button already sends its request: **transactions drag in the whole
`chests/` subsystem** — 2,391 further lines, `WiredChestWrapperView` alone being 949 — for a total
near 4,000. Contracts are self-contained.

Ported: the message layer (3 events, 3 parsers, 2 composers), `TradeRuleListEditorPreset`,
`AbstractContract`, `IContract`, `PaymentContract`, `TradeContract`, `RewardContract`,
`AddEditContractElement`, `WiredContractController` and `IWiredContractController`, plus
`createRuleEditorPreset` / `createRuleListEditorPreset`.

Details worth keeping:

- **`WiredContractContentsMessageParser` has a conditional tail.** Only id, type and definition are
  always present; type 0 then reads the payment block and type 2 the reward block, while type 1
  reads nothing more. Taking the wrong branch does not throw — it eats the next message's bytes.
- **The pending contract id is a one-shot latch.** `onOpenContract` stores it, `onContractContents`
  ignores any payload that does not match and then clears it, so an unrequested push is dropped.
- **Opening a contract is two round trips**: 1479 says which, 1594 asks for the contents, 2976
  brings them.
- **`AddEditContractElement` is one instance for the whole client**, handed to every rule editor as
  its `onEdit`/`onAdd`. It is the only wired window that remembers its position, because it reopens
  per chip beside whichever contract window is up.
- Contract editors are built lazily and reused, which is what makes the cached window location
  meaningful across opens.

**A trap for anyone naming these from the emulator:** it defines `1479` as
`Game2GetTotalGroupLeaderboardEvent`, in its *client→server* table, where ours is a server→client
event in the client registry. Independent tables — not a collision, but not corroboration either.
All five names are derived from the AS3 handlers (`onContractContents`, `onContractUpdateResult`,
`onOpenContract`), since `win63_version` predates contracts entirely.

#### Three recycler headers were invented, on both sides of the wire

Found while checking whether notifications' `onRecyclerFinished` was unwired or unported. It was
neither: the event exists, `HabboCatalog` subscribes it — under the wrong id.

| Event                          | Port was | WIN63 registry | Emulator was |
|--------------------------------|----------|----------------|--------------|
| `RecyclerStatusMessageEvent`   | 1919     | **2166**       | 1919         |
| `RecyclerFinishedMessageEvent` | 281      | **3617**       | 281          |
| `RecyclerPrizesMessageEvent`   | 3783     | **3367**       | 3783         |

`1919`, `281` and `3783` have **no entry in `_SafeCls_2046.as` at all**; `2166`, `3617` and `3367`
each have exactly one, and the parser shapes confirm the mapping member-for-member
(`recyclerStatus`+`recyclerTimeoutSeconds`, `recyclerFinishedStatus`+`prizeId`, `prizeLevels`). The
recycler worked only because client and emulator shared the same invention — which is precisely the
failure the 2026-08-02 cross-check warned about: *"an id absent from `_SafeCls_2046.as` is a guess,
wherever it comes from."* Corrected on both sides in the same pass; the new ids collide with
nothing in either registry.

Verified: `tsc --noEmit` clean on both packages, ESLint clean on every touched file, and a headless
boot with zero console errors, zero warnings and no duplicate-header report.

### Camera — complete client-side (2026-08-13)

**`ui/widget/camera` is 9/9 and both handlers are ported and wired**: `RoomDesktop.createWidget()`
now has `RWE_CAMERA` and `RWE_ROOM_THUMBNAIL_CAMERA` (42 → **44** of ~45 cases), `ui/widget/` is
247 → **256**, and all four camera layouts already ship. The whole flow is real — toolbar and
`camera/open` link entry, the five-slot viewfinder with its static slot cache, the shutter, the
photo lab with all 40 filter definitions and the achievement gate, the strength slider, the caption
guard, and the buy/publish/competition state machine including the publish cool-down timer.

**Both engine gaps the slice needed are now open**, so the camera runs end-to-end client-side:

- **`IRoomEngine.snapshotRoomCanvasToBitmap()`** is implemented on the existing
  `RoomRenderingCanvas.takeScreenShot()` (which already forces a 1:1, zero-offset,
  visibility-check-free render and restores state). AS3's signature draws into a caller-supplied
  `BitmapData` through a `Matrix`; this port has no mutable bitmap type, so it takes the region and
  returns an `ImageBitmap` instead — same pixels, different buffer owner. Both viewfinders drive
  their live preview from it, behind a `_captureInFlight` latch because the readback is async where
  AS3's draw is synchronous.
- **`IRoomEngine.getRenderRoomMessage()`** is implemented on a new `SpriteDataCollector`
  (`habbo/room/utils/`, name recovered from PRODUCTION where the WIN63 trees have `_SafeCls_1840`
  and `class_2200`), plus a new `getSortableSpriteList()` on the canvas. Both `collectPhotoData()`
  implementations now return a real, sendable composer.

Two `TODO(AS3)` markers remain in the collector, and they are genuine narrowings rather than
blockers: `getFurniData()` skips AS3's pass that merges *canvas-culled* objects and re-parents
boutique-mannequin sprites (needs `IRoomEngine.getRoomObjects()` and
`IRoomObjectSpriteVisualization.getSpriteList()`), and `getRoomPlanes()` emits only the background
plane, because `RoomVisualization` exposes no `planes` accessor and `RoomPlane` has no
`getDrawingDatas()`. The photo therefore carries the furniture and avatars over a flat
room-coloured backdrop rather than over the tiled floor and walls.

The nine remaining markers in the widget layer are all the same immutable-bitmap constraint —
per-button filter thumbnails, `renderAllEffects()`, save-to-file. The effect *state* is fully
tracked regardless: `getEffectDataJson()` reports the chosen filters correctly, so the render the
server produces carries them even though the local preview does not composite them.

`habbo/communication/messages/{incoming,parser,outgoing}/camera/` is complete: 6 events, 6 parsers,
6 composers, the 4 `Json*` payload DTOs, all 12 headers registered. Wire coverage moves 52 → 47 send
gaps and 57 → 51 recv gaps; the `camera` category is closed in both directions. Also added:
`StringUtil.makeMagicString()` (+ its 60-entry `CHARACTERS` table), which the port was missing, and
`IPlaneDrawingData` — obfuscated as `_SafeCls_2550`/`class_2507` in both WIN63 trees, name recovered
from PRODUCTION, member names from WIN63; neither tree has both halves.

**The finding that matters more than the code: the emulator's camera handlers are empty stubs.** All
five (`RenderRoom`, `PublishPhoto`, `PurchasePhoto`, `PhotoCompetition`,
`RequestCameraConfiguration`) are a bare `await ValueTask.CompletedTask.ConfigureAwait(false);` —
the message is accepted and dropped, and no `InitCamera`/`CameraStorageUrl`/`ThumbnailStatus` ever
comes back. **117 of the emulator's 515 handlers are that shape**, crafting's five included.

`scripts/wire-coverage.mjs` was reporting these as "the server is implemented and waiting" because
it matched on the handler *filename*. It now classifies the body, and the headline number drops from
**44 real handlers to 10**:

| Family                   | Send gaps | Real handler                                              |
|--------------------------|-----------|------------------------------------------------------------|
| Room state / interaction | 8         | 4 (`ClickCharacter`, `GetOccupiedTiles`, `GetRoomEntryTile`, `RequestFurniInventoryWhenNotInRoom`) |
| Targeted offers / shop   | 3         | 2 (`GetHabboClubExtendOffer`, `GetSilver`)                  |
| Talents / achievements   | 3         | 2 (`GetResolutionAchievements`, `ResetResolutionAchievement`) |
| NFT / collectibles       | 1         | 1 (`GetNftCredits`)                                         |
| Wired                    | 1         | 1 (`WiredSetObjectVariableValue`)                           |
| Camera, crafting, YouTube, SnowWar | — | **0 — every handler is a stub**                        |

This is the "read the body, not the name" rule (`.claude/rules/00-mandate.md`) applied to the other
side of the wire, and it is the second time in one day that counting files instead of reading them
produced a confident wrong answer — see the warning above the message-category tables.

One more thing worth knowing before the camera UI is wired: **the emulator has no header 1985 at
all**, so `RenderRoomThumbnailMessageComposer` is a client-only composer against this server, unlike
the other five which at least have a registered (if inert) handler.

### Full re-measure (2026-08-13)

Every table in this file was re-counted on 2026-08-13 with the standing methodology (primary-tree
module counts, `win63_version` message counts, `node scripts/todo-inventory.mjs` for markers). The
2026-07-28 snapshot had drifted badly — the notable movements:

- **Engine TS 3,482 → 4,540 (+1,058)**, of which the message tree alone is +473 (1,298 → 1,771).
  The summed per-category message gap fell **~711 → 361** (≈340 net of the `moderator`/`moderation`
  naming artefact), with `room` (49 → 0) and `users` (67 → 14) accounting for the biggest drops.
- **Module movements** (TS files): `habbo/ui` 83 → **300** (`widget/` 126 → 247 of 314;
  `RoomDesktop.createWidget()` 33 → 42 wired cases; `furniture/` is 51/54 with only the
  Vimeo/Youtube video pair missing), `habbo/avatar` 85 → **155** (the avatar editor is complete),
  `habbo/catalog` 157 → **206**, `habbo/communication` 1,336 → **1,812**, `habbo/inventory`
  50 → **80**, `habbo/sound` 10 → **32** (`music/`, `trax/`, `furni/` and `events/` all exist now
  and `HabboMusicController` is constructed for real — 2 `TODO(AS3)` left in the module),
  `habbo/help` 27 → **37**, `habbo/quest` 21 → **29**, `core/` 332 → **363**, `core/window`
  232 → **257**.
- **Three "Not started" rows are gone**: `habbo/nux` (5 TS), `habbo/phonenumber` (8 TS) and
  `habbo/userclassification` (2 TS) landed 2026-08-06 (`b5e8bc93`), and the first two are attached
  in `VortexMain` (`IID_HabboNuxDialogs` / `IID_HabboPhoneNumber`) — written *and* wired.
- **Unchanged**: `habbo/game` is still **0/63** with a 122-file message gap — the largest
  fully-absent feature in the port. The `camera` (22) and `crafting` (15) message categories remain
  untouched, as do their widgets (`ui/widget/camera` 0/9, `ui/widget/crafting` 0/13,
  `ui/widget/playlisteditor` 0/9 — the last three widget clusters at zero). Unlistened server
  messages hold at **58** (52 of them also in the AS3 registry, dominated by Game2/SnowWar).

### Daily tasks — the subscriber that was missing (2026-08-12)

`habbo/quest/dailytasks/DailyTasksController` (424 l. AS3) is ported and attached under
`IID_DailyTasks` from `HabboQuestEngine`, plus `ClaimDailyTaskComposer` (4101),
`UnseenDailyTasksCountUpdateEvent` and `IDailyTasksController`.

**The gap was not the messages.** All three server events (1824, 2506, 1065) were already
registered, both DTOs and all three parsers already existed and were byte-correct, and
`SessionDataManager.initSessionData()` already sent `GetDailyTasksComposer` (4100) every session.
What was missing was anything *subscribed*: the board arrived on every login and was dropped. This
is the `[[project_ported_but_unwired]]` failure mode with every piece present but the last one.

Worth keeping:

- **AS3 subscribes its three events in the constructor**, before dependencies resolve, so its own
  `addMessageEvent()` short-circuits on a null communication manager. The port subscribes in
  `initComponent()` instead. That is a deliberate divergence, noted at the constructor: porting the
  order faithfully would have reproduced exactly the bug this class exists to fix.
- **"Unseen" means completed, not new.** The count the toolbar badge reads is of tasks with status
  1 (finished, unclaimed) — a task the player has never opened does not appear.
- **Status names come from behaviour**, and the port already had them right: 0 active (progress bar,
  excluded from `isExpired`), 1 completed (`dailytasks.completed.caption`, claim enabled), 2 claimed
  (`dailytasks.claimed.caption`, claim disabled).
- **`claimTask()` narrows a long to an int.** The task id is `readLong()` in both parsers, but AS3's
  composer declares `param1:int`. Kept — the wire format is what the server parses.

**The five views landed in the same pass**: `DailyTasksView` (338), `DailyTaskView` (285),
`UnclaimedTasksView` (178), `DailyTaskRewardView` (101), `RewardDisplayWrapper` (56). Both layouts
ship (`daily_tasks_xml`, `dailytasks_unclaimed_xml`), so `dailytasks/open` from the toolbar now
opens a real board. `habbo/quest/dailytasks/` is complete at 1,421 AS3 lines.

**One live AS3 crash is fixed rather than reproduced**, and it is worth knowing about because the
shape recurs. `DailyTasksController` declares `_SafeStr_10392:Vector.<_SafeCls_2991>` with **no
initialiser**, and its constructor — which initialises the two fields declared either side of it —
never assigns it. `addTask()` then calls `.push()` on it for any task that arrives expired *and*
completed, which in AS3 is `null.push()`: TypeError #1009, thrown mid-loop inside
`onActiveDailyTasks()`, leaving the rest of the board unloaded. Nothing anywhere reads the list, so
the crash was its only effect. The port initialises it to `[]`: the branch stays faithful and stops
being a crash. Compare `[[project_as3_foreach_null_noop]]` — same root cause, opposite direction
(there AS3 tolerated null and TypeScript did not).

The toolbar badge was wired on 2026-08-13: `HabboToolbar` now attaches both
`UnseenAchievementsCountUpdateEvent` and `UnseenDailyTasksCountUpdateEvent` to its quest-engine
dependency, exactly as AS3 does, and `BottomBarLeft` gained the three counters that were missing
(`unseenDailyTasksCount`, `unseenRewardTrackRewardsCount`, `unseenProgMenuCount`). The achievements
badge had been emitting into nothing since it was ported; both now reach the PROGRESSION icon.

### Collectibles — the product previewer (2026-08-12)

Third collectibles pass. `CollectibleProductPreviewer` (234 l.), `EffectPreviewer` (100 l.),
`ICollectibleProductPreviewer`, the two remaining renderer wrappers (`CollectionItemWrapper`,
`MintableItemWrapper`), and real `previewIcon()`/`previewImage()` on `CollectiblesController`.

Four things worth keeping:

- **The controller now depends on the catalog that builds it.** `HabboCatalog`'s constructor
  creates `CollectiblesController` and attaches it *before* the catalog itself is attached under
  `IID_HabboCatalog` on the next line. That resolves because `ComponentContext.attachComponent()`
  does late binding — `_interfaceQueues` plus the unlock handler — and because AS3 declares the
  dependency optional, as this port now does. A required one there would have locked the component
  forever with no log.
- **The preview switches are rebased by one.** Both methods switch on `productTypeId + 1`, which is
  AS3's `productTypeId - -1` — a decompiler artefact of a switch the compiler rebased, kept rather
  than folded so the file still lines up with `getProductName()`.
- **`tempCategoryMapping()` is AS3's name and AS3's hard-coded table.** Only its `"I"` path is
  called and only its `=== 1` result tested, so the three ids it special-cases (3001, 3002, 4057)
  are exactly the wall items that get no preview.
- **Two branches are deliberately not ported.** The chat-style preview needs a `selectorPreview`
  the port's `IChatStyleLibrary` does not expose (icon) and a whole `PooledChatBubble` rendered to
  a BitmapData (image). And `handlePreviewImageEasterEgg()` is left out on purpose: it *sends a
  message* (`wf15`) on a trigger built from three obfuscated fields, and guessing that is not worth
  a packet.

`AbstractCollectibleItemRenderer` (204) and `CollectiblesRewardBoxView` (204) landed next, which
closes `showLootBoxReward()`: opening an NFT reward box now shows what came out, queued one popup
at a time.

`CollectiblesView` (582), `RewardClaimsTab` (329) and `RewardCollectibleItemRenderer` (148) landed
as the first tab slice, so `collectibles/open` now opens the real hub: eight tabs, the collector
score header, the currency balances, the wallet list — and one working tab.

`TransferNftsTab` (320) followed as the second tab — no renderer of its own — which closed the
hub's `onSilverBalanceUpdated()` hand-off and the non-Stardust wallet subset.

`ShopTab` (497) + `ShopCollectibleItemRenderer` (108) + `ShopNavigationNodeRenderer` (173) +
`NftStorePurchaseOffer` (178) landed as the third tab — the NFT store, with its category list,
offer grid, large preview and purchase path.

`MintInventoryListTab` (769) + `MintInventoryItemRenderer` (104) + `MintTokenPurchaseOffer` (166)
followed — **four of the five tabs now work**. That slice also added `getNonRentedInventoryIds()` to
`HabboInventory` and its interface (a real AS3 member that was simply missing), and gave
`CollectiblesController` its `IID_HabboInventory` dependency, so
`itemAdded`/`itemRemovedFromInventory` and `onInventoryInitialize` are wired instead of empty.

`CollectionsTab` (627), `CollectionView` (596), `CollectibleItemRenderer` (101),
`CollectionsNavigationNodeRenderer` (221), `CollectionProgressColor` (85) and
`RenderableShopNftItem` (60) closed it.

**`habbo/catalog/collectibles` is complete** — 29 AS3 files, 29 TS files, all five tabs live. With
`habbo/inventory/collectibles` and the message layer, the whole collectibles subsystem is ported:
~6,850 AS3 lines in the catalog half plus ~1,500 in the inventory half.

Four of the five `_SafeCls_*` names were derived and are marked as such: `ICollectorHub`,
`ICollectibleProductPreviewer`, `CollectibleRarity` and `CollectionProgressColor`. The rest of the
package was unobfuscated.

**Three AS3 fields in this package are declared, read, and never assigned**, so their branches are
dead in Flash: `ShopTab._SafeStr_5458` (a `CollectionView`, read only by `update()`),
`DailyTasksController._SafeStr_10392` (which additionally *crashes*, see the daily-tasks entry) and
`ShopTab`'s trailing `dispose()` block, which disposes a field `clearNavigationList()` has already
nulled. Each is documented at its port site rather than reproduced. All four are TODOs at their construction
sites in `CollectiblesView.initWidgets()` and `refresh()`, so their containers come up empty rather
than the window failing to build.

One thing to know before porting them: **two of the four concrete renderers must assign their
fields before `super()`.** `ShopCollectibleItemRenderer` and `RewardCollectibleItemRenderer` read
their own state from `updateVisuals()`, which the base constructor calls — legal in AS3 outright,
legal in TypeScript only via `declare`. `RewardClaimsTab.createRewardItem()` also calls
`updateVisuals()` a second time explicitly, for exactly that reason.

### Collectibles — the catalog message layer and the hub (2026-08-12)

The second collectibles pass, after the inventory tab above. What landed:

- **The whole message layer**: 18 incoming events + parsers, 8 DTOs, 15 outgoing composers, all
  registered. Unlistened server messages **78 → 58** — the single largest drop this file records.
- **`CollectiblesController`**, as a real DI component attached under `IIDCollectiblesController`
  from `HabboCatalog`'s constructor, exactly where AS3 attaches it. Its *hub* half is complete
  (`getProductName`/`getProductType`, the two reward-box messages, send/subscribe); its *view* half
  is TODO at each entry point.
- **`HabboCatalog.collectorHub` is no longer null**, which closed the two `FurniModel` TODOs and
  turned the inventory tab's product-code fallback into a real name lookup.

Still open: the collectibles **catalog UI** — `CollectiblesView` (582 l.) and its five tabs
(`MintInventoryListTab` 769, `CollectionsTab` 627, `ShopTab` 497, `RewardClaimsTab` 329,
`TransferNftsTab` 320), plus `CollectionView` 596, `CollectiblesRewardBoxView` 204 and the renderer
tree. ~4,200 of the subsystem's 6,851 lines.

Four things worth not rediscovering:

- **`win63_version` had the names, and I derived them instead.** CLAUDE.md says to cite that tree
  for names — it is the only one where messages keep readable *filenames* — and this pass derived
  16 composer names from the emulator's constants first. 13 matched. Three did not:
  `NftTransferAssets` (not `TransferNfts`), and the two
  `NftCollectiblesClaim{Bonus,Reward}Item`. Same on the incoming side: 15 of 18 matched, and the
  three that did not include **`CollectableMintableItemTypes`** — "Collectable", a typo in the
  original source that the handler name `onCollectableMintableItemTypesMessage` confirms. Check the
  filenames *before* deriving, not after.
- **Two parsers share obfuscated constants, with the values swapped.**
  `NftStorePurchaseMessageParser` and `CollectibleMintableItemResultMessageParser` both declare
  `_SafeStr_10258` and `_SafeStr_8683` (footers: `"_-HE"`, `"_-t1w"` in both), but 10258 is 1 in one
  and 0 in the other. `HabboCatalog.as::onNftStorePurchase()` raises its error alert on 10258, so
  10258 is the failure constant in both — and the mint parser's **0 is a failure**, the opposite of
  every sibling. Reading either file alone gives the wrong answer.
- **`CollectableMintableItemTypesMessageParser` accumulates on purpose.** Its `flush()` returns
  `false` (alone in the package) and its `parse()` does not reset the list, so successive messages
  concatenate. Both are AS3's; "fixing" either silently changes what the mint tab shows.
- **The rarity colour table is dead code in Flash too.** `getRarityColor()` upper-cases its input
  and looks it up in a lower-case table, so every rarity resolves to the grey fallback. Ported as
  written, with the table kept so the intended colours are recorded.

Three more emulator headers are wrong and are corrected in the working tree of
`Revision20260701/Headers.cs` (uncommitted — see the note under the inventory section):
`NftStorePurchaseMessageComposer` 3171 → **448**, `RedeemNftLootBoxResultMessageComposer`
2262 → **3332**, `RedeemNftLootBoxStateMessageComposer` 2857 → **3164**. None of 3171/2262/2857
appears in the WIN63 registry. Note 3332 is legitimately used in *both* directions there
(`_composers[3332]` is RenderRoom, `_SafeStr_4546[3332]` is the loot-box result) — the two tables
are independent, so that is not a collision.

### Collectibles — the inventory tab (2026-08-12)

`habbo/inventory/collectibles` is ported and wired: `CollectiblesModel`, `CollectiblesView`,
`CollectiblesGridView`, `CollectibleGroupedItem` (the four AS3 classes, ~1,500 lines), plus the
messages the tab needs — `NftAssetsMessageEvent`/`Parser` (2247), `TradeNftAssetsMessageEvent`/`Parser`
(850) and `AddNftToTradeComposer` (2481). `collectibles` is now an `_inventories` entry, and
`TradingModel`'s two NFT maps are typed `OrderedMap<string, CollectibleGroupedItem>` instead of
`unknown`.

What this closes and what it does not: the tab is the *inventory* half. The **catalog** half
(`habbo/catalog/collectibles`, ~4,900 lines across 29 classes — the collections/shop/mint/transfer
tabs and their renderers) is still untouched, and it owns `CollectiblesController`, which is what
`HabboCatalog.collectorHub` would return. That accessor stays null, typed now (`ICollectorHub`)
rather than `unknown`.

Four things worth not rediscovering:

- **The models were never being told.** `inventoryViewOpened()`, `closingInventoryView()` and
  `toggleInventorySubPage()` hand-dispatched to furni/pets/bots where AS3 loops every registered
  `IInventoryModel`. Registering the collectibles model would have changed nothing without this:
  `CollectiblesModel.categorySwitch()` is what sends `RequestNftAssetsComposer`, so the tab would
  have opened empty forever. The loop also reaches trading, wired_trading, recycler and marketplace,
  none of which had received these three callbacks either.
- **`tradingModel` was narrowed to the view's interface.** AS3's getter returns the concrete
  `TradingModel`; `ITradingModel` is the four-member contract the trade *view* uses. The port
  returned the interface, which put `running`, `ownUserNftItems` and `requestAddNftsToTrading()` out
  of reach of every model-side caller. Widening the getter to the AS3 type is source-compatible —
  the narrow interface is a supertype.
- **The lock polarity is inverted, on purpose.** `CollectibleGroupedItem._assetIds` maps
  assetId → *locked*, and `lockAsset(id, unlock)`'s second parameter is the state to move away from,
  so `lockAsset(id)` locks. `CollectiblesModel.updateItemLocks()` then calls `findGroupedItem(item,
  **false**)`: by then the copy is already in the trade, and the default "still holds this unlocked
  copy" test would reject the very group that needs locking.
- **One live AS3 bug, ported as written.** `CollectiblesModel.resetUnseenItems()` clears unseen
  category 7 (COLLECTIBLES) while `isUnseen()` queries category 5 (BOT). Nothing calls `isUnseen()`
  — not here and not in AS3 — so correcting it would invent behaviour the Flash client never had.

Two emulator headers were wrong and are fixed in `Revision20260701/Headers.cs`:
`TradeNftAssetInventoryMessageComposer` 3854 → **2247** and `TradeNftAssetsMessageComposer` 2159 →
**850**. Neither 3854 nor 2159 appears anywhere in the WIN63 registry; 2247 and 850 both do. Both
constants were header-only — no composer, no serializer, no handler references them — so the change
is to the table alone.

### The vault and habbicons (2026-08-16) — `habbo/catalog` 44/58 → 53/58

Two catalog subsystems that were entirely unported. Between them they closed **9 of the module's 13
missing subscriptions**; `sweep-unwired.mjs habbo/catalog` now reads 53/58.

**The vault** (`habbo/catalog/earnings`, 643 l. AS3). `EarningsController` + `EarningsView`, the
three `IncomeReward*` messages (3976/2984/1914, names recovered from vortex-emulator whose ids match
the registry), and one line in `PurseAreaExtension`.

That last line is the point. The port already had `getEarnings()` — returning a one-property
stand-in whose `showingIndicator` nothing ever set — and `refreshIndicators()` matched it by
assigning `visible = false` unconditionally. Complete-looking code on both sides of a value that
could never be true. `ICatalogEarnings` had exactly the one member `IEarningsController` has, so the
real controller dropped straight into its place.

The shipped `vault_view_xml` predates the 2026 tree: it has no `vaultWithdraw_button` or
`vaultOpenShop_button`, so those two `windowProcedure` cases are inert against this asset. Every
other name the view looks up is present.

**Habbicons** (`habbo/catalog/habbicons` + `habbo/habbicons/assets`, 23 files / ~6,000 l. AS3).
The hub window, its six child views, the tile popup, the purchase confirmation, the controller, and
the asset manager that fetches `habbicons.json` and two spritesheets from a configured root.

This is **the one subsystem where the source-of-truth chain runs out entirely**. Habbicons postdate
every unobfuscated tree, `win63_version` has none of them, and vortex-emulator carries no habbicon
header of any kind — 0 matches for `habbicon` in `Revision20260701/Headers.cs`. So all twelve ids
come from WIN63's registry alone (checked free of collisions), and every message name is *derived*
from the controller's use of it, flagged as such at each declaration. Nothing server-side will ever
send these; the client half is complete regardless.

One recovery did come out of it. `UnseenItemCategory`'s eighth constant was left unnamed with a note
saying nothing referenced it — `HabbiconController` references it five times
(`setUnseenItem`/`isUnseen`/`removeUnseen`/`resetCategory`/`getCount`), which fixes what 8 is. It is
`HABBICON`, recovered from use rather than invented. **The note was written when it was true and
went stale silently**, which is the same failure mode as the vault's indicator: a claim about the
codebase with no test behind it.

Two `TODO(AS3)` in `HabboCatalog` named the habbicon gap by file. Both are closed.

Still open in `habbo/catalog`, all four measured: 448 `NftStorePurchase` (class registered, never
constructed), 2901 `LtdRaffleEntered` (no id in the TS registry — message unported), and 3404/3599,
which are subscribed elsewhere but not by the catalog itself. 3599 belongs to `VideoOfferManager`,
which is in the `offers` slice still to come.

### Chat slash commands (2026-08-12)

`ChatInputWidgetHandler` now recognises the full set: expressions, moderation, room ops, the wired
menu, navigation. Three dispatch details are load-bearing and worth not rediscovering:

- **Not every command swallows the message.** AS3 alternates `return null` with `break`, and the
  fall-through cases are deliberate — `:d` plays the laugh *and* says ":D", and the `:crashme`…`:q`
  block exists purely so the text reaches the server, which handles them. The port carries this as a
  boolean return, including per-branch: `:kiss` swallows for a VIP and speaks for everyone else.
- **`:kick` and `:mute` test `!hasSecurity(4)`.** Reads like an inverted condition; it is not. Staff
  fall *through* so their line reaches the server's own command parser rather than the client's.
- **A bare `x` argument means the selected avatar**, substituted into the sent text as well, so
  `:ejectall x` reaches the server with a real name.

Blocked, one marker each: `:screenshot` and `:hidemouse` (unported `IRoomEngine` members), `:csmm`
(no game manager on the widget container), `:fps` (a Flash stage property with no engine-side
equivalent — the PixiJS ticker lives in the client package), and `:ping`'s latency figure
(`IHabboTracking.latencyPingMs`; it dispatches with AS3's own -1 fallback meanwhile).

### The recycler (2026-08-12) — one missing file, four TODOs, three of them wrong

`habbo/inventory/recycler/RecyclerModel` was genuinely absent, and it is 200 lines. Everything the
four surrounding TODOs named as blocking it was already in the tree:

| TODO said | Reality |
|---|---|
| `habbo/catalog/recycler` not ported (`InventoryMainView.emptyFurnimaticSlots`) | `IRecycler`, `FurniSlotItem`, `RecyclerLogic` all present; `HabboCatalog.getRecycler()` already returned one |
| recycler/trading branches unwired (`FurniModel.requestCurrentActionOnSelection`) | both targets existed |
| recycler *and marketplace* contribute nothing to `updateItemLocks()` | `MarketplaceModel` was being constructed and simply never added to `_inventories`, so `getOfferItemRefs()` had no caller |
| the catalog cannot reach the inventory | **true** — the only real one |

That last one is worth keeping in mind for the rest of the port: `HabboCatalog` had no
`IID_HabboInventory` dependency, so `setupInventoryForRecycler`, `requestInventoryFurniToRecycler`,
`returnInventoryFurniFromRecycler` and `get tradingActive` were all stubs. It has to be declared
**optional**, as AS3 declares it: `HabboInventory` requires the catalog, so the pair is a cycle and
the optional side is the only thing breaking it. `Component.injectDependency()` honours that —
optional deps skip `_requiredDependenciesCount` and resolve later — which is what keeps the catalog
from locking forever (see the DI note in `docs/CONTEXT.md`).

Registering `marketplace` and `recycler` in `_inventories` is also what makes
`getCategoryWindowContainer()` and `updateView()` resolve for those tabs at all.

### Wired trading (2026-08-12) — logic complete, views deliberately not

`inventory/wired_trading/` landed in four slices: the requirement DTOs, the message layer, the two
models, and the event subscription. What is **not** there is either view — `WiredTradingView` (447
lines), `WiredTradeRequirementsView` (418) and its three sub-views under `requirements/offerings/`
(717). Each model talks to an interface with a stub that logs `warn` on every call, and
`getWindowContainer()` returns null, so the tab has no contents and the state machine runs headless.

That is a decision, and the reason is checkable: **none of the seven wired-trading headers exists on
the reference emulator** (3650/1481/2137/2488 in, 3111/2818/2646 out). Nothing here can be exercised
end to end, so 1,684 lines of view would have been written blind against a server that will never
send to it.

Three things this slice is worth remembering for:

- **A name was occupied.** The port shipped a `TradeRequirementRule` that was really `_SafeCls_4486`,
  a constants holder — while `TradeRequirementRule.as` is a real, unobfuscated AS3 class in the same
  package. Renamed the constants to `TradeRequirementRulesType` (DERIVED) and gave the real class
  its name. Worth checking for elsewhere: a derived name that collides with a readable AS3 one makes
  the real class unportable and the collision is invisible until you try.
- **A DERIVED name can be upgraded by a later call site.** The four `TradeRequirement` constants
  shipped as `TYPE_0/1/2` with a note refusing to guess; porting `canOfferFurni()` settled all four
  from behaviour (`CF_`-prefixed = credit furni, two types exist to include or exclude it), so they
  are now named and marked derived-from-behaviour rather than recovered.
- **Registering an event is not subscribing to it.** The four events sat in `HabboMessages` for one
  commit with nothing listening, which would have left the whole model unreachable. Connecting them
  also caught a wrong key type on `updateItemGroupMaps` — the compiler only objects once a caller
  exists.

Every AS3 `_inventories` entry is now registered (`collectibles` landed 2026-08-12, below).
Badges and effects are the remaining exception, and a different one: AS3 registers them too, but
neither model implements `IInventoryModel` in this port, so neither is in the map.

The layout/skin rows changed **format**, not content: there is no JSON compile step any more —
`tools/build-window-assets.mjs` ships the dump's XML verbatim (see CLAUDE.md → Assets). Both
directories are gitignored and rebuilt from the dump, which is why hand-authored files live in
`src/vortex-layouts/` and `src/vortex-skins/` instead.

`TODO(AS3)` rising from 265 to 327 is not regression: the ports landed since 2026-07-19 are large
subsystems (wired/roomevents, the quest UI, the wired menu) whose deliberately-deferred branches are
tagged rather than silently omitted, as `.claude/rules/30-as3-traceability.md` requires. They sit in
143 files; the five densest are `HabboCatalog.ts` (30), `RoomEngine.ts` (17),
`inventory/furni/FurniModel.ts` (10), `roomevents/wired_setup/UserDefinedRoomEventsCtrl.ts` (8) and
`ui/widget/infostand/InfoStandUserView.ts` (7).

### TODO inventory — re-measure recipe

The counts above were hand-typed, which is why they were only ever a snapshot. `node
scripts/todo-inventory.mjs` re-measures them, classified by **what actually blocks each marker**
(the blocker is usually named two lines below the word TODO, so it reads the whole comment block,
not the marker line). It is read-only.

```bash
node scripts/todo-inventory.mjs             # summary + per-module + hottest files
node scripts/todo-inventory.mjs --stale     # markers whose blocker may have landed since
node scripts/todo-inventory.mjs --module habbo/catalog
node scripts/todo-inventory.mjs --json
```

`packages/*/dist` is excluded: it is gitignored build output carrying a duplicate of every `.d.ts`
marker, which inflates a naive `grep -c` by ~90.

**2026-08-13, at the full re-measure — 380 TODO in `packages/*/src` (323 `TODO(AS3)`, 57 plain),
29 flagged stale.** Flat against 2026-08-11 while the engine gained ~1,000 files, which is the same
effect as below one more time: `habbo/ui`'s +217 files and the message tree's +473 closed whole
subsystems nobody had written a marker against. By blocker: 221 local micro-gaps, 91 blocked on an
unported module, 30 wire/server, 22 Flash-only, 16 decided-against. Hottest modules: `habbo/catalog`
63, `habbo/ui` 54, `habbo/roomevents` 46, `habbo/room` 41, `habbo/inventory` 30.

**2026-08-11, after the catalog and guide passes — 378 TODO in `packages/*/src` (323 `TODO(AS3)`,
55 plain).** Three subsystem ports (room ads, targeted offers, `GuideSessionController`) moved the
count by five, which is exactly the point: a subsystem port closes far more than it has markers
for, because nobody writes a marker for a state machine that does not exist. `habbo/help` itself
went 10 → 9 while gaining a working guide tool.

**2026-08-10, after the help/CFH pass — 387 TODO in `packages/*/src` (332 `TODO(AS3)`, 55 plain), from 382:**

The count is worth watching across the pass rather than at its end, because it moved in both
directions for the same reason:

| Point in the pass                             | `TODO(AS3)` |
|------------------------------------------------|-------------|
| Start                                          | 328         |
| After the wire fixes and the marker rewrites   | **336**     |
| After the three window ports                   | **332**     |
| After `TopicsFlowHelpController`               | **329**     |
| After the room-ad and targeted-offer ports     | **324**     |
| After `GuideSessionController`                 | **323**     |
| After the five server-side wire gaps           | **320**     |
| After the pending guide ticket                 | **317**     |

It rose first because the pass kept finding behaviour missing with *no* marker on it —
`log.debug('Show guide tool')` is not a documented gap, it is an invisible one — and every stub it
touched was given a marker naming the class it waits on and a `warn` instead. Then it fell as
those same named classes were ported: `ChatReportController`, `ChatReviewReporterFeedbackCtrl`,
`HelpController` and `TopicsFlowHelpController`, ~3,000 lines of new TypeScript, closed the markers
the first half had opened.

**Net +1 across the whole pass**, over a module that went from receiving 21 messages and answering
every one with a `log.trace` to a working report flow. `habbo/help` itself went 18 → 11 markers
with **zero micro-gaps left**: everything remaining is a whole unported module, a settled decision,
a Flash-only feature, or a missing header. Judge a pass by which markers it closed and what started
working, never by the delta.

<details><summary>2026-08-10 — 382 TODO (327 <code>TODO(AS3)</code>, 55 plain), from 422 at the start of the day</summary>

| Blocker                                                     | Count |
|-------------------------------------------------------------|-------|
| Local micro-gap, no blocker cited                            | 217   |
| A whole unported module                                      | 99    |
| Wire / server (composer, parser, header)                     | 33    |
| Flash-only (BitmapData, filter, shader, Timer)               | 19    |
| Settled decision — will not be ported (legacy, dead, moot)    | 15    |

</details>

<details><summary>2026-08-09 — 425 TODO (368 <code>TODO(AS3)</code>, 57 plain), from 502</summary>

| Blocker                                                     | Count |
|-------------------------------------------------------------|-------|
| Local micro-gap, no blocker cited                            | 245   |
| A whole unported module                                      | 107   |
| Wire / server (composer, parser, header)                     | 35    |
| Flash-only (BitmapData, filter, shader, Timer)               | 20    |
| Settled decision — will not be ported (legacy, dead, moot)    | 19    |

</details>

**Read this number carefully.** The catalog placement pass the same day closed nine markers and
opened three new ones, for a net of three — while turning a dead subsystem into a working one. The
count measures *how much is flagged*, not how much works: a pass that replaces a vague marker with a
precise one barely moves it. Judge a pass by which markers it closed, not by the delta.

**`RoomPreviewer` pass (2026-08-09, 14 → 1 marker).** Nearly every marker in the file said some variant of "IRoomEngine exposes no X". Checking each against the engine first — before reading the AS3 — split them three ways, and the split is the reusable lesson: `updateObjectRoom()` / `updateObjectRoomVisibilities()` were **already on the interface** (pure staleness); `updateObjectUserAction()` and `updateObjectWallItemLocation()` existed on `RoomEngine` but had never been declared on `IRoomEngine` (one-line fixes); only `isInitialized`, `runUpdate()`, `disableUpdate` and the engine-side `changeObjectState()` were genuinely unported, and all four are small. `getBoolean` was also added to `IRoomEngine`: AS3 reaches the engine's config by casting it back to its Component base (`(_roomEngine as _SafeCls_50).getBoolean(...)`), which a TS interface cannot express.

`changeObjectState` deserves its own note. A name-based grep found four hits on `RoomEngine` and suggested it was already there; the body showed those belong to `class_1947`'s method — the room-object *event* handler that asks the server to toggle a real furni — which this port flattened into `RoomEngine` under the same name. The engine's own `changeObjectState()` (cycle a preview's automatic state locally, no server round trip) was missing entirely. The flattened one is now `sendObjectStateChange()`, with its AS3 trace unchanged.

What this restored: furniture and wall-item rotation in the catalog/inventory preview (both were hard `return false`, so the rotate buttons were permanently greyed), the automatic state cycling that animates a previewed furni every 2.5 s, wall/floor visibility, engine-freeze during rebuilds, and the immediate engine pass after a rotation. **One behaviour change to watch:** `isZoomEnabled()` returned a hard-coded `true`; it now reads `zoom.enabled` off the engine and defaults to false when unset, matching AS3 and the port's other `zoom.enabled` call sites — that selects the room-geometry zoom path instead of the canvas-scale one.

### The help/CFH pass (2026-08-10) — four field names the wire disagreed with

`habbo/help` was the "ported but never wired" pattern at full size: `HelpMessageHandler`
subscribed **21** incoming messages and answered every one with a `log.trace`. Reporting a photo,
a selfie, a room, a thread or a message reached `HabboHelp`, logged a line and stopped — nothing
was sent, and nothing the server sent back was shown. `CallForHelpManager` was 286 lines of
field-setters against 697 lines of AS3, with no reference to `HabboHelp` at all, so it *could not*
send even if asked.

**The wire fault, found before it shipped.** `CallForHelpFromSelfieMessageComposer` named its five
fields `(message, topicId, reportedUserId, photoId, roomId)`. Four are wrong, and the types
(`string, int, int, string, int`) lined up well enough that nothing ever failed to compile. Three
independent sources agree on the real order:

| Source | What it says |
|---|---|
| `CallForHelpManager.as::reportSelfie()` | shuffles on the way out: `new _SafeCls_3116(p1,p3,p4,p2,p5)` |
| `ExternalImageWidget.as:690` (its only caller) | passes `(shareUrl, description, activeRoomId, creatorId, objectId)` |
| `vortex-emulator` `CallForHelpFromSelfieMessageParser.cs` | pops `url, roomId, photoAuthorId, message, furniId` |

Resolved: `(extraDataId, roomId, photoAuthorId, message, roomObjectId)`. There is no `topicId` on
this message at all — the selfie report carries free text where the photo report (1964) carries a
topic id. The same three-position correction applies to `reportSelfie()`/`reportPhoto()` on
`IHabboHelp`, `HabboHelp` and `CallForHelpManager`: what the port called `userId` is the room id,
what it called `roomObjectId` is the photo's author. **Positions were already faithful at every
call site, so no bytes changed** — the fix stops the next caller from reading the names and
building a wrong packet. Two AS3 call sites for the photo variant genuinely disagree with each
other (`CallForHelpManager.reportPhoto()` vs `TopicsFlowHelpController.as:500`); the emulator's
parser settles it in favour of the latter, which is what the port already had.

**Header 1850 had no composer here.** It is `GuideAdvertisementRead`, sent by
`queryForGuideReportingStatus()` alongside 2455 and separately by `TalentTrackController`. Name
from `win63_version` (the only tree where it has a readable filename), header from the primary
registry, both corroborated by the emulator, which routes it to a real handler.

**`simpleAlert()` had two parameter names swapped.** `IHabboWindowManager.simpleAlert()` declared
`(title, message, subtitle, …)` but forwards positionally to `SimpleAlertDialog(title, subtitle,
message)`, which is what `HabboWindowManagerComponent.as` does. Every call site in the port
already passed AS3 order — `HabboFriendList` even carried a comment working around the names — so
renaming them changed nothing and removed a standing trap.

**`ICoreWindowManager` had drifted in three places** and nothing noticed because nothing
implements it (`HabboWindowManager` satisfies the parallel `IHabboWindowManager` instead):
`confirm()` declared the callback third and the flags fourth, the reverse of the source and of
`IHabboWindowManager`; `buildFromXML()`'s third parameter was typed `Map<string, IWindow>` and
named `namedWindows` when AS3's is the `<var>` substitution map; `getDesktop()` returned `IWindow`
where AS3 returns `IDesktopWindow`, already ported. `confirmWithModal()` was missing entirely — 10
of the source's 11 members.

**Two TODOs blamed the parser and were right, but about the wrong field.**
`ExtendedProfileWindowCtrl` hard-coded `hidden_icon.visible = false`, saying the hidden state
"isn't in the parsed `ExtendedProfileData`". It is on the wire: AS3 reads field 10 with
`readByte()` into a tri-state `onlineStatus` (0 offline / 1 online / 2 hidden) and switches all
three icons off it, while this port read the same byte with `readBoolean()`. Since `readBoolean()`
is literally `readByte() !== 0` in this port's `ByteArray`, the two consume the same byte —
reading it as a byte cannot desync anything. Separately, `_unknownBoolean1` turned out to be
`isHidden`, a *different* field that both AS3 and the emulator (`[Id(13)] IsHidden`) already
carried under that name.

**All five were fixed on 2026-08-11** (`vortex-emulator` commit "the four profile fields and two
badge fields the client already reads", plus this port's "the four profile fields, the two badge
fields, and header 1834"). Each needed both sides in one change — reading a field the server does
not write runs off the end of the packet, which is why every one of them had sat open rather than
being half-done. Two of the five turned out to be worse than recorded here: the missing profile
fields **truncated the packet**, and the two-field badge list **desynced** as soon as a player had
more than one badge, because the next slot id came out of the middle of the previous string. Wire
tests now lock both layouts, including the empty-list cases.

The two header lines (848 and 1834) are in that checkout's working tree but **not committed** —
`Headers.cs` also carries an in-progress navigator change that was not mine to stage, so they need
splitting out by hand (`git add -p`).

The table below is kept as the record of what was wrong:

| Gap | Client side | `vortex-emulator` side |
|---|---|---|
| Profile badge count / rank | parser stops after 19 fields | `ExtendedProfileMessageComposer.cs` declares 19 where AS3 reads 23 — missing `totalBadges`, `achievementLevel`, `badgeRarityCounts`, `totalBadgesRank` |
| Profile "hidden" icon | **done** — tri-state now read | `IsOnline` is a plain `bool`, so state 2 is unsendable |
| Badge rarity at login | parser matches the emulator | `BadgesEventMessageComposerSerializer.cs` writes 2 fields per badge where AS3 reads 4 (`ownerCount`, `badgeRarityId` missing) |
| `requestReportsStatus()` | no composer | header 1834 is in the primary registry and **not** in `Headers.cs` |
| `GetNextTargetedOffer` | sends on **848**, per WIN63's registry | `GetNextTargetedOfferEvent = 9004`, a placeholder its own comment calls unresolved — so the request reaches nothing and **no targeted offer can ever arrive**, however complete the client is |

The emulator was left untouched all pass: its working tree had unrelated navigator work in
progress, and staging someone else's changes there is the one thing that must not happen.

The last row is the only one of the five found by *confronting two registries* rather than by
reading either — the same method that produced every other real defect in this pass. It is also
the one where the temptation to "fix" the wrong side is strongest: changing the client to 9004
would make the two agree and the feature would still be wrong, because 848 is what the real client
sends.

**Badge metadata was a false blocker.** `BadgesModel.updateBadge()` said `Badge` had "no
rarity/ownerCount fields at all yet (a separate, unported badge-rarity feature)". Nothing was
unported — `BadgeReceivedEventParser` read both integers, the emulator wrote them, and
`BadgeRarityEnum.isStandaloneTier()` was already in use elsewhere. The two values were parsed,
handed to `HabboInventory`, and dropped one call short of the model. Threading them through
unlocked `refreshAvailableRareBadgeRarityIds()` and the three rarity accessors that decide which
filters the badge grid can offer.

**Then the windows, which is where the line count went.** The pipeline half could ask the server
whether the user had reports open and then had nowhere to go. Three controllers ported, ~1,700
lines:

| Class | AS3 lines | What it unblocks |
|---|---|---|
| `ChatReportController` | 337 | the chat-line picker a report is filed against |
| `HelpController` | 271 | `main_help` and the new-user tour popup |
| `ChatReviewReporterFeedbackCtrl` | 117 | the post-report feedback panel |

plus `showEmergencyHelpRequest()`, `showPendingRequest()`, `showAbusiveNotice()`, the user list,
the panel swap and the five window procedures on `CallForHelpManager` itself.

`HelpController` needed `getModalXmlWindow()`, which the pass had listed as blocked because
`buildModalDialogFromXML()` takes raw XML and the manager's name→XML map is private.
`buildModalWidgetLayout()` is the modal twin of `buildWidgetLayout()` — the two steps every AS3
component does inline inside its own `getModalXmlWindow()`.

Two AS3 oddities were ported as written rather than tidied, both with a comment saying why:
`saveEmergencyHelpRequestData()` tests `_reportType != 8 && _reportType == 7` in a branch the line
above has already returned on, so its left half is dead in AS3 too; and the tour popup's analytics
carry `showTime - now`, which is negative — the sign is what the backend receives.

**Then the new CFH flow.** `TopicsFlowHelpController` (933 lines) is one modal holding nine
containers shown one at a time, and it is not a linear walk: five entry points open it at five
different steps, and `updateBackButtonVisibility()` recomputes from the entry point rather than
from history, so a flow never offers to step back behind where it began. That is what the report
type is kept on the controller for. Its single exit picks a composer from that same type, with one
branch that is not about type at all — a bullying topic with `guides.enabled` and
`guardians.enabled` files a guide report instead.

Porting it closed eight members that had been stubs earlier the same day:
`toggleNewHelpWindow()`, `reportUser()`, `reportUserName()`, `reportUserFromIM()`, `reportRoom()`,
`reportThread()`, `reportMessage()` and `startPhotoReportingInNewCfhFlow()`. `reportRoom()` is
worth singling out: it had been routed through `CallForHelpManager.reportRoom()`, the older
pending-calls path, where AS3 sets the fields directly and opens the new flow. Two different
journeys under one name.

Two AS3 idioms kept with a comment rather than tidied: the reason list is emptied by setting its
height to 0 and straight back, which is how this window system is made to re-measure and reads as
dead code; and a clicked chat row is resolved with `is ITextLinkWindow` / `is _SafeCls_1857`, which
TypeScript cannot express for an interface, so the port tests for the members instead.

**Then the last three windows.** `HabboWayController` (152) and `SafetyBookletController` (163)
are page-turners, kept as two classes because AS3 keeps them apart and the differences are real:
the Habbo Way's page count is a hotel config value where the booklet's is a constant, the booklet
logs every page turn to the talent-track event log under "Quiz" because it is the reading half of
the safety quiz, and the booklet has two closing panels for hotels that switch the quiz off. Both
track the page turn *after* moving, so the analytics label carries the page landed on — that reads
like an off-by-one and is not. `WelcomeScreenController` (225) is the onboarding bubble: an
update-loop receiver that eases itself to a toolbar icon by halving the remaining gap each tick and
unregisters the moment it arrives.

### The layout name that made sixteen windows unreachable

Worth its own heading, because nothing in the toolchain caught it and it invalidated every window
above until it was fixed.

AS3's `getXmlWindow(name)` appends `"_xml"` before the lookup —
`assets.getAssetByName(name + "_xml")` — because an asset's real name is its `*Com.as` field name
and those carry the suffix. This port's `getXmlWindow()` passed the caller's short name straight
through. Layouts are registered under the shipped **file basename** (`App.ts` →
`parseWindowLayoutXml`, which names each entry after the file), so `chat_report_xml.xml` is keyed
`chat_report_xml` and a lookup for `chat_report` finds nothing: the null guard logs, no window
appears.

That was all sixteen: `chat_report`, `chat_report_item`, `emergency_help_request`, `bully_report`,
`pending_request`, `abusive_notice`, `main_help`, `welcome_tour_popup`, `topics_flow_help`,
`chat_review_reporter_feedback`, `habbo_way`, `safety_booklet`, `welcome_screen` and the four
`pending_*` views. All shipped, none reachable. **`tsc`, `pnpm build` and the boot check stayed
green throughout**, because none of these windows opens at boot — the failure surfaces only when a
user clicks Help.

It was found by asking a different question than "does it compile": *what key does the registry
hold, versus what key does the caller pass.* The two had never been compared. `resolveLayoutName()`
now tries the suffix first and falls back to the bare name — the fallback is load-bearing, because
a minority of layouts ship without it (`club_required.xml`), which is why
`HabboGroupsManager.getXmlWindow()` works while passing its name through unchanged.

This is the layout twin of the image-asset trap already recorded above: same cause, opposite
answer. Images drop `_png`; layouts keep `_xml`. Never assume either — check the shipped directory
for the exact filename.

**Then both quizzes.** `HabboWayQuizController` (387 lines) is one window serving the Habbo Way
quiz and the safety quiz — they differ in a quiz code, a set of illustrations and a localization
prefix, and share the flow, the scoring and the review screen. Answers are held client-side until
the last question is passed and submitted in one message, so changing an answer works; each
question's options are shuffled once and the order remembered, so stepping back and forward does
not reshuffle them. The option count is not on the wire — AS3 walks the localization table until a
key comes back empty, ported as a presence test.

It needed two composers the port did not have, `GetQuizQuestionsComposer` (1982) and
`PostQuizAnswersComposer` (1387), and it exposed a subscription gap: `QuizDataMessageEvent` and
`QuizResultsMessageEvent` were registered nowhere, so even a quiz request that went out would have
had its answer dropped. AS3 subscribes both inside the controller's own constructor, which is why
centralising subscriptions in `HelpMessageHandler` had quietly lost them.

Another AS3 fault ported as written: `showPage()`'s question branch builds the substituted
"page 1 of N" caption and overwrites it on the next line with the raw key, so the first question
shows an unsubstituted `${…}` until prev/next runs `setCurrentQuestion()`.

#### `GuideSessionController` — ported 2026-08-11

The seven markers that waited on it are gone, and `habbo/help` is down to **9**. The subsystem is
1,826 AS3 lines: the guide tool plus every window of the guide / helper / guardian flow, a state
machine with one window slot where each `setStateX()` closes the last window and installs its own
procedure. Nothing was blocked on assets — all 21 layouts already shipped and every incoming event
was already ported. What was missing was the machine between them, and four composers (3336, 2545,
349, 1801), all four corroborated by the emulator's AS3-traced constants. The nine guide composers
that already existed were re-checked against the registry at the same time; all nine match.

Three `GuideHelpManager` stubs that only logged a warning now forward for real —
`showGuideTool()`, `createHelpRequest()`, `openReportWindow()` — so a guide report reaches its form
instead of stopping after the server round trip.

**It also surfaced a wrong interface, which is the part worth remembering.**
`IIlluminaInputHandler` declared `onInput(widgetId: string, …)` where AS3 passes the widget
*window*. Every handler has to reach through it to clear the input field, so the port's one
implementer — `MainView` — worked around the missing argument by looking the widget up on itself.
That workaround is what made the wrong signature survive: it looked like a design choice rather
than a defect, and only a *second* implementer made the difference visible. Fixed to AS3's shape.

Two genuine gaps remain, both marked: no PerkAllowances event exists in this port, so the tool is
not revoked mid-session when the server withdraws `USE_GUIDE_TOOL`; and `IWindow` exposes no
`margins`, so a lifted-out tour description sits flush against its title.

**What is left in `habbo/help`: 9 markers, none of them a micro-gap.** One is a settled decision,
one is Flash-only, one is the wire — `requestReportsStatus()` needs header 1834, which is in the
primary registry and in neither this port nor `vortex-emulator` — and the rest are the two named
above plus the pending-ticket payload the wire parser does not read.

**The module now works end to end against a live server** — the report flow was confirmed by hand
on 2026-08-11, which is also what proved the layout-name fix above.

### 84 messages the server sends and the client does not listen for

`node scripts/unlistened-server-messages.mjs` compares `vortex-emulator`'s `*Composer` header
constants — server→client, so this client's *events* — against the `_events.set(id, …)` calls in
`HabboMessages.ts`. Read-only, re-runnable, and it takes a `--emulator <path>` if the sibling
checkout moved.

**2026-08-11, after the room-ad and targeted-offer ports: the emulator can send 512, this client
listens for 450, and 84 have no event at all.** 73 of those 84 are in the AS3 client's own
registry, so the real client handles them and the gap is this port's; the other 11 are the
emulator's own and need deciding rather than porting. (It read 445 / 89 / 78 earlier the same day,
before the three catalog headers below were closed.)

This is the audit that has no substitute. The client compiles, boots and renders perfectly while
dropping a packet the server took the trouble to build — the `ProductOffer` reply above sat
unhandled exactly that way, with `GetProductOfferComposer` going out on 1692 and a fully
serialised offer coming back on 1911 to nobody.

**The count is not a work list, and "the module is ported" is not enough either.** Most of the 84
belong to whole subsystems this port has not started — Game2/snowstorm (18), NFT and collectibles
(16), camera, crafting, treasure hunt, YouTube. But a header in a *ported* module can still be
blocked on an unported **consumer**, which is the trap worth naming: registering an event whose
handler has nowhere to deliver is ceremony, and it manufactures exactly the ported-but-unwired
shape this document keeps warning about.

The catalog group was checked one by one on 2026-08-11, and only two of the six were real:

| Header | Message | Verdict |
|---|---|---|
| 533 | `LimitedEditionSoldOut` | **done** — handler is on `HabboCatalog`, ported |
| 2735 | `GiftReceiverNotFound` | **done** — same |
| 1911 | `ProductOffer` | **done** — see above |
| 1750 | `IsOfferGiftable` | not in the AS3 event registry at all: the emulator's own, needs deciding |
| 2013 / 2155 | `TargetedOfferNotFound` / `TargetedOffer` | **done 2026-08-11** — was blocked on `catalog/targetedoffers/` (1,004 AS3 lines, directory held only `.gitkeep`); subsystem ported |
| 3787 | `RoomAdPurchaseInfo` | **done 2026-08-11** — was blocked on `RoomAdsCatalogWidget` (401 lines); widget ported, which also closed the three `roomAdPurchaseData` markers |

#### The two blocked groups, unblocked — 2026-08-11

Both were subsystem ports, not asset gaps: every layout already shipped.

**Room ads** (`RoomAdsCatalogWidget`, 401 l.). Porting the widget closed a chain that was cut in
three separate places, none of which the TODO count could see as one thing: the in-room event
panel's "extend" button called `HabboNavigator.openCatalogRoomAdsExtendPage()`, which only logged;
that would have called `HabboCatalog.openRoomAdCatalogPageInExtendedMode()`, which was empty; and
the page's own widget had no case in `CatalogPage.createWidget()`. Four headers: 3787 in, plus
2928 `PurchaseRoomAd` and 3607 `RoomAdPurchaseInitiated` out, with 366 already being sent to an
answer nobody read.

**Targeted offers** (1,432 l. with data and util). `OfferController`, five views, two data classes
and six messages. Wired into `HabboCatalog.initComponent()` after `loadProductData()`, since the
controller registers itself as a products-ready listener and that is what fires its first request.

**A header the emulator has wrong, found by confronting the two registries.** WIN63 has
`_composers[848] = _SafeCls_2561` for `GetNextTargetedOffer`; `vortex-emulator` listens on `9004`,
a placeholder its own comment calls unresolved. The client is correct at 848 and the emulator needs
the fix — until then the request lands nowhere and **no targeted offer can ever arrive**, however
complete this side is. This is the fifth emulator gap awaiting a go-ahead.

Two AS3 quirks were ported rather than corrected, both documented at the declaration:
`TargetedOffer.getSecondsRemaining()` keeps the `uint` wrap that makes `isExpired()` stop returning
true exactly when it should start, and `OfferController.dispose()` leaves `_disposed` unassigned as
AS3 does. In both cases the "fix" would change behaviour the server still drives.

Elsewhere, still unchecked or known-blocked:

| Header | Message | Note |
|---|---|---|
| 594 | `RoomChatSettings` | **correctly deferred** — verified against AS3: the handler only sets `floodSensitivity`, which nothing in this port reads. The existing TODO holds up |
| 2474 | `MyCfhReportStatus` | the emulator sends the reply but defines no header for the *request* (1834), so the feature is half-built on both sides. The client composer exists and `HabboHelp.requestReportsStatus()` sends it; the emulator's `Headers.cs` line is written but **still uncommitted** there, pending a split from another session's navigator work |
| 1746 | `SanctionStatus` | **fixed 2026-08-12** — the client was parsing a revision that no longer exists (see below); parser, DTOs, handler and window all rewritten to the 2026 shape, and the emulator serializer filled to match |
| 2129 | `RemainingMutePeriod` | **done 2026-08-11** — `RoomChatHandler`, as a chat event on the player's own avatar |
| 3510 | `BadgePointLimits` | **done 2026-08-11** — `HabboInventory` → `setBadgePointLimit()`, which had nothing feeding it |
| 1235 | occupied tiles | blocked: its only consumer is the floor-plan editor, unported |
| 9201 | custom stacking height | not in the AS3 registry — the emulator's own, needs deciding |
| 2145 | post-it placed | **done 2026-08-11** — and its serializer was empty, see below |
| 583 / 3727 | emerald and silver balances | **done 2026-08-11** — `HabboCatalog`; `Purse` and `PurseEvent` already had both |

#### The check this audit was missing — empty serializers

`PostItPlaced` (2145) is the reason the script grew a second pass. Everything about it looked
wired on the emulator: a composer record, a serializer, and a registration in `InventoryMap`. The
serializer's `Serialize` body was empty, so the header would have gone out with a zero-length
payload while the client read two ints off it. **Nothing about either side looked wrong from that
side alone** — which is the whole point, and the same shape as every other real defect this pass.

`node scripts/unlistened-server-messages.mjs` now cross-checks all three facts: an empty
`Serialize` body, a header this client listens for, and a client parser that actually reads. It read
27 on 2026-08-11, 25 after `RoomAdError` (2396) and `ObjectRemoveConfirm` (3643), and **0 as of
2026-08-12**: the script now prints *"No listened-for header has an empty serializer behind it."*

The remaining 25 went in one pass (`vortex-emulator` `3409065b` + `10f5f0b7`), and the method is the
part worth keeping. Every shape was taken from the WIN63 parser reached through the incoming
registry — event class from `_SafeCls_2046.as`, parser class from the event's constructor — and
**not** from this port's own parser for the same message. That distinction paid for itself twice:

- **1746 `SanctionStatus` was reading a message that no longer exists.** The port had thirteen flat
  fields, faithfully copied from `win63_version`'s `SanctionStatusEventParser` — a class in no other
  tree. The 2026 build sends a counted list of records, each nesting *two* sanction types. The two
  shapes share no prefix. Had the port been trusted, the batch would have pinned the wrong layout in
  a passing test. Fixed on all three client layers in `80435d4e`; nothing had noticed because the
  handler was a `log.trace` and `SanctionInfo` opened no window.
- **Two map entries paired a composer with another message's serializer.**
  `CampaignCalendarDoorOpened` was registered against the calendar-data serializer,
  `UnseenItemsEvent` against account-preferences. `AbstractSerializer<T>` casts, so both throw on
  first send. Both had survived review precisely *because* the bodies were empty — an empty
  `Serialize` reaches the cast and does nothing with the result, so the fault only appears once
  someone fills the body in. The correct serializers already existed; only the wrong name had been
  typed. `Vortex.Revisions.Tests/SerializerPairingTests.cs` now checks the whole revision for it.

The second of those is also why the count went 25 → 1 → 0 rather than straight to 0: repairing
`UnseenItems`' pairing pointed the audit at the *real* serializer, whose body was empty. The wrong
one wrote bytes, so the check had been answering a question about the wrong file.

Two shapes are easy to get wrong and are pinned by name in the tests: `WiredMenuError` (1230) writes
a **short**, alone among its neighbours, and `GuestRoomSearchResult` (160) guards its optional ad
entry with a **boolean** where the sibling `OfficialRooms` asks the same question with an int.
`ObjectRemoveConfirm`'s leading int remains the older example of the same trap — a *flag* the client
turns into a category, where sending the category would look right and be wrong.

Two cautions are baked into the check, both learned by getting it wrong first:

- **"Writes nothing" has to mean the packet, not the method.** A body that reads
  `CatalogOfferSerializer.SerializeAsPurchased(packet, message.Offer)` writes plenty without a
  single `Write*` call of its own. Looking only for `Write[A-Z]` reported 42, and the first three
  checked by hand were two false positives. Anything that hands `packet` on now counts as writing.
- **An empty serializer is legal.** Plenty of messages carry no payload, which is why the check
  only fires when the *client's* parser reads.

The four closed on 2026-08-11 were the ones this table listed as tractable, and all four were:
each had a real consumer already sitting in a ported module, and the emulator really sends all
four. What made them cheap is the same thing in every case — everything downstream existed and
nothing was connected to it. **Unlistened server messages: 89 → 78 across 2026-08-11/12.**

One thing this pass does *not* claim: filling a serializer does not make a feature work. None of the
26 is built by a handler, so the count that moved is "traps removed", not "messages now sent". What
each one buys is that the day someone writes the handler, the wire is already right.

### The 2026-08-10 wire pass — what the audits found once the TODO list ran dry

Four audits, run after the marker sweep below because the TODO count cannot see any of this.
`unwired-messages` went 5 → **0**; the client registry went 425 → 432 composers, 434 → 435 events;
send gaps 93 → 90, recv gaps 98 → 97. Small numbers, and every one of them was a wrong claim
rather than missing work.

**A marker said five composers stayed unregistered because "none has an entry in WIN63's
registry".** All five have one. Four had *moved id between builds*, which is what made the claim
look true: looking them up in `win63_version`'s registry finds a number, checking that number
against the primary registry finds a different message with a different arity, and that reads as
"no entry".

| Composer | win63_version | WIN63 primary |
|---|---|---|
| ForwardToASubmittableRoom | 2055 | **1917** |
| ForwardToRandomCompetitionRoom | 2517 | **3109** |
| GetIsUserPartOfCompetition | 2732 | **128** |
| GetIsBadgeRequestFulfilled | 2545 | **2236** |
| WiredDebugCommand | (absent) | **3608** |

Arity cannot resolve a one-String composer, so each was identified by its *caller* instead:
`_SafeCls_4528`/`_SafeCls_4538` in the primary tree match `class_4150`/`class_4146` line for line,
and `_SafeCls_4537` is pinned by the unobfuscated `requestCode` it compares against. **This is the
reusable method** — when a name is obfuscated on both sides, match the call site, not the shape.

**Three of those unblock the emulator, which was waiting on this client.** Its headers for
ForwardToASubmittableRoom (9001), ForwardToRandomCompetitionRoom (9002) and
GetIsBadgeRequestFulfilled (9112) are placeholders whose own comments say no ported client
counterpart existed to verify against; GetIsUserPartOfCompetition (2732) is dismissed there as
"likely a duplicate constant with no separate real message". It is 128 and it is real.

**One live wire fault found, in the emulator.** `OfficialSongIdMessageComposer = 2264` — that is
`win63_version`'s id, and the 2026 registry reassigned 2264 to
`WeeklyCompetitiveFriendsLeaderboardEvent`. The event's real header is **3050**, which is what the
client registers. A reply at 2264 would hand the song parser a leaderboard payload: `readString`
then `readInt` over the wrong bytes, throwing nothing. It is also the only one of the four sound
headers there with no AS3-verified note — *the absence of that note is the tell*. Not corrected in
the emulator: that repo had another header pass uncommitted in the same file.

**Room music, and what "reachable" costs.** All four sound messages are ported, but two of them
(add/remove jukebox disk) are sent only by `PlayListEditorWidget`, which needs nine sibling classes
and has no port — so they are registered and dormant. The third round trip *was* made reachable by
porting `SongDiskProductViewCatalogWidget` (268 lines): `CatalogWidgetName` already declared
`songDiskProductViewWidget` and `CatalogPage` had no case for it, so song-disk catalog pages built
no widget at all.

**Group forums — complete, 2026-08-10.** The feature was 4,125 lines across 18 AS3 classes in
`friendbar/groupforums/`, none of them ported (the directory held a `.gitkeep`); the seven layouts,
the images and `IIDHabboGroupForumController` all shipped already, with nothing implementing the
controller. It landed in three slices:

1. **Messages** — 4 DTOs, 9 parsers, 9 events, 12 composers, 21 headers. Group forums left **both**
   wire-coverage gap lists (send 90 → 78, recv 97 → 89) and the registry went 432 → 444 composers,
   435 → 444 events.
2. **The three page models** — `ForumsListData`, `ThreadsListData`, `MessagesListData`. They exist
   because a parser is flushed and reused for the next message: a view holding the parser's output
   would watch it empty underneath it.
3. **Controller and UI** — the remaining 14 classes, ~3,850 AS3 lines: `GroupForumController`
   (a DI Component with 9 dependencies, an `ILinkEventTracker` for `groupforum/`, 9 message
   handlers and the unread-count poll), `GroupForumView`, the three list views with their three
   item views, `ComposeMessageView`, `ForumSettingsView`, `StringBuffer`, `ForumModerationState`,
   `IGroupForumController` and `UnseenForumsCountUpdatedEvent`.

**And wired, not just ported.** `HabboFriendBar`'s third `attachComponent()` — commented out with
the other two because the component did not exist — is live, so `groupforum/<id>` links now resolve;
`HabboGroupsManager.openGroupForum()` has been emitting them all along. `HabboToolbar` gained the
`UNSEEN_FORUMS_COUNT` dependency AS3 declares (soft, null setter, event listener only) and
`onUnseenForumsCountUpdate()`, which is what puts the forum count into the me-menu badge.

Three things worth remembering from the port:

- **`ITextWindow.bold` was read-only in the interface** while `TextController` had had the setter
  all along — the same mismatch the file's own comment records for `italic`. Every forum row bolds
  an unread thread, so the interface was corrected rather than cast around.
- **`core/runtime/IUnknown.ts` did not exist.** The port's manager interfaces silently drop AS3's
  `extends IUnknown`, which works until an interface's *entire* content is that clause —
  `IGroupForumController` is one. It is now ported (`extends IDisposable`, plus `queueInterface`
  and `release`).
- **A `_soundManager` branch with no body, in both trees.** `updateUnreadForumsCount()` reads
  `if(_soundManager != null){}` in the primary *and* in `win63_version`, decompiled by a different
  tool. Two independent decompiles agreeing is the strongest evidence available that the bytecode
  really is shaped that way, so it is ported verbatim under a `TODO(AS3)` — `_soundManager` has no
  other use in the class, so the missing call is the only reason the dependency is injected.

Two things the message slice is worth remembering for:

- **`UpdateThreadMessageComposer` sends its two booleans swapped.** AS3 declares
  `(groupId, threadId, isLocked, isSticky)` and sends `[groupId, threadId, isSticky, isLocked]`.
  Both trees agree, so it is not decompiler noise, and `ThreadListItemView` settles which parameter
  is which — `lockThread(forum, id, !isLocked, isSticky)` against
  `stickThread(forum, id, isLocked, !isSticky)`. The emulator's parser reads IsLocked then IsSticky,
  so as things stand **pinning a thread there locks it and locking pins it**. A composer whose
  payload order differs from its parameter order cannot be caught by arity checking; only reading
  the body finds it.
- **The four parser pairs sharing a wire shape** (post/update thread, post/update message) are
  indistinguishable by shape *or* arity. What separates them is `GroupForumController`'s listener
  registrations, whose callback names (`onPostThreadMessage` vs `onUpdateThread`) are unobfuscated.
  Same method as the competition composers: **match the call site, not the shape.**

The 18 UI classes are done; what remains in `friendbar/` is `talent/` (0/6) and `popup/`
(`HabboEpicPopupView`). The `HabboFriendBar` docblock used to file both of those under
`groupforums/` — only the forum controller was ever there.

### The 2026-08-10 sweep — 422 → 382, and what the markers were hiding

Ten passes, all opened the same way: take the marker's stated blocker as a claim and check it
against the port *before* opening the AS3. Roughly half the markers checked did not survive that
step, and the ones that did split into small, contained ports rather than the subsystems they
named.

**The dominant pattern was a dependency that had landed since the marker was written.** Five
separate markers said "freeFlowChat isn't wired into RoomUI"; it is DI-resolved there and injected
into every `RoomDesktop`. Four said `IHabboNotifications` lacked the API for the wired
click-settings toggle; two-thirds of it was already there, and `NotificationExtraDataKey` already
declared the exact three keys AS3 packs into that argument. Four chat-style preview sites said
`chatStyleLibrary` "isn't ported" — `IChatStyleLibrary.getStyle()` and `IChatStyle.selectorPreview`
both existed, and the single missing piece was one `IIDHabboFreeFlowChat` dependency on
`HabboCatalog`. Two badge getters said the conversion was impossible because `createImageBitmap()`
is async; `OffscreenCanvas.transferToImageBitmap()` is not.

**Three markers were factually wrong about AS3, not about the port.**
`InfoStandWidgetHandler.update()` was said to drive "pet-command-tool countdowns and the pet update
timer"; AS3's `update()` is an empty body. `RoomToolsWidget` was told to gate its chat-history
button on `!isDisabledInPreferences` — that is `win63_version`'s reading; the primary tree says
`setChatHistoryButton(_freeFlowChat != null)`. `RoomChatInputWidget.getFriendBarWidth()` returned 0
where AS3 returns 1000 when there is no friend bar.

**Two headers under-declared the port by an order of magnitude.** `RoomWidgetFactory` said "only
RWE_INFOSTAND is implemented" above 39 of the AS3 factory's 45 cases; `RoomDesktop.createWidget()`
claimed 4 of 45 against 42. A header that wrong is worse than none: it sends the next reader to
re-port what is already there. Both now name the real remainder (six widget types, each blocked on
a subsystem with no module — camera, crafting, playlist editor, youtube, vimeo, thumbnail camera).

**Comment blocks cut in half by a bad edit are still in the tree.** Two more surfaced here — one in
`InfoStandWidgetHandler` whose halves had drifted onto two unrelated methods, one in
`ChatStyleSelector` that broke mid-clause. Both described *faithful* behaviour, so the fix was a
note, not code. This is the same failure `chore: rejoin 22 comment blocks a bad edit had cut in two`
(51901d8b) catalogued; assume more remain.

Subsystems that went from dead to working: the infostand's jukebox and song-disk views (both were
no-op shells with `window` hardcoded to null, while the widget already routed to them), the pet
infostand's 3s refresh poll, wired's deferred user-click menu and its dismissable click-settings
notification, the room-tools achievements button, music-disc titles in the inventory, group badges
in the friend bar and friend list, badge selection reaching the server, habblet links, and
chat-style previews in all four catalog sites.

One shared-helper bug fell out of it: `ColorConverter.hexToUint('')` returned `NaN` where AS3's
`uint("0x")` returns 0. AS3 deliberately leaves the last hop of the seasonal-currency colour lookup
unguarded, so an unseeded config property is *meant* to come back black.

### What the 2026-08-09 sweep actually taught

Nine passes, 502 → 455 markers. The reusable finding is not the count — it is that **a marker stating why it is blocked is a claim, and roughly half of them were wrong**. Cheapest first step, every time: check the claim against the code *before* opening the AS3.

The failure modes, in the order they cost the most:

1. **Grepped the wrong place.** The Builders Club placement composers were tagged "unported, header unresolved" after searching `outgoing/room/furniture/`; they were in `outgoing/catalog/`, registered, and implemented by the emulator. *This one was written during this same session* — the trap is not age. Check the header registry, not a guessed directory.
2. **Trusted a name over a body.** `changeObjectState` had four hits on `RoomEngine`, so it read as ported; those belong to `class_1947`'s method, flattened in under the same name, and the engine's own version was absent. Same shape as the `hideMainWindow()` regression already recorded in `.claude/rules/00-mandate.md`.
3. **Comments drifted off their methods.** Two `TODO(AS3)` blocks in `RoomEngine.ts` sat above unrelated declarations, describing code they were never about. Worse than a stale marker: it invites "verification" against the wrong method.
4. **Blocker named the wrong collaborator.** The catalog mover markers blamed a missing `CatalogObjectMover`; AS3 never owns one. `disposeObjectFurniture`'s blamed the stuff-data factory (present) rather than the synchronous-icon problem (real).

**Two silent omissions surfaced that no marker mentioned at all** — `onCreditBalance()` never calling `updatePurse()`, and `RoomEngineObjectPlacedEvent` dropping AS3's `placementSource`. Neither would ever have been found by working the TODO list, which is the argument for reading the AS3 method in full even when the marker looks narrow.

Subsystems that went from dead to working: catalog→room drag placement (NORMAL and BUILDERS_CLUB), club-purchase page memory, catalog-preview furniture and wall-item rotation plus automatic state cycling, inventory trade offering, wired inspection from the infostand, and the wired dialog's variable-overview/logs header buttons.

### Continued 2026-08-09 — 447 → 433, and two wire faults found by porting

Three further passes, each picked by looking for the *shared root* under several markers rather
than working the list top to bottom. That framing is the point: all three closed more markers than
they touched files.

**Bundle discounts (four markers, one root).** `PurchaseConfirmationDialog`, `SpinnerCatalogWidget`,
`BundlePurchaseExtraInfoWidget` and `ProductViewCatalogWidget` each carried a marker saying
bundle-quantity discounts could not work. All four pointed at the same thing:
`HabboCatalog.bundleDiscountRuleset` was a hardcoded `return null`, because
`BundleDiscountRulesetMessageEvent` (1073) and `GetBundleDiscountRulesetComposer` (317) were never
ported. Porting the message — plus its DTO and `HabboCatalogUtils.getDiscountItemsCount()` and its
three private helpers — made the getter real and the four call sites fell out. Headers from WIN63's
registry (`_SafeCls_2046.as:931` and `:1179`), corroborated by the emulator, whose serializer
already wrote the five fields in the AS3 read order.

The DTO's class name is **derived and says so at its declaration**: obfuscated as `_SafeCls_1903`
in WIN63, differently as `class_1766` in win63_version, and absent from the 2016 tree. Its members
are all real AS3 names, so the accessors are recovered even though the class is not.

**A decompiler artifact that would have shipped as dead code.** `win63_version`'s
`SpinnerCatalogWidget.refresh()` reads `visible = 0 > 0` with the computed local discarded — port
that literally and the container is permanently hidden. The primary tree has the real `_loc1_ > 0`.
Same family as the dropped E4X `@` already recorded in `CLAUDE.md`: when a decompile reads as dead
code, check the other tree before believing it.

**`selectAvatar` (three markers, one root).** `IRoomEngine`, `ChatInputWidgetHandler` and
`HabboFreeFlowChat` all said avatar selection was unported — clicking a user, clicking their chat
bubble, or picking one from the `@Name` autocomplete. `RoomEngine` now has
`selectAvatar()`/`getSelectedAvatarId()` and the body of the AS3 handler's `setSelectedAvatar()`
(`_SafeCls_1821.as:2115`), flattened in the same way `selectRoomObject()` already is.

That pass found a **wire fault in the emulator, not the client**: `LookToMessageComposer` (2508 per
WIN63's registry) had no port at all, and the emulator maps its `LookToMessageEvent` to 9103 — a
placeholder its own comment flagged as unresolved. Its parser already read the right two ints, so
only the header was wrong; corrected there with the citation. Header order held: WIN63 first, the
emulator as corroboration, and when they disagreed the registry won.

Two interface gaps were filled rather than worked around on the way:
`IRoomWidgetHandlerContainer` never declared `moderation` though AS3 does (RoomUI now DI-resolves it
optionally and injects it per desktop, exactly as it already does `userDefinedRoomEvents`), and
`IRoomDesktop` never declared `processWidgetMessage` though `RoomDesktop` implements it.

**A scroll fix that was not a fidelity bug.** The chat-bubble chooser and the guild badge colour
palette could not be scrolled to a chosen row. The entire wheel path turned out faithful —
`ItemListController.as:573/582` really does construct two `SmoothScroller`s with the defaults the
port passes, and `scrollBySteps`, `wheelDeltaToScrollDelta`, the `0.01` epsilon and
`getVerticalMaxScroll` all match line for line. The cause was the one piece with no AS3 counterpart:
`NativeWheelDelta.LINES_PER_NOTCH`, mapping a browser notch to Flash's 3 lines. Downstream that is a
fixed pixel intent divided by the scrollable overflow, so on a short grid one notch covered 25-47%
of the range. One line per notch puts those at 8-16%. Recorded here because the instinct to look for
an unfaithful port was wrong twice over: nothing downstream was touched.

### Continued again — 433 → 425, and the `win63_version` decompile stops being trustworthy

**The infostand's two live updates.** AS3's `InfoStandWidgetHandler` registers nine session
listeners; seven pet ones were wired and the two that matter while you are looking at somebody were
not — `RSUBE_FIGURE` (they changed look, motto or achievement score) and
`rsfgue_favourite_group_update`. The panel stayed frozen on whatever it showed when you clicked.
`InfoStandWidget.updateUserData()` and `favouriteGroupUpdated()` were both already written, each
under a marker reading "no current caller".

The favourite-group half needed a chain that did not exist at all:
`FavoriteMembershipUpdateMessageEvent` (1259, registry `_SafeCls_2046.as:1215`, class
`_SafeCls_3475`, corroborated) and its parser were unported, so `RoomUsersHandler` could not raise
`RoomSessionFavouriteGroupUpdateEvent` — a class that existed and nothing ever constructed.

**Pet faces in chat bubbles.** `ChatBubbleFactory.getPetImage()` was `return null` under a long
marker explaining it was "genuinely blocked, not deferred out of laziness":
`roomEngine.getPetImage()` supposedly existed nowhere, "confirmed via search". It exists on
`IRoomEngine` with the exact ten-parameter signature AS3 calls, and two catalog widgets had been
calling it all along. So had the two caches, `imageReady()`, `petImageReady()` and `PetFigureData`.
Everything but the twenty lines between them.

**`init()`'s three "blocked" steps.** The marker listed `refreshFurniData()`,
`getGiftWrappingConfiguration()` and `initBundleDiscounts()` as skipped for want of their backing
systems. None held up: one had landed the same day, `ISessionDataManager.refreshFurniData()` had
existed all along, and gift wrapping was one message away. `GetGiftWrappingConfigurationComposer`
(940), `GiftWrappingConfigurationEvent` (1369), `CatalogPublishedMessageEvent` (773) and the
`GiftWrappingConfiguration` DTO are ported. 1369 comes from the emulator **alone** — the registry
carries only the request side and this port had no counterpart to read the response id back from;
that is stated at the declaration rather than dressed up as AS3-verified.

`refreshFurniData()` would have been dead code by itself: AS3 only calls it from `init()` when a
flag set by `onCatalogPublished` says the data is stale, so that handler is ported too. The
deferral is deliberate — a user who never reopens the catalog never pays for the refetch.

**Two decompiler artifacts in one day, both in `win63_version`, both would have shipped.**
`SpinnerCatalogWidget.refresh()` reads `visible = 0 > 0` with the computed local discarded — a
permanently hidden container. `GiftWrappingConfigurationEventParser` reads `while(0 < _loc2_)` in
all four list loops, with `_loc3_` incremented and never tested — a browser hang on any non-empty
list. The primary tree has the correct code in both. Together with the dropped E4X `@` already in
`CLAUDE.md`, the rule is now unconditional: **when a `win63_version` decompile reads as dead or
absurd code, check the primary tree before porting it.**

**A comment-shape defect worth naming.** The orphan detector reported 49 blocks detached from their
declaration. Reading them showed one dominant shape, not 49 mistakes: a block split in two, the head
stranded above an unrelated member and the tail still in place above the right one, starting
mid-sentence — the halves rejoin into one grammatical sentence. 22 were rejoined mechanically on
that pairing (both halves name the same member). The 27 left each need their own judgement: most sit
inside a method body and are correctly placed, and a few document a member that exists in no tree,
where a lone marker is legitimate.

### Four measurement tools, and what they are each for

The TODO count only measures what someone remembered to mark. Two blind spots it cannot see got their own scripts on 2026-08-09, a third mode was added to this one, and the priority-0 sweep became the fourth script on 2026-08-14:

| Command | Question it answers |
|---|---|
| `node scripts/todo-inventory.mjs` | What is marked, classified by what actually blocks it |
| `node scripts/todo-inventory.mjs --stale` | Which markers name a blocker that has since landed |
| `node scripts/todo-inventory.mjs --orphans` | Which trace comments drifted off their declaration |
| `node scripts/wire-coverage.mjs` | What the server implements that the client cannot reach |
| `node scripts/unwired-messages.mjs` | What we wrote and never registered |
| `node scripts/sweep-unwired.mjs <module>…` | What AS3 subscribes to that the port's matching module does not |

**`sweep-unwired` is priority 0 mechanised**, and it exists because running that recipe by hand kept producing false negatives — three different ones now (`onActivityPoints` masked by a same-named method, a registration counted as present because it was commented out, and an event counted as wired because a *different* module constructs it). Its unit is the module, not the engine: it resolves each AS3 `_SafeCls_N` through the registry to an id, finds the TS class the port registers at that id, and asks whether **that module** constructs it. Validate it on a module already swept — `habbo/quest` reports 24/24.

**`wire-coverage` found the largest single defect of the day.** The whole moderation toolset was dead: 23 composers ported, none registered, every button reaching a composer the connection had no header for, and not one error logged. The player half — reporting — was the same, 20 ported and 2 registered, so a working queue would have had nothing to work on. Send gaps went 149 → 98 once both were wired, plus room competitions, which were unreachable for the same reason.

The discipline that made it safe is worth repeating: **resolve every id against WIN63's registry and compare the constructor arity before registering anything.** All 23 moderation composers matched; a third of the help ones did not — `CallForHelpMessageComposer` sent five fields where the class takes seven. Registering those would have put truncated reports on the wire, which no server rejects and no log shows. Two wrong headers surfaced the same way: `ChangeUserName` was registered at 879, which the registry gives to `ClaimNewUserName` — a different message taking the same single string, so it would have been silently mishandled.

**`--orphans` reports 63 hits and exists because the same defect appeared three times in one branch.** A trace comment that lost its declaration does not merely go stale, it describes whatever follows it — which invites verifying a claim against a method it was never about. Not every hit is a defect; a marker documenting a wholly absent feature legitimately stands alone.

**Re-run `--stale` after every port.** Porting a member mechanically invalidates the markers that cited it as their blocker, and nothing re-runs the detector on its own: the catalog's drag-into-room markers stayed "not ported yet" for several commits after the thing they named had shipped, in the same branch.

Six modules were absent from the port entirely and each blocked a cluster: `habbo/game` (only
`.gitkeep`), `catalog/habbicons`, `inventory/collectibles`, `inventory/recycler`,
`inventory/wired_trading`, `quest/dailytasks`. Three of the six have since landed —
`inventory/recycler` and `inventory/wired_trading` (2026-08-12) and `inventory/collectibles`
(2026-08-12) — leaving `habbo/game`, `catalog/habbicons` and `quest/dailytasks`, plus
`catalog/collectibles`, which the inventory pass named but did not touch.

**`--stale` is the cheapest lever, and its findings are candidates, not verdicts.** The port moves
faster than its comments: a marker saying "X is not ported" while an `X.ts` now exists is usually a
gap somebody already closed elsewhere. The 2026-08-09 pass verified all 44 candidates by hand and
6 were genuinely stale — `RoomUsersHandler`'s Macklebee room-shake, `EcotronBoxFurniWidget`'s copied
event constant, `MeMenuWidgetHandler._widget`'s `unknown` type, `HabboMusicController`'s two
sequencer methods (its class doc still called `loadTraxSong()` a stub long after it was
implemented), `FurniModel.isTradingOpen` + `requestSelectedFurniToTrading()`, and
`PurchasableClothingConfirmationView.refreshAvatar()`. The rest were legitimately narrower claims
about a *method* on a class that exists, which is why nothing here should be rewritten from the
tool's output alone.

The client row's 21 → 64 is not 43 files of new client work: the login rebuild and the onboarding
flow landed from 2026-07-31 and account for 47 of the 64 (`onBoardingHcUi/` 27, `onBoardingHc/` 4,
`onBoardingHcSteps/` 4, `login/` 12), and the rest is support. (The count included `lighting/` 8 at
the time; that directory was deleted on 2026-08-19 — see below.) The client package stays
deliberately thin — almost everything is engine-side.

The first two rows used to read "Primary WIN63 = 4,783", which is `win63_version`'s count — the
secondary tree. `src/unknowns/` is counted because 556 files under `src/com/sulake/` import from it
(parser DTOs, composers); it is client code, not a bundled stranger. See CLAUDE.md.

There is no reliable single global percentage right now. Raw file counts undercount converted JSON layouts, overcount `index.ts`/support files, and do not prove AS3 API parity. Use the module snapshot below as the working status.

---

## Current Module Snapshot

File counts re-measured 2026-08-13; the prose in each row carries its own dates and is only
rewritten where the count moved enough to make it wrong.

| Area / module              | AS3 files | TS files | Current status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
|----------------------------|-----------|----------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `core/`                    | 371       | 363      | Advanced. `core/window` is heavily ported; runtime/assets/communication still need targeted parity audits before being called final.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `core/window`              | 254       | 257      | Advanced and active. Not "not started"; several AS3 parity passes landed recently. Also gained the `WIN63-202607011411-782849652` shape/gradient/stroke/bitmap-fill skin primitives, and a minimal HTML-lite (`<b>`/`<i>`/`<u>`/`<br>`) parser for `FormattedTextController` with per-run bold/italic rendering — see notes below.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `iid/`                     | 60        | 54       | Structurally aligned by file count; still verify symbols against AS3 before marking complete.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `room/` root               | 80        | 67       | Advanced low-level room manager/renderer support. `IRoomObjectVisualization`/`RoomObjectSpriteVisualization` gained a real `getImage()`/`image` (Phase 5, offscreen single-object rasterization via a throwaway PixiJS `Container` + `renderer.extract.canvas()`, including the `extractDarknessToAlpha()` additive-glow-on-transparent-background edge case) — see notes below.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `habbo/room`               | 328       | 320      | Advanced. Room engine/object pipeline exists; visualization and edge behavior still require parity checks. Gained `ISelectedRoomObjectData`/`getSelectedObjectData()` (Phase 4, honest stub always returning `null` - full room-object-selection tracking isn't ported) and a real `setObjectMoverIconSpriteVisible()`/`getObjectMoverIconSpriteVisible()` (backed by the existing `_moverIconSprite`). Phase 5 added `RoomEngine.getFurnitureImage()`/`getWallItemImage()`/`getRoomImage()`/`getPetImage()`/`getRoomObjectImage()`/`getGenericRoomObjectImage()` (offscreen furniture/room image rendering) and wired `RoomPreviewer`'s 3 matching stubs for real. `ISelectedRoomObjectData`'s stub was then replaced with a real, faithful implementation (`SelectedRoomObjectData`/`setSelectedObjectData()`/`updateSelectedObjectData()`/`resetSelectedObjectData()`) backing a genuine floor-furniture placement/move ghost-object pipeline — see notes below.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `habbo/session`            | 84        | 90       | Advanced / likely near complete; keep auditing handlers and message wiring as new protocol gaps are found.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `habbo/navigator`          | 83        | 98       | Advanced and active. Logic plus major UI/window flows are present; recent work focused on AS3 visual parity.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `habbo/window`             | 110       | 100      | Advanced and active. Widgets/dialogs/resource management exist; do not describe this as unstarted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `habbo/ui`                 | 369       | 300      | **Was the largest remaining gap in the port — 83 → 300 TS files between 2026-07-28 and 2026-08-13.** No longer "mostly missing", but still the widest module by absolute file gap. Room desktop/widget shell exists; infostand (furni + user/bot identity, Phase 1), room-tools, chat-input, and the chat-style selector popup (`widget/chatinput/styleselector/`) are ported — see notes below. Chat *display* is live via `habbo/freeflowchat`. `RWE_CHAT_WIDGET` is now a wired case rather than the disabled one this row used to describe, but it is a fallback, not a second renderer: `ChatWidgetHandler.processEvent()` returns the moment `freeFlowChat` resolves, so every live bubble comes from `ChatBubbleFactory` (see the `habbo/freeflowchat` row). Its one real gap is game chat (`gce_game_chat`), blocked on the unported `habbo/game`. The friend bar itself has since been ported too (2026-07-29, see `habbo/friendbar`). **Sub-directory breakdown (2026-08-13, TS/AS3)** — `widget/` **256/314** (126 on 2026-08-07) and `handler/` **42/47**. The root shell — `RoomDesktop`, `RoomUI`, `RoomWidgetFactory`, `RoomDesktopLayoutManager` and the 5 interfaces — is **ported, 11 TS for 8 AS3**; only `_SafeCls_2850.as` (the `room_view`/`room_new_chat`/`room_widget` desktop layout helper) is unported. `RoomDesktop.createWidget()`'s switch wires **44** of AS3's ~45 `RWE_*` types (2026-08-13, after the camera slice; 33 on 2026-08-07) (dimmer, present, mannequin, both friend-furni types and custom stack height added 2026-08-02; doorbell, room queue, internal link, loading bar and conversion tracking 2026-08-07) (infostand, room-tools, chat-input, effects, avatar-info, chat, and — since 2026-07-28 — furni-trophy, furni-stickie, furni-placeholder, room-background-colour, furni-credit, furni-ecotronbox and furni-pet-package). **This is *not* the `RoomObjectFactory` pattern** — checked rather than assumed: the 66 files under `widget/` map exactly onto those 6 cases (infostand 15, roomtools 5, chatinput 5, roomchat 4, avatarinfo 4, contextmenu 3, effects 2) plus 43 of shared infrastructure (`messages/` 14, `events/` 9, `enums/` 3, `IRoomWidget`/`RoomWidgetBase`). There is no written-but-unreachable widget here; the switch is short because the widgets do not exist yet. The unit of work is therefore a full vertical slice per widget — AS3 widget + its handler + its `RWE_*` case + the messages/events it needs — not a cheap re-wiring pass. Inside `widget/` (re-measured 2026-08-13): `furniture/` **51/54** — the only missing files are the video-display pair (`VimeoDisplayWidget`/`YoutubeDisplayWidget`) and one obfuscated `_SafeCls_3218.as`; the "17 unported clusters" list that used to sit here is closed — `events/` 57/64, `messages/` 42/54, `avatarinfo/` 23/28, `enums/` 3/8, `crafting/` 0/13, `camera/` **9/9** (ported 2026-08-13), `playlisteditor/` 0/9, `memenu/` **9/9**, `chooser/` **8/8**, `poll/` **5/5**, `uihelpbubbles/` **4/4**, `friendrequest/` **2/2**, `doorbell/` **2/2**, `wordquiz/` **2/2**, `roomqueue/` **1/1**, `loadingbar/` **1/1**. Ported or near-ported: `infostand/` 16/17, `contextmenu/` 3/3, `effects/` 2/2, `roomtools/` 5/7, `chatinput/` 5/8.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `habbo/avatar`             | 140       | 155      | Advanced. Rendering data and manager work exists, and the avatar editor is complete (11 slices, ~7,200 l. AS3; the surviving `TODO(AS3)` markers there are inventory-side gaps, not unported files). TS exceeds AS3 where the port splits files.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `habbo/inventory`          | 73        | 80       | Advanced logic. Display/user workflows still need parity checks. The 2026-07-17 audit found and fixed four criticals here: an invented duplicate `IStuffData` tree (12 files, deleted — AS3 has one interface, `com.sulake.habbo.room.IStuffData`) that made clicking an area-hide/room-background furni throw; `setInventoryCategoryInit()` with no caller, so the whole furni list was re-requested on every tab switch; `UnseenItemTracker` subscribing to nothing and dispatching nothing; and the missing chest categories 24/25, which collapsed every chest of a type into one pile.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `habbo/toolbar`            | 41        | 49       | Advanced. Purse/bottom-bar work exists, all 8 toolbar extensions now wired (previously 6 were dead code — see note below). The Progression icon click itself worked since the 2026-07-17 audit (enum reverse-lookup fix), but nothing subscribed to it: `AbstractSubMenuController` (real base class — window build, hover/click procedure, toolbar-click self-toggle, unseen counters) and `ProgMenuController` (the "PROGRESSION" bottom-bar popup: achievements/quests/dailytasks/leaderboards/reward-track) were both entirely unported, so clicking it did nothing. Now wired into `BottomBarLeft` (constructed/disposed/repositioned alongside the existing `MeMenuNewController`) — **this is the toolbar's real, only entry point into the achievements panel in this client build**: neither the me-menu popup (`me_menu_new_view_xml`) nor the bottom bar itself (`bottom_bar_left_xml`) has an achievements region in the shipped assets (verified against the raw AS3 dump too — not a stale-asset gap, this build genuinely routes achievements through Progression). `MeMenuNewController` itself still doesn't extend the new base class (kept as-is, still self-contained) — a reasonable follow-up, not required for achievements access. Catalogue/Help/Camera/Wired Menu still have no subscriber since those feature modules are partial/missing; tagged `TODO(AS3)` in `HabboToolbar.ts::toggleWindowVisibility()`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `habbo/communication`      | 1,913     | 1,812    | Partial, but the fastest-moving area in the port: **+476 files between 2026-07-28 and 2026-08-13** (1,336 → 1,812), of which the message tree alone accounts for 1,771 (+209 the period before that). Large backlog still remains; see message section. Counted against `win63_version` — the primary tree keeps the message classes in `src/unknowns/`. Marketplace wire protocol (9 composers/8 events/8 parsers, Phase 3), Recycler wire protocol (3 composers/3 events/3 parsers + 3 prize-table data classes, Phase 4), and the sellable-pet-palettes protocol (1 composer/1 event/1 parser + 1 palette DTO, Phase 7) now ported.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `habbo/catalog`            | 265       | 206      | Partial. Browse→select→buy-one-item flow works end-to-end; ClubCenter (`clubcenter/`, status display only), Club/VIP purchase (`club/`, Phase 2), Marketplace (`marketplace/`, Phase 3), Recycler (`recycler/` + `CatalogObjectMover`, Phase 4), the guild catalog cluster (`guilds/GuildMembershipsController` + 4 widgets, Phase 6), and the pet catalog widgets (Phase 7) fully ported. 39/~45 `viewer/widgets/` cases wired in `CatalogPage.createWidget()` (see `CatalogWidgetName.ts`). The 7-phase roadmap is now complete; the ~6 remaining widget cases are the out-of-scope ones (Trax/song disks, classic trading, room ads, builder add-ons, user badge selector) — see "Recent Work Recorded".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         **The catalog→room placement state machine is now real** (2026-08-09): `requestSelectedItemToMover()`, `cancelFurniInMover()`, `resetObjectMover()`, `resetPlacedOfferData()`, `syncPlacedOfferWithPurchase()`, `itemAddedToInventory()`, `updateRoom()`, `isDraggable()`, the two placement callbacks (`onObjectPlacedInRoom()`/`onObjectPlaceOnUser()`) registered on the `IIDRoomEngine` dependency as AS3 does, plus the new `purchase/PlacedObjectPurchaseData` and the `dispatchRoomChangedToCatalogPage(s)()` pair. Three engine gaps had to be closed first: `RoomEngineObjectPlacedEvent` was dropping AS3’s `placementSource` (param14) entirely — without it no listener can tell its own placements from anyone else’s, so the catalog’s guard was unwritable; `getRoomStringValue()` did not exist; `addObjectFurniture()`/`addObjectWallItem()` were on `RoomEngine` but not on `IRoomEngine`. The old TODOs blamed “HabboCatalog does not own a `CatalogObjectMover`” — a misdiagnosis: AS3 never owns one, the mover is the *widget*’s drag icon and the catalog side runs on `initializeRoomObjectInsert()`. The BUILDERS_CLUB placement branch is real too — it sends `PlaceObjectFromCatalogComposer` (3849) or `PlaceWallItemFromCatalogComposer` (2740), resolving a search-result offer’s dummy page id back to a real page first, and re-arms the mover instead of closing when place-many is set. **Both composers were already ported and header-registered, and the emulator implements both** (`BuildersClubPlaceRoomItemMessageEvent` / `BuildersClubPlaceWallItemMessageEvent`, field order matching the AS3 constructors exactly); the first pass tagged them "unported" after grepping only `outgoing/room/furniture/` and missing `outgoing/catalog/` — a reminder that a "not ported" claim must be checked against the header registry, not a guessed directory. Still open, each verified against both trees and the emulator: the decoration branch of `itemAddedToInventory()` (composer `_SafePkg_2324._SafeCls_2323`, header 2292, unported — and the reference server has no handler for it — both re-verified), `onFriendBarSelectionEvent()` (needs `FriendBarSelectionEvent`, unported) and `onObjectSelected()` (needs `CatalogUserEvent`, unported). One faithful AS3 bug ported as-is: `itemAddedToInventory()` overwrites its `category` argument with the *room* category (10/20) and then switches it against `FurnitureCategory`’s WALL_PAPER/FLOOR/LANDSCAPE (2/3/4), so those three branches are unreachable in the real client too. **Second catalog pass, same day** (`HabboCatalog.ts` 49 → 35 markers across the day’s three passes): the club-purchase page memory is real (`rememberPageDuringVipPurchase()` / `forgetPageDuringVipPurchase()` / `doNotCloseAfterVipPurchase()` plus the one-shot `hideMainWindow()` veto they arm, and `onSubscriptionInfo()`’s responseType-2 branch that rebuilds the catalog and reopens the remembered page); `reset()` itself is ported (it existed only inlined inside `dispose()`); `getBuilderFurniPlaceableStatus()`’s lapsed-membership branch now really scans the room for a non-moderator onlooker — its TODO claimed `getRoomObjectCount()`/`getRoomObjectWithIndex()` did not exist on `IRoomEngine`, and both do; `productDataReady()` replays a `toggleCatalog()` that arrived before the product data (the port used to drop the click); `setLeftPaneVisibility()`’s TODO was pure staleness — the body was already complete. A sound-manager dependency was added (AS3 has one, this port had none), which restored `HBST_purchase` on a credit change and `HBST_pixels` on a duckets change — **and surfaced a silent omission: `onCreditBalance()` was never calling `updatePurse()`, so the catalog header kept a stale balance until something else refreshed it.** |
| `habbo/help`               | 34        | 37       | Partial/advanced. No longer zero; managers, CFH registry, and many help messages exist.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `habbo/moderation`         | 43        | 43       | **Complete, 2026-08-19.** Manager, message handler, issue queue, all nine tracked window controllers, the eight `actions/`, and the new mod tool (`NewModerationTool` + the five `new_mod_tool_tabs/` sub-views, recovered from `IIDNewModerationTool`). The tool is a prank surface: it sends only two badge codes, and opens on a "MODERATOR privileges" broadcast or a styleId-34 whisper in one MD5-gated room — which is why hurlant `MD5`/`Hex` are ported alongside it, under `src/hurlant/`, the port's first non-Sulake AS3.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `habbo/quest`              | 64        | 29       | Partial/advanced. `HabboQuestEngine` is now actually instantiated (was never attached in `VortexMain` — the whole subsystem was dormant) and its quest/achievement message headers are registered (out: `GetQuests`=2895, `GetSeasonalQuestsOnly`=1236, `GetAchievements`=2435, `AcceptQuest`=985, `RejectQuest`=20, `OpenQuestTracker`=588, `StartCampaign`=3969; in: `Quest`=54, `Quests`=3398, `SeasonalQuests`=1390, `QuestCompleted`=1272, `QuestCancelled`=1425, `Achievements`=1969, `Achievement`=3981 — all previously unregistered in `HabboMessages`, so `registerMessageEvent` silently dropped every one of them). `HabboQuestEngine` also now subscribes directly to `HabboToolbarEvent.TOOLBAR_CLICK` for `HTIE_ICON_ACHIEVEMENTS` (was never wired — AS3's `onHabboToolbarEvent`), which is what the toolbar's real "PROGRESSION" popup click reaches (see `habbo/toolbar` row: `ProgMenuController`). Me-menu → `HabboToolbar.questEngine` → `showAchievements()` → `AchievementController` is also wired end-to-end (though the me-menu popup's own shipped layout has no achievements region in this build), and `AchievementController`'s full `refresh()` render pipeline (categories grid, achievement grid, details panel, badge images via `BadgeImageWidget`, both progress bars via new `ProgressBar.ts`) is ported and verified (synthetic-data injection through the real `onAchievements()` path, zero runtime errors, correct AS3-shaped asset URIs requested). **The "classic" quest UI is now ported too**: `QuestsList` (the quest campaign list, composing `QuestEntry`+`Campaign`+`Quest`+`EntryArrows`+`CampaignCompleted` per row), `QuestDetails` (the single-quest popup, reusing `QuestsList`'s entry builder), `QuestTracker` (the toolbar-extension-hosted HUD widget, full slide-in/out + completion-sprite + in-room-prompt-nudge animation state machine), `QuestCompleted` (the congratulation dialog) and `NextQuestTimer` (the delayed-quest HUD widget) — `QuestController` now owns and wires all five, `HabboQuestEngine.showQuests()` calls the real `onToolbarClick()` instead of logging a TODO, and `QuestMessageData` gained the `waitPeriodSeconds` field it was entirely missing (confirmed via all 6 AS3 quest wire parsers: never set from the wire, mutable client-side state instead, matching AS3 exactly). Two gaps found and fixed along the way: `linkReceived()`'s `achievements/{category}` deep link called the same no-category `showAchievements()` as the plain case, dropping `selectCategoryInternalLink()` entirely; the `quests` link case called the visibility-gated `showQuests()` instead of AS3's unconditional `questController.onToolbarClick()` (so a link click while the list was already open silently did nothing instead of toggling it closed). The seasonal calendar (`seasonalcalendar/MainWindow`+`Calendar`+`CalendarArrowButton`+`CatalogPromo`+`RareTeaser`), daily tasks (`dailytasks/`), and the two achievement-resolution popups remain explicit `TODO(AS3)` gaps; the reward track (`rewardtrack/`, primary-tree-only) is **complete** — message layer, data model, controller and all 18 views, attached under `IID_RewardTrackController` and ticked from `HabboQuestEngine.update()` — `QuestController.seasonalCalendarWindow` is a documented always-null stub and `onToolbarClick()` falls back to the regular list when the seasonal calendar is config-enabled, rather than silently doing nothing. The twinkle celebration sparkle (`Animation`/`AnimationObject`/`Twinkle`/`TwinkleImages`) is also a documented stub (`HabboQuestEngine.getTwinkleAnimation()` returns `null`) — `QuestCompleted` shows and functions correctly without it. Achievements (and now quests) stay functionally blocked against the reference server, which implements them as an empty stub (`docs/CLIENT-SERVER-ARCHITECTURE.md` §20).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `habbo/groups`            | 31        | 35       | Partial. **Group creation now works end-to-end** — `GuildManagementWindowCtrl` + the badge editor (5 files) + `ColorGridCtrl`/`GuildSettingsCtrl`/`GroupCreatedWindowCtrl`/`HcRequiredWindowCtrl`, see notes below. Extended profile (click-to-view, basic scope) works via `ExtendedProfileWindowCtrl`. `GroupRoomInfoCtrl` (the in-room "you are in X's base" panel) is ported but gated off — see the note below. `GroupDetailsCtrl` + `DetailsWindowCtrl` + `GuildMembersWindowCtrl` now ported: the group card, its standalone window and the paged members list, so the **Manage / delete / leave / base-room / forum / buy-furni / members / pending-members links all work**, along with accept, give and remove rights, block, unblock and kick. Still unported: `badge_leaderboard/` (4 files), relationship status, badge glow/popup.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `habbo/friendlist`         | 40        | 41       | **Fully ported 2026-07-29** — domain (categories/requests/tabs/search results + the 4 Deps), all 11 views, the look-and-feel, both event classes, and `HabboFriendList` itself rebuilt off the AS3 structure (the old flat-`Map` re-architecture is gone). Wired and running: `VortexMain.initFriendList()` constructs it with `HabboFriendListCom`'s asset library, and its bitmaps are in `App.ts`'s allowlist. See "Recent Work Recorded".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `habbo/friendbar`         | 146       | 137      | Landing view (hotel view, `landingview/`) is fully ported: controller, 6-slot dynamic grid engine, all 22 widgets, `GenericWidget` + its ~24-type element-handler system, background parallax, wire protocol. **The bar itself now is too, 2026-07-29**: `data/` 8/8, `view/` 22/22 (`HabboFriendBarView` + the 9 tabs + 6 tokens + 4 utils), `events/` 7 — and `HabboFriendBar`'s two `attachComponent()` calls, commented out because neither component existed, are live. **Group forums followed, 2026-08-10**: `groupforums/` 17/17 (`GroupForumController`, `GroupForumView`, the three list views and their item views, compose, settings, `StringBuffer`, `ForumModerationState`, `IGroupForumController`, `UnseenForumsCountUpdatedEvent`), the third `attachComponent()` call live with them, and `HabboToolbar` subscribed to `UNSEEN_FORUMS_COUNT` for the me-menu badge. Still 0%: talent track (`talent/` 0/6), epic popup (`popup/HabboEpicPopupView`), and the onboarding flow (`onBoardingHc*`, out of scope — unrelated login/account-creation system).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `habbo/notifications`      | 34        | 18       | Partial. Several notification messages exist; manager/UI coverage is not complete.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `habbo/messenger`         | 12        | 15       | `HabboMessenger` + `DummyFriend` ported 2026-07-29 and attached against `IID_HabboMessenger`, which nothing provided before — a *required* dependency on an unprovided IID silently locks the dependent forever, which is what kept the friend bar from ever appearing. `MainView` (1237 l.) is ported and built on `MessengerInit`; **its four incoming handlers were empty stubs until 2026-08-02** — every received message, history fragment, room invite and IM error was read off the wire and dropped before reaching the view (see "Recent Work Recorded"). MiniMail (3884/74) landed the same day, behind `client.minimail.embed.enabled` as AS3 gates it. The only gap left is the habbicon picker, and it is not a messenger-sized one: `messenger/habbicons/` (1399 l.) needs `catalog/habbicons/` (5089 l., unported), `habbicons/assets/` (938 l.) and the `_SafePkg_1920` DTOs behind them — **7.4k lines of AS3 for a shop the emulator has no habbicons to stock**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `habbo/freeflowchat`       | 33        | 35       | Live and wired — the mandatory chat-bubble renderer (primary/2026 source drops the old `roomchat` bubble system entirely). Asset pipeline, full bubble rendering, real `ChatFlowStage` gravity/AABB-collision physics, avatar face-crop, and the style-selector popup are all ported — see notes below. Only the chat-history drag-down tray remains explicitly deferred.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `habbo/advertisement`      | 5         | 7        | Small module present; verify behavior before marking complete.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `habbo/campaign`           | 5         | 7        | **Complete 2026-08-17** — the advent calendar's 4 view files landed, and `HabboCampaigns` gained the 5 dependencies and the whole product-notification path it was missing. Verified headlessly against AS3's own debug fixture; see Recent Work.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `habbo/tracking`           | 10        | 10       | Structurally aligned by file count.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `habbo/utils`              | 28        | 23       | Partial/advanced.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `habbo/game`               | 63        | 0        | Not started.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `habbo/sound`              | 29        | 32       | **Effects work as of 2026-08-02.** The manager (`HabboSoundManagerFlash10`, 585 l.), both sound classes, the 4 interfaces and both enums are ported, attached against `IID_HabboSoundManager` in `VortexMain`, and fed by the 21 embedded mp3s the new `tools/import-crypted-sounds.mjs` extracts. **The rest of the module followed (trax from 2026-08-06, re-measured 2026-08-13)**: `music/` (6 files, `HabboMusicController` is constructed for real in `onRoomEngineReady`), `trax/` (7 files, the full sequencer chain), `furni/` (`FurniSamplePlaybackManager`) and `events/` (6 events) all exist — 32 TS vs 29 AS3, 2 `TODO(AS3)` left in the module. See "Recent Work Recorded".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `habbo/roomevents`         | 448       | 395      | In progress (`wired_setup` + `wired_menu` scope). **From ~55 TS files on 2026-07-19 to 395 on 2026-07-28** — the single biggest movement in the port this period. **Bloc A (foundation spine):** `HabboUserDefinedRoomEvents` DI component (attached in `VortexMain`) + `WiredEnvironment` + `IncomingMessages` handler routing 14 wire events; the def+variable data model (`Triggerable` + 6 furni-def parsers + `WiredContext`/variable DTOs) landed via multi-agent Workflow. **Bloc D (uibuilder foundation):** `roomevents/Util`, `WiredStyle` base + 6 concrete skins, `WiredUIPreset` base + wrappers + a `PresetManager` grown to the full leaf factory set (dropdown/variable-picker/value-or-variable/chrono/sub-variable/level-xp/floor/wrapper factories), the `newvariablepicker` cluster, and `getXmlWindow()`. **Bloc B (leaf types) — COMPLETE:** all six element-type registries (`ActionTypes`/`ConditionTypes`/`SelectorTypes`/`AddonTypes`/`VariableTypes`/`TriggerConfs`) instantiate and resolve every AS3 concrete type, including the last clusters — chests (give/transaction/chest-has/scanner/custom-contract + `TradeRequirementRule`/`TradeRequirementNode`/`ChestItemType` data), chrono (`TimeMatches`/`DateMatches` + `ChronoMatchBase`/`ChronoFieldRangeFilter`/Chrono presets), sub-variable addons (`VariableTimeUtil`/`Projectile`), `VariableLevelUp` (+ levelupper math + `LevelXpPreviewPreset`, IUpdateReceiver-driven), area selectors (`UsersInArea`/`FurniInArea` + `IRoomAreaSelectionManager`), and neighborhood selectors (`UsersInNeighborhood`/`FurniInNeighborhood` + `NeighborhoodFloor`/`SpiralUtils`). `getByCode` matches `code` **or** `negativeCode` (fixed systemic bug). Two visual editors are inert stubs pending un-ported rendering infra — `RoomAreaSelectionManager` (tile highlight) and `FloorDrawingPreset` (bitmap tile canvas); both leaves still (de)serialize faithfully. Still in progress: `UserDefinedRoomEventsCtrl` (Bloc C). **Bloc E (Wired Menu) — foundation COMPLETE:** message layer done (`SetWiredMenuPreferencesComposer` 3124 + `RequestWiredRoomLogsComposer` 706 added; account-prefs 724 / YouAreController 934 / `WiredPermissions` 3483 already registered), the **real** `WiredMenuController` DI component (7-dependency set, permission surface, account-preference toggles, play-test mode, `wiredmenu/` link-event routing incl. open/logs/inspection/overview deep links, `sendPreferences`, sub-controller ownership — replaces the old stub; message events register in `initComponent` since DI resolves after ctor here), the **real** `WiredMenuView` window shell (`wired_menu_view_xml` via `buildWidgetLayout`, lazy tab creation, tab select/align, show/hide on the desktop), the **real** `WiredMenuDefaultTab` base (active/viewing/loading state + message-event registry + IUpdateReceiver auto-registration) + `WiredMenuTabConfigs` registry + all 6 tabs (`WiredMenuInfoTab` real; monitor/overview/inspection/chests/settings are `TODO(AS3)` stubs carrying the exact controller-facing entry points — `inspectFurni`/`inspectUser`/`jumpToVariableByName`/`updatePreferencesUI` + the `IUpdateReceiver` shape). Sub-controllers (`WiredRoomLogListController` + `VariableManagement{Overview,Detail}Controller`) are ctor/dispose/`view`/`send` spine stubs. The temporary `IncomingMessages` WiredPermissions bridge (`applyPermissions`) was removed — the real controller registers `onWiredPermissions` itself, exactly as AS3. **Bloc E tabs — 5/6 DONE (real): info + settings + monitor + overview + inspection** (2026-07-24). `WiredMenuSettingsTab` (471 l) — permission-mask checkbox grids w/ AS3 dependency cascades, timezone dropdown, reload/roll-back (confirm dialog), preference checkboxes + UI-style dropdown, all 4 settings messages (`WiredMenuSettingsEvent` 491 + parser, `SaveWiredMenuSettings` 2553, `ReloadWiredRoomState` 501, `RequestWiredMenuSettings` 1862) + `STYLE_OPTIONS`. `WiredMenuMonitorTab` (405 l) — live room-resource stats (threshold-colored HTML captions + "panicking Frank" image w/ hotspot report), self-refreshing execution-error TableView (`ErrorDataTableObject` + `WiredErrorInfoView` popup), clear-log + room-log-overview buttons, POLL_MONITOR_MS polling via update(); its message layer (`WiredRoomStatsEvent` 1964 + `WiredRoomStatsData`, `WiredErrorLogsEvent` 3419 + `WiredErrorData`, request-stats 427/request-errors 452/clear 2386/report 3608 composers). **Table-view backbone COMPLETE** (`habbo/window/utils/tableview`, the shared virtualized row-renderer): `DeBouncer` (setTimeout-backed) + `ITableObject`/`TableCell`/`TableColumn`/`CellTemplate` + the view cluster `TableRowModel`/`TableCellView`/`TableRowView`/`TableView` (~2000 l total; TableCellView's highlight fade uses inline setInterval, the last flash-Timer use). `WiredMenuOverviewTab` (690 l, on the ported `WiredVariablesSynchronizer` + `VariableTableObject`/`PropertyTableObject`/`TextTableObject`/`VariableInfoBubbleView`/`VariableHoldersHighlighter`) and `WiredMenuInspectionTab` (838 l, + `VariableHolderPreviewer`/`VariableValueTableObject`/`VariableTypePicker`) are both real, on a new `common/PagedTableView` + `LoadingIcon` backbone. **Sub-controllers — 2/3 real:** `WiredRoomLogListController` (273 l + view 356 l + table object) and `VariableManagementOverviewController` (276 l + view 283 l + table object) are ported; `VariableManagementDetailController` is still a 53-line spine stub against AS3's 267. **Remaining Bloc E:** the `WiredMenuEvent` toolbar dispatch and the `WIRED_MENU_BUTTON_PREFERENCE_CHANGED` event, plus the `wiredmenu/open/settings` click-link 4th arg in `WiredMenuController`. The detail sub-controller, the `chests` tab and `wiredChest` itself have all landed (2026-08-14/15), and with `wiredChest` real, `wiredChest.onPermissionsChanged()` is called rather than deferred. **Bloc C (`UserDefinedRoomEventsCtrl`, 1,362 l) is display-complete but carries 16 `TODO(AS3)`s** — advanced input sources, copy-into paste mode, button modes 2/3, `fixQuantifierNames()`, and 6 wired-menu cross-calls now unblocked by Bloc E. Trading out of scope. |
| `habbo/nux`                | 4         | 5        | Ported and wired 2026-08-06 (`b5e8bc93`): `HabboNuxDialogs` + the 3 views (`NuxGiftSelectionView`, `NuxNoobRoomOfferView`, `NuxOfferOldUserView`), attached against `IID_HabboNuxDialogs` in `VortexMain`. Message layer: incoming 3/6, outgoing 3/4 — the remaining gap is wire, not UI.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `habbo/phonenumber`        | 7         | 8        | Ported and wired 2026-08-06 (`b5e8bc93`): `HabboPhoneNumber` + both collect/verification-code view pairs + both enums, attached against `IID_HabboPhoneNumber` in `VortexMain` (AS3 attaches it from `HabboToolbar.as:152`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `habbo/userclassification` | 1         | 2        | `UserClassificationData` (the module's single DTO) ported 2026-08-06; its message category is still 0 (incoming 0/1, outgoing 0/2, parser 0/1).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `vortex-client/src/onBoardingHc*` | 23 | 24 (+11) | The post-login new-user flow (`onBoardingHc/` 4, `onBoardingHcSteps/` 4) and the widget kit both flows are built from (`onBoardingHcUi/` 16 vs 15 AS3). The +11 are `onBoardingHcUi/display/` — a TS-only canvas-2D stand-in for the `flash.display`/`flash.text` classes the widgets are written against (Stage, DisplayObject, Bitmap(Data), TextField, Scale9Shape, filters, geom, Timer, tweens); it has no AS3 counterpart and is where the runtime-behaviour bugs live, not in the ported widgets. |
| `vortex-client/src/login`  | 10        | 12       | File-for-file against `WIN63-.../src/login/`: Environment/Login/SsoToken/Avatar/WebCaptcha + `LoginFlow`, `Background`, `ImageLoader(Event)`, `ILoginContext`. **This row used to claim Register and AvatarCreate screens** — both were invented, traced to files in no dump, and were deleted by the 2026-07-31 rebuild (the 701 client has no registration screen; account creation is web-side). Runtime-fixed 2026-08-01, see the log below. Untested by hand: `AvatarView` (account chooser) and `WebCaptchaView`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

---

## Communication Messages

Counted against `win63_version/habbo/communication/messages/`, which lays the classes out per
category (the primary tree keeps them under `src/unknowns/`). Re-measured 2026-08-13:

| Message type       | AS3 files | TS 2026-07-19 | TS 2026-07-28 | TS 2026-08-13 | Raw remaining delta |
|--------------------|-----------|---------------|---------------|---------------|---------------------|
| Incoming events    | 700       | 349           | 480           | **630**       | 70                  |
| Outgoing composers | 547       | 292           | 373           | **527**       | 20                  |
| Parsers            | 630       | 347           | 445           | **614**       | 16                  |
| **Total**          | **1,877** | **988**       | **1,298**     | **1,771**     | **106**             |

**+473 message files between 2026-07-28 and 2026-08-13** (on top of +310 the nine days before),
and the table above understates what remains: the raw delta nets off categories where the port has
*more* files than `win63_version` (see the caveat below). Summed per category, the real remaining
gap is **361 files** (~711 on 2026-07-28), of which 21 are the `moderator`/`moderation` naming
artefact — **~340 net**.

**`userdefinedroomevents` is closed** — it was the #1 gap at 55/25/31 and is now `64/55`, `29/25`,
`36/31`, i.e. complete plus the port's own additions. `game` keeps the top spot, essentially alone:
it is 122 of the 361 remaining files.

> **⚠ The two tables below are a routing hint, not a backlog — verified 2026-08-13.** The naming
> caveat further down is not an edge case, it is most of the number. Hand-checked that day:
> `roomsettings` outgoing reads 0/9 and **all nine composers exist**, under
> `outgoing/room/settings/`; `vault` outgoing reads 0/4 and all four exist under
> `outgoing/inventory/`; `gifts` outgoing reads 0/5 and the AS3 directory called `gifts` actually
> holds *phone-number* composers, all of which exist under `outgoing/preferences/`; `moderator`
> outgoing 0/21 is the long-known `moderation/` split. On top of that, a large share of the
> remainder is obfuscated `class_N.as` files that cannot be matched by name at all (24 in
> `incoming/navigator`, 16 in `incoming/users`, 14 in `incoming/catalog`), so they count as gaps
> whether or not they are ported.
>
> **The reliable measure is `node scripts/wire-coverage.mjs`**, which joins the client's own
> registry to the emulator's header table by id and reports what the *server* can do that the client
> cannot reach. At 2026-08-13: **52 send gaps** (44 with a real server handler waiting) and
> **57 recv gaps**. See "Wire coverage" below.

Top remaining gaps by category (TS/AS3 per column, ranked by summed gap, re-measured 2026-08-13):

| Category         | Incoming     | Outgoing     | Parser       | Gap   | Notes                                                               |
|------------------|--------------|--------------|--------------|-------|---------------------------------------------------------------------|
| `game`           | 5/44         | 0/27         | 5/61         | 122   | SnowWar/game protocol almost entirely absent; `habbo/game` is 0/63. Unchanged across every pass since 2026-07-19 — a third of the whole remaining gap. |
| `camera`         | 6/6          | 10/10        | 6/6          | 0     | **Closed 2026-08-13**, widget and handlers included.                |
| `catalog`        | 40/51        | 34/40        | 35/39        | 21    | Down from 51. Targeted offers, room ads, LTD raffle, vouchers still unported. |
| `moderator`      | 0/0          | 0/21         | 0/0          | 21    | Outgoing-only category; a naming artefact — the port files these under `moderation/` (24/0 there), so the real gap is ~0. See the caveat below. |
| `roomsettings`   | 10/18        | 0/9          | 15/12        | 17    | **Artefact, not a gap.** All 9 composers exist under `outgoing/room/settings/` (+2 extra), and `RoomSettingsCtrl` (1,358 l.) is built by `LegacyNavigator`. Of the 8 incoming, 6 are obfuscated `class_N` and 1 (`RoomSettingsErrorEvent`) is filed under `incoming/navigator/`. Real gap ≈ 1. |
| `crafting`       | 0/4          | 0/5          | 0/6          | 15    | Untouched; `ui/widget/crafting` is 0/13.                            |
| `users`          | 51/55        | 42/47        | 34/39        | 14    | Down from 67 — the biggest single-category drop of the 2026-08-13 pass. |
| `vault`          | 0/4          | 0/4          | 0/5          | 13    | **Outgoing is an artefact** — all 4 exist under `outgoing/inventory/`. The incoming/parser side is real (the income-reward + credit-vault events show up as recv gaps in wire-coverage). |
| `callforhelp`    | 0/7          | 0/0          | 0/5          | 12    | Partly an artefact: 3 of the 7 incoming exist under `help/`, 3 are obfuscated, 1 (`MyCfhReportStatus`) is a real gap and appears in wire-coverage both directions. |
| `moderation`     | 15/26        | 24/0         | 26/19        | 11    | See naming caveat; the 11 incoming are the real gap.                |
| `talent`         | 0/3          | 3/3          | 0/8          | 11    | Outgoing complete; the talent track itself is still unported in `friendbar` (0/6). |
| `nft`            | 4/6          | 0/5          | 4/7          | 10    | Barely started.                                                     |
| `avatar`         | 2/5          | 1/5          | 2/4          | 9     | Barely started.                                                     |
| `quest` / `friendfurni` | 15/20, 0/3 | 16/18, 0/1 | 24/16, 0/3 | 7 ea  | Quest partial (see naming caveat); friendfurni messages untouched — its `ui/widget` cluster is 7/7. |
| `navigator`      | 46/51        | 37/38        | 28/28        | 6     | Mostly done.                                                        |
| `gifts`          | 3/3          | 0/5          | 3/3          | 5     | **Artefact, and a mislabelled AS3 directory**: `outgoing/gifts/` in `win63_version` holds phone-number composers, not gift ones, and all four exist under `outgoing/preferences/`. Real gap 0. |

Closed or nearly closed since the last pass, kept for the record:

| Category         | Incoming     | Outgoing     | Parser       | Gap   | Notes                                                               |
|------------------|--------------|--------------|--------------|-------|---------------------------------------------------------------------|
| `room`           | 107/106      | 116/97       | 106/102      | 0     | **Closed** (was 49 on 2026-07-28). TS exceeds AS3 where the port splits files. |
| `inventory`      | 53/56        | 35/29        | 56/55        | 3     | Down from 39; three incoming left.                                  |
| `sound`          | 8/10         | 11/9         | 8/8          | 2     | Down from 25 — the trax/jukebox wire layer landed with `habbo/sound`'s music port. |
| `collectibles`   | 23/20        | 18/18        | 33/29        | 0     | Message layer complete 2026-08-12. TS exceeds AS3 where the port splits DTOs into files of their own and counts the two inventory-side NFT messages here; it is not >100% coverage. |
| `groupforums`    | 10/9         | 13/12        | 14/13        | 0     | Complete 2026-08-10, and driven by `GroupForumController`.          |

Everything else is a tail of ≤5-file categories (`friendlist` 5, `campaign` 5, `userclassification`
4, `nux` 4, `hotlooks` 4, `availability` 4, `help` 3, `competition` 2, `roomdirectory` 1,
`register` 1); `mysterybox`, `preferences`, `poll` and `userdefinedroomevents` have no remaining
gap at all.

**Naming caveat — some categories look over-ported because the port files them differently than
`win63_version`.** `moderation` shows `24/0` outgoing and `moderator` shows `0/21`: those are the
same messages under two directory names, and they are 21 of the 361 files the gap table counts.
`help` (`33/32` incoming), `roomsettings` (`15/12` parser) and `quest` (`24/16` parser) are the
same effect. The port also has five categories `win63_version` does not (`newnavigator`,
`nftwardrobe`, `rent`, `wardrobe`, `vortex` — 45 files), which is why the summed gap is larger than
the raw column delta. A per-category count is a routing hint, not a parity measurement —
only reading the AS3 registry (`.../habbo/communication/_SafeCls_2046.as`) settles what exists.

**File counts do not prove a message is reachable.** The 2026-07-24 room audit found 23 incoming
messages that were *written and registered* yet never subscribed, and 42 more registered-but-unused —
they counted as ported in every table above and did nothing at runtime. See "Recent Work Recorded".

### Wire coverage — the measure that replaces the tables above (2026-08-13)

`node scripts/wire-coverage.mjs` joins the client's registry (`HabboMessages.ts`) to the emulator's
header table by **id**, so it is immune to every failure mode the file-count tables have — directory
naming, renamed files, obfuscated `class_N`. It also reports whether the emulator has a real
`*MessageHandler.cs` behind each gap, which is what separates "a feature the server is waiting for"
from "a message nobody implements on either side".

```bash
node scripts/wire-coverage.mjs          # summary + the families that matter
node scripts/wire-coverage.mjs --full   # every gap with its id
```

At 2026-08-13 — emulator 506 client→server / 511 server→client, client registry 480 / 480:

| Direction                                        | Gaps | Meaning                                                    |
|--------------------------------------------------|------|------------------------------------------------------------|
| **Send** (client cannot trigger)                 | 52   | 44 have a real server handler waiting                       |
| **Recv** (server data the client drops)          | 57   | A screen stays empty or stale rather than erroring          |

By family, excluding SnowWar (17 send / 22 recv — see `habbo/game`), every remaining cluster is
small and self-contained:

**"Server" below means the handler body does something** — not that a `*MessageHandler.cs` exists.
The distinction is not academic: 117 of 515 handlers are empty stubs, and reading the filename
instead of the body is what made camera and crafting look ready (see the camera section above).

| Family                    | Send | Recv | Server | Client-side state                                        |
|---------------------------|------|------|--------|-----------------------------------------------------------|
| Crafting                  | 5    | 4    | **none — all 5 stubs** | `ui/widget/crafting` 0/13, messages 0/15     |
| Camera / photos           | ~~5~~ 0 | ~~4~~ 0 | **none — all 5 stubs** | **client-side complete 2026-08-13** (messages + `ui/widget/camera` 9/9 + both handlers + both RWE cases); blocked on the engine gaps above, and on a mute server |
| YouTube / Vimeo furni     | 3    | 3    | **none — stubs**   | the **only 2 files missing** from `ui/widget/furniture` (51/54) |
| Talent track              | 3    | 3    | 0 of 3 real  | `friendbar/talent` 0/6                               |
| Room state / interaction  | 8    | 2    | **4 real**   | scattered singles (entry tile, occupied tiles, click-character, furni inventory out of room) |
| Targeted offers / shop    | 3    | —    | **2 real**   | `habbo/catalog` finishing                            |
| Talents (resolution achievements) | 2 | — | **2 real** | `GetResolutionAchievements`, `ResetResolutionAchievement` |
| NFT / collectibles        | 1    | 1    | **1 real**   | `GetNftCredits`                                      |
| Wired                     | 1    | 2    | **1 real**   | permanent-variable slice                             |
| Treasure hunt             | 1    | 3    | none         | no client module                                     |
| Income reward / vault     | —    | 3    | —            | composers exist; nothing reads the responses         |
| Moderation (staff)        | 2    | 1    | none         | `habbo/moderation` 43/43 (2026-08-19)                |
| Seasonal calendar         | 1    | 1    | none         | documented `QuestController` stub                    |

### Header cross-check against the emulator (2026-08-02)

Three-way diff of `HabboMessages.ts` ↔ `vortex-emulator`'s `Revision20260701/Headers.cs`, arbitrated
by the WIN63 registry (`.../habbo/communication/_SafeCls_2046.as`). Recipe: parse
`this._events.set(id, Class)` / `this._composers.set(id, Class)` on our side (skip commented lines)
and `public const int Name = id;` per class on theirs — our `_events` ↔ their `MessageComposer`,
our `_composers` ↔ their `MessageEvent` — match on the name with the `Message`/`Event`/`Composer`
suffixes stripped, then resolve every disagreement by finding the id in `_SafeCls_2046.as` and
identifying the obfuscated class from its parser members and its call site.

Result: 21 wrong headers, all fixed. **5 were ours** — `NoobnessLevel` and
`AccountSafetyLockStatusChange` were swapped (70 ↔ 3913), `NavigatorSavedSearches` 866→432 (866 is
the unread-forums count), `ChangeEmailResult` 3909→2050 (3909 is `onTalentTrack`), and
`HabboGroupDeactivated` 2087→1948 (2087 is `onMembershipRequested`). **16 were the emulator's**,
including four id collisions where an invented header sat on a real one: NFT constants over
`GuildBaseSearch` (3744), `DeselectFavouriteHabboGroup` (306) and `PickupObject` (1919), plus
`CustomStackingHeightUpdate` over the wired `FurniAction` event (2552) — that last one was live,
serialized and sent by `RoomMap.cs`.

Two lessons for the next pass:

- **A name-based diff finds what a shape-based one cannot, and vice versa.** `RespectUser`/
  `Game2GetAccountGameStatus` had been resolved backwards by an earlier emulator-side pass, which
  parked `Game2GetAccountGameStatus` on a 9xxx placeholder. The WIN63 registry settles it in one
  line: it keeps `Game2GetAccountGameStatusMessageComposer` *unobfuscated* at 3377.
- **An id absent from `_SafeCls_2046.as` is a guess, wherever it comes from.** Every emulator error
  found here pointed at an id with no registry entry at all. Ours are worth the same check: 1935
  (`GetHeightMapMessageComposer`) has no registry entry and no sender — a dead registration.

Two same-named messages are *not* a conflict: `ChangeUserName` is 879 for the onboarding
`claimName()` (`NameChangeDialog.as:302`) and 1703 for the paid rename
(`NameChangeController.as:167`). We only implement 879, filed under `outgoing/help/`.

---

## Current Priorities

Re-ranked **2026-08-03**, biggest product gap first.

> **Read priority 0 first.** Three days of this week went on features that were already ported and
> simply never connected. Measuring that is cheap; porting the same thing twice is not.

0. **Sweep for ported-but-never-wired code, module by module.** This was by far the highest-yield
   work of the 2026-08-03 session and it is nowhere near exhausted. The failure mode is always the
   same: the class exists, type-checks and reads correctly, but nothing constructs it, subscribes to
   it or hands it a window — so it fails silently and looks like a missing feature. Four found in one
   day, each of which had looked like "not ported yet":
   - the client settings menu — `SettingsExtension` + 3 views held state and **built no window**
     (SolidJS-era stubs; see `project_no_solidjs`);
   - the in-room Room Info window — `HabboNavigator.openRoomInfo()` flipped a boolean and logged,
     while the complete 739-line `RoomInfoViewCtrl` sat on `LegacyNavigator`, unreachable;
   - the room-settings dialog — every reply existed, was registered and was parsed, and **nothing was
     subscribed**, so it was dropped on the floor;
   - 25 of the navigator's 47 incoming handlers, likewise unsubscribed.

   The recipe that found all four: for a module, list what AS3 registers/constructs
   (`grep -o "addHabboConnectionMessageEvent(new _SafeCls_[0-9]*(on[A-Za-z]*" <file>.as`), list what
   the port does, and diff — being alert to alias names. Then check each ported class has a
   constructor call, not just a definition.

   **Do not diff on the handler name alone.** Grepping the module for `onX` counts a same-named
   method on any other class of the module as "present", and that hid a missing registration on
   2026-08-14 (`QuestController.onActivityPoints()` masked the absent
   `ActivityPointsMessageEvent` subscription). Resolve each AS3 `_SafeCls_N` through
   `_SafeCls_2046.as` to its **id** first, then ask whether that id's event class is constructed in
   the port's handler — obfuscation-proof, and it also tells you whether the message is unported
   rather than merely unwired. **`friendlist`, `quest`, `notifications` and `groups` were swept that
   way on 2026-08-14** — see "The transverse-debt pass". Candidates not yet swept: `habbo/toolbar`,
   `habbo/navigator`'s remaining handlers, `habbo/session`.

   **`habbo/help` and `habbo/moderation` were swept 2026-08-09** — findings under Recent Work.
   Help had the module's whole CFH capture layer unwired (three registries fed by nobody);
   moderation had nothing, its gap is unported UI. A third failure mode showed up in help and is
   worth grepping for elsewhere: a handler ported as a *passive* object — same class name, same
   method bodies, but the AS3 constructor's `addEventListener`/`addMessageEvent` calls dropped, so
   it waits to be called by a caller that never existed.

   **`habbo/catalog` and `habbo/inventory` were swept 2026-08-06** — counts and findings under
   Recent Work. Neither had an unconstructed class; the gap in both is subscriptions. Note the
   idiom differs per module: catalog registers from `HabboCatalog.as` itself, inventory from a
   separate handler class (`_SafeCls_1951.as`, obfuscated in every tree), so grepping only the
   manager finds nothing and reads as "no messages". Find the registrations with
   `grep -rln "IMessageEvent" <module>/` first.
1. **`habbo/ui/widget/furniture` — 51/54, and `habbo/ui/handler` — 42/47**, re-measured
   2026-08-09 (the 2026-08-05 figures below were badly stale — 46/54 and 25/47 — because they
   counted obfuscated AS3 filenames as unported when the port carries them under recovered
   names; match handlers by the `RWE_*` id they declare, not by filename).
   **What is actually left in `widget/furniture` is one feature: video.**
   `YoutubeDisplayWidget` (448), `VimeoDisplayWidget` (150) and `_SafeCls_3218` (32, the
   player-state enum), plus their handlers `_SafeCls_3849` (207, `RWE_YOUTUBE`) and
   `_SafeCls_3484` (123, `RWE_VIMEO`) — ~960 lines. **It needs a deliberate deviation, not a
   port**: AS3 loads an embedded Flash player SWF through `flash.display.Loader`, which has no
   faithful equivalent; the web answer is a YouTube/Vimeo iframe, so decide the substitution
   before starting.
   The other 4 unported `ui/handler` files are whole unported clusters, not furniture:
   `CameraWidgetHandler`, `RoomThumbnailCameraWidgetHandler`, `CraftingWidgetHandler`,
   `PlayListEditorWidgetHandler` (`widget/camera/` 0/9, `crafting/` 0/13, `playlisteditor/` 0/9).
   **Track handlers with their widgets** — a widget whose handler is missing receives nothing.
   **Size these by the feature, not the file.** The trophy widget is 150 lines but its only
   dispatcher is that 331-line badge handler — the real slice is ~480 lines plus a badge-info
   composer/event/parser, a badge-rarity enum and two `HabboGroups` helpers. Porting a widget alone
   adds another entry to priority 0's list.
2. **Finish `habbo/roomevents`**. The enumerated remainder has been worked through — send gaps,
   `VariableManagementDetailController`, the `chests` tab, the sandbox donation tool, the reward
   notification, the contract editors, `wiredChest` and `wired_trading/transactions/` have all
   landed, and `wired_trading` is complete. What is left is the `WiredMenuEvent` toolbar dispatch,
   three `HabboUserDefinedRoomEvents` UI helpers nothing calls yet, and Bloc C's 16 `TODO(AS3)`s —
   several of which Bloc E has now unblocked. Re-measure the file count before quoting one; the old
   395/448 predates seven slices.
3. **Whole modules still at zero**: `habbo/game` — 0/63, and the `game` protocol at 122 files is
   the #1 message gap. **But 58 of those 63 files are `snowwar/`**: the manager itself is 5 files
   (`HabboGameManager`, `IncomingMessages`, `events/`, 2 obfuscated), and the rest is one large
   self-contained minigame, not infrastructure anything else waits on. Size it accordingly.
   `nux` (4/4), `phonenumber` (7/7) and `userclassification` (1/1) left this list on 2026-08-05.
   `habbo/sound` left it on 2026-08-02 at 10/29 — effects play. **`music/` was completed on
   2026-08-06** (controller + both play-list controllers, see Recent Work); what remains in
   **`habbo/sound` is complete as of 2026-08-06** (29/29): effects, music, the Trax sequencer and
   furniture samples. Songs play.
4. **Protocol batches, in gap order**: `game`, `users`, `collectibles`, `catalog`, `room`,
   `inventory`, `groupforums`. Read the naming caveat in the message section before trusting any
   per-category count.
5. **Retire the parity-audit backlog**: 94 majors + 68 minors from 2026-07-17 remain, each needing
   its own verification against the AS3 source before action — 3 of the 26 criticals did not survive
   that check, and acting on one would have broken the client.
6. **Social modules**: `habbo/friendlist` is 41/40, `friendbar`'s own bar is done
   (`data/` 8/8, `view/` 22/22), and the chat window's receive path was closed on 2026-08-02 —
   `habbo/messenger` is 9/12, with `habbicons/` (5 files, 1399 l.) and the two MiniMail events the
   only gaps left. `friendbar`'s `groupforums/` is 17/17 as of 2026-08-10, controller and UI both,
   and attached. Still shells: `habbo/moderation` 8/43, `habbo/groups` 21/31, and `friendbar`'s
   `talent/` 0/6.
7. **Standing rules, unchanged**: update this file and `docs/MESSAGES_PORT_BACKLOG.md` in the same
   batch as the port, not weeks later; never label a module complete from file count alone — mark it
   only after AS3 member-level API/lifecycle/dispose parity is checked.

---

## Member coverage (`scripts/as3-member-coverage.mjs`)

The inverse of `scripts/audit-as3-traces.mjs`. That one asks, of every `AS3:` citation the port
writes, whether the cited file exists and declares the member named. This one asks the question
that measures parity: of every AS3 file the port cites, which of its declared members has **no**
citation pointing at it.

It survives the obfuscation because it never compares names across the boundary. The port
deobfuscates as it writes — `_SafeCls_90.as` becomes `RoomEngine.ts` — but the ~15k trace comments
*are* the deobfuscation map, and the obfuscator left member names alone in 39,228 of 43,306
function declarations in the primary tree. The tool follows the trace to reach the file, then
compares names *inside* it, where they are readable.

**Measured 2026-08-20, primary tree only, 1.5s over the repo.** The second column is after the
same day's traceability pass (see "Trace hygiene" below); the measurable population *grew* because
repointed citations began resolving into the primary tree, so more of the port became visible — and
with it, more gaps. That is the expected direction, not a regression.

| | first run | after the trace pass | after `habbo/window` |
|---|---|---|---|
| AS3 files with >=1 `::member` trace | 877 | 879 | **888** |
| readable members declared in them | 15,884 | 15,955 | **16,035** |
| carrying a trace | 6,623 (41.7%) | 6,653 (41.7%) | **6,847 (42.7%)** |
| **absent from the TS — public/protected** | 819 | 787 | **693** <- the worklist |
| absent from the TS — private | 1,526 | 1,526 | 1,504 |
| present in the TS but untraced — public/protected | 4,445 | 4,495 | 4,493 |
| present in the TS but untraced — private | 2,471 | 2,494 | 2,498 |
| files cited at file level only (unmeasurable) | 1,877 | 1,907 | 1,909 |
| members whose AS3 name is obfuscated | 3,517 | 3,555 | 3,555 |
| citations to a file that does not exist | 80 | **7** | 7 |

**A false positive was removed before this number was trusted.** The presence check searched only
the TS files that *cite* the AS3 file, which assumes the port lives where the traces are.
`IRoomObjectSprite.ts` ports its interface faithfully and carries no `AS3:` citation at all, so all
30 of its members read as absent. The check now also looks at a TS file whose basename equals the
AS3 type name — deliberately narrow, since for `_SafeCls_90.as` nothing but the trace links it to
`RoomEngine.ts`. That took the worklist from 824 to **787**, and moved 37 members into the
traceability column where they belong.

The 787 sit in **225 files**, and they are concentrated: the top 10 files hold ~270 of them, while
104 files have exactly one. Worst first:

| absent | file | |
|---:|---|---|
| 48 | `habbo/room/_SafeCls_90.as` | RoomEngine |
| 34 | `core/window/components/ITextWindow.as` | |
| 33 | `habbo/room/_SafeCls_87.as` | IRoomEngineServices |
| 29 | `habbo/window/widgets/PixelLimitWidget.as` | |
| 26 | `habbo/window/widgets/BadgeImageWidget.as` | |
| 21 | `habbo/room/IRoomEngine.as` | |
| 18 | `core/window/components/TextController.as` | |
| 18 | `core/runtime/_SafeCls_56.as` | |
| 16 | `habbo/communication/demo/_SafeCls_98.as` | |

Spot-checked against the source before being treated as a backlog: RoomEngine's
`furniIconListenerKey` and `mouseEventsDisabledAboveY`, and ITextWindow's `antiAliasType`,
`borderColor`, `bottomScrollV`, `defaultTextFormat`, `embedFonts` and `gridFitType`, all have zero
occurrences in the TypeScript that carries their file's traces. The list is not inflated.

Note what closing an interface gap costs: adding a member to `ITextWindow` obliges every
implementor to declare it too, so these are not independent one-line edits.

Read the two gap columns as different work. **Absent** means the port never wrote the member —
that is the parity backlog. **Present but untraced** means the member exists in TypeScript and
carries no `AS3:` comment, which is a `.claude/rules/30-as3-traceability.md` violation and a much
cheaper fix. Conflating them is what made the first draft of this tool useless:
`IMAGE_QUERY_SCALE` is declared in `RoomObjectVariableEnum.as`, declared in the TS that ports it,
and untraced — listing it beside a member nobody wrote buries the real ones.

**The 1,877 unmeasurable files are their own finding.** 68% of cited primary-tree files carry no
member-level trace at all, so the tool cannot say whether the class is ported or merely
referenced. Counting them as 100% uncovered would be arithmetically true and analytically
worthless, so they sit outside the denominator.

### The first worklist module: `habbo/window`, 92 -> 0 (2026-08-20)

The measure's first consumer. `habbo/window` went from **92 absent public/protected members to
6**, and from 68.8% to 77.4% of its readable members traced; the global worklist went 787 -> 701.
Four things it turned up, in ascending order of how much they mattered:

- **Every `badge_image:` property a layout writes was dropped on the floor.** AS3's
  `set properties()` pushes each struct through `withoutNameSpace()` before handing it to the
  bitmap, whose own `set properties()` switches on `pivot_point`, `stretched_x`, `zoom_x` and the
  rest. The port forwarded the array **as it came in**, namespace included, so the bitmap's switch
  matched nothing. 42 shipped layouts write those keys — 57 `stretched_x`/`stretched_y`, 54
  `pivot_point`, 7 `zoom_x`/`zoom_y` — and every one of them was inert, which is to say every badge
  in the client drew at the default pivot and unstretched. The getter had the mirror bug: it
  returned only the widget's own two keys and never the bitmap's, minus `asset_uri`.
- **`_SafeCls_1989` had no TypeScript at all.** It is the bitmap-content property surface —
  `bitmapData`, `pivotPoint`, `stretchedX/Y`, `zoomX/Y`, `greyscale`, `etchingColor`,
  `etchingPoint`, `fitSizeToContents`, `wrapX/Y`, `flipX/Y`, `rotation` — that
  `IStaticBitmapWrapperWindow`, `IBitmapWrapperWindow`, `BadgeImageWidget` and `PixelLimitWidget`
  all extend or implement. The port had inlined five of its members into
  `IStaticBitmapWrapperWindow` and left a `TODO(AS3)` on `IBitmapWrapperWindow` listing the rest.
  It is now `core/window/utils/IBitmapDataContainer.ts`, the name recovered from PRODUCTION (the
  2016 declaration stops at `wrapY`; `flipX`, `flipY` and `rotation` are 2026 additions), and the
  two window interfaces extend it as AS3 does. That alone accounts for 55 of the 92.
- **`iterator` was missing from 21 of the 26 widgets, and wrong in 3 of the 5 that had it.**
  `_SafeCls_2028` — this port's `IWidget`, and declared in `core/window/`, not with the widgets —
  extends `IIterable`, so every widget declares it: 21 return `EmptyIterator.INSTANCE`,
  `RoomThumbnailWidget`/`RoomUserCountWidget` return null, `SeparatorWidget`/`IlluminaBorderWidget`
  return their child container's, `BalloonWidget` its border's. Three of the five that existed were
  written as *getters* returning the container's `iterator` **without calling it**, i.e. handing
  back the method object; `WidgetWindowController.iterator()` delegates with
  `typeof widget.iterator === 'function'` and would then have invoked it with the wrong `this`.
  All 26 are now the method form, and `IWidget` extends `IIterable`.
- **The glow is the one thing left unported, and deliberately.** `playGlow()` runs a 16ms timer
  that writes two `GlowFilter`s and a `ColorMatrixFilter` onto the widget window each tick.
  `GraphicContext.filters` stores its array and the window renderer never reads it, and the engine
  has no Flash filter value objects — the only ones in the port are the client-side CSS stand-ins
  under `onBoardingHcUi/display/Filters.ts`. `playGlow`/`clearGlow`/`glowColor` therefore ship as
  the real signatures with a `TODO(AS3)` naming the filters, the easing and the missing renderer
  support. Two callers are waiting on it: `InfoStandUserView` and `ExtendedProfileWindowCtrl`.

Also closed: `IWidget`'s citation, which pointed at `win63_version/core/window/class_3420.as` — a
class, not this interface, and one of the audit's unresolvable paths; `IResourceManager` gained
`createAsset()` and `removeAsset()`, both already on `ResourceManager` and both AS3 members of the
interface, which removes a cast in `BadgeImageWidget.forceRefresh()`; `HabboWindowType` gained
`SHAPE`/`GRADIENT`/`STROKE`/`BITMAP_FILL`; `AvatarImageWidget` gained `zoomX`/`zoomY` and the
`zoomBitmapData()` redraw they feed (no shipped layout writes them yet); plus
`HoverBitmapWidget.bitmapWrapper` and `RoomPreviewerWidget.toString()`.

Two new files carry derived names, both said so at the declaration: `BadgeImageType`
(`_SafeCls_3232`, obfuscated in all four trees on disk, including the 2016 one) and
`IPixelLimitWidget` (`_SafeCls_3624`, likewise).

#### The four widgets that rendered nothing

The last 6 members were one gap, not six: `PetImageWidget.imageReady`/`imageFailed`,
`FurnitureImageWidget.imageReady`/`imageFailed`, `CountdownWidget.update` and
`LimitedItemGridOverlayWidget.update` all belong to widgets whose `refresh()` was a stub reading
*"the UI layer handles rendering"* — the SolidJS-era phrasing this port no longer has a UI layer
for (see `project_no_solidjs`). Between them they are used by 32 shipped layouts and drew nothing.
All four now render, and `habbo/window` is at **0** absent public/protected members, 79% traced.

- **`FurnitureImageWidget`** asks the room engine for the image, keeps the pending request id
  against the type it was made for so a late `imageReady()` for a type the widget has moved off is
  ignored, and falls back to `placeholder_furni[_small]`. Its three setters — `furnitureType`,
  `scale`, `direction` — did not call `refresh()` at all, so even a working renderer would have
  drawn the constructor's value forever.
- **`PetImageWidget`** parses the figure with `PetFigureData`, calls `getPetImage()` with the
  custom parts, applies `zoomX`/`zoomY`, and halves them when `shrinkOnOverflow` is set and the pet
  would overflow its window. `petWidth`/`petHeight` now read off the pre-zoom bitmap AS3 keeps for
  exactly that purpose; they were two fields nothing ever assigned, so both answered 0. Its six
  setters were likewise refresh-less.
- **`CountdownWidget`** builds its display out of the `clock_base_xml` item list, cloning the
  layout's `counter` and `separator` items into `digits` groups and rewriting the `value`/`unit`
  captions per tick. The port had `digits` as a stored field; AS3 reads it back off the list as
  `(numListItems + 1) / 2`, which is what makes the setter's "did it actually change" test work,
  and the port's `getDisplayValues()` — a TS-only shim with no callers — is gone. It also
  registers itself as an update receiver at priority 10, which nothing did before, so the clock
  never ticked.
- **`LimitedItemGridOverlayWidget`**'s shine is a scroll, not a fade: the plaque asset is taller
  than the window, and every 10s a 250ms sweep walks the visible rectangle down it. `animated`
  now registers/removes the update receiver instead of only setting a boolean.

Two AS3 members had to be added under them: `RoomEngine.getFurnitureTypeId()` (delegating to
`RoomContentLoader.getActiveObjectTypeId()`, which already existed), and the derived-name
interfaces `IFurnitureImageWidget`, `IPetImageWidget` and `ICountdownWidget` — `_SafeCls_3791`,
`_SafeCls_3710` and `_SafeCls_2433`, all obfuscated in every tree, all implemented by exactly one
class each.

**One member stayed a TODO, and it is an asset-pipeline gap, not a widget one.**
`LimitedItemGridOverlayWidget.set serialNumber()` also draws the number, via
`habbo/window/utils/_SafeCls_4213.as::createBitmap()`, out of ten `unique_item_number_glyph_<n>`
assets. Those are not files: the WIN63 manifest (`src/layouts/2614_manifest_xml$fb2a1694….xml`)
declares them as named sub-rectangles of the single shipped `unique_item_label_number_glyphs.png`,
and this port's asset pipeline never reads that manifest. `BitmapDataAsset` already carries a
`rectangle`, so the missing half is registration. Whatever else that manifest names is invisible
for the same reason — worth measuring before the next asset bug is diagnosed as something else.

**Verified headlessly, and only as far as that goes.** `vortex-glaze` exposes
`window.glaze.state.openLayout()`, which makes it a widget-construction bench with no emulator:
eight layouts that between them instantiate `badge_image`, `countdown`, `pet_image`, `product_icon`
and `limited_item_overlay_grid` (`Achievement`, `AchievementResolutionCompleted`,
`AchievementResolutionProgress`, `AchievementsResolutions`, `collectible_view_xml`,
`product_icon_xml`, `furni_chest_contents_xml`, `gridItem`) all build with zero console errors and
zero page errors. What that does **not** cover: glaze has no room engine, so every `refresh()`
takes the placeholder branch and the `getPetImage()`/`getFurnitureImage()` half is still unexercised
— that needs the emulator and a login.

### `habbo/room` starts: the room could not be shielded from a UI panel (2026-08-21)

First slice off the room cluster, which is now the top of the worklist (RoomEngine 47,
IRoomEngineServices 33, IRoomEngine 20, `_SafeCls_2223` 13). `habbo/room` 145 -> 133 absent
public/protected members; global 693 -> 680.

`RoomEngine` had no way to suppress room mouse events under a UI panel. Absent: the
`mouseEventsDisabledAboveY` / `mouseEventsDisabledLeftToX` thresholds,
`setMouseEventsDisabledRect()` / `removeMouseEventsDisabledRect()` and the map behind them, the
private `isMouseEventDisabledByRect()`, and the guard at the top of
`handleRoomCanvasMouseEvent()` that reads all three. Three AS3 callers depend on it and one is
ported: `RoomToolsToolbarCtrl` registers the toolbar's `window_bg` rectangle on every
reposition and drops it when hidden or disposed — so until now **every click on the room-tools
toolbar also walked the avatar or selected the furniture underneath**.

Two things the slice turned up:

- **`RoomToolsToolbarCtrl.set visible()` was missing**, and it is one of the two hook points.
  AS3 overrides the base setter purely to call `updatePosition()` on show and
  `removeRoomMouseBlockRect()` on hide; the port had only `RoomToolsCtrlBase.set visible()`,
  which flips the window and nothing else. Note that `RoomToolsWidget` writes
  `_toolbarCtrl.window.visible` directly in two places, bypassing the setter either way —
  worth checking against AS3 when that widget is next touched.
- **The coordinate spaces do not match, and that is upstream's.** The rectangles come from
  `IWindow.getGlobalRectangle()` (desktop space) while `handleRoomCanvasMouseEvent()`'s x/y are
  canvas-local — `RoomDesktop.canvasMouseHandler()` subtracts the canvas' global position. AS3's
  own `canvasMouseHandler()` does the identical subtraction, so this is not a port slip: it
  works because the room canvas sits at the desktop origin. There is a comment on
  `isMouseEventDisabledByRect()` saying so, because the obvious "fix" would break it.

`HabboFreeFlowChat.disableRoomMouseEventsLeftOfX()` is ported with it, and is dormant: its only
AS3 callers are in `freeflowchat/history/visualization/ChatHistoryTray.as`, and the tray, its
scroll bar and its scroll view are all unported. That is a separate slice.

**Not exercised at runtime.** This path needs the emulator and a room; `vortex-glaze`, the bench
the widget slices used, has no room engine.

### Trace hygiene, 2026-08-20

`audit-as3-traces.mjs` is the forward direction and now exits 0. Three passes that day:

- **38 RoomEngine citations repointed** from `win63_version/habbo/room/class_34.as` to the primary
  `_SafeCls_90.as`, identified by `implements IRoomEngine`. All 32 distinct members were confirmed
  present in the primary file first. A first count of "21 present, 6 absent" was wrong: it tested
  `function <name>(`, which misses fields and every accessor (`function get <name>(`).
- **10 derived names annotated**, taking unannotated derived names to 0. Nine were genuine
  derivations (XMLVariableParser's HEX/UINT/POINT from their string values; RoomData's six fields
  from their readable accessors). The tenth, `getSortableSpriteList`, was not derived at all — it
  cited the 2016 `RoomSpriteCanvas.as`, which has no such member, and was repointed to
  `_SafeCls_3073.as:270`.
- **45 of 80 unresolvable paths repointed**, leaving 35. Package-number errors under
  `src/unknowns/`, directory moves within one tree, and citations to `win63_2021_version` /
  `win63_2023_version`, two trees absent from disk. The remaining 33 distinct paths were left
  deliberately: `class_N.as` basenames are not an identity across directories in `win63_version`
  (the cited `core/window/class_3420.as` "resolves" to a class where the port documents an
  interface), and the rest have no file of that basename in any tree. Both need identifying by
  members, which is not mechanical.

**What it does not measure.** Whether a covered member behaves like its AS3. A `TODO(AS3)` stub
with a correct trace counts as covered. A member the port deliberately renamed reads as a false
port gap. Every line is a worklist entry, not a verdict — read the AS3 body before porting
anything it reports, per `.claude/rules/00-mandate.md`.

```bash
node scripts/as3-member-coverage.mjs                      # whole port
node scripts/as3-member-coverage.mjs habbo/catalog --list # one module, enumerated
node scripts/as3-member-coverage.mjs --list --private     # include private members
node scripts/as3-member-coverage.mjs --all-trees          # include win63_version / PRODUCTION
```

---

## Cross-module parity audit (2026-07-17)

A member-level AS3↔TS comparison across every system except `communication/`, run by 11 parallel
agents against `WIN63-202607011411-782849652`. **188 divergences**, in code that already existed and
that `tsc --noEmit` reports clean — the compiler cannot see any of this.

Severity: 26 critical, 94 major, 68 minor. 178 confirmed by reading both sources, 10 plausible.
Browsable report: <https://claude.ai/code/artifact/a9ff8084-3d42-45e0-abfa-30ba08c4eaad>

**The 188 are not independent bugs — seven mechanisms repeat:**

1. **Comments assert things that are false.** Six cases, found by four agents that never spoke to
   each other. The worst cited its own grep: `ClubBuyController.showConfirmation()` was labelled
   "not a porting gap … confirmed by grep across the whole club/ + viewer/widgets/ surface". The
   grep was right; the conclusion was backwards — nothing calls it *directly* because
   `HabboCatalog.showPurchaseConfirmation()` is supposed to dispatch to it. Absence of callers was
   the bug's signature, read as proof of its absence. **This is the most expensive pattern: it
   closes the investigation at the exact moment it should have opened.**
2. **The correct code exists; the caller bypasses it.** `AvatarLogic` reimplemented
   `getExpressionTime()` wrongly while `AvatarAction.ts` held a faithful port; `PetLogic`
   reimplemented figure parsing off-by-one next to a faithful `PetFigureData`; `RoomManager` never
   called its own ported `hasVisualizationXML()`.
3. **A `default:` swallows the failure.** `RoomObjectFactory` covered 18 of AS3's 76 cases and
   defaulted to `FurnitureLogic`, so ~50 written-and-unused logic classes were unreachable.
4. **Data plumbed end to end, never consumed at the last step.** `furniture_selection_disable`
   written and never read; `_avatarDataContainer` filled and never used; `session.ownUserRoomId`'s
   setter wired to nothing.
5. **Type escape hatches hide bugs exactly where they matter.** `stuffData?: unknown` hid the
   IStuffData crash; `getAsset(): any` hides a `string` returned where a `BitmapDataAsset` is
   expected.
6. **A step skipped systematically.** `ILinkEventTracker` missing on both `HabboCatalog` and
   `HabboInventory`; `attachComponent(x, [])` on four components.
7. **Defaults inverted.** `purchaseOfferId` 0 instead of -1; `EmptyStuffData.compare()` returning
   true where AS3 returns false; `verifyClubLevel()` hardcoded to `return true`.

**Audit findings are leads, not verdicts.** Three of the 26 "criticals" did not survive
verification, and acting on one would have broken the client:

- *etching never drawn* and *greyscale never applied* — **false**. Both were implemented in
  `WindowComposite` (`drawEtching()` with all 8 AS3 offsets and 4 call sites; the exact AS3
  luminance matrix; `static_bitmap` is type 23 → `STATIC_BITMAP_WRAPPER` → `applyGreyscale()`).
  The agent grepped `TextController`/`SkinRenderer`/`BitmapSkinRenderer`, found nothing, and
  concluded absence — the behaviour lived in a class with no AS3 name. Four of its `core/window`
  findings share that single flawed premise, including *createRendererForType: 5 cases missing*,
  where the missing cases are real but the stated impact ("the root cause of the etching and
  greyscale") is not.

  **The agent was wrong about the pixels and right about the structure**, and the structural
  complaint hid a real bug none of the four stated: `LabelRenderer` had not been merged into
  `WindowComposite`, it had been *dropped*. Only `TextSkinRenderer`'s behaviour survived, so
  `vertical` was parsed, stored, and used by `TextLabelController` to swap its box's width and
  height — while nothing ever rotated the glyphs. Fixed by the split below.
- *RoomManager's DI dependencies missing* — **real divergence, wrong severity**. Adding them would
  block on IIDs nothing announces (AS3 provides them via a SWF component library this port doesn't
  have) and RoomManager would never initialize. The setter path is correct; see the comment there.

### Criticals fixed

26 were filed. One (*etching never drawn*) was refuted outright, leaving **25 real**; of those,
**24 are fixed** on `fix/as3-parity-criticals` (both packages typecheck clean after each) and one —
*RoomManager's DI dependencies* — is real but deliberately not acted on, because acting on it breaks
the client. See "Findings that did not survive" above. Highlights:

| Fix                                                        | Effect                                                                             |
|------------------------------------------------------------|------------------------------------------------------------------------------------|
| `RoomObjectFactory` switch: 18 → 74 of 76 AS3 types        | ~50 logic classes reachable; widgets open on click again                           |
| `Core.error()` + both frame-update loops                   | one `throw` in any room object no longer destroys the client                       |
| `IStuffData` unified on AS3's single interface             | crash on inventory click is now structurally impossible; 12 invented files deleted |
| `RoomSessionManager` gate + required deps                  | `gotoRoom()` parked again instead of racing the engine                             |
| `announceInterfaceAvailability()`                          | stops stranding receivers; a required dep can resolve                              |
| `HabboFreeFlowChat` requires the session manager           | chat renders (see below)                                                           |
| `clubLevel` normalisation + `hasVip` + `verifyClubLevel()` | HC members reach VIP layouts; non-HC get the HC centre                             |
| `QUESTS` → `PROGRESSION`                                   | the progression icon responds to clicks                                            |
| `UnseenItemTracker` wired                                  | "new items" badges work; also fixed a wire bug (see below)                         |

**Two bugs the audit missed, found while fixing:** `ResetUnseenItemsComposer` carried the payload of
a *different* AS3 message at header 699 (an extra int on the wire; dormant only because nothing sent
it) — 699 and 3771 are now two composers, as AS3 has. And `furniture_extra` was declared in AS3 and
written nowhere in the port.

**Majors: toolbar/quest cluster (8 filed, 6 fixed, 2 already-fixed-by-the-time-they-were-read).**
`HabboToolbar.ts` was missing 12 of its 14 AS3 public members (`navigator`/`questEngine` already
existed) — added `getIcon()`, `refreshPurseAreaIndicators()`, `getToolbarState()`,
`setUnseenItemCount()`, `onClubChanged()`, `createAndAttachDimmerWindow()`/`removeDimmer()`, and
`get soundManager/connection/freeFlowChat/messenger/roomEvents`, plus the dependency wiring each
needs. `soundManager`/`roomEvents` stay dormant (no `HabboSoundManager`/wired-events module is
attached against those IIDs yet — `IHabboUserDefinedRoomEvents.ts` already documented this;
`messenger` is live as of 2026-07-29, see below), matching this doc's own `RoomManager`-DI precedent: the correct member exists,
its producer doesn't yet. `BottomBarLeft.ts`'s `setToolbarState()` had `WIRED_MENU`'s visibility
hardcoded to `false`; now reads `toolbar.roomEvents?.showToolbarMenuButton()` like AS3 (currently
always `null`, for the same reason). `onNaviHover()` was entirely absent — added, routed through a
new `HabboToolbar.newNavigator` bridge getter, because this port wires the legacy `navigator` getter
directly off `IID_HabboNavigator` where AS3's returns `_newNavigator.legacyNavigator` (the object
that actually implements `showToolbarHover()`/`hideToolbarHover()`). `setUnseenItemCount()` wrote into
a `Map` nobody read; now creates/positions/shows the real counter window via the already-existing
`windowManager.createUnseenItemCounter()`. Two filed findings (`onNaviHover`'s toolbar listener and
`showAchievements()`→`achievementController.show()`) were already fixed by the time they were read —
confirms the mandate's "verify against source" rule again. `HabboQuestEngine.goToQuestRooms()` fired
`"navigator/goto/quest_rooms"` with no handler anywhere; now reads `quests.<prefix>.roomids`, picks a
room at random, and calls the real `navigator.goToRoom()`. `showQuests()` is left as a documented gap
(`emit('showQuests')` had no listener either, and `QuestController`'s `QuestsList` window — its
target — isn't ported, per that class's own notes) rather than papered over.

**Majors: session cluster (5 filed, 5 fixed).** `RoomSessionManager.disposeSession()` passed
`disposeEngine` as a 3rd `.emit()` argument instead of into the `RoomSessionEvent` constructor, so
`event.openLandingPage` (which `RoomUI.ts` checks to decide whether to reopen the hotel-view toolbar)
always read its default `true` — the landing page reopened on every `disposeSession(roomId, false)`
(room re-entry, `disposeGameSession()`) where AS3 suppresses it. The same method also called the
wrong engine method under the wrong gate (`disposeRoomInstance()`, only if `disposeEngine`) instead
of AS3's unconditional `purgeRoomContent()` (added to `IRoomEngine`/`RoomEngine.ts`, thin wrapper over
the already-ported `RoomContentLoader.purge()`) — room content leaked between two room entries.
`sessionReinitialize()` wrapped its whole body in `if(session)`, silently doing nothing when no
session existed at the old key; AS3 builds one (`skipOpc=true`) and continues regardless, which
`RoomSessionHandler.onRoomReady()`'s reconnect/forward path relies on. `UserDataManager.getUserBadges()`
fused a cache read with sending `GetSelectedBadgesMessageComposer`, so every badge read (infostand
refresh, list render) fired a network request; split into `getUserSelectedBadges()` (pure read) and
`requestUserSelectedBadges()` (the only one that sends), matching AS3's own separation. `badgesRank`
was missing end to end — added to `UserData`/`IUserData` (default `-1`, `-1` while blocked, mirroring
`achievementScore`), `UserDataManager.updateBadgesRank()`, and wired through both producers:
`RoomUsersHandler`'s initial user-list construction (`RoomUserData.badgesRank`, itself never wire-read
in AS3's own per-user DTO — stays `-1` there too) and its `onUserChange()` handler, which was entirely
unregistered (`UserChangeMessageEventParser` stopped after `achievementScore`, silently dropping an
unused string, a count-prefixed int-triplet list, and `badgesRank` off the wire — all now read to
match AS3's `_SafeCls_2646.parse()`; `RoomSessionUserFigureUpdateEvent` gained the matching field).

**Majors: core cluster (5 filed, 1 already-fixed, 4 fixed).**
`ComponentContext.announceInterfaceAvailability()` was flagged for fatal-severity/callback-clearing
bugs that turned out to already match AS3 exactly (fatal=true, no early queue-clear, calls receivers
with null) — verified against `_SafeCls_56.as` directly rather than acted on. `Component` was missing
`interpolate()`/`updateUrlProtocol()` (2 of 7 `ICoreConfiguration` delegation methods) — added, which
in turn let `CoreLocalizationManager.interpolate()` fall through to `super.interpolate()` when its own
localization-dictionary pass leaves unresolved `${...}` tokens, instead of returning them unexpanded
forever (AS3 `CoreLocalizationManager.as:449`). `CoreLocalizationManager` also had no `dispose()`
override at all — added, clearing `_localizations`/`_definitions`/`_nonExistingKeys`/`_acceptEmptyMap`
before `super.dispose()`. `AssetLibrary.ts` registered audio under `"audio/mpeg"` where AS3 (and every
caller) uses the literal `"sound/mp3"` as an internal type tag, so any `loadAssetFromFile(name, url,
"sound/mp3")` threw "Asset type declaration ... not found"; also added the missing `image/tiff`
declaration (`BitmapDataAsset`+`BitmapFileLoader`, both already ported). `IIDAssetLibrary.ts` claimed
"Based on AS3: com.sulake.iid.IIDAssetLibrary" — no such IID exists in any of the 3 source trees, AS3
threads a per-Component asset library through constructors instead; the DI bridge itself is working
code with two real consumers (`VortexMain.ts`, `AvatarRenderManager.ts`), so only the misleading trace
comment was fixed, now documented as a TS-only bridge with its rationale.

**Majors: habbo/ui cluster (2 filed, 2 fixed).** `ChatInputWidgetHandler` sent a chat message's raw
`styleId` straight to `sendChatMessage()`/`sendWhisperMessage()`/`sendShoutMessage()`; AS3 first
resolves it against `freeFlowChat.preferedChatStyle` (updating the stored preference on a real style
change, substituting the preference back in when the message's `styleId` is the `-1` sentinel) —
nothing was blocked by an unported module, `preferedChatStyle` and `container.freeFlowChat` already
existed on both interfaces, the resolution step was just missing. `RoomWidgetUserInfoUpdateEvent` had
neither `badgesRank` nor `selectedBadges`, which 30-as3-traceability.md treats as a violation on its
own (stub + TODO required, not silent omission) even though `selectedBadges`'s absence elsewhere was
already flagged; added both (`selectedBadges` stays an unproduced TODO stub, same Phase-1 scope cut as
`InfoStandUserData.ts`'s own note). `InfoStandWidgetHandler.handleGetUserInfoMessage()` now sets
`event.badgesRank` from `IUserData.badgesRank` (added earlier this session in the session cluster) —
unconditionally, unlike `achievementScore`'s `isActivityDisplayEnabled` gate. `InfoStandWidget.updateUserData()`
gained AS3's 6th `badgesRank` parameter (zero current callers, so zero risk); actually applying it to
`InfoStandUserData`/`InfoStandUserView` is left as a TODO alongside the same already-deferred
badge-display polish, rather than adding two more fields as a side quest.

**Majors: habbo/room cluster (1 filed, already-fixed — but a deeper related gap found and
documented).** `LegacyWallGeometry.getLocationOldFormat()`/`getOldLocation()`/`getOldLocationString()`
and `RoomMessageHandler.addWallItem()`'s `isOldFormat` branch — the finding's exact citations — all
already existed, correctly implemented, when read. What's still genuinely missing is one level up:
neither `RoomEngine.placeObject()` nor `confirmObjectMove()` branch on `category === OBJECT_CATEGORY_WALL`
to build a wall-location string and send it — `PlaceObjectMessageComposer.ts` already carried its own
TODO for this; `confirmObjectMove()` did not, so a matching one was added there (and a pointer added
in `placeObject()` itself) rather than rushing a multi-composer wall-placement wire implementation
sight-unseen. Placing/moving a wall item from the client still doesn't reach the server; receiving
one from the server already works correctly.

**Majors: habbo/inventory cluster (4 filed, 4 fixed).** `TradingModel.close()` only zeroed local
fields — no `TradeCancel` (`CloseTradingComposer`, header 3639, already registered) was ever sent and
`dispose()` incorrectly called `close()` (AS3's `dispose()` never does; it just tears down views and
nulls refs). Fixed both, and gave `TradingModel` a constructor-injected `IConnection` since it
previously had no way to send anything — `close()` has zero current callers (matches AS3's own
not-yet-ported Trading UI being the only caller), so this is correct-and-ready rather than reachable
today. `canAddMoreItems()` only checked the 9-group cap; past it, AS3 still allows stacking onto an
existing group via a category-specific key (`POSTER` uses the legacy string, `GUILD_FURNI` uses the
now-ported `getGuildFurniType()` reading a `StringArrayStuffData`, everything else is an `"I"/"S"`-
prefixed class id) — the signature changed to take the item's category/stuffData/stackable flag since
nothing called it yet either. `HabboInventory.setClubStatus()` had no HC-expiration timer at all; added
a `setInterval`-based one resending `ScrGetUserInfoMessageComposer('habbo_club')` (header 1071, already
registered) every minute while `0 < minutesUntilExpiration < 86400000`, torn down in `dispose()`.
`FurniModel.addOrUpdateItem()`/`removeFurni()` never told the catalog an item entered/left the
inventory; wired `catalog.itemAddedToInventory()` (a genuine call site now, though the receiving
method's own body — auto-placing a buy-and-place purchase — is a separate, newly-TODO'd gap) and
`updateActionView()`; `collectorHub.itemAddedToInventory()`/`itemRemovedFromInventory()` stay TODO'd
since `collectorHub` is a documented always-null stub with nothing to call into.

**Majors: habbo/avatar cluster (3 filed, 1 verified already-fixed, 2 fixed).** `getServerRenderData()`
returning an empty array was re-checked directly against source: neither AS3 consumer class
(`AvatarVisualization.ts`, `FurnitureMannequinVisualization.ts`) references it anywhere in this port,
matching this doc's own earlier note — left alone rather than building an unverifiable producer.
`AvatarImageBodyPartContainer`'s constructor took 3 params where AS3 takes 4 (the 4th, `faceOffset`, a
`Point`); its one call site (`AvatarImageCache.renderBodyPart()`) now remembers the face part's own
offset while iterating and passes it through, and `AvatarImage` gained `getFaceOffset()`/
`getHeadRegPoints()`/`isBlocked()` (the last is `return false` in AS3 too) — all three exist on the
interface but have no caller yet (`ChatBubble`/`PooledChatBubble` already consume a *different*
`style.faceOffset`, from `ChatStyle`, confirmed unrelated). `AvatarImageCache.reset()` and
`AvatarImage.resetCache()` — both entirely absent, no purge on a structure/figuredata reload — are now
ported; their own caller, `AvatarRenderManager.resetAllCaches()` iterating tracked `_activeImages`,
doesn't exist in this port either (no image-tracking list at all), so that wiring is a documented TODO
rather than an invented tracking mechanism.

**Majors: habbo/chat cluster (3 filed, 3 fixed).** `HabboNotificationItemStyle`'s constructor took 4
params where AS3 takes 7 — rewritten to extract `icon`/`internalLink`/`customLayout`/`customView` from
a style-map entry, let an explicit icon bitmap override it, and force `ownsIcon` false without one
(matching `dispose()`'s `_ownsIcon && _icon != null` gate); gained the 5 missing getters plus
`HabboNotificationItem.notificationId` (a computed `style.extraData['id']` read). `SingularNotificationController.addItem()`
gained the matching 7-param signature and a real `hasNotificationById()`-based dedup — both call sites
in `HabboNotifications.ts` updated for the new `iconBitmap` slot. `removeNotificationById()`/
`hasNotificationById()` (queue-only; the never-ported `HabboNotificationViewManager` half is TODO'd)
and `showModerationDisclaimer()`'s `RoomEnterEffect`-gated retry timer (making `MODERATION_DISCLAIMER_DELAY_MS`
finally used) are now real. AS3's per-type style-config-XML lookup that would *reject* an unknown
notification type has no config-loading system anywhere in this port to resolve against, so `addItem()`
never rejects — documented, not silently dropped. `maybeShowNewFeatureNotification()`/`onBadgeImage()`
stay stubs: their AS3 bodies need `NewFeatureNotification`/`HabboNotificationViewManager`, neither
ported. `ChatEventHandler.gameEventHandler()` (snowwar team chat) stays a class-level TODO — `habbo/game`
is entirely empty placeholder directories, so there is no `GameManager`/`GameChatEvent` to wire against;
`CHAT_STYLE_SNOWWAR_RED`/`_BLUE` stay declared-but-dead until that module exists, now with that reason
on record instead of silence.

**Majors: habbo/catalog cluster (2 filed, 2 fixed).** `CatalogNodeRenderable` was missing
`addSearchResultToList()` entirely — added alongside a `_renderDepth` field so `createWindow()` can be
called with an explicit render depth (search results) as well as the node's own tree depth (normal
list rendering), disposing the previous window first like AS3 does and never did here.
`HabboCatalog`'s ~24-missing-member finding turned out to be 3 false positives on re-verification
(`setCatalogBusy()`/`isBusy`, `linkPattern`/`linkReceived`, and the three `builderFurni*` getters were
all already ported) plus 21 real gaps: `purchaseOffer()`, `isNewIdentity()`,
`showHabbiconAlreadyOwnedAlert()`, `furniDataReady()`/`productDataReady()` (both wired as real
`IFurniDataListener`/`IProductDataListener` registrations, previously never called from
`initComponent()`), `notifications`/`avatarEditor` getters (DI-wired, the latter copying
`HabboLandingView`'s placeholder-manager pattern verbatim), `builderSecondsLeft`/
`builderSecondsLeftWithGrace` (data the parser already had, just discarded), `Purse.lastUpdated`, and
five new composers with real header IDs recovered from `_SafeCls_2046.as` (`purchaseNftOffer`,
`purchaseMintTokens`, `checkGiftable`, `getRoomAdsPurchaseInfo`, `purchaseProductAsGift` — the last
one's 9-param shape recovered from its only AS3 caller, `PurchaseConfirmationDialog.giveGift()`, since
neither the composer class name nor most of its param names exist in any tree). `roomSession` tracking
(`IID_RoomSessionManager` + `RSE_STARTED`/`RSE_ENDED`) didn't exist on this class at all — wiring it for
`getBuilderFurniPlaceableStatus()` also fixed `privateRoomSessionActive`, previously hardcoded `false`,
as the same AS3 handler sets both. `isHabbiconOfferOwned`/`isHabbiconOwned`/`specialItemsController`
stay `false`/`null` TODOs (`HabbiconController`/`SpecialItemsController` are both full unported
Components); `purchaseGameTokensOffer()` stays a TODO stub distinct from the already-stubbed
`buySnowWarTokensOffer()` (confirmed two separate real AS3 methods, not a naming collision) since its
`GameTokensOffer`/`SnowWarGameTokensMessageEvent` dependencies don't exist; `getMintTokenProductIcon()`
matches its two null-returning siblings for the same `Texture`-vs-`ImageBitmap` reason. This was the
last cluster in the 2026-07-17 parity-audit majors sweep.

**One fix broke chat, and the lesson generalises.** Restoring `RoomSessionManager`'s AS3-required
dependencies made it announce later, which pushed its announcement past
`HabboFreeFlowChat.initComponent()`. `ChatEventHandler` subscribes in its constructor behind
`if(roomSessionManager)` — AS3 has no such guard, because AS3 declares that dependency required. The
guard turned "throws loudly at startup" into "chat silently never renders". **Making a dependency
faithful to AS3 collapses the workarounds living in its shadow.** Expect more.

Diagnosing it needed a client-side packet tracer, which did not exist —
`core/communication/PacketLogger.ts`, console-driven (`__packets.on()`, `.unhandled()`). Server logs
cannot distinguish "packet arrived and nobody handled it" from "packet never arrived".

### Minors (11 clusters, 48 addressed)

Same audit, its 68 total minors — 48 were still open when this pass started (20 already closed
incidentally by the majors sweep above). Same rigor as majors: full AS3 read before each change,
never invent behavior, and verify every finding against current source before acting — three more
false positives surfaced this way (the `RETWE_OPEN_WIDGET`/`CLOSE_WIDGET` widget-target filter in
habbo/ui, the `hide.quests` rule and `navigateToURL()` in transverses — all already correct). Where a
finding depended on a genuinely unported subsystem (`HabbiconController`, `SpecialItemsController`,
`RoomShakingEffect`, badge-rarity fields, a re-architected friendlist/navigator), the fix is a
documented `TODO(AS3)`, not an invented stand-in. Two confirmed real AS3 bugs were ported faithfully,
bugs included, because both are provably dead in every caller in both trees:
`PollHandler.onPollOfferEvent()`'s `summary = headline` immediately overwritten by `summary = summary`,
and `NavigatorData.getEventCategoryById()`'s comparison against a property `FlatCategory` doesn't
have (kept the working lookup, documented the divergence instead of reintroducing the always-null
bug).

- **core** (10): `AssetLibrary.registerAssetTypeDeclaration()` now throws on a duplicate MIME type
  instead of silently overwriting; `ComponentEvents` split `UNLOCKED` into the public broadcast and
  AS3's separate `INTERNAL_UNLOCKED`; `CoreLocalizationManager` emits the real
  `LOCALIZATION_FAILED`/`LOCALIZATION_LOADED` constants instead of bare `'failed'`/`'loaded'` strings
  nothing was listening for; `RoomInstance.hasValueForName()` added (checks both split
  number/string maps this port uses in place of AS3's single map); `RoomObjectModel.getNumberArray()`/
  `getStringArray()` now return copies, not internal references. `Component`'s remaining findings
  (frame-update profiler wiring, `getInterfaceStructList`/`toXMLString`/`requiredDependencyIids`,
  `_context` surviving past `dispose()`) are documented `TODO(AS3)`s, each blocked on a structural
  difference from AS3 (no `Profiler`/`ProfilerViewer`, a different interface-management design in
  `ComponentContext`, a readonly field) rather than silently missing.
- **core/window** (4): a `WindowType.NULL` → `WindowController` safety net so an unresolved tag no
  longer drops its whole subtree; `WindowParser.windowToXMLString()` gained the dynamicStyle
  trim/flag-reset and a `<variables>` block (backed by a new `PropertyStruct.toXMLString()`,
  debug-only, zero current callers); `boxSizer.setAutoRearrange(true)` now runs unconditionally,
  matching AS3, instead of only inside the children branch; `TextController` gained a
  `default_text_format` setter stub (AS3 assigns a whole `TextFormat`; this port's formatting is
  already split into granular properties, so the stub just triggers a re-render).
- **habbo/ui** (7): `IRoomWidgetHandlerContainer` gained `getFurnitureOwnerId()`/`isOwnerOfPet()`/
  `navigator`; `isOwnerOfFurniture()` lost an invented `ownerId > 0 &&` guard AS3 doesn't have;
  `RoomWidgetFurniInfoUpdateEvent` gained `tradeable` and fixed `expiration`'s default (`-1`, not
  `0`); `tradeable` threaded end-to-end through `FurnitureData`/`FurnitureDataParser`/
  `InfoStandWidgetHandler`; `RoomToolsWidgetHandler` now subscribes/unsubscribes
  `PREFERENCES_UPDATED`; `RoomWidgetZoomToggleMessage` gained its real `ZOOM_TOGGLE` constant.
- **habbo/session** (4): `sendChatMessage()` now strips numeric HTML entities before sending like
  AS3; `start()`/`dispose()` now null and dispose `_openConnectionComposer` correctly instead of
  leaking the reference; `SessionDataManager` gained `getAllFloorItemDatas()`/`getAllWallItemDatas()`.
  The AS3 "Macklebee" easter egg in `RoomUsersHandler.onUsers()` (a bot triggering
  `RoomShakingEffect`) is a documented `TODO(AS3)` — that effect class doesn't exist in this port.
- **habbo/catalog** (3): `RoomPreviewer` gained `centerWallItems` and the matching
  `getCanvasOffset()` branch AS3 uses for wall-item preview centering; `PurchaseCatalogWidget`/
  `HabboCatalog` gained the habbicon-already-owned purchase guard and a `canPurchaseSelectedOffer`
  gate six button-enable call sites now use instead of the looser `extraParamRequirementsMet`.
- **habbo/inventory** (5): `BadgesModel` lost two invented behaviors (marking a badge active from
  load data, and a nonexistent active-badge cap); `Util.disableSection()`'s `DO_NOT_DISABLE` tag
  check is now a real early return; `HabboInventory` respects room-viewer mode on init and sets
  `_currentCategory` unconditionally on toggle; `GroupItem.getTradeableCount()` regained its
  `excludeLocked` param; `FurniModel` fixed `subCategorySwitch()`'s trading cases, stopped writing
  `_categorySelections` from non-user-driven selection, and reworked unseen-item promotion to check
  the tracker first. `updateBadge()`'s missing ownerCount/rarity and `saveBadgeSelection()`'s missing
  connection are documented gaps (a separate unported rarity feature; no connection reference on
  this model).
- **habbo/avatar** (3): `AvatarImage.getFullImageCacheKey()` lost an invented composite-key
  fallback for mismatched main/head direction; `getCroppedImage()` fixed to use the regPoint alone
  instead of combining it with an unrelated method's offset math; added the derived `MISC`/`PET`
  figure-part-type constants; fixed `EffectAssetDownloadManager.LIBRARY_LOADED`'s value and
  `AvatarAssetDownloadManager.MAX_SIMULTANEOUS_DOWNLOADS` (6, not an invented 4);
  `AvatarStructure.getParts()` now returns `null` (not a truthy empty array) so callers' `!partList`
  guards actually trip, and gained `getDefaultActionDefinition()`/`getDefaultLayActionDefinition()`/
  `getItemIds()`.
- **habbo/chat** (3): `NotificationType` gained 7 missing constants (dead in this class — zero AS3
  occurrences); `ChatEntry` gained `messageText` with a documented gap for AS3's typed message
  wrapper; `HabboFriendList.canBeAskedForAFriend()`'s missing sent-request dedup is a documented gap
  (this port's flat-Map friendlist re-architecture has no equivalent state to check).
- **habbo/room + habbo/navigator** (3): `RoomEngineObjectEvent`'s constants renamed to match AS3
  exactly (no invented `_OBJECT_` infix) plus 5 new ones added, with 4 consumer files updated;
  `PollHandler` now ports AS3's real copy-paste bug faithfully instead of the "fixed" version;
  `NavigatorData.getEventCategoryById()`'s divergence documented rather than reintroducing AS3's
  always-null comparison.
- **transverses** (6): `BottomBarLeft.getIconChildName()` now ports AS3's own private icon-name
  switch instead of delegating to the wrong (short-label) enum method; its `CAMERA` visibility rule
  now compounds onto prior per-tag visibility (`&&=`) instead of overwriting it; `HabboHelp`/
  `IHabboHelp` gained 8 getters and 3 CFH-flow methods, with ~16 further confirmed-absent members
  documented as zero current impact; `HabboWebTools` gained `HABBLET_PRIVACY`/`openPrivacy()`, a
  derived `TARGET_SELF` constant, an `isSpaWeb` setter (dead here — every AS3 use site is gated
  behind `ExternalInterface.available`), and `closeWebPageAndRestoreClient()`, and its
  `openExternalLinkWarning()` now emits on the existing `toolEvents` EventEmitter instead of calling
  `window.open()` directly.

This closes the 2026-07-17 parity audit entirely: 25/26 real criticals fixed (one deliberately not
acted on), 94/94 majors (3 false positives), and 48/48 open minors addressed.

### The window system

#### 2026-08-13 — the per-pixel mouse test, and a coverage pass

`WindowController.testLocalPointHitAgainstAlpha()` is the gate every mouse event passes through when
a window has `mouseThreshold > 0`, and it was **inert**. It calls `hitTestDrawBuffer()`, which looked
for a `BitmapData.hitTest()` method on the draw buffer; the buffer is an `OffscreenCanvas` and has no
such method, so the test returned "cannot tell" every time. On the shared-graphic-context branch AS3
answers `false` when it cannot tell, so those windows were **never hit at all**.

Measured on a glaze boot, walking all 460 live windows: 251 were on the alpha path and **169 of them
reported a miss at their own centre point**. `utils/BitmapHitTest.ts` now implements Flash's
semantics over the canvas (`getImageData` on a `willReadFrequently` context, cached per buffer), and
that number is **68**. The end-to-end mouse path was re-run before and after — buttons, hierarchy
rows and property inputs all receive `WME_DOWN`/`WME_UP`/`WME_CLICK` identically, and the render is
pixel-identical.

**`GraphicContext` now owns the buffer, as AS3 does.** It allocates in the constructor and
`WindowRendererItem` renders into it, so `fetchDrawBuffer()` is truthful and *both* branches of
`testLocalPointHitAgainstAlpha()` read real pixels. Two earlier drafts of this section called that
change a regression, because it moved the per-pixel test from 68 to 97 misses. It is not one, and the
evidence is:

- item buffer and graphic-context buffer are the **same object** for every non-shared-context window
  measured (78 of 78) — the buffer-identity theory was wrong;
- the `icon` held up as the counter-example has a **missing asset** (the one 404 on every glaze boot).
  Its buffer is blank because nothing drew into it, and the opaque screen pixel at its centre belongs
  to its parent. A miss there is the correct answer, and AS3 gives the same one;
- rendering is **byte-identical** before and after (same PNG hash), and all **14** probed click
  targets — buttons, dropdowns, checkboxes, hierarchy rows, property inputs, the bottom bar, the
  edited window on the canvas — still receive their events.

The lesson worth keeping: 68 → 97 measured *faithfulness*, not breakage, and reading it as a
regression cost two rounds of wrong explanation before anyone instrumented the thing.

**On the count of 68 itself — an earlier draft of this section was wrong, twice over.** It called them a
backlog of broken windows and blamed a "two-stage render" where `WindowComposite` draws content the
buffer never sees. Neither holds. `WindowComposite` blits the renderer item's buffer and nothing
else, as its own class note says; the render split already landed (see below). And measuring the 68
shows every one of them *has* a skin renderer — 30 `TextSkinRenderer`, 24 `BitmapSkinRenderer`, 8
`LabelRenderer`, 4 `BitmapDataRenderer`, 2 `NullSkinRenderer`. Roughly 38 are text and label windows
sampled at their centre point, which for left-aligned text falls between glyphs or past the end of
the string: a miss there is *correct*, and AS3 would answer the same. The scrollbar sliders have no
buffer because nothing has rendered them yet — they are off-screen in the layout the probe opened.

So the honest count of *suspect* windows is far below 68, and the number is an artefact of probing
centres rather than real hit points.

What is still open is narrower: `GraphicContext` does not allocate its buffer in the constructor the
way AS3 does. Wiring that up — with `WindowRendererItem` rendering into the same buffer — was tried
and took the miss count from 68 to **97**, including a plainly visible `icon`, so it was reverted.
The cause was never established. The likeliest suspect is the item's buffer-adoption path (`_refresh`
not being set when it adopts a buffer it has not drawn into), which would be a wiring bug rather than
an architectural one. Both spots carry a `TODO(AS3)` that says exactly this, and says to instrument
before theorising.

Also in this pass:

- `GraphicContext` was a hollow shell documented as feeding "the SolidJS client" (there is no SolidJS
  here). It now has AS3's real buffer API — `allocateDrawBuffer`/`releaseDrawBuffer`/`setDrawRegion`/
  `allocatedByteCount` — plus `getChildContextByName`, `showRedrawRegion`, `toString`.
- `GestureAgentService` (flick momentum: velocity decayed by 0.75 every 40 ms) existed in AS3 and as
  an inline no-op stub here. Ported and wired into `ServiceManager`.
- `SkinLayout` was missing `isFixedWidth`/`isFixedHeight`/`calculateWidth`/`calculateHeight`/
  `getDefaultRegion`. Ported.
- `MouseEventStage` was an **invented** name for AS3's `MouseListenerType`, and two of its three
  members were invented too — its `1` is `EVENTS_INSIDE_WINDOW`, not an "outside stage". Renamed, and
  `WindowMouseListener` now uses it instead of literal `0`/`1`/`3`.
- 14 AS3 files had no TS counterpart: `IChildEntity`, `IChildEntityArrayReader`, `IChildEntityArray`,
  `IEventQueue`, `IEventProcessor`, `IInputProcessorRoot`, `INotify`, `ITextFieldContainer`,
  `ISkinTemplate`, `ISkinTemplateEntity`, `ISkinLayout`, `BitmapSkinTemplate`,
  `BitmapSkinTemplateEntity`, `ITouchAwareWindow`, plus `WindowMessage`. Added; `ButtonController`
  and `DropBaseController` declare `ITouchAwareWindow` as AS3 does, and `BitmapSkinParser` builds
  `BitmapSkinTemplate`s.
- **125 `AS3:`/`@see` traces pointed at `sources/win63_2021_version/`, a tree that does not exist.**
  All repointed, each verified to exist in the tree named: 90 to the primary WIN63 tree, 28 to
  PRODUCTION (where the obfuscated interface names are readable), 7 enum tables to PRODUCTION.
  `core/window` now has zero references to a nonexistent tree.

`GenericEventQueue` is now ported and `MouseEventQueue` extends it, restoring AS3's split — including
the traversal flag that throws away a queue left open by a `begin()` with no matching `end()`.
`TextFieldCache` is deliberately **not** ported: it caches a configured `flash.text.TextField` per
style, an object this port does not have, and its one behavioural job — dropping the cache when the
style manager fires `change` — has no equivalent hazard here, because `GlyphAtlas` keys its registry
by font descriptor rather than style name, so a redefined style yields a new key rather than a stale
entry.

#### Traceability: the repo-wide pass, and how the first attempt went wrong

Across all four packages, **14,519 of 14,747** cited AS3 paths now resolve on disk, up from a tree
that still referenced `sources/win63_2021_version/` — a directory that does not exist — in 125 places
in `core/window` alone.

The first fixer checked only that the *replacement file* existed. That is not enough, and CLAUDE.md
says so: matching on the filename alone turns a stale citation into a confidently wrong one. It
produced 11 citations naming members the target file does not declare (`AvatarAction::EXPRESSION_67`,
`StuffDataBase::chestName`, `DesktopLayoutManager::setLayout`, …). Those five files were reverted and
the fixer rewritten to require the member to be **declared** in the file it points at. It resolves in
four steps, each verifiable: same class in another tree → the AS3 `extends` chain → a *unique*
declarer in the same package of the primary tree (this is how `core/utils/Map` was found to live on
as `_SafeCls_481`, which alone carries `replace()` and `getValueByIndex()`) → otherwise report, never
guess.

Six traces written during this same pass named **derived** identifiers as though recovered
(`GraphicContext`'s `_hasBitmapBuffer`, `_maskShape`, …). Corrected: the ones with a real name point
at it, the rest say plainly that they are derived and name the obfuscated identifier they stand for.
The same was done for `GradientController.DIRECTION_UP_LEFT`/`DIRECTION_DOWN_LEFT` (obfuscated as
`_SafeStr_10782`/`_SafeStr_10927`, and derivable only because their `*_RIGHT` siblings are readable)
and `ScrollBarController`'s `_smoothScroller` and `SCROLL_BUTTON_STEP`.

Every trace whose member is not declared where it points is now accounted for: **14** are constructor
traces, which cannot match in a file whose class is `_SafeCls_N`, and **98** carry an explicit
derived-name note. Zero are unmarked. That closes the rule CLAUDE.md states as "derive it and say so
at the declaration — never pass a derived name off as recovered", which the port had been quietly
breaking at scale.

Still open: **64** cited paths that resolve in no tree, mostly `unknowns/_SafePkg_*` and `class_N`
citations. Each needs identifying against the source by its members; scripting it further would only
reproduce, at scale, the wrong-citation mistake described above.

**Re-measure with:**

```bash
# every cited AS3 path that does not exist on disk
node scripts/audit-as3-traces.mjs
```

#### The renderer split

`core/window/graphics/renderer/` now ports **all 22 AS3 renderer classes, one for one**, and
`WindowRendererItem` drives them through `SkinContainer.getSkinRendererByTypeAndStyle()` exactly as
AS3 does. `WindowComposite` draws no window content at all: 470 lines, down from 1673, keeping only
what Flash's display list gave for free — walk the tree, blit each buffer, apply blend and the
dynamic-style transform, hit-test.

This document previously argued *against* the split ("splitting risks the documented pixel-work cache
for no visible gain"). Wrong on both halves. The cache moved with the code and still skips identical
work. And the gain was not cosmetic: the split is what surfaced `LabelRenderer` having been **dropped
rather than merged** — `vertical` was parsed, stored, used by `TextLabelController` to swap its box's
width and height, and never applied to the glyphs.

Three things worth keeping, because each was a deduction that turned out wrong until checked:

- **The port cannot be line-for-line, and that is structural.** AS3 does no text layout in these
  renderers — Flash's `TextField` has already measured, wrapped and rendered the glyphs before
  `draw()` runs, so `TextSkinRenderer.draw()` is two `BitmapData.draw()` calls blitting it (once
  through a flattening `ColorTransform` for the etch, once for the text). Canvas2D has no TextField,
  so measure/wrap/stroke lives in the TS class with nothing to trace to. What *is* traceable: the
  order, the offsets, the alpha gate, `isStateDrawable()`.
- **Wiring step was one switch.** `loadElementDescription()` already read `element.renderer` and fed
  `createRendererForType()`; the game's own element description already declared `renderer="text"`
  for text/formatted_text/password/link, `"label"` for label, `"bitmap"` for bitmap/static_bitmap.
  The five names AS3's table carries (bitmap, text, label, unknown, null) were the whole job. The
  type→renderer mapping was never a guess — it is read off the game's data.
- **The three feared risks were all phantoms, and the reasons are instructive.** `isStateDrawable()
  === (state === 0)` looked like it would blank text in hover states; it cannot, because
  `getTheActualState()` only returns a priority bit the renderer accepts, so a text renderer collapses
  every state to 0 — which is *why* AS3 writes `param1 == 0`. `WindowRendererItem`'s
  `fillRect(window.color)` looked like it would box every label; `_fillColor` defaults to `0xFFFFFF`
  in both trees, whose alpha byte is zero, so the fill is transparent. And coordinates needed no
  rebasing because `draw()` already takes its origin from the rect it is handed.

`isStateDrawable()` itself carried the same hoisting pattern one level down: `SkinRenderer`'s base
implementation was `BitmapSkinRenderer`'s (AS3's base returns `false`; its `BitmapSkinRenderer`
overrides with its own template table), and `FillSkinRenderer` had drifted to `return true` where AS3
does not override at all. All 11 renderers now match AS3.

**The clip model was the next thing the flattening got wrong.** `WindowComposite` applied a
`ctx.clip()` at every clipping window while walking the tree, i.e. the intersection of *all*
ancestors. That cut the 1px bottom border off buttons sitting flush against the bottom of a list that
owns its graphic context (the disabled "Place in room", the green HC buttons): the skin buffer held a
full symmetric border, but `furni`/`contentArea` clipped at `y=481` while the button's last row *was*
481. AS3 never cascades like that — `WindowRenderer.childRectToClippedDrawRegion()` walks up only
through windows that share their parent's graphic context (`USE_PARENT_GRAPHIC_CONTEXT`, `0x10`) and
**stops at the first window that owns its own context**, whose `BitmapData` is a fresh clip origin.
The fix threads the clip explicitly and resets it at own-context boundaries (an own-context window
without `FORCE_CLIPPING` discards the inherited clip); the shared-context cascade is unchanged, so no
other window's clip moved. This is the one-pass equivalent of AS3's per-`BitmapData` clip origin.

### Not yet done

- **`WindowComposite` is a one-pass bridge, not AS3's per-`BitmapData` tree.** The clip fix above
  reproduces AS3's *clip* result without allocating a surface per graphic-context-owning window. The
  fully structural port — an `OffscreenCanvas` per own-context window, mirroring AS3's `BitmapData`
  tree — is deferred. It is the only thing that would also make a per-context `colorTransform` tint a
  whole rendered subtree (today the CSS-filter approximation applies only to a window's own buffer,
  not its children). It is a full render-path rewrite with per-frame allocation/caching and broad
  regression surface, so it is its own deliberate task, not a rider on a bug fix.

- **The branch is lightly tested.** 35 commits; chat and the window system have been exercised live,
  the rest has not.
- 94 majors + 68 minors, each needing the same per-finding verification.
- ~~`WindowComposite` merges AS3's three renderers into one class~~ — **done**, and this document was
  wrong to argue against it (see "The window system" below).
- ~~`HabboAvatarGeometry.xml` ships an older revision~~ — **fixed 2026-08-14**, and it was three
  files, not one, plus two never shipped at all. See "The transverse-debt pass" above; the fix is
  `tools/import-avatar-configurations.mjs` rather than a copy, because `configurations/` is
  regenerated per checkout.

---

## Recent Work Recorded

- 🆕 **`habbo/moderation` — the mod-tool window cycle: the module is complete**, 2026-08-18.
  24 → 38 TS files. The eight remaining window controllers plus `IssueHandler` and the four
  held-back action classes, which form one dependency cycle and had to land together, along with the
  listener plumbing they need. **Only the new mod tool (`_SafeCls_1981` + 6 tabs, ~1 110 l.) is
  still open**, and it is independent.
  - **`ModerationMessageHandler` had no listener plumbing at all.** AS3 holds six listener lists and
    dispatches to them from `onUserInfo`, `onRoomInfo`, `onRoomVisits`, `onRoomUserClassification`,
    the three chatlog handlers and both room enter/exit handlers. Every one of those was a
    `log.debug` stub, so the whole tool was deaf: cards, chatlogs and room visits requested data
    that arrived and was dropped. All six lists and their eight add/remove members are ported.
  - **A copy-paste bug in AS3's own `removeUserClassificationListener`.** It filters
    `_SafeStr_8278` (the classification list) and assigns the result to **`_roomVisitsListeners`**.
    Ported literally, that leaves the classification listener subscribed forever *and* replaces the
    room-visits list with classification controllers, so the next room-visits answer would call
    `onRoomVisits()` on an object without it — silent in AS3, a throw here. Corrected to the list the
    filter reads, documented at the declaration; the same call this port already made for
    `DailyTasksController`'s uninitialised vector.
  - **Three composers had wrong parameter names**, all caught by reading the call sites rather than
    the signatures. `DefaultSanctionMessageComposer` read `(issueId, modActionId, message, cfhTopicId)`
    where `ModActionCtrl` sends `(targetUserId, cfhTopicId, message, issueId)`;
    `ModToolSanctionComposer` read `(issueId, sanctionTypeId, userId)` for `(-1, accountId, cfhTopicId)`;
    and `GetRoomChatlogMessageComposer` read `(roomId, unused)` where **both** call sites send
    `(roomKind, roomId)` — the room id was in the wrong slot. Same wire shape in all three, so nothing
    was mis-sent yet; nothing had ever called them.
  - **`IssueManager` was missing six members the controllers call**: `addModActionView`,
    `removeModActionView`, `getCfhTopics`, `requestSanctionDataForAccount`, `removeHandler`, and a
    real `handleBundle`/`unhandleBundle` (both logged and returned). `setCfhTopics` stored nothing.
    `handleBundle` now builds the `IssueHandler` and shows it through the tracker with the stored
    geometry, as AS3 does.
  - **`ModerationManager.userSelected()` now reaches the start panel**, and the moderator init packet
    shows it — AS3 shows the start panel there and nowhere else, so without that line the tool never
    appears for a moderator at all.
  - **An inversion in AS3 kept as-is**: `IssueHandler.sendWindowPreferences()` calls
    `setToolPreferences(x, y, width, height)` but the setter is declared `(x, y, height, width)`, so
    after the client sends its own geometry the cached pair is transposed and the next issue window
    opens the wrong shape. The composer still receives the correct order. Documented at the call.
  - **A dead block in `ChatlogCtrl` deliberately not reproduced**: AS3 sets `align` and bumps
    `rightMargin` through three separate `getTextFormat()` calls and never passes the result to
    `setTextFormat()`. In Flash each call returns a fresh copy, so all three writes land on throwaway
    objects. Reproducing it would mean adding two fields to `ITextFormat` that nothing reads.
  - **`ITextWindow.fontFace` is writable now**, for the same reason `bold` and `italic` already were:
    `TextController` has had the setter all along and the interface declared it read-only.
    `IssueHandler` bolds the highest-priority report's category by swapping the face.
  - `VisitUserUtil` (`habbo/util`) shipped with it — `UserClassificationCtrl`'s only caller — and its
    message is the ordinary follow-a-friend request (composer 886), not the name-based
    `VisitUserMessageComposer`.

- 🆕 **`habbo/moderation` — the issue browser**, 2026-08-18. 19 → 24 TS files.
  `IssueBrowser`, `IssueListView` and the three tab views (`MyIssuesView`, `OpenIssuesView`,
  `PickedIssuesView`), plus the `IssueManager` wiring they need. The slice is independent of the
  controller cycle and compiles on its own.
  - **`IssueManager.updateIssueBrowser()` was a "handled by SolidJS" stub** — the pattern that keeps
    turning up. AS3 drives the browser from three places plus a **repeating 15-second timer** built
    in the constructor; none of that existed. The constructor now builds the `IssueBrowser`, reads
    `max.call_for_help.results` (default 200) into the `issueListLimit` the row table caps itself
    with, and starts the timer; `dispose()` clears it.
  - **`playSound()` only logged.** AS3 plays `HBST_call_for_help` through the sound manager *and*
    suppresses it while the browser is open — a moderator already looking at the queue does not need
    to be told. Both halves were missing; the browser check needed `IssueBrowser.isOpen()`, which is
    why it could not be fixed before this slice.
  - **`init()` did not exist at all**, though `StartPanelCtrl` calls it to open the browser. Added
    with the AS3 body; its only caller is still in the unported controller cycle.
  - **The row table is resized, not rebuilt.** `IssueListView.update()` clones or destroys rows until
    the count matches, then refills in place — so rows outlive a refresh, which is why each row's
    three buttons `removeEventListener` before adding. Without that a row surviving ten refreshes
    would fire its pick handler ten times. The bundle id rides in `IWindow.id` on the button.
  - **The sort is ascending on both keys.** AS3 writes `sortOn([...], [16, 16])`; 16 is
    `Array.NUMERIC` and `DESCENDING` (2) is absent, so highest-priority-first is *not* what this
    does. Kept as the source has it.
  - **Colours converted with a tool, not by hand**: `4289914618` is `0xFFB2E6FA` and `4294967295` is
    `0xFFFFFFFF`. Rows alternate on a *one-based* index, so the first row is the tinted one.
  - **One forced deviation**: AS3 reads the row's target icon as a `BitmapData` from the component
    asset library and hands the wrapper a `.clone()`. In this port images are not in a component
    asset library at all — they live in the `ResourceManager` behind
    `IHabboWindowManager.getAsset()` — and the `ImageBitmap` it returns is immutable and shared, so
    there is nothing to clone. Documented at the call.
  - `IssueBrowser` builds its window once and only ever hides it, so the three tab views, their row
    pools and their scroll positions survive a close. A `closed_issues` tab name is declared in AS3
    and never used.

- 🆕 **`habbo/moderation` — foundations, and the manager's spine**, 2026-08-18. 8 → 18 TS
  files. This is the first slice of a module that is 43 AS3 files / ~7 700 l., of which the 8
  already-ported ones were the data and message layer.
  - **`LocalizationHelper` was `IssueCategoryNames` under an invented name**, traced to
    `win63_version/habbo/moderation/class_3472.as` — a tree CLAUDE.md forbids reading bodies from.
    The real class is unobfuscated in the primary tree. Renamed, retraced, and its two use sites
    migrated; the lookup table itself was already correct, hole included (AS3 switches on
    `source - 1` and has **no case 12**, so source 13 reads "Unknown" while 14 and 15 are named).
  - **The localization manager was wired to the wrong dependency and passed `null`.** AS3 feeds
    `IssueCategoryNames.setLocalizationManager()` from `IIDHabboLocalizationManager`; the port
    called it from the *session* dependency's callback with a literal `null`, so
    `help.cfh.topic.<n>` was never consulted and every CFH topic fell back to the English table.
  - **`ModerationManager` had 2 of AS3's 8 dependencies** and stubbed most of its surface. Now
    complete except the two unported UI members: `getXmlWindow()`, `openHkPage()`, `goToRoom()` via
    the navigator (it was firing a `navigator/goto/` link event instead), `logEvent()`/`trackGoogle()`
    through `IHabboTracking`, the `windowManager`/`windowTracker`/`soundManager`/`initMsg`/
    `isModerator` getters, `cfhTopics`, and the room-viewer-mode early return in `initComponent()`.
    AS3's field is `initMsg`, not the port's invented `initData`; the message handler's write was
    repointed.
  - **All eight dependencies are required**, since AS3's `ComponentDependency` and this port's both
    default that parameter to `true`. Noted at the declaration because the friend-bar one is stored
    and never read — in AS3 too — so it is a hard gate paid for nothing, and a required dependency
    on an IID nothing provides locks the component silently.
  - **`WindowTracker`'s nine window types are recovered from their call sites.** Only
    `TYPE_USERINFO`/`TYPE_ISSUEHANDLER`/`TYPE_ROOMINFO` are readable; the other six were derived
    from each `getType()` and from the three `new ChatlogCtrl(..., 3|4|5, ...)` sites (issue / room
    / user). **`RoomVisitsCtrl` and `UserClassificationCtrl` both return 6** and so share a slot,
    each closing the other — that is what both classes say and it is left alone.
  - **`SendMsgsCtrl` (180 l.) shipped separately**, because it is the *only* member of the
    controller graph with no internal dependencies. The other eight controllers plus `IssueHandler`
    form one cycle — `UserInfoCtrl` → `ModActionCtrl` → `UserInfoCtrl`, and
    `ChatlogCtrl` → `UserInfoFrameCtrl` → `UserInfoCtrl` → `ChatlogCtrl` — so nothing in it compiles
    until all nine (~3 200 l.) exist, along with the four held-back action classes they construct.
  - **Closed** by the two entries above: the controller cycle and the issue browser both shipped
    the same day, along with the four held-back action classes. Only the new mod tool
    (`_SafeCls_1981` + its 6 tabs, ~1 110 l.) is still open.

- 🆕 **The reward track — controller and views: the module is complete**, 2026-08-18.
  `habbo/quest/rewardtrack/` is 19 more TS files (controller + 18 under `view/`, ~1 900 l. of AS3),
  finishing what the message/model entry below started. `RewardTrackController` is a DI `Component`
  attached under a new `IID_RewardTrackController`, and `HabboQuestEngine` now builds it, forwards
  `update()` to it and disposes it — the `TODO(AS3)` on `HabboQuestEngine.update()` is gone.
  - **The same constructor-timing bug the daily-tasks controller had.** AS3 subscribes its four
    message events in the *constructor*, where `addMessageEvent()` short-circuits on a still-null
    `_communicationManager` and every subscription is silently lost. They are built in
    `initComponent()` here instead, exactly as `DailyTasksController` already does — same four
    events, same handlers, a timing fix rather than a behaviour change.
  - **`RewardTrackPrizeLayout` is pure geometry and now pinned by a probe.** The scale is set so the
    two closest prizes sit one tile apart, measuring the free and premium rows *separately* (a free
    and a premium prize at the same threshold is not a zero gap), then stretched to fill a page by
    32 doublings and 24 halvings. Probed: a 4-prize track gives 1 page and a 108.6 px gap for an
    84+15 tile (stretched to fill, never below); a single-prize track hits the `minimumGap <= 0`
    fallback and stays finite instead of dividing by zero; a 30-prize track pages to 6 and clamps
    `pageForPoints(99999)` to the last page.
  - **The main layout ships five templates that the view tears out.** `extractTemplates()` removes
    the prize tile, the premium tile, the point marker and the first row of both lists before
    anything is built, so each becomes a detached prototype the sub-views clone — which is also why
    `RewardTrackView.dispose()` disposes those five windows by hand.
  - **Verified headlessly through vortex-glaze**: both layouts build, all 30 child names the ported
    views look up resolve on the real tree (including the nested `track/loading_bar` and the two
    unclaimed counters), and all five templates actually detach — `prize_template` is gone from
    `prize_content` afterwards. `header_button_close` is absent from both layout XMLs but resolves
    on the built window: it comes from the `<frame>`'s own header
    (`habbo_window_layout_header_*`), the same way `HabbiconPurchaseConfirmationView` reaches it.
  - **Two browser deviations, both documented at the declaration.** The claim notification's icon is
    passed straight through instead of AS3's `BitmapData.clone()` — `ImageBitmap` is immutable and
    shared, so there is nothing to clone; and the staff-only "copy id" writes through
    `navigator.clipboard` (asynchronous, permission-gated) while still raising the confirmation
    optimistically, as AS3's synchronous AIR `Clipboard` does.

- 🆕 **The reward track — message layer and data model**, 2026-08-17. `habbo/quest` 35 → 46 TS
  files. The wire side (14 files) and the model side (5 files) are done and probed; the controller
  and the 17 view files are not yet ported.
  - **Every message name is DERIVED, and every id is real.** Six ids from WIN63's own registry
    (events 3794 / 522 / 58 / 2017, composers 1376 / 1789). The reward track is new in the 2026
    build: `win63_version` has no message file for it, PRODUCTION predates it, and the emulator has
    no header for any of the six in either direction. The names come from the unobfuscated handlers
    and call sites — `onRewardTracks`, `onRewardTrackClaimResult`, `onRewardTrackProgress`,
    `onRewardTrackPremiumPurchaseResult`, `claimPrize`, `purchasePremium`.
    **Not a collision**: the emulator's `Game2GameRejoinMessageComposer = 1376` is the other
    direction; the two tables are separate id spaces.
  - **`RewardTrackData` reads four fields conditionally.** `taskPointsBoost` (a `double`),
    `instantPoints`, `costDiamonds` and `costCredits` are on the wire only when `hasPremiumConfig`
    is set. Reading them unconditionally desyncs every track after the first one without a premium
    tier. `RewardTrackPrizeData.productItemTypeId` is a **short**, not an int.
  - **Failure codes stay numbered.** AS3 names only `SUCCESS`; the other eight (claim) and nine
    (premium) are `_SafeStr_N` and the controller never compares them individually — it tests
    `resultCode != 0` and appends the number to a localization key. Naming them would be guessing
    at meanings the client does not use.
  - **Three model behaviours that are easy to get subtly wrong**, now pinned by a probe:
    `complete` counts only *non-premium* prizes; `premiumComplete` is true whenever the track has
    no premium config at all, so a free-only track reads as premium-complete; and
    `RewardTrackPrize.available` is a **cached** flag that `refreshDerivedState()` writes through on
    every mutation, not a computed one. `activeLevelIndex` returns the first rung not yet reached
    and clamps to the last index once all are cleared.
  - **Verified headlessly** in two passes. Wire: a two-track packet — the first without a premium
    config, the second with one — round-trips with zero type mismatches (the probe's wrapper asserts
    `readShort`/`readDouble` were actually the reads used) and is consumed to the byte, which is what
    proves the conditional block. Model: the cached `available` flips when `markPremiumPurchased()`
    lifts the lock; rung index resolves 0 / 5 / 12 / 20 / 999 against rungs at 5 / 10 / 20 exactly;
    a zero-required rung short-circuits to a full ratio instead of dividing by zero.
  - **Now closed** by the entry below: the controller and all 18 view files shipped the same day.

- 🆕 **`habbo/quest/seasonalcalendar` is deliberately deferred**, 2026-08-17, by the user's
  decision. Its two messages shipped (`GetSeasonalCalendarDailyComposer` 1012,
  `SeasonalCalendarDailyOfferMessageEvent` 1641); the 6 view files did not. Across them: 22
  `BitmapData` allocations/mutations, 11 remote-loading touchpoints and 4 timers, needing three
  documented deviations — and none of it is verifiable without a live seasonal campaign's assets.
  Every other dependency was checked present (`WindowToggle`, four `HabboQuestEngine` members,
  `QuestController.getTracker()`, both events, and the `SeasonalCalendar` layout, whose asset key
  genuinely has no `_xml` suffix).
  - **The trap if picking it up**: the seasonal offer is the **full** catalog offer shape
    (`ClubOfferData`), not the shorter one `PurchaseOK` sends. The emulator keeps both deliberately
    as `CatalogOfferSerializer.Serialize()` and `SerializeAsPurchased()`; only the first carries
    `priceInSilver` and the trailing `previewImage`. Checked and found correct in passing:
    `ClubOfferData` matches the 2026 DTO field-for-field, unnamed trailing boolean included — the
    port's two offer parsers are two deliberate shapes, not drift.


- 🆕 **The quest twinkle kit, and the prefetch the resource manager refused**, 2026-08-17.
  `Animation`, `IAnimationObject`, `Twinkle` and `TwinkleImages` (297 l.) — the sparkle burst over
  the quest-completed dialog. Closes the two `TODO(AS3)`s that named them, in
  `HabboQuestEngine.getTwinkleAnimation()` (a documented `return null` stub) and in
  `QuestCompleted`, whose `_twinkleAnimation` was typed as a structural placeholder.
  - **`ResourceManager.retrieveAsset()` rejected every prefetch.** The port opened with
    `if(!uri || !receiver) return;`, so a null receiver was a silent no-op. AS3 guards only the
    *notification* on `param2 != null` (`ResourceManager.as` l.71 and l.87) and starts the load
    either way — and `TwinkleImages` warms its six frames exactly that way, with a null receiver.
    Fixed on the interface and the implementation; any other null-receiver prefetch in the port was
    equally dead.
  - **`QuestCompleted` never disposed the animation.** AS3 calls `dispose()` on it
    (`QuestCompleted.as` l.46-49); the port only dropped the reference, which leaked the sprites.
  - **The mutable-buffer deviation.** AS3 owns a `BitmapData` and does `fillRect`/`copyPixels`
    straight into the window's bitmap. `IBitmapWrapperWindow.bitmap` is an immutable `ImageBitmap`
    here, so the buffer is an `OffscreenCanvas` and each composed frame is converted with
    `createImageBitmap()` — async, so a frame lands one microtask late, invisible at AS3's 100 ms
    per twinkle frame. A generation counter drops a conversion overtaken by a newer one, the guard
    the campaign calendar's gradients already use.
  - **`TwinkleImages.get disposed()` is inverted in AS3** (`return _questEngine != null`) and is
    kept that way: nothing reads it, and `Animation` decides an object's lifetime through
    `isFinished()`. Correcting it would be an invented change.
  - **Verified headlessly through `vortex-glaze`**, frame by frame: at 799 ms a twinkle has no
    bitmap and is not finished; at 800 it is on sequence frame 1, at 900 frame 2, at 1300 the peak
    frame 6, at 1900 back to frame 1, and at 2000 it is finished with no bitmap. Its position lands
    inside the 44x44 scatter box. An `Animation` holding two twinkles offset 300 ms apart runs at
    1000 and 2100 ms and **stops itself at 2300**, exactly when the later one's eleven frames run
    out; `update()` after that is a no-op and `stop()` hides the window. Frames reach the window's
    bitmap and invalidate it.

- 🆕 **The talent track**, 2026-08-17. `friendbar/talent` 0/6 → 7/6, and with it the module's last
  zero directory. Four controllers (track, level-up, toolbar promo, citizenship popup), the progress
  meter, `HabboTalent` itself and the `IHabboTalent` marker — plus **eleven message files**, since
  the port had one of the four talent messages.
  - **Names all recovered, none derived at the message layer.**
    `sources/win63_version/habbo/communication/messages/{incoming,outgoing,parser}/talent/` carries
    the real filenames for the three events, the composer and the three parsers, and the emulator's
    header table corroborates every id (3909 / 2210 / 1564 / 2280). The five DTOs are obfuscated
    there too, but PRODUCTION has `TalentTrack.as` and `TalentTrackLevel.as` unobfuscated; only
    `TalentTrackTask`, `TalentTrackRewardPerk` and `TalentTrackRewardProduct` are derived.
  - **`TalentTrackTask`'s wire order is not its accessor order, and copying the wrong one would
    have desynced the packet silently.** AS3 reads `achievementId, requiredLevel, badgeCode, state,
    currentScore, totalScore`, but declares `get state()` first — and that getter returns the
    *fourth* field read. The first draft here followed the accessors and had `state` and
    `achievementId` swapped. The probe below is what caught it.
  - **Three colour literals were converted wrong by hand**, two in `TalentPromoCtrl` and one
    already committed in `BadgeLeaderboardController` (`DEFAULT_RANK_BORDER_COLOR` was written
    `0x638C6A` where 6521514 is `0x6382AA`). All AS3 `uint` colours in this session were re-derived
    with a converter rather than read; worth doing that from the start next time.
  - **Attached where AS3 attaches it.** `HabboFriendBar`'s constructor carried a commented-out
    `attachComponent(new HabboTalent(...))` under "future sub-components"; AS3 does exactly that at
    `HabboFriendBar.as:29`. `IID_HabboTalent` and `IID_HabboAvatarEditor` were both
    `createIID<unknown>` and are now typed, which is what let the dependency be declared at all.
    The component switches itself off when `talent.track.enabled` is unset, so attaching it costs
    nothing on a hotel that does not run tracks.
  - **Verified headlessly through `vortex-glaze`.** A hand-rolled wrapper feeds a synthetic
    two-level packet through `TalentTrackMessageEventParser`: every field lands where it was put —
    task 101 keeps `ACH_RoomEntry1` with 5/5, task 202 keeps `ACH_SafetyQuizGraduate1` with 3/10 —
    `hasProgressDisplay()` is false for the safety quiz and true for the room entry, `levelProgress`
    is 1 for the completed level and 0 for the in-progress one, `totalProgress` resolves to 0.5 off
    the in-progress level index, and `findTaskByAchievementId` finds 202 and refuses 999. The
    subscription reward keeps its 31 days. The level-up window then builds with **5 reward rows** —
    2 perks + 1 product with the two plus-signs AS3 interleaves between them.
  - **Not verified:** the track window's layout arithmetic. `createLevelPane()` is ~200 lines of
    width maths against eight cloned templates and needs the real `talent_track_xml` populated with
    real level art to mean anything.
  - **Server state:** `GetTalentTrackLevelMessageComposer`'s emulator handler is an empty stub, as
    is `GetTalentTrack`'s, so nothing answers either today.

- 🆕 **The badge leaderboard**, 2026-08-17. `habbo/groups` 25/31 → 35/31, closing the module. The
  9 files of `badge_leaderboard/` (1,833 l.) plus the message layer they needed, which did not
  exist: `GetBadgeLeaderboardMessageComposer` (1225) and `BadgeLeaderboardMessageEvent` (2503) with
  its parser and row DTO, both ids from WIN63's own registry and **both names derived** — the
  emulator has no header for either and `win63_version` predates the board.
  - **It was already reachable and led nowhere.** `BadgeLeaderboardUtils` was ported and two
    callers already build its links (`FurnitureBadgeDisplayWidgetHandler`, `InfoStandUserView`),
    but nothing registered a tracker for `badge_leaderboard/`. `HabboGroupsManager` now constructs
    the controller in its constructor and registers it in `initComponent()`, exactly where AS3
    does (`HabboGroupsManager.as:147` and `:216`) — and it hands it the manager's own asset
    library, because `badge_leaderboard_view` is declared by `HabboGroupsCom`, not by
    `HabboWindowManagerCom`.
  - **The interesting half is the chunk cache.** The board pages 10 rows at a time but the server
    answers 50, so `BadgeLeaderboardDataServer` slices locally, refreshes a chunk older than 60s
    *behind* the page it already delivered, prefetches the next chunk once the reader is one page
    from the end of the current one, and never has two requests for the same chunk in flight.
    `deliverPage()` drops a reply that is no longer the page asked for, or whose
    (request, chunk-sync-time) pair was already delivered — which is what stops a prefetch from
    repainting the page under the reader.
  - **`_SafeCls_2123` is genuinely empty.** The marker interface the controller implements declares
    no members in any tree; ported as `IBadgeLeaderboardController` (derived name) because the
    declaration exists.
  - **`IMessageEvent.getParser()` cannot be overridden here.** The port's `MessageEvent` already
    declares a generic method of that name, so the typed parser is exposed as a getter, which is
    what every other event in the tree does anyway (`SellablePetPalettesMessageEvent`).
  - **Verified headlessly through `vortex-glaze`.** Data server: a cold page 0 sends
    `[0,-1,0,50]` and delivers nothing; the 50-row reply delivers 10 rows from rank 1 with total
    120; pages 1 and 2 are served **with no further request** at first ranks 11 and 21; page 4
    prefetches chunk 1; page 12 is allowed (row 100 < 120) while page 30 is refused and answered
    with an empty page rather than a request. View: 10 row views built, the title lands on all five
    shadow copies, the dropdown holds its selection, the pinned row resolves `rank_own` where a
    normal row resolves `rank_number`, and hiding a row leaves the rest untouched.
  - **Not verified:** the avatar faces. They need `AvatarRenderManager`, which Glaze does not boot.

- 🆕 **The messenger's habbicon picker**, 2026-08-17. `habbo/messenger` 9/12 → 15/12, closing the
  module: the 5 files of `messenger/habbicons/` (1,399 l.) plus the composer and the DI edge they
  needed. `MainView` already had the whole surrounding wiring — `habbicon_button` routed to
  `toggleHabbiconPicker()`, `hideHabbiconPickerIfOutside()` was called from the window procedure,
  and `createBubbleMessage()` handled the habbicon bubble type. All of it led to a `log.warn`.
  - **The blocking TODO was stale on all three counts.** It read *"messenger/habbicons/,
    habbo/habbicons/assets/ and habbo/catalog/habbicons/ are all unported, and the port has no
    composer for a habbicon message either"*. The middle two shipped on 2026-08-12 with the
    collectibles pass; only the first and the composer were still true. A TODO that names its
    blockers is worth re-reading whenever one of them lands.
  - **`IID_HabbiconController` was declared `createIID<unknown>`**, so `HabboMessenger` could not
    take the dependency AS3 gives it (optional there too — `HabboCatalog` is what attaches the
    controller). Typed properly, and the messenger now declares it.
  - **`SendHabbiconMessageComposer`, header 1163**, from WIN63's registry
    (`_composers[1163] = _SafeCls_2591`). Name derived: habbicons postdate `win63_version` and the
    emulator's header table alike, so there is nothing to corroborate against — flagged at the
    declaration, as the other seven habbicon messages are.
  - **A bug the probe caught that review would not have.** The blank 40x40 placeholder a tile shows
    while the spritesheet loads was `createImageBitmap(new OffscreenCanvas(40, 40))`, and an
    OffscreenCanvas that has never been given a context is unallocated — every tile threw
    *"The ImageBitmap could not be allocated"*. Taking the 2d context first fixes it.
  - **`clearBitmap()` must not close its bitmap.** AS3 disposes it because it was handed a
    `clone()`; this port hands over `HabbiconAssetManager`'s cached preview (`createImageBitmap` is
    async and cannot clone synchronously — the deviation `HabbiconSetRailRowView` documents), so
    closing it would destroy the copy every other tile draws from.
  - **Verified headlessly through `vortex-glaze`** against a stub controller: 8 owned habbicons
    (one unowned, correctly skipped) and 2 collections produce exactly `Favorites`, `Recently used`
    and `summer` — `winter` is absent because its only member is unowned, and a recent id the user
    no longer owns is dropped rather than shown as a gap. Each grid pads to a whole row of 5;
    the window measures 251px, inside AS3's 94–304 band. A query matching nothing empties the list
    and shows `empty_view`; one matching everything collapses to a single `Search results` band of
    7 entries padded to 10; clearing it restores the three sections. `hide()` calls
    `resetUnseenHabbicons()`.
  - **Not verified:** anything needing real habbicon art. Glaze has no `habbicons.asset.root`, so
    every preview was the placeholder and `resolveEntryName()` fell back to `"Habbicon"`.

- 🆕 **The campaign calendar was a manager with no window**, 2026-08-17. `habbo/campaign` 2/5 → 7/5,
  and the same shape as the notification bubbles the day before: `linkReceived()` matched
  `openView/calendar`, then fell into a `log.trace` and a comment reading *"Calendar UI will be
  handled by SolidJS"*, which this port does not use. The four view files —
  `CalendarView` (304 l.), `CalendarItem` (140), `CalendarItemWiggle` (97) and
  `CalendarSpinnerUtil` (47) — are ported.
  - **The manager was the larger half of the gap.** It declared **one** of AS3's six dependencies.
    `_sessionDataManager`, `_windowManager`, `_localizationManager`, `_catalog` and `_roomEngine`
    were all absent, and with them `showCalendar()`/`hideCalendar()`, `showProductNotification()`,
    `requestIconFromRoomEngine()`, `getImageGalleryUrl()` and both public getters. Only the
    communication manager is `required: true`, as in AS3 — the window manager attaches after this
    component, and a hard dependency on an IID nothing provides yet locks the component silently.
  - **The opened-day bookkeeping sat in the wrong method.** The port pushed onto
    `calendarData.openedDays` straight from the door-opened handler; AS3 does it inside
    `showProductNotification()`, in the `getProductData(...) != null` branch. A prize whose product
    code the session data manager cannot resolve must leave the calendar untouched, and did not.
  - **Two forced deviations, both with precedent.** AS3 reads the layout from
    `campaigns.assets.getAssetByName("campaign_calendar_xml")`; this port keeps layouts in the
    window manager's registry, so it goes through `buildModalWidgetLayout()` — the substitution
    `AvatarEditorView` already documents. And `CalendarSpinnerUtil` fills a `Sprite` into a
    synchronously-returned `BitmapData`, which the browser has no equivalent for, so it is `async`
    like `MarketplaceChart.draw()`. That made a race: a selection moving mid-decode would apply one
    stale fade panel next to one fresh one. Both panels are now drawn before either is applied, and
    a generation counter drops the pair whole if the selection moved.
  - **Verified headlessly, and the fixture is AS3's own.** `HabboCampaigns.initComponent()` carries
    `campaignDays = 24, currentDay = 5, openedDays = [0,1], missedDays = [2,3]` behind an
    `if(false)` debug guard. Driven through `vortex-glaze` (the window subsystem standalone — see
    "Three window-layout engine bugs"): window builds, 24 cells cloned from the template at
    202+4 px, `resolveDayState` returns `[1,1,3,3,2,2,4,4]` for days `[0,1,2,3,4,5,6,23]` — opened,
    missed, available, future, exactly AS3's table. Forward/back move the heading 6 → 7 → 5;
    clicking the selected cell calls `openPackage(4)` while clicking another only moves the
    selection to it, which is AS3's two-step behaviour; `openPackageAsStaff` and `hideCalendar`
    both fire. Both gradients redraw at the item list's height on every selection.
  - **One thing the probe found that is not a bug.** `info_body` reads back the layout's placeholder
    caption, not the key written to it: `TextController.set text()` assigns `_caption` and then
    *returns* on a `${…}` value, leaving `_text` to a localization listener — and Glaze's
    localization stub never serves one. `caption` reads `${campaign.calendar.info.future}`
    correctly. Worth knowing before reading any Glaze-hosted probe's `.text`.
  - **`campaign_calendar_icon` is declared by no `*Com.as` in any tree** — the layout references it,
    only `campaign_calendar_icon_advent` ships, and AS3 skips a null piece the same way. Same class
    as `illumina_light_border_top_center`; do not "fix" it.

- 🆕 **Notifications had a queue and no view**, 2026-08-17. `SingularNotificationController` took
  every notification the client raises, styled it, de-duplicated it, and emitted it to nobody: its
  class comment said "the view is handled by SolidJS", which this port does not use (see
  `.claude/rules/window-ui.md`). Six of its members carried a `TODO(AS3)` pointing at the same two
  unported classes. Both are ported now — `HabboNotificationItemView` (612 l.) and
  `HabboNotificationViewManager` (359 l.) — along with `XMLVariableParser`, without which the
  style config could not be read at all.
  - **The feed is not the thing that was missing.** `habbo/notifications/feed/` — twelve classes,
    a five-pane browser — is dead code in this client and was left alone. `external_variables.txt`
    ships `notification.feed.enabled=false`, which gates all three of `_SafeCls_1951`'s
    `addFeedItem()` calls, and `_SafeStr_6523` (the `NotificationController`) is **never
    constructed**: not in WIN63's `initComponent()`, and not in the 2016 build either, where
    `_feedController` appears only as `= null`. Flipping the flag would fault, not work. The live
    half is `notification.items.enabled=true`, which is this one.
  - **Three things had to be true before a bubble could appear**, and none of them were. The
    config asset `habbo_notifications_config_xml` was not shipped: `build-window-assets.mjs`
    filtered every declaration whose root element is not `<layout>` or `<skin>`, and this is the
    dump's only `<variables>` document. Its per-type icons name assets with AS3's `_png` linkage
    suffix (`if_icon_temp_png`) where the asset build writes `if_icon_temp`, so a verbatim lookup
    returns null and the notification silently loses its icon; the suffix is stripped at the
    lookup and the six icons added to `LIBRARY_IMAGE_NAMES`. And `HabboNotifications` was
    constructed as `new HabboNotifications(ctx)` — no asset library, so `assets` was null and the
    config could not have been found even once shipped.
  - **An unknown notification type is refused, and that is AS3's own behaviour.** `addItem()`
    resolves `type` against the styles map and returns 0 when it is absent. The shipped config has
    21 types; `respect` is not one of them, so `onRespectNotification()` has never displayed
    anything in this build. The rejection now logs a `warn` rather than vanishing.
  - **The port's DI order does not match AS3's.** AS3 declares `IIDHabboWindowManager` and
    `IIDHabboToolbar` *required* dependencies of `HabboNotifications`, so its view manager can cache
    both in its constructor. This port declares them optional and attaches the toolbar (VortexMain
    :821) and window manager (:838) *after* the notifications component (:800) — both are null at
    `initComponent()`. Caching them would have pinned the bubble stack at y=4, on top of the
    toolbar, for the whole session; the manager reads them live instead, and its
    `EVE_EXTENSION_VIEW_RESIZED` subscription follows whichever toolbar is current.
  - **Verified**: `XMLVariableParser` parsed against the shipped asset in a real DOM — 6 top-level
    entries, 21 styles, `nft_opening` resolving its custom layout and view. That probe is also what
    showed the timings come back as *strings* (`time_display` = `"15000"`), which a raw `>` would
    have compared lexically. Not verified on screen: the client's boot stops at the login screen
    without the hotel running, so the layout, the fade cycle and the stacking have not been seen.

- 🆕 **Avatars are drawn as batched sprites, not composed images**, 2026-08-17. A room of 100
  avatars ran at about 12 fps. Two separate causes, found in that order, and the second only became
  visible once the first was gone.
  - **One AS3 infidelity made ~88% of compositions permanently uncacheable.** `renderBodyPart()`
    folded two different AS3 branches into one `else`: AS3 clears the cacheable flag only when an
    asset *resolved but carries no bitmap* — a transient state — and leaves it alone when the asset
    does not resolve at all, which is a permanent property of the figure. Nearly every avatar has
    some part with no asset, so nearly every composition was thrown away and rebuilt every frame.
    Measured before: 23,971 uncacheable against 3,152 cached. Restoring the condition took 100
    avatars to 100–145 fps.
  - **The cache made the work repeatable; it did not make it cheaper.** The first ~8 seconds still
    ran at 5–20 fps while every avatar composed each body part once per direction it turned. So the
    composition was removed instead: `describeBodyPart()` computes exactly what `createUnionImage()`
    would have drawn and returns the parts rather than rasterising them — no canvas, no readback, no
    texture upload, and therefore nothing left to warm. `AvatarVisualization` lays them out as
    ordinary room sprites in a block past the additions, and the renderer batches them.
  - **Result**: 100 avatars at 170 fps with no warm-up, 200 at 139. `avatar.compose.ms` fell from
    most of the frame to 0.03 ms, and `AvatarImageCache` no longer appears in the profile's top
    twelve. The cache now serves 0.75 hits/frame — describing a part costs less than remembering it.
    The remaining budget is scene-graph transforms (~30%) and the room's z-sort (7%), both
    proportional to sprite count rather than to figure count.
  - **The trap worth remembering**: `describeBodyPart()` copied `createUnionImage()`'s arithmetic
    line for line and was still wrong for mirrored directions, because `renderBodyPart()` reads its
    registration point off the `ImageData` that call *returns*, and that constructor rewrites
    `regPoint.x` into `-regPoint.x + rect.width` when its `flipH` is set. Copying formulas does not
    copy what happens between them; the transform was hidden in a constructor, so nothing at the
    call site showed it.
  - Both paths stay live behind `:spriteparts [on|off]`, sprite parts being the default.
    `AvatarRenderMode.generation` makes each avatar's cache flush itself on its next lookup, so the
    switch needs no room reload. An active palette effect declines the sprite path and composes, as
    its final whole-image pixel pass is not expressible as a per-sprite tint. Everything outside the
    room — previews, the avatar editor, the imager — still calls `getImage()` and is untouched.

- 🆕 **The monsterplant seed's second way in**, 2026-08-09. An audit of the whole monsterplant
  surface — seed, products, breeding — looking for the "ported but never wired" pattern. Almost
  everything was already there; the chain broke in exactly one place, and it was the same
  missing-translation-step family as the guild furni below.
  - **Double-clicking a placed seed did nothing.** `FurnitureMonsterplantSeedLogic.useObject()`
    raised `ROWRE_MONSTERPLANT_SEED_PLANT_CONFIRMATION_DIALOG` correctly, but
    `RoomEngine.handleObjectWidgetRequestEvent()` had no case for it, so it fell through to the
    `default:` "Unmapped room-object widget request" warn. Added, mapping to
    `ROWRE_REQUEST_MONSTERPLANT_SEED_PLANT_CONFIRMATION_DIALOG` per `_SafeCls_1821.as:1485-1487`.
    That event name keeps the `ROWRE_` prefix its siblings trade for `RETWE_`; AS3 spells it that
    way on **both** sides, so it is the source's inconsistency and not a typo to "fix".
  - **And nothing was listening for it either.** `FurnitureContextMenuWidgetHandler.set container()`
    subscribed to 3 of AS3's 5 engine events (`FurnitureContextMenuWidgetHandler.as:127-131`);
    the monsterplant one is now wired, with the `isOwnerOfFurniture()` gate AS3 puts on it — the
    same gate the seed bubble already had in `processEvent()`. Fixing only one of the two halves
    would have looked identical to fixing neither.
  - The other AS3 subscription still absent is
    `ROWRE_REQUEST_PURCHASABLE_CLOTHING_CONFIRMATION_DIALOG` (AS3 l.109/128): the room engine has
    no case raising it, so subscribing would add a listener nothing calls. Noted at the class doc
    rather than half-wired.
  - **Everything else in the feature measured clean.** Seed bubble + confirmation, the three
    use-product dialogs (fertilize/revive/rebreed), `BreedPetView` →
    `BreedMonsterPlantsConfirmationView` → `ConfirmPetBreedingView` → `BreedPetsResultView` /
    `NestBreedingSuccessView`, `OwnPetMenuView`'s monsterplant mode, `InfoStandPetView`'s
    growth/well-being panel, all 6 layouts, and every composer/parser with its header — all
    present, and each view is instantiated by a real caller. `plantSeed()` sending
    `UseFurnitureMessageComposer` was verified against the wire, not the name: AS3's
    `_SafeCls_3952` is `[itemId, param=0]` and registers at **3353**, the same id this port's
    `UseFurnitureMessageComposer` uses.
  - **Two stale claims removed while there.** `AvatarInfoWidgetHandler.getProcessedEvents()`
    carried a `TODO(AS3)` saying the two `ROSM_USE_PRODUCT_FROM_*` events had no trigger — both
    have been raised for a while (`RoomEngine.showUseProductSelection()` and the
    `ROWRE_PET_PRODUCT_MENU` case), so the use-product bubbles were never dead. And
    `FurnitureContextMenuWidgetHandler`'s header still described the monsterplant path as a stub.
  - **Server-side, the feature stops after planting** (out of scope for this pass, measured for
    the record): `vortex-emulator` has headers but neither parser nor handler for
    `HarvestPetMessageEvent` (1210) and `CompostPlantMessageEvent` (1989), and
    `RoomPetSystem.ProcessPetsAsync()` `continue`s on `Type == 16` — no growth, no drying out, no
    death, no revive. `PlantMonsterplantSeedAsync()` also rolls rarity with
    `Random.Shared.Next(1, 8)` instead of reading it off the seed.

- 🆕 **Four runtime warnings, four causes**, 2026-08-09. All found from a single room session's
  console output, and only the last one was cosmetic.
  - **`ExtensionView.detachExtension()` removed nothing.** It dropped the id from `_items` and
    stopped there — leaving the window in `_orderedItems`, which is what `refreshItemWindow()`
    rebuilds the strip from, and never asking for a refresh. So **every** toolbar extension that
    detaches stayed on screen for the session: the group-base panel (the reported symptom — walk
    into a room with no guild and it never leaves), the quest tracker, the next-quest timer, the
    phone-number prompts, the promos. `GroupRoomInfoCtrl.close()` was faithful all along; the
    thing it called was not. AS3 splices the ordered list, calls `_toolbar.removeDimmer()` and
    refreshes. While there: `attachExtension()` never called `createAndAttachDimmerWindow()`
    either, though both dimmer helpers were ported — so the new-user room-enter effect dimmed
    everything except the extension strip.
  - **Window layouts were never in the asset library.** A whole family of ported views reads its
    layout the way AS3 does — `assets.getAssetByName("settings_xml").content` — and the library
    only ever held images, sounds, avatar configurations and chat styles; layouts went solely
    into the window manager's widget-layout registry. All **17** such lookups returned null and
    each site turned that into "Missing layout X": the settings menu, the chat/sound/other
    settings views, and every furniture context bubble — including the guild one added the same
    day, which could not have opened whatever the server replied. They now register under the
    **file basename** (the `*Com.as` field name, i.e. the string AS3 asks for), never the internal
    `<layout name="...">`: that label differs from the real name for **633 of 783** layouts and
    **86** of those internal names are shared by two or more files.
  - **`RWE_HIGH_SCORE_DISPLAY`/`RWE_WORD_QUIZZ` had no container** — `getWidgetContainer()` looked
    `background_widgets` up by tag, where the layout declares it as `name=` with no tags at all
    and AS3 uses `getChildByName`.
  - **`SanctionStatusMessageEvent` was subscribed with no header**, so `MessageRegistry` logged
    "Unknown message event class" every boot. Its own comment said it was deliberately left
    unregistered while the line under it subscribed anyway.
  - **Not a bug: "No container found for widget: RWE_ME_MENU."** AS3's `getWidgetContainer()` is
    identical and no widget layout in any tree carries a `room_widget*` tag, so AS3 refuses that
    container too — it just returns false without logging. The layout's `room_widget_me_menu`
    container is genuinely unused.
  - **A build is not a source tree.** The first of these was reported against `vortex-client/dist`,
    which predated the fix by ten hours; the dev server aliases to `vortex-engine/src` but the
    built bundle does not. Check `dist/assets/index-*.js` for a string literal from the change
    before diagnosing anything — and check a *string* literal, not a class name: the minifier
    renames classes, so "my new class is absent from the bundle" proves nothing.

- 🆕 **Guild-furni context menu, end to end**, 2026-08-09. Double-clicking a guild-customised
  furni did nothing: the room engine logged "Unmapped room-object widget request", so the request
  never left the client.
  - **The whole chain was missing, not just the view.** Outgoing
    `GetGuildFurniContextMenuInfoMessageComposer` (826) and incoming
    `GuildFurniContextMenuInfoMessageEvent` (3220) + parser — both ids from WIN63's own registry
    (`_composers[826]`, `_SafeStr_4546[3220]`), corroborated by the emulator. All three class
    names are **DERIVED**: the AS3 ones live in `src/unknowns/` and are obfuscated in every tree.
  - `RoomEngine.handleObjectWidgetRequestEvent()` gained the `ROWRE_GUILD_FURNI_CONTEXT_MENU`
    case. It is the one case there that raises no widget event: AS3 sends 826 itself, with the
    guild id read off the object's own `furniture_guild_customized_guild_id`, and lets the reply
    open the bubble — the guild's name and membership flags exist only server-side.
  - `GuildFurnitureContextMenuView` (join / open-forum rows, the guild name as a link into the
    group profile, `groupforum/<id>` through the link bus) is ported; the shipped
    `guild_furni_menu.xml` carries every child name it addresses. `FurnitureContextMenuWidget`
    builds it, and `showGuildFurnitureContextMenu()` — the only caller of
    `FurnitureContextInfoView.setup()`'s third argument — is real.
  - **Three unrelated defects found in the same files**, all of the same family: the widget's
    `dispose()` dropped 3 of its 11 sub-views (the other 8 kept windows and listeners for the life
    of the page); `removePlantSeedConfirmationView()` was never called on furni removal even
    though the view it closes is built; and the handler's `dispose()` unregistered neither of its
    two message events, leaving them subscribed to a connection it then nulled. `sendJoinToGroup`
    was a comment where AS3 sends `JoinHabboGroupMessageComposer` — the composer already existed.
  - **The doc was stale in the widget's own header**, which claimed seven sub-views including this
    one were "TODO(AS3) stubs — none of them is ported"; six of the seven were built in the
    constructor three lines below it.

- 🆕 **Priority-0 sweep: `habbo/help` and `habbo/moderation`**, 2026-08-09. Both modules run the
  recipe from priority 0; only one of them had anything to find.
  - **`habbo/help` — three CFH registries ported, none of them fed.** `UserRegistry`,
    `ChatRegistry` and `InstantMessageRegistry` were all complete and all empty at runtime:
    `registerUser()`/`registerRoom()` had **zero call sites in the whole repo**, and both event
    handlers were passive objects whose `onChatEvent()`/`onInstantMessage()` nobody called. AS3's
    handlers subscribe *themselves* in their constructors — `ChatEventHandler` to
    `roomSessionManager`'s `RSCE_CHAT_EVENT`, `InstantMessageEventHandler` to `NewConsole` and
    `RoomInvite` through `help.addMessageEvent()` — so both now take the component, as AS3 does,
    instead of a bare registry.
  - **Four of AS3's `initComponent()` registrations were missing**, which is why nothing fed the
    user registry: `Users`, `RoomReady`, `GetGuestRoomResult` and `RoomEntryInfo`. All four events
    and parsers already existed; only the subscriptions were absent. The fifth,
    `onGameStageStarting`, is snowwar's own user list and stays a `TODO(AS3)` until `habbo/game`
    exists.
  - **`_currentRoomId` was never assigned** — declared, read by `reportBully()` and
    `startPhotoReportingInNewCfhFlow()`, written by nobody, so every report carried room 0.
    `onRoomEnter` sets it now, as AS3's `_SafeStr_4551` does.
  - `HabboHelp.addMessageEvent()` now tracks into `_messageEvents` and `dispose()` unregisters
    them, matching AS3's vector — required, since `InstantMessageEventHandler` registers through
    it and unregisters nothing itself. `IID_HabboFreeFlowChat` was added as an optional
    dependency with its `freeFlowChat` getter: the chat handler needs `chatStyleLibrary` to skip
    notification-style bubbles, exactly as AS3 does.
  - **`habbo/moderation` has no wiring gap** — it is a porting gap. The message layer was wired on
    2026-08-08, `ModerationManager` is attached, and it constructs `IssueManager` +
    `ModerationMessageHandler`, which build `IssueBundle`s from incoming issues. The 35 unported
    files are the mod-tool UI (`IssueBrowser`, the 4 issue views, `ModActionCtrl`, `RoomToolCtrl`,
    `UserInfoCtrl`, `WindowTracker`, `new_mod_tool_tabs/`). One duplicate noted, not acted on:
    `moderation/ModActionDefinition.ts` has no consumer — the parser uses its own
    `ModActionDefinitionData`, and the UI that would read the former is unported.
  - **Verification**: `tsc --noEmit` clean, ESLint clean (2 pre-existing `any` warnings),
    headless boot clean. The registries only fill against a live session, so the behaviour itself
    is untested — this is wiring verified by construction, not by observation.

- 🆕 **Bots — catalog, inventory, placement**, 2026-08-09. The bots tab existed as a layout, a tab
  button and a data-only `BotsModel` that implemented nothing; the category was never registered,
  so opening it rendered an empty panel whatever the server sent.
  - **Inventory**: `BotsModel` rewritten as a real `IInventoryModel` (items store + view, place,
    unseen category 5, `REOE_PLACED` re-open), plus `BotsView` and `BotGridItem`, both ported from
    `habbo/inventory/bots/`. Registered as `_inventories.add('bots', …)`, wired into
    `switchCategory()` and `closingInventoryView()`, and given the `IIDAvatarRenderManager`
    dependency AS3's `HabboInventory` declares (kept optional here — see the comment at the
    dependency).
  - **Messages**: `BotAddedToInventory` (3570) and `BotRemovedFromInventory` (2032) — both in
    WIN63's registry, both subscribed by `_SafeCls_1951.as`, neither ported. Without them a bought
    bot needed a tab reopen and a placed one stayed in the grid. `Bot` is now the faithful
    `_SafeCls_3143` DTO (**gender is read before figure**, against the getter order) and
    `BotInventoryMessageParser` exposes `items` keyed by id, as AS3 does.
  - **Catalog**: product type `"r"` is a *bot*, not a rentable avatar effect as the widget's own
    TODO claimed. `ProductViewCatalogWidget` now renders the offer preview (smile, body 4, head 3,
    cropped "full") and `PurchaseConfirmationDialog` the confirmation image (body 3, wave, smile,
    highlighted "full") — the latter also gained the `IAvatarImageListener` half AS3 declares.
  - **Placement**: the room-engine path (ghost, `PlaceBot` 2102, CTRL-click pickup 2743) was already
    there; the missing piece was the mover fallback icon, which logged "no icon path for user type
    4". It now takes AS3's `getGenericRoomObjectImage(typeName, figure, …)` branch.
  - **The server side is done and the docs said otherwise.** vortex-emulator grew bots on
    2026-08-08 (entity, migrations, inventory grain, room bot system, purchase grant);
    `docs/CLIENT-SERVER-ARCHITECTURE.md` §18 "Bots — Do Not Exist", its summary-table row and four
    client comments all asserted the opposite and have been corrected. `MoveBot` (1295) really is
    still unhandled server-side — the id is absent from `Headers.cs`.
  - **Room-side bot UI, same day.** `InfoStandRentableBotView` is no longer a stub: the click panel
    is ported in full (name/motto/hand item/owner line, avatar + badge, move/rotate/pick), with
    `RoomWidgetRentableBotInfoUpdateEvent`, `InfoStandRentableBotData.setData()` and the handler's
    `case 4` behind it. `RentableBotMenuView` ports the bubble menu (pick up, configure chat,
    rename, dress up, wander, dance, donate, NUX rows and cloned in-client links), on
    `RentableBotInfoData` — both class names DERIVED, the AS3 ones are obfuscated in every tree.
    The two skill editors (`BotChatterMarkovConfiguration`, `BotChangeNameConfiguration`) sit on
    the ported `BotSkillConfigurationViewBase` + `BotSkillEnum`.
    - **Five more messages**, all ids from WIN63's registry and corroborated by the emulator:
      `CommandBot` (3813) and `GetBotCommandConfigurationData` (2311) out; `BotCommandConfiguration`
      (2463), `BotSkillListUpdate` (1293), `BotForceOpenContextMenu` (2336) and `BotError` (520) in.
      `RoomDesktop` owns the two it translates into widget events, `RoomUsersHandler.onBotError()`
      the refusal alert — `IUserData.botSkillData` is typed `BotSkillData[]` now that one exists.
    - **Still open here**: `menu.bot.enabled` is `false` in the live external variables, so the
      bubble menu is gated off until a hotel turns it on (the click panel is not); AS3's
      `roomEngine.selectAvatar()` after a forced context menu is unported (`setSelectedAvatar` does
      not exist in this port), so only the selection highlight is missing from that path.

- 🗑️ **Room dynamic lighting removed**, 2026-08-19. The experiment added on 2026-08-07
  (`packages/vortex-client/src/lighting/`, 8 files / ~4,300 lines: N point lights from the moodlight
  and glowing furni, texture-derived cast shadows, per-sprite tinting, a Ctrl+Shift+L debug panel and
  a `window.VortexLighting` handle) is **deleted**, along with
  `docs/architectures/room-lighting-architecture.md`. It had no AS3 counterpart — the Flash client
  has three frozen face-colour constants and a global moodlight multiply and nothing else — so it
  made the render diverge from Habbo's. The removal is complete: the module was self-contained
  behind one `RoomLightingController.install()` call in `App.ts`, now gone, and no ported file was
  ever edited for it.
  - **Two room-geometry conventions were found while building it, and they outlive it** — they apply
    to anything drawn over the room, and both are silent when wrong: a tile is *centred* on its index
    (`RoomPlaneParser`: `planeX = x / 4 - 0.5`, so tile `t` spans `[t-0.5, t+0.5]` — and world→tile
    is `Math.round`, not the `Math.floor` `handleUserPlace()` uses); and the room is centred in the
    canvas by a `+ canvas.width / 2` term present in neither the geometry nor `screenOffset`, spelled
    out in `getRoomObjectScreenLocation()` and `getRoomObjectBoundingRectangle()`. Use the engine's
    own conversion rather than reimplementing from `RoomGeometry`.

- ✅ **Moderation wired into the boot** (incoming 757 `ModeratorInit` and the other thirteen),
  2026-08-08. Everything under `habbo/moderation/` was already ported — `ModerationManager`,
  `IssueManager`, `ModerationMessageHandler`, all fourteen events and parsers, all fourteen ids in
  the header table — and **nothing ever constructed the manager**, so `IID_HabboModeration` was
  declared and never provided. `VortexMain` now attaches it next to `HabboHelp`, which was dormant
  for exactly the same reason.
  - The distinction that made this invisible: registering a message *event* only maps an id to a
    parser. The callback comes from `ModerationMessageHandler`, and that is created in the manager's
    `initComponent()` — so with no manager, every moderation packet reached the dispatcher and found
    no subscriber. The header table's own comment claimed the set was "subscribed by
    ModerationMessageHandler", which was true of the code and false of the running client.
  - Attached after the help component so its two hard dependencies are already up (communication
    manager, session data manager) — a hard dependency on an unprovided IID locks a component
    forever with no log. `HabboCommunicationManager.initComponent()` creates the connection object
    eagerly, so the handler finds one to subscribe against.
  - Still open: the two AS3 consumers. `RoomUI` and `HabboFreeFlowChat` both take
    `IIDHabboModeration` as an optional dependency (`RoomUI.as:508`,
    `HabboFreeFlowChat.as:212`); neither port declares it, so nothing yet *uses* the manager.

- ✅ **Chat flood control (incoming 3614)**, 2026-08-08. `FloodControlMessageParser` (one int, the
  seconds) + `FloodControlMessageEvent`, header **3614** from WIN63's registry
  (`_SafeCls_2046.as::_events[3614] = _SafeCls_3307`), corroborated by vortex-emulator's
  `FloodControlMessageComposer = 3614`; subscribed by `RoomChatHandler`, where the port already
  carried the commented-out registration as a TODO.
  - The *consumer* was already built and wired: `ChatInputWidgetHandler` listens for
    `RSCE_FLOOD_EVENT` and turns it into `RoomWidgetFloodControlEvent.FLOOD_CONTROL`. Only the
    producer was missing — the inverse of the usual "ported but never wired" shape, and just as
    silent, since an event nobody emits looks exactly like a feature nobody wants.
  - AS3 does not route this through `dispatchChatEvent()`: it builds the event by hand with
    userId `-1` and the seconds in the **text** field, which is how the widget reads it back.

- ✅ **Wall-item *move*, and the unported repeated-placement subsystem**, 2026-08-07. Follows the
  wall-placement entry below; both were blocked on the same missing `getLegacyGeometry()`.
  - **Moving an already-placed wall item.** New `MoveWallItemMessageComposer` — header **2999** from
    WIN63's own registry (`_SafeCls_2046.as::_composers[2999] = _SafeCls_2682`), corroborated by
    vortex-emulator's `MoveWallItemMessageEvent = 2999`, whose parser reads an int and a string.
    AS3's constructor takes `(objectId, category, locationString)` and pushes only the first and the
    third — the category is accepted and dropped. `handleObjectMove()` gained the same tile/wall
    split `handleObjectPlace()` has, `confirmObjectMove()` its category-20 arm, and
    `modifyRoomObject()`'s `OBJECT_MOVE` no longer refuses category 20.
  - 🐛 **`handleObjectMouseDown()` omitted category 20**, so ALT-drag and decorate-drag never started
    on a wall item no matter how complete the rest of the move path was. AS3 gates on
    `category == 10 || category == 20 || monsterplant || rentable_bot` (`_SafeCls_1821.as:1063`).
  - **A floor drag onto an illegal tile now snaps back.** AS3's tile arm re-runs the move against the
    drag's *original* location when the hovered one fails; the port left the item at the last valid
    spot. Behaviour change for category 10, in the method being restructured.
  - ⭐ **Repeated placement was never ported at all** — this is why placing a stack stopped after one
    item. `initializeRoomObjectInsert()` takes one more argument in AS3, which `FurniModel` passes as
    `true` on both of its calls, and it drives a whole small subsystem: a `_repeatedPlacement` flag,
    the last placed type and direction (`clearRepeatedPlacementData()`), and — the load-bearing part
    — `recalibrateMovements(roomId, false)` at the end of the arm. That second argument turns the
    operation check *off* so the cached mouse-move is replayed unconditionally, building the next
    item's ghost immediately. Without it the next item is armed but invisible until the mouse moves
    again, and if the cursor is already where the next copy goes, it never does. Carrying the
    direction over is what makes a row come out aligned instead of resetting each copy.
  - 🐛 **One click reaches `placeObject()` twice, and repeated placement turned that into a second
    item.** `processRoomCanvasMouseEvent()` dedups per category *bucket*: the ghost is category 10
    (bucket -2), the room planes are category 0 (bucket 0), so a click on the same pixel is delivered
    to both and both confirm. Harmless while the first call left no selection behind — with the
    synchronous re-arm, the second delivery found a fresh OBJECT_PLACE and placed the next item on
    the same spot, two at a time and stacked. The confirmation now carries the click's `eventId`
    (one per canvas frame, the same key the bucket dedup uses) and refuses a repeat. AS3 needs no
    equivalent field: its ghost is mouse-transparent and only a click on the room object confirms,
    and where it does need this it reuses the same map (`setMouseEventId(0, "click", …)`).
  - `_moveMouseEventCache` now caches the move *event* (tile or wall), not tile coordinates — AS3
    replays it through `handleRoomObjectMouseMove()`, which has to know which surface was under the
    cursor. Coordinates alone could never replay a wall item's repeated placement.
  - 🐛 **`requestSelectedFurniPlacement()` had one parameter where AS3 has two**, so
    `attemptPlaceNextFurni()`'s `(false)` — AS3's `refuseRoomProperties` — was landing on
    `useLastSelectedIndex`. Latent in that path, but it hid that the first argument was never ported;
    `GroupItem` is the one caller that passes `true` (`GroupItem.as:985`).

- ✅ **Wall-item placement (category 20) — the last of the three placement categories**, 2026-08-07.
  Placing a wall item from the inventory was refused up front in
  `RoomEngine.initializeRoomObjectInsert()`, on a stale claim: that no AS3 tree carried the inverse
  of `LegacyWallGeometry.getLocation()`. It does, and this port already had it —
  `LegacyWallGeometry.getOldLocation()/getOldLocationString()` (`_SafeCls_1855.as`), the
  `":w=wallX,wallY l=localX,localY side"` encode the server parses.
  - **The geometry moved to where AS3 keeps it.** `RoomMessageHandler` held one private
    `LegacyWallGeometry` for the whole client; AS3 has one *per room* on `RoomInstanceData` and
    reads it back through `getLegacyGeometry(roomId)` (`_SafeCls_1984.as:569/906/979/1399`). It now
    lives on `IRoomEngineRoomInstanceData`, with `getLegacyGeometry()` on `RoomEngine` +
    `IRoomCreator`. That also fixes a latent bug: one shared instance meant a wall item placed after
    a room change was positioned against the previous room's height map. Note `addWallItem()` reads
    it from its own `roomId` argument, not `_currentRoomId` — AS3 does the same, at that one site.
  - **Ported**: `validateWallItemLocation()` (the two-pass clamp: slide the point back onto the wall,
    then reject when even the clamped point cannot fit), `handleWallItemMove()`, the category-20 arm
    of `handleObjectPlace()`, and `updateObjectRoomWindow()` (`_SafeCls_90.as`) — which also closes
    the standing TODO in `updateObjectWallItemLocation()`, where a wired-moved window kept its hole
    at the old spot.
  - **Routing**: `RoomObjectWallMouseEvent` was already dispatched by `RoomLogic` and already
    reached `onRoomObjectEvent()` — where it was swallowed by the generic `RoomObjectMouseEvent`
    arm it inherits from. It now branches first, into `handleWallMouseEvent()`. The category-20
    exclusion in `handleTileMouseEvent()` is gone too: AS3 wants the tile path to *dispose* a wall
    ghost, which is what makes it vanish when the cursor leaves the wall.
  - **`placeObject()`** takes AS3's two `placedOnFloor`/`placedOnWall` parameters (which kind of
    event confirmed the placement) and emits them on `REOE_PLACED` — `FurniModel.onObjectPlaced()`
    needs them to re-arm, and its wall gate (`objectId === ref`) is satisfied because the server
    sends `roomItemId` as `+|itemId|` for wall items and `-|itemId|` for floor ones. The direction
    now stays in degrees until after the wall string is built, since `getOldLocationString()`
    switches on 90 vs 180 and would have seen 2 vs 4.
  - 🐛 **The stickie test cannot be transcribed literally, and transcribing it literally broke the
    whole feature.** AS3 selects the stickie composer with
    `getModelController().getString("furniture_is_stickie") != null`. But `FurnitureStickieLogic`
    sets that flag to the **empty string**, and this port's `RoomObjectModel.getString()` returns
    `''` for a missing key too — so the literal transcription is true for every object and false for
    none, and every wall item went out as a `PlacePostItMessageComposer` the server drops. The
    symptom was precise and misleading: the ghost followed the wall correctly and the click placed
    nothing. `hasString()` is the distinction AS3's null return actually carries.
  - `PlaceObjectMessageComposer` regained its real AS3 signature (`_SafeCls_2135`:
    `id, category, wallLocation, x, y, rotation`) and its `switch(category - 10)` — one composer,
    `"id x y rot"` for floor and `"id :w=…"` for wall. The stickie branch
    (`furniture_is_stickie` → `PlacePostItMessageComposer`) is reachable now that the wall string is,
    and is wired.
  - **Still open**: *moving* an already-placed wall item. `modifyRoomObject()`'s `OBJECT_MOVE` still
    refuses category 20 — that one needs the move composer (`_SafeCls_2682`, `id + locationString`),
    which is not ported. Also left as TODOs in `placeObject()`: the `free_placement_room` alert and
    `setPlacedObjectData()`.

- ✅ **`vortex-imager` — external avatar + guild-badge imager, and two engine bugs it exposed**,
  2026-08-07. New package `packages/vortex-imager`: a Fastify service answering the
  `/habbo-imaging/…` routes the hotel config already points at (`habbo.imaging.avatar.url`,
  `group.badge.url`, `group_logo_url_template`).
  - **It runs the client's own renderer, not a second one.** `esbuild`'s `alias` rewrites every
    `pixi.js` import in the engine's transitive graph — 130 files rooted at `AvatarRenderManager`,
    importing exactly three symbols (`Texture`, `Spritesheet`, `Assets`) — to a Node shim over
    `@napi-rs/canvas`. `OffscreenCanvas`, `createImageBitmap`, `DOMParser`/`Document`/`Element`
    and `localStorage` are shimmed alongside it. The boot mirrors `VortexMain.prepareCore()` with
    the real `HabboConfigurationManager` pointed at the hotel's own `external_variables`, so the
    imager resolves the current asset build the same way and at the same time the client does.
  - 🐛 **Effect and dance animations were registered under the empty string.**
    `EffectAssetDownloadManager.onLibraryComplete()` passed a .nitro bundle's whole `animations`
    *map* (`{"Dance 1": {name: "dance.1", …}}`) to `registerAnimation()`, which keys by
    `data.name` — undefined for a map. Every `getAnimation("dance.1")` / `getAnimation("fx.N")`
    missed. Nothing failed loudly: an animated action with no animation renders its
    `assetpartdefinition`, and for both `Dance` and `AvatarEffect` that is `std` — so dances and
    effects came out as a plain standing avatar, in the room as well as here. Each entry is now
    registered under its own name.
  - 🐛 **Effect libraries were never registered with the `AssetAliasCollection`.** Only
    `AvatarAssetDownloadManager` did it, so `getAsset("h_std_fx2_1_1_2_0")` always returned null
    and no effect sprite — hoverboard, halo, hearts — could ever be drawn. AS3 needs no equivalent
    (its effect SWF lands in the shared `AssetLibraryCollection`); this port builds one
    `GraphicAssetCollection` per bundle, so the wiring is port-specific and marked `TS-only`.
  - ⚠️ **Two small-scale gaps left open, and worked around in the imager rather than in the
    engine.** `AvatarScaleType.SMALL` (`sh`) cannot render on this asset build — `hh_human_body`
    ships 244 assets, all `h_*`, no `sh_*` — and the port of `LARGE_TO_SMALL` (`h_50`) never
    halves the parts, so `h`-sized parts land almost entirely outside the 45x72 canvas. Both
    render a fully transparent avatar with nothing logged. `size=s` composites at full scale and
    halves the finished image instead; see `packages/vortex-imager/src/avatar/AvatarRequest.ts`.
  - **Badges are `BadgeEditorPartItem.getComposite()` ported verbatim** — 39x39, 13x13 grid cells,
    colour multiply, uncoloured mask on top. Part/colour catalogue and `?user=` lookups are read
    from the hotel database (`group_badge_parts`, `group_colors`, `players`), not mirrored.

- 🐛 **The `handler/` count was wrong all along, and the per-slice increments propagated it**,
  2026-08-07. The breakdown carried `handler/ 13/47`, and each priority-1 slice bumped it by one,
  reaching a claimed 27/47. The real figure is **39/47**.
  - **Two independent measurements agree**, which is what settles it: `ls handler/*.ts` gives 39
    files against AS3's 47, and `RoomDesktop.createWidget()`'s switch carries 39 `RWE_*` labels
    against AS3's 47. 25 handlers pre-dated this session; 14 were added by it.
  - **The 13 was already stale when this run started** — it was not broken by the increments, only
    carried forward by them. Every "handler/ N/47 → N+1/47" line in the slices below is therefore
    off by 12 in both terms; the *deltas* are right, the absolutes were not.
  - ⚠️ **Measuring by declared `type` gives a third, also-wrong answer.** Three handlers
    (`PollWidgetHandler`, `UiHelpBubbleWidgetHandler`, `FriendRequestWidgetHandler`) all return
    `"RWE_ROOM_POLL"` from `get type()` — an AS3 copy-paste — so a type-based census counts
    `RWE_UI_HELP_BUBBLE` and `RWE_FRIEND_REQUEST` as covered when they are not. Compare
    **switch label to switch label**, never `type` to switch label.
  - 📏 **The genuinely missing types**, by switch label: `RWE_CAMERA`, `RWE_CRAFTING`,
    `RWE_PLAYLIST_EDITOR_WIDGET`, `RWE_ROOM_THUMBNAIL_CAMERA`, plus
    `RWE_VIMEO`/`RWE_YOUTUBE` — the last two blocked on a video-player decision, see below.
    `RWE_CHAT_WIDGET` is in the port's switch and *not* in AS3's, which is deliberate and
    documented in the `habbo/freeflowchat` row.
    `RWE_EXTERNAL_IMAGE`, `RWE_UI_HELP_BUBBLE` and `RWE_ME_MENU`'s **handler** were all on this
    list and were ported 2026-08-07/08 — **42/47** now, re-measured with the recipe below (both
    measurements returned 42). `RWE_ME_MENU`'s *views* are still missing: the switch label and the
    whole message layer exist, `widget/memenu/` (9 files, 1,904 l.) does not.
  - **Re-measure recipe** (both must agree):
    ```
    ls packages/vortex-engine/src/habbo/ui/handler/*.ts | wc -l
    grep -c "case 'RWE_" packages/vortex-engine/src/habbo/ui/RoomDesktop.ts
    ```
  - ⏸ **`RWE_VIMEO` / `RWE_YOUTUBE` are deliberately not started.** Both widgets are built on
    `com.vimeo.api.VimeoPlayer` / the YouTube AS3 player — Flash SWF APIs with no web counterpart.
    Porting them now would produce a stub, not a feature; they need a decision on how a video
    player is embedded (a DOM overlay over the canvas, like the existing text-input bridge) before
    the widget layer is worth writing.

- 🔄 **Priority 1, slice 18: `RWE_ME_MENU` — the views**, 2026-08-08. The nine files of
  `widget/memenu/` (1,904 l. AS3), plus `RoomWidgetGetSettingsMessage`,
  `RoomWidgetAvatarEditorMessage`, `RoomWidgetShowOwnRoomsMessage`, the five remaining widget
  events, and `questEngine` on the container. `RWE_ME_MENU` is now complete — handler *and* views.
  - **One window, four swappable pages.** `changeView()` disposes the old view, builds the new one
    and drops it into the layout's `MAIN_CONTENT` slot at index 0. An unknown name logs and leaves
    the current view alone — but still runs `updateSize()`, so it resizes the window around the
    view already there.
  - 🐛 **Three AS3 defects in `unregisterUpdateEvents()` alone**, all kept: the two mini-mail
    types are never removed; the two avatar-editor types are removed against *each other's*
    callbacks, so neither comes off; and `GAME_MODE` is removed by passing `NORMAL_MODE` twice.
    There is also no `super` call, unlike `registerUpdateEvents()`.
  - 🐛 **`hide()` removes the view from the wrong parent.** `changeView()` parents it to
    `mainContainer`; `hide()` calls `removeChild` on `_window`. The remove finds nothing and only
    `visible = false` actually hides the menu. Kept.
  - **The toolbar toggle is not a plain toggle.** Clicking while the menu is open *raises* it
    instead of closing it when another window covers it — `WindowToggle.isHiddenByOtherWindows()`.
    So the first click after losing focus brings the menu back.
  - **Dance buttons are built at open time, not laid out.** Styles 2-4 are club-only and simply
    are not created without a subscription; the style is then parsed back out of the button's own
    name (`dance_3_button` → 3). They insert at `numListItems - 1`, before the list's last item.
  - **Sound settings commit on `dispose()`**, not on change: every drag and both end buttons send
    the *preview* message, and leaving the page is what stores. −1 in any slot means "keep".
  - **Probe-verified** (browser, real classes through their own specifiers, fake window tree): all
    four views build; the unknown-view no-op; the toggle both ways; club days/level rebuild the
    view while a periods-only change does not; the effects gate; **4 dance buttons with a
    subscription, 1 without**; a dance-3 click sending style 3; preview keeping the two untouched
    volumes; dispose storing; every main-view button reaching its manager; talents being the one
    button that leaves the menu open; and a gated button returning *before* the tracking call
    while an ungated one tracks.
  - ✅ **`RoomWidgetSettingsUpdateEvent`'s parameter/field mismatch confirmed at runtime.** Feeding
    it `(type, 0.4, 0.6, 0.8)` yields `generic = 0.8, furni = 0.6, trax = 0.4` — the first and last
    swapped, exactly as the AS3 bottom-up assignment specifies. Documented in slice 17; now
    measured.
  - ⚠️ **Two `TODO(AS3)` carried forward.** `MeMenuMainView.onPerkAllowances()` is unreachable:
    AS3 registers its own `PerkAllowances` message event on the connection, but this port already
    binds that event globally (header 1535) and its parser feeds `SessionDataManager`, so a second
    registration would double-parse. The guide button uses the cached answer and is not refreshed
    by a later push. And the clothes button still opens nothing — see the avatar-editor entry.

- 🐛 **Avatar editor, first run against the live client**, 2026-08-08. The editor opens and draws
  — window, tab strip, category tabs, grid, palettes, wardrobe column, preview and save bar are all
  on screen. Two problems surfaced, one code and one assets.
  - **Fixed: `BodyModel.updateIconImage()` handed a PixiJS `Texture` to `drawImage()`.** It cast
    `getCroppedImage()`'s result to `ImageBitmap`; this port's `AvatarImage` returns a `Texture`
    over an `OffscreenCanvas` where AS3 returns a `BitmapData`. The cast compiled and threw at
    runtime the moment the body page was built — and because the throw unwound through
    `initCategory` → `init` → `getWindowContainer` → `setViewToCategory`, it **aborted the page
    build mid-flight**. Routed through `AvatarTextureUtils.toImageBitmap()`.
    - **The hole is closed**: `IAvatarImage.getCroppedImage()`/`getImage()` were typed `any` and are
      now `Texture | null`, so this class of mistake is a build error. No other call site needed
      changing — the two catalogue consumers already read the texture's canvas resource properly.
  - **Measured, not code: 497 of the 2,730 libraries `figuremap.xml` references are absent from the
    asset host.** Re-measure by diffing the `<lib id>` values against
    `gordon/vortex-assets-PRODUCTION-*/`. Those clothing items can never render whatever the client
    does; the core libraries (`hh_human_body`, `hh_human_leg`, `hh_human_shirt`, `hh_human_shoe`)
    are present and serve HTTP 200 with valid headers.
  - **`NitroBundleLoader`'s error was actively misleading.** A missing `.nitro` comes back as a web
    page, not as a visible 404 — Apache serves its error document, Vite serves the SPA shell — and
    the parser read two characters of markup as a length, reporting "filename length out of range
    (25711)". `25711` is `0x646F`, i.e. the `do` of `<!doctype`. The guard only tested
    `<!doctype html` and `<html`; it now rejects any text/markup start and reports the **URL** and
    the first bytes.
  - 🔌 **Fixed: every clothing thumbnail resolved its sprites against the wrong index.** AS3 asks
    `avatarRenderManager.getAssetByName("h_std_ch_3216_2_0")` and Flash's flat library answers. This
    port's `.nitro` bundles key their spritesheet frames **library-prefixed** —
    `hh_human_shirt_h_std_ch_210_0_0` — and keep the unprefixed names, offsets and `source` aliases
    in a separate `assets` map, so the raw library lookup returns null for **every** sprite and
    `renderThumb()` fell back to the download icon each time. That is what the grid of grey
    person-glyphs was.
    - Measured by unpacking `hh_human_shirt.nitro` (gzip, not zlib — pako's `inflate` auto-detects,
      Python needs `wbits=47`): 1,029 frames, all prefixed; `assets` keyed `h_std_ch_1_0_0`.
    - `IAvatarRenderManager` gained `getSpriteAsset()`, forwarding to the **same
      `AssetAliasCollection` the room renderer uses**, which resolves both the prefix and the
      `source` aliases. `getAssetByName()` stays as-is for the flat window-manager images.
    - Probe-confirmed: the lookup now goes through `getSpriteAsset('h_std_ch_3216_2_0')`, never
      touches the raw library, and composites the sprite into the tile (green at its centre)
      instead of returning the download icon — while a part with no sprite at any direction still
      falls back to `downloadFigure()`.
  - 🔌 **Fixed: the avatar preview was empty because `FigureDataView` had never been ported.**
    `figuredata/` held `FigureData` and two interfaces; the concrete view (82 l. AS3) that actually
    draws the preview was missing, so `FigureData._view` stayed null and every `updateView()` was a
    no-op. AS3's `FigureData` constructor builds `new FigureDataView(this)` unconditionally — the
    port's optional second parameter now defaults to exactly that.
    - It has **two** drawing paths: a real avatar in the preview room when the room engine is up
      (which is what makes the rotate button work), and a flat rendered image otherwise, re-rendered
      from `avatarImageReady()` once the sprites arrive.
    - `IFigureDataOwner` had been narrowed to a single member back when no view existed; it now
      carries the `view` and `avatarRenderManager` reach AS3's `FigureData` always had.
    - `AvatarTextureUtils` gained `toCanvas()`: `IRoomPreviewerWidget.showPreview()` takes an
      `HTMLCanvasElement`, the port's stand-in for the raw `BitmapData` AS3 passes it.
    - The chain checks out: the layout declares `avatarWidget` with `widget_type=room_previewer`,
      and `HabboWidgetFactory` registers `room_previewer` → `RoomPreviewerWidget`.
  - 🔌 **Fixed: colour swatches were all white.** `AvatarEditorGridColorItem` tints a shared
    greyscale chip, and could not fetch it: AS3 reads one flat Flash library holding both layouts
    and images, while this port keeps layouts in the window manager's registry, **images in its
    `ResourceManager`**, and a DI component's own `assets` library holds neither —
    `HabboAvatarEditorManager.getAssetBitmap()` was reading that component library. `IResourceManager`
    gained a synchronous `getAsset()` (`retrieveAsset()` is receiver-based and `setupColor()` has to
    composite immediately), forwarded by the window manager.
  - 🐛 **Fixed server-side: `UserChange` (3798) was four fields short.** The client reads nine —
    id, figure, sex, customInfo, achievementScore, an unused string, a count-prefixed list of unused
    int triplets, badgesRank — and the emulator wrote the first five, so the sixth read threw
    `End of buffer` and the message was dropped whole: a figure or motto change made outside the
    room never landed. The parser is faithful AS3 and was **not** touched; the serializer was
    completed and covered by `UserChangeWireTests`. See that parser's note.
  - **The download→repaint chain is intact** (verified by reading it end to end):
    `AvatarEditorGridPartItem.analyzePartLayers()` → `downloadFigure()` →
    `AvatarAssetDownloadManager.loadFigureSetData()` registers the listener and calls
    `avatarImageReady()` when the libraries complete, and a *failed* library still emits `COMPLETE`
    so one missing file cannot stall the queue.

- ✅ **Avatar editor, slice 11: the rename dialog — the editor is COMPLETE**, 2026-08-08. Both
  `view/` files, **478 l. of AS3**: `AvatarEditorNameChangeView` (358) and
  `AvatarEditorNameSuggestionListRenderer` (120). With this the editor's ≈**7,200 l.** are ported
  end to end — **no `TODO(AS3)` left anywhere under `habbo/avatar/`'s editor side**, and
  `HabboAvatarEditor` no longer needs a logger because it has nothing left to report as missing.
  - **`AvatarEditorMessageHandler.onCheckUserNameResult()` finally routes.** It aims at the
    *dialog*, not the editor, and the dialog is built lazily on the first rename click — so a
    result arriving before then is still dropped. That is AS3's shape, not a gap.
  - **The dialog never sends the rename.** `select_name_button` has **no case** in the window
    procedure, and neither does `cancel_selection_button`; the only click AS3 handles is
    `check_name_button`. So the dialog can validate a name and then do nothing with it. Kept.
  - 🐛 **`nameCheckWaitEnd(false)` skips enabling the select button but never disables it**, so a
    rejection following a successful check leaves the button lit on a name the server has just
    refused. Probe-confirmed.
  - 🐛 **Result code 1 has an empty case** — it clears the wait state and leaves whatever message
    the previous code wrote. The 2016 build does the same, so it is deliberate. Probe-confirmed:
    the text after code 1 is still the merge-hotel-down message.
  - 🐛 **`_pendingName` is only ever assigned null**, so the guard in `set checkedName()` can only
    fire when the server returns a null name. Kept.
  - **Picking a suggestion enables the select button** for a name that has never been checked —
    `nameSelected()` calls `nameCheckWaitEnd(true)`. Probe-confirmed.
  - **The suggestion box is hand-laid-out, not an item list.** Each chip is built, measured, and
    either placed or **disposed** — too wide for the box, or past the forced 150px ceiling, and it
    is thrown away rather than clipped, so the box silently shows fewer names than the server sent.
    Probe-confirmed: three 40px chips in a 100px box land at (4,4), (49,4), (4,25) and `render()`
    returns 41; 500px chips place none and return 0; a 40px-tall stack caps at three rows.
  - **`CheckUserNameResultMessageEvent` was two codes short.** AS3 declares eight on the event class
    itself; the port had six. Added 6 and 7 to match its sibling `ChangeUserNameResultMessageEvent`,
    and documented that **every** identifier is obfuscated in every tree — including 2016 — so all
    eight names are derived, six of them from the localisation key the dialog picks per code.
  - **The host gained three members**: `registerLocalizationParameter()` for the `%name%`
    substitution, and `desktopWidth` so the dialog can slide back on screen. Probe-confirmed:
    opening at x 600 with a 300-wide dialog on an 800-wide desktop clamps to 500; at x 100 it stays.

- 🔄 **Avatar editor, slice 10: `nft/` + `hotlooks/` — the last two pages**, 2026-08-08. All five
  files, **572 l. of AS3**: `NftAvatarsModel` (145), `HotLooksModel` (128), `HotLooksView` (102),
  `NftAvatarsView` (102), `NftWardrobeParamView` (95). **`HabboAvatarEditor` now builds all eight
  pages plus the wardrobe**, and `loadNftFigure()`'s saved-NFT branch works for the first time.
  - **The only two parts of the editor that talk to the connection themselves.** Both models
    register their own message event and fire their own request **from the constructor** — i.e. at
    editor `init()`, long before anyone opens the tab. Everything else in the editor goes through
    `AvatarEditorMessageHandler`. Probe-confirmed: constructing either sends its composer and
    leaves exactly one listener registered.
  - 🐛 **`HotLooksModel.dispose()` leaks its listener.** It calls `super.dispose()` **first**, which
    nulls `_controller`, then guards the unregistration on `controller && controller.manager && …`
    — now false. A disposed model keeps receiving pushes and keeps building `Outfit`s into a map it
    has already dropped. `NftAvatarsModel.dispose()` does the same two steps in the **opposite**
    order and gets it right. Probe-confirmed both ways: one listener left behind, zero. Kept.
  - 🐛 **Neither arrival handler tells its view.** Both append to their list — never clearing, so a
    second answer doubles the grid — and neither calls `update()`. Looks and avatars that arrive
    after the page was last opened only appear when it is re-entered. Kept.
  - **Hot looks are bucketed per gender**, keyed by `gender.toUpperCase()`, and the page's getter
    reads the editor's *current* gender — so switching sex swaps the whole grid. Probe-confirmed:
    `m`/`M`/`F` sorts into `[M1, M2]` and `[F1]`.
  - **Both grids resolve a click through `window.parent`.** The procedure sits on the outfit tile's
    container, but the port dispatches it with the window the event actually landed on — a child —
    so the grid index has to be looked up on the parent. Verified against `WindowController.update()`,
    which calls `proc(event, this)` after `procedure` has bubbled up from the parent.
  - 🐛 **`selectNftAvatar()`'s highlight bookkeeping sits outside its guards**, so clicking a tile
    whose render never arrived un-lights the previous selection, lights nothing, and leaves the grid
    with no active tile — while staging and wearing nothing. Probe-confirmed.
  - 🐛 **An unknown NFT collection makes the caption read `null #<id>`** — `getLocalizedCollectionName()`
    returns null and the string is built by concatenation. The *colour* path has a real white
    default; the name path does not. Kept.
  - **The caption's colours are not the tile's.** Avatar and clothes reuse their tile's *active*
    background, but genesis gets `0xFF1E3B21`, a colour that appears nowhere else. Probe-confirmed
    across all four cases.
  - **`ICategoryModelOwner` gained four members** the two pages reach: `clubMemberLevel`,
    `loadAvatarInEditor()`, `setNftOutfit()` and `getLocalization()` on the host. `Outfit` and
    `NftOutfit` were retyped from the concrete editor to this interface — they touch three of its
    members, and it is what `HotLooksModel` has to hand.
  - **`HabboAvatarEditor` no longer needs a logger**: every "not ported yet" line it carried is gone.

- 🔄 **Avatar editor, slice 9: `effects/` — the effects page**, 2026-08-08. All five files,
  **648 l. of AS3**: `AvatarEditorGridViewEffects` (160), `EffectsParamView` (177),
  `AvatarEditorGridItemEffect` (127), `EffectsModel` (101), `EffectsView` (83). Plus the interface
  behind them, `habbo/avatar/_SafeCls_3781.as` → **`IAvatarEffect`** (name DERIVED: it is declared
  in `habbo/avatar/`, not `habbo/inventory/`, i.e. it is the *editor's* view of an effect;
  `habbo/inventory/_SafeCls_1973.as` is the inventory's own, narrower one). `AvatarEditorView`'s
  `effectsGridView` is no longer null and `HabboAvatarEditor` adds the page — **the me-menu's
  effects button now leads somewhere.**
  - **Two grids share one window.** `AvatarEditorGridViewEffects` wraps the *same*
    `grid_container` as `AvatarEditorGridView`; neither knows about the other, and whichever page
    is showing refills it. That is why the effects grid hides both palettes unconditionally — an
    effect has no colours and the previous page may have left them up.
  - **The grid always leads with a null-effect tile.** "Wear nothing" is index 0, so a real effect
    sits at its inventory index **plus one** — `EffectsModel.selectPart()` is built around that
    offset. Probe-confirmed: three effects give four tiles, and type 22 (second in the inventory)
    reports grid index 2.
  - **Selection fires on `WME_DOWN`, not on click** — unlike every clothing tile. Probe-confirmed:
    a `WME_CLICK` on an effect tile does nothing at all.
  - **`selectPart(-1)` means "restore what is worn"**, the sentinel `EffectsView.reset()` passes.
    It resolves the worn effect's grid position and marks it selected **without** calling
    `setAvatarEffectType()`, because nothing changed. Probe-confirmed: no application recorded.
  - 🐛 **The first deselect always targets index 0.** AS3 reads a missing `Dictionary` entry, gets
    `undefined`, and coerces it to `0` at the `int` parameter — so the very first selection
    un-highlights the "wear nothing" tile whether or not it was lit. Reproduced with `?? 0`.
  - 🐛 **`updateView(null)` runs twice** when the null tile is picked: once inside the branch and
    once at the shared tail. Kept.
  - 🐛 **The countdown never stops at zero** — `onSecondsTimer` post-decrements a local copy with no
    floor, so a lapsed effect ticks into negative seconds until the view is given something else.
    Kept.
  - **The bar is two rectangles, not a scaled asset**: opaque black, then the elapsed fraction in
    `0x20BF20`. A **permanent** effect substitutes the duration for the remaining time, so it reads
    full even at zero seconds left — probe-confirmed green at the last pixel.
  - **The time label is three localisation keys with placeholders**, substituted into the caption's
    *resolved* text. Probe-confirmed: 90 s → `01:30`, 3 725 s → `01:02:05` (the hours segment is
    dropped entirely under an hour), 200 000 s → `2 days`, permanent → its own string.
  - **`Effect.iconImage` was missing from the port** — AS3 returns the same `_SafeStr_5528` field
    from both `icon` and `iconImage`, and `IAvatarEffect` declares the second. Added as an alias
    with its own trace.
  - **`AvatarEditorGridItemEffect` composites nothing**, unlike its clothing counterpart: the icon
    arrives ready-made from the inventory. Probe-confirmed: the null tile shows the generic
    remove-selection icon at a stack count of 1 with the badge hidden; a stack of 3 shows the badge
    reading "3"; an owned-but-inactive effect gets no duration bar at all, which is how the grid
    distinguishes "running" from "in the backpack".

- 🔄 **Avatar editor, slice 8: `wardrobe/` — the side panel and the outfit tile**, 2026-08-08.
  All six files, **828 l. of AS3**: `WardrobeModel` (157), `WardrobeSlot` (224), `WardrobeView`
  (114), `Outfit` (112), `OutfitView` (150), `NftOutfit` (71). `HabboAvatarEditor.init()` now adds
  the wardrobe to `_sideContent`, so `AvatarEditorView.setSideContent('wardrobe')` finds a real
  panel instead of collapsing the container to 1px — and `AvatarEditorMessageHandler.onWardrobe()`
  delivers its outfits instead of logging them away.
  - **The panel is built out of two detached templates.** `avatareditor_wardrobe_base` ships one
    column and one slot; both are pulled out of the layout in `WardrobeView`'s constructor and kept
    only by reference. The column is cloned per column needed, and the slot template is handed to
    `WardrobeModel`, which clones it per slot. Column count is `trunc((slots + 6) / 7)` — the
    integer-ceiling idiom, exact: probe-confirmed 7 → 1, 10 → 2, 14 → 2, 15 → 3.
  - **Entitlement splits at slot 5**: the first five come with club, the rest with VIP. A club-only
    user therefore gets five usable slots and five dead ones — probe-confirmed
    `[true×5, false×5]`.
  - **`init()` requests the outfits before the slots exist.** View, then `getWardrobe()`, then the
    slots — so the server's answer always lands on slots that are already there, which is what
    `updateSlots()`'s `if(!_initialised) return` guards. Every `reset()` costs a fresh layout build
    *and* a fresh round trip.
  - 🐛 **The empty-slot artwork can never load.** `WardrobeSlot.updateView()` asks for
    `avatar_editor_wardrobe_empty_slot`; `HabboWindowManagerCom.as` declares it as
    `avatar_editor_wardrobe_**wardrobe**_empty_slot`. The lookup returns null in the real client
    too, `updateView()` returns early, and the consequence is bigger than a missing picture:
    **`set_button.visible = false` and `get_button.visible = false` are unreachable**, because
    every disabled or empty slot takes that branch and bails before reaching them. Proved by
    probe — supplying the artwork makes both buttons hide correctly; withholding it leaves them
    visible and nothing painted. Kept as written.
  - 🐛 **`updateSlots()`'s two null checks record debug data and then fall through** into the loop
    that would have thrown. The `ErrorReportStorage` entry is the point, not the guard. Kept.
  - 🐛 **`OutfitView.windowEventProc()` is never attached** — declared private, assigned to nothing.
    Its body is also inverted: everything sits inside `if(type != "WME_CLICK")`. Kept as the only
    record of what its two colour constants were for.
  - 🐛 **`WardrobeView.dispose()` disposes the two templates inside the `if(_window)` branch**, so a
    view whose window failed to build leaks exactly the two objects that do exist. Kept.
  - **`Outfit` and `WardrobeSlot` compose differently**: the outfit tile bottom-aligns the avatar in
    a 35×60 frame, the wardrobe slot centres it on both axes in 22×48. Probe-confirmed against real
    pixels — blue at (17, 59) and transparent at (17, 5) for the tile; the avatar's colour dead
    centre and a transparent corner for the slot.
  - **`NftOutfit` picks its colours from the contract key**: orange for `habbo:avatar`, grey for
    `habbo:clothes`, dark-green-on-gold for `habbo:avatar_genesis` (the only one with a gradient),
    white otherwise. `−1` is what `OutfitView` reads as "no gradient".
  - **`ISideContentModel.controller` and `HabboAvatarEditor.wardrobe` are no longer `unknown`** —
    typed to the concrete editor and `WardrobeModel`, as AS3 types them. `WardrobeSlot` reaches
    nine members through the controller, well past what a narrow interface would be worth.
  - **New TS-only helper `AvatarTextureUtils`**: this port's renderer returns a PixiJS `Texture`
    where AS3 returns a `BitmapData`, and `IBitmapWrapperWindow.bitmap` wants an `ImageBitmap`. It
    reads `texture.source.resource` directly rather than `renderer.extract.canvas()` — a
    freshly-composed avatar texture has not been through a render pass, and `extract` reads back a
    blank square for one that has not.
  - **Probe-verified**: `set_button` saves through the handler with the *current* figure and
    repaints before the server answers; `get_button`/`get_figure` clear any staged NFT and then
    load; both are gated on `verifyClubLevel()`; `updateSlots()` routes by `slotId`, ignoring
    position, and drops an id no slot has; gender normalises `f` → `F`.

- 🔄 **Avatar editor, slice 7: the window — `AvatarEditorView`, the grid, and the five pages**,
  2026-08-08. `AvatarEditorView` (657 l. AS3), `AvatarEditorGridView` (248), `CategoryBaseView`
  (107) and the five category views `BodyView` (125) / `HeadView` (139) / `TorsoView` (135) /
  `LegsView` (123) / `MiscView` (117) — **1,651 l. of AS3**. `common/` is now **12 of 12**, and
  the editor draws for the first time: `HabboAvatarEditor.init()` builds the view, each model
  builds its own, and the manager's grid items now receive a real cloned window instead of null.
  - **One grid serves every page.** `AvatarEditorGridView` wraps a single `grid_container` and is
    re-filled on each tab or part-type change rather than rebuilt — which is why it has to remember
    the part type: a click arrives with only a grid index, and the part type is what turns it back
    into a selection.
  - **The two templates are lifted out of the layout and never put back.** `THUMB_WINDOW` and
    `COLOUR_WINDOW` are `public static var`s on `AvatarEditorView`, found by name in
    `AvatarEditorContent`, removed from it, and assigned **only when still null** — so the first
    editor built in a session decides what every later one's thumbnails look like. This is what
    slice 6's grid items were missing.
  - **Tabs are pruned by hand, backwards.** The strip does not re-flow: every tab after a removed
    one is shifted left by its width in a nested loop. Probe-confirmed — a restricted
    `['generic','head']` editor leaves exactly two tabs at x 0 and 52.
  - 🐛 **`_categoryContainers` collects the pages of removed tabs too**: the name list is filled
    *before* the availability test, so a pruned tab's `<id>_content` container is still detached and
    stored. Harmless (nothing shows it) and kept.
  - 🐛 **`setViewToCategory()` removes the outgoing page before fetching the incoming one**, so a
    page whose view is not built leaves `contentArea` empty rather than unchanged. That is the
    visible symptom of the three unported pages: the tab lights, the panel goes blank. Kept and
    probe-confirmed.
  - 🐛 **`update()` assigns the wardrobe button's visibility twice**, the second time without the
    club test — so a non-subscriber sees the button. Kept: deleting the dead line would delete the
    evidence that the club rule was meant to apply.
  - 🐛 **`dispose()` is dead from the halfway point.** It nulls `_window`, then guards the whole
    child-clearing tail on `_window` being non-null — so the three loops never run and `_editor` is
    never cleared. Kept verbatim; the leak is AS3's. The tail also looks up `figureContainer`, a
    name that does not exist in the layout (the preview is `avatarWidget`).
  - 🐛 **`embedToContext()` guards its removals with `if(index)`, not `if(index >= 0)`** — index 0
    is skipped — and in the no-context branch calls `removeChildAt` on the argument it has just
    established is null. That line is only reachable when the index is non-zero, which cannot
    happen right after `addChild()`, so AS3 never actually throws. Both kept.
  - 🐛 **`getFrame()` tests `_frame != null` twice**, the second time after returning on it — the
    dispose-and-rebuild branch is unreachable. Kept.
  - ⚠️ **`AvatarEditorGridViewEffects` (160 l.) and `AvatarEditorNameChangeView` (358 l.) are not
    ported**, so `effectsGridView` is null and the name-change button logs a warning. Both are
    constructed inline by `AvatarEditorView` in AS3; both carry a `TODO(AS3)` at the call site.
  - 🔌 **The manager had no `IID_HabboWindowManager` dependency** — without it the editor has no way
    to build `AvatarEditorContent` and would have come up as a working model with no window at all,
    the failure mode this port keeps producing. Added, optional like the other five.
  - **`ICategoryModel.controller` is no longer `unknown`**: narrowed to `ICategoryModelOwner`, which
    gained `view`, `manager` and a read/**write** `gender` — `BodyView`'s two gender tabs assign it.
  - **Probe-verified**: `showPalettes` gives 0 → both hidden, 1 → full 210px, 2 → 100/100 with the
    second at x 110 (10px gap); a `GET_MORE` tile opens `catalog.clothes.page` while `REMOVE_ITEM`
    takes the ordinary select-by-index route; a palette click resolves its layer from which of the
    two grids holds it; the tab bitmap toggle is idempotent (`_off` is stripped before being
    re-appended); `HeadView.switchCategory('zz')` throws with **no** tab lit, because AS3 dims the
    old one first; `BodyView`'s hover-out restores by gender rather than by hover state; save
    disables the button, saves, and closes by instance id; rotate steps 4→5→6→7→0.

- 🔄 **Avatar editor, slice 6: the two grid items**, 2026-08-08.
  `AvatarEditorGridPartItem` (514 l. AS3) and `AvatarEditorGridColorItem` (145), plus
  `getAssetByName()` exposed on `IAvatarRenderManager`. `common/` is now **10 of 12** — only
  `AvatarEditorGridView` (248) and `CategoryBaseView` (107) are left, and both belong with
  `AvatarEditorView`.
  - **A clothing thumbnail is composited, not iconified.** `AvatarEditorGridPartItem` finds each of
    the part set's sprites in the renderer's own asset library, unions their bounds to crop the
    picture to the clothing, then draws them in avatar draw order with the user's chosen colours
    multiplied into the dyeable layers. Sprites not downloaded yet → a download icon and a
    `downloadFigure()` call.
  - **The direction search is per-thumbnail, not per-sprite.** `THUMB_DIRECTIONS = [2,6,0,4,3,1]`
    is walked until *any* sprite of the first part exists; every later part then reuses that same
    direction, so a thumbnail is never half-profile-half-front. Probe-confirmed with a fixture
    where only direction 6 exists.
  - **`getAssetByName()` was missing from `IAvatarRenderManager`.** AS3's render manager extends
    `Component` and inherits it, so any interface holder can call it; the port kept the library
    private. Exposed with the real AS3 trace.
  - 🐛 **`_downloadIcon` is static and nulled by every instance's `dispose()`**, so disposing one
    thumbnail forces the next to re-fetch. Kept.
  - ⚠️ **`avatarImageReady()` is empty in AS3** — the class implements the listener interface
    *solely* to be passed to `downloadFigure()`, so a thumbnail showing the download icon would
    never repaint. Ported as a repaint with a `TODO(AS3)` saying exactly which line to revert if
    the empty body turns out to be deliberate rather than a decompilation loss.
  - **The colour chip is one shared greyscale bitmap**, tinted per swatch — not a coloured
    rectangle. And its border has three visual states drawn from **two** assets (`_3` is both
    selected *and* hovered), which is why hover-out has to consult `isSelected`.
  - **Probe-verified against real pixels**: a white chip × (1, 0.5, 0) reads back
    `[255,128,0,255]`; a grey-200 sprite × (1,0,0) reads `[200,0,0]`; the draw-order sort turns
    `hr,bd,ha,sh,ch` into `bd,sh,ch,hr,ha`; `colorLayerCount` comes from the sprites' own
    `colorLayerIndex` (2, not the part count); a synthetic tile is id **−1**; the disabled alpha
    reads **51** (255 × 0.2); hover raises the background to 0.5 only when unselected; and the
    not-downloaded path both calls `downloadFigure()` and paints the icon.
  - ⚠️ **The manager now builds the real items with a `null` window** — AS3 clones a static
    template held on `AvatarEditorView`, which is not ported. The items are otherwise real (the
    colour-layer count is now computed rather than hardcoded to 1); with a null window
    `updateThumbVisualization()` returns early and nothing is drawn.

- ✅ **Avatar editor, slice 5: the core, the manager, and `IID_HabboAvatarEditor` wired**,
  2026-08-08. `HabboAvatarEditor` (968 l. AS3), `HabboAvatarEditorManager` (305),
  `AvatarEditorMessageHandler` (239), `IHabboAvatarEditor`, `AvatarEditorIdEnum`, `IOutfit`,
  `IAvatarEditorSaveListener`, `AvatarUpdateEvent`. **The editor is now reachable**: the me-menu's
  clothes button, the own-avatar bubble, the clothing-change furni and the `avatareditor/open`
  link all open it.
  - 🔌 **`IID_HabboAvatarEditor` had never been provided.** It was declared in `iid/` and
    resolved to null in **seven** places, so every "change clothes" path in the client was a
    silent no-op. `VortexMain` now attaches `HabboAvatarEditorManager` alongside the other
    managers, and all seven were re-typed from `unknown` and given their real calls:
    `RoomUI`, `RoomDesktop`, `IRoomWidgetHandlerContainer`, `MeMenuWidgetHandler` (open + close),
    `FurnitureClothingChangeWidgetHandler` (open, `saveFigure`, close), `AvatarInfoWidget`,
    `HabboCatalog`, `HabboLandingView` and `AvatarImageWidget`.
  - ⚠️ **Two of those were not on my first list** and were only found by grepping for stale
    TODOs before committing: `AvatarInfoWidget.openAvatarEditor()` (the own-avatar bubble's
    "change looks", an empty method) and `AvatarImageWidget` (the landing-view refresh). Grep for
    the symbol, do not work from a list.
  - **The view layer is the remaining gap, and it is explicit.** `IAvatarEditorView` is an
    interface with a `TODO(AS3)`; `_view` is null, so nothing is *drawn*. Everything else runs: the
    figure model, five of the eight pages, the grid generation, the save path and all nine
    messages.
  - 🐛 **`close()`'s switch has its `case 2` after the `default`**, so the bot editor is never
    disposed. Probe-confirmed the four behaviours: id 0 hides and keeps, id 1 hides + disposes +
    forgets, id 2 does **nothing at all**, anything else disposes + forgets without hiding.
  - 🐛 **`embedEditorToContext()` disposes the old editor and then calls `add()`** — which
    refuses a key already present, and the disposed editor was never removed from the map. So the
    replacement is never stored and every later `getEditor()` returns the disposed one. Kept:
    `setValue()` would silently change which editor the message handler talks to.
  - 🐛 **`AvatarImageWidget` listens for `"AVATAR_FIGURE_UPDATED"`** while the editor raises
    `AvatarUpdateEvent`, whose type is `"AVATAR_UPDATE"`. The strings do not match in AS3 either,
    so the listener never fires there; kept verbatim rather than "fixed", since pointing it at the
    real type would add a redraw AS3 does not do.
  - **Probe-verified end to end** against a stub host: gender filtering (male grid gets the unisex
    and male sets, female the unisex and female), the club and sellable filters, **two** palettes
    both built, the "remove" tile appearing only when the part is *not* mandatory, opening sending
    `GetSelectedNftWardrobeOutfitMessageComposer`, saving sending `UpdateFigureDataMessageComposer`
    **and** raising `AVATAR_UPDATE`, a save listener diverting **everything** (1 capture, 0
    messages, 0 events), the four `close()` semantics, and the three wired call sites — me-menu
    open+loadOwn, me-menu close on dispose, and the clothing furni sending
    `(77, 'F', 'hd-180-7')` then closing editor 1.
  - ⚠️ **Three `TODO(AS3)` carry real behaviour gaps**, each documented at the site:
    `hasFigureSetIdInInventory()` and `getLastActivatedEffect()` do not exist on
    `IHabboInventory`, so sellable items are hidden and the last effect is not restored on close;
    and `onRoomAvatarEffects` is skipped entirely because its own-user guard needs a room desktop
    the engine-layer manager does not have — applying it unguarded would let *another* user's
    effect change your preview.

- 🔄 **Avatar editor, slice 4: `CategoryBaseModel` + the five simple pages**, 2026-08-08.
  The shared page behaviour (344 l. AS3) and `BodyModel` (127), `HeadModel` (43), `TorsoModel` (42),
  `LegsModel` (41), `MiscModel` (52). Three of the eight pages are left — hot looks, effects and
  NFTs, 374 l., each with machinery of its own.
  - **A fourth narrow interface**, `ICategoryModelOwner`. AS3's `CategoryBaseModel` holds a whole
    `HabboAvatarEditor` and reaches through it for six things; `BodyModel` also goes two levels
    deep for `controller.manager.avatarRenderManager`. Flattened to five members so the pages port
    and verify before the editor exists — the same technique as slices 2 and 3.
  - ✅ **Two grid-item members were missing from slice 3's interfaces**, caught by reading
    `CategoryBaseModel` rather than assuming: `isDisabledForWearing` (on **both** items — it is what
    makes a dimmed club item revert instead of apply) and `iconImage` (written by `BodyModel` per
    face). Added.
  - **Which page owns which part types**, probe-confirmed against AS3: body `hd`; head
    `hr ha he ea fa`; torso `cc ch ca cp`; legs `lg sh wa`; misc `pt mc`.
  - **`MiscModel` is the only simple page that does more than declare.** `generateDataContent()`
    returns **null**, not an empty grid, for a part type with nothing wearable in it — and most
    users own no props — so the page would end up holding no `CategoryData` at all and
    `selectPart()` would be a silent no-op. `ensureEmptyCategory()` fills the gap. Probe-confirmed
    both ways: misc gets two empty grids, head under the same conditions gets **null**.
  - 🐛 **`BodyModel` does not revert a dimmed club colour**, where `CategoryBaseModel` does. It
    opens the advert and leaves the grid showing the unaffordable choice. It also dereferences the
    selected colour unguarded, and its `switchCategory()` skips the lazy `init()` the base runs.
    All three kept; probe-confirmed the divergence (head reverts to index 1, body stays on 2, both
    open the advert).
  - **The face grid is rendered, not iconified.** `BodyModel` draws one head per candidate face on
    the figure's *current* colours, and repaints them all when the skin colour changes. A late
    render repaints **only** the thumbnail whose figure string matches — probe-confirmed: 2 renders
    for 3 thumbnails (the synthetic one is skipped), then exactly 1 on a late callback.
  - ⚠️ **`getCurrentColorIndex()` can return `undefined`.** `selectColorIds()` builds
    `new Array(n)`, and a colour the palette does not contain leaves a **hole** — AS3 arrays behave
    the same way, so this is faithful, not a port defect. It surfaced in the probe as a missing
    key. Callers treat it as an index; nothing currently breaks, but it is worth knowing before
    the views index a palette with it.
  - **Probe-verified** besides the above: `init()` is lazy and runs exactly once (5 part types
    generated on first touch, still 5 after two more calls); the grid restores its part *and*
    colour from the figure; selecting writes back (`hr-100-61.hd-180-7`); selecting the synthetic
    "remove" thumbnail writes set id 0; a whole-page club strip fixing both `hr` and `ha` and
    leaving `hasClubItemsOverLevel()` false; the sellable sweep under owned and unowned; `reset()`
    rebuilding (5 → 10 generations) while `dispose()` does not.

- 🔄 **Avatar editor, slice 3: `common/` — the selection model**, 2026-08-08. `CategoryData`
  (519 l. AS3), `TabUtils`, and the five interfaces of `common/` — **8 of the 12 files**, ~1,100 l.
  of the 2,036. The four left are window-backed and belong with the view: `AvatarEditorGridPartItem`
  (514), `CategoryBaseModel` (344), `AvatarEditorGridView` (248), `AvatarEditorGridColorItem` (145),
  `CategoryBaseView` (107).
  - **Three obfuscated interfaces named from their implementors**, not guessed: `_SafeCls_2660` →
    `ICategoryModel` (only `CategoryBaseModel` implements it, and it is what `HabboAvatarEditor`'s
    `_categories` map holds), `_SafeCls_4089` → `ICategoryView` (**eight** implementors — the six
    that extend `CategoryBaseView` plus `HotLooksView` and `NftAvatarsView`, which implement it
    directly), `_SafeCls_3071` → `IAvatarEditorGridView` (only `AvatarEditorGridView`).
  - **A category is a whole *page*, not a part type.** Each owns several part types — the torso page
    edits `ch`, `cc`, `ca` and `cp` — which is why every `ICategoryModel` method is keyed by part
    type rather than by the page.
  - **There are two palettes, holding the same colours twice.** A part may be dyed in two
    independent layers, so `HabboAvatarEditor` builds the colour list twice and each layer keeps its
    own selection; `getSelectedColorIds()` then trims the pair to however many layers the *selected
    part* wears, floored at 1. Probe-confirmed: the same input `[2, 3]` yields `[2]` for a
    one-layer part and `[2, 3]` for a two-layer one, and selecting a colour in one layer leaves the
    other untouched.
  - **Two more narrow interfaces instead of window classes** — the same technique as slice 2.
    `CategoryData` holds concrete grid items (514 + 145 l., each a thumbnail with its own rendered
    avatar) but touches six members of one and three of the other; extracted as
    `IAvatarEditorGridPartItem` / `IAvatarEditorGridColorItem`.
  - ⚠️ **`hasFigureSetIdInInventory()` does not exist in this port.** AS3 passes the whole
    `IHabboInventory` to the two sellable-item methods and calls only that one predicate; it is
    backed by a `Vector.<int>` of owned figure-set ids fed by a setter at `HabboInventory.as:1086`,
    and **neither the list nor the setter is ported**. Extracted as `IFigureSetOwnership` with a
    `TODO(AS3)`: until it is implemented, passing null reports every sellable item as unowned,
    which *hides* them rather than showing an unowned one.
  - 🐛 **One AS3 index bug kept.** In `getSelectedColorIds()`, the guard
    `_loc7_.length <= _loc8_` compares a palette's length against the **layer index** where it
    plainly means the *palette* index it reads two lines later. Harmless for the real two-layer,
    many-swatch palettes — it can only skip a layer whose palette is shorter than its own index —
    so it is ported as written.
  - **Probe-verified**: selection by id and by index with correct deselection; an unknown id and an
    out-of-range index both keeping the previous selection; `getCurrentPart()` yielding null with
    nothing selected (AS3 reads `_parts[-1]`); the two-layer independence and trimming above; all
    four null bail-outs of `getSelectedColorIds()`; club stripping falling back to index 0 and
    **skipping to index 1 when index 0 is the synthetic "remove item" thumbnail**; colour stripping
    replacing only the over-level layer and doing nothing when the palette holds nothing
    affordable; the sellable checks under owned/unowned/null inventory; full disposal; and
    `TabUtils.setElementImage()` being idempotent (`tab_head` → `tab_head_off` → `tab_head_off`).

- 🔄 **Avatar editor, slice 2: `FigureData` — the editable figure**, 2026-08-08. The model every
  category page reads and writes: a part→set map, a part→colours map, and the string
  serialisation joining them (336 l. AS3).
  - ✅ **`structure/figure/` was already ported and is reusable as-is.** Checked before writing
    anything: `ISetType`, `IPalette`, `IPartColor`, `IFigurePartSet` and `IFigureData` all exist,
    because the avatar *renderer* needs the same figure structure the editor does. The editor reads
    it through `avatarRenderManager.getFigureData()`, which the port already exposes. That removes
    a whole layer from the estimate.
  - **Two narrow interfaces instead of the editor.** AS3's `FigureData` holds a whole
    `HabboAvatarEditor` and a concrete `FigureDataView`, but calls exactly **one** method on the
    first (`getDefaultColour()`) and one on the second (`update()`). Both extracted —
    `IFigureDataOwner`, `IFigureDataView` — so the model ports and verifies before
    `AvatarEditorView` exists. `HabboAvatarEditor` will satisfy the first as written.
  - **Only fifteen part types are writable**, and the list is hoisted out of AS3's two identical
    `switch` statements into one set — they are the same list in the source, and a part accepted by
    one but not the other would yield a figure with a colour and no set. `bd` (body) is a real
    `AvatarFigurePartType` and is **not** in it: the body is not editable.
  - **Probe-verified round trips.** `hr-100.hd-180-7.…wa-2007` comes back as
    `hr-100-0.…wa-2007-0` — a group with no colour is given colour **0**, so bare groups gain a
    suffix; the female default, whose every group already carries a colour, is byte-identical.
    `lg-716-66-62` keeps both colours. Exactly 15 types accepted and **0** leaked past the filter.
  - **A negative set id removes the part** — that is how the "no item" thumbnail clears a slot —
    but its colour entry is left behind. And `getFigureStringWithFace()` yields the **empty
    string**, not a bare `hd-<id>`, for a head with no colour recorded: AS3 guards on the colour
    before reading the set id.
  - **Which mutations refresh the preview, measured**: `loadAvatarData` 1, a batched
    `savePartData(update=false)` 0, an unbatched one **2** (the set and the colour each refresh),
    the `avatarEffectType` setter **0** — every caller pairs it with an explicit `updateView()` —
    and the `direction` setter 1.
  - Malformed input is swallowed rather than thrown: `parseFigureString` does no validation and
    lets `NaN` fail the `>= 0` test in `savePartSetId()`.

- 🔄 **Avatar editor, slice 1: the wire layer**, 2026-08-08. The four messages
  `AvatarEditorMessageHandler` needs that the port did not have — `WardrobeMessageEvent` (1484) +
  its parser + the `WardrobeOutfit` DTO, `AvatarEffectSelectedMessageEvent` (3629) + parser,
  `GetWardrobeMessageComposer` (2210) and `SaveWardrobeOutfitMessageComposer` (116), all registered
  in `HabboMessages`. Self-contained: no editor code depends on it yet, and nothing depends on the
  editor.
  - **Its handler needs 11 messages; 7 were already ported.** Measured against
    `AvatarEditorMessageHandler.as`'s constructor and its three send methods: `AvatarEffect` (2624),
    `AvatarEffectAdded` (1577), `UserRights` (3599), `AvatarEffectExpired` (2236),
    `CheckUserNameResult` (382), `AvatarEffectActivated` (3814) and `CheckUserName` (413) already
    existed.
  - ⚠️ **3629 is not a duplicate of 2624.** A previous round recorded "room-effect header fixed to
    2624 (was 3629)", which is right — but 3629 is a *separate, real* event: WIN63 registers
    `_SafeStr_4546[3629] = _SafeCls_3136`, an effect-**selected** push carrying a bare type, where
    2624 carries a user id and an effect id. Both are now registered, and the probe confirms they
    resolve to different classes.
  - **Wire order verified rather than assumed.** The wardrobe parser reads **state, then count,
    then (slot, figure, gender) per outfit** — a reader starting with the count is a byte out.
    Probe-confirmed with a scripted wrapper that records its own read sequence:
    `int,int,(int,string,string)×2`. And `SaveWardrobeOutfit` pushes its arguments **in order**,
    unlike its neighbour `UpdateFigureDataMessageComposer`, which reverses them.
  - All three composers/events corroborated against the emulator's `Headers.cs`
    (`GetWardrobeMessageEvent` 2210, `SaveWardrobeOutfitMessageEvent` 116,
    `WardrobeMessageComposer` 1484), with WIN63's own registry as the primary source.

- ✅ **The avatar editor is COMPLETE**, measured 2026-08-08 after slice 11. Its ≈**7,200 l. across
  ~45 files** are ported end to end: core (2,269), `common/` (2,036, 12/12), `wardrobe/` (828, 6/6),
  `effects/` (648, 5/5), `view/` (478, 2/2), `figuredata/` (418), `nft/` (342, 3/3), `hotlooks/`
  (230, 2/2), `generic/` (252) and the five simple pages. All eight pages, the wardrobe side panel
  and the rename dialog are built; `IID_HabboAvatarEditor` is provided by `HabboAvatarEditorManager`
  and wired at all nine call sites.
  - **Re-measure**: `grep -rn "TODO(AS3)" packages/vortex-engine/src/habbo/avatar/*.ts
    packages/vortex-engine/src/habbo/avatar/{common,wardrobe,effects,nft,hotlooks,view,figuredata,generic,head,torso,legs,misc}/`.
    Five survive, and **none of them is an unported AS3 file** — measured 2026-08-08:
    - three are the same **inventory-side** gap: `IHabboInventory` has neither
      `hasFigureSetIdInInventory()` nor `getLastActivatedEffect()`, so every sellable clothing item
      reports as unowned (and is therefore hidden) and the last activated effect is not restored on
      close. `IFigureSetOwnership`, `HabboAvatarEditorManager.get inventory()` and its `close()`
      carry one each.
    - one is a room-session gate in `AvatarEditorMessageHandler` (`parser.userId ==
      roomSession.ownUserRoomId`) that needs the room desktop.
    - one is the judgement call flagged in slice 6: `AvatarEditorGridPartItem.avatarImageReady()` is
      **empty** in AS3 and is ported as a repaint, with the exact line to revert named.
  - The `avatar/` directory also holds the *renderer* (`AvatarImage`, `AvatarStructure`, `cache/`,
    `geometry/`, …) — a separate subsystem with its own gaps, not counted here.
  - Two AS3 defects still shape what the user sees, both deliberate and documented at their
    declarations: the wardrobe's empty-slot artwork can never load (its lookup name is misspelled in
    AS3), and the rename dialog cannot actually send a rename.

- 🔄 **Priority 1, slice 17: `RWE_ME_MENU` — the handler and its whole message layer**,
  2026-08-08. `MeMenuWidgetHandler` (481 l. AS3) + the 5 missing widget messages + the 6 missing
  widget events, plus the container/manager wiring they needed. `handler/` goes **41/47 → 42/47**.
  `widget/memenu/` (**9 files, 1,904 l.**) is the next slice — this one is the back end only.
  Corrected from a first count of 6 files / 1,312 l., which came from a non-recursive glob and
  missed the `soundsettings/` subdirectory (3 files, 592 l.). `find`, not `ls widget/<dir>/*.as`.
  - **The only handler that subscribes to five managers at once** — inventory, toolbar, help,
    catalogue and messenger — and it does so from its own `container` setter, unsubscribing the
    previous container first. Everything it learns is re-broadcast as a widget update event; the
    views never read a manager directly. 24 message types, of which **four are declared and
    handled by no case** (`NAVIGATE_TO_ROOM`, `GET_WARDROBE`, `select_outfit`, `RWRWM_EFFECTS`) —
    AS3's, kept, because the registration is what routes them here at all.
  - 🐛 **`RoomWidgetSettingsUpdateEvent`'s parameters do not line up with its fields.** AS3 takes
    `(type, trax, furni, ui)` and assigns bottom-up: `_uiVolume = param4`, `_furniVolume = param3`,
    `_traxVolume = param2`. Every call site passes
    `(type, trax, furni, generic)`, so the *generic* volume is what surfaces as `uiVolume`. Kept
    exactly — "fixing" the order would silently relabel two of the three sliders.
  - **Three container members and one inventory forwarder were missing**: `soundManager`,
    `messenger`, `avatarEditor` on `IRoomWidgetHandlerContainer`/`RoomDesktop`/`RoomUI`, and
    `IHabboInventory.clubPastPeriods`. The last one is a trap worth recording: there are **two
    distinct `Purse` classes** in AS3 — `catalog/purse/Purse` has `pastClubDays`,
    `inventory/purse/Purse` has `clubPastPeriods`, and only the second is what `HabboInventory`
    holds. The port already uses the right one; several trace comments in
    `inventory/purse/Purse.ts` point at the catalogue path and are wrong.
  - **`HabboHelpTutorialEvent` was constants-only.** AS3 extends `flash.events.Event`, so it has
    always carried a `type`; the port had the four constants and no class body, because nothing
    had raised or received one. `IHabboHelp` and `IHabboMessenger` likewise did not expose the
    event bus their `Component` base has always had.
  - ⚠️ **Two deliberate gaps, both visible as `TODO(AS3)`.** `IID_HabboAvatarEditor` has no ported
    manager (the placeholder `HabboCatalog` and `HabboLandingView` already use), so
    `RWCM_OPEN_AVATAR_EDITOR` logs and raises the tutorial event without opening anything. And
    `roomEngine.selectAvatar()` does not exist — it delegates to
    `RoomObjectEventHandler.setSelectedAvatar()`, an unported mechanism that deselects the previous
    avatar, sends `RoomObjectAvatarSelectedMessage` and consults the wired click handlers. Its only
    effect here is highlighting your own avatar while the menu is open.
  - **`onHabboToolbarEvent()` is decompiler-damaged and ported empty.** AS3 reads the icon id into
    a local and opens an `if("HTIE_ICON_MEMENU" !== id)` whose body is empty — a switch that lost
    its cases. The subscription is kept because AS3 registers it; there is nothing recoverable to
    put inside, and inventing a body would be a guess.
  - **The AS3 `!disposed` guards could not be ported.** AS3 guards each unsubscribe with
    `!manager.disposed && manager.events`; none of this port's manager interfaces declares
    `disposed` (it lives on `Component`, which they do not extend). The optional chain on `events`
    covers the same case — the guard only ever protected a nulled Flash dispatcher.

- 🔄 **Priority 1, slice 16: `RWE_UI_HELP_BUBBLE` — the guided-tour help bubbles**, 2026-08-07.
  `UiHelpBubbleWidgetHandler` (79 l. AS3) + the 4-file `uihelpbubbles/` cluster (836 l.), plus
  `RoomWidgetScriptProceedMessage`, and `friendBarView`/`toolbar` on `RoomUI`. `handler/` goes
  **40/47 → 41/47**; `widget/uihelpbubbles/` goes 0/4 → **4/4**.
  - **It is not driven by room events at all.** The widget registers itself as an
    `ILinkEventTracker` for the `helpBubble/` prefix in its own constructor, and a server-side
    script drives it with `helpBubble/add/<element>/<textKey>/…` links. That is why the handler's
    `processEvent()` is empty: its only job is `sendScriptProceed()` when the last bubble closes.
  - **The element is found by asking four owners in a fixed order** — toolbar, friend bar, room
    tools, then the chat input as a special case — and the first that knows the name wins. A
    bubble whose element *nobody* knows is silently skipped and the tour continues, which is the
    recursive `addNextBubble()` in the else branch.
  - 🐛 **Two AS3 defects kept, both documented at the declaration.** `get type()` returns
    **"RWE_ROOM_POLL"**, not "RWE_UI_HELP_BUBBLE" — AS3's copy-paste, and the third handler to do
    it. And `getWidgetMessages()` registers `RWPM_ANSWER`, the *same string*
    `RoomWidgetPollMessage.ANSWER` uses, so both handlers are called for it and each is
    disambiguated only by its own `instanceof` failing. `PollWidgetHandler` already carried a note
    predicting exactly this; probe-confirmed that a poll answer does **not** reach
    `sendScriptProceed()`.
  - ⚠️ **`FBE_BAR_RESIZE_EVENT` was ported but never wired** — a third instance of the dominant
    failure mode. `HabboFriendBarView` emits it, `RoomUI.bottomBarResizeHandler()` handles it, and
    nothing connected them: the handler was only ever reached through the explicit
    `triggerbottomBarResize()`. AS3 attaches the listener on the `IIDHabboFriendBarView` dependency
    (RoomUI.as:532-538), which is the declaration this slice had to add anyway.
  - **`RoomUI.get toolbar()` was missing too**, though AS3 declares it at RoomUI.as:1270 — the
    field existed and was used internally, with no accessor.
  - **The icon enum needed a guarded lookup.** AS3 reads the constant straight off the class
    (`UiHelpBubbleIconEnum[token]`); doing that in JS also resolves `name`, `length` and
    `prototype`, so a script naming an element "name" would be handed the string
    "UiHelpBubbleIconEnum". Probe-confirmed: the naive form gives exactly that, the explicit table
    gives null and the raw token is used instead — which is the AS3 fallback.
  - **Probe-verified** (browser, real classes resolved through the modules' own specifiers):
    link-tracker registration and pattern; the sibling widgets pulled off the desktop; pair
    parsing incl. a too-short link and an odd trailing argument; the lookup order; and every
    branch of `checkElementPosition()` — balloon above mid-screen (520,265 / arrow "down"), flipped
    below near the top (520,110 / arrow "up", offset 20÷3), pinned left (x=10, offset −70), pinned
    right (x=900, offset 50), and the chat-input path (390,520) which is the only one where the
    initial −1 pointer offset survives. Every number re-derived by hand from the AS3 arithmetic.

- 🔄 **Priority 1, slice 15: `RWE_EXTERNAL_IMAGE` — the photo / selfie wall item**, 2026-08-07.
  `ExternalImageWidgetHandler` (162 l. AS3) + `ExternalImageWidget` (758 l. AS3), plus
  `IRoomEngine.getObjectsByCategory()` and `RoomUI.get habboHelp()`, which the port lacked.
  `handler/` goes **39/47 → 40/47**; both measurements re-run and agreeing at 40.
  - **The only widget opened from two unrelated sources.** `RETWE_OPEN_WIDGET` for a photo hanging
    in the room, and `ROSM_USE_PRODUCT_FROM_INVENTORY` for one still in the inventory — the second
    reads its JSON off the item's stuff data because there is no room object to read a model from.
    The AS3 `processEvent()` does **not** return after the inventory branch; it falls through to
    `as RoomEngineToWidgetEvent`, which is null for a use-product event (the two are siblings under
    `RoomEngineObjectEvent`), so the method exits there. Kept as written.
  - **It is a browser, not a viewer.** Next/previous walk every wall item of the *exact same type*
    in the room, wrapping both ways — so a small poster never browses into large ones and a selfie
    never browses into posters. That is what `getObjectsByCategory()` was needed for.
  - **The content is not in the furni.** `readFurniJson()` finds either an inline photo blob or an
    `id`; an id means a second, unauthenticated fetch to the extra-data service, whose **403** is
    the signal that the photo was moderated away — and the notice is shown only to someone who
    could have removed it anyway. An empty 200 body is "nothing to show", not an error.
  - 🐛 **Two dead members kept on purpose, both AS3's.** `reportWindow` is destroyed in `dispose()`
    and read by `onReportWindowEvent()` but **never assigned** — the in-widget report dialog was
    replaced by `IHabboHelp.startPhotoReportingInNewCfhFlow()` and only its construction was
    removed. `_SafeStr_11415` is declared with the image loader's type and never touched at all.
    Both ported with the reason at the declaration rather than dropped silently.
  - **Two Flash primitives adapted, marked `TS-only`.** The 1px black outline is AS3 drawing the
    photo four times through a black `ColorTransform` at (0,1),(1,0),(1,2),(2,1) then once at
    (1,1); this port composes the same five draws on an `OffscreenCanvas` with `source-in`.
    Verified in a browser probe: a 4×4 red photo yields a 6×6 bitmap, black opaque on the top and
    left edges, red at the centre, **transparent at the corner** — a plus, not a square, exactly as
    the four offsets specify. `stage.stageWidth/Height` becomes the desktop window's size.
  - ⚠️ **One member is a visible TODO, not a silent omission.** AS3 drops a bare Flash `TextField`
    into `name_copy_wrapper` for room controller level 5 only, so staff can select and copy the
    photographer's name. This port has no `TextField`; a holder object keeps the text assignments
    faithful and is handed to `setDisplayObject()` at the same point, but nothing renders it. The
    DOM input bridge is the closest existing mechanism. Non-staff are unaffected.
  - **Probe-verified** (browser, fake window tree, real classes resolved through the modules'
    own specifiers so `instanceof` compares the same class objects): all 23 layout child names
    resolve; `getType()` over the four furni types; `getJsonValue()` short-key-wins-with-empty-
    falls-back; `getImageUrl()` absolute vs bare and selfie-path vs poster; `readFurniJson()`
    routing incl. the malformed blob being swallowed; wraparound in both directions; the three
    `processEvent()` destinations; `deleteCard()`; and the outline pixels above.

- 🔄 **Priority 1, slice 14: the badge-display / achievement-resolution trophy**, 2026-08-07.
  `FurnitureBadgeDisplayWidgetHandler` (331 l.) + `AchievementResolutionTrophyFurniWidget` (150) +
  `RoomWidgetAchievementResolutionTrophyDataUpdateEvent` (94), plus the support layer they needed:
  `BadgeRarityEnum` (134), `BadgeLeaderboardUtils` (81) and the badge-info wire pair.
  - **One widget, two callers, one asynchronous.** The achievement resolution has everything in
    the furni's own stuff data and draws immediately; the badge display needs the badge's
    **rarity**, which only the server knows, so it parks five `_pending*` fields, fires a request
    and finishes when the answer arrives. A resolution arriving mid-flight clears the pending
    request, and the stale answer is then dropped by the badge-code re-check.
  - **The colour work is the point.** A standalone tier's background is its display colour
    lightened (more for rarer tiers, so it stays pale), and its header is the *silver* trophy
    header **multiplied** by that same tint — which keeps the header's shading while taking the
    tier's hue. Unique (6) is the exception: it gets the plain gold header and a white background,
    so the rarest badge is the one that is *not* tinted.
  - **Two new headers, both from WIN63's registry alone.** `GetBadgeInformationComposer` 3159
    (`_composers[3159]`) and `BadgeInformationEvent` 1153 (`_SafeStr_4546[1153]`). The emulator
    implements neither; its only 3159 is an unrelated *server→client* composer, which is a
    different table and so not a conflict. Both class names are **derived** — the identifiers
    exist in no tree, and unlike their `badges/` siblings there is no `win63_version` counterpart
    to recover them from.
  - **`BadgeRarityEnum`'s constants and keys disagree**, and that is AS3's: `VERY_RARE` (3) maps to
    `badge.rarity.epic`. Three of the seven constant names are derived from their own keys.
  - **The event's `color` is `theme + 1`** because the widget derives the theme back out as
    `color - 1` when no explicit theme is given — that is how one event serves both callers, with
    the achievement path sending only the first four fields and getting a plain gold trophy.
  - **One AS3 dead read kept visible:** the widget assigns `viewType` and reads it into a local
    that is never used — a second view type that was never built. Kept as a `void` read rather
    than dropped, so the intent stays legible.
  - **Verified in a browser**: the rarity enum's labels, standalone tests and glow colours behave
    across the uncommon flag; the leaderboard helpers page/format/cap correctly; the parser reads
    owner count **before** rarity; the resolution path sends nothing and draws gold/white; the
    badge-display path draws nothing and sends `["ACH_Test1"]`; an answer for a different badge is
    dropped; very-rare yields silver theme with a tinted header (4286225356) and lightened
    background (4288402175); unique yields gold, gold header and white; common yields the plain
    silver header, white, and **no** owner line at 0; an owner count of 5000 is hidden too; a
    resolution mid-flight makes the late answer inert; the failure case raises the alert with
    `help_error_state`; and the widget applies all four defaults when the extension fields are
    absent, takes them verbatim when present, and disposes the previous view first.
  - 📏 **`handler/` 26/47 → 27/47** — 6 widget types left in priority 1.

- 🔄 **Priority 1, slice 13: the furni chooser**, 2026-08-07.
  `FurniChooserWidgetHandler` (211 l.) + `FurniChooserWidget` (160) + `FurniView` (289) +
  `FurniChooserTableObject` (68). The `chooser/` package from slice 12 carried the rest, so this
  needed only its own four files — **`chooser/` is now 8/8**.
  - 🐛 **The search filter was decompiler-damaged, and was repaired against its intact
    sibling.** `FurniView.populateWithFilters()` comes back as
    `do { for each(token in tokens) {} ... break; } while(item.lowerCaseName.indexOf(token) != -1)`
    — an empty `for each` body, an unconditional `break`, and the real test stranded in the
    `while`. This is the fifth time this project has hit that exact shape. `UsersView` is the
    undamaged sibling and shows the intended structure; rebuilt as an **every-token** match, which
    is the only thing the `split(" ")` and the stranded `indexOf` can together mean.
  - **It maintains its list incrementally, unlike the user chooser.** A removal is spliced out
    locally (matched on id **and** category, since a floor and a wall item can share an id); an
    addition asks the handler for that *one* item and waits for
    `RWCCE_FURNI_CHOOSER_CONTENT_ADD`. A room with hundreds of furni would make the user chooser's
    full-refresh approach unusable. The add is idempotent through an id set, and does **not**
    re-sort — a newly placed item sits at the bottom until the next full refresh.
  - **Name resolution is the handler's real work**: the model carries only a type id, so the name
    comes from the session's furniture data, with the object's type string as the fallback. Wall
    items branch first on posters, whose number is parsed out of the type string and turned into a
    `poster_<n>_name` key — and the wall branch requires a *non-empty* localized name where the
    floor branch takes whatever the data gives.
  - **Items with a non-positive id are dropped** by the widget, which is how the engine's
    placeholders for furni still loading stay out of the list.
  - **`_ignoreListeners` is real here**, unlike in `UsersView` where it is never set:
    `constructOwners()` raises it while repopulating the owner dropdown so the resulting
    `WE_SELECTED` cannot re-filter mid-rebuild. The dropdown is also disabled and half-faded when
    there are fewer than two owners to choose between.
  - **Verified in a browser**: the handler lists both categories in order with names resolved four
    different ways (furniture data, type fallback, poster localisation, wall data); a top-up for a
    missing object emits nothing; selecting acts on categories 10 and 20 but not 100; the owner
    cell shows "-" for a null owner and for a Builders Club id while the identifier is
    `category-id`; the window opens at (690, 10) with the owner menu built from the items;
    "red sofa" matches one item and "red chair" matches none — **every** token must hit; clearing
    restores all three; the owner dropdown narrows to Alice's two; an incremental add appends
    without re-sorting and a duplicate add redraws nothing; a removal splices locally without
    asking the room, and a removal with the wrong category does nothing; an addition asks
    `RWRWM_FURNI_CHOOSER_ADD` for that one id; and everything is inert once the window is closed.
  - 📏 **`handler/` 25/47 → 26/47** — 7 widget types left in priority 1.

- 🔄 **Priority 1, slice 12: the user chooser**, 2026-08-07.
  `UserChooserWidgetHandler` (152 l.) + `UsersChooserWidget` (145) + `UsersView` (249) +
  `UsersChooserTableObject` (59) + the shared `ChooserItem` (80) / `ChooserWidgetBase` (33) /
  `RoomWidgetChooserContentEvent` (40). The `chooser/` package is the last shared backbone in
  `widget/`, so the furni chooser now needs only its own three files.
  - **It reads the room engine, not the session's user list** — the engine knows who is actually
    *rendered*, which is what a chooser is for. A room object with no user data yet is skipped, so
    an avatar still loading stays out.
  - **The table-view backbone it needs was already ported** for the wired menu, so this reuses
    `TableView`/`TableColumn`/`ITableObject` rather than adding anything.
  - **`ChooserItem.owner` is not a field.** Two id ranges override it, so a Builders Club or
    wired-temporary item reports its provenance instead of whoever placed it — and both labels are
    hardcoded English in AS3, not localisation keys.
  - **The refresh is delayed by 100ms on purpose.** A user arriving fires `RWROUE_USER_ADDED`
    before the room object reaches the user-data manager, so asking immediately would miss the very
    user who just arrived. It also only refreshes while the window is open.
  - **Two AS3 oddities kept:** `compareItems()` **never returns 0** — equal names, an empty name or
    a null item all return 1, making that sort unstable (the widget re-sorts properly afterwards,
    which is presumably why nobody noticed); and `_ignoreListeners` is checked by all three input
    handlers but never *set*, so the guard is permanently false.
  - **One contract difference recorded rather than papered over:** AS3's `getTableCell()` returns
    **null** for an unknown column, but this port's `ITableObject` declares it non-null and
    `TableRowView` passes the cell straight on. The default returns an empty cell instead; the
    branch is unreachable either way, since the table only asks for the columns it was initialised
    with.
  - **`activeRoomHasChooserDisabled`** added to `RoomEngine`/`IRoomEngine` — a room variable a room
    can set to opt out of being listed, and the sibling of the already-ported
    `activeRoomHasFreeFurniMovementsMode`.
  - **Verified in a browser**: `owner` returns "Builders Club"/"Temp (Wired)" for the two id ranges
    and the real owner otherwise; the handler lists three users out of four room objects, sorted,
    and reports `chooser_disabled`; selecting acts only on the user category and drops a foreign
    object; the window opens at (690, 10) against its parent's top-right with the amount indicator
    at 3; searching "o" narrows to Bob and Zoe **without asking the room again**, and toggles the
    clear button against the placeholder; clearing restores all three; the type dropdown filters to
    type 2, and selection 3 remaps to type 4 and yields nothing; a user arriving refreshes after
    100ms and not immediately, and not at all once the window is closed; closing resets `state` to
    0; `initialize(1)` asks for the list and `initialize(0)` does not.
  - 📏 **`handler/` 24/47 → 25/47** — 8 widget types left in priority 1.

- 🔄 **Priority 1, slice 11: the word quiz**, 2026-08-07. `WordQuizWidgetHandler` (147 l.) +
  `WordQuizWidget` (422) + `WordQuizView` (224) + `RoomWidgetWordQuizUpdateEvent`. Another case
  where the whole pipeline below it was already ported — three parsers, the session event and its
  handler — and only the widget layer was missing.
  - **The one handler that changes the room itself.** On an answer it makes the voter's avatar
    smile or frown (`updateRoomObjectUserGesture`, AS3's `updateObjectUserGesture`), which is what
    makes a quiz readable at a glance. It also **returns without dispatching** when the voter is
    not in the room — so their vote updates no counts either, not just no gesture.
  - **The signs are the bulk of it.** Each answer raises a thumb window keyed
    `<pollId>_<userId>_<layout>` that follows its avatar on a 40ms timer and fades in, holds, then
    fades out while drifting up — all from a single per-sign countdown, no tween. The user id is
    recovered by **splitting the window's own name** rather than being stored.
  - **Four AS3 oddities kept rather than smoothed:** the placement offsets differ between the
    first frame (+20, -20) and every later one (+29, -11); `poolWindow()` does not pool anything
    (the two pools it implies are only ever filled by `dispose()`, so never read); losing an
    avatar `return`s out of the whole per-sign loop instead of continuing; and `removeWindow()`
    **adds** the desktop resize listener where it meant to remove it, and bails when the window has
    no children.
  - **`_countdown = 4` is assigned then immediately overwritten** whenever a duration is given, so
    it only survives for a question with no duration — where nothing decrements it either. Kept.
  - **Two deliberate deviations:** AS3 *throws* "Failed to construct window from XML!" in both
    `createWindow()` and the measuring path. Both are reached from a server event, and a throw
    there takes the room UI down.
  - **Verified in a browser**: a like gestures `7:42:1` and a dislike `7:42:4`; an absent voter
    dispatches nothing at all; the question strip shows the topic, caps its width at
    `min(660, measured + 6)`, starts the countdown at `duration / 1000` and centres on its *first
    child's* width (x=200, y=6); voting sends `RWPM_ANSWER` with the poll id, question id and
    `["1"]`, then swaps to the result strip; a second vote sends nothing but still dismisses; the
    sign is named `3_555_wordquiz_like_xml`, starts at (120, 180) fully transparent, and after two
    40ms ticks sits at (129, 189) with blend 0.375 — two `FADE_IN_STEP`s; a dislike takes the
    other layout; finishing removes both signs and fills the tallies 5/2; a finish for a different
    question shows no results.
  - 📏 **`handler/` 23/47 → 24/47** — 9 widget types left in priority 1.

- 🔄 **Priority 1, slice 10: the high-score scoreboard**, 2026-08-07.
  `HighScoreDisplayWidgetHandler` (148 l., name **derived** from `_SafeCls_3964`) +
  `HighScoreDisplayWidget` (248).
  - **The first handler that repositions its widget every frame.** It registers itself as an
    update listener from its own `container` **setter** — nothing else does that — because the
    bubble is placed in room coordinates and has to follow the furni as the room scrolls. The
    offset is a flat `(-138, -400)` from the object's screen location.
  - **It needs no server round-trip**: the open case rebuilds the scoreboard from the object's own
    model through `HighScoreStuffData.initializeFromRoomObjectModel()`, which was already ported.
  - **The bubble lives in a full-desktop container of its own**, not in the layout manager — so
    `mainWindow` returns something here, unlike most of the recent slices.
  - **A quiet AS3 detail worth keeping:** `_openEvent` is never cleared on close. What stops a
    stale one driving anything is that `close()` resets the widget's room/object ids to -1, and
    `update()` requires all four of event, open bubble, matching room and matching object.
  - **Five score-type postfixes against three declared constants.** `fastesttime` and
    `longesttime` (indices 3-4) have no `SCORETYPE_*` constant in any tree, which is why the time
    formatting is selected by `indexOf("time") >= 0` on the postfix string rather than by a
    comparison. Ported as-is.
  - **`ROWRE_HIGH_SCORE_DISPLAY` / `ROWRE_HIDE_HIGH_SCORE_DISPLAY`** added to
    `RoomEngine.handleObjectWidgetRequestEvent()`.
  - **Verified in a browser**: the container is created as type 4 and sized to the desktop
    (1024x768); the template row is pulled out of the list before any entry is added; two entries
    render with `"Alice, Bob"` and `01:35` / `1:02:05` — AS3's asymmetric padding, where only the
    first two fields zero-pad, so the hours field is bare; a non-time score type uses the plain
    header and the raw number; an unconfigured furni (both types -1) registers **no** caption
    parameters; the bubble lands at (362, 200) from a screen location of (500, 600); a hide for a
    different object is ignored while the right one closes it and resets both ids to -1; the
    reopened bubble keeps the last position; `update()` does nothing once closed despite the stale
    open event; and `dispose()` leaves the update loop it joined.
  - 📏 **`handler/` 22/47 → 23/47** — 10 widget types left in priority 1.

- 🔄 **Priority 1, slice 9: the in-room friend request**, 2026-08-07.
  `FriendRequestWidgetHandler` (134 l.) + `FriendRequestWidget` (199) + `FriendRequestDialog`
  (297), the message and the event.
  - 🐛 **A third bus was missing.** This handler's three processed events come from **two**
    places: the request is a room-session event, but `FRE_ACCEPTED`/`FRE_DECLINED` belong to the
    friend-list component's own bus, so `RoomUI`'s session-event table cannot carry them. AS3
    subscribes to them in `RoomDesktop`'s `friendList` **setter** (lines 430-437) and unsubscribes
    in `dispose()` (633-638) — this port's setter only assigned the field. Ported, so accepting
    from the friend-list window now closes the in-room bubble too.
  - **The first consumer of slice 8.** `FriendRequestWidget.update()` is the one caller of
    `ObjectLocationRequestHandler`'s synchronous reply: it asks where each sender is and hands the
    rectangle to that sender's bubble. A **null** rectangle is how a request from someone who left
    the room gets dropped.
  - **The bubble is built by the first position update, not by the constructor** — so a request
    from someone who is not in the room never draws anything at all.
  - **The only animation in the slice is four lines.** The first placement jumps; after that a
    move of more than 5px eases halfway per frame, which is what makes the bubble trail a walking
    avatar. Hovering or holding a button freezes it, so it cannot walk out from under a click.
  - **The widget joins the update loop only while it holds bubbles** — `checkUpdateNeed()` runs
    after every add and remove, so an empty room costs nothing per frame.
  - **Three AS3 details kept rather than smoothed:** `type` returns `"RWE_ROOM_POLL"`, which is
    not this widget's type — a copy-paste it shares with `PollWidgetHandler` and
    `UiHelpBubbleWidgetHandler`, and harmless because `type` is only matched against an
    open/close-widget target; closing the bubble is **not** declining (it tells the server
    nothing and the request stays pending); and `showProfile()` alone skips the message-listener
    null check its two siblings have.
  - **Verified in a browser**: the session request dispatches SHOW with name and user id while
    both friend-list events dispatch HIDE with only the request id; the two buttons reach
    `acceptFriendRequest`/`declineFriendRequest` and a foreign object is dropped by the cast;
    adding a request joins the update loop at priority 10 and builds nothing; the first update
    asks `RWGOI_MESSAGE_GET_OBJECT_LOCATION` for user type 1, builds on layer 0 and places the
    window at (182, 270) — a jump; the next update to a target 200px away lands at 282 — halfway;
    hovering freezes it there; accepting sends, disposes and leaves the update loop; an absent
    sender is dropped without a window; a remote hide disposes without sending; close sends
    nothing; both profile links raise `RWOPEM_OPEN_USER_PROFILE`, the name underlines on hover and
    the icon swaps 21/22.
  - 📏 **`handler/` 21/47 → 22/47** — 11 widget types left in priority 1.

- 🔄 **Priority 1, slice 8: the object-location request**, 2026-08-07.
  `ObjectLocationRequestHandler` (128 l.) + `RoomWidgetGetObjectLocationMessage` +
  `RoomWidgetUserLocationUpdateEvent`, wired as `RWE_LOCATION_WIDGET`.
  - **The only handler that answers synchronously.** Every other one dispatches on the bus and
    returns null; this one's return value *is* the answer, which is why the event it builds is
    never emitted anywhere. It is also the only handler whose `type` is **null** in AS3, so it can
    never be the target of an open/close-widget event — kept null here.
  - **Another widget-less type**, like the internal link: AS3's factory has no case for it. It
    only works because `createWidget()` now leaves a handler registered when the factory returns
    null (fixed in slice 3-5).
  - **Two branches, one tail.** A user is looked up by `(webId, type)` through the user-data
    manager to get its room index; a game object is addressed by room index directly. Both then
    read the bounding rectangle and the projected screen location and offset them by the room
    view's origin. A user who is not in the room still gets an event, with both fields null — the
    caller tells that apart by the rectangle, not by a null event.
  - ⚠️ **`AvatarInfoWidget` is *not* routed through it yet, deliberately.** It computes the same
    thing inline. AS3 takes `screenLocation` from `getRoomObjectScreenLocation()` — the object's
    own location projected, an avatar's feet — where the inline block uses
    `(left + width/2, bottom)` off the bounding box. Those are near but not equal, so swapping the
    source would move every bubble in a working, recently-shipped feature. `TODO(AS3)` on
    `AvatarInfoWidget.positionView()` with that exact reason; it needs its own before/after check.
  - **Verified in a browser**: `type` is null and the two message types are claimed; a user
    lookup calls `getUserDataByType(555, 1)` then locates room index 42 on canvas 3 with category
    100, and both results come back shifted by the view origin; a game object skips the lookup; an
    absent user yields an event with two nulls while a missing user-data manager yields no event
    at all; a missing view rect returns the unshifted values rather than nothing; a foreign
    message object, an unknown type and a null container are all dropped.
  - 📏 **`handler/` 20/47 → 21/47** — 12 widget types left in priority 1.

- 🔄 **Priority 1, slice 7: polls — and the routing bug that made the last two slices dead**,
  2026-08-07. `PollWidgetHandler` (124 l.) + `PollWidget`/`PollSession`/`PollOfferDialog`/
  `PollContentDialog` (1022 l. of AS3) + `IPollDialog`, the event and the message.
  - 🐛 **Nothing forwarded room-session events to the room desktop.** AS3's `RoomUI` listener
    table routes 20 session event types to `roomSessionEventHandler()`, which hands them to the
    current desktop and through it to every widget handler that claimed them in
    `getProcessedEvents()`. This port had the *dialog* half of that table and not this one — so a
    handler could be built, registered in `_widgetEventHandlers` and **never called**. The
    doorbell and the room queue, both shipped earlier today, were in exactly that state; the poll
    slice needed the same route and is what exposed it. `roomSessionEventHandler()` and its
    20-entry list are ported, so all three work now.
  - 🐛 **The poll parser produced objects the dialog could not read.** `PollContentsEventParser`
    emitted anonymous `{value, text, type}` choices where every consumer reads
    `choiceText`/`choiceType`, and dropped `children` onto a spread copy. Nothing throws on that —
    it renders an empty answer list. The two AS3 DTOs are now ported (`PollQuestion`, `PollChoice`,
    both **derived** names: `_SafeCls_4294`/`_SafeCls_4101`, obfuscated in every tree) and the
    parser builds them.
  - **The NPS branch is the interesting logic.** A poll question can carry follow-ups; after a
    radio answer the dialog keeps the chosen choice's `choiceType` and serves the first child
    whose `questionCategory` matches, instead of advancing. Clearing the parent index is what
    stops one parent yielding twice, and clearing the choice type before each resolve is what
    makes a non-radio answer end the branch.
  - **`ITextWindow` was missing `htmlText`** — declared on AS3's interface (lines 37/107) and
    implemented by this port's `TextController` all along, just never exposed, so no caster could
    reach it. `PollOfferDialog` is the first ported caller that needs it; added.
  - **Four AS3 details kept rather than smoothed:** every offer button is one-shot, so a double
    click cannot both start and reject; "Later" is the only exit that tells the server nothing;
    two dead guards in `answerPollQuestion()` (`length < 0`, and more answers than choices) are
    kept because removing them would be an invented correction; and a duplicate offer logs and
    **re-offers on the existing session** rather than replacing it.
  - **Two deliberate deviations**, both the same reasoning as the doorbell's: AS3 *throws* when a
    poll answer layout is missing and when the window is gone during a text read. Both are click
    handlers, and a throw there takes the room UI down — they warn and render nothing instead.
  - **Verified in a browser**: the parser builds real `PollQuestion`/`PollChoice` with children and
    NPS flag; all three session events dispatch with the right fields and an unknown one does not;
    the three messages reach `sendPollStart/Reject/Answer` and a foreign object is dropped by the
    cast (the `RWPM_ANSWER` collision with `UiHelpBubbleWidgetHandler`); the offer sets both HTML
    texts and grows the window; a duplicate offer rebuilds on the same session; an error is a
    plain alert with no session; content for an unknown id is dropped; answering the radio
    question follows the matching **child** question, then answering that one sends
    `[4,200,['because']]`, shows the thanks alert and removes the session; the cancel confirmation
    builds once however many times cancel is clicked, and confirming abandons the poll.
  - 📏 **`handler/` 19/47 → 20/47** — 13 widget types left in priority 1.

- 🔄 **Priority 1, slice 6: the area-hide furni**, 2026-08-07. `FurnitureAreaHideWidgetHandler`
  (139 l.) + `AreaHideFurniWidget` (358 l.), the first slice in this run with a real window.
  - **The rectangle is picked on the room floor, not in the window.** The widget drives
    `IRoomAreaSelectionManager` — already implemented here for the wired InArea selector — and the
    corners come back through a callback. That callback counts as a settings change, so with
    `AUTO_SAVE` on (it is, in the shipped client) the pick saves itself and the Apply button is
    hidden dead weight.
  - **This handler does not use the widget event bus at all.** The widget hands *itself* to the
    handler in its own constructor, and the handler calls `open()`/`updateStatus()` directly;
    `getWidgetMessages()` returns null and the window sends its own two composers. It also has a
    `container` **getter** as well as a setter, which the widget reads to reach the connection —
    the only handler that does.
  - **Ported: `SetAreaHideDataMessageComposer`, header 1954** from WIN63's registry
    (`_composers[1954] = _SafeCls_3376`), corroborated by the emulator as `SetAreaHideDataEvent`,
    which is where the class name comes from — the AS3 identifier is obfuscated everywhere. The
    on/off toggle needed nothing new: it is a plain `UseFurnitureMessageComposer` (3353).
  - **Three AS3 details kept rather than smoothed:** `selectButton`'s getter calls
    `_window.deactivate()` — on that one getter of seven — so the room, not the window, holds
    focus for the floor pick, and it fires on every `refreshUI()`; the close button **hides**
    rather than destroys, so re-opening skips the rebuild; and the layout misspells the checkbox
    `invisiblity_checkbox`, which the lookup has to match.
  - **`ROWRE_HIDE_AREA` → `RETWE_REQUEST_AREA_HIDE`** added to
    `RoomEngine.handleObjectWidgetRequestEvent()`. The two names genuinely differ across the
    boundary — room-object side says HIDE_AREA, widget side says AREA_HIDE.
  - **Verified in a browser**: all three rights paths open it and no-rights does not; the model's
    four numbers and three `== 1` flags land on the right controls; opening activates the picker
    and highlights the saved rectangle; switching on dims all nine labels and both checkbox sets
    to 0.5 and releases the picker; a repeated state and a state for another object are both
    dropped; a floor pick sends `[55,11,12,13,14,true,false,true]`; the four buttons reach
    `startSelecting`, `clearHighlight`, `UseFurniture[55,0]` and a dead Apply; a non-click event
    does nothing; close hides without disposing and a status arriving afterwards is dropped; a
    checkbox change auto-saves; double dispose is safe.
  - 📏 **`handler/` 18/47 → 19/47** — 14 widget types left in priority 1.

- 🔄 **Priority 1, slices 3-5: the three handler-shaped widget types**, 2026-08-07. Internal link,
  loading bar and conversion tracking — the ones where the "widget" is barely a widget.
  - **`InternalLinkWidgetHandler`** (AS3 `_SafeCls_3630`, 102 l., name **derived** from the
    `RWE_INTERNAL_LINK` type it declares — the identifier is in no tree). It has **no widget at
    all**: `RoomWidgetFactory` has no case for it in AS3 either. The link is looked for in two
    places in order — the `internalLink` entry of the object's `furniture_data` string map, then
    the flat `furniture_internal_link` model string — because the editable furni writes the map
    and the fixed one writes the string. `disposed` is `_container === null`; this is the one
    handler with no `_disposed` flag.
  - 🐛 **A widget-less type was impossible before this.** `RoomDesktop.createWidget()` called
    `handler.dispose()` when the factory returned null. AS3 (line 985-988) just returns, leaving
    the handler registered in both tables — which is the entire mechanism behind
    `RWE_INTERNAL_LINK`. The port's version was worse than either option: it nulled the container
    while leaving the handler subscribed, so it kept being called and kept doing nothing. Now
    matches AS3.
  - **`LoadingBarWidgetHandler` + `LoadingBarWidget`** (87 + 163 l.) and
    `RoomWidgetLoadingBarUpdateEvent`. The handler is the only one that re-dispatches its input
    unchanged — the desktop already builds the event, so there is nothing to translate. Hiding
    **destroys** the window; the next show rebuilds it. `mainWindow` stays null: the bar centres
    itself and must not be placed by the layout manager.
  - ⚠️ **The loading bar has no trigger yet, and that is recorded rather than guessed.** AS3 drives
    it from a pending-resource list that the decompilation lost — `init()` reads
    `_pendingResources = []` immediately followed by `if(_pendingResources.length > 0)`, so the
    SHOW branch is dead in the source with no intact sibling to recover the list from. The other
    half, `checkInterrupts()`, also owns `roomSessionManager.startSession()`, which this port
    drives elsewhere; wiring it would start the session twice. Full `TODO(AS3)` on
    `RoomDesktop.init()`.
  - **`ConversionPointWidgetHandler` + `RoomWidgetConversionPointMessage`** (84 l.). Its widget is
    a bare `RoomWidgetBase` with no window — AS3's factory returns exactly that, so the handler is
    the feature. **Nothing in any of the three trees constructs the message**; the handler is its
    only reference, and it is ported because the handler is real and registered.
  - 🐛 **`RoomSession.sendConversionPoint()`'s parameters were named for the wrong fields** —
    `(type, value, extra, category, action)` for what the composer and this handler both call
    `(category, pointType, action, extraString, extraInt)`. Same five slots in the same order, so
    the wire never changed; only the names lied. Renamed on `RoomSession` and `IRoomSession`.
  - **`ROWRE_INTERNAL_LINK` → `RETWE_REQUEST_INTERNAL_LINK`** added to
    `RoomEngine.handleObjectWidgetRequestEvent()`, the one line that was missing between the
    furniture logic and the handler.
  - **Verified in a browser**: the map wins over the flat string, an empty map entry falls back to
    it, a missing map does too, and an empty result follows nothing; `getRoomObject` is called with
    (roomId, objectId, category) in that order; a null object and a foreign event type are both
    ignored; `disposed` tracks the container in all three states. The loading bar passes SHOW and
    HIDE through and drops anything else, builds once and centres twice across two shows, disposes
    on hide and builds a **different** window on the next show, and unregisters cleanly. The
    conversion message forwards all five fields in order, coerces both optional tails to `''`/`0`,
    and rejects a foreign type and a foreign object.
  - 📏 **`handler/` 15/47 → 18/47** — 15 widget types left in priority 1.

- 🔄 **Priority 1, slice 2: the room queue — "you are Nth in line"**, 2026-08-07.
  - **Ported end to end**: `RoomQueueWidgetHandler` (163 l. AS3), `RoomQueueWidget` (211 l.),
    `RoomWidgetRoomQueueUpdateEvent` and `RoomWidgetRoomQueueMessage`, wired at the factory,
    `RoomDesktop`'s handler switch and `RoomUI`. `RoomSessionQueueEvent` and its parser already
    existed and already fired — again only the widget layer was missing.
  - 🐛 **Correction to the above, found while porting the loading bar** (same day): the queue was
    first wired into `RoomUI`'s `REE_INITIALIZED` block alongside every other widget. AS3 creates
    it — and the loading bar — at line 1124-1125 inside `createRoomDesktop()`, **before** `init()`.
    That is not cosmetic: a room you are queued for never initializes, so a queue window built at
    room entry would never exist when it is needed. Moved.
  - **The position is a size, plus one.** The server reports how many people are *already* waiting;
    the handler adds one, because that is where you land. Which queue's size counts depends on club
    membership: with more than one queue open, a club member is counted in the `"c"` queue and
    everyone else in `"d"` (`RoomSessionQueueEvent.QUEUE_TYPE_NORMAL`). That choice also sets
    `isClubQueue`, which is what switches the caption to its `.hc` variant.
  - **Three constants had to be derived and say so at the declaration** — AS3 writes `"c"`, `1` and
    `2` inline. `QUEUE_TYPE_CLUB`, `TARGET_SPECTATOR` and `TARGET_VISITOR`; the same two numbers go
    straight back out through `changeQueue()`.
  - **Two AS3 details kept rather than smoothed:** the club half of the window is shown to people
    who do **not** have Habbo Club (it is the upsell, so the flag is inverted); and leaving or
    switching queues **destroys** the window instead of hiding it — the next status rebuilds it,
    which is what keeps the widget stateless between visits. A `queueSetTarget` outside 1–2 emits
    nothing at all, and no inventory means `hasHabboClub` defaults to *true*, hiding the upsell
    rather than showing it to everyone.
  - `HabboInventory` gains the `clubDays`/`clubPeriods` purse forwarders AS3 has, which is what the
    handler reads.
  - **Verified in a browser**: position is `size + 1` for a single queue (4→5); with two queues a
    non-member is counted in `d` (7→8) and a member in `c` (1→2, `isClubQueue` set); no inventory
    yields `hasHabboClub: true`; an unknown target emits nothing; a foreign message type is
    ignored; the four buttons reach `quit()`, `changeQueue(1)`, `changeQueue(2)` and
    `openClubCenter()`; the caption picks all four localisation variants correctly; the change
    button switches to the queue you are *not* in and disposes the window.
  - 📏 **`handler/` 13/47 → 15/47** — 18 widget types left in priority 1.

- 🔄 **Priority 1 opened: the doorbell, first of the 20 missing widget types**, 2026-08-07.
  - 📏 **The gap was re-measured by widget *type*, not by filename**, because the port renames the
    obfuscated handlers (`_SafeCls_3971` is `RentableSpaceWidgetHandler` here). By type: **20 of
    AS3's 47 handler types have no port handler**, across 22 files — and **not one of those 20 has
    a ported widget either**. Every one is a full slice (handler + widget + wiring), not a
    connection job. That is the honest size of priority 1.
  - **Ported: the doorbell, end to end** — `DoorbellWidgetHandler` (109 l.), `DoorbellWidget`
    (146), `DoorbellView` (158), `RoomWidgetDoorbellEvent` and `RoomWidgetLetUserInMessage`, wired
    at all three points AS3 wires them (factory, desktop handler, and `RoomUI`'s room-entry
    creation, in AS3's own order at line 944). Both layouts already shipped, and the session
    already dispatched `RSDE_DOORBELL` — only the widget layer was missing.
  - **Two AS3 details kept rather than smoothed:** `unregisterUpdateEvents()` does *not* call its
    super where `registerUpdateEvents()` does; and the 51st caller at a full door is **denied**,
    not ignored — the cap is enforced by answering, which is why `deny()` is what enforces it.
  - **One deliberate deviation:** AS3 *throws* when a doorbell layout fails to build. Here it warns
    and returns null — a missing layout is a build problem, and throwing would take the whole room
    UI down with it.
  - **Verified in a browser**: all three session events map to their widget events and an unknown
    one is ignored; the widget message reaches `roomSession.letUserIn` with the right flag and
    returns null; a repeated ring does not duplicate the row; accepting sends `Alice:true` and
    drops it; a doorbell answered **elsewhere** clears the row *without* sending anything; the
    50-cap holds and the 51st is denied (`overflow:false`); `denyAll()` sends 50 denials and
    empties the list; and unregistering stops the widget hearing the bus.

- ✅ **Priority-0 sweep finished — and it found 38 messages the dispatcher could never route**,
  2026-08-06. `habbo/moderation`, `habbo/friendlist` and `habbo/help` swept; **priority 0 is now
  closed**.
  - 🐛 **The whole moderation and help incoming sets were in no header table at all.** 14
    moderation events and 24 help/guide/CFH events were ported, parsed and — for 34 of them —
    *subscribed*, but `HabboMessages` had no entry for their ids, so the dispatcher had nothing to
    route the packet to. The mod tools and the entire guide/call-for-help system received nothing,
    silently. All 38 registered.
  - **How the ids were derived, since none of them could be guessed:** `win63_version` registers
    the same handlers with **unobfuscated class names**, WIN63 registers them obfuscated and owns
    the only authoritative header table. A handler spelled identically in both trees pins one to
    the other. Three could not be resolved that way and are **reported, not guessed**:
    `SanctionStatusMessageEvent` and `GuideSessionInviteRequesterMessageEvent` (no win63_version
    registration under that handler) and `MyCfhReportStatusMessageEvent` (not ported).
  - ⚠️ **The name trap, caught in the act.** WIN63 registers `_SafeCls_1807` under a handler called
    `onMySanctionStatusMessageEvent`, which reads like the port's `SanctionStatusMessageEvent` —
    but its parser exposes a single `sanctions()` *list* where the port's exposes ten
    single-sanction fields. Different message. Header 1746 was **not** assigned to it.
  - **Two moderation subscriptions were missing too**, both now wired: `onCfhTopics` (AS3
    subscribes that one message from *both* help and moderation — help builds the report dialog's
    categories, moderation stores them on the issue manager) and `onSanctions`, whose message
    (`CfhSanctionMessageEvent` + parser, header 1634) had to be ported. `ModerationManager` gains
    the `cfhTopics` setter AS3 has.
  - **A duplicate was avoided by checking first:** `UserClassificationMessageEvent` already
    existed under `incoming/moderation/` and was already subscribed — the earlier sweep missed it
    because the port names the file after the message and AS3 names the handler
    `onRoomUserClassification`. It only needed its header (543). A near-duplicate written in the
    meantime was deleted.
  - 📏 **Final counts:** `moderation` 15/17 registrations (the two missing ones are now wired, so
    17/17) but **8/43 files**; `friendlist` **12/12 and 41/40 files — nothing missing at all**;
    `help` 22/41 registrations, 27/34 files. **No orphan classes in any of the three** — nothing
    defined-but-never-constructed. The remaining gaps are unported features, not wiring.
  - **Verified in a browser**: all 38 ids resolve to the expected class, every one constructs, and
    every one carries its parser. The event table went from 369 to 407 entries with no duplicate
    id.
  - **Then the same filter was run over *every* incoming directory, not just these three.** It
    found 101 ported-but-unregistered classes, of which only **five were also subscribed** — the
    ones that actually cost something. Three are now registered: `IsUserPartOfCompetition` (1148),
    `SecondsUntil` (3620) and `IsBadgeRequestFulfilled` (2121), each resolved through the *landing
    view element* that registers it, because two of the three answer a handler called `onInfo` and
    the handler name alone could not tell them apart. **win63_version lists those three at
    1685/1557/2295 — all stale.**
  - 🐛 **One of the remaining two was a duplicate of a message already ported.**
    `GuideSessionInviteRequesterMessageEvent` carries a `@see` pointing at
    `GuideSessionInvitedToGuideRoomMessageEvent.as` — the same AS3 message, already ported and now
    registered at 776 — and in AS3 `GuideSessionInviteRequester` is a **composer**, not an event.
    Both port handlers were trace-only stubs. The duplicate event, its parser and its subscription
    are removed.
  - ⚠️ **The last one is a revision mismatch, and is deliberately left unregistered.** The port's
    `SanctionStatusMessageEvent` came from win63_version, where the message is one flat sanction.
    The 2026 client's message in that position (`_SafeCls_1807`, header 1746) carries a
    `sanctions()` **list**. Registering the old reader against the new id would misparse the wire,
    so the subscription stays inert behind a `TODO(AS3)` that names the header for whoever ports
    the current shape. **The event table is 410 entries, no duplicate id.**

- ✅ **`habbo/sound` COMPLETE: `furni/`, the last file**, 2026-08-06.
  - **`FurniSamplePlaybackManager` (206 l.)**, constructed by the sound manager on the room
    engine's own event bus — the third and last thing AS3 builds in `init()`. Furniture that makes
    a sound now loads its sample once per object, retriggers it on play, follows its pitch, and
    drops it when the object goes.
  - **Its volume is wired too:** `updateVolumeSetting()` had two `TODO(AS3)`s where AS3 pushes the
    furni channel down to this manager — muting the client silenced music but not furniture.
    Both closed, and both managers now dispose with the sound manager as AS3 disposes them.
  - **The same `dispose()` slip as `TraxSample`**: AS3 never sets `_disposed` here either. Kept,
    and for a sharper reason — the flag *is* read, inside its own load callback, so setting it
    would change behaviour rather than tidy it.
  - **Verified in a browser** against a real decoded sample: the manager subscribes to all four
    room events; an object whose `sampleId` is -1 is ignored outright; a real one loads, registers
    as an update receiver and takes the pitch from its initialisation event; `updateVolume(0.25)`
    reaches the loaded sound; disposing the object unregisters *and* disposes it; a play event for
    a disposed object is harmless; and `dispose()` unsubscribes all four listeners.
    The pitch itself is not readable from outside — AS3 has only `setPitch()`, no getter — so what
    is verified there is that the call path runs and the sound is registered for the update tick
    that drives it.
  - **`habbo/sound` is now 29/29.** It leaves priority 3's list entirely.

- ✅ **`habbo/sound/trax` finished: the sequencer, and Trax songs actually play**, 2026-08-06.
  - **`TraxSequencer` (817 l.) + `TraxSampleManager` (245 l.) + the audio graph.** The last
    `TODO(AS3)` between this port and audible music is closed: `loadTraxSong()` builds a real
    sequencer, `HabboSoundManagerFlash10` constructs the sample manager, and `update()` drives its
    decode budget.
  - **How Flash's streaming maps onto Web Audio.** Flash *pulls*: `Sound.play()` raises
    `sampleData` whenever it wants another 8192 frames. Web Audio has no such event, so
    `TraxStreamNode` (TS-only) inverts it — an `AudioWorklet` drains a queue and posts `need` when
    it runs low, which is what the sequencer answers by mixing another block. Its `starved`
    message is exactly AS3's "Audio buffer under run..." log. The processor is built from a Blob
    URL so the engine stays independent of any one build setup.
  - **Everything above the block boundary is AS3's**, including the two layout modes (cut mode
    rounds a sample's bars to nearest, legacy rounds up past a 12.5% threshold — the same song is
    a different length either way), the last-to-first channel mixing where the highest channel
    *overwrites* and the rest add, the two-stage finish, and both fades.
  - 🐛 **One degenerate case worth knowing:** with no fade-out set, `checkSongFinishing()` still
    raises the fade flag on the last block, and the fade arithmetic then divides by zero — AS3
    would write NaN into its output stream, except the block is past the song's end so its frame
    count is zero and the write is skipped. The port keeps the same guard, and the probe confirms
    zero NaN samples across a whole rendered song.
  - **Verified in a browser, including the real audio graph**: two channels of a two-bar song mix
    to exactly `(16385 + 8193) / 32768 = 0.750061` with both output channels equal; a 4-bar song
    renders 22 blocks of 8192 frames and reports 3.99 s (bars are laid out at 88000 samples but
    measured at 88200 — AS3 declares both constants and uses each in its own place); the complete
    event fires once; `stop()` with a fade-out defers completion and then fires it; a 1-second
    fade-in ramps **0.186 → 0.279 → 0.372 → 0.464 → 0.5** and stops exactly at full level, a
    fade-out falls by the same step; and `play()` through the worklet starts, runs the context and
    advances the play head.
  - ⚠️ **A first probe reported the fade-in dead — the probe was wrong, not the code.** It drove
    `render()`, and AS3 raises the fade flags only in `play()` (line 383); `render()` loops the
    block writer directly. Checked against the source before changing anything.
  - **What is left in `habbo/sound`:** `furni/` (206 l., furniture samples) and, inside the sample
    manager, AS3's `loadNextSong()` queue drain — a failed sample download leaves its song waiting
    rather than moving on to the next queued one.

- 🔄 **`habbo/sound/trax`, part 1: the numeric core**, 2026-08-06. **No audio yet** — the
  sequencer and its audio graph are part 2; this is everything underneath them.
  - **Ported (707 l. of AS3):** `TraxData` + `TraxChannel` + `TraxChannelItem` (a song's structure,
    parsed from the one string the server sends), `TraxSample` (the decode and the two mixing
    operations) and `TraxChannelSample` (the play offset a shared sample cannot hold itself).
  - **The packing is kept, deliberately.** AS3 stores decoded audio in a `Vector.<int>` with two
    16-bit values — or four 8-bit ones — per entry, and the sequencer's mixing arithmetic is
    written against exactly that layout. Unpacking to `Float32Array` would mean rewriting the
    mixer's numerics, which is the one place a silent, *audible* bug could hide. `Int32Array` here,
    with every accumulation forced through `| 0` because AS3's `int` wraps at 32 bits and the
    packing relies on it.
  - **Two AS3 quirks kept and documented:** `TraxSample.dispose()` never sets `_disposed`, so
    `disposed` always answers false and a second call repeats an idempotent body; and it nulls its
    usage array, which would make the one unguarded getter throw — emptied here instead, same
    answer everywhere it is actually read.
  - **Verified in a browser against the arithmetic itself**: the song string parses to 2 channels
    with deduplicated sample ids (including the id-0 silence entry) and its `meta` segment
    (cut mode, tempo 120); a malformed item abandons the rest of the song but keeps the channels
    already parsed, as AS3's early return does; a 16-bit/44 kHz sample quantises **-1 → -32767,
    0 → 1, 0.5 → 16385, 0.25 → 8193** and ignores the right channel entirely; `addSample()` mixes
    where `setSample()` overwrites; the 32-sample fade-out ramps down to silence at the loop point;
    11 kHz averages four frames into one value (**-1, 0, 0.5, 0.25 → -2047**) and writes it four
    times; running past the end zero-fills; and `TraxChannelSample` carries its offset forward
    between blocks.
  - **Part 2 is `TraxSequencer` (817 l.) and the audio graph.** Flash streams through
    `SampleDataEvent` — the runtime pulls 8192 frames at a time; the Web Audio equivalent is an
    `AudioWorklet` fed from a ring buffer, with AS3's "audio buffer under run" logging mapping onto
    the worklet's starvation signal. Until it lands, `loadTraxSong()` stays the documented stub and
    nothing plays.

- ✅ **`habbo/sound/music` finished: both play-list controllers**, 2026-08-06.
  - **`JukeboxPlayListController` (281 l.) and `SoundMachinePlayListController` (429 l.)**, built
    and disposed by the music controller on the room's own furniture events — the `TODO(AS3)`s the
    previous entry left. `IPlayListController` was already declared and, like the music controller
    before it, had nothing constructing it.
  - **Seven more messages**: `NowPlaying` (398), `JukeboxSongDisks` (2257), `JukeboxPlayListFull`
    (949), `PlayList` (1242), `PlayListSongAdded` (2785), `GetNowPlaying` (3707) and
    `GetSoundMachinePlayList` (3633), plus `PlayListStatusEvent`.
  - 🐛 **Two more decompiler-corrupted loops, repaired against an intact sibling.**
    `PlayListMessageEventParser` has both `while(0 < count)` with an untested counter *and*
    `new class_3136(0, 0, null, null)` — the four values it just read thrown away.
    `PlayListSongAddedMessageEventParser` reads the same four fields and is undamaged, which is
    what fixes the order: id, length, name, creator. `JukeboxSongDisksMessageEventParser` has the
    same untested-counter shape. Third time this pattern has appeared (see the badges parser).
  - **The two controllers differ in who drives them, and it shows.** The jukebox is told what is
    playing and how far in — `syncCount` in milliseconds, handed to `playSong()` as seconds — and
    only *requests* the next song so its samples are ready. The sound machine picks its own next
    track, wrapping at the end, and resolves a room's position by walking the list subtracting
    each song's length until the offset falls inside one.
  - **Verified in a browser**: all four headers resolve; the repaired parser reads three entries
    leaving no bytes; **90 000 ms into a 135 000 ms list lands inside the second track with a
    30-second play-head**; the next-entry scan wraps 13 → 11 and steps 11 → 12; an empty list is
    ignored rather than replacing a good one; the jukebox's now-playing sets its state and emits
    `NPE_SONG_CHANGED` on the *sound manager's* bus at priority 0, and a current song of -1 stops
    it; a disc whose song is unknown becomes a placeholder entry (length -1) and is requested; and
    the room events build a jukebox controller, then a sound-machine one, then null it on dispose.
  - **`music/` is now complete except what needs the sequencer** — `trax/` (1,495 l.) is the last
    dependency between this module and audible playback.

- ✅ **`habbo/sound/music` opened: `HabboMusicController`, and the Trax title that needed it**,
  2026-08-06.
  - **Why it existed at all:** `IHabboMusicController` and `ISongInfo` were already declared, and
    `HabboSoundManagerFlash10.init()` carried a `TODO(AS3)` where AS3 builds the controller — so
    `musicController` was permanently null and every caller of `getSongInfo()` got nothing. It is
    constructed now, from the same line AS3 builds it on (`HabboSoundManagerFlash10.as:423`), with
    the room engine's own event bus as its third argument.
  - **Ported: the song-metadata spine.** The 817-line controller — the metadata cache, the
    request queue batched once a second behind it, the song-disk inventory, and the four-slot
    priority stack that decides what a room should be hearing. Plus `SongDataEntry`,
    `SongStartRequestData`, the three sound events, and five messages that did not exist:
    `TraxSongInfoMessageEvent` (2278), `UserSongDisksInventoryMessageEvent` (1930),
    `GetSongInfoMessageComposer` (3130), `GetUserSongDisksMessageComposer` (1685),
    `GetJukeboxPlayListMessageComposer` (1281) — every header from WIN63's registry, every name
    from `win63_version`, which carries the sound messages unobfuscated.
  - 🐛 **The song-info parser reads six fields and drops one.** The order on the wire is *id, an
    unread string, name, data, length, creator*, and the constructor is then called as
    `(id, length, name, creator, data)` — a different order again. Skipping the unread read
    instead of performing it would desynchronise every following song in the batch.
  - **Playback stops exactly where the sequencer would be.** `loadTraxSong()` is a documented stub
    returning null (`habbo/sound/trax`, 1,495 l., unported), so every entry's `soundObject` stays
    null and the play paths return at AS3's own `soundObject == null || !ready` guards. The
    `TODO(AS3)`s that remain are the two play-list controllers (jukebox 281 l., sound machine
    429 l.) and the sample bookkeeping — all sequencer- or play-list-bound.
  - **The Trax song title now resolves in the trade tooltip**, which is what opened this module:
    a disc is named after its song, and if the controller does not know it yet the name is
    requested and the tooltip repaints on `SIR_TRAX_SONG_INFO_RECEIVED`. `HabboInventory` gains
    AS3's required `IIDHabboSoundManager` dependency to carry it.
  - **Verified in a browser**: the three headers resolve; the parser reads the emulator's order
    leaving no bytes; three `getSongInfo()`/request calls for two distinct songs collapse into one
    batched `[2, 101, 102]`; an empty queue sends nothing; the answer fills the cache and fires one
    event per song, with a duplicate answer firing none; and the disk inventory **holds its ready
    event back** while a song it names is unknown, requests that song, and fires once it arrives —
    AS3's deferred dispatch, exactly.

- ✅ **Trading, slice 4: the name-scam warning**, 2026-08-06.
  - **All 5 `namescam/` files ported.** The lookalike detector, its result and warning-data records,
    the controller, and the 315-line warning window — the other trader's name and avatar beside the
    names it resembles, with both ways out **locked for six seconds** so the warning cannot be
    clicked away unseen.
  - **The detector is the interesting half.** Two names match if they can be walked together
    allowing only: an exact character, a case-only difference (≤ 2), a lookalike swap
    (`0`/`O`/`o`/`Ö`/`ö`, `1`/`l`/`I`/`!`, … — free and unlimited), or a skipped `.`/`,`/`:` on
    either side (≤ 2). A name containing anything outside the allowed alphabet is never compared.
  - **One dead guard kept as written.** AS3's `detectNameScam()` opens with `if(!param1 && false)`
    — the `&& false` disables the early-out, so a self-initiated trade is checked like any other.
    That agrees with the constant above it
    (`SHOW_NAME_SCAM_WARNING_FOR_SELF_INITIATED_TRADES = true`) and explains why the constant is
    read by nothing. The port expresses it through the constant rather than "restoring" what the
    parameter name suggests.
  - **`HabboInventory` gains AS3's optional `IIDHabboFriendList` dependency** — the friend half of
    the comparison. Optional there and here, so it cannot deadlock the component.
  - **The class name `TradingNameScamDetector` is DERIVED**, and says so at its declaration:
    `_SafeCls_3934` is obfuscated in every tree including PRODUCTION, since it postdates 2016.
  - **Verified in a browser** against the algorithm: 1 and 2 case changes match and 3 does not;
    1 and 2 skipped small-punctuation marks match and 3 does not, from either side; lookalike
    swaps are free and unlimited (`oooo` ≈ `0Ö0ö`); `B8b` is not a lookalike of `Bob`; identical,
    empty, null and out-of-alphabet names never match; and `detect()` deduplicates its candidates,
    skips the identical name, and hands back copies.

- ✅ **Trading, slice 3: the hover tooltip — `ItemPopupCtrl`**, 2026-08-06.
  - **Ported from the 370-line AS3**: one window re-parented under whichever thumb is hovered,
    the 250 ms open / 100 ms close delays, the left/right placement with the arrow pointing back
    at the cell, the limited-item serial overlay, and the three content shapes (product previewer,
    external image, plain bitmap). `HabboInventory.getItemImage()` came with it — the large
    direction-180 scale-64 render the tooltip shows, which is a different request from the small
    grid icon.
  - **The hover half of `TradingView.thumbEventProc()` is now real**, including the credits tile's
    own text and icon, the poster and Ecotron-box name rules, and AS3's own
    `isExternalImagetype()` helper.
  - ⚠️ **The arrow assets are the `_png` trap again.** AS3 asks for `popup_arrow_right_png`; this
    port registers images under the bare basename, so the suffixed lookup would have returned null
    and the tooltip would have had no pointer, silently. Noted at the lookup — see
    `project_asset_name_no_png_suffix`.
  - **Two deliberate deviations, both noted at the line**: AS3 *crops* an oversized picture into a
    new 180x200 BitmapData (and *scales* an external one through a Matrix); here the bitmap is set
    as it is and the window clamped to the same maxima. Identical for every image under the cap,
    which is every inventory icon.
  - **Verified in a browser**: the constructor throws on a null window or asset library as AS3
    does and starts hidden; a plain item sets the name, centres its picture and drives the serial
    overlay from the stuff data (12/100, hidden again at 0); placement is exact — RIGHT lands at
    `parent.width + BOUNDS_MARGIN` with the arrow at `-arrow.width + 1`, LEFT mirrors it; and the
    delays fire at 250 ms and 100 ms.
  - **Still out on trading**: `namescam/` (5 files), the NFT half (waits on
    `habbo/inventory/collectibles`), and the Trax song title in the tooltip (waits on the sound
    manager's music controller).

- ✅ **Trading, slice 2: the trade window, and the map that had nowhere to host it**, 2026-08-06.
  - **`TradingView` ported (1,053 l. of AS3)** and owned by the model, as in AS3: both windows
    (normal + minimised), the two grids with their per-cell ids, the accept/cancel pair driven by
    the state machine, the 3-second confirm countdown, the silver-fee row, the per-side "account
    disabled" notices and the three alert popups. `IInventoryView` and `CreditTradingItem` came
    with it.
  - 🐛 **`getCategorySubWindowContainer()` returned null unconditionally**, so even a finished
    trade window had nowhere to be hosted. AS3 resolves it — and `getCategoryWindowContainer()`
    and `updateView(category)` — through an `_inventories` map keyed by category name, built in
    `init()`. The map is ported; `furni`, `trading` and `pets` register in it. The port had been
    switching on the category name in two of the three, and returning null in the third.
  - **Three more members were private or mistyped where AS3 has them public**: `GroupItem`'s
    `iconImage`/`iconCallbackId` (the view assigns them when a late icon arrives),
    `FurniModel.createGroupItem()`, and `PetsModel.getWindowContainer()` returned `unknown` where
    its own view returns `IWindowContainer | null` — the widening is what hid it from
    `IInventoryModel`. `PetsModel.selectItemById()` took a number against AS3's string.
  - **A re-entrancy was verified rather than "fixed".** Closing from CONFIRMING sends the cancel
    composer **twice**: `close()` sets CANCELLED, whose setter calls `close()` again, and the
    inner call still sees a non-READY state. AS3 does exactly this; the behaviour is now
    documented at `close()` instead of being tidied away.
  - **Verified in a browser**: the shipped `inventory_trading_xml` carries all 21 tags and names
    the view looks up; the window builds both layouts on first `getWindowContainer()`; an accept
    flips the lock icon to `inventory_trading_trading_locked_icon` and the button caption to
    `${inventory.trading.modify}`; the confirmation drives caption/enabled/help through
    countdown → confirm and registers `inventory.trading.countdown:counter=3`; clicking accept
    while CONFIRMING sends `ConfirmAcceptTradingComposer`; and a close from the other side raises
    `${inventory.trading.info.closed}` and leaves the state READY.
  - **Still out**: `ItemPopupCtrl` (370 l., the hover tooltip — shared with the furni and
    collectibles views, so it is its own slice), `namescam/` (5 files), and the NFT half, which
    waits on `habbo/inventory/collectibles`.

- 🔄 **Trading, slice 1 of 2: the whole network side, and a model that now matches AS3**,
  2026-08-06.
  - **The 12 answers are subscribed at last.** Seven were registered and read by nobody; five did
    not exist. New: `TradeOpenFailedEvent` (2855), `TradingOtherNotAllowedEvent` (814),
    `TradingYouAreNotAllowedEvent` (2294), `TradeSilverSetMessageEvent` (1490),
    `TradeSilverFeeMessageEvent` (3497), plus the `AddItemsToTradeComposer` (3370) and
    `SilverFeeMessageComposer` (2717). **All seven headers came from WIN63's own registry** and
    every one is corroborated by the emulator. `win63_version` carries the whole trading message
    set unobfuscated, so no name here is derived.
  - **`TradingModel` rewritten from the AS3, 315 → ~900 lines.** The port had a plausible
    skeleton with an invented `ITradingUser` record and a 4-member `ITradingModel` widened to 12.
    AS3's interface really is 4 members; the model really does hold both sides flat. Now faithful:
    the full 7-state machine with its per-transition side effects (and its two `throw`s), NFT and
    silver-fee state, `handleMessageEvent()`'s dispatch, and every `request*` composer.
  - **Two parsers were renamed to AS3's own accessors**, because the port's names hid the
    asymmetry the code depends on: `TradingOpenMessageParser` was `userOne/userTwo` where AS3 says
    `userID`/`otherUserID` — `onTradingOpen()` *swaps* the two when the second id is ours — and
    `TradingItemListMessageParser` returned two `{userId, items, …}` records where AS3 has eight
    flat accessors. Read order unchanged in both, and nothing consumed the old shapes.
  - 🐛 **`FurniModel.updateItemLocks()` took a parameter AS3 does not have**, so nothing ever
    passed one and no item was ever shown as locked. AS3 computes the list itself from the trading
    model; it does that now, and an offered item greys out in the furni grid. (Its early return
    also skips the action-view refresh, which the port did on both paths.)
  - **Two type escape hatches removed rather than cast around.** `IFurnitureItemData.slotId` was
    `string` where AS3's is a nullable `String` — the trading item parser returns null outright —
    which forced an `as unknown as` at the call site. Widened through `IFurnitureItem` too.
  - **`ErrorReportStorage.setDebugData()` was not the AS3 name.** It is `addDebugData()`, and it
    removes the key before re-adding so the newest entry sorts last in the dump. Renamed, 8 call
    sites updated.
  - ⚠️ **Two emulator-side stubs found while checking the wire.**
    `TradeSilverSetMessageComposerSerializer` and `TradeSilverFeeMessageComposerSerializer` have
    **empty `Serialize()` bodies** in `vortex-emulator`, while the client reads two ints and one
    int respectively — if either is ever sent, the client hits end-of-buffer. Nothing sends them
    today. Also note `Headers.cs` carries placeholder 9014 for `RemoveNftFromTradeEvent` with a
    comment saying no such composer exists in the 701 client: it does, at
    `_composers[521] = _SafeCls_3173`, called from `TradingModel.requestRemoveItemFromTrading()`.
  - **Verified in a browser, driving the real model**: all 12 event headers and 3 composer headers
    resolve to the right classes; the three new parsers read the emulator's exact order and leave
    no bytes; a full trade runs `RUNNING → COUNTDOWN → CONFIRMING → CONFIRMED → COMPLETED →
    READY`, sending `AcceptTradingComposer` then `ConfirmAcceptTradingComposer` and closing
    itself; an accept lands on the side its user id names; `AddItemsToTradeComposer([11,22,33])`
    serialises `[3,11,22,33]`; and an unlisted transition throws, as AS3 does.
  - **Slice 2 is TradingView**, and it needs four unported pieces first: `IInventoryView`,
    `ItemPopupCtrl`, `CreditTradingItem` and the inventory's trading sub-page hosting. The four
    layouts already ship (`inventory_trading_xml`, `…_minimized_xml`, `…_name_scam_warning_xml`,
    `…_wired_xml`). `namescam/` (5 files) and the NFT/collectibles half go with it.

- ✅ **Priority-0 sweep of `habbo/catalog` and `habbo/inventory` — and the badge list nobody
  read**, 2026-08-06.
  - 📏 **The measurement.** Catalog subscribes **31 of AS3's 45** registrations
    (`HabboCatalog.as:707-751`); inventory **17 of 52** (`_SafeCls_1951.as`). No class in either
    module is defined-but-never-constructed — every model (`TradingModel`, `BadgesModel`,
    `BotsModel`, `EffectsModel`, `PetsModel`, `FurniModel`) has a `new` in `HabboInventory`, and
    the one catalog class referenced from nowhere, `MarketPlaceOfferState`, is referenced from
    nowhere in AS3 either. The whole gap in both modules is the subscription list.
  - 🐛 **The badge list was requested and thrown away.** `HabboInventory.requestBadges()` sent
    `GetBadgesComposer`, and `BadgesMessageEvent` — ported, parsed, registered at 2748 — was
    subscribed only by `HabboClubCenter`, for its own club-badge lookup. Nothing fed
    `BadgesModel`, so the inventory badge tab stayed empty until a badge was awarded *live*
    (`onBadgeReceived`, the one badge message that was wired). Now `onBadges` runs AS3's order:
    assemble fragments → `initBadges()` → `updateView()` → `setInventoryCategoryInit('badges')`.
  - 🐛 **And the club centre could never resolve its badge either.** AS3's `addMessageFragment()`
    returns a single-fragment message **before it reads `fragmentNo`**; the port indexed every
    fragment by number and walked `0..totalFragments-1`. `vortex-emulator` numbers a lone fragment
    **1**, not 0 (`BadgesEventMessageComposerSerializer` writes `totalFragments 1, fragmentNo 1`),
    so the loop found nothing at 0 and returned every time. Ported faithfully in both places —
    this is why the helper exists in AS3 at all.
  - **Verified in a browser**: the parser reads the emulator's exact order — `int totalFragments,
    int fragmentNo, int count, then (int slotId, string badgeCode)` — leaves no bytes unread,
    derives `activeBadgeIds` from `slotId > 0`, and `BadgesModel.initBadges()` fills from it;
    the client resolves `BadgesMessageEvent` at **2748**, which is `MessageComposer.BadgesComposer`
    in `Headers.cs`. The two fragment fixes are verified against the AS3 helper and by typecheck,
    not against a live multi-fragment session.
  - **What the sweep leaves open, in order of product weight:**
    - **Trading landed 2026-08-06** (three entries above): messages, model, window and tooltip.
      What remains is the NFT half (waits on `habbo/inventory/collectibles`) and the Trax song
      title (waits on the sound manager's music controller).
    - **Inventory, the rest of the 35:** collectibles (no module), bots add/remove, post-it
      placed, not-enough-credits, badge point limits, `onUserBadges`, marketplace (4 handlers, and
      `inventory/marketplace/` is an empty directory here).
    - **Catalog, the 14:** `onCatalogPublished`, `onLimitedEditionSoldOut`, `onLtdRaffleEntered/
      Result`, `onGiftWrappingConfiguration`, `onGiftReceiverNotFound`, `onProductOffer`,
      `onNftStorePurchase`, `onBundleDiscountRuleset`, `onSilverBalance`, `onEmeraldBalance`,
      `onMarketplaceMakeOfferResult`, `onRoomExit`, `onSnowWarGameTokenOffer` (that last one waits
      on `habbo/game`). None of their message classes exist yet — these are ports, not wiring.
      `sendGetProductOffer()` already carries a `TODO(AS3)` saying its reply is unported: the
      request goes out on every limited-edition page and the answer is dropped.

- ✅ **The rentable-space furniture, end to end — and `FriendlyTime` was rendering hardcoded
  English client-wide**, 2026-08-05.
  - **The slice.** `RentableSpaceDisplayWidget` + `RentableSpaceWidgetHandler` + its six messages
    (status/rent-ok/rent-failed events with parsers, and the status/rent/cancel composers), wired
    into `RoomWidgetFactory`, `RoomDesktop` and `RoomUI`. `habbo/ui/widget/furniture` is now
    **46/54**, `habbo/ui/handler` **25/47**.
  - **All six message names came from AS3, not the emulator.** `win63_version` carries the whole
    `{incoming,outgoing,parser}/room/furniture/RentableSpace*` set unobfuscated; the emulator
    corroborates every one of the six headers (2800/2158/3117, 2626/3165/61). Only the handler's own
    class name is derived — `_SafeCls_3971` is obfuscated in every tree, identified by
    `implements IRoomWidgetHandler` plus its `RWE_RENTABLESPACE` type, and named after that.
  - 🐛 **`FriendlyTime` had lost its localization.** The port's version took no localization manager
    and concatenated English words — `"5 days"`, `"just now"` — where AS3 resolves
    `friendlytime.days` with an `amount` parameter, because word order differs per language. That is
    **9 call sites across 6 modules**: group profiles, room info, quests (×3), the wired monitor,
    the infostand's rent expiry and the club centre (×2). One of them already carried a note saying
    the parameter had been dropped. Now faithful — `getFriendlyTime` / `getShortFriendlyTime` /
    `getLocalization` all take the manager first, as AS3 does, and every caller passes one. Note the
    suffix is appended to the *key*, not the text: `('.ago')` looks up `friendlytime.days.ago`.
  - **A second interface had to admit null.** `IRoomWidgetHandler.getWidgetMessages()` said
    `string[]`, but AS3 returns a null `Array` from this handler (its widget calls it directly
    instead of sending widget messages). Widened to `string[] | null`, `RoomDesktop` spreads `?? []`
    — the same fix `getProcessedEvents()` needed earlier today, for the same reason.
  - **Two AS3 quirks kept rather than tidied**, both noted at the declaration: the container setter
    subscribes with no already-subscribed guard (a second container would subscribe twice), and
    `getUsersClubLevel()` is dead — the widget gates on credits, never on club level.
  - **Verified in a browser**: the handler answers `RWE_RENTABLESPACE`, returns null widget-messages
    and `[]` processed-events, and reports `disposed` before a container is set; all six headers
    resolve to the right classes; the rent composer's payload is `[4242]`; and the status parser
    reads **bool, int, int, str, int, int** — AS3's exact order — deriving `canRent` from a zero
    error code and blanking `renterId`/`renterName` to -1/"" when the space is free while keeping
    them when it is rented. `FriendlyTime` was probed against a fake manager: it emits
    `friendlytime.days` / `friendlytime.days.ago` / `friendlytime.hours.short` with
    `('amount', '10')`, and falls back to the bare key when handed no manager.

- ✅ **`habbo/ui/widget/furniture` 45/54, and the measurement that matters: `habbo/ui/handler` is
  23/47**, 2026-08-05.
  - 📏 **The tracked gap was the wrong one.** Priority 1 has counted widgets (44/54) since
    2026-08-03, but the handler layer behind them is a separate, larger hole nothing measured:
    **24 of AS3's 47 `ui/handler` files are unported, ~4,700 lines** — `MeMenuWidgetHandler` (481),
    `CraftingWidgetHandler` (379), `CameraWidgetHandler` (356), `FurnitureBadgeDisplayWidgetHandler`
    (331), `PlayListEditorWidgetHandler` (264), `FurniChooserWidgetHandler` (211) and 18 more. A
    widget without its handler receives nothing, so the two counts have to move together. Re-measure
    with `comm -23` over the two basename lists, the same recipe as the widget count.
  - **First slice closed end to end: the refusal dialogs.** `CustomUserNotificationWidget` (5
    variants — VIP hopper, VIP gate, costume hopper, and the two failed-respect notices) +
    `CustomUserNotificationWidgetHandler`, plus the message they both hang off:
    `CustomUserNotificationMessageEvent` / `…Parser` at header 169.
  - 🐛 **Header 169 was registered nowhere, and a second already-"ported" handler was missing its
    half of it.** AS3 subscribes `_SafeCls_3982` from *two* places: the notification handler opens
    the dialog, and `AvatarInfoWidgetHandler.onCustomUserNotificationMessage()` refunds the respect
    on codes 4-5. The port's `AvatarInfoWidgetHandler` had neither the subscription nor the method,
    so a failed respect vote silently kept the point — an orphan inside a file that counted as done.
    Both handlers now subscribe independently, as in AS3, and `dispose()` unsubscribes before
    nulling the container (the setter drops the reference the connection is reached through).
  - **Wired at all three points AS3 wires it**: `RoomWidgetFactory.createWidget()`,
    `RoomDesktop.createWidgetHandler()` and `RoomUI`'s `createDesktopWidget('RWE_CUSTOM_USER_NOTIFICATION')`
    (AS3 `RoomUI.as:956`).
  - **`IRoomWidgetHandler.getProcessedEvents()` had to admit null.** AS3 returns a null `Array` from
    this handler and `[]` from the others; the port's signature said `string[]`, so the faithful
    handler would not compile. Widened to `string[] | null` — every existing implementer still
    satisfies it — and `RoomDesktop` now spreads `?? []`.
  - ⚠️ **One branch could not be finished:** owning a costume should open the avatar editor on its
    effects tab (`container.avatarEditor.openEditor(…)` / `loadOwnAvatarInEditor(0)`).
    `IRoomWidgetHandlerContainer` declares `avatarEditor` in AS3 but not here, because
    HabboAvatarEditor has no ported manager — the same gap already marked in `HabboCatalog` and
    `HabboLandingView`. Marked `TODO(AS3)`; the not-owned branch (open the `costumes` catalog page)
    works.
  - **Verified in a browser**: widget, handler, factory and desktop all import; the handler answers
    `RWE_CUSTOM_USER_NOTIFICATION` — the exact string both switches test — `getProcessedEvents()`
    returns null and the desktop tolerates it; the five type constants match what the handler passes
    to `open()`; header 169 resolves to `CustomUserNotificationMessageEvent`; and the parser reads
    exactly one integer, returns it as `code`, and answers false to a null wrapper.

- ✅ **`habbo/nux` 4/4, `habbo/phonenumber` 7/7 and `habbo/userclassification` 1/1 — three modules
  off the "still at zero" list, with their 9 messages and both components wired**, 2026-08-05.
  - **What landed.** `HabboNuxDialogs` + its three views (phone-verification offer, gift picker,
    noob-lobby nag); `HabboPhoneNumber` + its four (collect dialog, verify dialog, and the two
    toolbar-badge minimized forms) + `ClientPhoneVerificationStatusEnum` / `PhoneNumberStatusEnum`;
    `UserClassificationData`. Plus `habbo/utils/TextWindowUtils` (its one caller is the collect
    dialog) and 13 message classes — 5 events, 4 composers, 3 parsers and 4 parser DTOs.
  - **Both components are attached, not just written** (priority 0's failure mode). AS3 puts all
    three up from `HabboToolbar`'s own constructor (`HabboToolbar.as:152-154`: phone, nux,
    campaigns); the port's `VortexMain` now does the same in the same order at steps 12b/12c,
    ahead of the campaign calendar that was already there. `HabboPhoneNumber` depends on
    `IID_HabboToolbar`, attached later at 12j — deferred resolution, exactly as in AS3, where the
    toolbar attaches this from inside its own constructor.
  - **Names: 11 of 13 message classes were recovered from AS3, not derived.** `win63_version` keeps
    the whole `incoming|outgoing|parser/{nux,gifts}` set unobfuscated
    (`NewUserExperienceGiftOfferEvent`, `TryVerificationCodeResultMessageEventParser`,
    `VerifyCodeMessageComposer`, …), PRODUCTION supplies `NewUserExperienceGiftOptions` /
    `NewUserExperienceGift` and both enum class names. Four identifiers exist in no tree and say so
    at the declaration: `NewUserExperienceProductOffer`, `NewUserExperienceGiftSelection`,
    `UserClassificationData`'s constants 1 and 3, and `ClientPhoneVerificationStatusEnum`'s
    constant 1 — all four are unreferenced everywhere, so nothing constrains them but their value.
  - 🐛 **The primary tree lost the country-name lookup, and the loss is invisible.**
    `PhoneNumberCollectView.createWindow()` reads `new JSONDecoder("{}", false)` in WIN63 — only
    the fallback. Ported as-is, every country name is undefined and the drop list renders blank
    while throwing nothing. `sources/win63_version/habbo/phonenumber/PhoneNumberCollectView.as`
    keeps the real expression, `getLocalization("phone.number.collect.countries") || "{}"`, and
    that is what shipped. Same class of decompiler damage as the `order-before` E4X case in
    CLAUDE.md, found the same way — by cross-referencing when a constant looked too empty to be
    real.
  - **Verified in a browser, not by reading.** `HabboPhoneNumber` ↔ `VerificationCodeInputView` is
    a genuine import cycle (the view calls the component's static `getTimer()`), and a cycle that
    resolves the wrong way leaves one side `undefined` until first use. All 14 new modules import
    clean through Vite, the cycle resolves (`getTimer()` returns a number across it), and the
    registry answers all 9 new headers with the right class: events 752/3307/2833/2845/712,
    composers 3490/1983/2890/1846. Full client boot could not be exercised — the client only
    bootstraps after a server login, and no emulator was running.
  - ⚠️ **Server-side, the NUX flow is inert.** `vortex-emulator`'s handlers for 3490 and 1983 are
    empty `ValueTask.CompletedTask` stubs whose parsers read no payload, and nothing anywhere
    constructs `NewUserExperienceGiftOfferEventMessageComposer` or its NotComplete sibling — so
    752 and 3307 are never sent. The client side is complete and correct; the dialogs simply have
    nothing to open them until the emulator grows the flow. The link-event path
    (`nux/lobbyoffer/show`) and the home-room timer are client-driven and do work.
  - ⚠️ **One member could not be finished:** `TextWindowUtils.setHTMLLinkStyle()` computes its four
    Flash link styles and drops them — this port's `ITextWindow` has no `styleSheet`, and the
    Canvas2D text renderer has no `flash.text.StyleSheet` notion at all. Left as a `TODO(AS3)` on
    the method naming the three pieces it needs. Links render in the layout's own colour.
  - **Two wrong comments corrected while in the area**, both about provenance:
    `ResetPhoneNumberStateMessageComposer` claimed its class was "obfuscated in every available
    tree" — `win63_version` has it unobfuscated under `outgoing/gifts/`; and
    `EventLogMessageComposer` (reused by the noob-lobby nag) documented itself as "Message ID:
    2297" while being registered at 3809, a number matching no registration in either tree.

- ✅ **Four UI defects reported from a live session: room-settings placeholders, a DOM input that
  outlived its window, the purse's "Get" for HC holders, and the stub purchase dialog**, 2026-08-05.
  - 🐛 **`%displayed%/%total%` rendered verbatim in the Rights tab.** `RoomSettingsCtrl.refreshFlatControllers()`
    had the two `UserListCtrl.refresh()` calls but not the four `registerParameter()` calls AS3 makes
    right after them (`navigator.flatctrls.userswithrights` / `.friends` × `displayed` / `total`).
    The column headers are localization keys with those placeholders, and `TextController` registers
    itself as a listener for its own `${...}` caption, so the four registrations were the whole fix.
  - 🐛 **A text field's DOM bridge stayed visible for the rest of the session** (`TextFieldController`).
    `onFocusOutEvent()` called `super.unfocus()`, skipping the override that sets
    `style.display = 'none'` and stops the caret-tracking frame loop — so every native blur (a click
    anywhere else on the canvas) left the element parked at its last position, outliving its window
    and its tab. Invisible on its own, since the element carries transparent text while the canvas
    paints the glyphs — **except** that Chrome's `:-webkit-autofill` rule overrides both `color` and
    `background`. With two `input[type=password]` in the room-settings Access tab, the password
    manager filled a plain field elsewhere with the account e-mail and painted it over the dialog.
    Fixed on both sides: the blur handler now goes through `this.unfocus()`, and every bridge element
    is created with `autocomplete="off"` (`new-password` for password fields, re-applied when the
    layout's `display_as_password` var lands after construction).
  - 🐛 **The toolbar purse showed the "Get" join label to users who hold HC.** `HabboInventory.setClubStatus()`
    was ported but never called: the inventory's `ScrSendUserInfo` registration
    (`_SafeCls_1951.as:164`, `onClubStatus`) was missing, so its purse kept `clubDays`/`clubPeriods`
    at 0, `get clubLevel()` answered 0, and `PurseClubArea`'s `clubLevel == 0` branch wrote
    `amountZeroText`. `HabboToolbar` was already listening for `HIHCE_HABBO_CLUB_CHANGED` with a
    comment saying the producer was unported — that is now the producer. (The catalog registers the
    same message independently for its own purse; AS3 does the same.)
  - ✅ **`PurchaseConfirmationDialog` builds the real `purchase_confirmation` layout.** It was a
    `windowManager.confirm()` one-liner showing `offer.localizationName` and a hand-formatted price,
    which is why a bundle read as a bare `classic3_plant*3` in an oversized empty box. Now ported:
    the product preview (`getFurnitureImage()`/`getWallItemImage()` via `imageReady()`, centred into
    the 126×152 wrapper the way AS3's `setImage()` composes it), the productdata name, the `X n`
    quantity row (removed from the item list when it does not apply, as AS3 does), the price box,
    the spending disclaimer, and rent-aware buy captions. The dialog now closes on the server's
    answer rather than on the click — `onPurchaseOK`/`onPurchaseError` dispose it, `onNotEnoughBalance`
    calls `notEnoughCredits()` — which is what AS3 does and what lets a rejected purchase leave it up.
    Gifting, the three collectible offer classes and the LTD raffle stay TODO in the class header.
  - 🐛 **The preview stayed blank, because `ImageResult`'s class header no longer described the
    code.** It says RoomEngine "always resolves via the id>0 pending path and delivers through
    `imageReady()` — even for already-cached assets", so the dialog was written to read only the
    callback. `getGenericRoomObjectImage()`'s body does the opposite for loaded content: it ends
    `result.data = offscreen.transferToImageBitmap(); result.id = 0; return result;` and never calls
    the listener. That branch was deliberate (an always-async result made the pet widgets'
    `imageReady()` -> `updateImage()` chain spin forever) — the header simply was not updated with
    it. **Callers must handle both halves**, which is what AS3 does
    (`_loc13_ = _loc6_.data; _SafeStr_6872 = _loc6_.id;` then `setImage()`). Header rewritten to say
    so. Two other call sites still read the callback only and so show nothing for a cached asset:
    `ClubGiftWidget.showPreview()` and `ProductViewCatalogWidget`'s no-room-canvas fallback.
  - ⚠️ **The product name is blank only when productdata has no row for the offer.** AS3 writes
    `getProductData(localizationId)?.name`, i.e. `null` for a miss — which leaves the layout's own
    design placeholder on screen. Stated deviation: fall back to `offer.localizationName`, AS3's own
    accessor for the same string (`Offer.as::get localizationName()`, which answers `${<id>}`), so
    the dialog shows what the catalog page behind it shows. The trigger case is catalog-seed data,
    not display code: the emulator names that offer `classic3_plant*3` and neither the live nor the
    dumped productdata carries it, though both carry other `code*n` bundles (`barchair_silo*10`).

- ✅ **vortex-glaze: the widget library now covers every buildable window type — and the `iconbutton`
  tag it exposed had never parsed at all**, 2026-08-04.
  - **The gap.** The palette listed 28 of the parser's 97 tags, and the hierarchy strip's
    Create/Convert drop menu listed 9 — so `Convert` could only reach nine types, and a created node
    was a 60×30 box with `params="0"` whatever its type (an `input` that could not take focus, a
    `button` that ignored the mouse). Both lists now come from one `ops/WidgetCatalog.ts`: **94
    entries**, grouped into 11 categories, each with the geometry, caption and params its family uses
    in the shipped layouts. `addChildOfType()` reads the same spec, so a type created from the strip
    is born identical to the same type picked in the popup.
  - **Four tags are excluded on purpose**, and `assertCatalogCoverage()` reports any other drift in
    dev: `null` (the parser's unresolved-tag fallback), `itemlist` (same type code as
    `itemlist_vertical`, so it would save itself out under the other name), `button_icon` (type 62,
    which AS3's own `Classes` leaves unregistered — `create()` returns nothing), and
    `itemgrid_horizontal`, whose `ItemGridController` throws *"Horizontal item grid not yet
    implemented!"* — an engine gap, found by instantiating all 95 candidates rather than by reading.
  - **The type picker is the popup, not a drop menu.** The ported `dropmenu` sizes its expanded view
    to the total item height and does not scroll, so 94 types in one column is unusable. The strip
    shows the armed type on a button that opens the library in `pick` mode; the strip is three rows
    now (`glaze_hierarchy_controls` 306×88, tree list moved to y=90).
  - 🐛 **`<iconbutton>` resolved to nothing, client-wide** (`core/window/enum/WindowType.ts`,
    `Classes.ts`). The port had `BUTTON_ICON = 79` with the tag `button_icon`. AS3's type table
    (`core/window/utils/_SafeCls_2816.as`) says `button_icon` = **62** and `iconbutton` = **79**, and
    its `Classes.as` registers `IconButtonController` on 79 and nothing on 62. So the tag the
    layouts actually use resolved to `undefined` → `WindowType.NULL`, and
    `parseElementDescriptionXml`'s `TYPE_MAP[name] ?? -1` dropped both `type="iconbutton"` skin
    descriptors before `ElementRegistry` ever saw them. Eight elements were dead: trading's
    silver ±, the moderation tool's ±coins/±furni, the chest's upgrade-capacity button, wired's
    Ubuntu `add_more`. Verified A/B in a browser: `silver_minus_button` is `type 0` /
    `WindowController` before the fix and `type 79` / `IconButtonController` after.
    `BUTTON_ICON` now holds 62 (name recovered from PRODUCTION) and stays unregistered, as in AS3;
    79 is `ICONBUTTON`, a derived name — its AS3 constant is obfuscated in every tree.
  - 🐛 **`frame_pointer_down` was drawn with the whole frame's skin** (`client/window/WindowXmlAssetParser.ts`).
    Same family, found by scanning every shipped layout for tags the parser cannot resolve — with
    `iconbutton` fixed, exactly one remained. `frame_pointer_down` is in no AS3 type table, yet it is
    both a tag in `habbo_window_layout_frame_7_xml` and a style-7 descriptor in the element
    description. AS3 passes the failed lookup straight into `addSkinRenderer(param1:uint, …)`, so it
    lands at `uint(undefined)` = **0** — the same 0 the layout parser gives the unresolved tag, which
    is the only reason that arrow renders at all. This port used `?? -1` and `ElementRegistry.load`
    dropped it, so the style-7 lookup fell through to style 0: `habbo_skin_frame_skin`, a whole
    9-slice frame inside a 16×12 box. Verified A/B on a real style-7 frame — the renderer at (0,7) is
    `default_frame_skin` before and `frame_pointer_down_skin` after; (0,0) is untouched. The first A/B
    read only the renderer's *class name*, which is `BitmapSkinRenderer` either way and said "no
    change" — the identity is in its name field.
  - **Client-wide, the tag/param tables now resolve everything the assets use**: 806 layouts + the
    element description, 0 unknown tags, 0 unknown `<param name>`.
  - **Verified in a browser, not by reading**: all 94 entries created through `addChildOfType` (94/94,
    each checked against its expected type code), `convertSelected` round-tripped on one type per
    category (11/11), and the popup built 105 rows (94 previews + 11 headers) with no failed
    preview. That run is also what caught `itemgrid_horizontal` and a header row whose caption never
    appeared (`glaze_label_xml`'s root *is* the text node, so `findChildByName('glaze_lbl')` never
    matched).

- 🐛 **OPEN — vortex-glaze's Save/Export silently drops most of a layout**, found 2026-08-04 while
  auditing the rest of the editor. **Not fixed; do not save over a real layout until it is.**
  - `serializeLayout` delegates to the engine's `WindowParser.windowToXMLString`, whose
    `serializeChildren` skips every child tagged `_EXCLUDE` — faithful to AS3, which does exactly the
    same. But the *parser* adds children through `getLayoutChildTarget()`, and for a frame that is
    `content_area` (`_CONTENT, _INTERNAL, _EXCLUDE`), for an item list / scrollable list / tab context
    it is the internal `_CONTAINER` (also `_EXCLUDE`). Read-in and write-out therefore disagree: every
    node the editor can actually select lives under a container the serializer refuses to walk into.
  - Measured: `group_management_window` 117 widget tags in source → **1** saved; `campaign_calendar_xml`
    31 → 1; `add_friends_tab_xml` 8 → 2, and re-importing its own saved output yields 3 of 10 live
    nodes. The `<layout><window>` envelope from the 2026-08-03 fix is correct — the file parses, which
    is precisely why the loss went unnoticed: the check was "does it load", not "did it keep the nodes".
  - AS3's serializer was a debug utility for a single widget, never Glaze's save format (the Flash
    Glaze had its own document model), so this is a tooling gap, not an engine parity gap — the engine
    function should stay as it is.
  - Proposed fix, ~40 lines and no behaviour change to shipped code: make `serializeChildren`
    `protected`, and give Glaze a `WindowParser` subclass that, for each child, skips `_EXCLUDE` as
    today **except** when the child is its parent's `getLayoutChildTarget()`, in which case it emits
    that child's children in its place. Frame internals (`titlebar`, `_FRAME_SCALER`) and a button's
    `_BTN_TEXT` stay excluded because they are not the layout target; the emitted shape then matches
    the source XML, where the items sit directly under `<itemlist><children>`.

- ✅ **The navigator's incoming messages closed at 47/47** (`habbo/navigator/IncomingMessages.ts`, 7 new events + 7 parsers), 2026-08-03.
  - Follow-up to the room-settings fix, which had measured this file at 22 of AS3's 47 registrations.
    All 17 genuinely-missing handlers are now ported; the five names that still differ are aliases
    this port already used (`onRoomInfo`/`onGetGuestRoomResult`, `onCompetitionData`/`onCompetitionRoomsData`,
    `onCategoriesWithUserCount`/`onCategoriesWithVisitorCount`, `onRoomEventEvent`/`onRoomEvent`,
    `onCanCreateRoomEventEvent`/`onCanCreateRoomEvent`).
  - **Ten needed only wiring** — their events, parsers and headers all existed and had simply never
    been subscribed: `RoomEntryInfo`, `CloseConnection`, `CantConnect`, `RoomForward`, `GenericError`,
    `UserRights`, `ScrSendUserInfo`, `RoomSettingsSaved`, `FriendListFragment`, `FriendListUpdate`.
    That is why entering a room left the info/settings/filter windows open over it, and why HC,
    event-mod and room-picker state never reached `NavigatorData`.
  - **Seven were missing entirely**, now created with headers from WIN63's registry and corroborated
    against the emulator: `GameStarted` 2902, `NoOwnedRoomsAlert` 735, `NoSuchFlat` 1122,
    `RoomFilterSettings` 3208, `RoomSettingsError` 3715 — plus `RoomMuteAll` **1172**, whose name is
    **DERIVED** because the emulator has no constant for that header at all. `FlatAccessible` (2051,
    `onDoorOpened`) turned out to already exist under `room/session`; the duplicate was deleted rather
    than kept.
  - AS3 quirks preserved with a note: `RoomFilterSettingsMessageEventParser.parse()` returns **false**
    where every sibling returns true; `onNoSuchFlat` and `onRoomSettingsError` have empty AS3 bodies
    (the latter reads its parser into a local and discards it); and `onCantConnect` switches on
    `reason - 1`. `GameStarted`'s parser reads nothing, with a `TODO(AS3)`: AS3 builds a `GameLobbyData`
    the navigator's handler never looks at, and messages are length-delimited so an unread body cannot
    desynchronise the stream.
  - **Server-side, in `vortex-emulator`**: `Vortex.Rooms/Grains/RoomGrain.cs` now resolves the room
    owner's name (`.Include(e => e.PlayerEntity)`, `OwnerName = entity.PlayerEntity?.Name`) where it
    hard-coded `string.Empty`. That was the whole reason the owner line in the room-info card was
    blank — the client was rendering exactly what it was sent.

- ✅ **The room settings window opens** (`habbo/navigator/IncomingMessages.ts` + `HabboNavigator.ts`), 2026-08-03.
  - Reported as "room filter opens, room settings does nothing" — and that contrast was the clue:
    the filter needs no server round trip, the settings do. `startRoomSettingsEdit()` sent
    `GetRoomSettings` (256) and **nothing on the client was subscribed to the reply**.
  - `RoomSettingsCtrl` is complete and holds all eight handlers; all nine incoming events and their
    parsers exist and are registered in `HabboMessages`. Only the subscription was missing, so every
    reply was parsed and dropped. Now wired: `RoomSettingsData`, `RoomSettingsSaveError`,
    `FlatControllers`, `FlatControllerAdded`, `FlatControllerRemoved`, `BannedUsersFromRoom`,
    `UserUnbannedFromRoom`, `ShowEnforceRoomCategoryDialog`.
  - **Re-measure**: AS3's navigator incoming-messages class (`_SafeCls_1951.as`) registers **47**
    handlers; this port's `IncomingMessages.ts` registered 22, now 30. The remaining 17 are listed
    by `grep -o "addHabboConnectionMessageEvent(new _SafeCls_[0-9]*(on[A-Za-z]*"` on that file —
    among them `onUserRights`, `onMuteAllEvent`, `onRoomFilterSettings`, `onNoSuchFlat`,
    `onGameStarted`, `onRoomEnter`/`onRoomExit`.
  - Also added the `UserInfoRegionUtil.setUserInfoState(false, …)` call AS3 makes on every room-info
    refresh (`RoomInfoViewCtrl.as` line 184), which resets the profile eye to its idle image.
  - **The missing owner name is server-side, not here.** `vortex-emulator`'s
    `Vortex.Rooms/Grains/RoomGrain.cs:402` builds the room snapshot with `OwnerName = string.Empty`
    and never resolves it from `entity.PlayerEntityId`, so the client renders exactly what it is
    sent. The parser and the serializer agree field-for-field; nothing to fix on this side.

- ✅ **The in-room "Room info" window actually opens** (`habbo/navigator/HabboNavigator.ts`, `IHabboNewNavigator.ts`), 2026-08-03.
  - Reported as `habbo.navigator.HabboNavigator Room info opened` in the log with nothing on screen —
    which named the culprit exactly: `openRoomInfo()` flipped a private boolean and logged, and that
    was the whole method. `RoomInfoViewCtrl` (739 l., complete) was never reached.
  - Structural cause: AS3's `HabboNavigator` implements **both** `_SafeCls_93` and `_SafeCls_94` (the
    transitional interface) and owns its own `RoomInfoViewCtrl`. This port split that in two —
    `HabboNavigator` implements only the first, and the in-room controllers live on `LegacyNavigator`,
    which `HabboNewNavigator` builds and holds privately. The old navigator is what is attached under
    `IID_HabboNavigator` and what `RoomDesktop` hands to `RoomToolsWidgetHandler`, so the click landed
    on the half without the controllers.
  - Fixed by exposing `legacyWrapper` on `IHabboNewNavigator` and giving `HabboNavigator` an
    **optional** `IID_HabboNewNavigator` dependency to reach it (hard would deadlock: the new
    navigator already depends on the old one). `toggleRoomInfoVisibility()` and `closeRoomInfo()` now
    drive the real controller.
  - AS3 quirk preserved: the toggle is guarded on `roomCreateViewCtrl` — a *different* controller —
    not on the room-info one it then toggles. Both are built and nulled together, so the test means
    "not disposed".
  - This is also the path to the **room settings**: the settings button lives on that window, so the
    earlier "room settings are complete but nothing opens" had the same single cause.

- ✅ **The personal word filter** (`toolbar/extensions/settings/WordFilterSettingsView.ts` 328 AS3 l., 3 composers, 2 events + 2 parsers), 2026-08-03.
  - Closes `toolbar/extensions/settings/` at 5/5: every entry in the client settings menu now opens
    a real window.
  - Nothing is applied locally — add and remove both go to the server, and the list only moves when
    `ModifyCustomFilterResult` comes back. Rows are built once and collapse to zero height rather
    than being removed, and a click selects a row that the remove button then acts on **by caption**,
    not by index.
  - **The emulator disagrees on every one of these five headers and implements none of them.** WIN63's
    registry gives `GetCustomFilter` **801**, `AddToCustomFilter` **2656**, `RemoveFromCustomFilter`
    **2209**, `GetCustomFilterResult` **2231**, `ModifyCustomFilterResult` **3622**; `Headers.cs` says
    3812 / 936 / 209 / 1426 / 1488, with no `AS3-verified` comment on any of them and no handler,
    parser or serializer behind them. The client uses WIN63's numbers, per the source-of-truth order,
    and says so at each declaration. **The feature cannot work until the server implements it.**
  - The two incoming events had to go into `HabboMessages._events` even though the view registers its
    own handlers: `MessageRegistry.registerMessageEvent()` maps an event **instance** to an id through
    that class table and warns `Unknown message event class` for anything absent, so a self-registering
    view still needs its events in the registry.
  - Both parsers are among the few this dump leaves **unobfuscated**
    (`GetCustomFilterResultMessageEventParser`, `ModifyCustomFilterResultMessageEventParser`), so those
    names are real. The three result constants on the event are not: AS3 tests only `1` (added) and `3`
    (removed), so those two names are derived from that use and `0` is declared with its value and no
    claim about its meaning.

- ✅ **Sound and other settings, and the slider under them** (`toolbar/extensions/settings/SoundSettingsView.ts` + `SoundSettingsItem.ts` + `OtherSettingsView.ts` rewritten, `toolbar/memenu/soundsettings/MeMenuSoundSettingsSlider.ts` rewritten, 3 new composers), 2026-08-03.
  - The sound chain was three SolidJS-era stubs deep — view, item and slider — none of which built
    a window. All three are now real.
  - `MeMenuSoundSettingsSlider` owns no drag logic: the window system moves `slider_button` and
    reports `WE_RELOCATED`, and the slider turns the x into a value. Its travel is the movement
    area **minus the button's own width**, which is what makes the far right read exactly `maxValue`.
  - A drag only previews. `saveVolume(..., false)` routes to `soundManager.previewVolume()`, so the
    player hears the change without it being stored; closing the window commits all three channels.
  - `OtherSettingsView` needed 3 composers, all newly registered: **`SetIgnoreRoomInvites` 1332**,
    **`SetRoomCameraPreferences` 3917**, **`ResetPhoneNumberState` 2056** (no payload). Headers from
    WIN63's registry, corroborated against the emulator. The wired-whisper checkbox sends nothing —
    AS3 keeps that one client-side.
  - Two AS3 quirks preserved with a note: `SoundSettingsItem._volume` has **no initializer**, so it
    starts NaN and `setValue()` never writes it — the mute icons therefore read "on" until the row is
    first touched, whatever the stored volume is (a `0` default would mute all three on open). And
    `MeMenuSoundSettingsSlider.setValue()` looks `slider_button` up on the container where
    `displaySlider()` looks it up under `slider_movement_area`; both find it, the lookup being recursive.
  - Omitted deliberately, with a `TODO(AS3)`: `SoundSettingsView`'s four `BitmapData` icon fields and
    their public getters. Nothing in the AS3 ever assigns them — the icons load by `assetUri` — so
    they can only ever be null.

- ✅ **The client settings menu opens** (`habbo/toolbar/extensions/SettingsExtension.ts` rewritten, `settings/ChatSettingsView.ts` new, `IHabboFreeFlowChat` +4 members), 2026-08-03.
  - Asked for as "add the client settings". The module was **not** missing — `SettingsExtension`,
    `SoundSettingsView` and `OtherSettingsView` all existed, as leftovers from the SolidJS era:
    they held state and built **no window at all**. `HabboToolbar.toggleSettingVisibility()` even
    carried a comment saying so, toggling a `visible` flag nothing rendered.
  - `SettingsExtension` now builds the real `settings_xml` column, one `setting_category_xml`
    button per entry, stacking each under the last and growing the menu to fit — which is how the
    two optional entries (Discord, word filter, both config-gated) can be skipped without a gap.
    It attaches as toolbar extension `settings` at index 1 and starts hidden, and
    `toggleSettingVisibility()` now toggles that window as AS3 does.
  - `ChatSettingsView` ported whole: three drop menus (chat mode, bubble width, scroll speed)
    writing straight through to `HabboFreeFlowChat`. No save button — every selection commits, and
    closing commits once more. The port's `IHabboFreeFlowChat` was missing `chatMode`,
    `chatBubbleWidth`, `chatScrollSpeed` and `updateChatPreferences()`, all four of which AS3's own
    interface (`freeflowchat/_SafeCls_70.as`) declares.
  - **Sound, other and word-filter settings all followed in the same session** (see the entries
    below): `toolbar/extensions/settings/` is now 5/5 and every menu entry opens a real window.
  - Checked while looking for this: **room settings are complete** (`navigator/roomsettings/`, 9/9
    including `RoomSettingsFriendListManager`), reachable from three entry points, with `GetRoomSettings`
    256 / `SaveRoomSettings` 725 / `RoomSettingsData` 791 registered and handled by the emulator.
    Nothing to add there.

- ✅ **The guild members list** (`habbo/groups/GuildMembersWindowCtrl.ts` 717 AS3 l., `habbo/utils/InfoText.ts`, 2 data classes, 5 events + 5 parsers, 7 composers), 2026-08-03.
  - The last large guild gap, and the one the previous slice had to stub: the members and
    pending-members links called `guildMembersWindowCtrl`, which returned null.
  - A paged grid of two-column rows: avatar, name, join date, and an action link whose meaning
    follows the row's rank — accept / give rights / remove rights / unblock — plus remove and
    (where `group.blocking.enabled`) block icons. Nothing is applied locally: every action is
    fire-and-forget, and the server answers with `GuildMembershipUpdated` (3477), which patches the
    single row, or `GuildMemberMgmtFailed` (1735), which alerts and re-runs the search.
  - Twelve headers registered, all read from WIN63's registry and corroborated against the emulator,
    whose handlers all exist: **events** `GuildMembers` 403, `GuildMembershipUpdated` 3477,
    `GuildMemberMgmtFailed` 1735, `GuildMembershipRejected` 595, `GroupMembershipRequested` 2087;
    **composers** `GetGuildMembers` 1337, `RejectMembershipRequest` 3200, `UnblockGroupMember` 2580,
    `RemoveAdminRightsFromMember` 3999, `AddAdminRightsToMember` 2152, `ApproveMembershipRequest`
    3505, `ApproveAllMembershipRequests` 1621.
  - The kick confirmation can now name its target: `HabboGroupsManager.onKickConfirmation()` resolves
    the user through the members page, which is the only place the client holds that name. That was
    the previous slice's remaining `TODO(AS3)`.
  - Two AS3 quirks preserved rather than corrected, both noted at the call site: `refreshUserEntry()`
    reads its "is this me" flag three lines **before** assigning it, so the admin column's visibility
    always tests against false; and `onAcceptAll()` is declared but never installed as a procedure,
    so the accept-all button is unbound in the original too.
  - `GuildMemberData` (the page payload — the singular name is the real one) and every field are
    recovered from the 2016 PRODUCTION build. Only the row class stayed obfuscated in **both** trees,
    so `GuildMemberEntryData` is DERIVED and says so; its five role constants are derived from what
    each AS3 getter compares against, since AS3 never references its own constants.

- ✅ **The group card and its window** (`habbo/groups/GroupDetailsCtrl.ts` 306 AS3 l., `DetailsWindowCtrl.ts` 120 AS3 l., 4 new message classes), 2026-08-03.
  - Reported as "the guild side does nothing when I click Manage". Two `${group.manage}` links
    exist in the layouts: `group_room_info.xml`'s button, which was wired, and `group.xml`'s, whose
    controller had never been ported — so that half of the guild UI could not be built at all.
  - `GroupDetailsCtrl` is the card (badge, description, counts and the link strip: base room, forum,
    members, pending members, manage, delete, buy furni, hottest groups); `DetailsWindowCtrl` is the
    frame around it, opening only on details replies that carry `openDetails` so a reply fetched for
    the toolbar panel never pops a window. Both are now fed by `HabboGroupsManager.onGroupDetails()`
    / `onGroupDeactivated()`, which previously carried a `TODO(AS3)` for exactly this.
  - Leaving a group turned out to need a flow nobody had ported: AS3 routes it through
    `handleUserKick(ownAvatarId, groupId)`, which sends **`GetMemberGuildItemCount` (781)**, waits
    for **`GuildMemberFurniCountInHQ` (1402)** and only then confirms and sends **`KickMember`
    (3156)**. All three were missing from the registry, along with **`DeactivateGuild` (2725)** for
    the delete link. Headers taken from WIN63's own registry (`_SafeCls_2046.as` lines 1138/1241/926/855)
    and corroborated against the emulator, whose comment on 3156 records that 781 — the furni-count
    request — was once mistaken for the kick itself.
  - Deferred with `TODO(AS3)`: `GuildMembersWindowCtrl` (717 l.), so the members and pending-members
    links no-op and the kick confirmation cannot name the target user. Leaving, the only path the
    ported UI reaches, is unaffected.

- ✅ **Window system realigned on the 2026 (701) tree**, 2026-08-03.
  - Prompted by "the further we go, the more small problems appear everywhere". The skeleton was
    never the problem: `WindowController` has all 153 AS3 members, and `WindowParam` (55 values),
    `WindowState` and `WindowStyle` match **bit for bit** — only `WindowType` 62 is absent. Every
    defect was inside a method body, and several came from **mixing revisions**.
  - **`ItemListController.process()` was win63_version's, not 2026's.** The 2023 build answers
    `WME_WHEEL`/`WME_WHEEL_HORIZONTAL` *and* `WME_DOWN/MOVE/UP` drag-scroll; the 701 build narrowed
    it to the two wheel types. Drag-scroll and its `enableScrollByDragging` / `disableAutodrag` /
    `stopDragging` API are gone — 2026's `IItemListWindow` declares none of them. 4 external call
    sites updated (`FurniView`, `BlockResultsView`, `RoomChatWidget` ×2).
  - **`MouseEventProcessor` revived a loop that is dead in AS3.** `process()` walked the ancestor
    chain calling `IInputProcessorRoot.process()`; AS3 guards that walk with `while(_loc6_ && …)`
    where `_loc6_` is still `false` (lines 230 / 272-285), so it never runs. Combined with the point
    above, every mousedown/mousemove over a list child drove the parent list's drag-scroll — even
    when the child had already handled the event. Removed.
  - **`WindowUtils.disableSection()`: three special cases restored.** The `TODO(AS3)`s claimed
    `_SafeCls_2013/2254/2326` were unidentifiable; they are empty marker interfaces with exactly one
    implementor each — `ButtonController`, `BorderController`, `BackgroundController`. AS3 skips the
    whole recursion/blend block for buttons (they render their own disabled state), and force-blends
    borders and backgrounds regardless of graphic-context ownership. A `BackgroundController` also
    carries its opacity in the **top byte of `color`**, not `blend`, so disabled sections' backgrounds
    never dimmed. 42 call sites.
  - **Horizontal wheel was dead end to end.** `ItemList`/`ItemGrid` handled `WME_WHEEL_HORIZONTAL`
    but nothing produced it: no constant, no conversion case, no source. Added
    `WindowMouseEvent.WHEEL_HORIZONTAL`, the `mouseWheelHorizontal` mapping, and axis resolution in
    `App.ts::_onWheel` (the DOM folds both axes into one `wheel` event where Flash raised two).
  - **`ScrollableItemListWindow.set isPartOfGridWindow()` wrote to `disableAutodrag`.** A
    copy-paste bug, not a port decision — AS3 assigns `_itemList.isPartOfGridWindow`. The flag is
    what makes a list inside a grid decline the wheel, so nested lists both consumed it.
  - **`PropertyStruct.withValue()` lost the `Array` comparison** → every array-typed write allocated
    a new struct and invalidated the window. `toString()` regained `Point`/`Rectangle`.
  - **Touch mode no longer masquerades as mouse.** `INPUT_MODE_TOUCH` built a `MouseEventQueue` +
    `MouseEventProcessor`. Ported `TabletEventQueue`/`TabletEventProcessor` — and note 2026's
    `process()` is a deliberate **no-op** that drains the queue.
  - **275 files traced to `sources/win63_2026_crypted_version/`, a tree that no longer exists.**
    Rewritten to `WIN63-202607011411-782849652`; 1387 of 1410 distinct paths now resolve to a real
    file. That unverifiability is how the `disableSection` "not identifiable" TODOs survived —
    `grep implements` answers them in seconds against the tree actually on disk.
  - **Still open, deliberately:** `MouseEventProcessor` is a faithful port that **never runs** —
    nothing feeds `WindowContext.inputEventQueue`; `App.ts` reimplements dispatch (~370 lines) with
    no bubbling, no `WME_UP_OUTSIDE`, no `ignoreMouseEvents` / `ROUTE_INPUT_EVENTS_TO_PARENT` /
    disabled-state gate, and no cursor-by-state. `WME_CLICK_AWAY` is produced only inside that dead
    processor, so the click-outside close of 8 views (own-avatar and pet menus, chat selectors,
    `ChatStyleSelector`) never fires. Also open: 193 traces to `win63_2021_version` and 60 to
    `win63_2023_version`, both non-existent trees — not remapped, because guessing the target is
    exactly the unverified change that caused this. And 23 traces name classes obfuscated in 2026.

- ✅ **vortex-glaze: multi-selection, zoom, snapping, tree drag & drop, variables, palette**, 2026-08-03.
  - Editor tooling only — no AS3 counterpart; the feature set was picked by comparing against
    [Clove](https://github.com/iSetht/clove), a React/Electron Habbo-layout editor, and taking what
    Glaze lacked. What was deliberately *not* taken: its TSX export (Nitro-only), its `.clove.json`
    project format (Glaze saves the layout XML itself, which is better) and its generated
    `windowTypes.json` catalogue (the port has the real `ThemeManager`).
  - **Canvas**: Ctrl/Shift-click and rubber-band selection, moves/nudges/deletes/clones over the
    whole selection, align against the selection's bounding box (a single node still aligns against
    its parent, as before), distribute over the selection with the old
    "children of the selected container" as the fallback, Alt-drag to duplicate, and 50–400 % zoom.
  - **Zoom without an engine change**: everything composites into one buffer, so the layer repaints
    the canvas background over the centre — nothing else is drawn there — and blits
    `renderWindowSnapshot(root)` through the zoom transform, cached until the renderer or the editor
    reports a change. Forwarded pointer coordinates are mapped through the inverse transform and
    *dropped* when they fall outside the content area, so a mapped point can never hit a chrome panel.
  - **Snapping** (`canvas/SnapGuides.ts`): a drag aligns to its siblings' and its parent's edges and
    centres within 6 screen px, with the guide lines painted while it holds; resize snaps the moved
    edge. Toggled from the toolbar.
  - **Hierarchy drag & drop**: before/inside/after by pointer position within the row, auto-expand
    after 400 ms over a collapsed container, drop past the last row = move to the root, plus
    Ctrl/Shift range selection and greying for nodes hidden by an ancestor. `reparent()` had been in
    `StructuralOps` since the beginning with no caller.
  - **Inspector**: a colour swatch that opens a generated swatch-grid picker (hex field kept, still
    authoritative), and the `<variables>` group — edit, add and remove, with add creating the
    `<variables>` block a palette-created node does not have.
  - **Palette**: a popup listing 28 window types, each previewed by a **real instance of that type
    built inside its row**, so no thumbnail machinery exists at all.
  - **Two real bugs found while verifying in the browser** (not by reading):
    - Edited `<var>` values silently never reached the saved XML when the engine serializer had
      emitted its own `<variables>` for that node — `injectVariables` skipped any node that already
      had a block. The editor's model now replaces it; it is the one seeded from the source XML.
    - **Every editor input field ate `${localization.key}` values and showed empty**, because
      `TextController.set text` diverts a `${…}` caption to the localization listener. Editing a
      localized `caption` therefore overwrote the key with an empty string. All editor inputs now
      ship `display_raw="true"`, which `TextFieldController` already honoured.
  - Both panels are `scrollable_itemlist_vertical` now (Flags and Variables were simply unreachable
    below the fold), which is what widened them to 318 / 368 px.

- 🐛 **vortex-glaze: four defects the first pass surfaced**, 2026-08-03 (same day, found by using it).
  - **Save wrote files nothing could read.** `serializeLayout` returned
    `WindowParser.windowToXMLString(root)` — the window element *alone* — so the saved file's root
    was a bare `<border>`/`<container>`. `parseWindowLayoutXml` takes a `<layout>` or a `<window>`
    root and returns `[]` for anything else, so every layout Glaze ever saved was silently
    unloadable and reopening one fell back to the bundled original. It now emits the
    `<layout><window>…</window></layout>` envelope, reusing the open layout's own source document so
    its attributes and `<sharedvariables>` survive, and it writes back the root's **authored**
    position — `openLayout` centres the root on the canvas, and that canvas coordinate was being
    saved into the file.
  - **The editor read layouts from the bundle and wrote them to the sources**, so even a correct
    save came back as its old self on the next page load. `GET /glaze/layouts` (dev middleware) now
    returns every source layout newer than `assets-xml.bundle` — normally none — and `GlazeBoot`
    re-registers them over the bundle.
  - **Popup close buttons did nothing.** The three layer-3 popups listened for `WE_CLOSE`/`WE_CLOSED`,
    which only `DropBaseController` emits; a frame's header builds an ordinary `closebutton` named
    `header_button_close` whose click bubbles to the frame's procedure, exactly as
    `InventoryMainView` handles it. Shared now in `ui/windows/PopupChrome.ts`, along with clamping a
    popup to the canvas so a short window cannot push its title bar off-screen.
  - **The theme dropdown claimed every window was Volter.** It listed four hard-coded themes while
    `ThemeManager` knows eight: `Misc` was missing from Set Theme, and any window whose theme is one
    of the virtual groupings (`None`, `Icon`, `Legacy border`) fell back to index 0. Set Theme offers
    the five real themes; the Property Editor lists whatever `getThemes()` reports.
  - Also: the Image Gallery set the **canvas background** whenever the selection was not a bitmap —
    it now inserts a `static_bitmap` sized to the image, which is what an image library is for; and
    the toolbar reflows instead of laying its last buttons past the right edge (below ~1700 px,
    Image Gallery and Widgets were unclickable), with the panels' lists finally following the
    frame height.

- ✅ **Clothing-change furniture (`RWE_CLOTHING_CHANGE`)**, 2026-08-02.
  - `ClothingChangeFurnitureWidget` + `FurnitureClothingChangeWidgetHandler`, plus the two
    widget classes they pass between them (`RoomWidgetClothingChangeUpdateEvent`,
    `RoomWidgetClothingChangeMessage`), none of which existed. The `boygirl` layout was shipped.
  - **Half of it works and half cannot**: the boy/girl chooser opens (rights-gated, as AS3 gates
    it) and picking a side resolves the furni's `furniture_clothing_boy`/`_girl` outfit with
    AS3's two hard-coded defaults — but `IRoomWidgetHandlerContainer` exposes no `avatarEditor`,
    so the editor that outfit is meant to load into is unreachable. Written as a `TODO(AS3)` at
    the exact method, with `saveFigure()` kept as the listener member it implements.

- ✅ **The effect-box open dialog**, 2026-08-02.
  - `EffectBoxOpenDialogView` (130 l.) — the simplest confirmation in the set: one layout,
    three buttons, a `UseFurniture` on OK. Its layout `effectbox_xml` was already shipped.
  - The room engine already emitted `RETWE_REQUEST_EFFECTBOX_OPEN_DIALOG` and
    `FurnitureContextMenuWidgetHandler` listened for none of it; the listener now sits beside
    its mystery-box sibling, **owner-gated** where the mystery box deliberately is not (the
    non-owner is the person putting a key in, the effect box is opened by whoever owns it).

- ✅ **Room-link furniture (`RWE_ROOM_LINK`)**, 2026-08-02.
  - `FurnitureRoomLinkWidget` (empty in AS3 too — the handler does everything) +
    `FurnitureRoomLinkHandler` (204 l.). Clicking a room-link furni asks the server for the
    target room's name and owner (`GetGuestRoom`), then confirms *which* room and *whose*
    before navigating, so the furni cannot teleport anyone silently.
  - Every message it needs was already ported (`GetGuestRoomMessageComposer`,
    `GetGuestRoomResultMessageEvent`); only the widget and handler were missing.
  - Two AS3 details kept: the link is read from the `furniture_data` map first and falls back
    to the `furniture_internal_link` model string (older furni), and `%%room_name%%` /
    `%%room_owner%%` are substituted by hand because the localisation carries them as literals.
  - **It also unblocks a documented gap**: reaching `context.createLinkEvent()` needs the room
    engine cast to its `Component` base, exactly as AS3 does it. `InfoStandWidgetHandler` has a
    TODO saying its wired-inspect actions cannot be routed for want of that cast — the shape is
    now written down in this handler.

- ✅ **Purchasable clothing: the dialog, and the two-step redeem it feeds**, 2026-08-02.
  - `PurchasableClothingConfirmationView` (217 l.) + `RedeemPurchasableClothing` (3637), the
    last `log.warn` route in `FurnitureContextMenuWidgetHandler` now gone.
  - **`redeemPurchasableClothing()` already existed — as a stub that sent nothing.** It armed
    the pending fields and carried a TODO saying neither message was ported. Both are now:
    `FigureSetIdsMessageEvent` (1231) had been in the port all along, and the redeem composer
    was the missing half. Completed: it sends, remembers figure *and* gender (the stub dropped
    both), and the new `onFigureSetIds()` applies the outfit only when the reply names this
    furni inside the freshness window.
  - **A stub is not a gap you can see from the outside.** This one type-checked, lint-passed and
    had a caller; the widget above it was the only thing missing. Found by reading the body,
    after a first attempt duplicated members that were already there.
  - Two documented shortfalls: `IHabboInventory.hasBoundFigureSetFurniture()` does not exist, so
    the dialog always opens (AS3 skips it for already-bound furni — one extra confirmation,
    never a wrong outfit); and the `avatar_preview` pane stays empty because
    `habbo/window/widgets`' avatar widget is unported.

- ✅ **The monsterplant seed: bubble, confirmation dialog, and the message between them**,
  2026-08-02.
  - `MonsterPlantSeedContextMenuView` + `MonsterPlantSeedConfirmationView` (278 l.) +
    `RoomWidgetUseProductMessage`, which existed in no form here. Both layouts
    (`use_product_widget_frame_plant_seed_xml`, `use_product_controller_plant_seed_xml`) were
    already shipped.
  - **Three separate stubs in `FurnitureContextMenuWidgetHandler` closed at once**, all of which
    named each other: `getWidgetMessages()` returned `[]` with a TODO saying the message class
    did not exist; `processWidgetMessage()` was a comment describing the `plantSeed()` call it
    could not make (both sides of which *did* exist); and `getFurniData()` — which the dialog
    reads the furni's category and localised name through — was simply absent. The seed route in
    `processEvent()` was the `log.warn` on top.
  - Two AS3 oddities kept: `updatePreviewImage()` takes the rendered furni bitmap and ignores
    it, filling both slots from the layout's own `bitmapAssetName` instead (passing the render
    in would change what the dialog shows), and the category switch is written as
    `category - 19`, so the offset is derived and named rather than left as a magic number.
  - Remaining in `contextmenu/`: `PurchasableClothingConfirmationView` (217 l.) and
    `GuildFurnitureContextMenuView`.

- ✅ **Two more furni context bubbles: generic-usable and random-teleport**, 2026-08-02.
  - `GenericUsableFurnitureContextMenuView` and `RandomTeleportContextMenuView` — siblings of
    the friend-furni bubble ported earlier today, same shape with a different layout and a
    `RWFAM_USE` action message instead of a direct composer. Both layouts
    (`generic_usable_menu`, `random_teleport_menu`) were already shipped.
  - `FurnitureContextMenuWidgetHandler`'s `CONTEXT_MENU_RANDOM_TELEPORT` case was a `log.warn`;
    it now calls the new `showRandomTeleportContextMenu()`. That method deliberately does *not*
    call `show()` on its view where its siblings do — AS3 omits it, and the omission is copied
    with a note rather than "fixed".
  - **Left together on purpose**: `MonsterPlantSeedContextMenuView` and
    `PurchasableClothing`'s route both end in a confirmation dialog
    (`MonsterPlantSeedConfirmationView` 278 l., `PurchasableClothingConfirmationView` 217 l.)
    that is not ported. Porting the bubbles alone would have given each a Use button that opens
    nothing — the two menus and their two dialogs are one slice, not four.

- ✅ **The build-mode stacking-height widget (`RWE_CUSTOM_STACK_HEIGHT`)**, 2026-08-02.
  - `CustomStackHeightWidget` (499 l. AS3) + handler (`_SafeCls_3852`, name derived), and two
    composers: `SetCustomStackingHeight` (3045, which the emulator has as
    `SetCustomStackingHeightEvent`) and `MoveFurnitureToAdjacentHeight` (3315, which **nothing**
    has — the two nudge arrows send into the void, noted at the registration).
  - **The throttling is the substance of this widget, not decoration.** Dragging the knob fires
    a relocation event per pixel, so a live send is *queued* at most once every 30 ms and a
    final one is forced on release; `canApplyLiveHeight()` then refuses to let the server's echo
    overwrite the field while the player is still dragging or mid-edit. Ported whole, including
    the detail that the final-send flag survives while the knob is held so a release always gets
    its send. AS3's `Timer` is a one-shot `setTimeout`, `getTimer()` is `performance.now()`.
  - The handler is the only one in the set that polls: it registers itself as an update listener
    *from its own `container` setter* and mirrors the furni's real z into the widget every frame
    it changes, so the slider follows a height somebody else moved.
  - Its `validateRights()` is deliberately wider than its siblings': owning the *furni* is enough
    here, even with no rights over the room.

- ✅ **`FriendFurniContextMenuView` — `friendfurni/` is now 7/7**, 2026-08-02.
  - The bubble over a friendship furni, with its own `friendfurni_menu` layout and a single Use
    button. That button sends the generic `UseFurnitureMessageComposer`, which is what asks the
    server to start the lock confirmation the previous slice ported.
  - **It was already half-wired and unreachable**: `FurnitureContextMenuWidgetHandler` routed
    `CONTEXT_MENU_FRIEND_FURNITURE` to a `log.warn` for an unported view, and
    `FurnitureContextMenuWidget`'s constructor carried it in a TODO list. Both now build and
    call it, through a new `showFriendFurnitureContextMenu()` that mirrors its mystery-box
    sibling line for line.
  - `onMinimizeHover` is defined locally rather than inherited: AS3 has it `protected` on
    `ContextInfoView`, but this port folded that method into a *private* `onMinimizedProc`
    belonging to the minimised bubble, so the normal window's copy has to live in the subclass —
    the same shape `MysteryBoxContextMenuView` already uses. Noted at the method rather than
    silently duplicated.
  - `_SafeStr_4722`, cleared at the end of `updateWindow()`, is this port's `_mouseOver`;
    stated where it is assigned, since the AS3 name says nothing.

- ✅ **The friendship-furni lock confirmation (`RWE_FRIEND_FURNI_CONFIRM`)**, 2026-08-02.
  - `FriendFurniConfirmWidget` + `FriendFurniConfirmWidgetHandler` (AS3 `_SafeCls_2535`, name
    derived), and **four messages that existed in no port**: `FriendFurniStartConfirmation`
    (2716), `FriendFurniOtherLockConfirmed` (3451), `FriendFurniCancelLock` (267) and the
    `FriendFurniConfirmLock` composer (3318). All four names are derived; only the composer had
    an emulator counterpart to match against.
  - **The emulator sends none of the three incoming ones**, so against this server the panel
    never opens — the client half is complete and inert, noted at the registration site rather
    than discovered later. Whoever adds them server-side will find the client already listening.
  - This is the only handler in the set driven purely by the network: `type` returns `''` and
    `getProcessedEvents()` is empty, because nothing routes to it by widget type — it registers
    its own messages the moment `RoomDesktop` hands it the connection, which is why the AS3
    switch case sets `connection` right after constructing it.
  - Opening a second panel while one is up answers **false** to the first rather than dropping
    it, so the other player is not left waiting on a dialog that is gone. AS3's behaviour, kept
    with the reason at the method.

- ✅ **Friendship-furni engravings (`RWE_FRIEND_FURNI_ENGRAVING`)**, 2026-08-02.
  - `FriendFurniEngravingWidget` + `FriendFurniEngravingView` and its three one-line subclasses
    (love lock, wild west, Habboween — they differ by layout only), plus the handler, whose AS3
    class is `_SafeCls_2994` in every tree and whose name is therefore **derived** from the
    widget it drives. All three layouts already shipped.
  - The plaque's contents are a `StringArrayStuffData` read off the room object's model, not
    requested: slots 1 and 2 are the names, 3 and 4 the figures, 5 the date. The right-hand
    avatar is turned to direction 4 so the pair faces inwards; a figure still rendering comes
    back as a placeholder and is filled in later by `avatarImageReady()` — which is only
    reachable now that the container exposes `avatarRenderManager` (see the mannequin entry).
  - `close(stuffId)` takes an id rather than closing unconditionally, so a second engraving
    opening cannot close the first from under it. Kept, with the reason at the method.
  - Left for the next slice: `RWE_FRIEND_FURNI_CONFIRM` (`FriendFurniConfirmWidget`,
    `FriendFurniContextMenuView`), which is a different widget type with its own handler.

- ✅ **The mannequin (`RWE_MANNEQUIN`), and the avatar-render access two widgets were missing**,
  2026-08-02.
  - `MannequinWidget` (523 l. AS3) + its handler: one frame window whose content is swapped
    between five layouts (owner editor, name-and-save, visitor wear, club upsell, wrong gender),
    all five already shipped as assets. Plus `SetMannequinFigure` (2301) and `SetMannequinName`
    (606), neither of which existed in this port — both AS3-verified in the emulator's headers,
    and the names are derived from there. `wear_button` reuses the generic
    `UseFurnitureMessageComposer` (3353), as AS3 does.
  - **`IRoomWidgetHandlerContainer` never exposed `avatarRenderManager`**, which AS3's does — the
    gap `ChatWidgetHandler` documented and the present widget hit hours earlier. Added to the
    interface, `RoomDesktop` and `RoomUI` (a new optional `IID_AvatarRenderManager` dependency,
    pushed to live desktops like the others). The mannequin cannot work without it; the present
    widget's sender face can now be revisited.
  - **`resolveClubLevel()` did not exist either.** Ported onto `AvatarRenderManager` from
    `_SafeCls_582.as`: the second loop is the non-obvious half — a part type the figure *omits*
    can still cost club level through `optionalFromClubLevel()`, which is what makes "no hat" a
    club look for a gender with a mandatory default.
  - **`ITextWindow.italic` was declared `readonly`** while `TextController` has had a setter all
    along, so `SearchView` was casting through `any` to reach it. Declared writable.
  - Two AS3 blits are assignments here (`updatePreviewImage`, and the present widget's
    `showIcon` earlier): compositing two `ImageBitmap`s needs a canvas round-trip, and the layout
    already carries the backdrop. Stated at both methods.

- ✅ **The gift box (`RWE_FURNI_PRESENT_WIDGET`)**, 2026-08-02.
  - Second furniture slice of the day: `PresentFurniWidget` (999 l. AS3),
    `FurniturePresentWidgetHandler`, `RoomWidgetPresentDataUpdateEvent`,
    `RoomWidgetPresentOpenMessage`, the `RETWE_REQUEST_PRESENT` translation, the handler and
    factory cases, and eager creation in `RoomUI`.
  - **`changeObjectModelData()` did not exist in this port at all** — AS3 declares it on
    `IRoomEngine` and the present handler is what needs it (opening a gift sets
    `furniture_disable_picking_animation` so the box does not replay its pickup animation).
    Ported onto `RoomEngine` through the object's own event handler, the same route
    `updateObjectFurniture()` takes. **`selectRoomObject()` was `private`** where AS3 has it
    public on the interface, so no widget could highlight anything; made public and declared,
    along with `getRoomObjectCount()`/`getRoomObjectWithIndex()` which existed but were not on
    `IRoomEngine`.
  - **Three gaps left visible as `TODO(AS3)` rather than invented.** `IHabboInventory` exposes
    none of `getFloorItemById`/`getWallItemById`/`placePetToRoom`/`removeUnseenFurniCounter`/
    `removeUnseenPetCounter` (they live on `IPetsModel`/`FurniModel`), so *Place in room* closes
    the dialog without placing — an inventory-side job. `IRoomWidgetHandlerContainer` still does
    not expose `avatarRenderManager` (the gap `ChatWidgetHandler` already documents), so the
    sender's face does not render. And this port has no `HabboTracking` singleton for the two
    catalog click logs.
  - `showIcon()` assigns the bitmap instead of blitting it into a centred slot: there is no
    `BitmapData.copyPixels` here, and centring an `ImageBitmap` inside another needs a canvas
    round-trip. Stated at the method.

- ✅ **The moodlight (`RWE_ROOM_DIMMER`), and a furniture-widget count that was wrong by 20 files**,
  2026-08-02.
  - This doc's priority #1 said `furniture/` was 0/54. It was **20/54** before this slice — ten
    clusters were already in. The habit that produces this is now twice-recorded (see the messenger
    entry below): **measure, then quote.** The remaining list is enumerated in Current Priorities
    so the next pass starts from files rather than from a number.
  - Full vertical slice: the 5 `dimmer/` classes (1108 l. AS3), `FurnitureDimmerWidgetHandler`,
    3 widget events + 3 widget messages, the `RWE_ROOM_DIMMER` cases in `RoomDesktop.createWidget()`
    and `RoomWidgetFactory`, the `RETWE_REQUEST_DIMMER` → `RWFWM_MESSAGE_REQUEST_DIMMER` translation
    in `RoomDesktop`, and eager creation in `RoomUI` alongside the other furni widgets.
  - **Everything below the widget already existed and was unreachable**: `RoomSession`'s three
    dimmer senders, `RoomEngine.updateObjectRoomColor()`, `RoomSessionDimmerPresetsEvent`,
    `RoomEngineDimmerStateEvent`, `RoomEngineToWidgetEvent.REQUEST_DIMMER` (emitted at
    `RoomEngine.ts:5553`) and both XML layouts + all six bitmaps. Nothing called any of it.
  - **A signature that would have sent the wrong packet.** `sendRoomDimmerSavePresetMessage()` was
    declared `(itemId, presetId, type, color, light, brightness)` where AS3 is
    `(presetNumber, effectTypeId, color, brightness, apply, objectId)`. The body sent a correct
    packet *for the TS order*, and it had no callers — so writing the handler against AS3's order
    (the natural thing to do) would have sent the object id as the preset number. Realigned on AS3,
    same as `MainView.addConsoleMessage()` earlier the same day. Both were found the same way:
    reading the AS3 call site before the TS signature.
  - Two small AS3 oddities ported as-is with the reason stated: `validateBrightness()` is
    `return true` (kept as the gate it is at all four call sites), and
    `RoomWidgetDimmerUpdateEvent.getPreset()` bounds-checks against `.count`, which does not exist
    on an Array — corrected to `.length`, which changes nothing for its one caller and stops an
    out-of-range read reaching the widget.
  - The colour grid tints one shared bitmap through `window.color` instead of AS3's per-cell
    `ColorTransform` on a clone: `BitmapDataRenderer.tintBitmap()` is that same multiply, one layer
    down.

- ✅ **Sound: the client had none at all, and now has effects**, 2026-08-02.
  - `habbo/sound` was 0/29 and `IID_HabboSoundManager` was announced by nobody, so every
    `playSound()` in the port — the messenger's two, the toolbar's, the room engine's
    `REPSE_PLAY_SOUND` — resolved to a null manager and returned. Ported the effects half:
    `HabboSoundManagerFlash10` (585 l.), `HabboSoundBase`, `HabboSoundWithPitch`, the 4
    interfaces (`IHabboSound`, `IHabboSoundManager`, `IHabboMusicController`,
    `IPlayListController`, all four names recovered from PRODUCTION's unobfuscated tree) and
    both enums. Attached in `VortexMain` after notifications, with the asset library AS3's
    `HabboSoundManagerFlash10Com` SWF supplies.
  - **The mp3s were in the dump and in no pipeline.** `src/_assets/` carries 21 of them; the
    importers only ever handled png and xml. New `tools/import-crypted-sounds.mjs` (sibling of
    `import-crypted-images.mjs`, same `lib/cryptedManifest.mjs` resolution) names each one after
    its `*Com.as` field — `1466__SafeCls_1517.mp3` → `sound_console_new_message.mp3` — because
    that field is the exact string `getSoundByAssetName()` asks for. 21/21 resolve; none is
    ambiguous. `App.ts` decodes them at boot and registers each as a `SoundAsset`, which already
    existed with `sound/mp3` wired to `SoundFileLoader` and had never been used.
  - **Flash → Web Audio.** `Sound` is an `AudioBuffer`, `SoundChannel` is an
    `AudioBufferSourceNode` + `GainNode` rebuilt per `play()` (source nodes are single-use, as
    Flash channels are), `SoundTransform(v)` is `gain.gain.value`. Flash's `loops` is a repeat
    count and Web Audio's `loop` is an unbounded flag, so the count is honoured by re-arming on
    `onended` — detaching the old handler first, or a looping channel re-arms itself over its
    replacement. `HabboSoundWithPitch`'s hand-rolled resampler is ported literally rather than
    swapped for `playbackRate`: nearest-neighbour is what the client actually sounds like.
  - **Two things that would have read as "sound is broken".** `_genericVolume` initialises to
    **0**, not 1 — nothing is audible until `AccountPreferences` (724) arrives, which is why the
    manager sends `GetSoundSettings` (541) from `init()`. And browsers keep an `AudioContext`
    suspended until a user gesture: `SoundContext` (TS-only, no AS3 counterpart — Flash has one
    implicit global mixer) resumes on the first pointer/key event and on every `play()`.
  - **An emulator bug found by porting the composer.** `SetSoundSettings` (3662) reads
    `(trax, furni, generic)` as its *constructor* arguments and pushes them **back to front**, so
    the wire is `[generic, furni, trax]`. `vortex-emulator`'s parser had been written from the
    signature and popped Trax first — turning the music volume down muted the interface instead.
    Fixed there in the same pass, with the reversal spelled out in both files' comments.
  - Not ported, and the reason the module is 10/29: `music/` (1688 l.), `trax/` (1495 l.),
    `furni/` (206 l.) and `events/`. `loadTraxSong()` warns and returns null, `musicController`
    stays null, and `updateVolumeSetting()` carries `TODO(AS3)`s where the furni playback manager
    would be told about the new volume.

- ✅ **The messenger's receive path: four handlers that read the wire and dropped it**, 2026-08-02.
  - The status table said `MainView` was unported and `_mainView` null. It is neither: the class is
    1854 TS lines against AS3's 1237, it is constructed in `onMessengerInit()`, and the friend list
    does send `MessengerInitMessageComposer` (3278), so the console has been building all along.
    **What was actually missing sat one level down** — `onNewConsoleMessage`, `onConsoleHistory`,
    `onRoomInvite` and `onInstantMessageError` were registered, reached, and returned after their
    `if(_mainView === null)` guard with nothing but a comment naming the call they were supposed to
    make. Every incoming chat message, history fragment, room invite and IM error was parsed off the
    wire and discarded. All four now delegate, and the two `!isOpen → playMessageReceivedSound()`
    branches came with them.
  - **`isOpen()` returned the wrong thing**, which is why those two sound branches could not have
    worked anyway: AS3 is `_mainView != null && _mainView.isOpen`, the port had only the null check,
    so the messenger claimed to be open from `MessengerInit` onward and a closed console would never
    have played the received-message sound.
  - **`MainView.addConsoleMessage()`'s parameter order did not match AS3** — `senderId` fifth and
    `secondsSinceSent` eighth, where AS3 has them at 5 and 8 the other way round. The body used the
    names correctly, so nothing was wrong until the first caller existed; had the handler been
    written against the TS order, every message would have carried the sender id as its age. Aligned
    on AS3 before wiring, as the source of truth demands.
  - **Two of the five events the old TODO called unavailable now exist**, and both are wired:
    `AccountPreferencesEvent` (724) restores `roomInvitesIgnored`, and `HabboGroupDetailsMessageEvent`
    (2847) completes the follow-a-friend-into-their-group's-room hop via
    `OpenFlatConnectionMessageComposer` — the room id only arrives with that answer, which is why AS3
    finishes the hop in the handler rather than at the click. The MiniMail pair stays out: neither
    message is in this port's set.
  - **The wire was already correct end to end**, checked rather than assumed: all five headers agree
    with `_SafeCls_2046.as` *and* the emulator (1590/468/933/3501/3194), and
    `NewConsoleMessageMessageSerializer` writes the tagged-union body (`int` discriminator, then text
    or habbicon id) that `ChatMessageContent.parse()` reads. Nothing server-side needed changing.
  - **MiniMail, same day.** Both events existed in AS3 and in the emulator's headers and in
    neither client parser set: `MiniMailNewMessageEvent` (3884, empty payload — the arrival *is*
    the increment) and `MiniMailUnreadCountEvent` (74, one int, authoritative). Registered by
    `HabboMessenger` only behind `getBoolean('client.minimail.embed.enabled')`, as AS3 gates them,
    and both dispatch through `MiniMailMessageEvent` — whose `NEW`/`UNREAD` constants are now
    `NEW_MESSAGE_NOTIFICATION` (AS3's own name) and `UNREAD_MESSAGE_COUNT` (derived; the
    identifier is `_SafeStr_11142` in every tree, only the value `MMME_unread` is recovered).
    The four new class names are derived too, and say so at the declaration — they match
    `vortex-emulator`'s `Headers.cs`, which had already AS3-verified both ids.
  - **What is left is not messenger work.** `MainView`'s 3 habbicon-picker TODOs need
    `messenger/habbicons/` (1399 l.), and that needs `catalog/habbicons/` (5089 l.),
    `habbicons/assets/` (938 l.) and the `_SafePkg_1920` DTOs — 7.4k lines of AS3 and a whole
    catalog subsystem, for a picker whose own emulator serializer notes it has "no habbicons to
    send". Costed, not started.

- ✅ **Three requested messages closed: BadgeReceived, the DailyTasks trio, and the user-move
  composers**, 2026-08-02.
  - **`BadgeReceived` (2840) had no client side at all** — the emulator sends it
    (`BadgeReceivedEventMessageComposer`, from `InventoryGrain.Furni`'s badge grants) and the client
    had no event, no parser and no registration, so every granted badge was dropped on the floor.
    New `BadgeReceivedEvent` + `BadgeReceivedEventParser` (`int badgeId, string badgeCode, int
    ownerCount, int badgeRarityId` — matches the emulator's serializer field for field), registered
    at 2840, plus **both** AS3 consumers, which are two different classes registering the same
    event: `HabboInventory.onBadgeReceived()` (`BadgesModel.updateBadge()` + `updateView()`) and
    `NotificationMessageHandler.onBadgeReceived()` (the `notification.new.badge` toast, linking to
    `inventory/open/badges`). The badge *bitmap* is still not attached to the toast: this port has
    two representations of a badge image (`HTMLImageElement` from `BadgeImageManager`,
    `ImageBitmap` on `HabboNotificationItemStyle.icon`) — `requestBadgeImage()` is still called
    because it starts the load, and the icon rides on the badge-code parameter meanwhile
    (`TODO(AS3)` at the call site).
  - **Two of the emulator's three DailyTasks headers were wrong, and one of them collided.** WIN63's
    own registry gives 1824 / **2506** / 1065 (ActiveList / TasksAdded / TaskUpdate);
    `Headers.cs` had 2507 / **1762** / 1065, inherited from the pre-Vortex header table. 1762 is
    really `CfhTopicsInit`, which the same file *also* declares correctly — two constants on one
    wire id. Headers corrected with their provenance; client side now carries all three events,
    their parsers, and the two DTOs the list messages nest (`long taskId, string taskCode, string
    questTypeCode, bool isBonus, string imageVersion, string catalogName, int requiredRepeats, int
    repeats, byte status, int secondsLeft`, then a reward array of `short productItemTypeId, string
    rewardTypeId, string extraParams, int amount`). **`DailyTaskData`/`DailyTaskRewardData` and the
    three status constants are derived names, said so at their declarations** — those types are
    obfuscated in every available tree. `TODO(AS3)`: `habbo/quest/dailytasks/` (the controller and
    its three views) is unported, so nothing subscribes yet; the emulator has no daily-task domain
    logic and no composers either, so the ids are a corrected wire table, not a working feature.
  - **`MovePetMessageComposer` was registered but constructed nowhere** — the real gap was
    `sendMoveUserObjectMessage()`, which the port had left as a `TODO(AS3)` + `log.warn`. Ported for
    real, including its asymmetry: the `rentable_bot` branch passes the raw room-object index to
    the new `MoveBotMessageComposer` (**1295**, `_SafeCls_2801`), the `monsterplant` branch resolves
    that index to the pet's `webID` through `userDataManager` first. Every category-100 path that
    fed it is now open: `modifyRoomObject()`'s rotate case, `confirmObjectMove()`'s drop, the
    SHIFT-rotate click and the ALT-drag mouse-down (AS3 gates that last one on *type*, not
    category — `_SafeCls_1821.as:1063`). Wall furni (20) stays excluded: `confirmObjectMove` still
    has no wall-move composer to finish a drag with.
  - **A rotate would have been a silent no-op without a second fix.** `getValidRoomObjectDirection()`
    had only the `furniture_allowed_directions` branch; AS3 picks the list by object type, reading
    `pet_allowed_directions` for a monsterplant (already written by `PetLogic`) and a fixed
    eight-way `USER_ALLOWED_DIRECTIONS` for a rentable bot. Without them a plant resolved to its
    current direction and would have sent a rotation that changed nothing. Its no-model guard now
    returns AS3's 0 instead of the current direction, and two orphaned comment fragments that
    documented the old scope were removed.
  - Not verified in a running client: the bot half cannot be — the server has no bot entity
    (`docs/CLIENT-SERVER-ARCHITECTURE.md` §"Bots — Do Not Exist"). Typecheck and lint are clean.

- ✅ **Account creation wired to the routes the dump already declares**, 2026-08-02. The investigation
  is most of the value here: **avatar creation was already complete on both sides** and needed
  nothing. `AuthenticationOK` carries `suggestedLoginActions`; `vortex-emulator`'s
  `SSOTicketMessageHandler` sends `AVATAR_NAME_CHANGE` while the player has no `NuxCompletedAt` and
  `NEW_ROOM_SELECT` while it has no home room; `App.ts` hands both to `OnBoardingHcFlow`; and the
  loop closes on the server (`ChangeUserNameMessageHandler` → `MarkNuxCompletedAsync`,
  `SelectInitialRoom` → home room). All four onboarding composers have handlers.
  - **Only the registration screen was missing, and only the screen.** `/api/public/registration/new`
    is a declared AS3 route (`HabboWebApiMethod.REGISTER`), `HabboWebApiSession.register()` was
    already ported, `WebApiLoginProvider` already routed both its arms
    (`showSelectAvatar`/`showRegistrationError`), and the emulator already implemented the endpoint
    with rate limiting and tests. Nothing called it — the 701 client registers on the website.
  - **New `RegisterView` + `SCREEN_REGISTER` (5) + `ILoginContext.initRegister()` + a third button on
    `LoginView`**, all marked `TS-only`. This is the same screen a previous port invented and the
    2026-07-31 rebuild deleted — the difference is that it now drives real AS3 methods against a real
    endpoint instead of an invented protocol, and says so at every declaration.
  - **`showSelectAvatar()` stops being empty**: a fresh account owns no player (`RegisterAsync`
    inserts the account row alone), so it posts `createAvatar()` — the other real AS3 route
    (`POST /api/user/avatars`). From there the existing provider code takes over unaided: the
    refreshed list has exactly one avatar, which its `/api/user/avatars` arm selects outright, and
    ticket → connection → onboarding follow.
  - **The avatar is created unnamed on purpose.** Naming it belongs to the onboarding step the server
    is about to ask for, and that step owns the look editor too — asking for a name in the web form
    would ask twice. Two lines of emulator: `CreateAvatarRequest.IsValid` no longer requires a name,
    and `CreateAvatarAsync` assigns a GUID-bearing placeholder when it is blank (`Player.Name` is
    uniquely indexed, so a counter would race).
  - **Additional avatars, from the picker.** There is no CMS in this project, so `AvatarView` is the
    only surface an account can ever gain a second avatar from — and AS3's shortcut for a lone avatar
    (`if(_loc7_.length == 1) selectAvatar(...)`, `WebApiLoginProvider`) made that screen unreachable
    for exactly the accounts that need it. The shortcut is dropped (deliberate divergence, restorable
    in one block; `_autoLogin` is false in this build, so nothing is stranded), and a New avatar
    button posts the same nameless `createAvatar()`. Two port-side consequences handled: the picker
    now repopulates, so `populateAvatars()` removes the previous holders instead of stacking rows,
    and the button is disabled for the round trip since a double click would create an avatar that
    cannot be deleted from here.
  - Captions are English literals: no registration key exists in the `default_localizations*` embeds.
    The two field prompts reuse `${connection.login.email}` / `${connection.login.password}`.
  - **Not runtime-verified.** Both sides compile and lint; the chain was traced call by call, not run.

- ✅ **Login + onboarding: first pass with a human driving it**, 2026-08-01/02. The 2026-07-31 rebuild
  was verified headlessly on two screens; everything below was invisible to that check and surfaced in
  minutes of real use. Eight fixes, all in the canvas-2D runtime under `onBoardingHcUi/display/` or in
  a port-specific decision — **no ported AS3 logic was wrong**.
  - **The avatar editor drew nothing, in a loop.** `AvatarEditor.toBitmapData()` accepted
    `BitmapData`/`ImageBitmap`/`HTMLCanvasElement`, but `AvatarImage.getImage()`/`getCroppedImage()`
    return a PixiJS `Texture` over the OffscreenCanvas they composited into — so *every* thumbnail and
    every preview repaint fell through to the "unrecognised form" warning. Now unwraps
    `texture.source.resource` over `texture.frame`, plus an `OffscreenCanvas` branch.
  - **Typed text sat at the bottom edge of the input.** Flash counts one empty line in an empty
    `TextField`; the port's `measure()` returned no lines, so `textHeight` was 0 and
    `InputField.init()`'s faithful `y = int((background.height - _field.height) / 2)` put the field at
    15 instead of 4. Same root cause made an empty field 610×**0** and therefore unclickable, so the
    press hit the grey prompt behind it — which has no listener and is a *sibling* of the background
    sprite that does, so the click died there. This is the class of bug the "read the body, not the
    name" rule is about: the AS3 arithmetic was transcribed exactly and still misbehaved, because a
    primitive underneath it was unfaithful.
  - **Four separate defects in `Stage`'s text-entry bridge** (Flash focuses an editable `TextField`
    natively; AS3 therefore only assigns `stage.focus` from the sprite *behind* it, and
    `InputField.onInputClicked()` — the one handler that did — removes itself after the first click):
    the stage now focuses an input field it hit-tests on mouse down; `preventDefault()` stops the
    default action from taking the caret straight back off the `<input>` (this is why an input needed
    **two** clicks — the first only parked the element, the second landed on the element itself);
    re-assigning the already-focused field re-focuses the element instead of returning early; and the
    frame loop drops focus when the field is no longer a visible part of the display list.
  - **That last one was the "sometimes I click ten times and nothing happens".** Views are dismissed
    by hiding, not tearing down (`startRoomPicking()` sets `_nameArea.visible = false`), so the
    `<input>` stayed parked over an invisible field: a ~610×22 transparent element above the canvas,
    eating every click inside it — including on the room picker's thumbnails. And since it is the
    canvas that releases focus, the state could not repair itself while the user kept clicking in the
    band. Belt and braces: `mousedown`/`mouseup`/`mousemove` are now bound to the overlay too and
    routed through the display list unless the point is over the field it actually serves, so the
    overlay can never swallow a click even if mispositioned. `#vortex-login-input` is addressable
    from the console for the next time something like this is suspected.
  - **`Password field is not contained in a form`** — the shared `<input>` now lives in a
    `display: contents` `<form>` whose `submit` is cancelled (a single-field form submits implicitly
    on Enter, which would reload the page; Enter already has a job — `SsoTokenView` reads it).
    `autocomplete` is per-field (`current-password`/`username`) instead of a blanket `off`.
  - **`Cannot read properties of undefined (reading 'length')` in `_onInputKeyDown`.** `event.key`
    is not guaranteed: nothing in this repo dispatches a synthetic `keydown` (checked), so it comes
    from a password manager or similar extension, which became interested in the field the moment it
    became a real password field in a form. `TextFieldController.resolveCharCode()` already guards
    this in the engine; `Stage` now does too. The throw also swallowed Enter for that keystroke.
  - **`EnvironmentView`'s restored login button wore the wrong skin.** The 701 dump declares
    `_loginButton` and never builds it (see the divergence note in the file); the port built it as a
    bare `Button`, which is the STYLE_ILLUMINA skin no login screen uses. Now a green `ColouredButton`
    like every other confirm button here — matching the ticket button beside it on purpose, since the
    only other hitch skins are Cancel-pink and HC-yellow.
  - **`RoomPicker`: one dead render blocked the whole step.** AS3 listens for `"complete"` and
    nothing else, and `initView()` is only ever called from `_loadedCount == _numOfRooms`, so a
    single 404/CORS failure left the step permanently empty with no way out of the flow. Failures now
    count (deliberate divergence, logged); that room renders blank and is not clickable. Also
    `_selectedIcon.mouseEnabled = false` — `_infoPanel` is added after the holders, so the marker sat
    on the corner of the selected thumbnail and ate the press meant for it.
  - **`OnBoardingHcFlow.showErrorMessage()` stacked balloons.** AS3 calls `removeChild()` on the flow
    while adding to `_mainSprite` two lines later — in Flash that is `ArgumentError #2025` on the
    second error; here `removeChild` no-ops, so they piled up. Removed from the parent it was added to.
  - Still not runtime-verified: `AvatarView`, `WebCaptchaView`, `HitchNameChangeDialog`,
    `RandomAvatarCloudsAnimation`. The room-picking round trip is wired correctly end to end —
    composer 3267 / event 3624 agree across the WIN63 registry, `Headers.cs` and our maps, and
    `vortex-emulator` has the full handler/composer/serializer chain.

- ✅ **Login + onboarding rebuilt from the 701 dump**, 2026-07-31. The login flow had drifted into a
  redesign: two invented screens (`SCREEN_REGISTER`, `SCREEN_AVATAR_CREATE`) whose AS3 traces pointed
  at files in no dump, an invented `isSsoTokenEnabled()` opening on Login where AS3 opens on the
  ticket screen, a split-screen SCSS layout, English strings hard-coded into the views, and
  `loadImages()` fetching one of the two landing illustrations. Replaced with the source.
  - **`onBoardingHcUi` ported** (15 widgets) onto a small canvas-2D display list under
    `onBoardingHcUi/display/` — the port's UI is canvas 2D (`WindowRenderer`), and these widgets are
    bitmap blitting, so PixiJS was the wrong target. `Scale9Shape` reproduces Flash's live
    `scale9Grid` (assigning `width` re-slices instead of scaling), which every button skin depends on.
  - **`login/` rewritten** — `LoginFlow` (4 screens, `init()` opens on `SCREEN_SSO_TOKEN`),
    `EnvironmentView`, `LoginView`, `AvatarView`, `SsoTokenView`, `Background`, `ImageLoader`,
    plus `WebCaptchaView`, which was previously a stub returning null.
  - **`ILoginViewer` restored to its 17 members** and `WebApiLoginProvider` rewritten against the
    web-API session instead of speaking `fetch()` itself; `register`/`createAvatar`/`checkName`
    (invented) are gone. `ICaptchaView`/`ICaptchaHandler` added.
  - **`loadDefaultEmbedLocalizations()` implemented** — it was a stub returning false, so every
    login caption rendered as its raw `${key}`. The twelve `default_localizations*` embeds now ship
    in `assets/configurations/`.
  - **Onboarding ported** (`onBoardingHc/`, `onBoardingHcSteps/`): `OnBoardingHcFlow`,
    `AvatarEditor`, `HitchNameChangeDialog` + `NameChangeDialog`, `RoomPicker`,
    `RandomAvatarCloudsAnimation`, `Background`. Triggered off `suggestedLoginActions`, as
    `HabboLandingView.onAvatarRendererReady()` does.
  - **Three messages added**: `UpdateFigureDataMessageComposer` (3339, gender-then-figure on the
    wire), `SelectInitialRoomMessageComposer` (3267) and `SelectInitialRoomMessageEvent` (3624).
    IDs from WIN63's registry, names corroborated by the emulator.
  - Verified headlessly: the ticket screen and the hotel picker render with localised text and the
    AS3 layout. The onboarding path needs a live server (it runs off `suggestedLoginActions`) and is
    **not** runtime-verified yet.

- ✅ **Dormant-TODO sweep: five clusters closed, five stale notes retracted**, 2026-07-31
  (branch `feat/dormant-todo-waves`). Chosen by scanning all 522 `TODO`s in `src/` for work that
  was blocked on something already present, rather than by module.
  - **Nine notification message events woken up.** `NotificationMessageHandler` gated moderator
    messages/cautions, bans, respects, HC gift claims, safety-lock (both directions) and
    restore-client behind "TODO: uncomment when X is implemented". Eight of the nine event classes
    already existed — under `incoming/notifications/`, not the `moderation/`/`users/`/`catalog/`
    paths the dead imports named, which is why they read as missing; the ninth is
    `UserObjectMessageEvent`. Not a straight uncomment: the commented bodies had drifted from
    `_SafeCls_1951.as` (missing `useNotifications()`/`useNotificationFeed()` gates, an empty
    `onRespectNotification`).
  - **Eight composers ported** (`SetObjectData` 246, `SetRandomState` 1942, `UseWallItem` 3590,
    `PlacePostIt` 1122, `PlaceBot` 2102, `RemoveBotFromFlat` 2743, `GetDailyTasks` 4100,
    `SetChatPreferences` 1149), all resolved from `_SafeCls_2046.as` by tracing send sites in
    `_SafeCls_1821.as`. Three of the TODOs naming them were wrong — `PlaceBot` was documented as
    1295, which belongs to the user-move composer; `SetChatPreferences` was documented as a
    boolean in the `isDisabledInPreferences` setter, which sends nothing in AS3. `UseWallItem` is
    the one header the emulator disagrees with (it listens on 1540 and calls its own entry
    "UNRESOLVED"); registry wins, note is on the composer. Wired up random-state furni, wall-item
    use, ad-furni stuff-data save, bot place/pickup, and the daily-tasks request at login.
  - **`BadgeImageManager` ported** — `SessionDataManager`'s five badge accessors all returned
    `null`, so no badge rendered anywhere in the client. Also fixed the four `*AssetName`
    accessors, which were not stubs but inventions (`<badge>_b`, `group_badge_<badge>`) naming
    assets nothing ever registered. Caches through a `Map` rather than the AssetLibrary, because
    `BitmapDataAsset.content` is a PixiJS `Texture` while the whole badge API is typed
    `HTMLImageElement`; documented on the class.
  - **`restrict` now filters typing.** `TextController` parsed, stored and cloned the mask without
    ever applying it, and `ITextFieldWindow` did not expose it. Flash's semantics ported and
    validated against ten cases; masks live on the wired number/text/textarea inputs and the paged
    table's page box.
  - **The ten flagged `HabboMessages` headers audited.** Two describe live gaps; five were stale or
    misread (see the commit). The one that matters: 996 `Users` is missing AS3's `badgesRank` read,
    and it stays missing on purpose — `vortex-emulator`'s `RoomAvatarSerializer` stops at
    `IsModerator` too, so reading it client-side alone would desync every later user in the packet.
    Needs both repos changed together.
  - Two smaller fixes found while reading around the above: `changeRoomObjectState()` was missing
    the `!activeRoomHasFreeFurniMovementsMode` half of its play-test guard, and
    `UsersMessageParser`'s `userType==3` branch set `webID = -roomIndex` instead of the id it read.
  - Retracted rather than implemented: four `ChatWidgetHandler` localisation TODOs. The behaviour
    is already ported in `ChatBubbleFactory.applySpecialChatContent()`, and that handler is dead
    once `freeFlowChat` resolves — the primary tree has no `ui/handler/ChatWidgetHandler.as` at all.

- ✅ **`HabboActivityPointNotificationMessageEvent` (2046) ported and wired to its three AS3 subscribers**, 2026-07-30.
  - The single-currency counterpart of `ActivityPointsMessageEvent` (509, whole wallet) was missing
    from the client entirely, while `vortex-emulator` already sends it from `PlayerWalletModule`
    on every `CurrencyType.ActivityPoints` movement — so ducket/diamond balances only refreshed on
    a full-wallet push. Event + parser (`amount`, `change`, `type`; three ints) added and registered.
  - Header **2046** from `_SafeCls_2046.as` (`_SafeStr_4546[2046] = _SafeCls_2011`), corroborated by
    the emulator's `Revision20260701/Headers.cs`. The **1016** carried by `win63_version` and Nitro
    is a 2016-build id and is not used.
  - Subscribed in all three places AS3 does: `HabboCatalog.onActivityPointNotification()` (writes the
    one currency into the purse *without* clearing the others, `updatePurse()`, `PurseEvent` +
    `PurseUpdateEvent`), `NotificationMessageHandler.onActivityPointNotification()` (loyalty-only
    bubble, `change > 0`, `if_icon_diamond` — the port drops AS3's `_png` suffix) and
    `QuestMessageHandler.onActivityPointsNotification()` → `QuestController.onActivityPoints()`.
  - Two gaps left visible as `TODO(AS3)`: the catalog's `HBST_pixels` sound (this port's
    `HabboCatalog` has no sound-manager dependency at all) and the quest module's own
    `ActivityPointsMessageEvent` registration, which AS3 has and only `HabboCatalog` carries here.

- ✅ **Pets: the `avatarinfo` UI slice closed — context menus, use-product and the whole breeding
  chain**, 2026-08-03. Completes the scope the 2026-07-30 entry below left open.
  - **Eleven new classes in `ui/widget/avatarinfo/`** (`avatarinfo/` goes 4/28 → 15/28):
    `PetInfoData`, `PetMenuView`, `OwnPetMenuView`, `BreedPetView`, `UseProductView`,
    `UseProductConfirmationView`, `BreedMonsterPlantsConfirmationView`, `ConfirmPetBreedingView`,
    `BreedPetsResultView`/`Data` and `NestBreedingSuccessView`, plus `widget/events/UseProductItem`.
    Every layout they need was already shipping (`pet_menu`, `own_pet_menu`, `breed_pet_menu`,
    `use_product_menu`, the seven `use_product_controller_*`, the four frames,
    `confirm_pet_breeding_xml`, `breed_pets_confirmation_xml`, `breed_pets_result_xml`,
    `nestBreedingSuccess_xml`, `pet_breeding_pet_preview_xml`) — nothing new had to be built.
  - **`AvatarInfoWidgetHandler` went from 142 to ~490 lines**: it had *zero* pet code against AS3's
    636. Added the three session subscriptions (`RSPFUE_PET_STATUS_UPDATE`, `RSPLUE_PET_LEVEL_UPDATE`,
    `RSPFUE_NEST_BREEDING_SUCCESS`), `findPetWithWebId()`, `getFurniData()`,
    `activateUseProductMenuForPets()`/`activateBreedMenuForPets()` (the two room scans that decide
    which pets a product or a breeding offer applies to), `requestBreedMenu()` and the five action
    branches (`RWUAM_REQUEST_PET_UPDATE`, `RWUPM_PET_PRODUCT`, `RWUAM_HARVEST_PET`,
    `RWUAM_COMPOST_PLANT` with its confirm dialog, `RWUAM_REQUEST_BREED_PET`).
  - **The three breeding composers now have a sender.** `BreedPets` (1922), `ConfirmPetBreeding`
    (2872) and `CancelPetBreeding` (3367) were constructed nowhere; `AvatarInfoWidget` now owns
    `breedPets()`/`acceptBreedPets()`/`cancelBreedPets()`/`confirmPetBreeding()`/`cancelPetBreeding()`
    and the four `RWPPBE_*`/`RWPBRE_*` desktop events that drive them have listeners.
  - **One real bug found while wiring, in code that was already there.** `_handlePetInfo` is cleared
    by `RWUAM_REQUEST_PET_UPDATE` so the infostand's own refresh does not re-open the bubble — but
    nothing set it back, because AS3 does that in the `RWROUE_OBJECT_SELECTED` branch this port had
    never subscribed to. Without it the pet bubble would stop opening for the rest of the session
    after the first refresh.
  - **Notifications closed**: `PetImageUtility` ported and hung off `HabboNotifications`
    (`get petImageUtility()`, `get windowManager()`), and `onPetLevelNotification()`/`onPetReceived()`/
    `onPetRespectFailed()` — three parse-then-return stubs — now render their feed item / alert.
  - **Two smaller gaps**: `InfoStandWidget.onPetFigureUpdate()` was an empty body and now
    re-rasterises through the handler's `getPetImage()` (made public: this port's event carries the
    figure string where AS3's carries a rendered BitmapData);
    `AnimatedPetVisualizationData.defineVisualizations()` is overridden, so `disableheadturn` is read
    instead of `isAllowedToTurnHead` being hardcoded true.
  - **Supporting members added where the port lacked them**: `IRoomEngine.getActiveRoomIsPlayingGame()`,
    a nullable `listener` on `getPetImage()` (the implementation always allowed it; only the type did
    not), and `IHabboInventory.getFloorItemById()`/`requestSelectedFurniToMover()` — both AS3
    `IHabboInventory` members, needed by the breeding-result dialog's place/pick pair.
  - **Server mismatch resolved**, so the `PetExperienceEventParser` warning is gone: Revision20260701
    now writes `PetId, PetRoomIndex, GainedExperience` and `MovePetMessageEvent` is 432.
  - **The use-product trigger closed too, in the same pass.** Both AS3 entry points now exist:
    `RoomEngine.showUseProductSelection()` (the inventory's `use_btn`, whose `FurniModel` stub
    already named the missing engine method) and the `ROWRE_PET_PRODUCT_MENU` case of
    `handleObjectWidgetRequestEvent()` (double-clicking a product already in the room —
    `FurniturePetProductLogic` was ported and raising the request, but the engine's switch fell
    through to its `log.warn` default). `RoomUI` forwards both `ROSM_USE_PRODUCT_*` to
    `roomObjectEventHandler` as AS3 does at RoomUI.as:299-304, which is the only hop between the
    engine and the bubbles.
  - **Scope left open, marked TODO(AS3), not omitted.** `IHabboUserDefinedRoomEvents` is still null
    everywhere, so the wired click gate reads as "no wired" and `WIRED_USER_CLICK_HANDLED` is not
    subscribed. The `avatarinfo` avatar-name bubbles, decorate mode, the bot menus and the highlight
    timer remain unported.

- ✅ **Pets: protocol re-derived from WIN63's own registry, session + infostand chain closed**, 2026-07-30.
  - **The derivation is gone.** `PetVocalMessageEvent` (3073) and `IssuePetCommandMessageComposer`
    (3072) were Vortex-custom inventions with no counterpart in any AS3 tree, and 3073 is
    `WhisperMessageEvent` — registering the pet event there silently overwrote whisper in the
    `_events` Map. Both classes and their parser are deleted. There is no "issue pet command"
    packet in the source: AS3 issues a command as ordinary chat, `"<pet name> <localised command>"`,
    through `InfoStandWidgetHandler`'s `RWPCM_PET_COMMAND` branch.
  - **Every pet header re-read from `_SafeCls_2046.as`.** The pet message package is
    package-obfuscated in the primary dump, so each class was identified by the handler that
    subscribes to it (`RoomUsersHandler.as:85-95`, `habbo/inventory/_SafeCls_1951.as:159-195`,
    `RoomChatHandler.as:41`, `habbo/room/_SafeCls_1984.as:284`) and then looked up in the registry.
    All eight previously-registered room-pet ids were already right and are now annotated with their
    obfuscated class; the block used to claim they came from the emulator.
  - **One real header bug found.** `MovePetMessageComposer` was registered at **2761**, which is
    `win63_version`'s id — a different build. This revision's registry has no 2761 at all; the real
    header is **432** (`_SafeCls_2560`, sent by `_SafeCls_1821::sendMoveUserObjectMessage()` on the
    `monsterplant` branch). Every monster-plant move was going nowhere. `vortex-emulator`'s
    `Revision20260701/Headers.cs` carries the same wrong 2761 and still needs fixing there.
  - **One real parser bug found.** `PetInventoryMessageParser` never read `rarityLevel`, the last
    field of each pet record, so in a multi-pet fragment the next pet's id was read out of the
    previous pet's rarity field. `Pet.rarityLevel` was a hard-coded `-1` behind a TODO; it is now
    parsed and stored.
  - **Eight incoming messages added** with parsers: `PetBreeding` 939, `ConfirmBreedingRequest`
    1477, `ConfirmBreedingResult` 2068, `NestBreedingSuccess` 40, `GoToBreedingNestFailure` 2441,
    `PetRemovedFromInventory` 3013, `PetAddedToInventory` 3653, `PetSupplementedNotification` 3858 —
    plus the two breeding DTOs (`BreedingPetInfo`, `PetBreedingRarityCategory`).
    **Four composers added**: `BreedPets` 1922, `ConfirmPetBreeding` 2872, `CancelPetBreeding` 3367,
    `PassCarryItemToPet` 1429.
  - **Handlers closed.** `RoomUsersHandler` had only `onPetInfo`; the other ten AS3 pet handlers now
    exist, so commands, placing errors, figure/status/level updates and the whole breeding chain
    have a subscriber. `RoomChatHandler.onPetSupplementedNotification()` and the inventory's
    `onPetAdded`/`onPetRemoved`/`onGoToBreedingNestFailure` are ported too.
  - **Infostand chain closed.** The nine missing `RoomWidget*` pet events and their three data
    classes, `RoomWidgetPetCommandMessage`, seven `InfoStandWidgetHandler` session subscriptions,
    the pet-care composer branches (treat/water/light/carry-item, previously a `log.warn` TODO), and
    **`PetCommandTool`** itself — which removes the four stubs `InfoStandPetView` was carrying and
    makes the `train` button actually open the training window.
  - **Scope left open, marked TODO(AS3), not omitted.** The `avatarinfo` pet views are still absent:
    `OwnPetMenuView`, `PetMenuView`, `PetInfoData`, `BreedPetView`, `ConfirmPetBreedingView`,
    `BreedPetsResultView`/`Data`, `NestBreedingSuccessView`, `BreedMonsterPlantsConfirmationView`,
    `UseProductView`/`UseProductConfirmationView`. The breeding messages therefore reach
    `desktopEvents` with nothing listening yet — the wire and session layers are complete beneath
    them. `InfoStandWidget.onPetFigureUpdate()` also still needs the figure re-rasterised.

- ✅ **Mystery box + mystery trophy wired end to end**, 2026-07-29.
  - **What landed.** The three missing incoming messages (`ShowMysteryBoxWait` 691,
    `CancelMysteryBoxWait` 3840, `GotMysteryBoxPrize` 353) with their parsers, the two composers
    (`MysteryBoxWaitingCanceled` 1063, `OpenMysteryTrophy` 2242), and the whole UI cluster that had
    no port at all: `FurnitureContextInfoView`, `FurnitureContextMenuWidget` +
    `FurnitureContextMenuWidgetHandler`, `MysteryBoxContextMenuView`, `MysteryBoxOpenDialogView`,
    `MysteryBoxToolbarExtension` and `MysteryTrophyOpenDialogView`. `RWE_FURNITURE_CONTEXT_MENU` was
    already being requested by `RoomUI` — the factory returned null for it, so nothing existed
    behind the request.
  - **Two bugs found in code that was already there, both silent.** (1) `FurnitureLogic` gated its
    context-menu event types and its mouse-click dispatch on the private `_contextMenu` **field**
    where AS3 reads the `contextMenu` **getter**. Every logic that supplies its menu by overriding
    the getter — the mystery box, the mystery trophy, the guild furni, the monsterplant seed — never
    reached `contextMenuType`, so the field stayed null and the menu could not open for any of them.
    (2) `RoomUI` forwarded the `RETWE_REQUEST_*` widget requests to the desktop but not
    `RETWE_OPEN/CLOSE_FURNI_CONTEXT_MENU`, which is how a handler's `processEvent()` is reached at
    all. Either one alone would have left the new widget dead.
  - **Also ported while in `FurnitureLogic`:** the `RoomObjectSelectedMessage` branch of
    `processUpdateMessage()`, which opens/closes the menu on select/deselect. It is inert today —
    nothing in this port constructs that message yet — and says so at the site.
  - **Scope left open, marked TODO(AS3), not omitted.** The widget's other AS3 sub-views (guild,
    random teleport, monsterplant seed + confirmation, friend furni, generic usable, purchasable
    clothing, effect box) are stubs, as are the handler's guild-furni-info and figure-set-ids
    message events. The room engine does not raise their requests either, so nothing fires into a
    gap.
  - **Server side already existed.** `vortex-emulator`'s `RoomGrain.MysteryBox` +
    `PlayerMysteryBoxGrain` route `UseFurniture` (3353) into the wait/prize flow, and the live
    gamedata carries `mysterybox.tracker.active`, `mysterybox.faq.url` and all 49
    `mysterybox.*` texts — so the tracker and both dialogs have their data.

- ✅ **`habbo/friendlist` ported in full** (3 → 41 TS files against 40 AS3), 2026-07-29.
  - **What landed.** The domain (`Friend`, `FriendCategory`, `FriendCategories`, `FriendRequest(s)`,
    `FriendListTab(s)`, `AvatarSearchResults`, `FriendOnlineImageListener` and the four `*Deps`
    pairs), all eleven views (`FriendListView`, `FriendListTabsView`, `FriendsView`,
    `FriendRequestsView`, `SearchView`, `AlertView` + `FriendRemoveView`/`RoomInviteView`,
    `RelationshipStatusSelector`, `OpenedToWebPopup`, `Util`), `FriendListLookAndFeel`,
    `RelationshipStatusEnum`, `FriendListTabEnum`, both event classes, and `HabboFriendList`
    rebuilt on top of them. Also `habbo/utils/UserInfoRegionUtil` (AS3 `_SafeCls_3877`, used by
    three of the views) and `IHabboNotifications.addItemWithBitmap()`, which the friend-online
    toast needs and which the notifications component was missing.
  - **The old manager was a documented re-architecture, and it is gone.** `HabboFriendList.ts` kept
    friends in one flat `Map<number, FriendData>` with no categories, no requests domain and no
    search state — its own `canBeAskedForAFriend()` carried a `TODO(AS3)` saying the two missing
    conditions could not be checked "against a flat-Map re-architecture". Both conditions are now
    checked, because the state they read exists. The port-specific `friendListEvents` bus is kept
    and still fed: several ported systems listen on it.
  - **Two wiring gaps, neither introduced here, both since closed.** (1) `HabboFriendList` was
    **never constructed** — `VortexMain` registered `HabboGroupsManager`, `HabboNotifications` and
    the rest, but nothing instantiated the friend list, so no version of this code had ever run.
    `initFriendList()` does it now. (2) Its bitmaps (`open_minimail`, `room_invite`, `hdr_friends`,
    `arrow_*`, …) were not in `App.ts`'s asset-library allowlist, the same trap recorded for the
    group wizard — a missing entry renders blank rather than throwing. The names were taken off
    `HabboFriendListCom.as`'s `*_png` fields rather than off a grep of call sites, because AS3
    resolves some of them by suffix and a grep misses those.
  - **Category names go in as `${...}` tokens, not resolved text.** AS3 resolves eagerly at
    construction because its localization tables are loaded before the socket opens. Here the texts
    arrive over HTTP and `MessengerInit` can win the race — and `getLocalization()` returns `''`
    for a key it has not loaded yet, so the name was frozen to the empty string for the session.
    Resolving at draw time is what `CoreLocalizationManager` already does for every caption.
  - **Names.** Recovered where a tree has them (`IFriend`, `RelationshipStatusEnum`,
    `IHabboFriendsList` → `IHabboFriendList`, all from PRODUCTION); derived and **stated as derived
    at the declaration** where no tree does (`IFriendsView`, `IFriendRequestsView`,
    `FriendRequestsView`, `FriendListTabEnum`, `FriendListLookAndFeel`, `UserInfoRegionUtil`, the
    four `I*Deps`, and `HEART`/`..._MINIMIZED`, both read off their own value tables).
  - **One dead field found on the way.** `Friend` reads `lastAccess` off the wire DTO, but the DTO
    (`src/unknowns/_SafePkg_1764/_SafeCls_1774.as`) declares that field and **never assigns it** —
    its constructor reads 14 values and that is not one of them. It is always null in the real
    client; `FriendData.ts` is right not to have it.

- ✅ **The friend bar, the messenger, and the silent DI lock that hid all of it**, 2026-07-29.
  - **What landed.** `friendbar/data/` 8/8 (`HabboFriendBarData` + `FriendEntity`,
    `FriendNotification`, `FriendRequest` and their interfaces), `friendbar/view/` 22/22
    (`HabboFriendBarView`, the 9 tabs, the 6 tokens, the 4 utils), `friendbar/events/` 7, and
    `habbo/messenger/` `HabboMessenger` + `DummyFriend`. `HabboFriendBar`'s two
    `attachComponent()` calls — commented out because neither component existed — are live and in
    AS3's order (data, view, landing view), and its `set visible()` stub delegates through
    `queueInterface()` the way AS3 does.
  - **The bar never appeared, and nothing in the bar was why.** `HabboFriendBarData` declares
    `IID_HabboMessenger` a **required** dependency, and no component in this port was ever attached
    against that IID. A required dependency on an unprovided IID leaves the component locked for the
    whole session: `initComponent()` never runs, its message handlers are never registered, its
    events never fire. **Nothing throws and nothing logs.** The view waits on the data component in
    turn, so `buildUserInterface()` was never reached. Writing `HabboMessenger` fixed it; softening
    the dependency would have "fixed" it too, at the cost of null-guards through the whole domain
    layer. Before declaring a dependency required, confirm something calls
    `attachComponent(..., [IID_X])` — and read the file, because a one-line grep gives false
    negatives on multi-line attach calls (it gave two here).
  - **Still open: the chat window.** `_mainView` is null until `MainView` (1237 l.) and
    `habbicons/` (1399 l.) are ported, so clicking a friend opens nothing. Every delegation through
    it is guarded, which is AS3's own shape rather than a concession — the source guards all of
    them, because the view is built lazily on first open. `IID_HabboSoundManager` and
    `IID_HabboHelp` are declared optional for the reason above, with the reason on the line.
  - **Two AS3 quirks kept verbatim**, each with its reason on the line: the animated path in
    `FriendsView.select()` is guarded by an always-false expression and can never run, and
    `releaseFriendTabWindow()` removes three icon listeners with one handler.
  - **Names.** `AddFriendsTab` (`_SafeCls_3052`) and `OpenMessengerTab` (`_SafeCls_3739`) are
    obfuscated in every tree; both names are **derived**, from their layouts and their actions, and
    said so at the declaration, as are the constants whose names had to come off their values.

- ✅ **Two asset lookups that could never hit, both silent** (`AvatarImageWidget`,
  `SimpleAlertDialog`), 2026-07-29. `getAssetByName()` returns null without complaint, so the
  bitmap renders blank and the layout does not build.
  - **`_png` is not part of the name.** Images register under their bare file basename
    (`App.ts::registerImageAssets`), while AS3 looks them up as the `*Com.as` field name
    (`hdr_friends_png`). Code ported literally therefore asks for a name the library never had.
    Found in three places: `HabboFriendList.getButtonImage()`,
    `AvatarImageWidget.requestPlaceholder()`, and the bundle itself, where a sibling variant had
    been "fixed" by renaming the *file* to `placeholder_avatar_small_head_cropped_png.png` — the
    wrong end of it. Note the extraction tool sometimes does the opposite and names a file after the
    embed rather than the field (`icons_toolbar_me_menu_placeholder` ships as
    `me_menu_placeholder.png`); that one needs the generator fixed, not the call site.
  - **Layout names are per-component and two components declare `simple_alert_xml`.**
    `HabboWindowManagerCom` and `HabboFriendListCom` both do, so both ship qualified and the bare
    key does not exist — `simpleAlert()` was throwing for **every** caller in the client, not just
    the friend list's. Join declarations to files on the embed's whole linkage name; collapsing to
    the short name merges distinct assets.

- ✅ **A dead emulator left the client sitting there fully rendered**, 2026-07-29.
  Two independent causes. `setConnectionActions()` had **no caller**, so `_connectionActions` stayed
  null and every state change — the server going away included — was logged and dropped;
  `App.ts::installConnectionActions()` provides them now. And `connectionClosed()` took no
  arguments, so every close looked alike and there was no way to react to one without reacting to
  all: it carries the `CloseEvent`'s `code`/`wasClean` now, and `HabboCommunicationManager` acts
  only on **1000/1001**, the two a peer sends deliberately. That distinction is the whole point — a
  backgrounded tab drops the socket with 1006, and Chrome halts the tab's JS entirely after ~5
  minutes hidden, so treating a transport drop as a disconnect logs the player out for switching
  tabs. AS3 hands this to `loginFlow.showDisconnected()`, but this port disposes its `LoginFlow`
  once boot is done and there is no live instance to show; a reload lands on the same login screen
  with no half-torn-down engine behind it.

- ✅ **Input fields: the Flash `border` was never drawn, and the caret bridge was aligned to the wrong box** (`core/window/graphics/renderer/TextSkinRenderer.ts`, `core/window/components/TextController.ts`, `TextFieldController.ts`).
  - **Where the border went missing.** AS3 never draws one either — `TextSkinRenderer.draw()` is `param2.draw(_loc8_, matrix, ...)`, a blit of the *live* `flash.text.TextField`, and Flash paints that field's background **and** border into the bitmap before it is copied. `TextController` only mirrors the two layout properties onto `_field` (`setBorder`/`setBorderColor`). This port has no `_field`; it strokes glyphs itself, so the border simply did not exist and every `<var key="border">` field rendered as a bare fill — room settings' name/description/tags, the group wizard's, the messenger's search box, ~20 layouts in total. `drawBorder()` now runs **before** the empty-text early return, because AS3 blits the field whatever it holds: an empty bordered field still has its border. `borderColor` also had no public accessor (AS3 declares one, l.657), so `border_color` — `0x959595` in room settings — was parsed into a field nothing could read.
  - **The caret sat where the text was not**, for three independent reasons, all in the hidden-`<input>` bridge that stands in for Flash's own caret. (1) The element was laid over the *whole field* with the field's height, and a single-line `<input>` centres its editor vertically no matter what `line-height` says — a 9px line in a 26px field landed ~7px low, and in the 80px description box, halfway down it. (2) `font-family` was assigned the raw `fontFace`, which is invalid CSS for any quoted family (`Volter (Goldfish)` — parentheses), so the browser dropped the declaration and advanced the caret in **Arial** while the canvas drew Volter. (3) The element's kind was decided in the constructor, before `multiline`/`word_wrap` arrive from the layout's `<variables>` — so a wrapped field got a single-line `<input>`, its caret running off horizontally while the canvas wrapped the text over four lines. The element now covers the text box only (one line tall unless the field wraps), takes its font from the same builder the measuring path uses, and is rebuilt as a `<textarea>` when the field turns out to be multi-line. `maxChars` was applied at construction for the same reason and had the same fate; it is applied with the geometry now.
  - **`word_wrap` on an editable field also had no effect on the canvas side**: the renderer's wrap test excluded `TEXTFIELD`, though Flash lays text out from `multiline`/`wordWrap` alone, with no regard for whether the field is editable.
  - **A pre-existing 2px offset, made visible by the border.** Flash reserves a gutter on all four sides of a TextField; the port applied only the top one, so glyphs ran straight into the newly-drawn border line. Fixed for input/password fields (`FLASH_TEXT_FIELD_LEFT_GUTTER`) and left as a `TODO(AS3)` for the rest: an auto-sized label takes its width from the measured text, so shifting glyphs right without widening the box — `measureLayout()` compensates only for the vertical gutter, and only for background-filled fields — would clip the last 2px off captions across the client.
  - **Verified in a browser, not by reading**: the layouts were built standalone through `vortex-glaze`'s window subsystem and the DOM bridge's normally-transparent text was coloured red to expose where the caret actually is. Before: red text several pixels below/right of the black canvas text, description unwrapped, no border pixels anywhere on the field's edges. After: red on black exactly, and the field edge reads `#000000` (group wizard) / `#959595` (room settings, its `border_color`).

- ✅ **Room-preview canvases were never taken off the stage when their widget died** (`habbo/room/preview/RoomPreviewer.ts`, `habbo/window/widgets/RoomPreviewerWidget.ts`, `habbo/catalog/viewer/widgets/ProductViewCatalogWidget.ts`). Both widgets' `dispose()` unregistered the canvas from `PreviewCanvasStack` and dropped the reference — but never detached the PixiJS container, which `RoomEngine.createRoomCanvas()` parents onto the **shared root stage**, not into the window tree. In Flash the canvas is a child of the widget's own window, so disposing the window removes it from the display list and AS3's `RoomPreviewer.dispose()` can ignore it entirely; here nothing owned it. With the per-frame sync callback also unregistered, nothing hid it either: the last previewed piece of furniture stayed rendered on the stage forever, frozen at its last screen position, showing through the transparent hole the composite punches for the room view. The same file already did the right thing in `showPreview()` (`parent?.removeChild()`) when swapping the live canvas for a static bitmap — only the teardown path missed it. `RoomPreviewer.releaseRoomCanvas()` (TS-only, needs `IRoomEngine.disposeRenderingCanvas()`, also TS-only) hands the canvas back for the widget that owns its previewer; the catalog's widget only detaches its own container, because the previewer belongs to `HabboCatalog` and is shared — a widget disposed *after* its replacement was built (page switches rebuild the product view) would otherwise dispose the fresh canvas out from under it, both keying on the same preview room id.

- ✅ **The trophy furni widget — and the room-object widget-request bridge no furni widget could work without** (`habbo/ui/widget/furniture/trophy/`, `handler/FurnitureTrophyWidgetHandler.ts`, `RoomEngine.handleObjectWidgetRequestEvent()`).
  - **The blocking gap was one missing translation step, not the widgets.** The chain is `double-click → FurnitureTrophyLogic.useObject() → ROWRE_TROPHY → RoomEngine → RETWE_REQUEST_TROPHY → RoomDesktop → RoomWidgetFurniToWidgetMessage → FurnitureTrophyWidgetHandler → RoomWidgetTrophyDataUpdateEvent → TrophyFurniWidget → TrophyView`. Both event vocabularies already existed in full — `RoomObjectWidgetRequestEvent` with 35 `ROWRE_*` constants, `RoomEngineToWidgetEvent` with 30 `RETWE_*` — and the furniture logics already emitted the first. **Nothing anywhere translated one into the other**, so every `ROWRE_*` a furni raised died in `RoomEngine.onRoomObjectEvent()`. Mechanism 4 from the 2026-07-17 audit ("data plumbed end to end, never consumed at the last step"), and it gated *all 24* furniture widget clusters, not just this one. `handleObjectWidgetRequestEvent()` is a faithful port of `_SafeCls_1821.as` (AS3's `RoomObjectEventHandler`, folded into `RoomEngine` here because that is already the class implementing `IRoomRenderingCanvasMouseListener`): 14 cases mapped, the rest carrying an explicit `TODO(AS3)` that lists them — left unmapped rather than emitted, so no event fires that nothing can service.
  - **A fidelity bug in `FurnitureTrophyLogic` that would have defeated the port anyway**: it emitted `ROWRE_OPEN_WIDGET`, the *generic* "this furni has a widget" request, where AS3 emits `ROWRE_TROPHY` — which is what the trophy handler claims, so the engraving could never have arrived. It was also missing `getEventTypes()` and the `mouseEvent()` override that makes a trophy open on double-click, and carried an invented `widgetType = 'trophy'` field with no AS3 counterpart (now gone).
  - **The cluster keeps AS3's interface/view split rather than collapsing it** (rule 2): `ITrophyFurniWidget`/`ITrophyView`, `TrophyTheme`, `TrophyFurniWidget`, and the two views — including AS3's own asymmetry, where `NikoTrophyView` takes the concrete `TrophyFurniWidget` instead of the interface because it also reads `localizations` and `configuration`, neither of which `ITrophyFurniWidget` declares.
  - **Two AS3 oddities preserved rather than "fixed"**: `TrophyFurniWidget.get color()` returns the constant white tint and ignores the event's `color`, which carries the *theme* instead (1-based, hence `normalize(color - 1)`); and `updateInterface()` switches on `viewType - 10`, so only 10 (Niko silver) and 20 (Niko gold) reach `NikoTrophyView`.
  - **Which widget went first was decided by which layouts actually ship**, not by size: `trophy.xml`, `niko_trophy.xml` and `trophy_bg_{gold,silver,bronze}.png` are all present in the built assets. `present/` (999 lines) and `roomlink/` ship no layout at all and would have rendered nothing — the silent-failure pattern this project keeps rediscovering.
  - Furni widgets are created **eagerly at room entry** (`RoomUI.as:941`): creating one is what registers its handler's message types, so the widget has to exist before the furni is ever clicked.
  - **Not verified live.** `pnpm build` and `pnpm lint` are clean, but nothing has yet opened a trophy in a running room — that needs the emulator and an engraved trophy placed.

- ✅ **`sendRaw()` stopped reporting success for sends that went nowhere** (`core/communication/connection/SocketConnection.ts`, commit `1e0ca5f`). Switch tabs, come back, and the client looked alive while doing nothing — every click silently dropped, only a reconnect helped. `sendRaw()` queued into `_pendingMessages` and returned `true` whenever `_connected` was false; that branch cannot distinguish "not connected yet" from "connection lost", and `onclose` leaves `_socket` non-null while clearing `_connected`, so a dropped link sent every subsequent action into a queue only `onopen` drains — and there is no reconnect. AS3 has no queue on that path at all (`SocketConnection.as::send()` l.222-229: `if(this._socket.connected){ writeBytes; flush; } else { return false; }`); it queues in exactly one other case (`authenticated && !configurationReady`), which this port already models separately as `_pendingComposers`. **The byte-level queue on `!_connected` was a port invention.** Queuing is now limited to `readyState === CONNECTING` — the async-connect window Flash did not have — and anything else returns `false` with a one-shot warning. `onclose` also records close code/reason/`wasClean`, evidence that previously lived only for the handler's lifetime: 1006 says the transport dropped rather than the peer closing, which is what a backgrounded tab or a sleeping machine looks like.

- ✅ **Six message events had no reachable header, and header collisions were silent** (`HabboMessages.ts`, commit `5622901`). `RoomCompetitionController`, `AchievementsResolutionController` and the navigator each registered handlers that could never fire — `MessageRegistry` dropped them with a bare "Unknown message event class" at startup and the replies simply never arrived. Five had no header at all. The sixth, `GuestRoomSearchResultMessageEvent`, is the instructive one: it sat on **1265**, which is the Wired furni-trigger push, and since `_events` is a `Map`, the later `set(1265, WiredFurniTriggerEvent)` further down the file silently replaced it. `MessageRegistry`'s own duplicate check never fires because the `Map` has already deduped by the time it sees the config — **ID collisions are now loud**. IDs were recovered from WIN63's own registry (`_SafeCls_2046.as`) by identifying each obfuscated event class through the parser it constructs, since member names are not obfuscated: 3143, 1844, 1166, 2617, 3222, 160. All six independently match what `vortex-emulator` already sends — a second source agreeing on the same numbers, in the order CLAUDE.md prescribes (registry first, emulator as corroboration).

- ✅ **Boot order now follows AS3: nothing loads before login** (commit `49dae39`). `HabboAir.as::createLoginFlowOrLoadingScreen()` returns *before* creating the loading screen when there is no SSO ticket, which makes the very next call — `startCoreInitializationIfPossible()` — bail on `if(_loadingScreen == null)`. So AS3 starts no core, no `SessionDataManager`, and no furnidata download until the user is authenticated. This port booted the whole engine first and showed the login flow afterwards, because `LoginFlow` was handed the engine's configuration manager. `LoginFlow` now builds its own from the embedded configuration assets alone, which is what AS3's `createConfiguration()` does (`login/LoginFlow.as:157`, out of `HabboConfigurationCom`) — every property the login path reads ships in `common_configuration_txt`, so no `external_variables` download is needed either. The avatar creator, the one screen with no AS3 counterpart and the only one needing a renderer, boots the engine on demand through an `ensureEngine` callback. Two more mismatches fixed in the same area: **the loading screen had two owners** (`VortexMain.onExitFrame()` implemented AS3's real condition, `HabboAir.as:466`, while `App` also disposed it unconditionally after `connect()`, so its lifetime depended on which fired first — the engine owns it alone now), and **window skins/layouts are handed to `HabboWindowManager` at construction** via `IVortexConfig.windowAssets`, the way AS3 passes its `AssetLibrary` as the third constructor argument (`HabboWindowManagerComponent.as:142`), instead of being pushed in after `Vortex.bootstrap()` returned — which left the manager alive but empty while the socket was already open.

- ✅ **Logging rebuilt: namespaced levels, bound console sinks, and a level audit over all ~905 call sites** (`core/utils/Logger.ts`, 186 files retagged). Infrastructure, not a port — AS3's `debug()`/`warning()` (`_SafeCls_56.as` l.71-97) store the last message and dispatch a component event; they were never console output, so none of this codebase's console logging is AS3-traceable and the levels were the port's own choice.
  - **Names are now dotted module paths** (`habbo.room.RoomEngine`, `core.communication.connection.SocketConnection`), resolved by walking the path outwards. The old flat namespace could not express "this subsystem at DEBUG", and it collided: `Moderation` was three different files, `Navigator` two, `ChatBubbleFactory` two across packages. Enforced by `no-restricted-syntax` in `eslint.config.mjs`; 38 hand-written `[ClassName]` message prefixes that existed *because* the real prefix was useless are gone.
  - **`log.debug` is a property holding a pre-bound `console` method, not a wrapper.** A disabled level is a shared no-op, and — the bigger win — the console attributes each line to the **caller** instead of to `Logger.ts`. Verified in Edge: boot lines report `HabboLocalizationManager.ts:75`, `LoginFlow.ts:653`. This is why timestamps and `Logger.onRecord()` are opt-in: both need a wrapper frame and give the attribution back.
  - **New `TRACE` floor**, off at every default, for per-frame/per-packet/per-item detail. 42 "…received" packet-arrival traces moved down to it. `__packets.on()` raises its own namespace to TRACE explicitly, or it would silently do nothing.
  - **Level audit**: 51 `info` → `debug` (component lifecycle — `'Navigator initialized'` is not a milestone), 27 `debug`/`info` → `warn` (`'Unknown pricing model'`, `'not ported yet'`, `'Could not find element'` — the silent-failure class this project keeps rediscovering; at `debug` they are invisible in production, which is exactly where they matter). Dev default moved DEBUG → INFO. Pre-login boot console: **5 lines**, of which 2 are Vite's. Post-login was not measured — it needs a running emulator.
  - **Runtime control, no rebuild**: `__log.set('habbo.room', 'trace')`, `__log.only(ns)`, `__log.list()`, `__log.spec()`; same spec as `?log=warn,habbo.room:trace,-core.window` and the `vortex:log` localStorage key. `log.once(key)` dedupes a warning raised from a loop. Full reference: `docs/STYLEGUIDE.md` → Logging.
  - **One latent bug fixed while testing**: `applySpec(spec, persist=false)` mutated the config without bumping the invalidation generation, so a non-persisted spec only took effect if some later call happened to invalidate first. `persist` now decides durability only, never whether a change applies.

- ✅ **Text rendering moved from `ctx.fillText()` to a baked glyph atlas — and Flash's four text-quality fields finally do something** (`core/window/utils/GlyphAtlas.ts`, `TextSkinRenderer`, `LabelRenderer`, `TextController`, `TextLabelController`).
  - **The gap was never structural.** Text already drew into each window's own cached `OffscreenCanvas` (`WindowRendererItem.render()`), so there was no canvas-per-text problem to solve. What was wrong is that `antiAliasType`, `sharpness`, `thickness` and `gridFitType` were parsed, stored and then **ignored** — `ctx.fillText()` has no equivalent knobs — while `text_styles_css` sets `anti-alias-type: normal` on every Volter 9px style, i.e. on essentially the whole UI. Flash's `"normal"` is the pre-Flash-8 rasteriser: near-binary coverage on a pixel font. Skia gives greyscale AA instead. That difference *is* the "it doesn't look like Flash" complaint.
  - **Why a threshold alone does not work, and what does.** Thresholding a greyscale-AA'd 9px render destroys sub-pixel-wide stems — the middle of an `M` sits around 40% coverage and vanishes. Averaging supersamples has the same failure for the same reason. The atlas instead renders at 5× and reads back **only the sub-pixel at each destination pixel's centre**: the literal "does the outline cover the pixel centre" test an aliased rasteriser performs. The supersampling adds no blur because samples are never averaged; it only shrinks the browser's AA to 1/5 px before the read. Verified side-by-side against the previous rendering before wiring anything in.
  - **AS3 mapping**: `"normal"` → centre-sample + threshold; `gridFitType = "pixel"` → integer glyph origins and advances. Defaults are AS3's (`advanced` + `pixel`, sharpness/thickness 0 — ctor l.82-90, style resolution l.554-566).
  - **`"advanced"` deliberately stays on `ctx.fillText()`** (`GlyphAtlas.handles()`). `fillText()` composites glyphs with a text-specific gamma correction — dark-on-light coverage is lightened so stems do not read as heavy — that `drawImage()` of a pre-rasterised glyph does not get. Identical coverage therefore comes out visibly **bolder** through the atlas; that is what made the navigator's Ubuntu tabs (`button_shiny_regular`: advanced, sharpness 80, thickness -15) look bold against the AS3 client. The trade only pays where the atlas buys fidelity: `"normal"` is binary, so it has no mid-tones for gamma to act on and cannot suffer this; `"advanced"` greyscale AA is already close to the browser's, so the atlas would have bought speed at the price of a permanently heavier face. Every Volter style is `normal`, so the atlas still covers nearly the whole UI. The `sharpness`/`thickness` curve is kept in `buildAlphaLut()` — reversing the policy is one edit to `handles()` — and its two scale divisors are marked DERIVED at their declaration, since Flash's rasteriser is closed and no AS3 source states how either value scales.
  - **Two real port gaps closed on the way.** `TextController` had no public `antiAliasType`/`gridFitType`/`sharpness`/`thickness` accessors although AS3 declares all four (l.637/687/737/782). `TextLabelController` resolved its text style but **discarded** those same four values, where AS3's `TextFieldCache.getTextField()` (l.51-54) stamps them onto the pooled `TextField` and the controller's getters read them straight back off it (l.50/95/129/174).
  - **Measurement had to move too.** With `gridFitType = "pixel"` the atlas rounds advances, so `TextController.measureLineWidth()` and `TextLabelController.refresh()` now measure *through the atlas*. Leaving them on `ctx.measureText()` would auto-size fields to a width the renderer never draws.
  - **Costs**: one atlas page per (font string, quality) — a few hundred KB for the whole client — plus a colourised copy per fill colour, capped at 24 and rebuilt only when a new glyph is baked. Baking is lazy per glyph. All pages are dropped on `document.fonts` `loadingdone`, since a web font arriving after a bake would otherwise leave the fallback face cached forever — a staleness `fillText()` cannot have.
  - **Kill switch**: `globalThis.__vortexTextAtlas = false` restores the exact previous `fillText()` path, which is kept intact rather than deleted.
  - **Follow-up — the two halves of the text system disagreed on line height.** `TextController.getLineHeight()` used `measureFontLineHeight()` (font metrics: Volter 9px is 11.02px, so a 12px line) while `TextSkinRenderer.compositeTextMultiline()` used `fontSize + 2 + leading` = 11px, an approximation with nothing behind it. The controller therefore sized a multiline box at 12px per line and the renderer stacked lines every 11px, drifting text one pixel per line upward inside its own box — visible on descriptions, long tooltips and any multiline field with a background or border. Now both call `measureFontLineHeight()`; it was the only such hardcoded guess left in either package.
  - **Two dead ends, both measured rather than assumed** — recorded so they are not re-investigated. (1) HiDPI: `Vortex.ts` initialises Pixi at `resolution: 1`, so a display-scaled screen would have the compositor resample the whole canvas; the machine this was tested on reports `AppliedDPI = 96` (100%), so it does not apply here — it would return on a scaled display. (2) Kerning: none of `Volter.ttf`, `Volter Bold.ttf` or `Ubuntu.ttf` carries a `kern`/`GPOS`/`GSUB` table, so the atlas summing per-glyph advances is exactly equal to `measureText()` over the whole string, and no text width can have shifted. A third, `ctx.textRendering = 'geometricPrecision'` at bake time, was tried on the live client and made no visible difference — expected at `SUPERSAMPLE = 5`, where hinting can move a stem by at most 1/5 of a final pixel; see the note at that constant.
  - Comparison harness used to pick the approach: `packages/vortex-client/text-lab.html` (dev-server only, not part of the build).

- ✅ **Navigator modern re-skin — a Vortex-only restyle, zero code changes** (`vortex-client/src/vortex-layouts/navigator_frame_2_xml.xml`). **Not a port**: the dump's navigator layout is restyled, not reimplemented, so the file carries no `AS3:` traces and says so at the top.
  - **Override mechanism, not a fork of the asset.** The file deliberately reuses the dump asset's name. `App.registerVortexLayouts()` (step 3b) runs *after* the dump layouts are registered (step 3) and writes into the same `HabboWindowManager._widgetLayouts` map, so registering `navigator_frame_2_xml` replaces the dump entry that `NavigatorView.LAYOUT_NAME` resolves. It could not live in `src/assets/window-layouts/`: that directory is gitignored and rebuilt by `build-window-assets.mjs`, which would wipe it on the next asset build.
  - **The contract that had to survive** is enumerated in the file header and was checked mechanically (names/types/tags diffed against the dump layout): every `findChildByName` target, the three `block_results` items *in order* (`createMainWindow` clones item 0 three times), the `TEMPLATE`/`SUBTEMPLATE`/`category_header` tags, `navigator_entry_tile` staying a list item of its container, and the geometry anchors code reads back — frame width 578 (`MAX_WINDOW_WIDTH`), `left_pane.x`/`right_pane.x` (their delta is `setLeftPaneVisibility`'s collapse offset), and the row template's height (`rowEntryTemplateHeight` sizes each category card). The tab strip's x cannot be styled from XML at all: `STARTING_TAB_POSITION = 115` overwrites it at runtime.
  - **Six deliberate type changes**, each verified against the *body* of every call that touches the element, not its name: `left_pane`, `create_room_border`, `random_room_border`, `promote_room_border` and `navigator_entry_tile` `<border>`→`<container>` (only `.visible`/`.x` are read, and a skin border paints square corners behind a rounded card); `room_info_usercount_border` `<border>`→`<shape>` so that the `getUserCountColor()` value the factory writes into `.color` renders as a rounded pill. `navigator_entry_row_container` deliberately stays a `<border>` — the alternating row tint from `getModulatedBackgroundColor()` needs a skin-rendered fill.
  - **No new assets.** Modern chrome comes from what already ships: `<shape shape="round_rectangle">` + `radius`/`stroke_*` (`ShapeSkinRenderer`), `<gradient blend BLEND_ADD>`, and the Illumina-light styles already used across the client — dropmenu 100, input border 105, scrollable itemlist 100. This matters: `src/assets/images` is gitignored dump output, so a design depending on new PNGs would not be committable.
  - **Out of scope, and why**: the mockup's "Trending Room" hero, per-room tag labels, "View all" links, tab icons and a bottom action bar have no controller to populate them — they are code, not layout. Create/Random stay in the right pane rather than moving into the sidebar as the mockup shows: the navigator opens with `setLeftPaneVisibility(false)`, so a sidebar Create Room would be hidden by default.
  - Full production build passes and bundles the layout.

- ✅ **Furni editor — a Vortex-only staff tool for rewriting a placed furni's stored server state live** (`habbo/vortex/furnieditor/`, `vortex-client/src/vortex-layouts/`, + emulator). **Not a port**: there is no AS3 or Habbo equivalent, so nothing here carries an `AS3:` trace and every file says so at the top. It edits the real persisted row — transform, altitude, wall offset, extra data, owner and definition — gated server-side on a new `room.furni.edit` capability.
  - **Live refresh came for free.** `RoomMessageHandler` already handles `ObjectUpdate` (pos/rot/state), `ObjectDataUpdate` (stuff data) and `ItemDataUpdate` (wall), so the server re-broadcasts the **standard** composers after an edit and every client in the room updates with no new incoming machinery. Only an outgoing edit request was actually needed; the read/ack packet exists so the window shows the server's truth rather than what was typed.
  - **Wire**: 4 Vortex-specific headers in the **8000–8999 band**, chosen because it is empty in *both* registries (0 occurrences in `HabboMessages.ts` and in the emulator's `Headers.cs`) and clear of the `9xxx` placeholders, which are unresolved *real* messages awaiting a retrace — do not allocate Vortex headers there. 8001 get-data / 8002 data+ack / 8003 apply / 8004 rights. Both sides carry a note; never renumber one alone.
  - **Permission**: `Capabilities.Room.FurniEdit`, deliberately separate from `BuildAny` — that one only lifts the ownership check on the ordinary place/move path, and everything it allows an ordinary player could also do in their own room. This one grants writes with no player-facing equivalent (reassigning ownership, swapping a definition, arbitrary altitude). Both handlers re-resolve it per request; the handshake flag (8004) only decides whether the button is drawn. Every edit publishes `ItemStaffEditedEvent` → `ItemEventType.StaffAction` forensics with before/after JSON per touched field.
  - **Three constraints found by reading the emulator, each worth knowing before extending this**:
    - **A definition swap cannot cross `ProductType`.** `RoomItemsProvider.CreateFromEntity` picks `RoomFloorItem` vs `RoomWallItem` from the definition, so a floor item that became a wall definition would come back as a different kind of object on the next room load. Refused with `definition_product_type_mismatch`.
    - **The dirty-item flush does not persist `definition_id`.** `RoomPersistenceGrain` writes owner/x/y/z/rotation/extra-data/wall-offset and nothing else, and `RoomItem.Definition` is `init`-only — so the swap is a rebuild (detach + re-attach a new object with the same id) plus a **direct** `ExecuteUpdateAsync` on the column. Without that write the swap would show in the room and silently revert on reload.
    - **The swap must not use `ObjectModule.RemoveObjectAsync`.** That enqueues the item with `remove: true`, and nothing clears the flag before the next flush: the row's `room_id` would be nulled moments after the replacement was attached, and the furni would vanish on the next load. It also runs `Logic.OnPickupAsync`, a semantic a swap must not trigger. `SwapFurniDefinitionAsync` is that method minus those two steps, and says why in-code.
  - **Client**: `HabboFurniEditor` (DI Component + `IID_HabboFurniEditor`, per the manager convention) + `FurniEditorView`, built from **authored** layouts in `vortex-client/src/vortex-layouts/` registered by `App.registerVortexLayouts()` via `import.meta.glob` — deliberately *not* `src/assets/window-layouts/`, which is gitignored and regenerated by `build-window-assets.mjs`, so a file there would be wiped on the next asset build. The entry button is added as a list item to the infostand's `infostand_element_list` for the same reason (`furni_view` ships verbatim from the dump). Reached via `Vortex.instance` rather than `IRoomWidgetHandlerContainer`, because adding a non-AS3 member to that ported interface would make the port's traceability lie.
  - **Each input commits on its own mask bit** (no Apply button — that is what makes it real-time), but the payload always carries **every** field: X and Y share the `POSITION` bit, so a commit that filled only the edited one would have teleported the item to row 0 the moment its X was touched. The mask still decides what the server reads; the rest is inert filler. A `_applyingServerState` guard stops the server's echo from bouncing back as fresh edits, since `WE_CHANGE` fires for programmatic writes too.
  - **Second window: the furniture *definition* editor** (`furniture_definitions`, i.e. a furniture *type*), opened from the placed-item editor on that item's current base. Headers 8005/8006/8007, gated on its own `furniture.definition.edit` capability — deliberately not folded into `room.furni.edit`, because one write here changes every placed copy and every future purchase, where the other one touches a single item. The write is `IFurnitureAdminService.UpdateAsync`, the same service the dashboard uses, so the duplicate sprite/type/category guard and the `IFurnitureDefinitionProvider` reload after every write are inherited rather than reimplemented — that reload is what makes it live, since every handler reads the provider's snapshot.
    - **What "live" covers, and what it does not.** The 19 columns are read by the *server*: interaction (`Logic`), footprint, stack height, walk/sit/lay, state count, usage and trade policy — those take effect immediately, hotel-wide, with no restart. The client's *rendering* does not change: `SessionDataManager` loads furnidata from `furnidata.load.url` (an assets host), not from this table, and the emulator does not generate it. Editing `Width` changes where the server allows the item, not the sprite the client draws. Generating furnidata from the DB was considered and deliberately left out of scope.
    - Explicit Save rather than per-field commit, unlike the placed-item window: the write path rewrites every column at once, so a per-keystroke commit would splatter half-typed values across the whole row.
  - **Localization**: no hard-coded strings. Layout captions use `${vortex.furni_editor.*}` / `${vortex.furni_definition.*}` through `WindowParser.localizationResolver`, and the strings built in code go through `getLocalizationWithParamMap` with the key itself as the default, so a missing key is visibly missing rather than blank. The keys ship as plain `key=value` lists in `packages/vortex-client/src/vortex-locale/` for the hotel to paste into its own localization file — no loader, deliberately: the external file stays the single source of truth.
  - **UI redesign pass** (both windows). Fields the operator can't be expected to know by heart became dropdowns (`<dropmenu>` → `IDropMenuWindow.populateWithStrings`/`selection`/`enumerateSelection`): the definition editor's **interaction (`logic`)** — all 151 `[RoomObjectLogic("...")]` keys, extracted from `Vortex.Rooms/Object/Logic/**`, in `FurniDefinitionOptions.ts` — plus **product / category / usage / stuff-data**, mapped index↔enum value. The logic dropdown injects the current server value if the embedded list has drifted, so an existing row is never silently reassigned (drift only hides a value from the picker). Genuinely free fields stay inputs. Both authored layouts were rebuilt in the glaze vocabulary — tinted section-header bars (`container background` + bold `il_button` label), a shared label column (x=8) and field column so inputs align, 21-high `style=105` boxes and `style=100` dropmenus — replacing the flat, unaligned 28px stack. Styles verified present in the client's own dump skins (dropmenu 100, checkbox 101). New section-header localization keys added to the two `.txt` lists.
  - Both packages typecheck + lint clean (0 errors), full production build passes and bundles the layouts; the emulator solution builds with 0 errors.

- ✅ **Room furniture-manipulation click shortcuts (CTRL=pick up, SHIFT=rotate) — the "Habbo keyboard shortcuts" that are actually mouse-modifier gestures.** An exhaustive AS3 sweep established there are essentially **no room keyboard shortcuts** (no arrow-walking/camera pan/screenshot): the only in-room keyboard use is chat (SHIFT+Enter=shout, already ported at `RoomChatInputView.ts:391`). Everything users call a "shortcut" is a modifier+click in `RoomObjectEventHandler` (`_SafeCls_1821.as`): plain click sends the click-through (gated off at line 581 when any modifier is held), then `handleRoomObjectMouseClick` branches — **SHIFT-only click rotates floor furni** (cat 10 only, line 753 → `REOE_REQUEST_ROTATE`), **CTRL-only click picks up floor/wall furni** (line 763 → `REOE_REQUEST_PICKUP`), ALT-drag moves (mouse-down, line 1065 → `REOE_REQUEST_MOVE`). These `REOE_REQUEST_*` events were defined in the port but **never emitted or handled**.
  - **Wired the full faithful chain**: `RoomEngine.handleObjectMouseEvent` now emits `REOE_REQUEST_ROTATE`/`REOE_REQUEST_PICKUP` for the two exact gestures (only these are intercepted; plain clicks and other combos fall through to normal selection; disabled in game mode per AS3 line 751) → `RoomUI` subscribes the three request events → `RoomDesktop.roomObjectEventHandler` handles them exactly as AS3 `RoomDesktop.as:1222-1236`: `checkFurniManipulationRights()` (ported: controllerLevel≥1 / isAnyRoomController / isOwnerOfFurniture / `activeRoomHasFreeFurniMovementsMode`) gates MOVE/ROTATE, PICKUP is ungated (server validates), then `modifyRoomObject(OBJECT_MOVE/OBJECT_ROTATE_POSITIVE/OBJECT_PICKUP)` — all three cases already existed. Added `RoomEngine.activeRoomHasFreeFurniMovementsMode` (reads the `free_furni_movements_mode` room var). Also removed a leftover `[CLICK]` debug `log.info` that was flooding the console on every furni click.
  - **Follow-up pass — the remaining modifier/wheel gestures wired** (same faithful chain):
    - **ALT+drag = move floor furni (cat 10)** — added `RoomEngine.handleObjectMouseDown()` (ports `handleRoomObjectMouseDown`, `_SafeCls_1821.as:1040`): ALT-only mouse-down (or a plain drag in decorate mode, per `decorateModeMove()`) on floor furni dispatches `REOE_REQUEST_MOVE` → the existing `RoomDesktop`/`checkFurniManipulationRights`/`modifyRoomObject(OBJECT_MOVE)` chain → the ghost follows the cursor (`handleObjectMove`) and a click confirms (`confirmObjectMove`). Routed via a new `ROE_MOUSE_DOWN` branch in `handleObjectMouseEvent`. AS3 also dispatches this for wall (cat 20) and bot/plant (cat 100), but the port's move machinery + wall/user-move composers are cat-10-only (pre-existing documented gaps), so only cat 10 is enabled.
    - **Wheel-rotate-preview** — ported `RoomEngine.rotateActiveObjectPreview()` (`_SafeCls_1821.as:829`, pure-local: validates the next direction, `setDirection`, `updateSelectedObjectData`, `recalibrateMovements`). Wired in `App.ts._onWheel`: a plain wheel (no modifier) over the room while a cat-10 place/move preview is active rotates the ghost (Flash delta>0 = scroll-up = forward → DOM `deltaY < 0`); CTRL+wheel still zooms.
    - **CTRL+click monsterplant = pick up** — added `modifyRoomObject` `OBJECT_PICKUP_PET` (`roomSession.pickUpPet(webID)` resolved via `userDataManager.getUserDataByIndex`). Fully works (pets are server-supported).
  - **Deferred, `TODO(AS3)` (genuinely blocked, documented in-code)**: rentable-**bot** pickup/rotate + monsterplant **rotate** need the unported user-move/bot composers (`_SafeCls_3108` id 2743, `_SafeCls_2801` id 1295, `_SafeCls_2560` id 432) **and the Turbo server has no bots** (docs/CLIENT-SERVER-ARCHITECTURE.md §"Bots — Do Not Exist"), so building them would be untestable dead wire code — the gestures are recognised, the send is stubbed with a `log.warn`; **cat-20 wall move** + **cat-100 move commit** (wall-move composer + `getLegacyGeometry`, user-move composer — same pre-existing gaps); **SHIFT+drag rectangular area selection** (`RoomAreaSelectionManager`'s interactive tile-highlight is a standing TODO — a decorate multi-select subsystem, left as one deliberate remaining piece). Both packages typecheck + lint clean (0 errors).

- 🔴 **Clicking empty space never deselected, so no context UI ever closed — `RoomEngine` was discarding `canvas.handleMouseEvent()`'s return value.** Surfaced live: the own-avatar bubble wouldn't dismiss on clicking away, and the user noted *the furni/user InfoStand panels had the same problem* — which is what moved the search upstream of the new widget to the shared cause.
  - **Root cause.** AS3 (`_SafeCls_90.as:2391-2398`) dismisses from the **canvas** mouse handler: `if(!canvas.handleMouseEvent(...)) { if(click) dispatch REOE_DESELECTED(roomId, -1, MINIMUM) }` — an unconditional deselect whenever no room object consumed the click. The port called `canvas.handleMouseEvent(...)` and **threw the boolean away** (`RoomEngine.handleRoomCanvasMouseEvent`), so the "nothing consumed this click" branch did not exist. `deselectRoomObject()` is additionally guarded on a prior selection, so it could not cover the gap either. `InfoStandWidget` already listened for `RWROUE_OBJECT_DESELECTED`; it simply never arrived. Fixed by capturing the return and emitting the AS3 event — which closed the InfoStand panels with no change to them.
  - **A wrong first attempt worth recording**: the same AS3 snippet was first ported into `handleObjectMouseEvent`, which only runs once a room object *was* hit — so an empty-space click never reached it and nothing changed. The fix was matched to AS3 by code content instead of by tracing which port function corresponds to the AS3 one. The second pass added a temporary `[CLICK-AWAY]` log to confirm the real path from the console rather than deduce it again.
- ✅ **Own-avatar bubble finished to AS3 parity** (follow-up to the slice below): opens on **clicking your own avatar** (the id needed for positioning was only resolved in `selectOwnAvatar()`, so the direct-click entry produced an invisible bubble until the toolbar me-menu had been used once); real **minimize** collapsing to the `minimized_menu` bubble and re-expanding on click; **name change** wired to `habboHelp.startNameChange()` with `IHabboHelp` added to `IRoomWidgetHandlerContainer` (injected RoomUI → RoomDesktop, mirroring the inventory wiring); and a null-guard pass on `HabboInventory`'s effect API (`_effectsModel` is created in the lazy `init()`, so the bubble could read effects before the inventory existed — AS3 guards `effectsModel == null` in each method). dance/expression/posture, effects, and drop-carry/wired-inspect/open-profile all reach their handlers through the faithful message routing. Only `change_looks` (avatar editor) and `decorate` (DecorateModeView) remain `TODO(AS3)`, blocked on unported subsystems.

- ✅ **Own-avatar bubble menu (`RWE_AVATAR_INFO` slice) — the faithful trigger for the effects widget** (`habbo/ui/widget/avatarinfo/`, `habbo/ui/widget/contextmenu/`, `habbo/ui/handler/AvatarInfoWidgetHandler.ts`). The effects flyout shipped without a trigger; the authentic entry point is the in-room bubble next to your own avatar (`OwnAvatarMenuView`). Investigation established `MeMenuMainView`/`RWE_ME_MENU` is legacy dead code in WIN63 — the modern bubble is `OwnAvatarMenuView`, part of the `RWE_AVATAR_INFO` widget. Ported the **own-avatar slice**, deferring pets/bots/other-avatars/decorate/breeding/use-product.
  - **Built**: the message primitives (`RoomWidgetDanceMessage`/`RoomWidgetAvatarExpressionMessage`/`RoomWidgetChangePostureMessage`, `AvatarExpressionEnum`), `AvatarInfoData` (`_SafeCls_2798`, populated from the existing `RWUIUE_OWN_USER` event), the context-menu view base hierarchy (`ContextInfoView` build/show/hide/per-frame-position + `ButtonMenuView` showButton/showButtonGrid/hover + `AvatarContextInfoButtonView`), `OwnAvatarMenuView` (mode-driven buttons: normal/club-dances/expressions/signs; each dispatches a `RoomWidget*` message), the shell `AvatarInfoWidget` (opens on `RWUIUE_OWN_USER`, repositions each frame via the update receiver + live state getters isDancing/hasClub/hasEffectOn/hasVip/posture/swimming/riding), and `AvatarInfoWidgetHandler` (toolbar MEMENU → `selectOwnAvatar`; routes dance/expression/posture → `roomSession.send*`). Wired `RWE_AVATAR_INFO` into `RoomDesktop.createWidget` + `RoomWidgetFactory` (RoomUI already creates it).
  - **Reused**: the 4 avatar composers (Dance/Expression/ChangePosture/Sign via `RoomSession.send*`), `getRoomObjectBoundingRectangle` for positioning, the toolbar `HabboToolbarEvent` MEMENU click, the ported `InfoStandWidgetHandler`'s `RWUIUE_OWN_USER` emission, the existing `FixedSizeStack`, and `EffectsWidgetHandler` (the bubble's `effects` button dispatches `RWRWM_EFFECTS`, closing the loop from the effects branch).
  - **AS3 adaptations** (documented in-code): positioning calls `roomEngine.getRoomObjectBoundingRectangle` directly instead of the `RWGOI` message round-trip (+ derives the screen point from the rect, since `getRoomObjectScreenLocation` is private); `selectOwnAvatar` sends `GET_OBJECT_INFO` (no `roomEngine.selectAvatar` in the port); action routing is consolidated into one handler (AS3 splits it across MeMenu/InfoStand handlers); `isDancing` is derived optimistically from the sent dance style (no `RSDE_DANCE` round-trip); the hover hit-test refinement and the FixedSizeStack are simplified/reused.
  - **Deferred, `TODO(AS3)`**: `openAvatarEditor` (avatar editor not ported), decorate mode (`isUserDecorating` stub), name-change (`habboHelp` not ported), the sign-grid per-icon `setImageAsset`, the minimized/new-user-help views, and the friend relationship badge. Typechecks + lints clean (0 errors).

- ✅ **Avatar effects UI — the me-menu EffectsWidget + its owned-effects data pipeline** (`habbo/inventory/effects/`, `habbo/ui/widget/effects/`). Investigation first corrected two stale premises: (1) the effect **render/apply** pipeline (network `AvatarEffect` 3629 → `AvatarLogic`/`AvatarVisualization.appendAction(EFFECT)` → `EffectAssetDownloadManager` → fx animation) was **already fully ported and wired** — nothing to build there; (2) the **emulator** (`vortex-emulator`, Turbo Cloud) effects subsystem was **not** deferred as its docs claimed — it is fully implemented (parsers/handlers/`PlayerEffectGrain`/`player_effects` table/serializers/login-push/room re-sync/catalog grant/expiry/wired) and **byte-compatible** with the client. All effect header IDs and all 5 serializer field orders were cross-checked against the WIN63 registry (`_SafeCls_2046.as`) and the client parsers and **match** (2405/1577/3814/2236/3629 out; 2362/3022 in). The emulator's extra Selected-ack composer at 1333 is harmless — the client registers no handler at 1333 (no collision).
  - **What was actually missing was the client-side effects *UI* + data population.** The `inventory/effects/` `EffectsModel`/`Effect` existed but were **orphaned** (never populated; `EffectsModel` was a data-pure divergence that sent no composers). Reworked `EffectsModel` to the AS3 shape (takes an `IConnection`, sends `AvatarEffectActivatedComposer` 3022 / `AvatarEffectSelectedComposer` 2362 itself). Ported the 3 missing incoming pushes — `AvatarEffectAdded` (1577), `AvatarEffectActivated` (3814), `AvatarEffectExpired` (2236), parser read-orders verified against `unknowns/_SafePkg_2975/*`. Added the inventory-owned effects message handling (faithful to `inventory/_SafeCls_1951.as`) in `HabboInventory` — populates `EffectsModel` from 2405/1577/3814/2236 and fires `HabboInventoryEffectsEvent('HIEE_EFFECTS_CHANGED')` — plus the `getAvatarEffects()`/`setEffectSelected()`/`setEffectDeselected()`/`getAvatarEffect()` API the widget consumes. (`SessionDataManager` still caches 2405 in a dead field; the `MessageRegistry` allows both listeners.)
  - **The inventory "Effects" tab was deliberately not ported** — it is legacy/unreachable in the modern client (no `effects` tab in `inventory_xml`, the WIN63 dump's `182_inventory.xml`, or `InventoryMainView.getWindow()`). The real modern surface is the in-room me-menu flyout, so `EffectsWidget` + `EffectView` (`memenu_effect_{selected,unselected,inactive}` rows, countdown bar, icon) + `EffectsWidgetHandler` (RWE_EFFECTS) were ported from `ui/widget/effects/`. Wired: `IRoomWidgetHandlerContainer.inventory` (new; injected RoomUI→RoomDesktop via `IID_HabboInventory`), registered in `RoomWidgetFactory` + `RoomDesktop.createWidget` + created on room enter in `RoomUI`. `App.ts` now eager-decodes `fx_icon_*`/`memenu_fx_*` into the asset library (they were URL-registered only — same silent-blank-bitmap trap the `ctlg_` fix addressed).
  - **Deferred, `TODO(AS3)`: the trigger.** The faithful entry point (the avatar-bubble me-menu `ui/widget/memenu/MeMenuMainView`, or the own-avatar `OwnAvatarMenuView` 'effects' case) is not ported, so nothing yet dispatches `RWRWM_EFFECTS`. The widget is complete and opens as soon as that message is sent; entry-point choice left to a follow-up. Typechecks + lints clean (0 errors).

- 🔴 **Landing view: every fixed-placeholder widget (avatarimage, dailyquest, catalogpromo, communitygoal, expiringcatalogpage, etc. — all 15 `FIXED_WIDGET_TYPES`) was silently never created, despite the module being marked "fully ported."** Surfaced live: the hotel view's `widget_placeholder_avatarimage` icon never got swapped for the real avatar, and the browser console showed nothing at all about it — not even a failed-lookup warning, which was the tell that `initialize()` was never being reached.
  - **Root cause — `WidgetContainerLayout.ts::registerPlaceholderAnchoredWidget()` had an invented `if(!this._landingView || !this._settings) return;` guard with no AS3 equivalent.** The constructor calls `registerFixedWidgets()` *before* assigning `this._settings = new CommonWidgetSettings(...)`, in both AS3 and this port, so `_settings` is genuinely `null` at that call site — matching AS3 exactly. AS3's own `registerPlaceholderAnchoredWidget()` doesn't check for that; it pushes the `WidgetContainer` unconditionally, passing the still-null settings through (harmless: `WidgetContainer` accepts a null `settings`, and none of the 15 fixed widget types implement `ISettingsAwareWidget`, so nothing ever reads it). The added guard turned that AS3-faithful `null` into a silent, permanent no-op — `_widgetContainers` never received any of the 15 fixed widgets, so `WidgetContainerLayout.activate()`'s refresh loop had nothing to iterate and every `widget_placeholder_*` XML placeholder stayed on screen forever. Fixed by dropping the `!this._settings` half of the guard.
  - **A real but secondary bug found en route**: `friendbar/landingview/widget/AvatarImageWidget.ts::initialize()` called `landingView.getXmlWindow('avatar_image')`, which resolves to the bare `avatar_image_xml` key — but that asset was split into `HabboFriendBar_avatar_image_xml`/`HabboWindowManager_avatar_image_xml` by the collision-qualification fix described below (same-named embed in two components), and the bare key was never re-pointed at either. `habbo/window/widgets/AvatarImageWidget.ts:96` already had the equivalent fix (with a comment documenting the exact collision) for its own, unrelated internal lookup — the landing-view call site had just been missed. Fixed to call `windowManager.buildWidgetLayout('HabboFriendBar_avatar_image_xml')` directly. This alone had no visible effect until the guard above was also fixed, since the code path calling it was unreachable either way.
  - **Diagnosis method, worth repeating**: reading `WidgetContainer.refresh()` in isolation looked correct (it unconditionally removes+disposes the placeholder once `initialize()` runs, even if the widget's container came back null) — the actual break was one level up, in whether `refresh()` ever got a `WidgetContainer` to call in the first place. Confirmed via live console probes against the running client (`Vortex.instance.windowManager`, walking the real desktop window tree with `findChildByName`/`getChildByName`, and calling `buildWidgetLayout()` directly to rule out an asset-side failure) rather than more code-reading — the asset lookup worked standalone the whole time, which is what pointed at construction/registration instead of asset resolution.

- 🔴 **Window layouts and skins now ship as XML named exactly as AS3 names them; the JSON compile step is gone, and with it two silent asset-corruption bugs.** Five tools (`compile-window-layouts`, `compile-window-skins`, `build-asset-name-manifest`, `organize-binary-layout-xml`, `import-crypted-layouts`, `import-crypted-skins`) collapse into one `build-window-assets.mjs`; `WindowLayoutXmlSerializer.ts` is deleted.
  - **The JSON was a round trip.** `registerWidgetLayout(name, xml)` has always taken an XML *string*: the pipeline compiled XML→JSON at build time, then `WindowLayoutXmlSerializer` re-serialized JSON→XML at boot to feed the same parser. `WindowParser` already folds `<params>` children into the param bitfield and URL-decodes `name`/`tags`/`caption` at runtime, exactly as AS3 does, so everything the compile step pre-baked was recomputed anyway. Assets are now emitted verbatim.
  - **Naming is now single-sourced from `*Com.as`**, the field name AS3 passes to `assets.getAssetByName()`. The old chain guessed three ways: a uid manifest built from the sibling `../vortex-client` checkout (which resolved only 157 of 783 layouts — its regex matched `public static const`, and 2045 of that tree's 2535 declarations are `var`), a filename derivation off `binaryDataXml_organized/` (whose names come from the XML's internal `<layout name="...">`, a Flash-authoring label AS3 never reads), and `resolveSkinAssetId()`'s layout→asset back-inference. All three are gone, along with the `../vortex-client` dependency. 2958 declarations resolve with **0 unresolved XML refs**.
  - **Two real bugs, both from collapsing an embed's linkage name to its short form** (`stripTypeSuffix`, still correct for the image/chatstyle importers that own it). `avatar_image_xml` is declared by *both* HabboFriendBar (`_SafeCls_224`) and HabboWindowManager (`_SafeCls_223`) — different embeds, same field name, because AS3 asset libraries are per-component. Both reduce to `avatar_image`, so only one file was ever written and FriendBar's variant was silently lost. Worse, `_SafeCls_894` (`header_png$…`, a `Bitmap` declared as `badge_leaderboard_header`) and `_SafeCls_895` (`header_xml$…`, the real `habbo_window_layout_header_xml`) both reduce to `header` — so the header *layout* was shipped under an *image's* name. The `*_png` entries that used to sit in `window-layouts/` (`icon_messenger_png`, `illumina_light_switch_png`, `icons_facebook`) were all this. Declarations are now joined to files on the whole linkage name, hash included; genuine same-name collisions are qualified by component (3 of them).
  - **`normalizeAssetName()` no longer strips `_xml` from the element description's `asset`/`window_layout`.** Both are asset-library keys AS3 uses verbatim; the strip forced the old pipeline to *also* emit 61 `habbo_window_layout_*` aliases so `getLayoutByTypeAndStyle()` and `loadSkinAssets()` could find them. It still strips `_png` for atlas refs, which do map to filenames.
  - **`InfoStandFurniView`'s white background was a port bug, not an asset bug** — and the previous "fix" (hand-editing the generated `furni_view.json`'s `info_border` from style 2 to style 1, commit `7806456`) masked it and was erased by regeneration. Style 2 is `border_colorless`: a deliberately untinted skin. AS3's `update()` tints it per provenance — `isBuilderClubId` → `0x331C00`, `isTempId` → `0x142B44`, else `0x3D3D3D` — and sets `spacerColor` alongside. The port had an invented `isBuilderClubId(id) ? 0xD77900 : 0xFFFFFF`, no temp branch, and no `spacerColor` setter at all; `0xFFFFFF` is the white. Ported for real, along with `isTempId`/`isNormalId` and the true ranges (`sources/win63_version/habbo/utils/class_2262.as` is an older revision with an unbounded `isBuilderClubId` and no temp range — the port had followed it).
  - **Assets are no longer tracked.** They are derived dump content like `sources/`, `configurations/` and `*.png`; the 990 committed JSON files were the anomaly. Both directories are now gitignored and regenerated per checkout.
  - **Verified**, not reasoned: all 916 documents parse in the real Blink `DOMParser` (Edge headless, mirroring `parseXmlDocument`'s own repair fallback) with 1 needing the existing `repairMalformedAttributeSpacing()` and 0 failures; and the client boots to `Element registry loaded: 332 descriptors` + `Skin assets loaded: 309 renderers registered from 132 skins` — the latter being the decisive check, since a wrong `asset` key would have matched no descriptors and registered 0.
  - Known gap, pre-existing: `RoomUserCountWidget` requests `room_user_count_xml`, which is declared nowhere in the AS3 and never existed as an asset. It resolved to nothing before this change too.

- 🟢 **Pet pickup from a room now actually fires — the last missing link in the pet infostand chain.** The pet buttons (`btn_pick`/`btn_kick` for an owned pet) dispatched `RoomWidgetUserActionMessage('RWUAM_PICKUP_PET', petId)` correctly, but `InfoStandWidgetHandler.processWidgetMessage()` had **no `RoomWidgetUserActionMessage` branch at all** — every `RWUAM_*` message fell straight through to the `UNIMPLEMENTED_WIDGET_MESSAGES` debug log and was dropped. Ported AS3's `processWidgetMessage()` `RWUAM_*` pet cases as `processUserActionMessage()`: it pre-resolves the pet via `userDataManager.getPetUserData(userId)` and bails on `null` (AS3's stale-click guard), then routes `PICK_UP_PET`→`pickUpPet`, `MOUNT_PET`/`DISMOUNT_PET`, `TOGGLE_PET_RIDING/BREEDING_PERMISSION`, `SADDLE_OFF`, and `RESPECT_PET` (the spaced `' RWUAM_RESPECT_PET'` literal) to the already-ported `roomSession`/`sessionDataManager` methods. `pickUpPet` → `PickUpPetComposer` (header **1640**, answered by the emulator's `RemovePetFromFlatMessageHandler` → `PickUpPetInRoomAsync`). Pet care composers (treat/water/light: AS3 `_SafeCls_1909`/`_SafeCls_2695`) are unported and left as a visible `TODO(AS3)`; the user-scoped `RWUAM_*` cases (whisper/kick/ban/…) belong to the not-yet-wired InfoStandUserView and stay in the unimplemented list.
  - **Guest place-pet now wired too.** `PetsModel.placePetToRoom()` had the room-owner path (drag-drop via `initializeRoomObjectInsert`) but the guest branch (non-owner, pets allowed) was a `log.warn` stub. Ported AS3's `new _SafeCls_2777(pet.id, 0, 0)` as `PlacePetComposer` (`[petId, x, y]`, header **1018** `PlacePetMessageEvent`, matching the emulator's `PlacePetMessageParser` → `PlacePetMessageHandler`). AS3 always sends x=y=0; the server resolves the drop tile. Not to be confused with `MovePetMessageComposer` (4-arg vortex-custom, header 2761), which repositions an already-placed pet.

- 🔴 **Pets are usable for the first time: they render as themselves in a room, and clicking one opens the infostand. Three separate one-line-shaped bugs, each found by a trace, none by reading code.** Verified live against the running client + emulator.
  - **Pets rendered as black boxes — and had since forever.** `RoomEngine.addObjectUser()` skipped AS3's `if(_loc11_ == "pet") _loc11_ = getPetType(param7);`, so the room object was created with the literal type `'pet'`. No asset library is called that, and `RoomContentLoader.hasInternalContent()` recognises only `'user'`/`'game_snowball'`/`'game_snowsplash'`, so `RoomManager` resolved logic+visualization off a non-existent bundle and hit its `default:` — **`FurnitureLogic` + `FurnitureVisualization`**. `FurnitureLogic` ignores `RoomObjectAvatarFigureUpdateMessage`, so the figure was dropped, the model never got `pet_type`/`pet_palette_index`/`pet_color`, and **`PetLogic` was attached to nothing — dead code since it was ported**. Ported AS3's `getPetType(figure)` (first figure token → `contentLoader.getPetType(typeId)`) and made `addRoomObjectUser()` pass `type` to `createRoomObject()` instead of a mapped `logicType` — that mapping was a pure identity (`RoomObjectLogicEnum` and `RoomObjectUserTypes` declare the same four strings), so it changed nothing for users/bots but undid the pet-type resolution. This also explains why "pet rendering made to work" (below) did not cover it: that pass validated `getPetImage()`, the catalog path, which never goes through here.
  - **`UserDataManager.requestPetInfo()` was an empty `TODO(AS3)`** — the client never asked the server for pet info, so the whole Phase 8 chain sat idle waiting for a message that was never sent. Ported `GetPetInfoMessageComposer` (header **3899**, AS3-verified in the emulator's own `Headers.cs`, answered for real by `GetPetInfoMessageHandler` with `PetInfo` 3192) and wired it. Also ported `PetSelectedMessageComposer` (**2757**, likewise real server-side — it is what answers with `PetCommands` 332).
  - **Two `UserDataManager` instances, only one wired.** `SessionDataManager` builds one *and gives it a connection*; `RoomSession` builds its own and never did — and `container.roomSession.userDataManager` is the second one. Every composer-sending method on it was silently inert. AS3's `RoomSession.set connection()` forwards the connection to its UserDataManager (`if(var_1480 != null) var_1480.connection = param1;`); this port only did `this._connection = value`. Same shape as the duplicate-IStuffData-trees trap: two instance trees, the one in use is not the one wired.
  - **`InfoStandPetView` ported in full** (59-line stub → the 892-line AS3): name, centred pet image, level/breed/age, respect, the happiness/experience/energy bars or the monsterplant well-being/growth pair, and the right-to-left button strip. `PetCommandTool` is not ported, so the training tool is a documented `TODO(AS3)` at four call sites. Also ported `RoomWidgetUserActionMessage` (**absent entirely** — the pet buttons had no message class to dispatch; note `RESPECT_PET`'s value has a leading space verbatim from AS3, and the handler matches the same spaced literal) and `SecondsFormatter` (AS3's class name is obfuscated in the primary *and* secondary and absent from the tertiary — unrecoverable, so the class name is this port's; `formatSeconds()` is AS3's own).
  - **Method note.** All three root causes came from instrumenting the running client; every prior guess from reading the code was wrong (including "the black square is the `place_holder_pet`" — it was a real pet object with furniture logic). One probe that hardcoded room-object ids from an earlier session reported "no object" for a fix that was in fact working: check the trace measures the object you think it does.

- 🔴 **Phase 8 step 2: the 8 remaining `room/pets` events+parsers ported — and the premise that "the server already sends everything" is false for 6 of them.** Cross-checking every AS3 parser against `Revision20260701`'s serializer (the method that validated every parser in step 1) found that only **`PetCommands` (332)** and **`PetPlacingError` (3195)** agree with the server. Three serializers have an **empty body** (`PetStatusUpdate` 2753, `PetFigureUpdate` 3796, `PetRespectFailed` 31) and three write a different message than the AS3 defines (`PetLevelUpdate` 3104 omits the leading roomIndex; `PetBreedingResult` 2940 writes 3 ints instead of two 7-field records; `PetExperience` 946 writes 5 ints where the client reads 3). `Revision20260112` is identical throughout — unfinished server work, not a regression. All 8 composers *are* dispatched (2-4 call sites each). Full table now in `docs/CLIENT-SERVER-ARCHITECTURE.md` §18, which previously claimed pets were "Fully Implemented" — corrected to scope that claim to the game logic.
  - **`PetExperience` (946) is the only one that fails silently** and is the real hazard: the server writes more data than the client reads, so nothing throws — `petRoomIndex` receives the total experience and `gainedExperience` receives the next-level threshold. Carries a prominent `TODO(AS3)`. The other five throw `RangeError('End of buffer')`, which `SocketConnection` catches and logs per message (each packet has its own wrapper, so no stream desync) — a loud failure, which is the better kind.
  - Ported AS3-faithfully and registered regardless: the client's job is to speak the real protocol and the fix belongs in the emulator's serializers. Headers taken from the emulator's own `Revision20260701/Headers.cs` (several carry its "AS3-verified" annotations); 20260701 is the revision that pairs with `WIN63-202607011411`. **The primary AS3 tree has no pet message package at all** — it is package-obfuscated under `src/unknowns/`, so `win63_version` is the authority for this whole set (another instance of the known-wrong `src/unknowns/` claim in `CLAUDE.md`).
  - **A 5th occurrence of the `while(0 < count)` decompiler corruption** found in `parser/inventory/pets/class_2486.as` (the pet figure wire class `PetFigureUpdate` embeds) — the custom-part loop increments an index it never compares. Already correctly ported as `parsePetFigureData()`, which `PetFigureUpdateEventParser` reuses rather than duplicating.
  - **`PetRespectFailedEvent` was already ported and registered** under `incoming/notifications/` (its directory does not match AS3's `room/pets/`, but its parser is already field-exact). Writing a second copy would have **silently overwritten** the first, since `_events` is a `Map` and `.set()` does not warn — caught by a collision scan before the build. Left the original in place; a global scan now confirms no duplicate header exists anywhere in `HabboMessages.ts`.

- 🟢 **Phase 8: the `PetInfo` data chain now runs from the wire to `InfoStandPetData`** (1 new widget event + 3 files filled in), continuing `e72e51f` (`PetInfoMessageEvent` parser + header 3192 + `RoomUsersHandler.onPetInfo()` → `RoomSessionPetInfoUpdateEvent`). The break was that **nothing subscribed to `RSPIUE_PET_INFO`**: `InfoStandWidgetHandler.set container()` wired only its connection's group-details event and registered *none* of AS3's nine `roomSessionManager` listeners, so the session event was emitted into the void. The chain is now: click → `handleGetObjectInfoMessage()` case 100 → `handleGetPetInfoMessage(userData.webID)` (AS3 passes the **webID** here, not the room index it passes to the user/bot handlers) → `requestPetInfo()` → server → parser → `RoomUsersHandler` → `sessionEvents` → `InfoStandWidgetHandler.onPetInfo()` → `RoomWidgetPetInfoUpdateEvent` → `InfoStandWidget.onPetInfo()` → `InfoStandPetData.setData()`.
  - **`RoomWidgetPetInfoUpdateEvent`** ported (10 constructor-assigned read-only getters + 24 getter/setter pairs, preserving AS3's split); `InfoStandPetData.setData()` and `InfoStandWidget.onPetInfo()` were empty stubs and now copy AS3's exact field set — note AS3 deliberately does *not* copy `hasFreeSaddle`/`isRiding`/`canBreed`/`canHarvest`/`canRevive` into `InfoStandPetData`, and routes `petRespectLeft` to `InfoStandUserData` instead.
  - **The secondary tree's `onPetInfo()` is decompiler-corrupted and the primary is clean** — the same trap as `PetAnimationSizeData` above. `win63_version` computes the monsterplant growth posture (`"std"` / `"grw"+level`) into a local, then drops it: the cache key decompiles to `figure + ""` and the image call to `getPetImage(figure, null)`. The primary keeps both: key is `figure + "/posture=" + posture`, call is `getPetImage(figure, posture)`. Porting the secondary verbatim would have collapsed every growth stage of a monsterplant onto one cache entry.
  - **`InfoStandPetView` was still a 59-line stub against 892 lines of AS3** at the time of this entry (`window` always null), so `selectView(PET)` resolved to no child and the panel showed nothing. Superseded — see the pet-usability entry above, which ports the view and closes the chain.
  - ~~`PetSelectedMessageComposer` … its header is unverified and `Revision20260701` has no handler for it.~~ **This was wrong.** The header is AS3-verified (2757) and `PetSelectedMessageHandler` does exist. The claim came from a grep run with a relative `vortex-emulator/` path from inside `vortex-modern-client/`, where that directory does not exist — it returned nothing, and the empty result was read as proof of absence. A false negative is not evidence; check the command resolves before trusting a null result. Composer now ported and registered.

- 🔴 **Pet rendering made to work for the first time — 10 engine bugs, found by driving the real client, most of them not pet-specific.** Phase 7's widgets were the first caller to exercise `RoomEngine.getPetImage()` end-to-end, and it had never functioned since being ported. Every one of these failed *silently* (a `continue`, a default, a coercion) rather than throwing, which is why they had gone unnoticed. Verified live against the running client at each step.
  - **`RoomObjectVisualizationFactory`**: `case PET_ANIMATED` was missing from `createRoomObjectVisualization()`'s switch entirely, so every pet resolved to a null visualization and `getGenericRoomObjectImage()` returned `id = -1`. `pet_animated` was also folded into the *avatar* branch of `getRoomObjectVisualizationData()` and handed an `AvatarVisualizationData`; AS3 gives it its own `AnimatedPetVisualizationData` (Nitro's own factory agrees). Added an `assets` setter (AS3 reads `assets` off its Component base) so `AnimatedPetVisualizationData.commonAssets` can be injected, wired from `RoomEngine`'s constructor.
  - **`AnimatedPetVisualizationData` had three *invented* overrides** — `getDirectionValue()`/`getZOffset()`/`getTag()` — with no AS3 counterpart, shadowing `FurnitureVisualizationData`'s real implementations. `getDirectionValue()` returned `Math.floor(direction / 45) * 45`, i.e. the raw angle (90) instead of the visualization's own direction index (0-7), so every sprite was looked up as `cat_64_a_90_0` — a name no pet library contains. **Deleting them is what first made a pet appear.** They were labelled `// TODO: Implement...`, which is what made them read as gaps to fill rather than code to remove.
  - **Asset layer, 4 bugs, all affecting any palettised `.nitro` content and not just pets**: `RoomContentLoader` returns before `collection.define()` for every bundle with a spritesheet, and `define()` was the only place palettes were parsed — so no `.nitro` library ever had any (`defineFromSpritesheet()` now takes them). `rgb` is an array of `[r,g,b]` triplets, not a flat byte run, and `new Uint8Array([[0,0,0],…])` coerces each triplet to 0 — every palette loaded solid black. `usesPalette` is a real JSON boolean and was read through `getNumber()`, which handled only numbers and strings, so it always answered "no palette". `colorizePalette()` blitted `texture.source.resource` — the *whole atlas* — at 0,0 into a frame-sized canvas, colourising whatever sat in the atlas's top-left corner.
  - **`AnimatedPetVisualization` was missing four AS3 overrides**: `getAsset()` (pets are palette-swapped via `getAssetWithPalette()`; without it they rendered greyscale even though `_paletteName`/`_customLayerIds`/`_customPartIds`/`_customPaletteIds` were already being filled in and read by nobody), `getSpriteColor()` (same shape: `pet_color` was parsed off the model every frame and never applied), `getSpriteAssetName()`/`getSpriteZOffset()` + the `getDirection()`/`isHeadSprite()`/`isNonHeadSprite()`/`isSaddleSprite()` helpers (a pet's head resolves its own direction), and **`getAnimationId()`** — the last one found: furniture has one animation so the base answers with its single shared `animationId`, but a pet runs posture and gesture states concurrently and each must answer with its own. Without it both states resolved to the same animation and produced identical frames; since `getSpriteXOffset()`/`getSpriteYOffset()` sum the frames of *every* state (faithfully to AS3), each layer's offset was applied exactly twice — the head and tail rendered visibly detached.
  - **`PetAnimationSizeData` (AS3 name; Nitro calls it `PetSizeData`) had never been ported**, and `AnimatedPetVisualizationData`'s posture/gesture methods were stubs returning `-1`/`''`, `createSizeData()` explicitly commented "when pet rendering is implemented". Ported from the primary tree (the secondary's copy is decompiler-corrupted: `add(null,0)` instead of `add(_loc6_,_loc7_)`); uses `OrderedMap`, not a native `Map`, because AS3 reads these tables back *by index* via `getKey()`.
  - **`FurnitureVisualizationData.processVisualizationElements()` iterated a hard-coded `['layers','directions','colors','animations']`** where AS3 walks the visualization node's actual children. No subclass could contribute an element, so `AnimatedPetVisualizationData`'s `'postures'`/`'gestures'` cases were unreachable dead code and no pet ever had an animation. Now iterates the real keys — safe because an unknown name falls through the switch and returns true, in this port exactly as in AS3.
  - **`RoomEngine.getGenericRoomObjectImage()` did not honour AS3's `result.id = 0` "synchronous hit" contract** — documented on `ImageResult` itself, just never implemented. It handed the canvas to the async `createImageBitmap()` and delivered via `imageReady()` instead, on the stated grounds that Texture→ImageBitmap "is inherently asynchronous in the browser". It is not: `OffscreenCanvas.transferToImageBitmap()` is synchronous and this codebase already relied on that elsewhere. The consequence was not cosmetic: AS3-faithful callers call `onWidgetsInitialized()` from `imageReady()`, which re-dispatches `SelectProduct` → `updateImage()` → `getPetImage()`, and with an always-async result that re-enters `imageReady()` and **loops forever**, allocating a temporary-room object per iteration. Fixed at the source rather than guarded per widget (`NewPetsCatalogWidget` has the same shape and would have looped too).
  - **`App.registerImageAssets()` only fed the window manager's URL registry**, so `CatalogWidget.getAssetBitmapData()` — which reads `vortex.assets` — got null for every catalog bitmap: `ColourGridCatalogWidget`'s swatches and `RecyclerCatalogWidget`'s slot background rendered blank. The PNGs were present all along (`src/assets/images/ctlg_clr_*.png`); only the registration was missing. Scoped to the `ctlg_` prefix (each entry eagerly decodes an ImageBitmap).
  - **Still open**: `RoomManager.isContentAvailable()` answers `hasInternalContent()` where AS3 answers `getGraphicAssetCollection() != null`, so `getGenericRoomObjectImage()` always parks its listener in `_pendingImageListeners` — a queue drained only on content *load*. Left as a documented `TODO(AS3)` **with the two failing approaches recorded**: returning AS3's expression, with or without `&& isLoaded(type)`, regresses to no image at all, because `createRoomObject()` and this guard disagree about the same instant (the former builds against the place-holder library, the latter then reports "available" once the .nitro is browser-cached). The real fix is upstream, in the `updateObjectContents()` re-initialisation path.
- 🟢 **Phase 7 complete: pet catalog widgets — the 7-phase catalog roadmap is now closed** (3 widgets + a 4-file wire-protocol slice + 2 widget events + 4 `RoomEngine` delegates), taking `CatalogPage.createWidget()` from 36 to 39 of ~45 AS3 cases. Chosen as the roadmap's last item; `docs/CLIENT-SERVER-ARCHITECTURE.md` §18 confirms pets are 100% implemented server-side (`RoomPetSystem`, `PetPaletteEntity`, palettes loaded at startup), so this is exercisable end-to-end.
  - **Wire protocol** (`communication/messages/{outgoing,incoming,parser}/catalog/`): `GetSellablePetPalettesComposer` (header 3342, single string productCode) and `SellablePetPalettesMessageEvent`+`SellablePetPalettesMessageEventParser` (header 3350). Headers cross-verified against `../vortex-emulator/Turbo.Revisions/Revision20260701/Headers.cs`, whose AS3-verified comment for 3342 literally names both the primary class (`_SafeCls_2003`) and `HabboCatalog::getSellablePetPalettes()` as its sender. Confirmed the emulator's `Headers.cs` names messages from the **server's** perspective (its `…MessageEvent` = the client's composer and vice-versa) by checking three already-registered pairs.
  - **`SellablePetPalette`** (the per-entry DTO) has an unrecoverable class name in every tree (`[SecureSWF(rename="true")]`) — ported under a TS-derived name, documented as such, same handling as `GuildMembership`. Its getters are *not* obfuscated, so those names are real.
  - **`src/unknowns/` is not what CLAUDE.md says it is** — worth acting on. CLAUDE.md calls it "an unrelated, fully-obfuscated module bundled in the same dump, not part of the Habbo client". That is demonstrably wrong: the primary tree has **no `messages/*/catalog/` directories at all** (only `camera`/`inventory`/`landingview`/`quest`/`room`/`userdefinedroomevents`), because its catalog message layer was *package*-obfuscated and dumped into `unknowns/_SafePkg_1714` (parser + palette DTO), `_SafePkg_1716` (event) and `_SafePkg_1749` (composer). Those files are imported directly by the genuine `PetsCatalogWidget.as`/`NewPetsCatalogWidget.as`, and the same directory holds `SnowWarGameTokenOffer.as`/`SnowWarGameTokensMessageParser.as` under their **real** names. Following CLAUDE.md literally costs real information: the primary's parser copy is clean while the secondary's is decompiler-corrupted (see next bullet). This port's 4 new message files trace to the primary's `unknowns/` paths per an explicit user call; note the surrounding convention still traces messages to `win63_version` (192 files vs 2), which is consistent with the primary genuinely lacking them at any non-`unknowns/` path.
  - **Third instance of the `while(0 < count)` decompiler corruption**: `SellablePetPalettesMessageEventParser`'s secondary-tree copy has the index declared and incremented but never compared — an infinite loop. The primary's copy (`unknowns/_SafePkg_1714/_SafeCls_1905.as`) is correct and even keeps readable field names (`_productCode`/`_sellablePalettes`); it is what was ported. Same signature as `HabboClubOffersMessageEventParser` (Phase 2). `PetsCatalogWidget.as`'s secondary copy is corrupted in two more spots (`onDropMenuEvent()` loses its local, `updateAvailablePalettes()` passes `null` instead of the fetched array) where the primary is clean — a good reminder that tree priority is load-bearing, not cosmetic.
  - **`RoomEngine`/`IRoomEngine` gained 4 genuine AS3 members that had never been ported**: `getPetColor()`/`getPetColorsByTag()`/`getPetLayerIdForTag()`/`getPetDefaultPalette()`. All four already existed on `RoomContentLoader` but were exposed nowhere, while the widgets call them via `roomEngine.*`. Ported as AS3's null-guarded delegates with its exact fallbacks (`null`/`null`/`-1`/`null`) — note `getPetColorsByTag()` returns `null`, not an empty array, on a missing loader, and that distinction is preserved rather than smoothed over.
  - **`HabboCatalog`**: real `approveName()` and `getSellablePetPalettes()` (an `OrderedMap` cache that returns `null` on a miss and fires the request; the response returns via `onSellablePetPalettes()` → `CatalogWidgetSellablePetPalettesEvent`), plus `onApproveNameResult()`. **`ApproveNameMessageEvent` was registered in `HabboMessages` but had no listener anywhere — dead code until now.** `showPurchaseConfirmation()` gained AS3's params 7-8: param7 is **declared and never read anywhere in AS3's own method body** (a dead parameter in the original client, kept for signature fidelity), and param8 (the rendered pet image) is accepted but not displayed — AS3 forwards it to `PurchaseConfirmationDialog.showOffer()`, which is this port's documented minimal stub with no preview-image surface, so it is a `TODO(AS3)` rather than a scope expansion into the dialog.
  - **Widgets**: `PetsCatalogWidget` (classic pets, type index < 8: breed drop-menu + colour swatches), `NewPetsCatalogWidget` (type index >= 8: per-palette primary/secondary colour swatches, no drop-menu, purchase colour hardcoded white since the palette carries the colouring), `PetPreviewCatalogWidget` (read-only preview, decodes `customParams` four different ways by furniture category 13-16). The two buy widgets split on exactly the `< 8` / `>= 8` boundary and each bails out of `init()` for the other's range. `NewPetsCatalogWidget`'s swatches are consumed by the already-ported `ColourGridCatalogWidget` via `MULTI_COLOUR_ARRAY`.
  - **TS deviation, documented in all three**: `getPetImage()` returns null where AS3 returns a ready `BitmapData`, because `RoomEngine` always resolves an `ImageResult` through the asynchronous `id>0` path (a pre-existing architectural constraint documented on `ImageResult` itself: Texture→ImageBitmap cannot be synchronous in a browser). AS3 has that same async path and does the same thing in its own `imageReady()`, so the preview behaves identically. AS3's `BitmapData` compositing (`fillRect` to a fully transparent `0x00FFFFFF`, Matrix upscale, centred `copyPixels`) is reproduced on an `OffscreenCanvas` per `ColourGridCatalogWidget`'s established recipe. `PetPreviewCatalogWidget`'s dead `_gridItemLayout` field is omitted (AS3 assigns it and never reads it; this port has no raw-XML asset path anyway). Several AS3 quirks preserved rather than "fixed": both buy widgets' `onPurchase()` only *logs* on a pending name approval and falls through, the colour-index bound checks use `>` instead of `>=`, and `PetPreviewCatalogWidget`'s category-16 branch logs a short param list but does not break.
- 🟢 **Phase 6 complete: guild catalog cluster** (`habbo/catalog/guilds/GuildMembershipsController.ts` + 4 new catalog widgets + a small guild wire-protocol slice), taking `CatalogPage.createWidget()` from 32 to 36 of ~45 AS3 cases. Chosen next per the 7-phase roadmap because `docs/CLIENT-SERVER-ARCHITECTURE.md` §14 confirms Groups/Guilds is fully implemented server-side (unlike Recycler/Club-VIP when those phases started), so this is exercisable end-to-end immediately.
  - **Wire protocol** (`communication/messages/{outgoing,incoming,parser}/users/`): `GetGuildMembershipsMessageComposer`/`GetGuildCreationInfoMessageComposer` (both empty-payload) and `GuildMembershipsMessageEvent`+`GuildMembershipsMessageEventParser` (array-of-`GuildMembership`). Headers (3918/2989 outgoing, 3994 incoming) cross-verified two ways: primary-tree class resolution via `win63_version` name recovery, and independently against `../vortex-emulator/Turbo.Revisions/Revision20260701/Headers.cs`'s own AS3-verified comments (one of which literally names `BuyGuildWidget::onButtonClicked()` as the real sender for header 2989). `GuildMembership` (the per-entry DTO, AS3: `_SafeCls_3623`/`class_3489`) has an unrecoverable class name in every source tree checked (`win63_version`, `win63_2023_version`, primary), consistent with a `[SecureSWF(rename="true")]` tag — ported under a TS-derived name, documented as such.
  - **`GuildMembershipsController`** (no `I*.as` interface in any tree, concrete class only): mediates one registered `GuildSelectorCatalogWidget` at a time, requests memberships on register, re-fetches on `onGuildVisualSettingsChanged(guildId)` (AS3's own `guildId` param is accepted but unused - re-fetch is gated only on "a selector widget is open"). `HabboCatalog.onGuildMemberships()`/`onGuildVisualSettingsChanged()` forward the raw incoming event through, matching the existing `ClubExtendController.onOffer()` precedent (not `ClubBuyController.onOffers()`'s pre-unwrapped-data precedent) - the two otherwise-similar AS3 handler shapes disagree on this and both were preserved faithfully where they already existed.
  - **`HabboCatalog.ts` gained a real `navigator` getter** (`IHabboNavigator`, sourced from a new `IID_HabboNewNavigator` dependency's `.legacyNavigator`) - a genuine pre-existing AS3 member (`get navigator() : _SafeCls_93 { return _newNavigator.legacyNavigator; }`) that had never been ported, needed by `GuildSelectorCatalogWidget.onFindGroups()` → `navigator.performGuildBaseSearch()`. Also wired `HabboCatalog`'s dependency on `IID_HabboGroupsManager`'s `GSCIME_GUILD_VISUAL_SETTINGS_CHANGED` event (AS3: a 4-arg `ComponentDependency` with only an event-listener array, no plain setter) using the existing `ComponentDependency`/`IDependencyEventListener` infrastructure, which already supported this exact shape unused until now. Currently dormant: `HabboGroupsManager` never dispatches that event yet (the guild-settings management UI that would fire it isn't ported), so `onGuildVisualSettingsChanged()` is real but unreachable today - same class of gap as `ClubBuyController.showConfirmation()`.
  - **`StringArrayStuffData.ts` gained a real `setArray(data: string[])`** (AS3: trivial passthrough setter, `_SafeStr_4556 = param1`) - needed by `GuildSelectorCatalogWidget.getPreviewerStuffData()` to build a literal-array stuff-data for the room previewer. This was a pre-existing, already-documented gap (`ItemGridCatalogWidget.ts`'s guild-recolor preview branch has a code comment noting `StringArrayStuffData` "only supports construction from a room-object model/message wrapper" - that branch itself is still not ported, but the underlying capability gap is now closed for any future work that needs it).
  - **Widgets**: `GuildSelectorCatalogWidget` (drop-list of the user's groups, 2-color swatch bitmap built via `OffscreenCanvas` fillRect - AS3's `GUILD_COLORS_BMP_BORDER_COLOR`/`_WIDTH` constants are preserved as declared-but-unreferenced, matching the original), `GuildForumSelectorCatalogWidget` (subclass: filters to forum-eligible groups, warns if the selection already has a forum), `GuildBadgeViewCatalogWidget` (badge preview, listens for `GUILD_SELECTED`; its AS3 ctor's `GuildMembershipsController` param is genuinely unused at runtime, ported for signature fidelity only), `BuyGuildWidget` (single button; AS3's ctor has an `if(_button)` guard that's always false at construction time, preserved as harmless dead code rather than cleaned up).
- 🔴 **Root-cause fix: `HabboWindowManager.buildFromXML()` passed the desktop as `parent` instead of `null`, unlike real AS3 - fixed at the source in `WindowParser`/`WindowContext`, not patched per call site.** User-reported symptom: a small unstyled grid-item-shaped badge (price text + currency icon) appeared fixed in the screen's top-left corner whenever a catalog page with an item grid loaded, and never went away, not even after closing the catalog; later found to reproduce for the chat style selector too. Found via `WindowDebuggerOverlay`'s picker + a new ancestor-chain readout (see below), which showed the orphan's parent was a bare `desktop_N`, not the real owning window.
  - **Investigation history (worth keeping - the first two fix attempts are real dead ends, not just noise)**: attempt 1 patched the 3 catalog call sites that build a `.clone()`-source template and never re-add it (`removeChild()` after construction) - fixed the reported symptom but left the same bug latent everywhere else `buildFromXML()` is called this way. Attempt 2 fixed `WindowParser`/`HabboWindowManager` to pass `parent = null` like AS3 (`HabboWindowManagerComponent.as::buildFromXML()`: `parseAndConstruct(param1, null, param3)`) - this is correct AS3 architecture (AS3's `WindowParser` constructor takes and stores its owning context independent of `parent`, which is why it can safely pass `null`; this port's `WindowParser` had no such field, so `buildFromXML()` was passing `desktop` as a workaround, with the side effect of auto-attaching every top-level built window to the desktop) - but shipping it alone broke room click-to-move, furniture clicks, and own-avatar clicks entirely, because `WindowContext.create()`'s `USE_PARENT_GRAPHIC_CONTEXT` (`0x10`) param-flag handling means a `null`-parent window with that flag routes to `_substituteParent` (a legitimate AS3 "holding pen" controller, `SubstituteParentController.ts`) instead of falling through to the desktop - and one specific caller (`RoomDesktopLayoutManager.setLayout()`, next bullet) was missing the explicit re-attach AS3 requires for exactly that case, which the *previous* desktop-fallback bug had been silently masking.
  - **The actual fix, in order**: (1) `WindowParser` constructor now takes and stores the owning `IWindowContext` (`core/window/utils/WindowParser.ts`), matching AS3's `WindowParser(context)` constructor - `parseSingleWindowEntity()` falls back to this stored context (`this._context`) instead of `parent.context` when there's no parent to read it off of. (2) `HabboWindowManager.buildFromXML()` now passes `null` as parent (was `desktop`), matching AS3 exactly. (3) `RoomDesktopLayoutManager.setLayout()` (`habbo/ui/RoomDesktopLayoutManager.ts`) - the ROOT of the *entire* room desktop UI (room view, infostand, room tools, chat input/chat widgets all nest inside it via `addRoomView()`/`addWidgetWindow()`) - was missing the explicit `desktop.addChild(this._layoutContainer)` call that real AS3 has right after building it (`DesktopLayoutManager.as::setLayout()` line 55: `(_layoutContainer.desktop as class_3151).addChild(_layoutContainer);` - confirmed via the clean `win63_2023_version` reference, since `win63_version`'s copy of this class is corrupted here). `room_desktop_layout_xml`'s root does carry the `0x10` flag (confirmed by inspecting the compiled JSON), so this one call site was a real, pre-existing gap that the old bug happened to paper over, not something introduced by fixing `WindowParser`.
  - **Audit method for the rest of the codebase**: rather than auditing all ~65 `buildFromXML()`/`buildWidgetLayout()` call sites by hand, wrote a one-off script checking every referenced compiled `window-layouts/*.json`'s root `params` for the `0x10` bit - only a root carrying that flag is even affected by the `parent=null` change (everything else still unconditionally falls through to `parent ?? this._desktop` inside `WindowContext.create()`, completely unchanged by this fix). Of the ~35 flagged layouts found this way, all but `room_desktop_layout_xml` already had a correct, explicit re-attach downstream (`rootWindow =` on an `IWidgetWindow` placeholder - used by all 26 files in `habbo/window/widgets/`, which internally does an unconditional `addChild()`; or `addGridItem()`/`addListItem()`/a direct `addChild()` by the constructing code's caller) - confirmed the shared `WindowController.addChild()`/list/grid-item-add implementations all correctly detach a child from whatever parent it already has (including `SubstituteParentController`) before reattaching, so these patterns are safe regardless of which parent a window started under.
  - Reverted the 3 catalog-specific `removeChild()` band-aids from attempt 1 once the real fix landed - they're redundant now (those templates simply never get attached anywhere to begin with) and AS3 has no equivalent of them.
  - **New debugger capability, not AS3-related** (`vortex-client/src/debugger/WindowDebuggerOverlay.ts`): the tree inspector previously only showed a picked window's *descendants*; there was no way to see what it was actually parented under. Added an ancestor-chain readout (`IWindow.parent` walked to the root) to both the node detail panel and the "Copy tree as text" output - this is what made both the original leak and the room-interactivity regression diagnosable at all.
- 🟡 **Catalog gap audit + 7-phase implementation roadmap** (plan file: see session notes) — three parallel deep-research passes inventoried all 48 AS3 widgets under `catalog/viewer/widgets/` against the 10 already ported, plus every dependent controller/logic/data class, cross-checked against `docs/CLIENT-SERVER-ARCHITECTURE.md` for real server-side coverage (Marketplace and Groups/Guilds confirmed real and working server-side; classic Trading confirmed a complete stub; Club/VIP purchase and Recycler server coverage unconfirmed, flagged as a risk to verify early). Sequenced into 7 phases: (1) no-new-protocol widgets, (2) Habbo Club/VIP purchase, (3) Marketplace, (4) Recycler + `CatalogObjectMover`, (5) offscreen furniture/room image rendering (`RoomEngine.getFurnitureImage`/`getWallItemImage`/`getRoomImage`/`getGenericRoomObjectImage`, currently 0% ported and blocking 5 widgets at once), (6) guild catalog cluster, (7) pets. Trax/song-disk widgets and classic trading explicitly marked out of scope (each would need porting an entire unrelated 0%-ported subsystem for 1-2 small widgets).
- 🟡 **Phase 1: 13 of ~16 no-new-protocol widgets ported** (`SpecialInfoWidget`, `WarningCatalogWidget`, `TextInputCatalogWidget`, `FirstProductSelectorCatalogWidget`, `SingleViewCatalogWidget`, `BuilderCatalogWidget`, `SoldLtdItemsCatalogWidget`, `MadMoneyCatalogWidget`, `AddOnBadgeViewCatalogWidget`, `FeaturedItemsCatalogWidget`, `ColourGridCatalogWidget`, `BundleGridViewCatalogWidget`, `ActivityPointDisplayCatalogWidget`), all registered in `CatalogPage.createWidget()`. 4 new internal widget-bus events added (`TextInputEvent`, `CatalogWidgetShowWarningTextEvent`, `CatalogWidgetRoomChangedEvent`, `CatalogWidgetMultiColoursEvent`). `MadMoneyCatalogWidget` ported faithfully as inert - confirmed directly in the primary AS3 source that `init()`'s button lookup is followed by an empty `if` block (no listener ever attached), so its `eventProc()`/`windowManager.alert("TODO", "Fix in MadMoneyCatalogWidget.as", ...)` is dead code in the original client too, not a porting gap.
  - **New `CatalogWidgetName.ts`** (TS-only, no AS3 equivalent): named constants for every `CatalogPage.createWidget()` layout-name case (ported and not-yet-ported alike), replacing the raw string literals both in that switch and in each widget's own `attachWidgetView()` call - user-requested cleanup, matches this codebase's existing "class with `static readonly` members" convention for AS3-style enums (`ActivityPointTypeEnum`, `HabboToolbarIconEnum`, ...) rather than a native TS `enum`.
  - **`BuilderCatalogWidget` surfaced a real cross-system gap deeper than the initial survey found**: its button-enabled state depends on `HabboCatalog.getBuilderFurniPlaceableStatusForOffer()`, which didn't exist on this port at all (only referenced in a pre-existing `isDraggable()` TODO comment). Real AS3 logic needs `builderFurniCount`/`builderFurniLimit`/`roomSession` (owner/guild-room/controller-level/user-data-manager), none of which are wired into the catalog yet. Added as an honest `TODO(AS3)` stub always returning `0` ("placeable") - safe because the actual placement action (`requestSelectedItemToMover()`) is itself still a `CatalogObjectMover`-blocked stub (Phase 4), so nothing is falsely blocked or falsely placed. Also fixed `requestSelectedItemToMover()`'s signature, which was missing the AS3 `placeMany: boolean = false` third parameter entirely.
  - **`FeaturedItemsCatalogWidget` needed a real (if small) `HabboCatalog.ts` data-plumbing fix, not just the widget**: `frontPageItems` was never stored anywhere, even though `CatalogPageMessageEventParser` already parses it in full (`FrontPageItem[]`, itself already ported). Added the missing `_frontPageItems` field + `frontPageItems` getter (now on `IHabboCatalog` too) and the one-line assignment in `onCatalogPage()` AS3 already does right before `showCatalogPage()`.
  - **`ColourGridCatalogWidget`'s AS3 `BitmapData.colorTransform()`-based swatch recolouring** ported via the same canvas `'multiply'`/`'destination-in'` compositing recipe already established in `BitmapSkinRenderer.colorizeEntity()`, using `OffscreenCanvas.transferToImageBitmap()` (synchronous) instead of the async `createImageBitmap()` to avoid turning a synchronous AS3 flow async. Confirmed the `color_chooser_cell`/`gridItem`/`badgeGridItem` one-off XML templates AS3 loads via `assets.getAssetByName()` + `buildFromXML()` are already present as ordinary compiled `window-layouts/*.json` and buildable via the normal `buildWidgetLayout()` path - no raw-XML-asset special-casing needed.
  - **Corrected mid-session**: `UserBadgeSelectorCatalogWidget` genuinely needs a new `HabboCatalog.inventory` getter (still doesn't exist at all) plus a new incoming badge-update message event and `buildFromXML`-per-item construction - still outstanding. `ActivityPointDisplayCatalogWidget`'s dependency turned out to already be fully ported as `ActivityPointTypeEnum.ts` (the earlier claim that it was missing was wrong - found the real file on a second, more careful look). Note: that existing port's `isVisible()` does **not** literally match the primary AS3 source's corrupted `if([1,2,4].indexOf(param1) != 1) return false` (which only ever passes for `param1 === 2`) - it implements the evidently-intended "hide only NO_OP_1/2/4" logic instead. This predates this session; flagged here rather than silently touched, since rewriting working, already-relied-upon code to match a near-certainly-decompiler-corrupted line needs an explicit call, not a drive-by "fix".
- 🟢 **Phase 1 complete: last 3 stragglers ported** (`RedeemItemCodeCatalogWidget`, `SpacesNewCatalogWidget`, `BundlePurchaseExtraInfoWidget` + the whole `bundlepurchaseinfodisplay/` sub-module), taking `CatalogPage.createWidget()` from 20 to 23 of ~45 AS3 cases:
  - **`RedeemItemCodeCatalogWidget`**: ported the previously-empty-stub `HabboCatalog.redeemVoucher()` for real, adding `RedeemVoucherMessageComposer` (header 471) plus the `VoucherRedeemOkMessageEvent`/`VoucherRedeemErrorMessageEvent` response events + parsers (headers 2083/2211, all three sourced from `win63_version/habbo/communication/class_1881.as` since the primary tree's registry file is fully obfuscated) and the matching `HabboCatalog.onVoucherRedeemOk()`/`onVoucherRedeemError()`/`alertDialogEventProcessor()` handlers. `alertDialogEventProcessor()`'s AS3 counterpart also calls `resetPlacedOfferData()`, which depends on the unported `CatalogObjectMover`/`PlacedObjectPurchaseData` (Phase 4) — added as a real but currently-no-op method (matches AS3 exactly today, since nothing sets placed-offer state yet) rather than inlining or omitting it.
  - **`SpacesNewCatalogWidget`**: subclasses `ItemGridCatalogWidget`, adding a wall/floor/landscape category selector that drives the "spaces" room preview (new `CatalogWidgetUpdateRoomPreviewEvent`). **TS-only deviation, documented in-code**: AS3 restarts a persistent `flash.utils.Timer` after a category switch whose fire-closure is permanently bound to the *first* (empty) `populateItemGrid()` call from `init()` — meaning in the original client, only the first 3 offers of any category with more than 3 items ever get their icons loaded (confirmed by reading `ItemGridCatalogWidget.as`'s `init()`/`loadItemGridGraphics()` closures directly, not decompiler corruption). This port's timer is a plain `setInterval` created fresh wherever needed (an architecture decision already made when `ItemGridCatalogWidget.ts` was ported, predating this session), so `switchCategory()` starts a fresh interval over the *current* category's offers instead of reproducing the stale-closure quirk.
  - **`BundlePurchaseExtraInfoWidget` + `bundlepurchaseinfodisplay/`** (9 AS3 files, all new except `ExtraInfoItemData.ts` which already existed): `ExtraInfoViewManager` (implements the engine's existing `IUpdateReceiver`/`registerUpdateReceiver()` rather than a bespoke AS3-style update hook), `ExtraInfoListItem`/`UpdateableExtraInfoListItem` base classes, and the 5 concrete row types (`ExtraInfoPromoItem`, `ExtraInfoBundlesInfoItem`, `ExtraInfoDiscountValueItem`, `ExtraInfoBonusBadgeItem`/`ExtraInfoBonusAchievementItem` — the last two are inert stubs matching AS3, since nothing in this widget ever constructs those two item types). `ExtraInfoItemData.ts` was missing its `TYPE_BUNDLES_INFO_SCREEN` constant despite it being recoverable in both the primary and secondary trees — added. Two of its six type constants (values 2 and 4) are unrecoverable by name in all three source trees; named `TYPE_DISCOUNT_VALUE`/`TYPE_BONUS_ACHIEVEMENT` (TS-only names, not invented — derived unambiguously from which concrete list-item class `ExtraInfoViewManager.as::addItem()`'s switch constructs for each value). Added `HabboCatalogUtils.spinnerValueChangedEventTrack()`/`bundlesInfoShownEventTrack()`/`discountShownEventTrack()` (delegating to the already-ported `HabboTracking.trackEventLogOncePerSession()` instead of duplicating AS3's hand-rolled per-method boolean flags — behaviorally identical since one `HabboCatalogUtils` lives exactly as long as one session) and `bundleDiscountHighestFlatPriceStep` (same not-yet-parsed-config-message `TODO(AS3)` as the existing `bundleDiscountFlatPriceSteps` stub). Wired a new `IID_HabboTracking` dependency into `HabboCatalog.ts` (existing DI pattern, same as `HabboHelp`/`HabboNavigator`) so `HabboCatalogUtils` can reach it. **Documented null-guard deviation**: `onSpinnerEvent()`'s AS3 condition dereferences `catalog.bundleDiscountRuleset.bundleSize`, but `bundleDiscountRuleset` is a pre-existing always-null stub on this port — null-guarded instead of literally ported, using the same cast convention `ProductViewCatalogWidget.ts::setSpinnerToBundleRuleset()` already established for this same stub.
- 🟢 **Phase 2 complete: Habbo Club/VIP purchase** (`habbo/catalog/club/`, 12 new files + 5 new catalog widgets), taking `CatalogPage.createWidget()` from 23 to 28 of ~45 AS3 cases. Confirmed via `docs/CLIENT-SERVER-ARCHITECTURE.md` §9 that generic catalog purchasing (`CatalogPurchaseGrain`) is "genuinely solid" server-side and already resolves club level/discount via `IPlayerGrain.GetClubSubscriptionAsync` — no new server-side risk identified for this phase.
  - **Controllers**: `ClubBuyController` (buy HC/VIP days, requests offers via new `GetClubOffersMessageComposer`/`HabboClubOffersMessageEvent`, header 2965/2426), `ClubExtendController` (renew-before-expiry upsell, new `HabboClubExtendOfferMessageEvent`/header 3509 + `PurchaseVipMembershipExtensionComposer`/`PurchaseBasicMembershipExtensionComposer`, headers 415/3352), `ClubGiftController` (gift Club to a friend — reuses the already-ported `GetClubGiftMessageComposer`/`ClubGiftInfoEvent` pair from the earlier ClubCenter work instead of new protocol, only needed one new composer, `SelectClubGiftComposer`, header 227). All three wired into `HabboCatalog.init()` (`createClubBuyController()`/`createClubExtendController()`/`createClubGiftController()`, previously a documented `TODO(AS3)` gap) alongside `getClubBuyController()`/`getClubExtendController()`/`getClubGiftController()`, `purchaseProduct()`, `purchaseVipMembershipExtension()`/`purchaseBasicMembershipExtension()`, and `interpolate()`/`updateUrlProtocol()` (HabboCatalog-as-config-accessor methods `Component` didn't already expose).
  - **New wire-protocol wrinkle found via cross-referencing three source trees**: `HabboClubOffersMessageEventParser`'s secondary-tree copy (`class_2374.as`) has a genuine infinite-loop bug (`while(0 < count)`, never incremented/compared) — the primary tree's copy is correct (`while(i < count) { ...; i++; }`) and was used as the actual source of truth, per the documented tree-priority order.
  - **`ClubBuyOfferData`** (implements the existing `IPurchasableOffer` interface, confirmed member-for-member) is constructed directly by the parser via a new `fromWrapper()` static factory, skipping AS3's unnamed intermediate wire-DTO class (unrecoverable in all three trees). `ClubExtendOfferData` extends it with 4 more fields; ported its `discountActivityPointAmount` getter's real bug faithfully (multiplies by `months` twice, confirmed identical in the primary and secondary trees, unlike the sibling `discountCreditAmount` getter which only multiplies once).
  - **Confirmation dialogs**: `ClubBuyConfirmationDialog` (real but currently unreachable — confirmed via grep that neither `ClubBuyItem` nor `VipBuyItem`, the only two "buy" buttons in the live widget flow, ever call `ClubBuyController.showConfirmation()`; both route through the generic `HabboCatalog.showPurchaseConfirmation()`/`PurchaseConfirmationDialog` instead, matching this session's `MadMoneyCatalogWidget`-style "port it anyway, it's genuinely dead in the original client too" precedent), `ClubExtendConfirmationDialog` (real renewal upsell UI: price breakdown, animated credit-icon flourish, remote teaser image via the already-ported `loadAssetFromFile`/`AssetLoaderEvent` pipeline), `ClubGiftConfirmationDialog`. All three widgets' raw-XML-asset + `buildFromXML()` window-construction pattern (`assets.getAssetByName(name) as XmlAsset` + `windowManager.buildFromXML(xml)`) was replaced with the already-established `catalog.utils.createWindow(name)` compiled-layout-registry call, per the same substitution this port already uses elsewhere for one-off XML templates (see `ColourGridCatalogWidget`'s port notes) — not a new deviation, an existing one applied consistently.
  - **Widgets**: `ClubBuyCatalogWidget`, `VipBuyCatalogWidget` (also handles VIP gifting via an `isGift` constructor flag — reused directly for both `vipBuyWidget` and `vipGiftWidget`, matching AS3), `LoyaltyVipBuyCatalogWidget` (near-identical to `VipBuyCatalogWidget`, differs only in offer-request source), `ClubGiftWidget` (converts the raw `ClubOfferData[]` from `ClubGiftController` into real `Offer`/`Product` instances before display, matching AS3's own `updateList()`). Two small pre-existing `ITextWindow` gaps surfaced and were left as documented `TODO(AS3)` stubs rather than expanded: no `align`/`leading` fields on the per-range text-format type (`fixFormatting()`), and no `StyleSheet`-equivalent for CSS-in-caption links (`setLinkStyle()`, same gap `LocalizationCatalogWidget.ts` already documented). `ClubGiftWidget`'s hover-preview (`showPreview()`) needed `RoomEngine.getFurnitureImage()`/`getWallItemImage()` — Phase 5 scope at the time this was written, now ported (see Phase 5 entry below).
- 🟢 **Phase 3 complete: Marketplace** (`habbo/catalog/marketplace/`, 10 new files + 2 new catalog widgets + a full new wire-protocol module), taking `CatalogPage.createWidget()` from 28 to 30 of ~45 AS3 cases.
  - **Core utility gap found and filled first**: `MarketPlaceLogic`/both widgets all depend on AS3's `com.sulake.core.utils.Map` (an insertion-ordered, index-addressable key/value `Proxy` — real name confirmed via `PRODUCTION-201601012205-226667486/src/com/sulake/core/utils/Map.as`, whose own `callProperty()` literally returns `"Map"` for `toString()`), which had never been ported despite being used engine-wide in AS3. Added `core/utils/OrderedMap.ts` (renamed to avoid shadowing the built-in `Map`) implementing the full API (`add`/`remove`/`getValue`/`getWithIndex`/`getKey`/`getKeys`/`hasKey`/`indexOf`/`concatenate`/`clone`/`reset`/`dispose`, plus `values()`/`Symbol.iterator` standing in for AS3's `for each`/Proxy iteration).
  - **Wire protocol** (`communication/messages/{outgoing,incoming,parser}/marketplace/`): 9 composers, 8 events, 8 parsers. Two composer/event pairs (`CancelAllMarketplaceOffersMessageComposer`/`MarketplaceCancelAllOffersResultEvent`, headers 831/921; `ClearOwnMarketplaceHistoryMessageComposer`/`MarketplaceClearOwnHistoryResultEvent`, headers 1242/1760) are genuinely absent from `win63_version`/`PRODUCTION-201601012205-226667486` entirely — newer features only in the primary tree, headers confirmed directly from its registry (`_SafeCls_2046.as`), TS-derived names since no readable-name tree has them. `CancelMarketplaceOfferMessageComposer` (header 2096) is what `redeemExpiredMarketPlaceOffer()` actually sends — client-facing wording is "redeem" but the wire-level name (recovered from `win63_version`) is "cancel".
  - **Decompiler corruption found and worked around**: both `MarketPlaceOffersEventParser` and `MarketPlaceOwnOffersEventParser` are corrupted in `win63_version` in the same distinctive way already seen once this session for `HabboClubOffersMessageEventParser` — non-terminating loop condition, discarded reads, and `_offers.push(null)` instead of the constructed object. The primary tree's copies (`_SafeCls_2885`/`_SafeCls_3542`) are complete and were used as the real source; field-by-field diffing them also revealed the two parsers read genuinely different shapes (public offers read `timeLeftMinutes`/`averagePrice`/`offerCount` per-entry and a trailing `totalItemsFound`, with an `isUsable`/`isUsed` variant via furniType 4; own offers read a leading `creditsWaiting` once, no offer count, and a conditional `statusTime` `readLong()` only when status is SOLD/EXPIRED) — preserved as two distinct parsers rather than unified.
  - **Two-class raw-DTO split preserved from AS3**: the wire parsers construct an obfuscated, unrecoverable-by-name raw entry class (`_SafeCls_2516`/`class_3020`/`_Str_4192` — a decompiler placeholder in all three trees; ported as `MarketPlaceOfferEntry`, TS-derived name) which `MarketPlaceLogic.onOffers()`/`onOwnOffers()` then re-project field-by-field into the real domain class `MarketPlaceOfferData` (implements `IMarketPlaceOfferData`) — a deliberate AS3 architectural split, not collapsed into one class per the "never simplify AS3 architecture" rule.
  - **Two small supporting data classes recovered by name from `PRODUCTION-201601012205-226667486`** (both obfuscated in the two newer trees): `MarketplaceItemStats` (price/volume history for a furni type; its `offerCount` accessor is named from the real private field `_currentOfferCount`, not the ambiguous getter name) and `MarketPlaceOfferState` (an `ONGOING`/`SOLD`/`EXPIRED` constant set with a genuine duplicate-value quirk — two differently-named constants both `= 1`, confirmed identical across all three trees — that is unreferenced anywhere in the current client; ported for completeness, not wired to anything, matching its real orphaned status). `MarketPlaceOfferStatus` (`OPEN`/`SOLD`/`EXPIRED` + `isClearable()`, gating `clearOwnHistory()`) has no readable-name counterpart in any tree at all - TS-derived from usage.
  - **Pre-existing header collision found and fixed**: `HabboMessages.ts` had `LatencyPingRequestMessageComposer` registered at header 1242 (carried over from `win63_version`'s registry), but the primary tree's registry (`_SafeCls_2046.as`) reassigns 1242 to `ClearOwnMarketplaceHistoryMessageComposer` and registers the real latency-ping composer (`_SafeCls_2653`, same single-int-param shape, confirmed by reading the class directly) at 544 instead — moved to 544 to resolve the collision, since the primary tree is this port's protocol source of truth.
  - **`MarketplaceChart`** (price/volume line chart bitmap): AS3 draws synchronously into a `BitmapData` via `Shape`/`TextField`/`Matrix`; ported to `OffscreenCanvas` + `createImageBitmap()`, which makes `draw()` return `Promise<ImageBitmap>` instead of a synchronous value — the one unavoidable signature change, documented in-code, since browsers have no synchronous canvas→`ImageBitmap` path (same constraint already documented on `ImageResult.ts`).
  - **`MarketplaceConfirmationDialog`**: reuses the already-ported `IRoomEngine.getFurnitureIcon()`/`getWallItemIcon()` + `IGetImageListener` async-icon pattern (established by `ProductImageWidget.ts`) rather than needing a new Phase 5 stub, and `HabboCatalogUtils.replaceCenteredImage()` in place of AS3's manual `BitmapData.copyPixels()` centering (same pre-existing `ImageBitmap`-is-immutable substitution used throughout the catalog module).
  - **Widgets**: `MarketPlaceCatalogWidget` (search/browse/buy, with a `setInterval`-based list population batching 5 items per 25ms tick, and a details panel with a price-history/trade-volume chart selector) and `MarketPlaceOwnItemsCatalogWidget` (ongoing/sold/expired "my offers" tabs, with its own AS3-faithful per-status window-recycling pool capped at 2000 entries per status, and local substring search over localized name+description). Both wired into `HabboCatalog.getMarketPlace()` (lazily created in `init()` alongside the Phase 2 club controllers) and the 8 new `onMarketPlace*`/`onMarketplace*` event handlers registered in `initComponent()`.
- 🟢 **Phase 4 complete: Recycler + `CatalogObjectMover`** (`habbo/catalog/recycler/`, 9 new files + `CatalogObjectMover.ts` + 2 new catalog widgets + a full new wire-protocol module), taking `CatalogPage.createWidget()` from 30 to 32 of ~45 AS3 cases. Confirmed via a direct read of `vortex-emulator`'s codebase that the server has **zero** Recycler implementation (no `Turbo.PacketHandlers` handler, no message classes, no grain logic) — ported client-first anyway per explicit direction, so the feature is wire-complete and will start working the moment server support exists, but cannot be exercised end-to-end today.
  - **Wire protocol** (`communication/messages/{outgoing,incoming,parser}/catalog/`): 3 composers (`GetRecyclerStatusMessageComposer`/`GetRecyclerPrizesMessageComposer`, both empty-body; `RecycleItemsMessageComposer`, an item-id-array send), 3 events (`RecyclerStatusMessageEvent`/`RecyclerFinishedMessageEvent`/`RecyclerPrizesMessageEvent`) + parsers, plus 3 supporting prize-table data classes (`PrizeLevelMessageData`/`PrizeMessageData`/`PrizeMessageSubProduct`, one `PrizeLevelMessageData` per star tier). Headers (1246/2516/2956 outgoing; 1919/281/3783 incoming) came directly from `vortex-emulator/Turbo.Revisions/Revision20260701/Headers.cs`, already realigned against this same primary AS3 tree earlier in this session — cross-checked one pair (`MarketPlaceOffersComposer`/`MarketPlaceOffersEvent`, header 2442) against `HabboMessages.ts`'s existing Marketplace registration to confirm both sides of this port target the identical protocol baseline before trusting the new numbers.
  - **Decompiler corruption found again, same pattern as Phase 2/3**: `PrizeLevelMessageData`'s and `PrizeMessageData`'s deal-bundle sub-product loops are both `while(0 < count)` with an unrelated counter incremented inside (never terminates as literally written) — ported as bounded `for` loops per the now-established precedent for this exact corruption shape.
  - **`RecyclerLogic`** (state machine: local status off/ready/waiting-for-server × server status off/ready/waiting, slot pool sized from config `recycler.number_of_slots` default 5, ducket cost from `recycler.ducket_cost`, timeout from `recycler.timeout_seconds`, cached prize table with a pending-callback queue for `getPrizeTable()`) ported member-for-member against `IRecycler`/`IRecyclerVisualization`, including the quirky `setNextRecycleAllowedTimestamp()`/`secondsToWait()` interplay (`_systemStatus` gets force-set to `3` as a side effect of the timestamp setter, called both internally and externally from the widget's animation-complete handler) — preserved exactly as AS3 has it rather than "cleaned up".
  - **Prize-table class family**: `FurniSlotItem` (slot contents), `PrizeGridItem`→`PrizeContainer`→`DealPrizeContainer` (single-product vs. bundle-deal prize display, sharing icon-resolution/grid-item plumbing with the existing `ProductGridItem`/`IGridItem` base already used by regular catalog grids), `PrizeLevelContainer` (one per star tier, builds the right container subclass per prize), `RecycleRewardDisplayWrapper` (adapts a prize to the generic `IProductDisplayInfo` product-preview widget). The `"chat_style"` prize-icon branch (`PrizeGridItem.initProductIcon()`) needs `HabboCatalog.freeFlowChat.chatStyleLibrary`, which isn't exposed on `HabboCatalog` yet — same pre-existing gap `HabboCatalogUtils.showExtraOnProduct()` already documents; left as a logged, honest no-op rather than invented.
  - **`CatalogObjectMover`** (drag-ghost-icon while placing a purchased/moved/returned-from-recycler furni item): confirmed by reading it fully that it sends no network messages of its own and only needs `IRoomEngine.getFurnitureIcon()`/`getWallItemIcon()` (already ported) for the icon bitmap. **One AS3 mechanism deliberately not reproduced 1:1**: AS3 mounts its overlay `Sprite` onto `mainContainer.desktop.getDisplayObject()` (the window system's own root display object); this port instead calls the *already-existing* `IRoomEngine.addStageChild()`/`removeStageChild()` (built for `RoomEngine.ts`'s own in-room object-mover icon sprite) — same "mount directly on the PixiJS stage, above everything" outcome, reusing established engine plumbing instead of threading a new `getDisplayObject()`-equivalent through the window-system layer. `mainContainer` is still a real public setter (AS3 has one) but unused internally as a result.
  - **Two small, well-scoped `IRoomEngine` additions, not full features**: (1) `ISelectedRoomObjectData.ts` ported as a faithful data-shape interface (confirmed by reading the real, non-obfuscated AS3 file) plus `getSelectedObjectData(roomId)` — but the concrete implementation (obfuscated `class_34.as`, covering full room-object selection for both placement *and* already-placed objects being moved/inspected) is a much larger, unrelated subsystem that was out of scope here, so this always returns `null` (an honest `TODO(AS3)` stub; every caller — `CatalogObjectMover`, `RecyclerCatalogWidget`'s slot click handler — already null-checks it, so slot clicks currently always behave as "release slot" rather than "place item" until this is filled in). (2) `setObjectMoverIconSpriteVisible()`/`getObjectMoverIconSpriteVisible()` ported **for real**, not stubbed — `RoomEngine.ts` already tracks a `_moverIconSprite` for its own in-room placement-icon flow, so these just toggle/read its existing `.visible`.
  - **`HabboCatalog` wiring**: real `_recycler: RecyclerLogic` field + `createRecycler()` (called from `init()` alongside the Phase 2/3 controllers) + `getRecycler()` (was a `null`-returning stub), `getRecyclerStatus()`/`getRecyclerPrizes()`/`sendRecycleItems()` now send real composers, 3 new `onRecycler*` handlers registered in `initComponent()`. `setupInventoryForRecycler()`/`requestInventoryFurniToRecycler()`/`returnInventoryFurniFromRecycler()` remain honest no-op/zero/`false` stubs — AS3 delegates these to `HabboInventory`'s own `RecyclerModel` (a purely local inventory-side furni-locking helper, confirmed via a full read — it sends no network messages either), and `HabboInventory` isn't wired into `HabboCatalog` at all yet. Also refreshed 4 pre-existing stale `TODO(AS3)` comments elsewhere in `HabboCatalog.ts`/`CatalogPage.ts` that said "CatalogObjectMover isn't ported" — it now exists, but `requestSelectedItemToMover()`/`cancelFurniInMover()`/`resetPlacedOfferData()` (the purchase-flow call sites that would each need to own a `CatalogObjectMover` instance) are separate, still-open wiring gaps, not re-closed by this phase.
  - **Widgets**: `RecyclerCatalogWidget` (slot grid, ducket-cost display, recycle-button state machine, abort flow) and `RecyclerPrizesCatalogWidget` (browsable star-tier prize grids, reusing `gridItem`/`recyclerPrizesWidgetLevelItem` compiled layouts already present in the JSON registry) — plus their two small supporting helpers, `RecyclerEngineAnimator` (the spinning-arrow/shake animation state machine, `setInterval`-based in place of `flash.utils.Timer`) and `FrankRecyclerEmotion` (floating pat-Frank blush/heart bubbles with a static reuse pool; one AS3 branch — nulling `_bitmap` then reading it again in the same tick — is a decompiler control-flow artifact, ported as an early `return` instead of reproducing the guaranteed crash).
- 🟢 **3 new incoming message events registered by header (2986/1956/2913)**: `RoomVisualizationSettingsEvent`/`RoomVisualizationSettingsEventParser` (2986, `wallsHidden`/`wallThicknessMultiplier`/`floorThicknessMultiplier`, the live wall-visibility/thickness push) and `RoomPropertyMessageEvent`/`RoomPropertyMessageEventParser` (1956, `floorType`/`wallType`/`landscapeType`/`animatedLandscapeType`, a single live room-decoration property update) under `messages/{incoming,parser}/room/engine/` — names recovered by cross-referencing the primary tree's obfuscated registry entries (`_SafeCls_2046.as` header table) against `sources/win63_version`'s readable copies of the same parsers, confirmed field-for-field. `FaqTextMessageEvent`/`FaqTextMessageParser` (2913, `questionId`/`answerText`, the answer to `GetFaqTextMessageComposer` — that composer itself not yet ported) under `messages/{incoming,parser}/help/` — name recovered via `sources/PRODUCTION-201601012205-226667486/OriginalClassNames.txt`, exact field match against the primary tree's real (non-obfuscated) parser file. **Cross-checked against `vortex-emulator/Turbo.Revisions/Revision20260701/Headers.cs` mid-session**: its 1956/2986 entries carry `// AS3-verified` comments matching this port's identification exactly; its `WiredPermissionsComposer = 2913` has no such comment (unlike its neighbors) and collides with the AS3-verified FAQ header — the real `WiredPermissionsEvent`/`WiredPermissionsEventParser` (`canModify`/`canRead`) is registered at header **3483** in the primary tree, not 2913. Registered as-is (matching AS3), header 2913's emulator-side mismatch left as a note for a future `vortex-emulator`-side fix, not touched here. **Follow-up, same session**: user confirmed `vortex-emulator` does have `WiredPermissionsComposer` registered at 3483 too (both headers exist server-side) — added `WiredPermissionsEvent`/`WiredPermissionsEventParser` (3483, `canModify`/`canRead`) under `messages/{incoming,parser}/userdefinedroomevents/` (flat, matching the existing `WiredRewardResultMessageEvent` placement rather than nesting AS3's `wiredmenu` subpackage) — name recovered via `sources/win63_version`'s readable copy, exact field match against the primary tree's `_SafeCls_3768`(event, `src/unknowns/_SafePkg_2538/`)/`_SafeCls_2783`(parser, real path) pair.
  - **Follow-up, same session — actually wired 2986/1956, not just registered**: user reported live `SocketConnection` "No registered handler" debug logs for all three headers, which surfaced that a header→class mapping in `HabboMessages.ts` alone does nothing without a real `connection.addMessageEvent()` subscription (same class of gap as the pre-existing `ScrSendUserInfoEvent` bug documented above). Traced the real consumer for both room messages to `sources/win63_version/habbo/room/class_1788.as` (confirmed to be `RoomEngine`'s own `RoomMessageHandler`-equivalent, matching this port's `RoomMessageHandler.ts` 1:1 by its `addMessageEvent()` subscription list) — its `onRoomProperty()`/`onRoomVisualizationSettings()` call straight through to `RoomEngine.updateObjectRoom()`/`updateObjectRoomVisibilities()`/`updateObjectRoomPlaneThicknesses()` (`class_34.as`, confirmed real `RoomEngine`), which just dispatch the already-fully-ported `RoomObjectRoomUpdateMessage`/`RoomObjectRoomPlaneVisibilityUpdateMessage`/`RoomObjectRoomPlanePropertyUpdateMessage` internal messages that `RoomLogic.ts` already listens for and applies to the room object's model (`RoomVisualization.ts` already reads `room_floor_type`/`room_wall_type`/`room_landscape_type`/thickness-multiplier model vars every update tick) — this whole internal pipeline existed and was fully working, just never fed by a live network message. Added the 3 missing `IRoomEngine`/`IRoomCreator` methods to `RoomEngine.ts` (faithful port of `class_34.as`, including the `IRoomInstance.setString()` room-level cache AS3 also writes, skippable via a `skipModelUpdate` param) and wired `RoomMessageHandler.onRoomProperty()`/`onRoomVisualizationSettings()` to call them against `_currentRoomId`. **TODO(AS3)**: `updateObjectRoom()`'s "room object doesn't exist yet" buffering branch is decompiler-corrupted in `class_34.as` (literal `null.floorType = param2` lines) and wasn't ported — every current call site already requires the room to exist, so this only matters for a future caller that fires before room entry completes. `animatedLandscapeType` (parsed but never dispatched to an update message, matching AS3's own `RoomLogic.updatePlaneTypes()` which also only handles 3 of the parser's 4 fields) is preserved as a real gap, not silently dropped, exactly as AS3 has it. **`WiredPermissionsEvent` (3483) deliberately left unsubscribed**: traced its real-AS3 consumer to `WiredMenuController` (`com.sulake.habbo.roomevents.wired_menu.WiredMenuController.onWiredPermissions()`, primary tree, non-obfuscated) — an entire unported subsystem (`IHabboUserDefinedRoomEvents` has zero concrete implementation on this port, confirmed via its own `TODO(AS3)` header comment), so there is no real place to route `canModify`/`canRead` yet; inventing a placeholder consumer was rejected as scope creep. The "no registered handler" debug log for 3483 is expected to keep appearing until `WiredMenuController` itself gets ported. **Follow-up (Bloc E foundation, this session): resolved** — the real `WiredMenuController` is now ported and registers `onWiredPermissions` (3483) itself in `initComponent`, so `canModify`/`canRead` route to the live controller and the temporary `IncomingMessages.applyPermissions` bridge was removed. 3483 is now a registered handler; the debug log no longer fires for it.
- 🔴 **AUDIT 2026-07-24 — 23 incoming messages the AS3 subscribes to are NOT registered in this port at all.** Triggered by a user report that `WiredMovementsMessage` was never wired. Method: collect every `addMessageEvent(new X(...))` in the AS3 `roomevents/` tree + `room/_SafeCls_1984.as` (the RoomMessageHandler equivalent), resolve each class to its header via the WIN63 registry `_SafeStr_4546[<h>] = <class>` in `communication/_SafeCls_2046.as`, and diff against the port's `_events.set(...)` list. **16 of the 23 are in RoomMessageHandler** — an area previously described as done, which is exactly why the gap stayed invisible (completeness was being measured as "files I ported", not "gaps that exist"). Verified samples: 264/1131/834 are genuinely absent from `HabboMessages.ts` and genuinely subscribed in AS3.
  - **RoomMessageHandler (16, header → AS3 handler):** 254 `onItemRemoveMultiple` · 264 `onDiceValue` · 325 `onWiredMovements` · 536 `onSpecialRoomEvent` · 540 `onItemDataUpdate` · 834 `onItemStateUpdate` · 1131 `onAreaHide` · 1778 `onOneWayDoorStatus` · 1787 `onItemsStateUpdate` · 2126 `onGuideSessionEnded` · 2361 `onObjectRemoveMultiple` · 2377 `onGuideSessionError` · 2458 `onBCPlacementWarning` · 2624 `onAvatarEffect` · 3643 `onObjectRemoveConfirm` · 3649 `onGuideSessionStarted`. Visible symptoms: dice show no value, furni states never update, one-way gates inert, wired area-hide inert, guide sessions dead, avatar effects never triggered (matches the standing "trigger deferred" note).
    - **✅ CLOSED (2026-07-24): 16/16 fixed**, plus 6 more found by re-measuring. Commits: `689f54a` 264 `onDiceValue` · `7d46764` 1778/254/2361 · `0de4ec0` the wall-item trio 834/1787/540 · `7824586` 1131 `onAreaHide` · `63fc51c` 325 `onWiredMovements` · `5316023` 2624 `onAvatarEffect` + 3649/2126/2377 `onGuideSession*` · `22c3e1a` 3643/2458 confirm dialogs · `0df631e` 536 `onSpecialRoomEvent` · `f2dc29d` the 6 orphaned subscriptions below.
      - **The shape of the bug class, confirmed 16 times: the pipeline exists, nothing feeds it.** In almost every case the engine and logic side was already ported and correct; only the parser+event+registration+subscription were missing.
      - **2624 `onAvatarEffect` was not a missing port at all — the header was wrong.** Parser, event, subscription and handler all existed and matched AS3 exactly; the port registered them on 3629, which the registry maps to a different message (`_SafeCls_3136` → parser `_SafeCls_4142`, a single `type` int, consumed by `AvatarEditorMessageHandler.onAvatarEffectSelected`). The room-side header is 2624. That one number is why in-room avatar effects never appeared despite the whole render pipeline being ported — it also retires the standing "trigger deferred" note.
      - **The 325 wall-item branch was NOT blocked on `getLegacyGeometry()`** as previously recorded: this port keeps its `LegacyWallGeometry` on `RoomMessageHandler`, initialised from the heightmap, and already used it for every other wall-item placement.
      - **New engine surface added along the way:** `updateObjectWallItemState/Data`, `updateAreaHide`, `updateObjectWallItemLocation`, `updateObjectUserDir`, `updateObjectRoomColor`, `setIsPlayingGame`, `setHanditemControlBlocked`, `leaveSpectate`, `getRoomGeometry`, `getObjectRoom`/`getObjectWallItem`; `updateObjectFurnitureLocation` widened 6→8 (overshoot+curve) and `updateObjectUser` 10→11 (jumpPower); `roomSessionManager` / `sessionDataManager` / `windowManager` exposed on `IRoomCreator` and `IID_HabboWindowManager` taken as a RoomEngine dependency, as AS3 does. `RoomObjectAvatarDirectionUpdateMessage`'s ctor was corrected to AS3's `(location, direction, dirHead)` — the port's 1-arg version dropped the body direction. `RoomRotatingEffect` / `RoomShakingEffect` ported into `room/utils`.
      - **Two remaining display-side gaps, both marked `TODO(AS3)` in code, neither a message gap:** `RoomRenderingCanvas.render()` does not poll the shake/rotate effects (no `changeShaking()`/`changeRotation()`), so 536's effects 0 and 1 drive their state machines but produce no motion — zoom and disco are complete; and `RoomEngine.updateObjectRoomWindow()` is unported, so a window furni moved by wired keeps its plane mask at the old position.
    - **Re-measured after the fixes — both audit modes now come back clean for RoomMessageHandler.** All 57 events AS3 subscribes to are registered, and all 57 have a live consumer (51 in `RoomMessageHandler`, 6 elsewhere in the port). The re-measurement surfaced **6 further mode-2 orphans** that were fully ported and registered but had no subscriber at all — fixed in `f2dc29d`: 946 `onPetExperience` · 3796 `onPetFigureUpdate` · 1600 `onPlayingGame` · 412 `onYouAreNotSpectator` · 1052 `onGamePlayerNumberValue` · 2942 `onConfigurationItemStates`. `HanditemConfigurationMessageParser` was also reading only 1 of its 4 booleans, silently dropping three room toggles at the wire.
    - ⚠ **Still open, newly measured, NOT fixed:** headers 253 `onIgnoreResult` and 1825 `onBlockUserUpdate` are consumed in the port by `IgnoredUsersManager` / `BlockedUsersManager`, which maintain the lists but do **not** do the room-object half AS3's `RoomMessageHandler` does (`updateObjectUserAction(..., 'figure_is_muted', ...)`) — verified: neither file mentions `figure_is_muted` or `updateObjectUserAction`. So the mute marker never appears on the avatar. Same question is unverified for the chat trio (311/1776/3072) handled by `RoomChatHandler`.
  - **wired_menu / wired_trading (7, expected — their tabs/subsystems are unported):** 1230 + 2179 (inspection tab), 3506 (overview tab), 2910 (chests tab), 1022 + 2323 + 2738 (chest sub-controllers).
  - ⚠ **This audit only covers failure mode 1 (never registered).** Mode 2 — registered but nothing ever `addMessageEvent()`s it, so it silently no-ops (the `ScrSendUserInfoEvent` class of bug, and 3483 before the real controller landed) — is audited separately below. Findings are claims: verify each against the AS3 before acting.
  - ⚠ **Both audits were scoped to `roomevents/` + `room/_SafeCls_1984.as`.** They say nothing about the rest of the AS3 tree; re-running the same two commands over another subsystem is the way to find its gaps.
- 🔴 **AUDIT 2026-07-24 (mode 2) — 42 incoming messages are registered in `HabboMessages.ts` but nothing ever subscribes to them, so they parse-and-vanish instead of even logging "no registered handler".** Method: extract every `_events.set(<h>, <Class>)` pair, then diff the class names against every `new <Class>(` in the two packages excluding `HabboMessages.ts` — a registered event that is never constructed is never passed to `addMessageEvent()`. Heuristic validated against known-working controls (`RoomVisualizationSettingsEvent`, `ObjectsDataUpdateMessageEvent`, `WiredPermissionsEvent` all come back instantiated) and two suspects verified by hand (`NewConsoleMessageEvent` 468 and `TradingOpenMessageEvent` 953 appear only in their own definition file, the registration, and an index re-export). Clusters, by feature:
  - **Trading (7):** 560 `TradingAccept` · 699 `TradingClose` · 953 `TradingOpen` · 1070 `TradingCompleted` · 2275 `TradingItemList` · 3138 `TradingConfirmation` · 3556 `TradingNotOpen` — the whole trade flow is inert.
  - **Pets (10):** 332 `PetCommands` · ✅ 946 `PetExperience` (`f2dc29d`) · 1200 `PetInventory` · 1784 `PetRespectNotification` · 2753 `PetStatusUpdate` · 2940 `PetBreedingResult` · 3073 `PetVocal` · 3104 `PetLevelUpdate` · 3195 `PetPlacingError` · ✅ 3796 `PetFigureUpdate` (`f2dc29d`).
  - **Room settings / moderation (8):** 726 `FlatControllers` · 791 `RoomSettingsData` · 845 `BannedUsersFromRoom` · 879 `RoomSettingsSaveError` · 1359 `FlatControllerAdded` · 1783 `RoomSettingsSaved` · 2089 `UserUnbannedFromRoom` · 2944 `ShowEnforceRoomCategoryDialog` · 3335 `FlatControllerRemoved`.
  - **Messenger / console (6):** 468 `NewConsoleMessage` · 933 `ConsoleMessageHistory` · 3194 `RoomInvite` · 3339 `RoomForward` · 3360 `RelationshipStatusInfo` · 3501 `InstantMessageError`.
  - **Games (3) — ✅ all fixed (`f2dc29d`):** 412 `YouAreNotSpectator` · 1052 `GamePlayerValue` · 1600 `YouArePlayingGame`.
  - **Misc (8):** 682 `BotInventory` · 2430 `CantConnect` · 2913 `FaqText` · ✅ 2942 `HanditemConfiguration` (`f2dc29d`) · 3874 `HandItemReceived` · 3909 `ChangeEmailResult` · 4600 `RentableSpaceConfig`.
  - **Progress: 6/42 fixed** — the six that belonged to `RoomMessageHandler`. The other 36 need their own subsystems (trading, pet UI, room settings, messenger) and are untouched.
  - Caveat: "never constructed" is strong evidence of no subscription but each still needs a one-line check that the feature is not driven some other way before being treated as a bug.
  - **Follow-up, same session — "0" treated as "no override"**: once 1956 was actually wired, the user's room (floor/wall paint never customized) started rendering a real, wrong-looking motif instead of the classic default. Root cause confirmed by reading `vortex-emulator/Turbo.Rooms/Grains/RoomGrain.cs` (lines ~317-326): the server sends `RoomPropertyMessageComposer{Key, Value=entity.PaintFloor/PaintWall/PaintLandscape.ToString()}` straight from the DB columns, whose C# default/unset value is `0` - the emulator never omits the property or substitutes the real default motif ids for an unset room, and AS3's own `updateObjectRoom()` has no `"0"` special-case to fall back on (confirmed by reading it directly), so a real client talking to this same server would show the identical wrong motif. This is a `vortex-emulator` data/protocol quirk, not an AS3 behavior - documented as such (not tagged `AS3:`) via a new `RoomMessageHandler.normalizeRoomPropertyValue()` that maps `"0"` to `null` before calling `updateObjectRoom()`, so the pre-existing `initializeRoom()` default motifs (`'111'`/`'201'`/`'1'`) stay in effect until a real (non-`"0"`) value arrives.
- 🟢 **Phase 5 complete: offscreen furniture/room image rendering** (`RoomEngine.getFurnitureImage()`/`getWallItemImage()`/`getRoomImage()`/`getPetImage()`/`getRoomObjectImage()`/`getGenericRoomObjectImage()` + `IRoomObjectVisualization.getImage()`), unblocking the 3 widgets that were waiting on it: `ProductImageWidget` (wall/floor item full-size preview), `ClubGiftWidget` (gift-offer hover preview), `ProductViewCatalogWidget` (no-room-canvas fallback path — the room-canvas path itself was already working).
  - **Core finding that reframed the whole phase**: the roadmap's "0% ported" note undersold how much was already there. `RoomPreviewer.ts` (973 lines, live on-screen furniture/avatar preview canvas) was already advanced and unrelated to this gap — the real missing piece was a completely different mechanism: `RoomEngine.getGenericRoomObjectImage()`, which spins up a *hidden* room object (no canvas) for a single forced render pass, then tears it down. Most of its supporting primitives already existed and needed no changes: `IRoomManager.createRoom()`/`getRoom()` (raw string-keyed rooms, bypassing the numeric-roomId wrapper - AS3 does the same for its own `"temporary_room"`), `IRoomInstance.createRoomObject()`/`disposeObject()`, `RoomContentLoader.getObjectCategory()`/`isContentAvailable()`, `RoomGeometry`, and critically `RoomEngine.contentLoaded()` (already existed for the icon/thumbnail path - extended, not replaced, to also resolve pending "image" listeners the same way AS3's `contentLoaded()` does).
  - **The one genuinely new piece**: `IRoomObjectVisualization.getImage()`/`get image()` (declared in the base interface in AS3, not just the sprite-specific one) didn't exist in this port at all. AS3 composites via `BitmapData.draw()` (a software rasterizer needing no live renderer); PixiJS needs an actual `Renderer` to rasterize, so this builds a throwaway `Container` of plain `Sprite`s per AS3's `IRoomObjectSprite` field (position/tint/alpha/blendMode/flip mirroring AS3's `Matrix`+`ColorTransform` math exactly, sorted by `relativeDepth` descending) and reads it back via `Vortex.instance.application.renderer.extract.canvas()` - the same synchronous texture-to-canvas mechanism `RoomEngine.pixiTextureToCanvas()` already used elsewhere, applied to a `Container` instead of a single `Texture`.
  - **`extractDarknessToAlpha()` ported in full** (the additive-glow-on-transparent-background edge case, user-confirmed in scope rather than deferred): AS3's per-pixel `BitmapData.getVector()`/`setVector()` HSL loop, ported as a synchronous 2D-canvas `getImageData()`/`putImageData()` pass using the already-ported `ColorConverter.rgbToHSL()`/`hslToRGB()` (confirmed identical H/S/L bit-packing convention by reading both).
  - **TS deviation, matches this port's established `ImageResult` convention**: AS3 populates `ImageResult.data` synchronously when content is already loaded (id=0) and only defers via a listener when it isn't. `ImageBitmap` conversion is always async in the browser (documented on `ImageResult.ts` since Phase 1), so this always resolves through the `id>0` pending path and delivers via `imageReady()`/`imageFailed()` - same convention `getGenericRoomObjectThumbnail()` (the icon path) already established. One real behavioral difference from a literal port: in the "content not loaded yet" branch, AS3 still captures and stores a (necessarily blank) image on the result before registering the listener; this port skips that blank capture entirely since nothing may ever read it synchronously anyway.
  - **Two lower-priority AS3 members ported with an honest, narrower gap** rather than skipped: `getRoomObjectImage()` (queries an already-placed room object's own furniture_color/extras/figure) has a `TODO(AS3)` for the `furniture_data_format != 0` branch, which needs a stuff-data-wrapper factory (AS3 `_SafeCls_2295.getStuffDataWrapperForType()`) that isn't ported. `initializeRoomForGettingImage()` (backs `getRoomImage()`'s `type="room"` case) wires the real floor/wall/landscape model strings and door mask, but the tile-height/geometry init is a `TODO(AS3)` - the `RoomVisualization` event handler's `initialize()` data-shape contract for a standalone "room" object isn't confirmed on this port (`RoomPreviewer`'s own room setup goes through a different, higher-level `IRoomEngine.initializeRoom()` path that doesn't apply here). Neither gap blocks any of the 3 widgets this phase was scoped to unblock - `getRoomImage()` itself is used by later, not-yet-started phases (room-ad/guild previews).
  - **`ProductCategoryMapping.categoryMapping()`** (already ported) turned out to gate `ProductImageWidget`'s wall-item case in AS3 too - only wall items where `categoryMapping("I", classId) === 1` get a full-image preview; others fall through to `clearPreviewer()`, matching AS3 exactly rather than an invented simplification.
  - **`ClubGiftWidget`'s hover preview needed one adaptation AS3 doesn't have**: AS3's `showPreview()` passes a `null` listener and reads `ImageResult.data` synchronously (safe in AS3 since content is normally already loaded by hover time). Since this port never trusts synchronous `ImageResult.data` (see above), `ClubGiftWidget` now implements `IGetImageListener` and delivers the preview through `imageReady()`, tracked by a pending-request id - same shape as `ProductImageWidget`'s already-established `_pendingImageId` pattern, applied here for the first time in this widget.
- 🟢 **Floor-furniture placement/move ghost-object pipeline ported for real, plus 4 unrelated pre-existing bugs found and fixed while chasing user bug reports** (rotate a piece of furniture: server round-trip was correct but nothing visually changed; drag-move showed a preview sitting at the wrong height; placing anything from inventory silently did nothing). What started as a rotation-animation report cascaded into a full audit of the client/server furniture lifecycle; all fixes below are confirmed against the real AS3, not invented workarounds.
  - **Real ghost-object placement** (`RoomEngine.ts::initializeRoomObjectInsert()`/`handleObjectPlace()`/`placeObject()`, matching `_SafeCls_1821.as`'s real method names recovered from the primary tree): placing an item from inventory now spawns an actual semi-transparent room object that snaps tile-by-tile via `handleFurnitureMove()`/`validateFurnitureLocation()` against a real `FurniStackingHeightMap` (now built from the live `HeightMapMessage`/`HeightMapUpdateMessage`, previously unused dead code), falling back to the old flat icon only when no valid tile is hovered — same fallback behavior AS3 itself has. `OBJECT_MOVE` (dragging an already-placed item) was unified onto the same `SelectedRoomObjectData`/`handleObjectMove()`/`confirmObjectMove()` pipeline, replacing a separate simplified `_pendingMove` mechanism that ignored stacking height entirely. Wall-item placement is still out of scope: AS3 runs it through the identical pipeline (`handleWallItemMove()`/`validateWallItemLocation()`), but finalizing it needs `PlaceObjectMessageComposer`'s `wallLocation` string encode (world position → `"w=x,y l=x,y r|l"`), and no AS3 source here has the inverse of `LegacyWallGeometry.getLocation()` to port that encode from — left rejected rather than guessed.
  - **Rotating an already-placed item now validates like AS3** (`getValidRoomObjectDirection()`, already-ported but unused, plus a new `validateFurnitureDirection()`): previously cycled blindly through 4 cardinal directions with no stacking check; now respects `furniture_allowed_directions` and rejects a rotation that wouldn't fit.
  - **`TileObjectMap` wired in** (`RoomEngine.getTileObjectMap()`/`refreshTileObjectMap()`, `RoomInstanceData`'s real AS3 setter behavior reproduced: assigning a new `FurniStackingHeightMap` also rebuilds the paired `TileObjectMap` at the same dimensions): was a fully-written, fully-unused utility class since some earlier phase.
  - **Root cause #1 — `RoomManager.ts::createRoomObject()`/its content-loaded re-init path both called `logic.initialize(null)`** instead of the already-fetched `visualizationConfig`, so `FurnitureLogic`'s `furniture_size_x/y/z`, `furniture_center_x/y/z`, and `furniture_allowed_directions` were never populated for *any* furniture — the stacking-map validation above was silently defaulting every item to a 1×1 footprint with no rotation restriction data at all.
  - **Root cause #2 — fixing #1 exposed a second bug in `FurnitureLogic.ts::initialize()`**: it read `data.model.dimensions`/`data.model.directions.direction` (an object list), but the real compiled bundle nests the furniture's logic config one level deeper (`data.logic.model...`, sibling to `assets`/`spritesheet`/`visualizations`) and stores `directions` as a flat array of raw index numbers, not `{id}` objects. Fixed both the nesting path and the entry shape (defensively accepting either shape for the latter, matching an existing quirk-handling pattern already used elsewhere in `RoomContentLoader.ts` for singular-vs-array XML→JSON collapsing).
  - **Root cause #3, the one that actually explained the placeholder-forever symptom — `RoomEngine.ts::addRoomObjectFurniture()`/`addRoomObjectWallItem()` passed a pre-resolved `logicType` (e.g. `"furniture_multistate"`) into `room.createRoomObject(id, type, category)`'s `type` parameter**, instead of the real furniture classname. AS3's `createObjectFurniture()`/`createObject()` (`_SafeCls_90.as`) always passes the real classname and lets the room-object-creation path resolve `visualizationType`/`logicType` itself; passing an already-resolved, generic logic-type string made `loadObjectContent()` try to fetch assets for a type that doesn't exist (empty URL list), so the object could never leave placeholder content — not just missing rotation data, but permanently placeholder-only for its whole lifetime.
  - **Root cause #4 — even after #1–#3, a `RoomObjectModel` "immutable" write-once flag (confirmed identical in real AS3's own `setNumber()`/`setNumberArray()`: `if (protect) firstWriteWins` — this is faithful AS3 behavior, not a porting bug) meant the *first* `logic.initialize()` call (still fired at creation time with placeholder data) permanently locked `furniture_allowed_directions`/size fields before the real content's re-init call could ever take effect.** Fixed by gating `RoomManager.createRoomObject()`'s `logic.initialize()` call on `initialized` (real content already available) — when using placeholder content, logic is left uninitialized at creation time and gets its one real `initialize()` call from the content-loaded re-init path instead, matching the effect of AS3's own conditional skip (real AS3's placeholder bundle apparently carries no logic XML at all, so it never reaches this call in the first place; this port's placeholder bundle does have one, generic and wrong, so the fix reproduces AS3's *outcome* via the information already available here rather than inventing a new condition).
  - **No visual change on rotate, once the direction data above was correct, traced to a 5th and final gap**: `RoomEngine.ts::updateObjectFurniture()` called `object.setLocation()`/`setDirection()` directly instead of dispatching a `RoomObjectUpdateMessage`/`RoomObjectDataUpdateMessage` through `object.getEventHandler()?.processUpdateMessage()` like AS3's real `updateObjectFurniture()` (`_SafeCls_90.as`) does — bypassing `FurnitureLogic`'s existing-but-dead rotation-bounce mechanism (`_bounceStep`/`BOUNCE_STEPS`/`getLocationOffset()`, already fully implemented, never reachable). Rewired to dispatch through the event handler (also added the missing `RoomEngineObjectEvent.REOE_UPDATED` constant, recovered from the primary tree's `_SafeStr_10306`). Once reachable, the bounce animation ran ~2–3x too fast: AS3's `_bounceStep` also has no internal throttle, relying on the whole engine ticking at a fixed, much lower Flash framerate; this port's logic layer runs at raw `requestAnimationFrame` rate with no equivalent throttle (unlike `FurnitureVisualization.ts`, which already self-throttles to ~24fps/41ms for the same reason) — added the same 41ms throttle to `FurnitureLogic`'s bounce-step advance.
- 🟢 **Vortex-custom protocol messages wired: rentable space config + pet vocal/command/move** — 6 new message classes (not part of any official AS3 dump; source is the sibling `vortex-client` AS3/Flash project that pairs with the real `vortex-emulator`, confirmed via its git history: commits `f3bba54`/`d6bc0d0`/`e8dc43d`). `GetRentableSpaceConfigMessageComposer`/`ConfigureRentableSpaceMessageComposer` (outgoing, headers 4600/4601) + `RentableSpaceConfigMessageEvent`/`RentableSpaceConfigMessageEventParser` (incoming, header 4600 — same number as the outgoing side is not a collision, they're different wire tables) under `messages/{outgoing,incoming,parser}/room/furniture/`. `IssuePetCommandMessageComposer`/`MovePetMessageComposer` (outgoing, headers 3072/2761) + `PetVocalMessageEvent`/`PetVocalMessageEventParser` (incoming, header 3073) under `messages/{outgoing,incoming,parser}/room/pet/` (new `incoming/room/pet/` and `parser/room/pet/` folders added, `outgoing/room/pet/` already existed). All header values cross-confirmed two ways: exact match against `vortex-client/src/.../HabboMessages.as`'s own registry, and the literal IDs named in its commit messages. Registered in `HabboMessages.ts` with `// Vortex-custom (not in official AS3 dumps)` comments in place of the usual `sources/WIN63-...` trace, since the mandate's AS3-traceability format doesn't apply to a feature that was never in the official client.
- 🟡 **`HabboCatalog` never registered `ScrSendUserInfoEvent` (message id 1114)** — surfaced as `SocketConnection`'s "No registered handler for incoming message id 1114" debug log on every login/club-status push. The event class/parser (`ScrSendUserInfoEvent`/`ScrSendUserInfoMessageParser`) and its `HabboMessages.ts` id mapping already existed and parsed correctly; only the `addMessageEvent()` subscription + handler (`onSubscriptionInfo`) were missing from `HabboCatalog.initComponent()`, so club/VIP subscription state (`Purse.clubDays`/`clubPeriods`/`isVIP`/`pastClubDays`/`pastVipDays`/`isExpiring`/`minutesUntilExpiration`/`minutesSinceLastModified`) was silently never applied and the purse UI's club badge never updated. Ported `onSubscriptionInfo()` for real, including the `ExternalInterface.call("FlashExternalInterface.subscriptionUpdated", ...)` web-bridge call, now routed through `HabboWebTools` (new `subscriptionUpdated()` static method + `IHabboWebToolsEvents` entry, matching that class's existing `FlashExternalInterface.*` bridge-method pattern). **TODO(AS3)**: the `responseType === RESPONSE_TYPE_PURCHASE` branch (`reset()` + reopen the page remembered via `rememberPageDuringVipPurchase()`) is not ported — `reset()` tears down/reloads the whole catalog (navigators/viewer/product data) after a club purchase, and its prerequisite `rememberPageDuringVipPurchase()` is still a no-op stub.
- ✅ **In-room group panel + the navigator's guild badge** (`habbo/groups/GroupRoomInfoCtrl.ts`, `habbo/navigator/GuildInfoCtrl.ts`, `GetGuildEditInfoMessageComposer` 874): two separate faults behind "no guild widget in the room, and the navigator's guild icon does nothing". `GuildInfoCtrl` set the guild name but never touched the badge — AS3 looks `guild_badge` up on the *outer* container (not on the `guild_info` window it just built; different windows) and pushes `groupBadgeCode`/`habboGroupId` into its `BadgeImageWidget`; that block was absent, and the click handler was commented out with a stale "composer unavailable" note. `GroupRoomInfoCtrl` was not ported at all — now whole, with `HabboGroupsManager` forwarding `GetGuestRoomResult` (3042, gated on `enterRoom` so navigator listing lookups can't retarget it), group details, deactivation and `CloseConnection`. **Caveat, not in the code**: both entry points are gated on `groupRoomInfo.enabled` (toolbar attach additionally on `groupRoomInfo.attach.enabled`), and neither key is defined anywhere in this setup — not in the shipped `common_configuration`, not by the emulator. `getBoolean()` answers false for a missing key, so the panel stays hidden until external variables supply them; hardcoding a default would diverge from AS3.
- ✅ **Group creation ported end-to-end** (`habbo/groups/GuildManagementWindowCtrl.ts` + `ColorGridCtrl.ts`, `GuildSettingsCtrl.ts`, `GroupCreatedWindowCtrl.ts`, `HcRequiredWindowCtrl.ts`, `badge/` ×5, and 19 new message classes): the request side already worked — `BuyGuildWidget` sent `GetGuildCreationInfo` (2989) — but **nothing on the client could receive the reply**. There was no incoming event, no parser and no wizard, so the button fired into silence. Now wired: events 973 `GuildCreationInfo` / 1288 `GuildEditInfo` / 1132 `GuildEditorData` / 2138 `GuildCreated` / 496 `GuildEditFailed`, composers 3398 `GetGuildEditorData` / 207 `CreateGuild` / 2009 `UpdateGuildIdentity` / 3882 `UpdateGuildBadge` / 3421 `UpdateGuildColors` / 3716 `UpdateGuildSettings` (headers from WIN63's own registry `_SafeCls_2046.as`, corroborated against `vortex-emulator`'s `Headers.cs`; both serializers verified field-for-field against the port's parsers). The badge editor composites in `OffscreenCanvas` — AS3's `ColorTransform` multiply becomes a `multiply` blend plus `destination-in` to restore the part's alpha. `HabboGroupsManager` also gained the room-id/HC/flat-created listeners the wizard reads (`RoomEntryInfo`, `ScrSendUserInfo`, `FlatCreated`). Deferred with `TODO(AS3)`: `GuildMembersWindowCtrl` (the members link null-checks, as AS3 does), `GroupRoomInfoCtrl.expectedGroupId`, and `onRoomLeave` (nothing to close until `DetailsWindowCtrl`/`GroupRoomInfoCtrl` land).
- ✅ **InfoStand user/bot identity display + extended user profile (click-to-view) ported end-to-end** (`habbo/ui/handler/InfoStandWidgetHandler.ts`, `habbo/ui/widget/infostand/InfoStandUserView.ts`/`InfoStandBotView.ts`, new `habbo/groups/ExtendedProfileWindowCtrl.ts`): was category-100 stub (no user/bot info panel at all, no click-to-profile). Real user/bot identity views ported (avatar, motto, online status, profile-name link) — the click-to-profile handler (`RWOPEM_OPEN_USER_PROFILE`) sends `GetExtendedProfileMessageComposer`, which was defined but never registered in `HabboMessages.ts` (silently dropped every send). Basic-scope `ExtendedProfileWindowCtrl` ported: header, badges (no glow/hover popup), add/block/unblock-as-friend with confirm dialogs, change-looks/change-badges links. Groups list (`GroupDetailsCtrl`, 306 AS3 lines), relationship status, and badge glow/popup explicitly deferred (`TODO(AS3)`).
  - Found and fixed a second, unrelated blocker while diagnosing "the profile window never opens": `HabboGroupsManager`'s `IID_HabboFriendList` dependency was required, but nothing in the DI graph provides that interface (the three other consumers all mark it optional) — the manager was permanently locked, so *none* of its message events, old or new, ever registered. No console error, no warning; the fix (`required: false`) also silently un-breaks pre-existing group functionality that was likely broken by this same gap.
  - Also fixed two window-system bugs surfaced by this work, both broader than this one feature: `WindowParser` resolved `${key}` localization tokens eagerly at parse time, before `TextController`'s setter could detect the `${key}` shape and register the live-update listener `registerParameter()`'s `%param%` substitution needs — froze any caption using both patterns together with literal `%placeholders%` forever. And `FormattedTextController` has no equivalent to Flash's native `TextField.htmlText` tag rendering — added a minimal `<b>`/`<i>`/`<u>`/`<br>` parser (`core/window/utils/HtmlFormatting.ts`) plus per-run bold/italic support in `WindowComposite`'s text renderer (previously only color/underline).
  - Also fixed a black-square avatar bug (`AvatarImageWidget.ts`): used `renderer.extract.canvas()`, a GPU-round-trip method for textures that went through an actual PixiJS render pass, on a canvas-backed `Texture.from({resource})` texture that never did — switched to a direct `texture.source.resource` read, matching `ProductGridItem`'s existing pattern. And an invented `widgetWindow.visible = true` shortcut that was masking the real AS3 "crikey" easter-egg `sticker_croco`/`avatar_image` visibility swap, which lives in `setMotto()`.
- ✅ **Clickable chat message links ported end-to-end** (`habbo/ui/widget/roomchat/RoomChatItem.ts`): was a documented `TODO(AS3)` — messages with links rendered as plain text. Found a deeper bug while porting it: `RoomChatHandler.ts` flattened the parser's `{url, displayText, isTrusted}` link objects down to bare display-text strings before they reached the chat bubble, so even a correct render would have had no URL to open. Fixed at the source, re-typed `IChatLink[]` through `RoomSessionChatEvent`/`RoomWidgetChatUpdateEvent`. Also added the missing rendering primitives: `ITextWindow`/`TextController` gained `getCharIndexAtPoint()`/`getTextFormat()`/`setTextFormat()` with per-range format "runs" (AS3's `setTextFormat()` is a no-op without a valid range - preserved as-is), and `WindowComposite.compositeText()` gained a run-aware single-line path so one text window can mix a base style with a colored/underlined link substring. Cross-referencing `win63_version` against `win63_2023_version` also confirmed `if(var_1993 && false)` in the latter's click handler is decompiler corruption (`&& false` constant-folded); the real condition is `links.length > 0`.
- ✅ **`ModalDialog.refresh()` ported for real** (`habbo/window/utils/ModalDialog.ts`): was `desktop.visible = false` (room disappears behind a dialog instead of dimming). AS3 captures/darkens the desktop via `BitmapData.draw(stage)`; this port's equivalent (`WindowComposite`, Canvas2D, lives in `vortex-engine` — not PixiJS/client) gained `renderWindowToCanvas()`/`darken()` primitives, and `ModalDialog.refresh()` now snapshots the real desktop before hiding it (this compositor skips invisible desktops, unlike Flash's `draw()`), darkens it, and layers in each stacked dialog's own frozen appearance — full multi-dialog-stacking parity, not just single-dialog dimming.
- ✅ **Landing view (hotel view) fully ported** (`habbo/friendbar/landingview/`, ~76 AS3 files → 76 TS files): was a shell (empty background window, toolbar state switching only, zero widgets rendered) — now the complete AS3 subsystem.
  - **Controller (`HabboLandingView.ts`)**: added the missing `catalog`/`questEngine`/`habboHelp`/`avatarEditor`/`gameManager`/`tracking` DI dependencies and getters, `goToRoom()`/`requestBadge()`/`communityGoalVote()`/`getProductData()`, `hotel_view_welcome_window` cleanup, new-identity dynamic-slot override, `onExpiredLinkClick`/`onNavigatorSettings` listeners, and an `initialization_error` fallback window in `tryInitialize()`. **Onboarding flow (`OnBoardingHcFlow`) intentionally excluded** — confirmed by the user to be an unrelated login/account-creation system, not part of hotel view; the landing view activates unconditionally rather than gating on `suggestedLoginActions`.
  - **Layout engine**: `WidgetContainer` (placeholder/dynamic-slot binding), `DynamicLayoutManager` (6-slot grid, ported the slot-balancing math verbatim including an apparent off-by-one in the AS3 that only balances slots 0-4, not 5 — not "fixed"), `CommonWidgetSettings`, `MovingBackgroundObjects` + 4 motion classes (`Linear`/`Spiral`/`RandomWalk`/`StaticAnimated`) + `PathResetEvent`, and `WidgetContainerLayout` completed end-to-end (`registerFixedWidgets`/`registerDynamicWidgets`, dynamic-vs-custom resize paths, background-image timing codes, navigator auto-open, scheduling composer).
  - **All 22 widgets** ported to `widget/`, plus the composable `GenericWidget` + its `ElementHandlerFactory` and ~24 content-element handler classes (button/text/timer/image/badge/meter variants), de-obfuscated by content-signature cross-reference against `win63_version` (directory position between the two trees was found to be unreliable/misaligned — don't rely on it for future de-obfuscation in this module).
  - **Wire protocol**: promo articles (was already ported, just needed a consuming widget), community-goal voting (`CommunityGoalVoteMessageComposer`/`CommunityVoteReceivedEvent`, absent from the primary `WIN63-202607011411-782849652` tree — registered using `win63_version`'s real header ids 1104/551, matching this project's existing precedent of reusing win63_version ids elsewhere, e.g. `GetCommunityGoalHallOfFameMessageComposer=1034`), `ForwardToARandomPromotedRoomMessageComposer`/`RequestABadgeComposer` (ids 2120/2276, same rationale), `CurrentTimingCodeMessageEvent`+parser (competition module), plus a handful of other previously-missing composer/event pairs discovered mid-port.
  - **Interface-completeness fixes found along the way** (concrete controllers already had the members; the interfaces didn't expose them — same pattern as prior audit sessions): `IBitmapWrapperWindow`/`IStaticBitmapWrapperWindow` gained `stretchedX`/`stretchedY`; `ITextWindow` gained `etchingPosition`+`border`; `IHabboQuestEngine` gained `reenableRoomCompetitionWindow()`.
  - **Local-dev config**: `landing.view.dynamic.slot.{1-5}.widget` default assignments added to `common_configuration_txt.txt` (production would get these from server-pushed `external_variables`, which this project doesn't have a real backend for — same category as the pre-existing `landing.view.background_left/right.uri` defaults in that file).
  - **Process note**: this was built via 5 parallel forked agents (4 widget batches + GenericWidget/elements); 2 of the 5 initially reported false completion (generic status messages instead of actual deliverables, caught by checking the filesystem rather than trusting the report) and had to be resumed with a corrective prompt — worth remembering if delegating large parallel porting batches again.
- 🟡 **`SocketConnection.processReceivedData()` swallowed a fatal wire-format error, causing an infinite console-spam loop of "Invalid message length" with a different random value every frame**: `WireFormatter.splitMessages()` throws on an out-of-range length (faithful to AS3 `class_2705.as::splitMessages()`, which has the same unconditional throw with no `encryption.reset()`/buffer compaction on that path). AS3's caller (`class_1752.as::processReceivedData()`) catches this, notifies `messageParseError()`, and **re-throws** - i.e. treats it as fatal. Our port's catch block only logged the error and returned, leaving `receivedBuffer` uncompacted and the encryption keystream already advanced past the corrupted length prefix (the failed `decipher()` call consumed keystream with no matching `reset()`). Since something calls `processReceivedData()` every frame, this replayed the same stale bytes against an ever-advancing keystream forever, producing a new garbage `length` value each time. Fixed by clearing the buffer and closing the connection (via the existing `connectionError` callback + `close()`) instead of silently continuing.
- 🟡 **`RoomContentLoader.getClassName()` silently dropped indexed-color floor/wall furniture (e.g. `carpet_polar*73`) — icon rendered fine, the actual 3D furniture never loaded, with zero console output anywhere**: `populateFurniData()` correctly mirrors AS3, storing the raw `"baseClassName*colourIndex"` string in `_activeObjectTypes`/`_wallItemTypes` while keying `_floorItems`/`_wallItems` by the base name alone (matching `class_1835.as::populateFurniData()`'s `var_765`/`var_752` split exactly). AS3's public `getActiveObjectType()`/`getWallItemType()` both call `getObjectType()` to strip the `*colourIndex` suffix before returning - but `RoomContentLoader.getClassName()` (the helper `RoomEngine.getFurnitureClassName()` actually calls when creating a room object) read `_activeObjectTypes`/`_wallItemTypes` directly, bypassing that stripping. The raw suffixed className then flowed into `getObjectCategory()`, which checks `_floorItems.has(type)` - a lookup that can never match a suffixed key, so it fell through to `RoomObjectCategoryEnum.MINIMUM`. `getObjectContentURLs()` requires `OBJECT_CATEGORY_FURNITURE`/`OBJECT_CATEGORY_WALL` to build any download URL, so it silently returned `[]`, and `loadObjectContent()`'s `if(urls.length > 0)` gate silently returned `false` - no network request, no error event, no log, and the object stayed queued in `_pendingFurnitureViz` forever (so `createVisualizationForFurniture()` was never even called for it). Fixed by making `getClassName()` delegate to the already-correct `getActiveObjectType()`/`getWallItemType()` instead of reading the raw maps. Also added warn logs to two previously-silent branches in `createVisualizationForFurniture()` itself (`getGraphicAssetCollection()`/`getVisualizationXML()` returning null) while diagnosing this, in case a different className fails to resolve for a different reason in the future.
- 🟡 **Further bugs found live in the same debugging session (input bubbling, skin rendering, PixiJS renderer setup)**:
  - **`VortexApp`'s mouse-wheel handler never bubbled**: `WindowController.update()` returns `false` for `WHEEL` by default — only `ItemListController`/`ScrollBarController` override it to actually scroll. Our handler dispatched the `WHEEL` event straight at `findWindowAtPoint()`'s result (the deepest/leaf window under the cursor — inside a scrollable box, usually a list item/icon/label, not the scrollable container itself) and stopped, so scrolling only worked on the rare occasion the cursor happened to be directly over the scrollable window itself. Fixed by walking up the `parent` chain (mirroring `MouseEventProcessor.passMouseEvent()`'s bubbling for other mouse events) re-dispatching at each ancestor until one actually reports `handled`.
  - **PixiJS `multiView` not enabled**: several widgets read pixels back off-screen via `renderer.extract.canvas()` (`AvatarImageWidget` avatar/badge previews, `RoomEngine.pixiTextureToCanvas()` room icons, `RoomRenderingCanvas.takeScreenShot()`) — each of those is, from WebGL's point of view, "another canvas" sharing the app's GL context. Without `multiView`, PixiJS's `GlContextSystem.ensureCanvasSize()` warns on every one of those extractions ("multiView is disabled, but targetCanvas is not the main canvas") because it only expects the single view canvas. Set `multiView: true` on the `Application.init()` config in `Vortex.ts` — PixiJS's documented flag for exactly this one-context/several-canvases case.
  - **`BitmapSkinParser`/`HabboWindowManager` renderer cache key too coarse, and descriptor `states` ignored**: AS3's `parseSkinDescription()` prefers the caller-supplied `states` (the element descriptor's own `<states>`) and only falls back to the skin XML's own `<states>` when that list is empty — our `BitmapSkinParser.parse()` never accepted a states override at all, which silently broke skins like `habbo_skin_icon_set` whose own top-level `states` is empty (every icon style's state→layout/template mapping instead lives entirely on its element descriptor). Compounding this, `HabboWindowManager`'s renderer cache was keyed by `layout` filter alone, but several descriptors share the same `layout` (e.g. `icon16` reused across arrow styles 0-9) while pointing at different `states`/`template` — so the first-parsed descriptor's renderer silently got reused for every other descriptor sharing that layout name. Fixed by adding an optional `descriptorStates` parameter to `parse()` (AS3-priority: descriptor states first, XML states as fallback) and folding the descriptor's own states into the cache key (`` `${layoutFilter}::${JSON.stringify(descriptor.states ?? [])}` ``).
  - **`BitmapSkinRenderer.draw()` stretched pieces that should never scale**: AS3 only resizes a drawn piece when its scale mode actually calls for it (`STRETCH`/`TILED` on either axis); `FIXED`/`MOVE`/`CENTER` always blit the piece at its own native size via `BitmapData.copyPixels()`, never stretched to fill the layout region's authored box, even when that box is a different size than the source crop (e.g. a small icon glyph centered in a bigger padded box). Our port instead sized the draw off `destW === srcW`/`destH === srcH`, which stretched every `FIXED`-mode entity whose region size didn't happen to match its source crop. Fixed by branching on the actual scale mode (`tiled` / `stretched` / neither) instead of comparing dimensions, matching AS3's `_loc16_`/`_loc9_` logic in `BitmapSkinRenderer.as::draw()`.
- 🟡 **Catalog icon/preview rendering bugs found and fixed live (dev-environment debugging session)**: three separate, previously-invisible bugs, each masking the next.
  - **`BitmapFileLoader.dispose()` premature `Assets.unload()`**: `AssetLibrary.handleAssetLoadEvent()` disposes the loader as routine cleanup right after handing its loaded `Texture` off to a new `BitmapDataAsset` (which caches it under an asset name for reuse). `dispose()` was calling `Assets.unload(url)`, telling PixiJS's `Assets` cache to release the GPU resource almost immediately — so a furniture/product icon rendered once (before/during the async unload) but broke on every later reuse of that cached asset (icon disappears on a second click; a catalog page loading many icons at once hit the race on nearly all of them). Fixed by dropping the local `_texture` reference only; the `BitmapDataAsset` now owns the texture's lifecycle from there (its own `dispose()` already calls `texture.destroy(true)`).
  - **`RoomEngine`/`AvatarImageWidget` texture→canvas conversion fragility**: both read `texture.source.resource` directly and `drawImage()`'d it — but PixiJS doesn't guarantee a CPU-side resource stays attached to a `TextureSource` once uploaded to the GPU (`resource` can legitimately be `undefined` for a fully valid on-screen texture). Switched both to `renderer.extract.canvas(texture)`, PixiJS's own supported readback API.
  - **`ProductGridItem.setIconImage()` use-after-close**: AS3 (`ProductGridItem.as::setIconImage()`) copies the source `BitmapData`'s pixels into a buffer the target *already owns* (`copyPixels()`), so disposing the source parameter afterward never touches what's on screen. Our port instead did `target.bitmap = image` (a reference assignment, not a copy) and then unconditionally called `image.close()` on that *same* object when `disposeSource` was true — destroying the bitmap the target had just been handed. This was the actual root cause behind "icon shows once then disappears" surviving even after the `BitmapFileLoader` fix above. Fixed: only close the source when it was *not* handed to a live target.
  - **`ProductViewCatalogWidget`'s `product_image_widget` never hidden**: AS3 finds this child (`var_1030`), unconditionally hides it in `init()` and again before every preview render, only re-showing it for the one product type its own preview switch doesn't otherwise handle (`ProductDisplayWrapper.isSupported()` — in practice only `chat_style`, since `"r"` has its own earlier case in the same switch and is therefore unreachable there despite `isSupported()` accepting it too). Our port never looked this node up at all, so its `placeholder_image` (visible by default per `ProductImageWidget`'s constructor) stayed on screen permanently, overlapping the room-canvas furniture/wall-item preview. Fixed by porting the lookup/hide-on-init/hide-before-render steps, and also implemented the previously-stubbed `ProductDisplayWrapper` (`habbo/catalog/viewer/ProductDisplayWrapper.ts` — a small `IProductDisplayInfo` adapter over `IProduct`, per the primary `WIN63-202607011411-782849652` source which adds `"r"` support and `botFigureString`/`extraData` over the secondary source's chat-style-only version) to wire up that default case for real.
- 🟡 **`LocalizationCatalogWidget.retrieveCatalogImage()` fully ported** (network fallback for a catalog page's localized images not already in the local asset library - was just logging and skipping): now calls the real `IAssetLibrary.loadAssetFromFile()` (already used elsewhere, e.g. `AvatarAssetDownloadLibrary`), builds the URL from `image.library.catalogue.url` or `image.library.url + "Top_Story_Images/"` depending on whether the target element carries a `TOP_STORY` tag (matching AS3 exactly), and applies the image via the existing `setElementImage()` once loaded. One AS3-fidelity note: AS3 listens via `addEventListener("AssetLoaderEventComplete", ...)`, but our `AssetLoaderStruct.dispatchEvent()` always emits under the generic `'event'` key regardless of the event's own `.type` - so a literal translation would silently never fire. Used the already-established, actually-working pattern instead (`loader.events.on('event', event => { if(event.type === AssetLoaderEventType.COMPLETE) ... })`, same as `AvatarAssetDownloadLibrary`/`EffectAssetDownloadLibrary`).
- 🟡 **`UniqueLimitedItemWidget` ported and registered** (the "X left of Y" LTD supply overlay on the product view page): was entirely missing - not implemented, not registered in `CatalogPage.createWidget()`'s switch (`'limitedItemWidget'` case). Its layout container (`limitedItemWidget` -> `unique_item_overlay_container`, `widget_type="limited_item_overlay_supply"`) has no default-hidden attribute in the compiled layout - AS3's widget class is the only thing that ever calls `window.visible = false`, so with the widget never constructed it showed constantly regardless of the selected product, which is what surfaced this gap. Full port: shows only for `pricing_model_single` + `isUniqueLimitedItem` offers, sets `supplyLeft`/`seriesSize` on the existing `LimitedItemSupplyLeftOverlayWidget`, and refreshes every 20s via a new real `HabboCatalog.sendGetProductOffer()` (new `GetProductOfferComposer`, message ID `1692` - cross-checked against both `win63_version` (id `3691`, a different build's numbering) and the primary `WIN63-202607011411-782849652` manifest (`1692`), which wins per the project's source-priority rule).
  - **TODO(AS3), clearly scoped**: the *response* handler (`ProductOfferEvent`/`onProductOffer`, ~40 AS3 lines reconstructing a full `Offer`/`Product` from the reply) isn't ported - it's a materially separate feature (new parser + full offer/product reconstruction shape + `CatalogPage.updateLimitedItemsLeft()` + `ProductOfferUpdatedEvent` dispatch). The periodic request now goes out correctly over the wire, but nothing currently handles the reply, so the supply count won't live-update if someone else buys the last one while a page is open - it's still correct at selection time from the initial catalog-page load data.
- 🟡 **`ProductViewCatalogWidget` preview interaction controls ported** (rotate_avatar_left/right, toggle_preview_magic, toggle_preview_zoom - previously present in every layout but entirely unwired): full `_previewMode` state machine (none/avatar/floor-furniture/wall-item), avatar direction rotation, avatar pose cycling ("preview magic" - stand/walk/dance/sit/lay/wave, skipping invalid poses per direction like AS3's diagonal-sit and non-cardinal-lay checks), and the 2-stage zoom animation (`applyRoomCanvasZoom()`/`onPreviewZoomAnimationFrame()`), all matching `ProductViewCatalogWidget.as` method-for-method. Also added the "s"-product category-23 sub-case (avatar wearing a rideable/wearable floor item, via `sessionDataManager.getFloorItemData()` + `avatarRenderManager.getFigureStringWithFigureIds()`), previously entirely missing (always fell into the plain-furniture path).
  - **New real `RoomPreviewer` API**: `updateAvatarDirectionAndLocation()` - previously missing entirely, now calls the real `IRoomEngine.updateRoomObjectUser()` (found under that name rather than AS3's generic `updateObjectUser()` - this engine has specific `updateRoomObjectUser*` methods per concern instead of one generic entry point). `addAvatarIntoRoom()`'s `effect` param now defaults to `0` (was required, blocking the 1-arg AS3 call shape used by the category-23 case).
  - **New `RoomPreviewer` stubs, honestly non-functional**: `canRotatePreviewFurniture()`/`rotatePreviewFurniture()`/`canRotatePreviewWallItem()`/`rotatePreviewWallItem()` always report "not rotatable" - AS3's versions need the previewed object's allowed-direction set, which the ported room engine doesn't expose yet. Floor-furniture/wall-item preview modes wire up correctly (rotate buttons appear) but stay disabled; avatar-mode rotation doesn't depend on this gap and works fully.
  - **TS deviation, documented at each site**: AS3 drives `setFloorFurnitureRotationAvailabilityMonitorEnabled()` and the zoom animation via a DisplayObject's own `enterFrame` event; ported via the window manager's shared update-receiver loop instead (same mechanism `HintManager` already uses), since there's no per-DisplayObject frame event here.
- 🟡 **`habbo/window/theme` audited (`Theme.ts`, `ThemeManager.ts`)**: found and fixed a missing theme plus 13 missing default properties.
  - **Missing "Misc" theme**: AS3 registers a real (`isReal=true`) `Theme.MISC` covering styles 10000-10007 (Ubuntu's cloned property set); our port had neither the `Theme.MISC` constant nor the theme registration, so `getThemeAndIntent()`/`getPropertyDefaults()` for that style range silently fell back to `"None"` instead. Added both.
  - **13 missing `PropertyMap` defaults**: `fill_mode`, `flip_x`, `flip_y`, `color1`, `color2`, `mode` (gradient), `radius`, `shape`, `sides`, `stroke_color`, `stroke_hsv_shade`, `stroke_thickness`, `tint` were never registered as theme defaults. Checked real runtime impact before fixing: `GradientController`/`ShapeController`/`StrokeController` carry their own internal defaults independent of the theme map (so no visual regression), and `BitmapFillController`'s hardcoded JS fallbacks (`'stretch'`/`false`) happened to already match AS3's real defaults by coincidence — so this was a silent metadata gap (dev-tool property-editor validation info), not a rendering bug, but still a genuine AS3-fidelity gap now closed.
  - Confirmed AS3 order/values otherwise match exactly; the one AS3 quirk *not* replicated (the base property map object being mutated by reference after being handed to the `None`/`Icon`/`Legacy border` theme constructors, changing their stored `etching_color`/`illumina_border:border_style` defaults post-hoc) is intentionally left as-is — those 3 themes are never `isReal`, so `getPropertyDefaults()` never reads their stored map at all; replicating the reference-aliasing would be invisible either way.
- 🟡 **`habbo/window` root-file audit finished (HintManager/HintTarget, ResourceManager, ElementRegistry, HabboWindowManager, and the 4 root interfaces)**: found and fixed 2 real bugs, confirmed the rest faithful.
  - **`HabboWindowManager.buildFromXML()` bug**: AS3 wires every frame built this way to `helpButtonAction = openHelpPage` (`if(result is FrameController) result.helpButtonAction = ...`); our port never did this, so the "?" help button on any frame built via `buildFromXML()` was permanently inert. Fixed. (`openHelpPage()` itself is still a `TODO(AS3)` stub pending `HabboPagesViewer`, so nothing visibly changes yet, but the wiring is now correct for when it lands.)
  - **`ResourceManager.retrieveAsset()` bug**: on a failed URL fetch, AS3 falls back to delivering the `"missing_image_icon"` placeholder to queued receivers; our port only cleared the loading flag and left receivers queued forever with nothing ever shown. Fixed — delivers the cached `missing_image_icon` bitmap on fetch failure, same as AS3.
  - **`HintManager.ts`/`HintTarget.ts`**: fully faithful; only benign differences (zero-rect vs `null` passed to `context.create()`, self-corrected by `fitSizeToContents`; a defensive `Math.max(1, distance)` guard against a division-by-zero AS3 itself doesn't guard).
  - **Dead parallel "declarative window" system removed** (user-confirmed, per the project's own "AS3 is the source of truth, never invent" rule): `HabboWindowManager`'s `openWindow()`/`closeWindow()`/`getWindow()`/`getWindows()`/`registerLayout()`/`getLayout()`/`getMaxZOrder()`, the `_windows`/`_layouts`/`_nextId`/`_windowEvents` fields, `WindowManagerEvents`, and the whole `IWindowInstance.ts` + `WindowLayoutParser.ts` files had zero AS3 equivalent and zero real callers anywhere in either package (confirmed by grep before deleting) — an early prototype superseded by the real `WindowContext`/`WindowParser`/`create()`/`buildFromXML()`/`buildWidgetLayout()` path and never cleaned up. `IWindowLayout.ts`'s data-shape interfaces were kept: `vortex-client/src/App.ts` genuinely uses them for its own JSON->XML layout serialization, unrelated to the deleted method cluster.
    - **Bug uncovered by the removal**: deleting `getLayout()` broke `SimpleAlertDialog.ts`, which was calling `windowManager.getLayout('simple_alert')` and checking `typeof result === 'string'` — but `getLayout()` always returned an `IWindowLayout` *object* (or `null`), never a string, and `'simple_alert'` was never a name anything registered under `registerLayout()` in the first place. So `SimpleAlertDialog` was **already silently broken before this session** — every call fell into its own `log.warn('Missing simple_alert XML layout'); return;` early-out and showed nothing. Fixed by adding a public `IHabboWindowManager.requireWidgetLayout(name, purpose)` (promoted from a private helper already used by `alert()`/`confirm()`/etc, matching AS3's inline `assets.getAssetByName("simple_alert_xml")` pattern) and pointing `SimpleAlertDialog` at it with the correct asset name.
  - **Minor fixes**: `IHabboWindowManager.createWidget()` was mislabeled `// TS-only`, it's a real AS3 member (`HabboWindowManagerComponent.as::createWidget()`) — trace comment corrected. `ElementRegistry.ts` doc comment updated to clarify it's a TS-only JSON index (not a `SkinContainer` port) that feeds the real, AS3-faithful `SkinContainer`.
- 🟡 **`HabboWidgetFactory` completed: `ProductIconWidget`/`ProductImageWidget` ported, plus a `createWidget()` silent-failure fix** (`habbo/window` root-file audit): the widget registry (`WidgetClasses.as` in AS3) has 26 entries; ours only had 24 — missing `product_icon`/`product_image`, the catalog/inventory product-preview widgets. Both are now ported (`ProductIconWidget.ts` small icon preview, `ProductImageWidget.ts` larger angled preview) and registered. Also found the factory's trace comment pointed at a nonexistent file (`class_3474.as` — a class-number collision with an unrelated AS3 module) and `createWidget()` silently returned `null` for an unknown widget type where AS3 throws `"Unknown widget type ..."`; now logs a warning instead of throwing (matches this session's other "loud instead of silent" fixes, without the blast radius of a hard throw).
  - **New supporting API**: `IHabboWindowManager`/`HabboWindowManager` gained `catalog`/`freeFlowChat`/`configuration` accessors (all real AS3 members, wired via the existing DI `ComponentDependency` pattern for `catalog`/`freeFlowChat`, direct field exposure for `configuration` which was already collected but never exposed). `IIconWindow` was missing `fitToSize()` (the *only* real AS3 member — its previous single field, `imageUrl`, didn't exist in AS3 and had zero callers, so it was replaced rather than kept alongside). `IBitmapWrapperWindow`/`IStaticBitmapWrapperWindow` were both missing `pivotPoint` (present on the concrete `BitmapDataController` they each inherit from/delegate to; AS3's real interface, obfuscated as `_SafeCls_1989`, exposes it plus zoom/wrap/flip/etch/greyscale properties still not surfaced on either interface). `BadgeImageWidget`/`PetImageWidget.refresh()` made `public` (were `private`; `ProductIconWidget`/`ProductImageWidget` call them externally after a blend change, matching AS3) and `BadgeImageWidget` gained a real `pivotPoint` passthrough.
  - **Deferred within `ProductImageWidget`** (`TODO(AS3)`, each naming the real gap): wall/floor-item image preview needs `IRoomEngine.getWallItemImage()`/`getFurnitureImage()` (angled 3D renders — AS3's `getFurnitureIcon()`/`getWallItemIcon()`, which we do have, turn out to just be `getFurnitureImage(...,scale=1,...)` convenience wrappers around the same missing general method); pixel-effect-on-avatar preview needs an unported `EffectPreviewer` class; the chat-style/easter-egg preview path needs `IHabboFreeFlowChat.createPreviewBitmap()` (stubbed to always return `null` in a new small `ProductCategoryMapping.createChatItemPreview()` helper — the easter-egg repeat-count tracking itself is fully real, it just can't render the reveal image yet, exactly like AS3 when its own preview creation fails). `ProductIconWidget`'s habbicon case is similarly stubbed (needs `HabbiconAssetManager`, unported).
- 🟡 **`HabboCatalogUtils` (partial) + `SimplePriceCatalogWidget` ported; `CatalogPage` now creates real widgets** (`habbo/catalog` phase 4, continued): per user direction, ported the price-display pipeline of `HabboCatalogUtils` (`catalog.utils`) for real — `createWindow()`, `showPriceInContainer()`/`renderPriceInContainer()`/`renderPriceItem()`/`createPriceContainer()`, `getPriceArray()`/`getPriceMap()`, `showPriceOnProduct()` (the method both `SimplePriceCatalogWidget` and, later, `ProductViewCatalogWidget` need), `calculateBundlePrice()`, `setCatalogItemImage()`/`displayProductIcon()`/`imageReady()`/`imageFailed()`. `SimplePriceCatalogWidget` itself is fully real. `CatalogPage.createWidget()`/`createWidgetsRecursion()`/`initializeWidgets()`/`removeWidgets()` (including the init-failure cascading-removal logic) are now genuinely wired for `itemGridWidget`/`simplePriceWidget` — a page with those two layout windows renders and behaves for real; `CatalogPage.selectOffer()` now really calls into `ItemGridCatalogWidget.select()`.
  - **win63 vs. clean-reference version split**: `HabboCatalogUtils.as` (631 lines, win63) is one of the most heavily decompiler-corrupted files found this session (`getPriceArray`/`getPriceMap` compute the right value into a local, then push a hardcoded `{amount: 0, ...}` instead of using it — same "compute correctly, return/use the wrong thing" pattern as this session's other corruption finds). Used `vortex-client`'s clean, uncorrupted `HabboCatalogUtils.as` as the primary structural reference for the ported subset, cross-checked against win63 for real newer-build additions (silver/emerald currency lines in `getPriceArray`, which the older clean reference doesn't have).
  - **New real `HabboCatalog`/`IHabboCatalog` wiring**: `utils` getter (+ `_utils` field, constructed eagerly like AS3's own `_utils`), `getSeasonalCurrencyActivityPointType()`, `displayProductIcon()` now delegates to `utils.displayProductIcon()` for real (was an empty stub).
  - **Interface-completeness fix**: `IBitmapWrapperWindow` was missing `bitmapAssetName` (AS3's `class_2251.bitmapAssetName` — the concrete `BitmapWrapperController` already implements it, same class of gap as `ITextWindow.etchingColor`/`.focus()`/`.unfocus()` found earlier this phase).
  - **Deferred within `HabboCatalogUtils`** (`TODO(AS3)`, not needed by `SimplePriceCatalogWidget`): `showVipBenefits()`/`openLink()` (needs `VipBenefitsWindow`, not ported), `showBadgeOnProduct()`/`showAssetImageAsBadgeOnProduct()`/`hideBadgeFromProduct()` (needs `IBadgeImageWidget`, not ported — these are what `ProductViewCatalogWidget` needs next), bundle-discount-ruleset math (`getDiscountItemsCount()` + helpers, needs `BundleDiscountRuleset` message data, itself unported), `resolveBundleDiscountFlatPriceSteps()`, and the 3 `HabboTracking` one-shot analytics methods. `showPriceOnProduct()`'s seasonal-currency price colour branch (`getSeasonalCurrencyPriceColor()`, needs `ColorConverter.hexToUint()` + seeded config) falls back to the default price colour instead.
  - Next: `ProductViewCatalogWidget`/`SingleViewCatalogWidget` (needs the deferred badge/chat-style display methods above, plus `RoomPreviewer` for the in-room product preview — still the most heavily corrupted widget file found) or `PurchaseCatalogWidget` (independent of `HabboCatalogUtils`, but needs `PurchaseConfirmationDialog` to actually complete a purchase) are the two remaining candidates to reach a full "browse → select → buy" flow.
- 🟡 **First catalog widget ported: `ItemGridCatalogWidget`** (`habbo/catalog` phase 4, started): the offer-grid engine — grid item creation (single/multi-price layouts), batched/timer-based icon loading (25ms tick, 3 offers/tick, matching AS3's `Timer`), selection (`select()`/`SelectProductEvent`/`SetExtraPurchaseParameterEvent`), and the `default_3x3_color_grouping` swatch-grouping logic (`onColourIndex`/`getCurrentItemColors`) are all real. Added `ICatalogWidget`/`CatalogWidget` (the shared widget base — `page`/`events`/`window`, `attachWidgetView()`), `IDragAndDropDoneReceiver`, and 4 small widget-event classes (`SelectProductEvent`, `SetExtraPurchaseParameterEvent`, `CatalogWidgetColoursEvent`, `CatalogWidgetColourIndexEvent`).
  - **Deferred within this widget** (`TODO(AS3)`, each naming the real gap): guild-recolor furniture preview (`onGuildSelected`) — needs `StringArrayStuffData.setArray()`, which this port's `StringArrayStuffData` doesn't have (it only builds from a room-object model/message wrapper, not an ad-hoc string array); drag-to-room placement (`startDragAndDrop`) — needs `HabboCatalog.requestSelectedItemToMover()` + `CatalogObjectMover`, both still unported (same deferred in-room purchase flow noted in `Offer.ts`).
  - **Widget-layer scope finding, important for planning phase 4's remaining widgets**: `SimplePriceCatalogWidget` and `ProductViewCatalogWidget`/`SingleViewCatalogWidget` (the rest of the originally-planned minimal set) both funnel through `HabboCatalogUtils` (`catalog.utils`, 631 AS3 lines, not ported — price/badge/extra-icon display helpers used across most widgets) — and `ProductViewCatalogWidget` (683 AS3 lines) is additionally one of the most heavily decompiler-corrupted files found this session (dozens of `null.` substitutions) and depends on `RoomPreviewer` (product-in-room 3D preview, not ported). `PurchaseCatalogWidget` (327 lines) turned out to be independent of `HabboCatalogUtils`/`RoomPreviewer`, but its core action, `HabboCatalog.showPurchaseConfirmation()`, needs `PurchaseConfirmationDialog` (a full offer-confirmation/gift dialog, not ported) to do anything. **Net effect**: a real end-to-end "browse → select → buy" flow needs at least one of {`HabboCatalogUtils`, `PurchaseConfirmationDialog`} ported in addition to more widgets — this is now a user-facing scope decision, not just "port more widgets."
- ✅ **`CatalogPage`/`CatalogViewer` orchestration skeleton ported** (`habbo/catalog` phase 3b, completes phase 3): `CatalogViewer` (owns the currently-shown page + its container, page-switch/refresh logic, `roomEngine`/`setLeftPaneVisibility` pass-through) and `CatalogPage` (loads a page's layout window via `windowManager.buildFromXML()`, offer/localization/mode accessors, `selectOffer`/`replaceOffers`/`updateLimitedItemsLeft`) are both real and wired to each other. **What's intentionally not real yet**: `CatalogPage`'s `createWidget()` — a ~45-case switch instantiating one widget class per layout window name (`itemGridWidget`, `productViewWidget`, `purchaseWidget`, ...) — is a `TODO(AS3)` no-op, since none of `viewer/widgets/` (52 AS3 files) is ported. A loaded page therefore renders its background/layout window but has zero interactive content until widgets start landing. `CatalogViewer.showSearchResults()` is also a stub (needs the deferred `FurnitureOffer`).
  - **New real `HabboCatalog` wiring**: `roomEngine` getter + `IID_RoomEngine` DI dependency (real AS3 member, was missing), `setLeftPaneVisibility()` (real logic, but a no-op until Phase 5's `createMainWindow()` sets `_mainWindow` — same pattern as `setImageFromAsset`'s cache-hit-only path from phase 3a).
  - **Interface-completeness fix**: `ITextFieldWindow` was missing `focus()`/`unfocus()` (AS3's `class_1898`/`IFocusWindow` — the concrete `TextFieldController` already implements them) — added `extends IFocusWindow`.
  - Next phase (phase 4, not started): a minimal `viewer/widgets/` set (`ItemGridCatalogWidget`, `SimplePriceCatalogWidget`, `ProductViewCatalogWidget`/`SingleViewCatalogWidget`, `PurchaseCatalogWidget`) sufficient to browse a category and buy one offer end-to-end, then wiring `HabboCatalog.ts`'s still-stub `loadCatalogPage`/`purchaseOffer`/`purchaseProduct`/`openCatalog*`/`getCatalogNavigator` (phase 5) to the navigation+viewer+widget pieces built in phases 1-4.
- ✅ **Catalog product/offer data model ported** (`habbo/catalog` phase 3a — the "browsable/purchasable item" layer that `CatalogPage`/`CatalogViewer` will render; those two orchestrators are the next, not-yet-started slice): `IGridItem`/`IItemGrid`/`IPageLocalization`/`IProduct`/`IProductContainer`/`ICatalogPage`/`ICatalogViewer` interfaces, `PageLocalization`, `ProductGridItem` (base grid-item view + drag/select mouse handling), `Product` (single product icon rendering across all AS3 product types), `ProductContainer`/`SingleProductContainer`/`MultiProductContainer`/`BundleProductContainer` (pricing-model-specific grid item variants), `Offer`/`IPurchasableOffer` (the top-level purchasable unit). `FurniProductContainer`/`FurnitureOffer`/`GameTokensOffer`/`CatalogObjectMover`/`ProductDisplayWrapper` were read but deliberately deferred — they depend on `catalog.roomEngine`/`sendGetProductOffer`, which belong to the in-room "buy this placed item" flow, not the base catalog-browsing path `Offer`/`ProductContainer` cover.
  - **New real (non-stub) `HabboCatalog` wiring**: `avatarRenderManager`/`sessionDataManager` getters + DI dependencies (both real AS3 members, previously missing), `getProductData()`/`getFurnitureData()` now call through to `sessionDataManager` for real (previously always-null stubs).
  - **Two more decompiler-corruption fixes** (cross-checked against `vortex-client` first, per convention): `HabboCatalog.as::getFurnitureData()` computes the right value into a local but always `return null`s instead of returning it. `ProductContainer.as::set view()`'s ninja-effect-addon check decompiles as `while(true) { ...; if(null.productType...) ...}` (infinite loop reading a literal `null`) — reconstructed as a bounded 2-item loop over `products[i]`, matching the clean reference exactly. Also found (not corruption, a genuine missing method body): `Product.as::set view()`'s multi-count branch computes `multiContainer`/`multiCounter` locals but the win63 build's copy never uses them — reconstructed from `vortex-client`, which shows `multiContainer.visible = true` / `multiCounter.text = "x" + productCount`.
  - **Real icon rendering wired for furniture/wall-item/effect/subscription/avatar-render/badge product types** (matching AS3, per explicit user direction to favor real wiring over stubs where the backing systems exist). One unavoidable architecture divergence, called out inline: AS3's avatar/badge icon paths are synchronous (`BitmapData`); the PixiJS port's equivalents (`AvatarImage.getCroppedImage()` → `Texture`, `sessionDataManager.getBadgeImage()` → `HTMLImageElement`) need the browser's async `createImageBitmap()` with no synchronous alternative, so those two paths apply the icon via a promise continuation instead of a direct return — the call shape is otherwise unchanged.
  - **`TODO(AS3)` stubs added, each naming the specific missing dependency** (per project convention — compatible signatures exist, behavior does not yet): `HabboCatalog.isDraggable()` (needs room-session owner/guild/controller-level state and Builders-Club furniture-placement status — always returns `false`, i.e. drag-and-drop off, rather than guessing); `HabboCatalog.setImageFromAsset()`'s async cache-miss fallback (`retrievePreviewAsset()`); `Product.initIcon()`'s `chat_style` case (`catalog.freeFlowChat.chatStyleLibrary` doesn't exist at all) and its wall-item room-preview special case; the `BIRE_BADGE_IMAGE_READY` live-update event for badges requested before they're cached.
  - Naming: kept AS3's literal `UPPER_SNAKE_CASE` constant/field names throughout (`Offer.PRICING_MODEL_*`, `PageLocalization.LAYOUTS_TEXT_FIELDS`, etc.) per explicit user direction — this trips the repo's `camelCase` ESLint naming rule (pre-existing tension already present in `ClubOfferProductData.ts`/`FrontPageItem.ts`), left as-is intentionally.
- ✅ **Catalog navigation module ported** (`habbo/catalog` phase 2 — category tree + tab/list navigator UI; builds on phase 1's message layer): `ICatalogNavigator`/`ICatalogNode` interfaces, `CatalogNode` (invisible/non-interactive tree node), `CatalogNodeRenderable` (the real, window-rendering node — builds its list-item window from `navigator.getItemTemplate(depth).clone()`, wires `WME_CLICK`/`WME_OVER`/`WME_OUT`), `CatalogNavigator` (owns the category tree, the tabbed/non-tabbed top-level view, search/filter, and page-open routing), `TopViewSelector` (tab bar for the tabbed variant), `RequestedPage`, and `event/CatalogEvent` + `navigation/events/{CatalogPageOpenedEvent,CatalogFurniPurchaseEvent}`. Added `HabboCatalog.useNonTabbedCatalog()` (real, config-driven — `CatalogNavigator`'s constructor needs it) and `ITextWindow.etchingColor` (existing implementation gap: `TextController` already had it, the interface didn't expose it — `CatalogNodeRenderable`'s active/inactive tab-item styling needs it through the interface).
  - **Two more decompiler-corruption bugs found and fixed** (per convention, cross-checked against `vortex-client` first): `CatalogNavigator.as::getPathToNodeWithLayout()` reads/returns a literal `null` instead of the recursive call's result (`null.length`/`null.unshift` — guaranteed `TypeError` if that branch is ever hit); `openPageById()` reads `undefined[0]` instead of the just-computed offer-node list. Both reconstructed with the obvious intended local variable. Unlike phase 1's corruption (identical logic to the clean reference, just mangled), this method itself doesn't exist at all in `vortex-client`'s older build — `win63_version` added a genuine "deep hierarchy" path-to-node feature on top of it, confirmed via the `isDeepHierarchy`/`catalog.deep.hierarchy` config flag also present in `getItemTemplate()`/`CatalogNodeRenderable.createWindow()`'s depth-based icon indent math (which itself differs materially from `vortex-client` — win63 repositions the icon itself via `icon.x`, not just its visibility — a real newer-build behavior change, ported as-is, not corruption).
  - Next phase (not started): `viewer/` (`CatalogPage`/`CatalogViewer`/`Offer`/`Product` models + a minimal widget set to actually render a page and buy something), then `HabboCatalog.ts`'s real `loadCatalogPage`/`purchaseOffer`/`purchaseProduct`/`openCatalog*`/`getCatalogNavigator` (still stubs).
- ✅ **Catalog core message layer ported** (`habbo/catalog` phase 1 of a multi-phase port — navigation/viewer/purchase UI is still not started): `GetCatalogIndexComposer`/`CatalogIndexMessageEvent`+`Parser` (category tree, header 1606/2248), `GetCatalogPageComposer`/`CatalogPageMessageEvent`+`Parser` (page contents, header 2400/1405), `PurchaseFromCatalogComposer`/`PurchaseOKMessageEvent`+`Parser` (header 2697/2558), plus the failure paths `PurchaseErrorMessageEvent`(930)/`PurchaseNotAllowedMessageEvent`(1872)/`NotEnoughBalanceMessageEvent`(3883). Numeric headers came from the real emulator (`vortex-emulator/Turbo.Revisions/Revision20260112/Headers.cs`), not guessed. New data classes: `NodeData` (recursive category tree node), `CatalogLocalizationData`, `FrontPageItem`, `PurchaseOKMessageOfferData`. `CatalogPageMessageEventParser`'s `offers` reuses the already-ported `ClubOfferData`/`ClubOfferProductData` (confirmed via `win63_version`'s own imports — `CatalogPageMessageEventParser.as` and `PurchaseOKMessageEventParser.as` both import the *same* anonymous classes, `class_2138`/`class_2104`, that `ClubGiftInfoEventParser` already uses — this is one shared offer/product shape across catalog and club-gift, not a coincidence).
  - **Decompiler corruption found and fixed** (per project convention, cross-checked against the clean `vortex-client` reference before "fixing"): `class_2971.as` (`NodeData`), `class_2409.as` (`CatalogLocalizationData`), and `CatalogPageMessageEventParser.as` all have count-prefixed read loops written as `while(0 < _loc2_) { ...; _loc3_++; }` — the loop condition checks the original count and never terminates as literally decompiled (an infinite loop if the count is nonzero). The real loop is `while(_loc3_ < _loc2_)` (counter vs. count), confirmed identical in `vortex-client`'s clean equivalents (`NodeData.as`, `CatalogLocalizationData.as`, `CatalogPageMessageParser.as`). Ported as a normal bounded `for` loop.
  - Next phases (not started): `navigation/` (`CatalogNavigator`/`CatalogNode`), `viewer/` (`CatalogPage`/`CatalogViewer`/`Offer`/`Product` models — depends on the `ItemGridController`/`ItemListController` fixes below), a minimal widget set to render a page and buy an offer, then `HabboCatalog.ts`'s real (currently stub) `loadCatalogPage`/`purchaseOffer`/`purchaseProduct`/`openCatalog*` methods.
- ✅ **Window property-application timing fixed at the architecture level** (root cause of a Club Center HC Payday/Gift section height-doubling bug, 208→416 on reopen): AS3 initializes instance fields with literal defaults *before* the constructor body runs (including before `super()`); TypeScript/JS (with `target: ES2022`) initializes a derived class's own field initializers *after* `super()` returns — never before, and no tsconfig flag changes this. `WindowContext.create()` now applies `properties`/`caption` uniformly *after* the window is fully constructed for every window class (previously attempted passing `properties` into the constructor to match AS3's literal parameter order, which crashed `DropMenuController`/`ItemGridController` — their own `set properties()` touches fields not yet initialized during `super()`). `ItemListController` now creates its internal `_container` *lazily* (`ensureContainer()`, called from `addListItem`/`addListItemAt`/`populate`, not the constructor), keeping it `null` during property application exactly as AS3's own `_container` field is during `super()` — this is what actually restores order-independence between the `scale_to_fit_items`/`resize_on_item_update` layout vars. Also fixed in `WindowParser.ts`: `itemgrid_vertical` children were being routed through `addListItemAt` instead of `addGridItemAt` (per `ItemGridIterator.as::setProperty()`), causing grid children to render as side-by-side list columns instead of stacked grid cells. See `[[project_as3_field_init_before_super]]` memory for full detail — do not "fix" this class of bug by reordering JSON layout vars or changing the TS compile target, both were tried and rejected.
- ✅ **`WIN63-202607011411-782849652` window-system diff ported (new AS3 source added to `sources/`)**: this new source tree is a heavily obfuscated later client build (flat `_SafeCls_N`/`_SafePkg_N` files for one unrelated module, but a normal `com/sulake/...` package tree for `core/window`/`habbo/window`). Diffed file-by-file against `win63_version` (filtering out anonymous `class_N`/`_SafeCls_N` renumbering noise, which is not a real change) and confirmed via the `Classes.as`/`WidgetClasses.as` type registries — previously-unused window-type slots 22/24/25/26 are now real, and `WidgetClasses` gained one entry — that exactly 10 `core/window` classes and 5 `habbo/window` items are genuinely new:
  - **New skin primitives** (`core/window/components` + matching `graphics/renderer`): `ShapeController`/`ShapeSkinRenderer` (rectangle/round_rectangle/ellipse/rhombus + inset stroke — AS3 rasterizes to `BitmapData` pixel-by-pixel; ported to native Canvas2D path fill/stroke instead), `GradientController`/`GradientSkinRenderer` (linear/radial 8-direction two-color gradient — AS3 always calls `beginGradientFill("radial", ...)` regardless of `mode`, which looks like a bug but is preserved as-is per project convention; reproduced via a clip + rotated/scaled `createRadialGradient`, matching Flash's `createGradientBox()` idiom), `StrokeController`/`StrokeSkinRenderer` (per-side top/right/bottom/left stroke bitmask + optional rounded corners), `BitmapFillController`/`BitmapFillSkinRenderer` (stretch/tile/center/cover/contain fill modes with tint/greyscale/spacing, tile mode via a cached repeating `CanvasPattern` cell). Registered as `WindowType.SHAPE_WRAPPER`(22, previously reserved/unused)/`GRADIENT`(24, new)/`STROKE`(25, new)/`BITMAP_FILL`(26, new) in `Classes.ts`, and as `'shape'`/`'gradient'`/`'stroke'`/`'bitmap_fill'` renderer types in `HabboWindowManager.createRendererForType()`.
  - **Fixed a pre-existing wrong tag-name guess**: the AS3 skin-catalog tag table (`core/window/utils/_SafeCls_2816.as`/old `class_3537.as`, both confirmed identical here) has never had a `"shape_wrapper"` entry — the real tag for type 22 has always been `"shape"`. The TS `WindowType.ts`/`WindowXmlAssetParser.ts`/`compile-window-skins.mjs`/`compile-window-layouts.mjs` tables all had an invented `'shape_wrapper'` key (confirmed unused anywhere in actual asset data); replaced with `'shape'` in all four, consistent with the numeric `WindowType.SHAPE_WRAPPER` constant name (which *is* real AS3, unlike the tag string).
  - **Other new utilities**: `graphics/renderer/HsvLayerColor` (HSV-shade color derivation, used by `ShapeController.strokeHsvShade`), `utils/SmoothScroller` (inertial/eased scroll animator, cubic-bezier with an inverse-delta duration ramp — ported the AS3 `Timer` to `setInterval`/`performance.now()`, matching `ItemListController`'s existing scroll-wheel timer convention), `utils/WindowUtils` (`disableSection()` — recursive tag-based section disabling; two AS3 anonymous-type branches — an interactive-control early-out and two container subtypes that force-blend themselves — could not be identified with confidence from the obfuscated source and are marked `TODO(AS3)` rather than guessed), `habbo/window/utils/AnimatedColor`/`AnimatedScalar` (color/scalar tweening helpers, the latter using constant-acceleration physics bounded by max velocity).
  - **`ChestItemGridOverlayWidget`** (new `habbo_window/widgets` entry, `WidgetClasses`'s only new registration: `"chest_overlay_grid"`): silver/gold/brown plaque + contents-count overlay for chest-type grid items, same shape as the existing `RarityItemGridOverlayWidget`/`LimitedItemGridOverlayWidget`. Its `color` setter is fully implemented (`IStaticBitmapWrapperWindow.assetUri` swap). Its `contentsCount` setter is `TODO(AS3)`-stubbed (state tracked, not rendered): AS3 composites `unique_item_number_glyph_*` bitmaps via a shared, still-unported helper (`habbo/window/utils/_SafeCls_4213.as`) that both `RarityItemGridOverlayWidget`/`LimitedItemGridOverlayWidget` *also* skip in the existing TS port — `IHabboWindowManager` doesn't currently expose raw asset lookup to widgets, so this matches the pre-existing sibling simplification rather than introducing a new one.
  - **Also added while porting `BitmapFillController`**: `BitmapDataController.flipX`/`flipY` — these already existed in AS3 (both old and new builds) but were missing from the TS port entirely; `BitmapFillSkinRenderer`'s stretch/tile/cover/contain math genuinely needs them.
  - No real skin/layout JSON data exercises these new types yet — `WIN63-202607011411-782849652` only ships AS3 source, not a full extracted `binaryDataXml` asset catalog, so nothing has been fabricated on the data side; the classes and registries are wired and ready for when real 2026-era skin/layout data is available.
- ✅ **ClubCenter avatar-preview pipeline fixed end-to-end, plus an `HTMLTextController` rich-text gap**:
  - `RoomPreviewerWidget.showPreview()` (the static-bitmap preview mode used by `ClubCenterView`'s membership-comparison avatar) only stored the given URL on an unused `previewImageUrl` getter — nothing ever displayed it. Ported the real AS3 `showPreview(param1:BitmapData)`: swaps out the live PixiJS room canvas (unregistering its per-frame `syncCanvasPositionBound` sync callback) and hands a pre-scaled (2x, matching AS3's `Bitmap.scaleX/scaleY = 2`) canvas to the window's `display_object_wrapper` slot instead.
  - `WindowComposite.ts` only knew how to punch a transparent `clearRect` hole for `display_object_wrapper` windows (so an external PixiJS layer could show through) — it had no path for a plain pre-rendered bitmap (`HTMLCanvasElement`/`ImageBitmap`/etc.) occupying that same slot, so `showPreview()`'s canvas would've been invisible even once wired up. Added `isDrawableImage()` to tell the two cases apart and `drawImage()` the bitmap straight into the composite when there's no separate PIXI layer to reveal.
  - `AvatarImage.ts::getCroppedImage()` rendered the full (mostly empty) offscreen canvas without cropping — AS3 tracks the union of each drawn body part's rect (`Rectangle.union()`) and crops to that bounding box at the end; ported the same via a new `unionRect()` helper.
  - `ClubCenterView.ts` read the (never-cropped) texture back as a `toDataURL()` string; switched to handing `RoomPreviewerWidget.showPreview()` a copied canvas directly (copied rather than the live texture, since `avatarImage.dispose()` runs immediately after and would invalidate it).
  - `HTMLTextController.ts`'s `set text()` was a bare passthrough (no localization-key detection, no HTML-to-plain-text reduction) — AS3's `htmlText` goes through Flash's real rich-text renderer, which this port's plain-string `TextController` can't do, so `<br>`/tags need reducing to `\n`/stripped before reaching `_text` or they'd render as literal characters. Added `convertHtmlToPlainText()` and a matching `set localization()` override.
  - `docs/CLIENT-SERVER-ARCHITECTURE.md` was rewritten from scratch: the previous draft assumed a Java/Netty "Arcturus"-style server, which doesn't match this project's actual server (Turbo Cloud — .NET/Orleans/SuperSocket/EF Core) or client (`vortex-client`, protocol `WIN63-202601121721-391685409`). Old protocol IDs/bug list discarded rather than patched; rewritten sourced directly from `vortex-emulator`/`vortex-client`.
- ✅ **Furni inventory preview panel: z-order and missing name/description fixed**:
  - `RoomPreviewerWidget`'s mini room canvas and the main room view's canvas both get parented directly onto the same shared PixiJS stage (see `RoomEngine.createRoomCanvas()`) — their relative stacking depends purely on PixiJS child-add order, not window z-order. Entering a room while the inventory preview was already open recreated the main room canvas *after* the preview's, pushing it on top and visually burying the preview wherever their screen rects overlapped. Fixed by re-asserting front-of-stage every frame in `syncCanvasPosition()` (which already runs continuously via the shared ticker).
  - `GroupItem.name`/`description` were computed once in `push()` and cached — including when empty. `FurnitureDataParser` injects the `roomItem.name.<id>`/`wallItem.name.<id>` localization keys these read from, but nothing guarantees that finishes before the first inventory items arrive and call `push()` (a race that doesn't exist in AS3, where furnidata + external texts load strictly before inventory contents). Once cached empty, name/description stayed blank forever. Now the getters retry the lookup lazily on read instead of trusting the one-time push() computation.
- ✅ **Furni inventory preview panel: fixed unconditionally-visible next/view buttons and rarity/LTD badges** — `FurniView.ts::updateActionView()` never set visibility on `viewItemButton`/`nextItemButton` (AS3: only shown for "external image" wall items — photo/moodlight frames with cyclable custom images, hidden for every other item) or on `unique_limited_item_overlay_widget`/`rarity_item_overlay_widget` (only shown for genuinely limited-edition/rare items, driven by `stuffData.uniqueSerialNumber`/`rarityLevel`). All four defaulted to the layout's base visibility (effectively always-on), so every regular furni item showed "Next"/"View" buttons plus an uninitialized rarity/LTD badge overlay (rendering as a placeholder "forbidden" icon). Ported the real AS3 conditions; the grid-thumbnail equivalent (`GroupItem.ts::updateItemImageVisual()`) already had this right, this was specific to the big preview panel.
- ✅ **Chat display fixes found via cross-reference with a clean AS3 source (`C:\Users\Ctuto\Desktop\Habbo\vortex-client`, a compilable non-decompiled project — much more reliable than `win63_version`/`win63_2023_version` for corrupted sections)**:
  - `RoomChatItem.ts` conflated `userType` and `userCategory` into a single field — `onItemMouseClick/Down/Over/Out` were passing the wrong value through, and the pet vertical-offset special-case (`userType===2 && petType===15`) always read the wrong field. Split into `_userType`/`_senderCategory` matching AS3.
  - `RoomChatWidget.resetArea()` was a no-op TODO (reasoned that AS3 gates it on a null history viewer, which we always are) — but AS3 always constructs a (possibly-disabled) history viewer, so this method is never actually skipped there; it's what shrinks the chat area back down after it grows. Implemented for real (dropping only the historyViewer-derived width/height terms, which are 0 without that widget).
  - Confirmed via the clean source that the from-scratch reconstruction of `selectItemsToMove()`/`moveItemsUp()` (both corrupted in every previously-available decompiled copy) was accurate.
  - Fixed a camera-sync bootstrap bug in `ChatWidgetHandler.updateWidgetPosition()`: AS3 deliberately initializes the reference scale to `geometry.scale - 10` (not the real value) so the scale-changed branch is guaranteed to fire once and bootstrap downstream listeners; the port used the real value, which would've suppressed that first sync.
- ✅ **Furniture move/rotate/pickup wired to the server** — `RoomEngine.modifyRoomObject()` only handled `OBJECT_PICKUP`/`OBJECT_EJECT`, and even those only removed the object from the local view without telling the server (so it wouldn't actually persist — the item would reappear on reload). `OBJECT_ROTATE_POSITIVE`/`NEGATIVE` fell through to an unimplemented no-op entirely. Added `MoveObjectMessageComposer`/`PickupObjectMessageComposer` (new, matching AS3's `RoomObjectEventHandler.as::modifyRoomObject()`) and wired rotate (cycles 90°, sends the new direction at the same tile) and pickup/eject (sends the pickup composer before the local dispose) through `container.connection`.
  - `TODO(AS3)`: rotation skips AS3's `furniture_allowed_directions` model-array validation (some furniture only rotates through a subset of the 8 compass directions) — always cycles the 4 cardinal directions here.
- ✅ **`OBJECT_MOVE` (furniture drag-to-relocate) implemented** — `RoomEngine.modifyRoomObject()`'s `OBJECT_MOVE` case previously logged "not implemented yet" and did nothing; the InfoStand furni view's "Move" button (`InfoStandFurniView.ts::onButtonClicked()` → `RWFAM_MOVE` → `InfoStandWidgetHandler.ts`) was already wired up to it, so clicking it was a silent no-op. AS3's real version is corrupted/`null.`-riddled in both `win63_version/habbo/room/class_1947.as` and `class_34.as`; ported from the clean `C:\Users\Ctuto\Desktop\Habbo\vortex-client\src\com\sulake\habbo\room\RoomObjectEventHandler.as`/`RoomEngine.as` instead (`modifyRoomObject()`, `handleObjectMove()`/`handleFurnitureMove()`, `handleRoomObjectMouseClick()`'s `OBJECT_MOVE` case, `setObjectAlphaMultiplier()`). New `RoomEngine.ts` state: `_pendingMove` (mirrors AS3's `SelectedRoomObjectData`) tracks the object plus its pre-move location/direction; starting a move sets `furniture_alpha_multiplier` to 0.5 on the model (already consumed by `FurnitureVisualization.ts`, no renderer changes needed) and cross-cancels any in-flight catalog placement (`_pendingPlacement`) and vice versa. Hovering a floor tile (`handleTileMouseEvent()`'s `ROE_MOUSE_MOVE`) snaps the semi-transparent object to that tile at that tile's own floor height (`event.tileZ` — the same value the tile cursor is positioned at); clicking a tile sends `MoveObjectMessageComposer(objectId, tileX, tileY, direction)` — reusing the same composer already used for rotate — and hands the object's final position back to the existing server-echo path (`RoomObjectMoveUpdateMessage`/`MovingObjectLogic`, already live for rotate) rather than trusting the local preview position.
  - Initial version kept the object at its *original* z while dragging instead of the hovered tile's height, so it visibly floated/sank on any room with steps (caught via live testing right after the first pass) — fixed by switching to `event.tileZ`.
  - `TODO(AS3)` scope cuts (matching the pre-existing wall-item gap in `initializeRoomObjectInsert()`): wall items (category 20) aren't supported — AS3's `handleWallItemMove()`/wall-location-string math isn't ported. Client-side `FurniStackingHeightMap`/`validateFurnitureLocation()` collision validation is skipped, and the hover preview ignores any furniture already stacked on the hovered tile (both would need the same stacking-height system — the server is authoritative and rejects illegal spots regardless). There's still no cancel/right-click binding for an in-progress move — that's shared with the unbuilt furniture-context-menu widget, same as before.
- ✅ **Two toolbar-adjacent bug fixes found via live testing**:
  - `BottomBackgroundBorder` (the toolbar's background panel) was fully implemented but never constructed anywhere (`grep` found zero call sites besides its own file/barrel export) — `HabboToolbar.as::initComponent()` constructs it alongside `BottomBarLeft`, but the TS port only ever created `BottomBarLeft`. Added construction (and disposal) in `HabboToolbar.ts`'s lazy `bottomBarLeft` accessor. This is why the toolbar's background never rendered, most visible once in a room (icons floating directly on the room canvas with no panel behind them).
  - The toolbar's RECEPTION icon (`HabboLandingView.ts::onToolbarClick()`) was a no-op: AS3's `RoomSessionManager` keys every session under one hard-coded slot (`getRoomIdentifier()` always returns `"hard_coded_room_id"`), so its `getSession(-1)`/`disposeSession(-1)` really mean "the current session, whatever it is." The TS port keys sessions by their real room id (`room_${roomId}`, to support multiple concurrent sessions — a deliberate divergence, not a bug), so `-1` never matched anything and the click silently did nothing. Fixed by using `roomEngine.activeRoomId` instead of the literal `-1`. Also restored the `QuitMessageComposer` send that AS3 does before disposing the session (missing from the TS port — meant the server was never told the client left).
- 🟡 **Third room widget ported: chat input (RWE_CHAT_INPUT_WIDGET)**: `RoomChatInputWidget`/`RoomChatInputView`/`ChatInputWidgetHandler` ported from `sources/win63_version/habbo/ui/widget/chatinput/*.as`. Core input box works: create/position (relative to toolbar/room-tools/friend-bar widths), focus, typing indicator (`RWCTM_TYPING_STATUS`), send on Enter with speak/whisper/shout mode-prefix parsing, flood control show/hide. New supporting classes: `RoomWidgetChatMessage`, `RoomWidgetChatTypingMessage`, `RoomWidgetChatSelectAvatarMessage`, `RoomWidgetRequestWidgetMessage`, `RoomWidgetChatInputContentUpdateEvent`, `RoomWidgetFloodControlEvent`, `RoomWidgetUserInfoUpdateEvent`, `RoomWidgetZoomToggleMessage`.
  - `TODO(AS3)` scope cuts (all noted in-code): the NUX first-time chat reminder animation, the room-enter "chat dimmer" overlay, and the help-button hover tooltip are not ported. `ChatInputWidgetHandler`'s ~50-command slash-command switch (`:kick`, `:mute`, `:zoom`, `:fps`, `:cam`, `:wired`, `:reload`, etc. — spanning moderation/wired-menu/room-engine-debug features that don't exist yet) is not ported; only normal speak/whisper/shout chat-sending is wired, slash commands currently pass through as literal chat text. `getFriendBarWidth()` returns 0 (friend bar isn't wired into `RoomUI`, see `habbo/friendbar` row). Custom chat styles (`ChatStyleSelector`/`freeFlowChat.chatStyleLibrary`) **are now ported** — see the `habbo/freeflowchat` row below.
  - **Superseded, not "not started"**: the old chat *display* widget (`RWE_CHAT_WIDGET` → `RoomChatWidget`/`RoomChatItem`/`RoomChatHistoryPulldown`/`RoomChatHistoryViewer`/`ChatBubbleFactory`, ~2900 AS3 lines) is deliberately disabled — `ChatWidgetHandler.processEvent()` early-returns when `container.freeFlowChat` is set, matching primary/2026 source (which removed the old system's user-preference toggle and dropped `RoomWidgetEnum.RWE_CHAT_WIDGET` entirely). The live speech-bubble renderer is now `habbo/freeflowchat` — see that row.
- ✅ **Bottom toolbar: wired 6 dead extensions + tagged remaining gaps**: `HabboToolbar.as::onPerksUpdated()` calls 8 extension-init methods; the TS port only called 2 (`initPurseAreaExtension`, `initSeasonalCurrencyExtension`) — the other 6 extension classes (`SettingsExtension`, `ClubDiscountPromoExtension`, `CitizenshipVipDiscountPromoExtension`, `CitizenshipVipQuestsPromoExtension`, `VideoOfferExtension`, `OfferExtension`) were fully implemented but never constructed. Added the matching `initXExtension()` methods (with their exact AS3 config-flag gates) plus disposal, so all 8 now run. Also added explicit `TODO(AS3)` markers (previously silent/untagged, violating the project's traceability rule) for: the friends list gap in `HabboFriendBar.ts` (see `habbo/friendbar` row above), and the toolbar icons with no click subscriber yet, in `HabboToolbar.ts::toggleWindowVisibility()`.
- 🟡 **First room widget ported: infostand (furni-only)**: `IRoomWidget`/`RoomWidgetBase` foundation, `InfoStandWidget`/`InfoStandFurniView`/`InfoStandFurniData`/`InfoStandCrackableFurniView` fully ported (click furni → info panel; move/rotate/pickup/eject/use/wired-inspect all work end-to-end through `RoomEngine.modifyRoomObject()`/`useRoomObjectInActiveRoom()`), `InfoStandWidgetHandler` scoped to furni message/event cases. `RoomDesktop.createWidget()`/`processWidgetMessage()`/`roomObjectEventHandler()` are wired for real (previously stubs); `RoomEngine` gained click-to-select (`REOE_OBJECT_SELECTED`/`DESELECTED`), `modifyRoomObject()`, `useRoomObjectInActiveRoom()`. User/Pet/Bot/RentableBot/Jukebox/SongDisk sub-views are inert `TODO(AS3)` stubs — next widget-port session should either flesh these out or pick the next `RWE_*` type from `RoomWidgetFactory.as`. Remaining furni-only gap: the Builder's Club "place more from infostand" flow (`InfoStandWidget.as::onRoomObjectPlaced()`, needs a `BuildersClubPlaceRoomItemMessageComposer`/`BuildersClubPlaceWallItemMessageComposer` pair plus a `RWROUE_OBJECT_PLACED` widget-event bridge off the existing `REOE_PLACED` room-engine event) is still `TODO(AS3)` — deliberately deferred since it touches the placement pipeline, not just the info panel.
- ✅ **Infostand furni thumbnail now renders the live placed object**: `InfoStandWidgetHandler.requestFurniImage()` was using the catalog-type icon as a stand-in, with a comment claiming `RoomEngine.getRoomObjectImage()`'s underlying pipeline (`RoomPreviewer.getRoomObjectImage()`) was still an unimplemented stub — that stub was actually already wired for real in an earlier Phase 5 pass, making the comment stale. Switched to `RoomEngine.getRoomObjectImage(roomId, furniId, category, ...)` (reads the object's own current color/extras/state), replicating AS3's scale-64-then-fallback-to-scale-1 retry when the render is null or exceeds the panel's 140x200 slot.
  - Found and fixed the actual blocker while wiring this up: `RoomEngine.getGenericRoomObjectImage()`'s "content already available" branch reset `result.id` to `0` before asynchronously calling `listener.imageReady(objectId, ...)` — so the id returned to the caller never matched the id the async callback actually delivered, and the callback was silently dropped whenever the object's content was already loaded (the common case for anything already visible in the room). Removed the reset so `result.id` and the delivered id always agree; this also fixes the same latent bug in `ProductViewCatalogWidget`/`ClubGiftWidget`'s catalog preview images, which shared the exact same correlation pattern.
  - That fix still left the thumbnail permanently blank: AS3's `handleGetFurniInfoMessage()` calls `getRoomObjectImage(..., listener=null)` — a synchronous one-shot read that bypasses `isRoomObjectContentAvailable()`'s pending-listener gate entirely, safe because AS3 always has a real BitmapData the instant it's called (the object is already rendering live). TS can't do a truly synchronous ImageBitmap capture, so it must pass a real listener — which then hit the pending gate (furniture never satisfies `isRoomObjectContentAvailable()`, that check is 'user'/snowball-only) and waited on a future `contentLoaded` event that, for content loaded long ago, never fires again. Added a TS-only `forceImmediate` param to `getGenericRoomObjectImage()`; `getRoomObjectImage()` passes it whenever it found a live object with a loaded model (by construction, always true for something already rendered on screen), skipping the pending branch and delivering via the async `imageReady()` path immediately instead.
- ✅ **Infostand USE button was permanently unreachable**: found via live console tracing (temporary debug logging in `WindowRendererItem`/`InfoStandFurniView`, removed after diagnosis) after the config flag (`infostand.use.button.enabled`) and controller rights were confirmed correct and the button still never appeared. `RoomEngine.addRoomObjectFurniture()`/`addRoomObjectWallItem()` received `usagePolicy`/`expiryTime` as parameters but never wrote them onto the room object's model (`RoomObjectModel.getNumber()` returns `NaN` for an unset key) — so `InfoStandWidgetHandler`'s `event.usagePolicy` was always `NaN`, and `NaN === 1`/`NaN === 2` are never true regardless of config or rights. Also silently dropped: `addObjectWallItem()`'s `usagePolicy`/`secondsToExpiration` parameters, never forwarded to `addRoomObjectWallItem()` at all (which didn't even accept them). Added the missing `model.setNumber(FURNITURE_USAGE_POLICY/FURNITURE_EXPIRY_TIME/FURNITURE_EXPIRY_TIMESTAMP, ...)` calls to both floor and wall item creation, matching `_SafeCls_90.as`'s `addObjectFurnitureFromData()`/`addObjectWallItemFromData()` exactly — this also fixes rented-item expiration display and the extend/buyout button visibility, which read the same two previously-never-set keys.
- ✅ **Infostand `info_border` was using the wrong border skin (style 2, blank, instead of style 1, dark)**: the currently-compiled `furni_view.xml` (`937__SafeCls_629.xml`, uid `680D3126-...`, present identically in both the primary WIN63-2026 and win63_2023 dumps) declares `info_border` as `style="2"` ("white borderless" — genuinely blank per this port's skin renderer, verified pixel-for-pixel against the real Flash-extracted `habbo_blue_skin.png` atlas). A third source tree (`WIN63-202601121721-391685409`) contains two alternate `furni_view.xml` revisions (different uids, structurally identical to — and a superset of — the currently-ported one, same element names throughout) that both use `style="1"` ("black borderless", a real verified dark-gray `61,61,61` skin in the same atlas) instead. Patched the compiled `furni_view.json`'s `info_border.style` from `"2"` to `"1"` directly, since `sources/` is a read-only reference archive with no override mechanism — documented here since JSON can't carry inline comments. If a future source re-sync regenerates this file from `937__SafeCls_629.xml`, the style will need to be corrected again until that specific source file's `style` value is fixed upstream.
- ✅ **Infostand widget visual bug-fix pass (position/font/cropping)**: found via iterative live testing after the initial infostand port.
  - `RoomDesktopLayoutManager.getWidgetContainer()` matched widget-type strings case-sensitively (`'RWE_INFOSTAND'.indexOf('infostand')` never matched), silently misrouting the widget's window to the wrong/no container.
  - `RoomDesktopLayoutManager.setLayout()`/`addWidgetWindow()`/`getWidgetContainer()` were re-ported from the clean `win63_2023_version/com/sulake/habbo/ui/DesktopLayoutManager.as` (the primary `win63_version/habbo/ui/class_3019.as` has decompiler corruption here: a `null.`-ref infinite loop and a dropped final `return`) — now uses AS3's real tag-based `getWidgetContainer()` (reads the widget window's own `.tags`, not its type-string name) and wires the real `trimContainer()` (`WE_CHILD_RESIZED` → shrink-wrap a single-child slot to that child's size) instead of an invented one-off reposition hack that was tried and reverted.
  - Custom webfonts (`Volter (Goldfish)`, `Ubuntu*`) were bundled but never registered with the browser — `App.ts` now loads them via the `FontFace` API before first render.
  - `ctx.font` strings built with an unquoted multi-word family name (`9px Volter (Goldfish), Ubuntu, ...`) are invalid Canvas 2D syntax and silently drop the whole font declaration with no console error; centralized quoting now lives in `core/window/utils/CanvasFontString.ts::quoteFontFamilyList()`, used by `WindowComposite`, `TextController`, and `TextLabelController`.
  - `TextController`/`TextLabelController` line-height was a flat `fontSize`-based guess (AS3 used Flash's real measured `TextField.textHeight`); replaced with `CanvasFontString.ts::measureFontLineHeight()` using `TextMetrics.fontBoundingBoxAscent/Descent` (the font's fixed design metrics — not `actualBoundingBox*`, which is per-glyph ink and was tried/reverted for being inconsistent across caption strings). Fixes descenders getting clipped (e.g. "My World" rendering as "Mv World").
  - JSON layouts commonly declare text margins as one nested `vars.margins: {left,top,right,bottom}` var (serialized to XML as a `Map`), but `TextController`/`TextLabelController`'s property-application only recognized 4 flat `margin_left`/`margin_top`/`margin_right`/`margin_bottom` keys — the nested form was silently dropped, so every such label rendered with 0 margins (buttons shrink-wrapped via AS3's real `ButtonController` `EXPAND_TO_ACCOMMODATE_CHILDREN`/`WE_CHILD_RESIZED` → `width = 0` behavior ended up tight enough to look cropped/too-narrow). Both controllers now handle a `case 'margins'`.
- 🟡 **Second room widget ported: room-tools (RWE_ROOM_TOOLS)**: `RoomToolsWidget`/`RoomToolsCtrlBase`/`RoomToolsToolbarCtrl`/`RoomToolsInfoCtrl`/`RoomToolsHistory` fully ported from `sources/win63_version/habbo/ui/widget/roomtools/*.as` (no decompiler corruption found there), `RoomToolsWidgetHandler` fully ported. Settings/room-info popup, collapse/expand slide animation, camera button dispatch (`HabboToolbarEvent`), like/rate, share-room popup + clipboard, room-history back/forward navigation, and tag-click-to-search all work. `RoomWidgetFactory`/`RoomDesktop.createWidget()` wired for `'RWE_ROOM_TOOLS'` matching `RoomDesktop.as:885-890` exactly (constructs the handler, sets `.communicationManager`/`.navigator`). Added `IID_HabboNavigator`/`IID_HabboCommunicationManager` DI wiring to `RoomUI`/`RoomDesktop` (previously registered symbols, never consumed here); added `RoomUI.desktop` (AS3's `var_22` is a single field, the TS port already keyed multiple desktops by room identifier in a `Map` for the underlying multi-session architecture — this exposes "the most recently created desktop" as the AS3-equivalent view) and `IRoomDesktop.getWidget()` (was implemented on the concrete class but missing from the interface). Also created the missing `RoomWidgetZoomToggleMessage` (referenced by infostand-era `getWidgetMessages()` docs but never actually added).
  - One remaining scope cut, logged in-code as `TODO(AS3)`: the `room_tools_history_item` layout asset is not bundled, so the visited-rooms **dropdown list** doesn't render (back/forward navigation buttons work fully, they only need the in-memory visited-rooms array). `freeFlowChat` **is now wired into `RoomUI`/`RoomDesktop`** (see the `habbo/freeflowchat` row) — the chat-history *toggle button* itself is still a no-op, since the drag-down history tray it would open (`ChatHistoryTray`/`ChatHistoryScrollView`) is a separate, explicitly-deferred feature.
  - AS3's cross-room widget-instance reuse (`RoomUI.as`'s `var_4627`/`var_1358`: `RWE_ROOM_TOOLS` and 7 other widget types are reconstructed-or-reused via `widget.reuse(newDesktop)` instead of always reconstructing) is not ported — `RoomDesktop.createWidget()` now sets the `widget.reusable` flag correctly per type (previously hardcoded `false` for everything, which happened to also affect infostand — that flag is currently unread/inert either way) but the caller-side instance cache in `RoomUI` doesn't exist yet, so every widget is still freshly constructed each room-enter.
- ✅ **core/window dropdown compact text follow-up**: `WindowComposite` treats `_DROPLIST_TITLETEXT` and dropdown item `_BTN_TEXT` as compact Flash TextField content, with AS3-style metrics/clipping and baseline offset.
- ✅ **Navigator dropdown/create-room input follow-up**: top-level navigator tabs select the AS3 matching search index, dropmenu title text uses compact Flash margins, create-room thumbnails use AS3 interpolated image URIs plus tile label colors, and `TextFieldManager` refocuses the browser input after placeholder clearing.
- ✅ **Navigator create-room/search/dropdown tabs AS3 parity pass**: `RoomCreateViewCtrl` follows WIN63 prepare/refresh/thumbnail/dropdown/create composer flow; navigator tabs avoid converted Flash `y=-1` truncation; search input border clicks focus the text field.
- ✅ **Navigator room usercount label AS3 follow-up**: `RoomEntryElementFactory.updateCommonEntryElements()` keeps the compact `room_usercount` label on the Flash visual baseline after caption updates.
- ✅ **core/window TextField vertical AS3 parity fix**: `WindowComposite.compositeText()` applies the Flash `TextField` top gutter so purse currency numbers and other glyphs no longer render too high.
- ✅ **Window ResourceManager pending asset wake-up**: `registerAssetUrl()` starts lazy loading for receivers that requested a static bitmap before the bundled image URL was registered.
- ✅ **Configuration/localization/gamedata parity passes**: AS3 `external_texts`, `external_variables`, `furnidata`, `productdata`, avatar figuremap, and room content loading behavior were realigned over recent batches.
- 🐛 **Eight AS3-divergence bugs found via targeted audit and fixed**: `RoomPlaneParser.ts` (`hasRightEdge` never recomputed after the height-scan loop, so it reflected the pre-shrink rect width — same bug class as the previously-fixed `hasTopEdge` polarity inversion); `RoomPlane.ts::getTexture()` (dropped the `time` argument to the rasterizer, permanently freezing `LandscapeRasterizer`'s animation timestamp so animated planes re-rasterized every frame instead of every 500ms); `RoomPreviewerWidget.ts` (the `scale`/`offsetX`/`offsetY`/`zoom` setters never called `refresh()`, so changing zoom/pan on the furniture/pet preview did nothing; `onClickRoomView()` was an empty stub instead of forwarding to `changeRoomObjectState()`); `RoomSettingsCtrl.ts` (`parseInt(...) || 14` treated a legitimately-typed `0` for chat hearing range as falsy and silently overrode it to 14); `PurseClubArea.ts` (computed `clubLevel` inline from `sessionDataManager` with `isVIP` checked *before* `hasClub`, inverting AS3's priority — a VIP flag with 0 club days/periods showed the VIP icon instead of the "no club" state; fixed by porting the real `HabboInventory.clubLevel` getter, which didn't exist in the TS port at all, and pointing this class at it); `FurniView.ts` (invalid/reformatted trade quantity input reset the count to a literal `0` and submitted a 0-item trade offer instead of clamping to `Math.max(1, ...)` like the AS3 source); `MeMenuSoundSettingsSlider.ts` and `RoomCreateViewCtrl.ts::centerContent()` (both used `Math.round`/kept fractional pixel offsets where AS3's `int`-typed setters truncate toward zero, not round).
- 🐛 **Known gap, not yet fixed**: `IlluminaBorderWidget.ts::refresh()` is an empty stub (`// TODO: Border drawing stub`) and none of its 12 property setters (padding, side padding, child margin, 6 child slots, landing view mode, border style) call it. AS3's `refresh()` does the entire 9-slice border draw plus content/side-padding and top/bottom child positioning math (`sources/win63_version/.../IlluminaBorderWidget.as::refresh()` — note: the win63 copy is decompiler-corrupted here, `switch(0 % 3)` and a dropped top/bottom branch; use `sources/PRODUCTION-201601012205-226667486/.../IlluminaBorderWidget.as` as the clean reference). This widget's entire layout system is currently inert.
- 🐛 **Second audit batch: 5 more AS3-divergence bugs found and fixed, `habbo/session` + `habbo/communication` (encryption/login/parser) audited**: `RoomSession.ts::set roomControllerLevel()` silently ignored an out-of-range value instead of resetting to `ROOM_CONTROL_LEVEL_NONE` (0) like AS3 — an invalid/corrupt controller-level from the server left the user at their previous elevated permission level instead of being demoted; `FloorHeightMapMessageParser.ts` was missing the `cameraInitX`/`cameraInitY`/`cameraInitZ` wire fields entirely (AS3 reads them right after `areaHideData`; zero references anywhere in the engine before this fix); `HeightMapMessageParser.ts` computed `_height = total / this._width` without truncation, diverging from AS3's `int`-typed assignment for malformed/non-exact-multiple payloads; `HabboLocalizationManager.ts` had three issues — `getExternalVariablesUrl()`/`getExternalVariablesHash()` read dead fields that were never assigned anywhere (now delegate to `getGameDataResources()`), `requestLocalizationInit()` had an invented `else` branch calling `loadExternalTexts()` that matches neither win63 nor PRODUCTION-201601012205-226667486 AS3 (confirmed `gamedata.hashes.url` is always set via `common_configuration_txt.txt`, so the branch was dead in practice — removed), and a failed load only logged an error instead of calling `Core.crash()` like AS3, so anything awaiting `'complete'` would hang silently instead of surfacing a fatal error. Also wired the previously-inert `flags:uint` constructor parameter (`HabboConfigurationFlags.SKIP_EXTERNAL_VARIABLES`/`SKIP_LOCALIZATIONS`) through `Component` → `CoreLocalizationManager` → `HabboLocalizationManager`/`HabboConfigurationManager`, including `HabboConfigurationManager.resetAll()`'s previously-missing final branch (immediate unlock+complete vs. eager `initConfigurationDownload()`) — behaviorally inert today since nothing passes non-default flags yet, but now matches the full AS3 member signature. `HabboConfigurationManager.as::parseArguments()` (flashvars/query-string config overrides from `(context as ICore).arguments`, `_`→`.` key normalization, then `clearArguments()`) was unported entirely — now implemented and wired into `resetAll()` at the correct call site; behaviorally inert today since nothing currently populates `ICore.arguments`, but no longer a silent gap. `communication/encryption` (ArcFour, DiffieHellman) and `communication/login` audited clean — no divergence (note: win63's `ArcFour.as` KSA swap is itself decompiler-corrupted, `sbox[j]=0` instead of completing the swap; the TS port already has the correct swap, don't "fix" it to match win63). `communication/messages/parser` loop-bearing files (79 of 339) partially audited, most of `communication/messages/incoming`/`outgoing` (600+ simple field composers) and 58 of the 79 loop-bearing parsers not yet covered — candidates for a follow-up pass.
- 🐛 **Third audit batch: 10 more AS3-divergence bugs found and fixed in `habbo/communication/messages/{incoming,outgoing}/{room,inventory}`** (targeted audit of the actual wire-format composers/parsers, not just the higher-level model/UI code):
  - `RoomDimmerChangeStateComposer.ts`/`RoomDimmerGetPresetsComposer.ts` sent an empty array — AS3 both take a single `objectId:int` (the dimmer furni's item id, confirmed via `FurnitureDimmerWidgetHandler.as` call sites) and send `[objectId]`. `RoomDimmerSavePresetComposer.ts` was missing 2 of AS3's 7 wire fields (a hardcoded `false` and the `objectId`). All three now take the id and match AS3's array shape; wired through `RoomSession.ts`'s previously-unused `_itemId` params. Zero live impact today (the dimmer UI widget itself isn't ported yet), but was blocking that port from ever working.
  - `MountPetComposer.ts`/`DismountPetComposer.ts` — AS3 has exactly ONE composer, `MountPetMessageComposer(petId, mount:Boolean)`; the TS port invented a split into two composers that both dropped the boolean entirely, relying on an assumption ("server toggles state") not in AS3. Merged back into one `MountPetComposer(petId, mount)`, deleted the invented `DismountPetComposer`.
  - `BanUserWithDurationMessageComposer.ts` — wrong field order (`[userId, banType, roomId]` vs AS3's `[userId, roomId, banType]`) **and** wrong type (`banType` retyped as a number with an invented 1/2/0 mapping; AS3 sends the literal ban-duration string). `RoomSession.banUserWithDuration()`'s hand-rolled string→number switch was removed; the AS3 string constant now flows straight through.
  - `TradingItemListMessageParser.ts` reused `FurniListItemParser` (the regular inventory item format) to parse trade-offer items — AS3 uses a completely different class (`class_3066`: adds `isGroupable` before `stuffData`, replaces the tradeable/sellable/rent-period flags with `creationDay`/`creationMonth`/`creationYear`). Created `TradingFurniItemParser.ts` matching `class_3066` field-for-field; every traded item was being misparsed from the first field onward.
  - `PetInventoryMessageParser.ts` was densely wrong: read `breedId`'s wire slot as a loop count, undercounted the custom-parts loop by 3x (AS3 reads 3 ints per custom part, TS read 1), invented two fields that don't exist on the wire (`hasCustomLayerPart`, `petRarity`), and hardcoded `level: 1` with a comment claiming "level comes in a separate message" — false, AS3 reads it as the very next field. Rewritten to match `class_2838`/`class_2486` exactly; the parser cursor was desynced from the first pet entry onward.
  - `BadgesMessageParser.ts` (inventory) never read `totalFragments`/`fragmentNo`, and split parsing into two invented phases (a badge-id-only loop, then a separate slotId+badgeId loop) instead of AS3's single `count` + `(slotId, badgeId)` loop. Rewritten to match `BadgesEventParser.as`; cross-checked against the already-correct `HabboUserBadgesMessageParser.ts` (users module), which has the right shape and confirmed this was a regression specific to the inventory-badges file.
  - `FurniListAddOrUpdateMessageParser.ts` — **live bug**: parsed exactly one item with a comment claiming "AS3 reads 1 item, but message may contain more" (backwards — AS3 reads a `count` then loops `count` times). `HabboInventory.onFurniListAddOrUpdate()` already correctly loops over `parser.items`, so this was silently dropping/corrupting every item after the first whenever the server batched more than one add/update in a single message.
  - `FurniListRemoveMultipleEventParser.as` (bulk furni removal, message ID 149) had no TS port at all — zero references anywhere in the engine. Fully ported: `FurniListRemoveMultipleMessageParser.ts`, `FurniListRemoveMultipleMessageEvent.ts`, registered in `HabboMessages.ts`, and wired into `HabboInventory.ts::onFurniListRemoveMultiple()` calling the new `FurniModel.removeFurnis()` (added to `FurniModel`/`IFurniModel`, matching AS3's `removeFurnis()`).
  - Audited clean, no divergence: `communication/messages/{incoming,outgoing}/navigator` + `newnavigator` (96 files — one dead-code constructor overload noted, not a bug), all `incoming/room` wrapper files (47), `outgoing/room/action`/`avatar`/`pet`(except Mount/Dismount)/`settings`/most of `furniture`(except the 3 RoomDimmer bugs), `outgoing/room/engine` (MoveAvatar/MoveObject/PlaceObject/PickupObject/GetFurnitureAliases).
  - Not yet covered (follow-up candidates): `outgoing/room/chat`/`session` (8 files), most of `incoming`/`outgoing` `inventory`/`users` beyond the item-list parsers above (~90 files, `BlockListMessageParser`/`IgnoredUsersMessageParser` flagged as the likely next risk), and `help`/`notifications`/`friendlist`/`moderation`/`quest`/`handshake`/`catalog`/etc. incoming/outgoing directories (untouched this pass).
- ✅ **ClubCenter ported** (`habbo/catalog/clubcenter/`): `HabboClubCenter` (manager, `ILinkEventTracker` on `habboUI/` — this is what makes `HabboCatalog.openClubCenter()`'s pre-existing `habboUI/open/hccenter` link actually do something), `ClubCenterView` (status/badge/gift-count popup), `ClubSpecialInfoBubbleView` (payday breakdown callout), `util/ClubStatus.ts`, `util/BadgeResolver.ts`. New message infra it needed: `ClubGiftInfoEvent`/`ClubGiftInfoEventParser` (+ `ClubOfferData`/`ClubOfferProductData`/`ClubGiftEligibilityData`) and `GetClubGiftMessageComposer`, IDs 3771/1604 — none of this existed before. Wired into `VortexMain.ts::prepareCore()` under `IID_HabboClubCenter` (the IID stub already existed, typed `unknown`, now typed properly).
  - Two intentional adaptations, not gaps: (1) `hc_badge` in `club_center.json` was switched from a raw `bitmap` tag (AS3's approach: `SessionDataManager.requestBadgeImage()` + a `BIRE_BADGE_IMAGE_READY` retry event) to `widget_type: badge_image`, reusing the already-working `BadgeImageWidget` instead of re-deriving that retry dance. (2) `IRoomPreviewerWidget.showPreview()` was never actually implemented before this (stored a URL, rendered nothing, zero callers) — this port is its first real caller; the avatar preview Texture is converted to a data URL via a plain 2D canvas (`AvatarImage.getCroppedImage()` is backed directly by an `OffscreenCanvas`, no PixiJS renderer/GPU readback needed).
  - Known gap, tagged `TODO(AS3)` in `HabboClubCenter.ts::initComponent()`: the "earn credits via video" offer button (`btn_earn`) has no real backing system — `IOfferCenter`/`IOfferExtension` don't exist anywhere (`catalog/offers/` is empty) and `IHabboCatalog.getOfferCenter()`/`getHabboClubOffers()` are stubs. AS3 itself gates this button behind `offers.enabled`+`offers.habboclub.enabled` config flags, so it stays hidden exactly as it would with those flags off — not an invented workaround, just inert until the offer-center system is ported.
- ✅ **`habbo/freeflowchat` is now the live, wired-in chat-bubble renderer** (supersedes the "standalone, unwired" state described in earlier snapshots) — ported across 5 phases per a scoped "Plan 1: foundations" (full asset pipeline + bubble rendering + style selector + simple non-colliding placement), followed by a "Plan 2" pass (real `ChatFlowStage` gravity/collision physics + avatar face-crop) once Plan 1 was verified live against a real server:
  - **Asset pipeline** (`tools/import-chatstyles.mjs`, new): hash-preserving extraction of the 89-style `chatstyles_xml` catalog plus each style's `regpoints`/bitmap assets (background, pointer, emblem, emblem-multiline, icon, selector-preview, color) from `WIN63-202607011411-782849652`. `import-crypted-images.mjs` couldn't be reused as-is — its `resolveRawFileName()` strips the `$<hash>` suffix off embed names, which would've aliased all 89 different `style_<id>_chat_bubble_base` embeds onto a single raw file; the new tool resolves by the full hash-bearing value instead. Runtime glue: `App.ts::registerChatStyleImageAssets()` registers each PNG as a raw `ImageBitmap` (not the standard `image/png`→`BitmapDataAsset`/PixiJS-`Texture` pipeline — `ChatStyle.getNewBackgroundSprite()` needs a real `ImageBitmap` for `OffscreenCanvas.drawImage()`), while `VortexMain.ts::registerChatStyleTextAssets()` registers the XML catalog and `regpoints` text synchronously during `prepareCore()`.
  - **`ChatStyleLibrary`**: TS-only divergence from AS3 (documented in-code) — this port's per-style bitmaps register into the asset library *asynchronously* (after `Vortex.bootstrap()` resolves), unlike AS3's synchronously-embedded assets, so styles are built lazily on first `getStyle()`/`getStyleIds()` access instead of eagerly in the constructor (an eager-build attempt during development raced the async image registration and silently built every style with an `undefined` background — caught during manual verification, see the bug list below).
  - **`ChatBubble`/`PooledChatBubble`** (`viewer/visualization/`): full AS3 rendering ported — 9-slice tinted background, pointer bitmap tracked to the speaker's screen X, emblem, HTML-markup-aware text (`ChatTextLayout.ts`'s run-based canvas layout, since this port has no PixiJS `HTMLText` equivalent anywhere in the codebase; `ChatMarkup.ts` ports the AS3 bracket-tag markup), overflow clip mask, 150ms linear move tween. `ChatBubble` wraps an internal `PooledChatBubble` instance rather than duplicating AS3's near-identical composition logic across both classes.
  - **`ChatFlowViewer`/`ChatFlowStage`/`ChatViewController`** (`viewer/`, `viewer/simulation/`): initially shipped as a simple, non-colliding placeholder (bubbles spawn anchored above the speaker and scroll upward at a constant rate, no collision avoidance) — since replaced by the real AS3 physics engine, see the "Plan 2" bullet below.
  - **`ChatStyleSelector`** (`habbo/ui/widget/chatinput/styleselector/`, new directory): grid + font-size popup wired into `RoomChatInputView`'s "styles" button, with id-range/`isHcOnly`/`isAmbassadorOnly`/`isStaffOverrideable`/purchasable/NFT-ownership filtering. Two deliberate deviations from upstream AS3 bugs (flagged in-code): `_selected`/`_styleRequiresUpdate` are per-instance instead of AS3's class-`static` fields, and `dispose()` fully clears `_entries` instead of AS3's `while(length>1)pop()` (which leaves one stray entry in every source tree checked).
  - **`RoomUI`/`RoomDesktop`/`ChatWidgetHandler`**: `freeFlowChat.displayObject` now mounts into the room's chat container (`RoomDesktopLayoutManager.getChatContainer()`, retyped from `IWindowContainer` to `IDisplayObjectWrapper` to match the actual layout element), and the old `RoomChatWidget` path is disabled via an `if(container.freeFlowChat) return;` guard in `ChatWidgetHandler`, matching primary/2026 source exactly (no user-preference toggle — that was flash/win63-only and was dropped there).
  - **Thirteen runtime-only bugs found and fixed** (four during manual verification, nine more reported by the user across several follow-up rounds of live testing against a real server — all compiled and linted clean; all broke silently at runtime, none caught by `tsc`):
    1. `VortexMain.ts::prepareCore()` constructed `HabboFreeFlowChat` without passing the asset library (`new HabboFreeFlowChat(ctx)` — every sibling component, e.g. `RoomEngine`/`HabboCatalog`/`RoomUI`, passes `this._assets` as a constructor arg; this one didn't), so `chatFlow.assets` was always `null` and `ChatBubbleFactory` never built a `ChatStyleLibrary` at all.
    2. `IID_HabboFreeFlowChat` was never registered — `ctx.attachComponent(this._freeFlowChat, [])` used an empty interface array, so the `ComponentDependency(IID_HabboFreeFlowChat, ...)` in `RoomUI`/`RoomSessionManager`/`HabboWindowManager` never resolved and `RoomUI._freeFlowChat` stayed `null` forever, silently breaking all of the wiring above despite a clean build.
    3. `PooledChatBubble.update()` only set `visible = true` inside the branch that fires when a `moveTo()`-driven tween completes; AS3's real condition (`PooledChatBubble.as::update()`) is unconditional — `visible` flips true once total elapsed time since `recreate()` exceeds 150ms, regardless of movement state. Since `ChatFlowViewer.insertBubble()` always spawns via `warpTo()` (Plan 1 has no approach animation), which never sets the tween-start marker the port's version depended on, every bubble was permanently invisible. Fixed to match AS3's unconditional elapsed-time check.
    4. `ChatTextLayout.ts::layoutChatText()` (pure text-layout bug, not wiring): consecutive words within the same styled run rendered with no space between them (`"Hello world"` → `"Helloworld"`) — the word-wrap loop measured a whitespace segment's width for line-break positioning but never wrote the space character into any placed run's `text`, and the same-run merge path (`previous.text + word`) then joined the two words directly. Fixed by appending the space onto the preceding placed run before the next word can merge into it.
    5. **`RoomChatInputView.ts::createWindow()` broke chat typing entirely on a real server** (`custom.chat.styles.enabled`-gated code path the headless-boot check never reached, since it needs a live room, not just the login screen). The port called `createOrUpdateChatStylesView()` *before* attaching `chat_input`'s own `WindowMouseEvent.DOWN`/`WindowKeyboardEvent.KEY_DOWN`/`WE_CHANGE` listeners; real AS3 (`RoomChatInputView.as::createWindow()`, lines 300-345) does the opposite — `_window.procedure` is set immediately after building the window, the input's own listeners are attached next, and `createOrUpdateChatStylesView()` runs dead last. The style-selector construction (see bug 6) was throwing synchronously and aborting `createWindow()` *before* the input listeners got attached, so no keystroke ever reached the text field. Reordered to match AS3 exactly; typing no longer depends on the chat-style feature succeeding.
    6. **`ItemGridController`'s `resize_on_item_update` auto-grow fights `ChatStyleSelector.gridColumns`'s width cap, so the style grid never wraps** — reported by the user as "it's a grid but renders as one giant horizontal strip" (and, per bug 7, contributed to that strip becoming wide enough to interfere with normal chat input, though it turned out not to be the actual cause of that). Traced with a stack-trace-logging harness driven live against the actual running app (no server needed for this one — reproduced by constructing the same `styleselector_menu_new_xml`/`chatinput_chatstyle_template_xml` windows the real code uses): `itemgrid`'s layout node carries `resize_on_item_update="true"` (genuine AS3 layout data). Every `addGridItem()` call that creates a new column runs `ItemListController.addListItem()` on the grid's own outer list, which grows the grid's internal `_container` by one cell-width; because `_container` has AS3's `EXPAND_TO_ACCOMMODATE_CHILDREN`-equivalent param bit (`0x400000`) armed, that growth immediately propagates back up to the grid's own `.width` (`WindowController.ts`'s `WE_RESIZED` handler: `this._parent.width = this._parent.width + (this._width - this._previousRect.width)`). So `ItemGridController.resolveColumnForNextItem()`'s wrap decision (`lastCol.right + item.width <= this._width`) was always comparing against a width that had *just* grown to fit one more column — never failing, so it never wrapped to a new row; confirmed by a stack trace showing the grid's own `setRectangle` being invoked recursively via `_container`'s resize propagation. Fixed by also pinning `grid.limits.maxWidth` to the same value `gridColumns` computes (`ChatStyleSelector.ts`) — the pre-existing `IWindow.limits`/`IRectLimiter` rect-clamp (`setRectangle()` already clamps against `_rectLimits.maxWidth` before applying any resize) now caps the auto-grow at the intended column budget without touching `resize_on_item_update` itself or any other `ItemGridController` consumer. Verified with 24 real chat styles: was one ~1300px-wide single row, now wraps correctly into a 6-column × 4-row grid. Not yet investigated: whether other `ItemGridController` users elsewhere in the codebase that combine `resize_on_item_update="true"` with a manually-assigned fixed `.width` hit the same defect (this fix only pins `maxWidth` for the chat-style selector's own grid instance). A separate, still-open gap found during this same investigation: the per-style bitmap assigned to each cloned grid-cell's `bubble_preview` (`IBitmapWrapperWindow.bitmap`) renders as a plain grey placeholder instead of the style's actual artwork — confirmed the assigned `ImageBitmap` data itself is correct and distinct per style (spot-checked all 89), so the gap is somewhere in `WindowComposite.compositeBitmapWrapper()`'s draw path or its per-window raster cache for freshly-cloned template windows specifically, not yet root-caused.
    7. **`App.ts`'s canvas `mousedown` handler never called `event.preventDefault()`, so every click silently lost keyboard focus a moment after being set** — this was the actual reason typing produced nothing after clicking the chat input, and bug 5's reordering fix (needed on its own merits) didn't touch it. `<canvas>` isn't natively focusable; a browser's default action for an unhandled `mousedown` event blurs whatever currently holds focus (or, if the target itself is focusable, focuses that instead) once every JS listener for the event has finished running — and that default action fires *after* this port's own window-system dispatch, which synchronously calls `TextFieldController.focus()` → `HTMLInputElement.focus()` on the chat input's hidden proxy `<input>`. Without `preventDefault()`, the browser's default action ran right after and silently reverted `document.activeElement` back to `<body>` — the drawn caret still appeared (it's driven by this port's own internal focus flag, set successfully before the revert) so focus *looked* like it worked, but no keystroke ever reached the hidden input again. Reproduced and confirmed in a minimal, app-independent Playwright test (a bare `<canvas>` + hidden `<input>`, `.focus()` called from a `mousedown` listener): `document.activeElement` reverted to `<body>`/canvas without `preventDefault()`, stayed on the input with it. Fixed by calling `e.preventDefault()` at the top of `_onMouseDown`. This is a `vortex-client`-only fix (canvas input handling has no AS3 equivalent — Flash's stage focus model doesn't have this DOM-specific quirk) and affects every text field in the app, not just chat, though chat input was the only one exercised enough during testing to surface it.
    8. **`TextFieldController`'s native `keydown`/`keyup` handlers computed a bogus `charCode` for every non-printable key** — `e.key?.charCodeAt(0)` is correct for printable characters (`e.key` *is* the typed character, e.g. `'a'`/`' '`), but for control keys `e.key` is a multi-character name (`'Enter'`, `'Backspace'`, `'Tab'`, ...), so `.charCodeAt(0)` silently returned the first *letter's* code instead of the intended control-character value — `'Enter'.charCodeAt(0)` is 69 (`'E'`), not 13. `RoomChatInputView.onKeyDown()`'s `event.charCode === 13` send-on-Enter check (and the `=== 32`/space check, which happened to work by coincidence since `' '` is itself length-1) therefore never matched Enter, so typing worked (confirmed after bug 7's fix) but pressing Enter did nothing. Added `TextFieldController.resolveCharCode()`: uses `e.key.charCodeAt(0)` when `e.key.length === 1` (printable), falls back to the legacy `e.keyCode` otherwise (still carries the expected values for control keys: Enter=13, Backspace=8, Tab=9, ...). Affects every text field's Enter/Backspace/Tab/Escape handling app-wide, not just chat.
    9. **Chat bubbles never appeared for real messages despite rendering correctly in isolated testing** — `ChatEventHandler.onRoomChat()` (`habbo/freeflowchat/data/`) had `const userLocation = null;` as an explicit, documented `TODO(AS3)` stub ("when `IRoomEngine.getRoomObject` is available") — it already was, this just hadn't been wired up. Every bubble's `PooledChatBubble.userScreenPos` getter early-returns `{x: 0, y: 0}` when `item.userLocation` is `null` (`if(!item.userLocation) return {x: 0, y: 0};`), so `ChatFlowStage.insertBubble()` always anchored every bubble at the room canvas's top-left corner instead of above the speaker — invisible/nonsensical in practice, not just visually off. This gap was invisible to this feature's own manual verification (bugs 1-4 above) because those tests called `PooledChatBubble.warpTo()` directly with hand-picked coordinates, bypassing `ChatFlowStage`'s positioning path entirely — the position pipeline itself was never actually exercised until a real server round-trip. Fixed by porting the real AS3 lookup from `ChatEventHandler.as::onRoomChat()` (previously not read closely enough for this method): `roomEngine.getRoomObject(event.session.roomId, event.userId, 100).getLocation()` (note: AS3 passes `event.userId` directly as the object id here, not `userData.roomObjectId` — different from the same lookup elsewhere in `ChatBubbleFactory`). Also ported the adjacent `sessionDataManager.isBlocked(senderData.webID)` early-return from the same AS3 method, silently dropped in the original port along with the location lookup.
    10. **`ChatFlowStage` never ticked** — found immediately after bug 9 while re-checking the whole per-frame update path: unlike `ChatFlowViewer` (whose constructor calls `chatFlow.registerUpdateReceiver(this, 1)`), the ported `ChatFlowStage` constructor never called `registerUpdateReceiver` at all, so its `update()` (upward scroll + off-screen `readyToRecycle` cleanup) was dead code, never invoked by anything. Confirmed against real AS3 (`ChatFlowStage.as::ChatFlowStage()`, which does call `registerUpdateReceiver(this, 2)` — a *different* priority than `ChatFlowViewer`'s 1, i.e. two independently-registered receivers, not one delegating to the other, matching what `ChatFlowViewer.as::update()` shows: it never calls into `ChatFlowStage` either). Added the missing `registerUpdateReceiver(this, 2)` call (plus the matching `removeUpdateReceiver` in `dispose()`, mirroring `ChatFlowViewer`'s own pattern). Doesn't affect a bubble's initial appearance (that path was already fine), but without it bubbles would never scroll upward or ever get recycled/removed once spawned.
    11. **`HabboFreeFlowChat.getScreenPointFromRoomLocation()`'s earlier port dropped AS3's stage-recentring term, producing wildly off-screen bubble positions** (e.g. x=-534 for a speaker standing at a perfectly normal, on-screen tile). `RoomGeometry.getScreenPoint()` returns a point relative to the isometric camera's own centre, not the canvas's top-left corner — real AS3 (`HabboFreeFlowChat.as::getScreenPointFromRoomLocation()`) recentres it via `(stage.stageWidth * canvasScale / 2) + rawPoint.x * canvasScale + offset.x` (same for Y/stageHeight) before returning. An earlier pass through this method (this session, before the bug was found) deliberately dropped that term, reasoning "`ChatFlowViewer.rootDisplayObject` already shares the room's own coordinate space, so `getScreenPoint()` + `offset` alone (proven correct by the legacy `ChatWidgetHandler.ts`) should apply directly" — wrong on both counts: (a) `ChatWidgetHandler`'s *own* working code recentres too, just differently (`RoomChatWidget.ts:640`: `item.x = x + this._container.width/2 + this._container.x`), missed on the earlier read; (b) bug 12 below shows `rootDisplayObject` isn't even part of the room's coordinate space at all. Root-caused via a live diagnostic harness the user ran in DevTools (patched `ChatFlowStage.insertBubble()`/`ChatEventHandler` in the running app to log every intermediate value for a real chat message) — confirmed `getScreenPoint()` was returning small, sane numbers (e.g. `{x: -64, y: -28}` for a speaker at room tile `(3, 5, 0)`) that were never being recentred onto the visible viewport. Restored the full AS3 formula, using `window.innerWidth`/`innerHeight` as the `stage.stageWidth`/`stageHeight` stand-in (same convention `PooledChatBubble.ts` already uses elsewhere in this feature, since this port has one shared PixiJS canvas rather than AS3's per-room Flash sub-stages).
    12. **`ChatFlowViewer.rootDisplayObject` (the container every chat bubble lives in) was never actually added to the PixiJS stage** — the single biggest gap, found via a second live diagnostic (walking `rootDisplayObject`'s parent chain up to `application.stage`, and dumping `stage.children`): the chain terminated one level short of `stage`, and `stage.children` had exactly one entry, the room's own `RoomRenderingCanvas` — the bubble container was parented only within the window system's own bookkeeping tree (built for `WindowComposite`'s Canvas2D compositing pass, a *separate* rendering path from the WebGL/PixiJS stage; see `DisplayObjectWrapperController.ts`). `RoomUI.ts`'s existing `getChatContainer()?.setDisplayObject(...)` call (Phase 2) only registers the reference so `WindowComposite` can punch a transparent hole in its Canvas2D UI layer at that screen rect — a hole reveals whatever's *already rendering* on the PixiJS stage underneath it, and since `rootDisplayObject` was never a stage descendant, there was nothing there to reveal. No AS3 equivalent (Flash has one unified display tree, no Canvas2D/WebGL split), so this is TS-port-only infrastructure that was simply never finished: added `IRoomEngine.addStageChild()`/`removeStageChild()` (mounts/unmounts a display object directly on the PixiJS stage, above every room rendering canvas already added — the same `_pixiStage` reference `RoomRenderingCanvas` mounting already uses) and call it from `HabboFreeFlowChat.roomEntered()`/`roomLeft()`.
    13. **`ChatFlowStage`'s upward scroll used an invented continuous-creep model instead of AS3's real discrete-jump timer, so simultaneous bubbles visually piled up without appearing to move** — once bugs 9-12 above got bubbles actually appearing on screen, the user reported them "stacking" instead of scrolling away. The Plan-1 placeholder computed a `pixels/ms` rate (`MOVE_UP_AMOUNT_PIXELS / 10000`) and applied a tiny fractional move every frame — real AS3 (`ChatFlowStage.as::update()`) instead accumulates elapsed time and, once `MOVE_UP_TIMER_DEFAULT` (10000ms — the only value this port ever sees, since `roomChatSettings.scrollSpeed`, which would otherwise pick 3000/6000/12000ms, is a documented always-null `TODO(AS3)` stub) has passed, jumps every bubble up by the full `MOVE_UP_AMOUNT_PIXELS` (19px) at once via `scrollUp()`, then resets the timer. At ~0.0019px/frame the old continuous version was movement well below the threshold of human perception, so bubbles read as permanently stuck even though they were technically inching upward. Rewrote `update()` to match the real timer-accumulate-then-jump model (still without AS3's gravity/collision-avoidance physics, which stays deferred to "Plan 2" per this section's header) — `bubble.moveTo()`'s existing 150ms tween smooths each 19px jump into a visible but non-jarring motion, matching what a real Habbo client shows.
  - Verified end-to-end via a headless-Chromium harness driving the actual running app (no live game server available for a full login→room→chat test): all 89 style-preview thumbnails render as real, visually distinct images, and a fully composited bubble (9-slice background + pointer + bold username + italic multi-line word-wrapped message body) renders correctly across multiple different styles.
- ✅ **"Plan 2" — real `ChatFlowStage` physics + avatar face-crop** — after Plan 1 shipped, the user reported (live, against a real server) that simultaneous speakers' bubbles overlapped messily instead of stacking like real Habbo, and that bubbles showed no avatar face; both were the two gaps explicitly deferred above, and were ported in full rather than patched around:
  - **`ChatBubbleSimulationEntity`/`ChatBubbleSimulationWithLimitedWideRect`/`ChatBubbleCollisionEvent`/`ChatFlowGravity`** (new, `viewer/simulation/`): full port of AS3's per-bubble physics state (`visualRect`/`wideRect` AABBs, horizontal/vertical impulse accumulation with negative-feedback smoothing on `x`), the pairwise collision wrapper, and the gravity/attraction helper. `ChatFlowGravity`'s real name was only recoverable from the **tertiary** source tree (`PRODUCTION-201601012205-226667486`) — obfuscated as `_SafeCls_2911`/`"_-oC"` in the primary tree and left as the generic, still-unrecovered `class_3255` in the secondary tree, so per project rules on recovering real identifiers the tertiary tree was used instead of inventing a name.
  - **`ChatFlowStage`** (`viewer/simulation/`, rewritten): replaced the Plan-1 placeholder with the real algorithm — `insertBubble()` relaxes a new bubble toward its speaker's existing bubbles via up to 10 rounds of `ChatFlowGravity` pull before clamping it back within 15px of the speaker; every `update()` tick runs `simulate()`, up to 20 rounds of pairwise AABB intersection + impulse resolution (sideways nudge if bubbles overlap ≤15px horizontally, otherwise a vertical push separating whichever bubble is older/lower); `scrollUp()` still jumps every bubble `MOVE_UP_AMOUNT_PIXELS` (19px) every scroll interval as before, now followed by a gravity re-centring pass and a `simulate()` call. Line-by-line mode (spacer bubbles, vertical-only resolution) is ported for fidelity and reachable once a real account-preferences message sets `chatMode=1` (see bug 14 below) — defaults to free-flow (mode 0) until then. `PooledChatBubble.scrolledUserPositionX` (new getter) was needed to support the entities' per-frame speaker-tracking (`syncToUserScreenPosition()`); confirmed several of the class's declared constants (`MINIMUM_COLLIDER_WIDTH`, `ChatBubbleSimulationWithLimitedWideRect.WIDERECT_WIDTH`, `ChatFlowGravity`'s three `INPUT_GRAVITY_*` constants) are dead in the AS3 source itself — declared but every call site hardcodes the matching literal instead of referencing them — and ported that exact literal-vs-symbolic split rather than "cleaning it up".
  - **`ChatBubbleFactory.getUserImage()`** (`viewer/`, implemented — no longer a stub): builds the bubble's avatar head-crop via `avatarRenderManager.createAvatarImage(figureString, zoom.enabled ? 'h' : 'sh', ...)` + the already-fully-ported `HabboFaceFocuser.focusUserFace()` (turned out to need only wiring, not new infrastructure — both `HabboFaceFocuser` and a `Texture`→`ImageBitmap` path already existed from unrelated earlier work, contrary to the stale "neither exists yet" note this replaces). Required exposing `avatarRenderManager` and `getBoolean()` on `IHabboFreeFlowChat` (mirroring the existing `HabboToolbar` DI pattern) since `ChatBubbleFactory` only ever holds the interface, not the concrete `HabboFreeFlowChat`.
  - **Bug 15 (found 2026-07-11, same live user report as bug 14: face crop still not rendering after the wiring above): `AvatarImage.getImage()`/`getCroppedImage()` silently ignored their own `scale` parameter — a genuine, pre-existing gap in `habbo/avatar/`, unrelated to freeflowchat, just never exercised by any caller with `scale !== 1` before `HabboFaceFocuser` became reachable.** A three-round live diagnostic (console logging → per-field sprite/texture inspection → visually rendering the actual bitmaps to an on-page canvas overlay, since the numeric checks all came back valid) showed the full avatar render was correct but the face crop returned a fully transparent 25×25 bitmap — the crop rectangle was landing in empty margin left of the head, not on it. Root cause, confirmed by reading `AvatarImage.as::getImage()` line-by-line: real AS3 calls `class_2495.resampleBitmapData(bitmap, scale)` on the composited canvas whenever `scale != 1` *before* handing it back — `HabboFaceFocuser.focusUserFace()`'s `X_OFFSETS`/`Y_OFFSETS` crop coordinates (already correctly ported, confirmed byte-for-byte against AS3) are calibrated assuming that resampling already happened, i.e. that a `scale=0.5` call returns a canvas *half the pixel size* of an unscaled one. This port's `getImage()` (and the sibling `getCroppedImage()`, same bug) never applied any resampling at all, so `zoom.enabled=true` sessions (`"h"`/large avatar scale + 0.5x icon-crop multiplier) got back a full-size, unshrunk canvas and cropped the wrong region — invisible/transparent result, not a crash, so nothing else caught it. Ported the real resample algorithm as `AvatarImage.resampleCanvas()`/`resizeCanvas()` (progressive halving below 0.5x scale for quality, matching `resampleBitmapData()`/`resizeBitmapData()` exactly) and wired it into both methods, including AS3's specific quirk of caching the *unscaled* bitmap and only resampling the returned copy (so a later same-pose call at a different scale still recomputes correctly). The containing AS3 class's real name is unrecoverable from any of the three source trees (obfuscated `_SafeCls_2871` in the primary tree, generic decompiler name `class_2495` in the secondary tree, absent from the tertiary tree) — the method names themselves are real/legible, only the wrapper class isn't, so `resampleCanvas`/`resizeCanvas` are private statics on `AvatarImage` rather than a wrongly-named standalone class. Independently corroborated by ESLint: `pnpm lint` had *already* been flagging `scale` as an unused parameter on both methods before this fix (a real, live lint error, not just a stylistic warning) — confirms the gap was there prior to this session, not something introduced by the chat-bubble wiring.
  - **`getPetImage()` stays a stub, now for a precisely-scoped reason** (not the vague placeholder it replaces): `IRoomEngine.getPetImage()` doesn't exist and there is no pet-avatar sprite-composition code anywhere in `habbo/room/` — unlike the user-avatar case, this isn't a wiring gap but a missing subsystem comparable in scope to `AvatarRenderManager` itself, out of scope for this pass; documented `TODO(AS3)` in-code with the exact blocking dependency chain, cross-referenced against the identical gap already noted in `ChatWidgetHandler.ts`.
  - **Bug 14 (found 2026-07-11, live user report: "remonte pas assez vite" - scrolls up noticeably too slowly): `roomChatSettings` was permanently `null`, capping the scroll interval at `ChatFlowStage`'s 10000ms fallback instead of AS3's real ~6000ms default.** Root-caused by reading `HabboFreeFlowChat.as`'s actual constructor: it calls `refreshEffectiveChatSettings()` immediately (`_chatMode=0, _chatBubbleWidth=1, scrollSpeed=1, floodSensitivity=1` field defaults), so AS3's `roomChatSettings` is *never* null - `ChatFlowStage.refreshSettings()` always finds it non-null and resolves `scrollSpeed=1` to the 6000ms tier from the very first frame, well before any server message arrives. This port's `HabboFreeFlowChat` never did that self-initialization, leaving `roomChatSettings` null forever and `ChatFlowStage` stuck on its documented-as-unreachable 10000ms fallback - a real, measurable (67% slower) behavioral gap, not just a cosmetic one. Fixed: `HabboFreeFlowChat`'s constructor now calls a new `refreshEffectiveChatSettings()` (matching AS3's own method + defaults exactly) to self-initialize `roomChatSettings`; also wired the real `onAccountPreferences()` handler (`AccountPreferencesEvent`, message 2082) so a genuine server preference update still overrides the default afterward. This surfaced a second, independent bug in the process: `AccountPreferencesParser.ts` stopped reading 11 fields early (after `preferedChatStyle`), silently dropping `chatMode`/`chatBubbleWidth`/`chatScrollSpeed`/`chatSizePreference` (plus `wiredMenuButton`/`wiredInspectButton`/`playTestMode`/`wiredWhisperDisabled`/`showAllNotifications`/`wiredUiStyle`) even though the real AS3 parser (recovered from `src/unknowns/_SafePkg_1927/_SafeCls_1926.as` - genuinely part of the client despite the path, directly imported by `HabboFreeFlowChat.as`/`SessionDataManager.as`, unlike the rest of `src/unknowns/`) reads all of them, the last six behind `bytesAvailable > 0` guards for older/shorter server builds. Rewrote the parser to match field-for-field. `onRoomChatSettings()`/`onGuestRoomData()` (AS3's two other `refreshEffectiveChatSettings()` call sites, both only ever touching `floodSensitivity`) are deliberately not wired - `floodSensitivity` isn't exposed on `IRoomChatSettings` and nothing in this port reads it yet, documented `TODO(AS3)` in both `HabboFreeFlowChat.ts` and `IHabboFreeFlowChat.ts`.
  - **Deferred, still `TODO(AS3)`**: the chat-history drag-down tray (`ChatHistoryTray`/`ChatHistoryScrollView` — a distinct feature AS3 bundles in the same container; its toggle button stays a no-op) and pet-avatar chat-bubble faces (`getPetImage()`, above).
  - **Bug 16 (found 2026-07-20, live user report: picking a new bubble style works for the session but never sticks across relogin): `preferedChatStyle`/`chatFontSizeMode`'s setters never persisted the choice server-side, only the composer to do so was missing.** `ChatInputWidgetHandler.ts` already resolves and remembers a style change in-memory for the current session (`freeFlowChat.preferedChatStyle = chatMessage.styleId`, confirmed already wired), and `onAccountPreferences()` already reads `parser.preferedChatStyle` back on login — the two setters just had their real AS3 send commented out behind a `TODO: Send SetChatStylePreferenceComposer when composer is implemented`. Recovered the composer from the primary source (`_SafePkg_2091/_SafeCls_2257.as`, header 2634 in `_SafeCls_2046.as`) — a 2-int `(preferedChatStyle, chatFontSizeMode)` composer, the 2026-era 2-arg evolution of the older 1-arg form the stale comment referenced — added `SetChatStylePreferenceComposer.ts` (`outgoing/preferences/`) and wired both setters to send it with the current value of both fields, matching AS3's `set preferedChatStyle()`/`set chatFontSizeMode()` exactly (both setters send the same composer). `isDisabledInPreferences`'s separate `SetChatPreferencesMessageComposer` gap is unrelated and stays deferred.
- ✅ **Custom Volter glyph rasterizer for chat bubbles** (`core/text/rasterizer/`, new — `ttfOutline.ts`/`outlineFlatten.ts`/`distanceRaster.ts`/`glyphRasterizer.ts`/`flashTextDraw.ts`/`FontOutlineRegistry.ts`): `ChatTextLayout.ts`'s `layoutChatText()`/`drawChatText()` measured and drew chat text via the browser's native `ctx.font`/`ctx.measureText()`/`ctx.fillText()`, which (a) never reproduced Flash's actual text rasterizer and (b) was silently falling back to a generic system font — `App.ts::loadWebFonts()` registers the `FontFace` under family `"Volter (Goldfish)"`, but `ChatStyleLibrary.as`'s real default (`"Volter"`, no suffix — verified byte-for-byte at `ChatStyleLibrary.as:114` in the primary `WIN63-202607011411-782849652` source, confirmed the current AS3-authoritative build) never matched it, so the CSS font-family lookup silently skipped straight to `Arial, sans-serif`. Not an AS3-portable fix (Flash's embedded-font rasterization is native Player engine code, no AS3 source exists for it) — instead, a from-scratch TrueType outline parser + polygon rasterizer reads the project's existing bundled `Volter.ttf`/`Volter Bold.ttf` directly and draws real glyph shapes via `putImageData()`, replacing both measurement and drawing so they can't disagree with each other the way two independent native-font calls could. Deliberately scoped to Volter/`anti-alias-type: normal` only (matches TextStyleManager's real Habbo CSS for every `chat_*` style) — Ubuntu/`anti-alias-type: advanced` text in `WindowComposite.ts` (window captions, buttons, dialogs) still renders via native `ctx.font` and is unaffected by this pass. Falls back to the native path automatically if `FontOutlineRegistry` hasn't registered Volter yet. Researched (read-only, never installed) `iSetht/truffle-text` — a same-day-published, unaudited npm package claiming pixel-perfect Habbo text via ~3MB of reverse-engineered Adobe-AIR calibration data — for its general rasterization *technique* (grid-fit hinting + signed-distance-field density curves); deliberately did not depend on the package, its calibration blobs, its bundled fonts, or its per-glyph magic-number correction tables, since none of that is independently verifiable. Result is a reasoned approximation of Flash's glyph rendering, not a pixel-perfect one — full grid-fit/CSM density-curve rendering for Ubuntu's `advanced` AA styles (`WindowComposite.ts`) is unimplemented follow-up work.
- 🐛 **Full-repo lint cleanup (2026-07-19), surfaced by an unrelated rename.** Renaming `helium-engine`/`helium-client` → `vortex-engine`/`vortex-client` made `git mv` mark every file in both packages as renamed, which made the pre-commit hook's `lint-staged` glob match the entire codebase instead of just genuinely-changed lines — running `eslint` repo-wide for the first time via that gate and surfacing 347 pre-existing errors, none related to the rename itself. Fixing them found several real gaps, not just style:
  - **`GameClickTarget.ts`'s invisible game-interaction hit target never actually rendered anything** — AS3's `update()` lazily creates `new BitmapData(WIDTH, HEIGHT, true, 0)` (a transparent 46×60 placeholder) and assigns it as the sprite's `asset` before setting `alphaTolerance = MATCH_ALL_PIXELS`; the port set the offsets/tolerance but never created or assigned any texture, so `IRoomObjectSprite.texture` (which also drives `width`/`height`, both read-only) stayed `null` and the click target had no hit-testable bounds at all. Fixed by lazily building an equivalent transparent `Texture` via `OffscreenCanvas` + `Texture.from()`, matching the existing `BlankStyle.ts`/`PooledChatBubble.ts` idiom for placeholder textures in this port.
  - **`FurnitureRoomBrandingVisualization.ts` computed `shouldScale`/`effectiveSize` (whether/what to downscale a branded billboard image at icon zoom) and then never used either** — AS3's `checkAndCreateImageForCurrentState()` halves the source bitmap via a 0.5-scale `Matrix` draw when rendering at icon size (32) unless the image URL contains `"noscale"`, and uses the *resulting* (possibly-halved) bitmap's own dimensions for the flip-state offsets; the port always used the full-size texture and the original (never-downscaled) dimensions for offsets. Fixed by porting the downscale via `OffscreenCanvas`+`drawImage` (reading the source `Texture`'s underlying `CanvasImageSource` the same way `ExtendedSprite.ts` already does) and computing offsets from the effective post-scale dimensions, matching AS3's `_loc2_.width`/`_loc2_.height`.
  - **`Exception.ts` silently dropped its own `id` constructor parameter** — AS3's native `Error(message, id)` exposes `id` as `error.errorID`, which real AS3 code reads back out when reporting a caught exception's category (`CoreComponentContext.as`, multiple `error(..., e.errorID, e)` call sites); a JS `Error` has no such slot, and this port's `Exception` subclass (which *could* carry it) never stored it, so `CoreComponentContext.ts` hardcoded `-1` for that category with a comment claiming "a JS Error has no equivalent" — true only for the native case, not for this port's own `Exception`. Added an `id` getter to `Exception` and wired both `CoreComponentContext.ts` call sites to use `e instanceof Exception ? e.id : -1`.
  - **`TextLabelController.ts::refresh()` never dispatched a synthetic `WE_RESIZED`** when a text-content change left the label the same size it already was (so the normal resize-triggered dispatch never fires) — AS3 explicitly re-dispatches in that case unless the refresh was itself resize-triggered, and the sibling `TextController.ts::refresh()` already has this exact logic; `TextLabelController` was just missing it. Ported directly from the sibling implementation.
  - **`ICoreWindowManager.notify()`/`confirm()`, `HabboNavigator`/`LegacyNavigator`'s `getButton()`/`refreshButton()`, `CallForHelpManager.reportSelfie()`/`reportPhoto()`, `GuideHelpManager.createHelpRequest()`, `HabboHelp.showWelcomeScreen()`** — confirmed pre-existing, already-documented-elsewhere stub/unwired states (no `ICoreWindowManager` implementer exists yet; the `help/` subsystem classes track fields but never send composer messages), not new gaps from this pass. Left as `TODO(AS3)`-commented stubs with unused params prefixed `_` rather than implemented, since each needs its own scoped investigation (composer wire formats, `WindowContext`-style completion-callback wiring) beyond a lint pass's scope.
  - **ESLint config gaps** (as opposed to code bugs): `@typescript-eslint/naming-convention`'s `method` selector had no `leadingUnderscore` allowance for private/protected members, so the ~90 bound event-handler methods written as arrow-function class fields (`private _onClick = (e) => {...}`, mixed with ~259 handlers *without* the underscore — both patterns are genuine, pre-existing convention, not a violation) all failed; and there was no exception for AS3 obfuscated placeholders (`_SafeStr_N`) or verbatim-AS3 snake_case field names (`DefaultAttStruct.width_min` etc.), which must never be renamed per the traceability mandate. Fixed the rule config directly (`eslint.config.mjs`) instead of papering over each site with per-line `eslint-disable` comments (several of which already existed and are now removed as dead).
  - Everything else (~275 of the ~347 errors) was mechanical: unused-but-intentional stub parameters prefixed `_` (matching this file's own established convention), `Function`-typed callbacks replaced with concrete signatures where one real call site existed or `(...args: any[]) => any` where the callback registry is genuinely polymorphic (`IWindow.addEventListener`, matching AS3's own untyped `Function`), inline `import('./X').Y` type annotations converted to top-level `import type`, and dead `let x = 0` initializers removed where every code path reassigns before the first read (verified via TypeScript's definite-assignment checking, not just visual inspection — two cases in `PlaneMaterialCellColumn.ts`/`PlaneMaterialCellMatrix.ts` turned out to be AS3 itself computing an overflow-adjusted offset that's then unconditionally discarded before its only use, confirmed byte-for-byte against AS3 rather than "improved").
  - Full repo: `pnpm build` (tsc + vite) and `pnpm lint` both clean (0 errors, 295 pre-existing `no-explicit-any` warnings untouched).
- 🐛 **Wheel scrolling jumped straight to the top or the bottom, never in between (found 2026-07-29, live user report: same symptom in the client's windows and in vortex-glaze).** Two independent causes, one per code path.
  - **The DOM's `deltaY` was fed in as if it were Flash's `MouseEvent.delta`.** Flash reports the wheel in *lines* (3 per notch on Windows) and every consumer here is built on that unit — `SmoothScroller` multiplies a line by `DEFAULT_SCROLL_STEP` (25px), `ScrollBarController` maps one to 0.06 of its 0..1 offset. The DOM reports pixels (~100 per notch, `deltaMode` 0), so `App.ts::_onWheel()`/`CanvasSurface.ts::_onWheel()` passing `-e.deltaY` made one notch worth 100 lines = 2500px of scroll intent; on a 500px-of-travel list that overshoots `maxScrollV` 5× and clamps to 0 or 1. Measured against the compiled `SmoothScroller`: one notch moved the full 500px before, 75px after. Added `core/window/utils/NativeWheelDelta.ts` (no AS3 counterpart — AS3 never needed one) converting pixel/line/page deltas to Flash lines at 3 per notch, so the ported chain reproduces the original client's feel rather than an invented one, and both entry points now use it. Trackpads, which emit many small pixel deltas instead of notches, come out proportionally small and scroll smoothly.
  - **`ScrollBarController`'s own wheel handling was still the pre-2026 fixed-step jump.** The port traced to `win63_version`, where both wheel branches (`update()` and `scrollButtonEventProc()`) test `delta > 0` and add/subtract a flat `_scrollStep` (0.1 = 10% of the range) with no animation — so with the cursor over the scrollbar itself, every wheel event was a 10% teleport regardless of magnitude. The primary tree replaced this with a `SmoothScroller` (`new SmoothScroller(getScrollOffset, setSmoothScrollOffset, getSmoothScrollAxisSize, 200, 60, true, null, 15)`), both branches calling `scrollWithWheel(delta)`, plus `setScrollPosition()`'s third `fromSmoothScroller` parameter feeding external position changes into `adjustStartPosition()`. Ported all of it. Kept AS3's oddity verbatim: `getSmoothScrollAxisSize()` is a bare `25 / _scrollStep` even though the scroller is constructed with a step of 15, so one wheel line moves 0.06 of the offset, not `_scrollStep` — documented at the declaration rather than "corrected". Also ported the `scrollable` setter's missing `setScrollPosition(scrollH/scrollV, false)` (without it `_offset` stays 0 while the target is scrolled, and `_offset` is where the smooth scroller starts from).
  - **Known remaining divergence, deliberately not changed**: the primary tree's increment/decrement buttons and track paging use a literal `SCROLL_STEP_SIZE` (15) where this port (following `win63_version`) uses the target's own `scrollStepH`/`scrollStepV`. Unrelated to the wheel and affects a different interaction, so it's flagged rather than folded into this fix.
- 🐛 **`HabboMessages.ts` had `GetBonusRareInfoMessageComposer` registered at wire header 957** (found via a real emulator log: `GiveSupplementToPetMessageParser` repeatedly throwing `"Not enough data: need 4, remaining 0"` for packet 957). Ground truth (`vortex-client/HabboMessages.as`, `sources/win63_version/habbo/communication/class_1881.as`, and the emulator's `Turbo.Revisions/Revision20260112/Headers.cs`) all agree 957 is `GiveSupplementToPetMessageComposer`'s header and `GetBonusRareInfoMessageComposer`'s real header is 75. Since `GetBonusRareInfoMessageComposer` sends an empty body and fires automatically from `BonusRarePromoWidget.ts` on every landing-view load, every such load sent an empty packet tagged 957, which the server routed to the pet-supplement parser expecting two ints. Fixed the header (957 → 75) and ported the previously entirely-missing `GiveSupplementToPetMessageComposer.ts` (`petId`, `supplementType` — AS3 only exists in `sources/win63_version/habbo/communication/messages/outgoing/room/engine/`, not the primary tree), registered at 957. No UI calls it yet — giving pets supplements isn't wired up anywhere in the client (no `PetSupplementEnum` port, no pet-care UI action) — this was a protocol-registry-only fix, not a new feature.

---

## Validation

This documentation update is based on read-only filesystem counts. No TypeScript build was run for this docs-only change.

To refresh the snapshot manually:

```powershell
(Get-ChildItem sources\WIN63-202607011411-782849652 -Recurse -Filter *.as -File).Count
(Get-ChildItem sources\win63_version -Recurse -Filter *.as -File).Count
(Get-ChildItem sources\PRODUCTION-201601012205-226667486 -Recurse -Filter *.as -File).Count
(Get-ChildItem packages\vortex-engine\src -Recurse -Filter *.ts -File).Count
(Get-ChildItem packages\vortex-client\src -Recurse -Filter *.ts -File).Count
```

